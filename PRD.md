# postpilot PRD

사진과 거친 메모를 넣으면 각자의 문체로 블로그 초안을 만들고, 플랫폼별 형식으로
복사하거나 연결된 로컬 기기를 통해 네이버 블로그에 발행할 수 있게 하는 개인용 도구.

|           |                    |
| --------- | ------------------ |
| 버전      | 1.4                |
| 사용자    | 2명 (본인, 배우자) |
| 기술 스택 | 확정 — §6          |

> 1.4에서 바뀐 것: 사이트 UI를 한국어·영어로 제공하고, 새 글마다 현재 UI 언어를 초기값으로 삼되
> 독립적으로 바꿀 수 있는 생성 대상 언어를 추가했다. UI locale, 다음 전체 생성의 `target_language`, 현재
> 정본의 `content_language`를 분리해 화면 언어 전환이나 대상 언어 변경이 기존 글을 암묵적으로 번역하지
> 않게 했다. 각 말투는 기준 언어 하나를 가지며, 다른 언어 생성에는 언어 중립 특성만 투영하고 다른 언어의
> 글은 그 말투 학습에 섞지 않는다. 1.3에서는 실제 사용에서 네이버 사진 배치가 여전히 가장 큰 수작업으로 남아, 사용자가 명시적으로
> 요청한 확정 글을 계정별로 연결된 Mac의 결정적 로컬 네이버 발행기가 최종 발행하도록 §3.1·F-10을 개정했다. 플랫폼
> 로그인은 VPS가 아니라 로컬 기기에만 남고 기존 복사 내보내기는 유지된다. 1.2에서는 계정당 하나였던 문체 프로필을 계정 안의 여러 독립 `말투`로 확장하고,
> 모든 글이 하나의 말투를 명시적으로 선택하도록 §3.4·F-2~F-9·§5를 개정했다. 1.0→1.1에서는
> 스택을 확정하고 §3.5 생성 진행과 §3.2 블록 표현을 추가했으며 문서·리포 이름을 postpilot으로
> 통일했다.

---

## 1. 목표와 비목표

### 문제

블로그 글 한 편 쓰는 데 30분이 걸린다. 사진을 고르고 순서를 정하고 문장을 다듬는
과정이 대부분이다. 경험 자체를 기록하는 데 드는 시간은 그중 3분이다.

### 목표

- 사진과 메모만 던지면 발행 직전 상태의 초안이 나온다.
- 초안이 **내 문체**로 나온다. 일반적인 AI 문체가 아니라.
- 두 사용자가 서로의 말투를 볼 수 없고, 각 사용자는 용도에 맞는 여러 말투를 서로 섞지 않고 쓴다.
- 네이버 · 티스토리 · 자체 사이트에 각각 맞는 형식으로 바로 복사할 수 있다.
- 확정한 네이버 글은 휴대폰에서 명시적으로 요청하면 연결된 Mac이 사진 배치와 최종 발행까지 끝낸다.
- 사이트 UI를 한국어와 영어로 사용할 수 있다.
- 글마다 한국어·영어 중 생성 언어를 고르며, 현재 UI 언어를 초기값으로 쓰되 글의 언어는 독립적으로 유지한다.

### 비목표

- 협업 편집, 댓글, 리뷰 승인 플로우
- SEO 최적화, 키워드 분석, 발행 스케줄링
- 네이버 블로그 외 플랫폼의 자동 발행, 인스타그램 릴스·네이버 클립 생성/발행(후속 계획)
- 이미지 생성, 이미지 보정, 얼굴 모자이크 (§8 참조)
- 3인 이상, 조직 계정, 권한 등급
- 기존 글·메모·관찰 결과의 자동 번역, 언어별 URL, locale 선택의 계정 간 동기화
- 한 말투 안의 언어별 하위 프로필. 다른 언어를 학습하려면 그 언어를 기준으로 하는 별도 말투를 만든다.

### 성공 기준

- 초안 완성까지 3분 이내 (모델 응답 시간 포함).
- 10편쯤 쓴 뒤, 초안을 손대지 않고 발행하는 비율이 절반을 넘는다.
- 배우자가 도움 없이 혼자 한 편을 끝낸다.

> 3분 기준은 **어떤 모델을 고르느냐에 종속된다**(§3.3, §6.5). 무료·저사양 모델을
> 고른 세션에서 3분을 넘겼다면 그건 제품 실패가 아니라 그 선택의 결과다. 측정할 땐
> 어떤 모델 조합이었는지를 같이 남긴다.

---

## 2. 사용자 시나리오

**주 시나리오 (S1)**
저녁에 폰으로 접속 → 저장된 선택 또는 브라우저 선호 언어로 UI가 열림 → 새 글의 생성 언어가 그 UI 언어로 시작함 → 필요하면
한국어·영어 중 다른 언어로 변경 → 새 글의 말투를 드롭다운에서 선택 → 오늘 찍은 사진 8장 업로드 → 메모 5줄 입력 → `A/B 비교 생성` → 앱을 닫음 →
목록의 `AI 결과 확인`으로 돌아와 두 후보 중 하나를 적용 → "더 짧게" 한 번 → 글을 `확정` → `네이버에 발행`을 명시적으로 확인 →
Mac의 연결된 브라우저가 글과 사진을 배치하고 최종 발행 → 휴대폰에서 결과 URL 확인. 앱 안에서 문장을 직접 다듬었다면, 사용자가
`말투 학습`까지 명시적으로 선택했을 때만 그 최종본을
다음 글의 문체 근거로 삼는다. 빠르게 한 편만 만들 때는 별도의 `생성`으로 활성 작성 모델 하나만 호출한다.

**보조 시나리오 (S2) — 선택 사항**
과거 글이 있으면 대상 `말투` 안에서 한 편을 붙여넣어 분석할 수 있다. 과거 글이 전혀 없어도 그 말투의 첫 글을 바로 생성하고,
완성한 글을 직접 확정하면서 한 편씩 학습한다. 다른 말투의 글·샘플을 선택하거나 공유하지 않으며, 30편 같은 가입·학습 최소치는
없다.

**보조 시나리오 (S3)**
초안이 계속 존댓말로 나옴 → "반말로 써줘" 입력하면서 "규칙으로 저장" 체크 →
같은 말투를 선택한 다음 글부터 반말로 나옴.

---

## 3. 핵심 설계 결정

구현 전에 합의가 필요한 항목들. 이후 요구사항은 여기서 파생된다.

### 3.1 발행은 명시적이고, 플랫폼 권한은 로컬 기기에만 둔다

네이버는 공식 글쓰기 API가 없으므로 사진 배치와 최종 발행에는 브라우저 자동화가 필요하다. 처음에는
복사로 끝내면 충분하다고 판단했지만, 실제 사용에서는 여러 사진을 찾아 올리고 본문 위치에 맞춰 배치하는
단계가 여전히 가장 번거로운 수작업으로 남았다.

그렇다고 VPS에 네이버 로그인 세션과 브라우저를 두지는 않는다. 해외 VPS의 낯선 네트워크와 장기 보관된
플랫폼 쿠키는 계정 복구·보안·상호작용 모두에 불리하다. 대신 계정별로 연결된 Mac이 아웃바운드로 작업을
가져가고, Mac에만 있는 전용 Chromium 프로필과 버전이 고정된 로컬 발행기가 사람과 같은 편집 화면에서 실행한다.

- 발행은 정확히 확정된 글에서 사용자가 별도 `네이버에 발행`을 확인했을 때만 시작한다. 확정, 학습,
  자동저장, 스케줄 또는 기기 재연결은 발행을 암시하지 않는다.
- VPS는 내구성 있는 작업·고정된 블록/사진 스냅샷·진행 상태만 가진다. 네이버 비밀번호·쿠키·브라우저
  프로필·CDP 주소는 Mac 밖으로 나가지 않는다.
- 최종 발행 직후 결과를 확인하지 못한 작업은 중복 위험 때문에 자동 재시도하지 않는다.
- 서버는 고정된 글 manifest와 설정만 전달한다. selector·JavaScript·화면 좌표·키 입력 순서를 내려보내는
  범용 매크로 프로토콜은 만들지 않으며, 좌표 녹화 대신 로컬에 설치된 버전별 DOM/접근성/CDP 드라이버가
  발행 없는 버전별 호환성 점검으로 계정·단일 탭·편집기 구조·읽기 검증 능력을 확인한다. 알 수 없는 구조에서는
  추측하지 않고 발행 전에 닫힌다.
- 네이버 외 플랫폼은 기존 복사 내보내기를 유지한다. 후속 플랫폼은 공식 게시 API가 있으면 API를 우선한다.

**스택에 미친 영향:** 프론트는 계속 Cloudflare Workers의 순수 정적 SPA다. 새 런타임은 로컬 Mac 동반
에이전트이며, 외부 인바운드 포트를 열지 않고 VPS의 Connect API를 폴링한다(§6.1, plan 12).

### 3.2 정본은 HTML이 아니라 블록 구조다

**네이버 스마트에디터 ONE에는 HTML 소스 입력 모드가 없다.** 붙여넣은 마크업은
버려진다. HTML을 정본으로 삼으면 네이버에는 글을 넣을 방법이 아예 없다.

따라서 생성 결과의 정본은 구조화된 블록 배열이고, 플랫폼별 출력은 거기서
파생시킨다. 이 결정을 뒤집으면 네이버 지원이 불가능해진다.

```
블록 배열 (정본)
  ├─ 네이버    → 평문 + 사진 자리 표시
  ├─ 티스토리  → HTML 조각
  ├─ 자체 사이트 → 완성된 HTML 페이지
  └─ 마크다운  → front matter 포함
```

**표현 방식 — proto 평면 메시지.** 블록은 `proto/postpilot/v1/`에 정의한다.
`type` enum 하나 + 종류별 선택 필드를 **한 메시지에 평면으로** 둔다(oneof 아님).

```proto
enum BlockType { BLOCK_TYPE_UNSPECIFIED = 0; TEXT = 1; HEADING = 2;
                 IMAGE = 3; QUOTE = 4; LIST = 5; }

message Block {
  BlockType type = 1;
  string content = 2;   // text · heading · quote
  int32  level   = 3;   // heading
  string file    = 4;   // image — 실제 첨부 파일명
  string alt     = 5;   // image
  string caption = 6;   // image
  repeated string items = 7;  // list
}
```

- **FE는 생성된 TS 타입을 그대로 받는다** — 스키마가 proto 한 곳에만 산다.
- **저장은 protojson 문자열**로 SQLite TEXT 컬럼에(§5).
- **LLM이 돌려준 JSON이 거의 그대로 언마샬된다** — oneof였다면 protojson의 oneof
  인코딩이 모델이 내는 `{"type":"text"}` 모양과 달라 변환 계층이 하나 더 붙는다.
- 대가: `type=TEXT`인데 `items`가 채워진 것 같은 조합이 타입상 가능하다.
  **역직렬화 직후 검증 함수 하나가 반드시 있어야 한다**(§7).

`text` 블록 하나가 문단 하나다. 모델이 긴 문단을 하나에 몰아넣지 않도록 프롬프트에
명시해야 한다.

### 3.3 생성은 관찰과 작성으로 나누고, 작성 후보 비교는 선택한다

사진 8장과 메모를 한 번에 던지면 모델이 뒤쪽 사진을 뭉뚱그린다. 나눈다:

1. **관찰 단계** — 사진만 보고 장면·분위기·읽히는 텍스트를 구조화해 뽑는다.
   글은 쓰지 않는다.
2. **작성 단계** — 그 관찰 결과 + 메모 + 문체 프로필로 글을 쓴다.

관찰 단계는 정형 출력이라 저렴한 모델로도 충분하다. 문체가 갈리는 건 작성
단계다. **두 단계의 활성 모델을 각각 고른다** — 등록된 프로바이더·모델 목록에서
단계별로(§6.4). 일반 `생성`은 관찰을 최대 한 번 수행하고 활성 작성 모델 하나만 호출해
정본을 만든다. `A/B 비교 생성`을 명시적으로 누른 경우에만 같은 관찰 결과·메모·문체
스냅샷을 서로 다른 작성 모델 두 개에 보내 후보를 만들며, 사용자가 적용한 후보만 정본이 된다.

앱은 버전이 붙은 추천 조합을 제공할 수 있지만 자동으로 적용하지 않는다. 사용자가
`추천 조합 적용`을 눌러야 저장되며, 각 단계와 A/B 쌍은 이후에도 따로 바꿀 수 있다.
관찰·문체 분석 모델의 A/B는 별도 모델 실험 화면에서 같은 사진/글 묶음으로 수행한다.
한 비교에서 여러 단계를 동시에 바꾸지 않는다.

### 3.4 말투 프로필은 계정 안에서 여러 개, 서로 완전히 독립

하나를 두 계정이 공유하면 두 사람 문체의 평균값이 나온다. 한 계정 안에서도 일기와
리뷰처럼 의도적으로 다른 말투를 한 프로필에 넣으면 어느 쪽에도 맞지 않는 평균값이
된다. 따라서 사용자는 `말투`를 여러 개 만들 수 있고, 각 말투는 프로필·샘플·완성 글·
규칙·검증 이력을 서로 공유하거나 상속하지 않는다. 글은 정확히 한 말투에 속하며 생성
전에 드롭다운으로 명시적으로 선택한다. 서버가 제목·태그·플랫폼을 보고 추측하지 않는다.

계정마다 활성 기본 말투 하나를 유지한다. 기존 계정과 글은 마이그레이션에서 `기본 말투`로
귀속한다. 말투 삭제는 soft delete다. 그 말투의 글과 학습 이력은 남고 글에는 삭제된 말투로
표시되지만 새 글이나 AI 작업에는 선택할 수 없다. 복원하거나 글을 활성 말투로 바꿀 수 있다.

한국어 문체는 **종결어미**(~다 / ~해요 / ~습니다)에서 대부분 결정된다. 문체 분석은
이 항목을 최우선으로 다뤄야 한다.

각 말투 프로필은 어휘·종결어미·문장·구조·여섯 성향 축을 가진 버전형 스냅샷이다. 근거가 약한 값은 추측하지 않고 `알 수 없음`으로
표시하며, 사용자의 필드별 직접 설정이 측정·분석보다 우선한다. 해당 말투의 완성 글 한 편부터 예시와 측정 근거로 쓸 수 있다.

### 3.5 생성은 요청이 아니라 작업이다

생성에 30초~2분이 걸린다(§6.6). 이걸 단일 RPC의 응답 대기로 처리하면, 주 시나리오가
"저녁에 폰으로"라 화면이 꺼지거나 셀룰러↔wifi가 전환되는 순간 2분치 작업과 토큰이
그대로 날아간다.

그래서 생성은 **서버에 남는 작업 레코드**다.

```
StartGeneration(post)       → 즉시 job_id 반환, 활성 작성 모델 하나로 생성
StartWriteExperiment(post)  → 즉시 job_id + experiment_id 반환, 두 작성 후보 비교
GetGeneration(job_id)       → { 상태, 단계, 진행률, 실패 사유 }  ← 클라이언트가 폴링
                              일반 생성은 완료 시 Post.content가 채워지고, 비교는 두 후보가 선택 대기한다
```

- 끊겨도 재접속하면 이어진다. 목록에서 그 글을 열면 생성 중 상태가 그대로 보인다.
- 진행률이 **진짜 진행률**이 된다 — "사진 3/8 관찰됨", "작성 중".
- 작업은 워커 고루틴이 소비한다. API 프로세스 안에서 돈다(2인 규모라 별 프로세스
  불필요). 재시작 시 `running` 상태로 남은 작업은 `failed`로 정리한다.
- 폴링 간격은 2초. 완료 후에는 멈춘다.

수정(F-6)도 같은 작업 큐를 탄다. 짧지만 같은 이유로 끊길 수 있다.
말투 학습·규칙 비교·프로필 검증도 명시적 사용자 동작 뒤에만 작업으로 등록된다. 서버 기동, 타이머, 페이지 열기, 복사/내보내기는
이 작업이나 프로바이더 호출을 시작하지 않는다.

### 3.6 UI locale과 글의 언어는 서로 다른 상태다

화면을 어떤 언어로 읽는지와 글을 어떤 언어로 생성하는지는 같은 초기값을 공유할 뿐, 같은 상태가 아니다.
둘을 묶으면 영어 UI로 잠깐 바꾼 것만으로 이미 쓰던 한국어 글의 수정·내보내기 언어가 바뀌는 오류가 생긴다.

- 지원 언어 코드는 `ko`, `en`이다. `ko-KR`은 `ko`, `en-US`는 `en`처럼 브라우저 locale의 base tag를
  정규화한다.
- UI locale 우선순위는 **브라우저에 저장한 명시적 선택 → `navigator.languages`의 첫 지원 언어 → `ko`**다.
  URL과 로그인 세션은 locale 전환으로 바뀌지 않으며, 계정이나 다른 기기로 동기화하지 않는다.
- 새 글의 `target_language`는 생성 당시 UI locale로 시작하지만 사용자가 바꿀 수 있다. 이후 UI locale을
  바꿔도 저장된 글의 target은 변하지 않는다.
- `content_language`는 현재 정본을 만든 마지막 기계 결과의 명시적 언어 계약이다. target을 바꾸거나 직접 편집해도 기존
  정본을 번역·감지·relabel하지 않는다. 다음 명시적 전체 생성 또는 작성 A/B 결과를 적용할 때만 target이 content language가 된다.
- AI 수정은 현재 `content_language`를 유지한다. target과 다른 언어로 바꾸는 경로는 전체 생성/A-B이며,
  자동 번역은 없다.
- 관찰은 사진의 사실을 구조화할 뿐이므로 언어 대상과 독립적이다([I3]). 작성·수정·작성 A/B는 작업을 시작할
  때 언어를 스냅샷에 고정하고 재시도·재접속에서도 바꾸지 않는다([I5]).
- 각 말투는 기준 언어 하나를 가진다. 같은 언어에서는 전체 프로필을 쓰고, 다른 언어에서는 구조·성향 같은
  언어 중립 특성만 투영한다. 기준 언어와 다른 정본은 그 말투의 샘플·완성 글·규칙 근거로 학습하지 않는다([I4]).
- target이 말투·용도·수정 지시의 언어 요구와 충돌하면 target/content language 제약이 우선한다. 모델이나
  서버가 다른 언어를 추측해 선택하지 않는다.
- 동기 RPC 실패는 Connect 상태와 별도로 안정적인 `reason + params` detail을 보내고, 생성 작업·실험·검증·발행처럼
  나중에 다시 읽는 실패는 같은 구조의 `Failure`를 저장한다. 서버가 미리 만든 한국어·영어 문장과 provider 원문은
  표시 계약이 아니며, 브라우저가 현재 UI locale의 catalog에서 사유를 렌더링한다.

---

## 4. 기능 요구사항

### F-1. 계정과 로그인

- 아이디 + 비밀번호 로그인. 세션은 30일 유지.
- **가입 화면 없음.** 계정은 운영자가 서버 측 도구로 생성한다.
  2인용 도구를 공개 인터넷에 올리면서 가입을 열어둘 이유가 없다.
  → `backend/cmd/adduser` 를 VPS에서 `docker compose run --rm api adduser <id>` 로 실행.
- 비밀번호는 **argon2id**로 저장(`golang.org/x/crypto/argon2`). 평문·단순 해시 금지.
- 존재하지 않는 아이디와 틀린 비밀번호의 응답 시간이 구분되지 않아야 한다.
  → 아이디가 없어도 **더미 해시로 검증을 한 번 돌린다**. 그냥 early-return하면
  타이밍으로 아이디 존재가 새어 나간다.

**세션 전달 — HttpOnly 쿠키.**
프론트는 `postpilot.<도메인>`(Cloudflare), API는 `api.postpilot.<도메인>`이라 **교차
출처지만 동일 사이트**다(등록 도메인이 같다). 그래서:

```
Set-Cookie: pp_session=<불투명 토큰>; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=2592000
```

- `Domain` 을 **지정하지 않는다**(host-only). 지정하면 같은 등록 도메인의 다른
  프로젝트에도 쿠키가 딸려간다.
- JS가 토큰을 만질 수 없으므로 XSS로 30일 세션이 새지 않는다.
- CORS에 `AllowCredentials: true` + 정확한 origin 1개(`CORS_ORIGIN`)를 켠다.
  와일드카드 origin은 credentials와 함께 쓸 수 없다.
- 프론트의 Connect 트랜스포트에 `credentials: 'include'` 를 준다.
- 토큰은 **불투명 랜덤값**(32바이트)이고 서버 테이블에서 조회한다. JWT 아님 —
  즉시 무효화가 가능해야 하고, 2인 규모에 조회 비용은 문제가 안 된다.

**완료 조건**

- 세션 없이 API 호출 시 401.
- 세션 토큰을 한 글자 변조하면 401.
- 로그인 실패 응답에 아이디 존재 여부가 드러나지 않는다.
- 브라우저 devtools의 JS에서 `document.cookie` 로 세션 토큰이 보이지 않는다.

### F-2. 글 작성 입력

- 모든 글은 같은 계정의 활성 말투 하나를 반드시 선택한다. 새 글 드롭다운은 계정의 기본 말투로 시작하지만 요청에는 실제
  `voice_id`를 보내며, 서버는 누락·타인 소유·삭제된 말투를 기본값으로 대체하지 않는다.
- 글의 말투는 나중에 활성 말투로 바꿀 수 있다. 본문·사진·확정 상태는 유지하고 기존 말투의 학습 이력은 옮기지 않는다. 이전
  말투로 만든 기계 기준본은 무효화하여 새 말투로 생성/수정한 뒤에만 그 말투 학습에 쓸 수 있다.
- 새 글은 `target_language`를 반드시 저장한다. 프론트는 현재 UI locale을 초기값으로 보내지만 사용자가 `ko`·`en` 중
  바꿀 수 있고, 서버는 누락되거나 지원하지 않는 값을 다른 언어로 추측하지 않는다. 기존 정본이 있는 글에서 target을 바꾸면
  정본과 `content_language`는 그대로이며 다음 전체 생성/A-B부터 적용된다.
- 가제(선택), 경험 메모(자유 텍스트), 사진 다중 업로드.
- **메모 자동 저장.** 입력 후 1초 내외로 저장(디바운스된 `SavePostDraft` 호출).
  폰에서 앱을 닫아도 안 날아간다.
- 사용자가 고를 수 있는 파일: jpg, jpeg, png, webp, **heic, heif**.
  그 외 확장자는 조용히 거부하고 거부 목록을 사용자에게 알린다.
- 개별 파일 상한 25MB — **원본 선택 시점 기준**. 서버에 닿는 건 변환 후 파일이다.
- 업로드한 사진을 개별 삭제할 수 있다. 삭제 시 R2 오브젝트도 함께 지운다.

**업로드 경로 (§6.2가 근거)**

```
브라우저
 1. HEIC/HEIF면 WASM으로 디코딩
 2. canvas로 긴 변 1024px 축소 → JPEG 인코딩
 3. CreateUpload(post, 파일명) → 서버가 R2 presigned PUT URL 발급
 4. R2로 직접 PUT              ← API 서버를 거치지 않는다
 5. ConfirmUpload(upload_id)   → 서버가 오브젝트 존재를 확인하고 Image 행 생성
```

- API 서버가 사진 바이트를 중계하지 않는다. VPS 대역폭과 메모리를 안 쓴다.
- 폰에서 올라가는 양이 25MB → 200KB 수준으로 줄어든다. S1의 체감이 여기서 갈린다.
- R2 버킷에 **CORS 설정이 필요하다**(프론트 origin에서 PUT 허용). 빼먹으면 3단계까지
  성공하고 4단계에서만 조용히 실패한다.
- presigned URL 유효기간은 10분. `ConfirmUpload` 가 안 온 업로드는 고아 오브젝트가
  되므로 주기적으로 정리한다(§9).

**완료 조건**

- 메모 입력 후 브라우저를 강제 종료하고 재접속하면 내용이 남아 있다.
- 실행 파일을 업로드하면 저장되지 않고 건너뛴 목록에 표시된다.
- 아이폰에서 찍은 HEIC 사진 8장이 전부 올라가고 컨택트 시트에 보인다.
- 사진을 삭제하면 R2에서도 사라진다.

### F-3. 문체 학습

- 선택한 말투의 이력이 0편이어도 첫 글 생성은 가능하다. 생성 결과를 앱에서 블록 단위로 다듬은 뒤 사용자가 먼저 **확정**하고,
  이어서 **확정하고 말투 학습** 또는 나중의 **말투 학습**을 명시적으로 눌러야 기계 기준본과 최종본의 차이를 학습한다.
  확정만 해도 완전한 성공이며, 열기·복사·내보내기·시간 경과로는 학습하지 않는다.
- **한 편부터 동작한다.** 각 말투의 첫 완성 글은 그 말투의 첫 문체 근거이자 다음 생성의 예시가 된다. 30편을 요구하거나 권장하지 않는다.
- 프로필은 어휘 / 종결어미 / 문장 / 구조 / 성향 축과 명시적 unknown, 출처, 버전 기록을 제공한다. 종결어미 분포와 문장 길이는
  결정적으로 측정하며 모델 추정보다 우선한다.
- 반복된 최종 편집은 근거가 붙은 대조 규칙 후보가 된다. 서로 다른 근거 3회에 활성화하며, 모순은 사용자가 확인한다. 180일
  미사용 규칙은 다음 명시적 생성/수정 요청에서만 중지된다.
- 문장 의견은 단어·종결어미·길이·구조 중 하나를 요구한다. 의견이나 “수정 없이 마음에 듦”만으로 규칙을 만들거나 활성화하지 않는다.
- 과거 글 붙여넣기는 선택 사항이며 이 경로에만 최소 200자를 적용한다. 추가·삭제 시 그 말투에 속한 가져오기 자료와 완성 글
  전체만 함께 재분석한다. 다른 말투의 앱 내 글이나 샘플을 선택·복사·공유하는 경로는 제공하지 않는다.
- 규칙 블라인드 비교와 3편 기반 프로필 검증은 별도의 명시적 동작이다. 모델을 확인한 후에만 실행하며 자동/주기 실행하지 않는다.
- 각 말투는 생성 시 정한 기준 언어 `ko` 또는 `en` 하나를 가지며 나중에 relabel하지 않는다. 같은 언어의 정본만 샘플·완성 글·
  규칙 근거로 학습할 수 있다. 교차 언어 생성은 허용하지만 언어 중립 프로필만 투영하고, 다른 언어 정본의 말투 학습은 사유와
  함께 거부해 한 프로필을 두 언어의 평균으로 오염시키지 않는다.

**문체 분석이 반드시 다뤄야 할 항목**
종결어미 분포 / 평균 문장 길이 / 문단당 문장 수 / 자주 쓰는 접속사·부사 /
말버릇 / 이모지·감탄사·말줄임표 사용 여부와 빈도 / 비유와 수치 사용 /
**한 번도 안 쓰는 흔한 표현** / 1인칭 표현.

마지막 항목이 중요하다. "이건 안 쓴다"가 지키기 가장 쉬운 규칙이다.

**완료 조건**

- 프로필이 비어 있으면 그 사실을 알리되 생성은 막지 않는다.
- 두 계정이 서로의 문체 규칙을 읽지 못하고, 한 계정의 두 말투도 프로필·예시·규칙·검증 이력을 섞지 않는다.
- 앱을 가만히 두거나 다시 시작해도 말투 관련 프로바이더 호출이 발생하지 않는다.

### F-4. 글 생성

- 관찰 단계 → 작성 단계 순으로 호출 (§3.3).
- 일반 `생성`은 명시적으로 저장된 활성 관찰/작성 모델을 사용하고, 관찰을 최대 한 번 수행한 뒤 작성 모델 하나의 결과를
  `posts.content`에 쓰며 상태를 `review`로 바꾼다. 활성 모델이 없으면 서버가 추측하거나 대체하지 않는다.
- 별도 `A/B 비교 생성`은 작성 A/B 모델 두 개를 명시적으로 사용한다. 관찰은 최대 한 번만 수행하고 같은 입력 스냅샷으로
  후보 두 개를 만든다. 모델명은 선택 전까지 서버 응답에서도 숨기고 좌/우 위치를 무작위로 고정한다.
- 비교 작업 완료만으로 후보를 정본에 쓰지 않는다. 사용자가 `결과 적용` 또는 `결과 적용하고 활성 모델로 변경`한 후보만
  `posts.content`가 되고 상태가 `review`로 바뀐다. 전자는 활성 모델을 바꾸지 않는다.
- 결과는 블록 배열 + 제목 + 요약 + 태그.
- 목표 글자 수는 접힌 옵션에서 필요할 때만 켠다. 꺼져 있으면 길이 제한 없이 자연스럽게 생성하고, 켜져 있으면 사용자가 고른
  정확한 값과 글에 지정된 활성 말투의 문체 규칙·최대 3개 작성 예시를 같은 입력 스냅샷에 고정한다.
- 일반 생성과 작성 A/B는 글의 `target_language`를 같은 입력 스냅샷에 고정한다. 제목·요약·태그와 모든 텍스트 블록,
  이미지 alt·caption을 그 언어로 작성하되 실제 파일명·인용 원문·고유명사를 억지로 번역하지 않는다. A/B 두 후보는 반드시
  같은 target을 받고, 적용된 결과만 그 글의 `content_language`를 원자적으로 갱신한다.
- **모델이 지어낸 사진 파일명은 걸러낸다.** 실제 첨부된 파일만 남긴다.
- 사진을 안 넣어도 메모만으로 생성된다.
- 진행 상태는 §3.5의 폴링으로 표시한다.

**완료 조건**

- 사진 0장으로 생성해도 오류 없이 글이 나온다.
- 결과에 첨부되지 않은 이미지 참조가 하나도 없다.
- 생성 중 브라우저를 닫았다 열어도 진행 상태가 이어서 보인다.
- 완료 후 브라우저를 닫았다 열어도 같은 좌/우 후보를 다시 보고 선택할 수 있다.

### F-5. 관찰 결과 표시 (컨택트 시트)

업로드한 사진을 가로 스트립으로 늘어놓고, 각 사진 밑에 **모델이 뭘 봤는지**를
그대로 보여준다.

이건 장식이 아니라 **디버깅 화면**이다. 글이 이상하면 열에 아홉은 관찰 단계가
잘못 본 것이다. 사용자가 프롬프트를 의심하기 전에 여기부터 볼 수 있어야 한다.

- 썸네일은 R2 presigned GET URL(유효기간 짧게)로 부른다. 버킷은 **공개하지 않는다** —
  남의 사진이 URL만 알면 보이는 상태가 되면 안 된다.

### F-6. AI로 수정

- 자연어 요청으로 초안을 고친다. ("더 짧게", "존댓말로", "카페 얘기 늘려줘")
- 요청받은 부분만 고치고 나머지 문장은 유지한다.
- 이미지 블록의 파일명은 바뀌지 않는다. 순서 변경은 허용.
- **수정 시에도 글에 지정된 말투 프로필만 다시 주입한다.** 안 하면 서너 번 고친 뒤 모델이
  자기 기본 문체로 돌아간다.
- **"이 요청을 규칙으로 저장" 체크박스.** 켜면 요청 문장이 그 글의 말투 프로필에 누적되어
  다음 글부터 적용된다. 같은 지적을 반복하지 않게 하는 장치다.
- 여러 번 반복 가능.
- 성공한 AI 수정은 새 기계 기준본이 된다. 이후 직접 편집만 현재 내용 revision을 바꾸며, 학습 확정 전에는 기존 기준본이 보존된다.
- AI 수정은 현재 정본의 `content_language`를 작업 스냅샷에 고정한다. 저장된 다음 target이나 “다른 언어로 써줘” 같은 충돌
  지시는 기존 정본을 암묵적으로 번역하지 않으며, 언어를 바꾸려면 전체 생성/A-B를 명시적으로 실행한다.
- 현재 정본 언어가 말투 기준 언어와 다르면 수정 자체는 허용하지만 `이 요청을 규칙으로 저장`은 비활성화한다. 서버도
  `save_as_rule = true`를 작업 등록과 규칙 변경 전에 거부해 다른 언어의 지시가 말투 프로필에 들어가지 않게 한다.

**완료 조건**

- "존댓말로"를 저장한 뒤 같은 말투로 새 글을 생성하면 존댓말로 나오고 다른 말투에는 적용되지 않는다.
- 5회 연속 수정 후에도 원래 문체가 유지된다.

### F-7. 플랫폼별 출력

네 가지 형식을 탭으로 전환하며 보고, 버튼 하나로 클립보드에 복사한다.

| 형식          | 출력                                              | 안내 문구                                    |
| ------------- | ------------------------------------------------- | -------------------------------------------- |
| 네이버 블로그 | 평문. 사진 위치를 `[사진 파일명 — 캡션]`으로 표시 | 붙여넣고 표시된 자리에 사진 드래그           |
| 티스토리      | HTML 조각. 이미지 태그 옆에 교체 안내 주석        | HTML 모드에 붙여넣고 사진 업로드 후 src 교체 |
| 자체 사이트   | 완성된 단독 HTML 페이지                           | 그대로 저장                                  |
| 마크다운      | front matter 포함                                 | Hugo / Jekyll / Obsidian                     |

- 자체 사이트 형식의 **디자인은 고정 템플릿**이다. 모델이 매번 CSS를 만들면 글마다
  디자인이 달라진다.
- 변환은 **프론트에서 한다.** 블록 배열은 이미 클라이언트에 있고, 네 형식 모두
  순수 함수(블록 → 문자열)라 서버 왕복이 필요 없다. 탭 전환이 즉시 반응한다.
  → `frontend/src/features/export-*` 에 형식당 함수 하나 + 스냅샷 테스트.
- 클립보드 API가 막힌 환경(http 등)에서는 텍스트를 자동 선택해 수동 복사를 유도한다.
- 내보내기는 `content_language`를 따라 정본을 그대로 변환하며 번역하지 않는다. 단독 HTML의 `lang`과 Markdown의 언어
  메타데이터는 실제 content language를 사용하고, 나중에 바꾼 target만으로 기존 출력이 변하지 않는다.

### F-8. 글 목록

- 자기 글만 보인다. 제목, 말투, 수정 시각, 상태. soft delete된 말투는 `삭제된 말투 · <이름>`으로 남는다.
- 목록에서 열면 메모와 초안이 복원되어 이어서 작업할 수 있다.
- 생성 중인 글은 `AI 생성 중`, 명시적 A/B 비교에서 두 후보가 끝난 글은 `AI 결과 확인`, 일부 실패는
  `AI 결과 오류`로 구분된다(§3.5). 선택 대기 글은 비교 화면으로 들어갈 수 있다.

### F-9. 모델 실험과 리더보드

- `/ai-models`에서 추천 조합을 명시적으로 적용하고 관찰·문체 분석·작성 단계의 활성
  모델과 A/B 쌍을 각각 바꿀 수 있다. 신규 계정에는 자동 선택이 없다.
- 관찰 실험은 같은 글의 사진 묶음, 문체 분석 실험은 명시적으로 선택한 한 말투의 전체 샘플 묶음, 작성 실험은
  같은 관찰·메모·문체 스냅샷을 두 모델에 준다. 한 실험은 한 단계만 비교한다.
- 선택 전에는 모델명을 숨기고 좌/우를 무작위 배치한다. `둘 다 사용하지 않기`와 실패한
  후보만 재시도하기를 지원하며, 단일 생존 결과는 쓸 수 있지만 승패에는 넣지 않는다.
- 계정별·단계별 리더보드에서 Elo, 비교 수, 승/패/승률, 평균 지연, 토큰과 비용을 본다.
  다른 계정의 결과나 순위는 볼 수 없다.
- 작성 실험은 `target_language`를 입력 스냅샷·해시·보존 메타데이터에 포함하고, 두 후보와 재시도에 같은 값을 쓴다.
  현재 리더보드는 모든 단계에서 계정·단계별로 집계하며, target language는 재현 가능한 비교 입력으로만 보존한다.
- 관찰/분석 승자는 결과 적용과 활성 모델 적용을 각각 명시적으로 선택한다. 분석 결과는 실험이 고정한 같은 활성 말투에만
  적용할 수 있고, 사용자가 쓴 `rules`는 바뀌지 않는다.
- 선택·기각 후 입력과 두 후보 본문은 30일 뒤 지우되, 리더보드 재계산에 필요한 모델,
  승패, 사용량·비용·시간 메타데이터는 계정 삭제 전까지 남긴다.

### F-10. 연결된 Mac으로 네이버 발행

- 글 완성의 기존 `내보내기` 아래에 별도 `발행하기` 섹션을 둔다. 현재 revision이 정확히 확정된 글에서만
  `네이버에 발행`을 명시적으로 확인할 수 있으며, 확인하면 제목·본문·사진·캡션·태그·카테고리·공개 여부와
  최종 발행까지 실행한다.
- Mac의 최소 동반 앱은 백그라운드 CLI + loopback 설정 화면이다. postpilot 계정을 기기 코드로 연결하고,
  지원하는 Chromium 계열 브라우저와 전용 프로필을 고른 뒤 Naver 로그인을 확인한다. 두 postpilot 사용자는
  서로 다른 기기 토큰·브라우저 프로필·작업 디렉터리를 쓴다.
- 작업은 VPS SQLite에 먼저 남고 Mac은 아웃바운드 폴링으로 가져간다. Mac이 꺼져 있으면 기다리며, 연결이
  돌아오면 별도 휴대폰 동작 없이 이어간다. SSE/WebSocket 연결 자체는 정본 상태가 아니다.
- plan 02의 1024px JPEG를 R2 안에서 작업 전용 키로 복사해 고정한다. 원본·사진 바이트·플랫폼 자격증명은
  API 프로세스를 지나지 않는다.
- 결정적 로컬 발행기는 고정 manifest와 현재 작업의 JPEG만 읽고, 버전이 붙은 DOM/접근성/CDP 동작만
  실행한다. 서버가 selector·JavaScript·좌표 매크로를 공급할 수 없고, 편집기 구조·계정·본문·사진·설정을
  정확히 검증하지 못하면 최종 클릭 전에 실패한다. 최종 발행 버튼 직전 `committing`을 서버에 기록하며,
  클릭은 검증된 최종 제어에 정확히 한 번만 보낸다. 이후 같은 탭의 예상 블로그 URL과 제목·블록·사진·
  캡션·태그·카테고리 읽기 검증이 끝나야 성공이다. 그 전에 연결이 끊기면 `outcome_unknown`에서 멈추고
  자동 재시도하지 않는다.
- 로그인 만료, CAPTCHA, 2FA, 계정 불일치는 `needs_attention`으로 멈춘다. 우회하지 않는다.
- 공개/비공개, 카테고리, 정본 태그를 지원한다. 예약 발행과 휴대폰 Push 알림은 후속이다.

**완료 조건**

- Mac이 꺼진 상태에서 휴대폰으로 발행을 요청하고 나중에 Mac을 켜면, 확정 당시의 글과 JPEG 8장이 올바른
  순서로 한 번만 발행되고 휴대폰 화면에 Naver URL이 표시된다.
- 최종 클릭 직후 Mac 프로세스를 강제 종료해도 시스템은 자동 재시도하지 않아 중복 글을 만들지 않는다.

### F-11. UI 다국어 지원

- 로그인부터 로그인 후 현재 SPA 화면의 정적 문구, validation, loading/empty/error, dialog, 접근성 이름을
  한국어·영어 catalog로 제공한다. 공개 설명 페이지용 `marketing` namespace는 예약되어 있으며 그 페이지
  자체는 plan 15/job 34에서 추가한다.
- 사용자가 쓴 글·메모·말투/용도 이름, 모델·provider·플랫폼 고유명, 외부 provider 진단 원문은 번역하지 않는다.
  제품이 아는 동기 오류는 `AppErrorDetail { reason, params }`, 영속 오류는
  `Failure { reason, params, technical_detail }`로 전달·저장하고 화면에서 현재 locale로 번역한다. 알 수 없거나
  detail이 없거나 손상된 실패는 `UNKNOWN_FAILURE`의 일반 번역으로 폴백하며 raw message를 UI 문구로 사용하지 않는다.
- 날짜·숫자·상대 시간은 같은 데이터에 `Intl` locale formatting만 적용한다. locale 전환은 데이터·세션·URL·작업 상태를
  바꾸지 않는다.
- 첫 React 화면 전에 저장값과 `navigator.languages`로 locale을 결정하고 `<html lang>`을 맞춘다. 사용자는 로그인 화면과
  로그인 후 shell에서 locale을 바꿀 수 있고, 명시적 선택은 같은 브라우저에 저장된다. 공개 header는 job 34가 같은 control을
  합성한다.
- 한국어·영어 catalog의 key 집합은 같아야 하며 누락은 CI에서 실패한다. 런타임에 번역 key 자체를 사용자에게 노출하지 않는다.

**완료 조건**

- 저장된 선택이 없는 `en-US` 브라우저는 같은 URL에서 영어, `ko-KR`은 한국어, 지원하지 않는 locale은 한국어로 열린다.
- 언어를 바꾸면 `<html lang>`, 화면 문구와 locale formatting이 즉시 바뀌며 사용자가 쓴 데이터와 진행 중 작업은 그대로다.
- 제품이 아는 동기·영속 오류는 현재 locale로 다시 렌더링되고, 내부 오류·비밀·provider 원문으로 번역 분기하지 않는다.

---

## 5. 데이터 모델

**SQLite**(§6.3). 마이그레이션은 goose, 쿼리는 sqlc로 Go 코드 생성.

```sql
users            id TEXT PK, password_hash TEXT, created_at

sessions         token TEXT PK          -- 32바이트 랜덤의 해시
                 user_id TEXT FK, expires_at, created_at

voices           id TEXT PK, user_id FK -- 계정당 여러 말투, 활성 기본값 하나
                 name, source_language    -- 말투 근거의 기준 언어 ko | en, 생성 뒤 불변
                 is_default, deleted_at, created_at, updated_at

voice_profiles   voice_id TEXT PK FK, user_id FK -- 말투당 1:1
                 styleguide TEXT        -- 생성·수정된 문체 규칙
                 rules      TEXT        -- 사용자가 저장한 수정 요청 누적
                 updated_at

voice_samples    id, voice_id FK, user_id FK, label TEXT, body TEXT, created_at

posts            slug TEXT PK, user_id FK, voice_id FK
                 title TEXT, memo TEXT          -- 사용자 입력
                 target_language TEXT           -- 다음 전체 생성/A-B의 ko | en
                 content_language TEXT nullable -- 현재 content가 실제로 쓰인 언어
                 observations TEXT              -- 관찰 단계 결과 (JSON)
                 content TEXT                   -- 블록 배열 (protojson) ← 정본
                 status TEXT                    -- draft | review | finalized
                 created_at, updated_at

images           id, post_slug FK
                 filename TEXT                  -- 모델·출력이 참조하는 이름
                 r2_key TEXT                    -- 실제 오브젝트 키
                 width, height, bytes
                 created_at

generation_jobs  id, post_slug FK, user_id FK, voice_id FK nullable -- §3.5, 말투 작업/스냅샷
                 kind TEXT                      -- generate | revise | analyze_voice | learn_voice | compare_voice_rule | validate_voice_profile
                 status TEXT                    -- queued | running | done | failed
                 stage TEXT                     -- observe | write
                 progress_done, progress_total
                 error_reason TEXT, error_params TEXT, technical_detail TEXT
                 error TEXT                     -- 이전 행/내부 진단 호환, UI 표시 금지
                 observe_model TEXT, write_model TEXT   -- 어떤 선택으로 돌았는지
                 created_at, updated_at

model_experiments id, user_id FK, voice_id FK nullable, post_slug FK nullable
                  stage, target_language nullable, status, job_id
                  input_snapshot, input_hash, prompt_version
                  winner_candidate_id, outcome
                  apply_error_reason, apply_error_params, apply_technical_detail
                  adoption_error_reason, adoption_error_params, adoption_technical_detail
                  created_at, finished_at, decided_at, content_expires_at

model_experiment_candidates id, experiment_id FK
                  provider_id, model_id, model_label, display_side
                  status, output, error_reason, error_params, technical_detail
                  error                              -- 이전 행/내부 진단 호환
                  prompt_tokens, completion_tokens, cost_microusd, latency_ms

publishing_agents id, user_id FK, token_hash, platform, label
                  platform_account_id, platform_account_label, browser_label
                  categories_json, default_category_id, default_visibility
                  last_seen_at, revoked_at, created_at, updated_at

publish_jobs      id, user_id FK, post_slug snapshot, post_created_at incarnation
                  (agent_id,user_id) composite FK, platform, status, stage, progress_seq
                  content_revision, manifest_json, settings_json
                  lease_token_hash, lease_expires_at
                  error_reason, error_params, technical_detail, error_message, platform_post_url
                  created_at, claimed_at, committed_at, published_at, updated_at

publish_assets    job_id FK, ordinal, filename, source_filename, staged_key, bytes
```

마이그레이션 0007은 `posts`에 `content_revision`, 불변 `machine_baseline`, 그 revision, 선택형 `target_length`를 더하고,
후속 마이그레이션은 정확한 `finalized_revision`과 시간을 더한다. 문체 쪽에는 불변
프로필 버전/직접 설정, 완성 글 출처, 멱등 학습 이벤트, 대조 규칙·근거·충돌 확인, 문장 의견, 규칙 비교 후보, 프로필 검증 항목을
계정 범위로 저장한다. 기존 `styleguide`와 `rules`는 그대로 남아 수동 안내로 계속 주입된다.

plan 10의 후속 임베디드 마이그레이션은 기존 계정마다 `기본 말투`를 하나 만들고 모든 글과 0005/0007 문체 데이터를 그 id로
무손실 backfill한다. 그 뒤 문체 테이블의 버전·유일성·작업 경계를 `user_id` 단독이 아니라 `(user_id, voice_id)`로 분할하고,
`posts.machine_baseline_voice_id`로 말투 변경 전 기준본이 새 말투 학습에 섞이지 않게 한다.

1.4의 임베디드 마이그레이션은 provider나 언어 감지기를 호출하지 않고 기존 말투의 `source_language`와 기존 글의
`target_language`를 모두 `ko`로 결정적으로 backfill한다. 비어 있지 않은 기존 정본과 기계 기준본의
`content_language`도 `ko`로 채우고, 정본이 없는 글은 `NULL`로 둔다. 새 말투·글은 서비스가 검증한 `ko | en`을
명시적으로 저장하며 DB 기본값으로 요청 누락을 숨기지 않는다. 기존 영속 오류 문장은 내부 호환용으로 남기되 알려진
상태는 구조화된 사유로 옮기고, 나머지 이전 실패는 raw 문장 대신 `UNKNOWN_FAILURE`로 투영한다.

사용자에게 보이는 모든 영속 실패는 렌더링된 문장 대신 `error_reason`과 flat scalar만 담는
JSON object `error_params`를 정본으로 삼고, 선택적 진단은 `technical_detail`에 둔다. SQL 오류·stack·secret·provider response body·prompt·사용자 글은 params에
넣지 않으며 `error`/`error_message` 호환 필드는 UI가 직접 표시하지 않는다.

- **세션 토큰은 해시해서 저장한다.** DB가 새도 세션이 바로 탈취되지 않게.
- **이미지 바이트는 DB에 없다.** R2 오브젝트 키만 들고, 실제 파일은 R2에 있다(§6.2).
  키 규칙: `posts/{slug}/{image_id}.jpg`.
- `posts.content` 는 `Block` 메시지 배열의 protojson. 스키마는 proto가 소유한다(§3.2).

**블록 배열 (정본)** — proto 정의는 §3.2. 직렬화된 모양:

```jsonc
{
  "title": "성산 3일차",
  "summary": "바람 많던 날",
  "tags": ["제주", "성산"],
  "blocks": [
    { "type": "TEXT", "content": "문단 하나" },
    { "type": "HEADING", "level": 2, "content": "소제목" },
    { "type": "IMAGE", "file": "IMG_1.jpg", "alt": "...", "caption": "..." },
    { "type": "QUOTE", "content": "강조할 한 줄" },
    { "type": "LIST", "items": ["항목", "항목"] },
  ],
}
```

**프로필 주입 순서** — 글에 지정된 단 하나의 말투에서만 아래 순서로 넣는다.

전체 생성의 `target_language` 또는 수정의 `content_language`가 말투의 `source_language`와 같을 때만 전체
프로필을 아래 순서로 넣는다. 다르면 그 말투의 typed `structure`와 여섯 `axes`만 결정적으로 투영하고,
lexical·endings·syntax/connectives·legacy styleguide·자유 `rules`·활성 대조 규칙·샘플·완성 글 발췌는 모두
제외한다. 이 투영은 번역이나 추가 모델 호출 없이 같은 말투 스냅샷을 whitelist하는 순수 동작이며 다른 말투의 근거를
읽지 않는다([I3], [I4]). 관찰 결과는 이 언어별 프로필 투영에 들어가지 않고 기존처럼 언어와 독립적인 사진 사실로 남는다.

```
1. styleguide          규칙
2. 활성 대조 규칙·금지 표현  검증된 제약
3. 유사한 완성/가져오기 글 발췌 0~3편  근거(없어도 생성 가능)
4. rules               사용자가 직접 지정한 것 ← 마지막
```

사용자가 직접 쓴 규칙이 마지막이다. 사람이 구체적 실패를 보고 쓴 것이라 가중치가
높아야 하고, 프롬프트 후반부가 더 잘 지켜진다.

> **덤:** 이 순서는 프롬프트 캐싱과도 맞아떨어진다. 1~2는 글마다 바뀌지 않는
> 안정적 접두사고, 캐싱은 접두사 일치로 동작한다. 프로필을 앞에, 이번 글의 메모와
> 관찰 결과를 뒤에 두면 프로바이더가 캐싱을 지원할 때 그대로 이득을 본다.
> F-6의 반복 수정에서 특히 크다 — 매 수정마다 프로필 전체를 다시 주입하니까.

---

## 6. 기술 스택 (확정)

### 6.1 전체 모양

```
브라우저
 ├─ postpilot.<도메인> ────────▶ Cloudflare Worker (정적 자산)
 │                                Vite + React 19 + TS, Tailwind v4,
 │                                TanStack Router/Query, FSD 구조
 └─ api.postpilot.<도메인> ────▶ VPS
                                  edge Caddy (TLS) → Go API 컨테이너
                                  Protobuf + Connect (unary)
                                  SQLite (볼륨) + 워커 고루틴
사진: Cloudflare R2 (브라우저가 직접 PUT / GET)

Mac 동반 에이전트 ──outbound poll──▶ api.postpilot.<도메인>
  └─ 결정적 로컬 네이버 발행기 + 계정별 전용 Chromium 프로필 ──▶ 네이버 스마트에디터/최종 발행
```

**서버가 반드시 필요하다** — LLM API 키를 브라우저에 둘 수 없다. Go API가 그 역할을
겸한다. 프론트는 §3.1의 로컬 발행 경계 덕분에 순수 정적으로 남는다. Mac 동반 에이전트는 별도 런타임이며
VPS나 Cloudflare가 대신 브라우저를 구동하지 않는다.

배포·CI 구성은 `DEPLOY.md`. 지금 리포에 이미 들어있다.

### 6.2 이미지 처리 — 브라우저에서

**HEIC 디코딩과 1024px 축소를 브라우저가 한다.** libheif WASM으로 디코딩하고
canvas로 축소해 JPEG으로 인코딩한 뒤 R2에 올린다.

이유:

- Go에는 실용적인 순수 Go HEIC 디코더가 없다. 서버에서 하려면 cgo libheif가 필요하고,
  그러면 **CGO-free distroless 정적 이미지를 포기**하고 Debian slim + 시스템 라이브러리로
  가야 한다. 이미지가 커지고 빌드가 복잡해진다.
- 어차피 1024px로 줄일 거라면 원본을 네트워크로 올릴 이유가 없다. 폰 업로드가
  25MB에서 200KB 수준으로 줄어든다.
- §8의 얼굴 모자이크를 나중에 넣는다면 **반드시 업로드 전 로컬**이어야 하는데,
  그 자리가 이미 여기에 마련된다.

대가: WASM 번들이 1~2MB 붙고, 구형 폰에서 8장 변환에 몇 초 걸린다. 변환 중임을
UI에 표시한다. 원본은 서버에 남지 않는다 — 나중에 더 큰 해상도가 필요해지면
그때 다시 올려야 한다.

전송 포맷은 JPEG으로 통일. 품질 0.85.

### 6.3 저장소 — SQLite

`modernc.org/sqlite`(순수 Go, cgo 없음)를 쓴다. `mattn/go-sqlite3`는 cgo라
distroless 정적 빌드가 깨진다.

- DB 파일은 VPS 볼륨(`/srv/postpilot-<env>/data/postpilot.db`)에 둔다.
  컨테이너 재시작마다 초기화되는 환경은 부적합하다.
- WAL 모드. 쓰기는 단일 커넥션으로 직렬화한다(SQLite의 단일 라이터 특성).
  2인 규모에 병목이 아니다.
- **마이그레이션은 바이너리에 embed되어 기동 시 돈다**(`//go:embed` + goose). 별도
  마이그레이션 컨테이너를 두지 않는다 — DB가 이 컨테이너의 로컬 파일이라 맞출 공용
  서버가 없고, 이렇게 하면 스키마가 그걸 읽는 코드와 어긋날 수 없다.
  실패하면 프로세스가 죽고 → 배포의 `/health` 게이트가 잡고 → 이전 이미지 태그로
  되돌아간다(`DEPLOY.md §2`). "실패하면 api를 교체하지 않는다"와 같은 결과를,
  SQLite에서 실제로 동작하는 방식으로 얻는다.

### 6.4 모델 공급자 — 추상화 계층 + 필요할 때 추가

Go 쪽에 포트 하나를 두고, 프로바이더는 그 구현체로 붙인다. 벤더에 묶이지 않는다.

```go
// internal/llm — 이 인터페이스가 경계다. 위층(observe/write)은 프로바이더를 모른다.
type Provider interface {
    Name() string
    Complete(ctx context.Context, req Request) (Response, error)
}

type Request struct {
    Model    string
    System   string
    Messages []Message   // 텍스트 + 이미지 파트
    JSONSchema []byte    // 있으면 구조화 출력 요구
}
```

- **프로바이더 등록은 설정 파일**(`config/providers.yaml`)로. 코드 변경 없이 모델을
  추가한다. API 키는 파일이 아니라 env로 참조한다.
  ```yaml
  providers:
    - id: openrouter
      adapter: openai_compatible # base_url만 다르면 이 어댑터로 끝
      base_url: https://openrouter.ai/api/v1
      api_key_env: OPENROUTER_API_KEY
      models: [...]
    - id: anthropic
      adapter: anthropic # 스키마가 실제로 다른 소수 중 하나
      api_key_env: ANTHROPIC_API_KEY
      models: [...]
  ```
- 같은 파일에 표시용 컨텍스트·가격 스냅샷과 버전이 붙은 추천 조합을 둘 수 있다.
  가격은 참고값이고 실제 응답의 사용량 비용이 우선이다. 추천 조합은 사용자가
  명시적으로 적용할 때만 계정 선택에 저장된다.
- **어댑터는 필요할 때 하나씩 늘린다.** 1.0에 몇 개를 넣을지 미리 못 박지 않는다.
  `openai_compatible` 하나가 OpenRouter·Together·Groq·Fireworks·DeepInfra·OpenAI·
  Ollama·vLLM·LM Studio를 덮고, Google도 호환 엔드포인트가 있다.
- **API 키가 없는 프로바이더의 모델은 드롭다운에서 비활성**으로 보인다.
- 어댑터가 **비전 입력과 구조화 출력을 지원하는지**를 모델 항목에 표시한다.
  관찰 단계 드롭다운에는 비전 지원 모델만 뜬다.
- Anthropic 어댑터를 붙일 땐 **공식 Go SDK**(`github.com/anthropics/anthropic-sdk-go`)를
  쓴다. 직접 HTTP를 짜지 않는다 — 요청 형태가 자주 바뀐다.

**구조화 출력을 지원하는 프로바이더에서는 반드시 켠다.** §7 첫 줄의 "모델이 JSON 대신
설명문을 반환" 케이스가 대부분 사라진다. 지원하지 않는 모델을 위해 파서 폴백은 남긴다.

### 6.5 무료 모델을 쓸 경우

드롭다운에서 무료 모델을 고른 세션에만 해당한다. 앱이 강제하지 않는다.

- 무료 모델은 **예고 없이 사라진다.** 특정 모델 ID를 고정하면 어느 날 죽는다.
  자동 라우팅 모델(예: OpenRouter의 `openrouter/free`)을 목록에 넣어 두는 게 안전.
  선택했던 모델이 사라지면 드롭다운에서 회색으로 보이고 오류 사유가 뜬다.
- 요청 한도가 하루 단위로 걸린다. 사진 한 장당 한 번씩 호출하면 금방 소진되므로
  **관찰 단계는 3~4장씩 묶어서** 호출한다. (묶음 크기는 설정값.)
- 무료 모델은 긴 글 중반부터 지정한 어조에서 이탈하는 경향이 있다. 관찰만 무료,
  작성은 유료로 섞는 조합이 §3.3의 단계별 드롭다운으로 자연스럽게 가능하다.

### 6.6 응답 시간

생성에 30초~2분이 걸린다. §3.5의 작업 레코드가 이걸 다룬다.

- **서버리스 실행 시간 제한 문제는 없다.** API가 VPS의 상시 컨테이너라 상한이 없다.
  Cloudflare Workers는 정적 자산만 서빙하므로 CPU 제한과 무관하다.
- 프로바이더 호출에는 넉넉한 타임아웃(단계당 5분)을 준다. 긴 출력이 예상되면
  어댑터가 스트리밍으로 받아 HTTP 타임아웃을 피한다 — 스트림은 서버 안에서만 쓰고,
  클라이언트에는 폴링으로 진행률만 나간다.

---

## 7. 엣지 케이스

| 상황                                                                  | 처리                                                                                                                                                                    |
| --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 모델이 JSON 대신 설명문이나 코드펜스를 반환                           | 구조화 출력을 지원하면 애초에 안 생김. 아니면 펜스 제거 후 파싱하고, 실패 시 `MODEL_OUTPUT_INVALID`를 현재 locale로 표시. 응답 원문은 UI나 영속 실패 params에 넣지 않음 |
| 블록 필드 조합이 어긋남 (`type=TEXT`인데 `items`가 참)                | 역직렬화 직후 검증에서 해당 블록 폐기 + 로그 (§3.2의 대가)                                                                                                              |
| 모델이 첨부되지 않은 파일명을 참조                                    | 해당 이미지 블록 제거                                                                                                                                                   |
| 관찰 결과 개수가 사진 개수와 불일치                                   | 실제 파일명 기준으로 매칭, 초과분 버림                                                                                                                                  |
| 같은 날 같은 제목으로 두 번 생성                                      | slug 뒤에 일련번호                                                                                                                                                      |
| 사진 0장                                                              | 메모만으로 생성                                                                                                                                                         |
| API 키 미설정                                                         | 해당 프로바이더의 모델이 드롭다운에서 비활성                                                                                                                            |
| 모델 제공자가 호출 빈도를 거부                                        | 작업이 `failed`, `MODEL_RATE_LIMITED`를 현재 locale로 표시하고 다른 모델 선택 유도                                                                                      |
| 선택했던 모델이 사라짐                                                | 드롭다운에서 회색 + 사유 표시, 마지막 선택 초기화                                                                                                                       |
| 브라우저가 HEIC를 못 여는 구형 기기                                   | 그 파일만 건너뛴 목록에 넣고 사유 표시 (§F-2)                                                                                                                           |
| R2 PUT 실패 / presigned 만료                                          | 그 사진만 재시도 버튼. `ConfirmUpload` 없는 오브젝트는 고아로 정리                                                                                                      |
| 생성 중 서버 재시작                                                   | 기동 시 `running` 작업을 `failed`로 정리, 사용자에게 재시도 안내                                                                                                        |
| 대기 중 말투 작업과 함께 서버 재시작                                  | 워커 시작 전에 실패 처리해 기동 자체가 프로바이더 호출을 시작하지 않음                                                                                                  |
| 저장한 UI locale이 손상되었거나 브라우저 선호 언어가 `ko`·`en`이 아님 | 저장값을 무시하고 `navigator.languages`의 첫 지원 언어, 없으면 `ko` 사용                                                                                                |
| 기존 정본이 있는 글의 target language를 변경                          | 정본·기계 기준본·확정 상태·`content_language`·관찰 결과는 유지하고 다음 전체 생성/A-B에만 적용                                                                          |
| 작업 실행 중 또는 A/B 선택 대기 중 target language를 다시 변경        | 실행·후보는 시작 당시 target을 유지하고 적용 시 그 값으로 `content_language`만 기록. 글에 더 나중에 저장한 target은 덮어쓰지 않음                                       |
| 말투 기준 언어와 정본 언어가 다름                                     | 생성·수정·확정·내보내기·발행은 허용, 말투 학습·문장 의견·대조 근거·규칙 저장은 provider 호출 전에 거부                                                                  |
| 알 수 없는 오류 reason 또는 detail 없는 네트워크 실패                 | `UNKNOWN_FAILURE`의 일반 번역으로 폴백하고 raw backend/provider message는 표시하지 않음                                                                                  |
| 클립보드 API 차단                                                     | 텍스트 자동 선택으로 폴백                                                                                                                                               |
| 발행 요청 때 Mac이 꺼짐                                               | SQLite의 `queued`로 대기, Mac이 다시 폴링하면 claim                                                                                                                     |
| 네이버 로그인 만료 / CAPTCHA / 2FA                                    | `needs_attention`, 로컬 전용 프로필에서 사람이 해결하기 전까지 중단                                                                                                     |
| 최종 발행 클릭 후 연결이 끊김                                         | `outcome_unknown`, 중복 방지를 위해 자동 재시도 금지                                                                                                                    |
| 남의 글 slug로 직접 접근                                              | 403                                                                                                                                                                     |
| 남의 사진 R2 키를 알아냄                                              | presigned URL 없이는 못 읽음(버킷 비공개), 발급은 소유자 확인 후에만                                                                                                    |

---

## 8. 범위 밖 (명시적)

- **얼굴 모자이크.** 사용자가 업로드 전에 직접 처리한다. 파이프라인에 넣을 경우
  반드시 **업로드 전 로컬에서** 처리해야 한다 — 무료 티어 API 중에는 입출력을
  학습에 사용한다고 명시한 곳이 있고, 타인 얼굴이 담긴 원본을 외부로 보내는 건
  개인정보 문제가 된다.
  → §6.2가 브라우저 변환으로 정해졌으므로, 넣게 되면 **그 변환 단계 안**이 자리다.
- 네이버 블로그 외 플랫폼 자동 발행, 발행 예약, 휴대폰 Push 알림 (§3.1, F-10)
- 인스타그램 릴스·네이버 클립의 영상 생성/발행 — 네이버 블로그 발행 자동화 이후 별도 계획
- 이미지 생성
- 초안 버전 히스토리 / 되돌리기 (문체 프로필의 불변 버전·복원 기록은 포함)

---

## 9. 미결정 사항

1. ~~**배포처.**~~ → 해소. VPS + Cloudflare(§6.1), `DEPLOY.md`.
2. **모바일 앱 여부.** 현재는 반응형 웹으로 충분하다고 판단. 폰에서 사진 선택과
   긴 텍스트 입력이 실사용에서 불편하면 재검토.
3. ~~**문체 규칙 자동 정리.**~~ → 주기 작업 없이 해소. 근거 기반 후보/활성/중지 수명주기와 명시적 모순 확인을 사용한다.
4. **백업.** 1.0에서 백업 대상이 **둘로 나뉘었다** — SQLite 파일이 있는 VPS 볼륨과
   R2 버킷. R2를 고른 대가다. 자동화 여부·주기 미정.
   (한 곳으로 모으고 싶으면 사진도 VPS 볼륨에 두는 쪽이었다.)
5. **고아 오브젝트 정리 주기.** `ConfirmUpload`가 안 온 R2 오브젝트를 언제 지울지.
   일 1회 정도면 충분해 보이나 미정.

---

## 10. 지금 리포에 없는 것

스캐폴드는 프레임워크와 배포 경로까지만 있다. 이 PRD를 구현하려면 순서대로:

1. **SQLite + goose + sqlc** — `internal/db`, 마이그레이션 1번(§5 스키마),
   `deploy-backend.yml` rollout 앞단에 마이그레이션 스텝.
2. **인증** — `internal/auth`(argon2id, 세션), rpcserver에 인터셉터,
   CORS credentials, `cmd/adduser`.
3. **proto 확장** — `post.proto`(Block·Post·GenerationJob), `voice.proto`, `auth.proto`.
4. **R2** — `internal/storage`(presigned PUT/GET), 버킷 CORS, 스택 `.env`에 자격증명.
5. **LLM 포트 + 첫 어댑터** — `internal/llm`, `config/providers.yaml`.
6. **작업 워커** — `internal/job`, API 프로세스 안의 고루틴(§3.5).
7. **프론트** — 업로드/변환(WASM), 컨택트 시트, 수정, 플랫폼 탭.
8. **Mac 발행 동반 에이전트** — 계정별 기기 연결, 백그라운드 CLI/loopback 설정 화면,
   버전별 DOM/접근성/CDP 네이버 발행 드라이버와 Chromium 프로필, 네이버 발행 작업/상태 UI(plan 12).

1~2가 끝나야 나머지가 붙을 자리가 생긴다.
