# 클립 렌더 성능과 인프라 계획 — 2026-09-18

> Historical investigation, not the deployment contract. The implemented pull/lease,
> private artifact and recovery contracts are in `spec/ssot/ARCH.md` (ARCH-45..59)
> and `backend/build/RENDER.md`; current commands are in [DEPLOY.md §8](DEPLOY.md#media-environments).
> The three supported layouts and future isolated NVIDIA checks are documented there;
> production GPU selection is still pending CLIP-163 approval and hardware validation.
> The host inventory, open queue notes and timings below describe 2026-09-18 and
> must not be used as current capacity measurements or GPU activation evidence.

렌더가 운영에서 반복해 실패한 건을 조사하다가, 원인이 하나가 아니라 **천장 여러 겹**이라는
것이 드러나 계획 문서로 범위를 넓혔다. 무엇이 실측이고 무엇이 추정인지, 무엇을 고쳤고 무엇이
남았는지를 구분해 적었다. 앞선 기록(`CLIP-WORKSPACE-FINDINGS.md`)을 흡수했다.

> **2026-09-18 갱신** — 테일넷을 실제로 구축하고 큐 경로를 뚫었다. 바뀐 곳은 넷이다.
> 8절에 실제 노드 구성과 `tailscale serve` 결정·검증이 들어갔고, 7-1과 5-1에서 **Vultr 이전이
> 보류**로 바뀌었으며, 10절에 "Micro로 충분한가"가 미확인 항목으로 추가되었고, 12절에 tailnet
> 확인 명령이 붙었다. **큐 API 자체는 여전히 미구현이고 경로만 열려 있다.**

**목표는 셋이고 우선순위가 아니라 전부 중요하다: 속도, 경제성, 안정성.**
그중 속도가 제품의 생사를 가른다. 영상 하나에 20~30분이면 아무도 쓰지 않는다.

---

## 1. 지금 서 있는 자리

### 1-1. 박스

운영 API는 cosimosi와 같은 Lightsail 인스턴스에 있다. 2026-09-18에 정체를 확정했다.

| | |
|---|---|
| 인스턴스 | **EC2 t3.micro** (`i-02475e6b497a62250`, ap-northeast-2a) |
| 번들 | Lightsail Micro-1GB, **월 $7** |
| CPU | Xeon Platinum 8259CL 2.5GHz, **물리 코어 1개** (HT로 vCPU 2) |
| RAM | 909 MB (+ swap 2 GB) |
| 디스크 | 38 GB |
| 동거 | cosimosi api·worker, edge caddy |
| 백업 | **없음.** cron도 litestream도 스냅샷 스크립트도 없음 |

백업이 없는 것은 클로즈 알파 중이라 의도된 상태다. **클로즈 베타부터 붙인다.**

### 1-2. 실측 (2026-09-18, 유휴 상태, 실사 1080×1920 30fps 소스)

실제 렌더가 쓰는 인코더 인자를 그대로 써서 쟀다.

| 항목 | 값 |
|---|---|
| 리프 렌더 (무손실 yuv444p, ultrafast crf 0) | 300프레임 17.4초 = **17.2 fps** |
| 전달 인코드 (veryfast crf 20, yuv420p) | 300프레임 30.9초 = **9.7 fps** |
| 무손실 중간 파일 크기 | 10초에 259 MB = **25.9 MB/초 (207 Mbps)** |
| ffmpeg 실제 CPU 점유 | **181.8%** — `-threads 1`인데도 두 vCPU를 모두 씀 |
| 인코드 스레드 1→2 | 리프 8.90→7.54초(**+15%**), 전달 인코드는 11.81→12.52초로 **악화** |
| 리프 렌더 2개 직렬 대 병렬 | 35.39초 대 34.61초 = **2.2%** |
| 리프 렌더 3개 직렬 대 병렬 | 53.44초 대 51.47초 = **3.7%** |
| 12분 연속 부하 중 성능 저하 | **없음** (steal 1.85%) |
| 부하 중 메모리 | 여유 **51 MB**, swap 163→218 MB 사용, ffmpeg 단독 RES 222 MB |
| 부하 중 `/health` 지연 | p50 0.78 ms, p95 0.91 ms — **영향 없음** |

마지막 줄이 가설 하나를 지웠다. **렌더가 API를 굶기지 않는다.** 반면 여유 메모리 51 MB는
당장의 위험이고, 렌더 중 OOM이 나면 잡이 사라진다.

---

## 2. 속도를 막는 천장

### 2-1. 하드웨어 — 버스트 크레딧 (추정, 산술 근거)

t3.micro의 기준선은 vCPU당 10%, 시간당 12크레딧, 최대 288크레딧이다. 렌더가 181.8%를
점유하므로 순 소비는 분당 1.62크레딧이다.

| | |
|---|---|
| 만충에서 연속 렌더 | **178분** |
| 20분 잡 1개 | **33크레딧** |
| 만충으로 처리 가능 | **연속 8~9개** |
| 잡 1개분 재충전 | **2.75시간의 완전 유휴** |
| 고갈 시 | 0.2 vCPU로 제한 → **9.1배 느려짐** |
| 그 결과 | 93초 루트 병합이 약 850초(14.2분) → `CLIP_MEDIA_TIMEOUT` 기본 15분에 닿음 |

즉 **하루 8~9개까지는 정상이고 그 위부터 느려지다가 타임아웃으로 죽는다.** 코드에는 흔적이
남지 않는다.

**이것은 산술이지 실측이 아니다.** 12분 연속 부하로도 저하가 재현되지 않았는데, 박스가
오래 유휴여서 크레딧이 만충이었기 때문이다. 확증하려면 Lightsail 콘솔의
`BurstCapacityPercentage`·`BurstCapacityTime` 그래프를 최근 2주로 봐야 한다.

### 2-2. 소프트웨어 — 폭 1이 세 겹

| 값 | 위치 | 효과 |
|---|---|---|
| `process: make(chan struct{}, 1)` | `media/workspace.go:109` | ffmpeg·ffprobe·resvg를 **전역 동시 1개**로 직렬화 |
| `EncodeThreads: 1` | `platform/config/clip.go:12` | 인코더와 필터가 스레드 1개 |
| `WorkerConcurrency = 1` | `platform/config/config.go:120` | 잡을 한 번에 하나만 처리 |

셋 다 상수이고 환경변수가 없다. **그래서 지금 코드는 코어를 몇 개 주더라도 1개만 쓴다.**

1-2의 병렬 벤치마크가 이것을 확인한다. 프로세스를 2~3개 동시에 돌려도 2~4%밖에 빨라지지
않는다. 물리 코어가 1개라서다. **두 천장은 한 묶음으로 걷어내야 한다.** 박스를 키워도 코드가
코어를 1개만 쓰고, 코드를 병렬화해도 이 박스에는 코어가 1개뿐이다.

병렬화할 수 있는 일은 실제로 있다. 리프 렌더 N개, 레벨 0 머지 `ceil(N/6)`개,
`composition_render.go:196`의 `declaredLayer` 루프가 만드는 캡션 레이어 M개가 모두 독립이다.
루트 병합과 최종 전달 인코드는 직렬로 남으므로 그쪽은 `-threads`를 코어 수에 맞춰야 한다.

또 하나: **분석 단계가 완전히 순차다.** `generation.go:473-493`이 청크를 하나씩 돌며 매번
LLM 응답을 기다린다. 청크는 최대 49개까지 가능하다. 이건 CPU가 아니라 I/O 대기라서
병렬화해도 렌더와 코어를 다투지 않는다. 다만 "직전 호출이 유효해야 다음 유료 호출을 허용"하는
의도적 직렬화가 주석에 있으므로, 폭을 4~8로 제한하는 형태가 맞다.

### 2-3. 남은 작업 공간 문제 — 파일 한 개당 2 GiB

`media/render.go:806`이 렌더가 쓰는 **모든 출력 파일**의 상한으로 `Sources.MaxFileBytes`를
쓴다. 이 값은 원래 **사용자가 업로드하는 원본 영상 한 개**의 최대 용량(2 GiB)이고, 성격이 전혀
다른 무손실 중간 파일이 같은 자에 묶여 있다.

```go
ceiling := r.media.cfg.Sources.MaxFileBytes   // 2 GiB
limit := ceiling - diskHeadroom
args = append(args, "-fs", strconv.FormatInt(limit, 10), output)
```

`-fs`는 그 크기에 닿으면 멈추라는 지시이므로, 루트 병합이 2 GiB에 닿는 순간 중단된다.

**진단은 코드로 확증된다.** `-fs` 계열의 거절은 맨 `clip.ErrWorkspaceLimit`을 반환하는 반면
예산·디스크 계열은 `capacity.go`의 `limited()`를 거쳐 `workspaceLimit` 래퍼를 단다. 즉
**로그에 `media_limit_check`가 없다는 것 자체가 `-fs`라는 단정적 증거**다.

무손실 크기는 화면 내용에 따라 달라진다. 2026-09-15 운영 실사 측정은 46.5 MB/초였고
2026-09-18 젤리피시 샘플은 25.9 MB/초였다. 50초 클립이면 각각 2.17 GiB와 1.26 GiB라
**2 GiB 경계를 사이에 두고 갈린다.** 따라서 "50초는 구조적으로 불가"가 아니라 **"내용에 따라
불가"**가 정확한 표현이다.

---

## 3. 시간이 어디로 가는가

2026-09-15 실측 잡(결과 30초, 컷 11개, 소스 20개 합계 112초).

| 단계 | 초 | 비중 | 성격 |
|---|---|---|---|
| prepare | 285 | 23% | CPU 디코드 (소스 → 720p 분석 사본) |
| analyze | 69 | 6% | **LLM 순차 대기** — I/O이지 CPU가 아님 |
| plan | 13 | 1% | LLM |
| **render** | **857** | **70%** | CPU |
| ├ footage | 82 | | 컷별 독립 |
| ├ encode | 461 | | 머지 병렬 가능 + 루트 직렬 |
| ├ overlay | 304 | | 레이어 독립 + 합성 패스 |
| └ validate | 9 | | |
| save | 1 | | |
| **합계** | **1,225 (20.4분)** | | |

**이 측정은 패스를 줄인 T190~T196(2026-09-16) 이전이다.** 지금은 더 나을 것이므로 새 실측이
모든 추정의 선행 조건이다. 렌더가 결과 길이에 비례하므로 60초 클립이면 렌더가 두 배다.

### 레버별 누적 효과 (추정)

| 단계 | 지금 | +LLM 병렬 | +렌더 병렬·4코어 | +GPU |
|---|---|---|---|---|
| prepare | 285 | 285 | 90 | **25** |
| analyze | 69 | 12 | 12 | 12 |
| plan | 13 | 13 | 13 | 13 |
| render | 857 | 857 | 326 | **60** |
| 기타 | 10 | 10 | 10 | 10 |
| **합계** | **20.4분** | **19.6분** | **7.5분** | **2.0분** |

---

## 4. 무손실 4:4:4는 왜 필요한가 — 그리고 GPU와 어떻게 만나는가

자주 나오는 질문이라 적어 둔다. **원본은 4:2:0인데 중간 파일이 4:4:4인 이유는, 원본은 한 번만
디코딩되지만 중간 파일은 3~5번 다시 인코딩되기 때문이다.**

세대마다 4:2:0이면 색차를 줄였다 늘리는 왕복이 생기고 그 손실은 되돌릴 수 없다. 게다가 우리는
중간에 **합성 그래픽**(resvg가 그리는 캡션·플레이트·배지)을 얹는데, 날카로운 경계와 채도 높은
색은 정확히 4:2:0이 가장 티 나는 조건이다. 그래서 체인 전체를 4:4:4로 유지하다가 전달
인코드에서 딱 한 번 4:2:0으로 내린다. `media/composition.go:34-35`의 주석이 같은 말을 한다.

### GPU 지원 매트릭스 — 설계 규모를 가르는 사실

| 코덱·형식 | NVENC (인코드) | NVDEC (디코드) |
|---|---|---|
| H.264 4:4:4 무손실 | ✅ Turing+ | ❌ **4:2:0만** |
| **HEVC 4:4:4 무손실** | ✅ **Turing+** | ✅ **Turing+** |

**중간 코덱을 H.264에서 HEVC로 바꾸면 GPU가 읽고 쓰기를 모두 한다.** 4:4:4를 버릴 필요가 없고,
지금 아키텍처를 그대로 두고 인코더만 갈아끼우는 형태가 된다. 이것이 GPU 작업을 "파이프라인
재설계"에서 "코덱 교체 + 이미지 빌드"로 줄인다.

남는 미지수는 **필터**다. `scale_cuda`·`overlay_cuda`는 있으나 `xfade`는 확인이 필요하다.
GPU에 유지하지 못하면 프레임이 GPU↔시스템 메모리를 왕복해 이득이 줄어든다. **친구분 서버에서
가장 먼저 잴 것이 이것이다.**

또한 prepare 단계의 소스 디코드도 GPU가 먹는다. 사용자가 올리는 영상은 4:2:0이라 NVDEC가
그대로 읽는다. prepare가 전체의 23%이므로 이것만으로도 크다.

---

## 5. 계획

### 5-1. 순서

| # | 할 일 | 소요 | 비용 | 결과 |
|---|---|---|---|---|
| **1** | ~~웹+DB를 **Vultr CPU Optimized 2 vCPU 서울**로~~ **보류 (2026-09-18)** | 반나절 | $20/월 | 3번이 스로틀의 원인을 제거하므로 근거가 겹친다 — 7-1 |
| **2** | 새 단계별 실측 **(1과 병행)** | 반나절 | — | 3절의 추정을 숫자로 |
| **3** | **미디어 실행기**(prepare+render) 분리 → `blah-02` | 2~3주 | $0 | 계약 완성. API 박스 CPU 해방 |
| **4** | 렌더 병렬화 + LLM 관측 병렬화 | 1~2주 | — | **7.5분** |
| **5** | 진행률 + 저해상도 프리뷰 | 며칠 | — | 체감 **1.5분** |
| **6** | GPU 전환 및 검증 (`blah-02`) | 1~2주 | $0 | **가부 결정** |
| **7** | 플랫폼 결정 → **미디어 실행기만** 클라우드로 | 1~2주 | 결과에 따라 | 동시성 제한 해제 |

**핵심은 7번에서 옮기는 것이 미디어 실행기 하나뿐이라는 점이다.** 웹과 DB는 움직이지 않는다
(1번이 보류되었으므로 지금은 blah-03에 머무른다). DB 이사는 가장 위험한 종류의 이사이므로
하더라도 한 번만 한다.

~~**1번을 3번 뒤로 미루지 않는다.**~~ **2026-09-18에 뒤집혔다.** 원래 논지는 "안정성이 지금
깨져 있으므로 분리를 기다리지 말라"였는데, 깨뜨린 주체가 렌더의 버스트 크레딧 소모다. 3번이
렌더를 blah-02로 보내면 그 소모가 사라지므로 이사 자체가 필요 없어질 수 있다. **따라서 1번은
3번 뒤로 미루고, 렌더 재개 후의 메모리·swap 관측으로 판단한다.**

다만 **백업(Litestream → R2)은 1번에 묶어 두지 않는다.** 그것은 박스 교체와 독립이고 지금이
가장 위험한 항목이므로, 이사 보류와 무관하게 진행한다.

**페이즈 경계를 렌더가 아니라 "미디어 전체"로 긋는다.** 렌더만 떼면 API 박스에 prepare의
285초가 남는다. prepare까지 함께 떼면 API 박스는 LLM 조율과 DB만 남아 CPU가 거의 필요
없어지고, 계속 싸게 유지할 수 있다. 다만 prepare가 만드는 분석 청크를 LLM이 읽어야 하므로
**실행기가 청크를 R2에 올리고 API가 그것을 모델에 넘기는** 형태가 된다. 청크는 720p 60초라
작아서 전송 비용은 무시할 만하다.

### 5-2. 각 단계의 합격 기준

6번의 GPU 검증은 기준을 숫자로 못 박아야 결론이 난다.

```sh
# 0. GPU 세대 확인 — Turing 이상이어야 함
nvidia-smi --query-gpu=name,driver_version --format=csv
```

| # | 확인할 것 | 불합격이면 |
|---|---|---|
| 0 | GPU가 **Turing 이상**인가 | Pascal 이하면 HEVC 4:4:4 디코드 불가 → GPU 계획 재검토 |
| 1 | `hevc_nvenc`로 4:4:4 무손실이 실제로 써지는가 | 4:2:0 전환 검토, 화질 판단 필요 |
| 2 | **`xfade`를 GPU에 유지할 수 있는가** | 왕복 비용을 재고 이득이 남는지 판단 |
| 3 | 전 구간 벽시계가 **3배 이상** 빨라지는가 | 3배 미만이면 4번의 7.5분으로 충분, GPU 보류 |
| 4 | 화질 게이트(`composition_quality_test.go`)를 통과하는가 | 통과 못 하면 어떤 속도든 무의미 |

**3번의 3배가 판단선이다.** CPU 병렬화만으로 7.5분이 나오는데 GPU가 5분밖에 못 만들면,
플랫폼을 GCP로 좁히고 두 코드 경로를 유지할 값어치가 없다.

### 5-3. 코드 쪽 남은 작업

순서대로 적었다. 1~3은 5-1의 4번에, 4~6은 별건이다.

1. **세마포어 폭과 `EncodeThreads`·`DecodeThreads`를 환경변수로 노출.** 먼저 해야 박스를 바꿀
   때 재빌드가 필요 없고 새 박스에서 최적값을 찾을 수 있다.
2. **독립 패스 병렬화** — 리프 렌더, 레벨 0 머지, 캡션 레이어.
3. **직렬 패스의 스레드 확장** — 루트 병합과 전달 인코드의 `-threads`를 코어 수에.
4. **중간 노드 crf 0 → 12~14.** 바이트가 5~10배 줄어 2-3절의 문제가 사라지고 I/O도 준다.
   화질은 오프라인 리뷰 게이트로 확인한다.
5. **2단 머지 트리의 3세대 동시 점유 수정.** 아래 6-2 참고.
6. **파일 상한 분리** — 2-3절. 다만 **4번을 먼저 하면 필요 없어질 수 있다.**

---

## 6. 이미 고친 것과 아직 남은 것

### 6-1. 고쳐서 배포된 것

| 커밋 | 내용 |
|---|---|
| `06f5c6a6` | 리프를 루트 인코드 **전에** 해제해 무손실 3세대 동시 점유를 2세대로 내림. 삭제를 공용 머지 함수가 아니라 합성 경로의 호출 지점에 둔 것은 옳았다 — 레거시 컷 렌더 경로의 컷 파일은 오디오를 품고 있어 머지가 지우면 안 된다 |
| `238499b2` | 롤아웃의 `docker image prune -f`가 태그 붙은 이미지를 회수하지 못해 104개가 15 GB를 점유하던 문제. `-af --filter "until=24h"`로 교체. **이번 실패의 원인은 아니었다** |
| `6fc19415` | CLIP-139 이전 계약을 주장하던 `regions_test.go` 두 개가 main을 빨갛게 유지하던 것 |

관측도 보강되어, 실패 시 `media_limit_check`(request/listing/budget/prepared/disk/output)와
`media_workspace_bytes`·`media_requested_bytes`·`media_free_bytes`가 남는다. 작업 라벨도
`decode` 일괄에서 `render_cut`·`compose_video`로 갈랐다.

### 6-2. 남은 결함 — 2단 머지 트리의 3세대 점유

`06f5c6a6`의 수정은 **머지 트리가 한 단일 때만** 3세대를 막는다.

`composition.go:47-78`의 루프에서 레벨 0이 소비한 리프는 `temporary`가 아니라 그 자리에서
삭제되지 않고 트리 전체가 반환된 뒤 `removeConsumed`가 처리한다. `MergeInputs`가 6이고
`MaxCuts`가 100이므로 **컷이 37개를 넘으면 레벨이 두 단**이 되고, 레벨 1이 도는 동안 리프 세대
전체가 살아 있는 채로 레벨 0 머지와 레벨 1 머지가 공존한다. 실측 46.5 MB/초로 90초 클립이면
3세대가 12.5 GiB여서 8 GiB 예산을 확실히 넘는다.

`composition_stress_test.go:113-117`은 렌더가 끝난 뒤의 잔여물만 확인하므로 이 피크를 잡지
못한다. 수정은 레벨 0의 각 그룹이 인코딩을 마친 직후에 비임시 입력도 해제할 수 있는지를 합성
경로에서 판단해 넘기는 형태가 된다.

### 6-3. 파일 상한을 "남은 예산만큼"으로 두면 안 되는 이유

앞선 기록이 제안했던 "무손실 중간 파일의 상한을 작업 공간 예산에서 남은 만큼"은 두 가지를
잃는다.

- **선행 예약이 무력해진다.** `render.go:809`의 `capacity(ws, ceiling)`은 인코딩 시작 전에
  2 GiB를 통째로 예약해 **CPU를 쓰기 전에 싸게 거절**한다. 남은 예산으로 두면 항상 통과하는
  항등식이 되어, 거절이 100 ms 감시 루프로 밀리고 90초 인코딩을 다 태운 뒤 실패한다.
- **하류 단계의 몫이 안 남는다.** 루트 병합 다음에 그라운드 샘플링, 캡션 레이어 생성(프레임
  시퀀스는 같은 예산에 달린다), crf 20 전달 인코드가 온다.

**대신 출력 크기 로깅을 먼저 해서 예측 기반 예약(프레임 수 × 실측 바이트/초)으로 가야 한다.**
그리고 5-3의 4번(crf 12~14)을 하면 이 문제 자체가 사라질 수 있다.

---

## 7. 인프라 — 결정과 보류

### 7-1. 결정된 것

| 조각 | 선택 | 이유 |
|---|---|---|
| 웹 + DB | ~~Vultr CPU Optimized 2 vCPU/4GB 서울, $20~~ → **보류 (2026-09-18)** | 이전의 근거는 5-1의 "스로틀 실패 제거"였는데, 스로틀의 원인이 렌더의 버스트 크레딧 소모이므로 렌더가 blah-02로 빠지면 근거가 함께 사라진다. 월 $7과 $20~40의 차이이기도 하다. 다만 **"Micro로 충분"은 아직 확정하지 않는다** — 10절 |
| 미디어 실행기 (당분간) | **`blah-02`, Tailscale** | 공짜. GPU 실험실. 분리 계약을 안전하게 완성. 구성은 8절 |
| 큐의 tailnet 노출 | **`tailscale serve`** (2026-09-18 확정) | IP 바인딩은 tailscaled 장애 시 공인 IP로 열리는 fail-open이다. serve는 fail-closed다 — 8절 |
| 내구성 | **Litestream → R2** (클로즈 베타부터) | 비용 0, 코드 변경 0. R2를 이미 쓴다 |
| 엣지 | Caddy 현상 유지 | 병목이 아님 |

### 7-2. GPU 검증(5-1의 6번) 뒤로 미룬 것

미디어 실행기의 최종 목적지는 6번 결과로 갈린다.

| GPU 검증 | 목적지 | 근거 |
|---|---|---|
| **합격** | **Cloud Run GPU (L4, 서울)** | **Fargate는 GPU를 지원하지 않는다.** Cloud Run은 CPU 잡과 GPU 잡이 같은 서비스라 컨테이너와 플래그만 바뀐다. 0으로 축소, 초당 과금 |
| **불합격** | **Fargate Spot (ARM, 서울)** | 재시도 가능한 배치라 Spot 70% 할인을 그대로 먹는다. ARM 20% 추가 할인 |

**플랫폼을 두 번 만들지 않으려면 지금 정하지 않는 것이 맞다.** 계약만 전송 방식에 중립적으로
설계하면 미룰 수 있다.

### 7-3. 탈락시킨 선택지

| 후보 | 탈락 이유 |
|---|---|
| Lightsail Compute-optimized ($42) | 같은 사양에 Vultr의 2배 |
| OCI Ampere A1 춘천 ($23, 실코어 2개) | 실코어당 단가는 최저지만 arm64 이미지 작업 대비 절감폭이 작다. 같은 노력을 렌더 분리에 쓰는 편이 이득이 크다 |
| DigitalOcean ($42) / Linode ($45) | 같은 사양에 2배이고 국내 리전 없음 |
| Hetzner CCX13 (€43 EU / €54 싱가포르) | 2026-06 조정으로 전용 vCPU가 113~175% 인상. 국내 리전 없음 |
| Oracle 무료 등급 | 2026-06-15부로 2 OCPU/12 GB로 반감. **춘천은 Always Free A1 프로비저닝 불가** |
| Contabo 등 공유 vCPU 초저가 | 코어를 수 분간 100%로 붙잡는 워크로드라 지금과 같은 스로틀이 재현된다 |
| AWS Lambda | **900초 고정**(상향 불가)에 `/tmp` 10 GB. CPU 구간이 1,142초라 지금은 불가 |

---

## 8. 미디어 실행기 계약 — 지켜야 할 다섯 가지

친구분 서버에서 작업하는 동안 실행기가 그 박스에 종속되면, 7번이 결정이 아니라 **또 한 번의
이사 프로젝트**가 된다. 다섯 개만 지키면 마지막 단계가 컨테이너를 다른 곳에 띄우는 것으로
끝난다.

| 종속되기 쉬운 것 | 대신 |
|---|---|
| 로컬 디스크 경로에 소스가 있다고 가정 | **presigned URL로만** 접근 |
| DB에 직접 붙어 상태를 읽음 | **잡 명세에 다 담아** 넘김 |
| 항상 켜져 있다고 가정 | 잡을 **풀(pull)**하고 임대·타임아웃 회수 |
| GPU가 항상 있다고 가정 | 실행기가 **코덱과 필터 그래프를 스스로 고름.** 계약에는 결과물 규격만 |
| 박스에 쌓인 캐시에 의존 | 매 잡이 **깨끗한 작업 공간**에서 시작 |

### 전달 방식은 풀(pull)

```
[API 박스 · blah-03]                 [미디어 실행기 · blah-02]
  잡 큐 + SQLite                        무상태 컨테이너
     │                                        │
     │◀── GET  /internal/media/next ──────────┤
     │──── 잡 명세 + presigned GET URL ──────▶│
     │                                        │  R2에서 소스 받고
     │                                        │  ffmpeg 돌리고
     │                                        │  R2에 결과·청크 올리고
     │◀── POST /internal/media/result ────────┤
```

**API가 실행기에 도달할 필요가 없다**는 것이 핵심이다. 집 인터넷이 끊기거나 재부팅되어도 잡이
큐에 남아 있다가 실행기가 돌아오면 이어진다. 푸시였다면 API가 타임아웃을 처리해야 한다.

### Tailscale 구성 — 2026-09-18 구축 완료

계획 단계의 예시(`--hostname=media-01`, `tag:media`)가 아니라 **실제로 구축된 형태**를 적는다.
아래 표와 검증 결과는 모두 그날 실측한 것이다.

테일넷은 `blah.official0417@gmail.com` 소유이고 MagicDNS 접미사는 `tail46233e.ts.net`이다.
**ACL은 전체 허용**이라 새 노드는 붙는 즉시 모든 노드에 닿는다. SSH 정책도 모든 노드·모든
계정에 `accept`이고 재인증을 요구하는 `check`가 아니다(`tailscale debug netmap`의 `SSHPolicy`로
확인). 계획이 권고했던 `tag:media`는 **쓰이지 않았고** 모든 노드가 그 계정에 매여 있다.

| 노드 | 정체 | tailnet IP | 접속 | 역할 |
|---|---|---|---|---|
| `blah-01` | 구 teumsae | 100.84.218.63 | `ssh blah@blah-01` | design-system-platform, Ollama |
| `blah-02` | 집 서버 (Ryzen 5 5600 / 39GB / **RTX 3080 Ti**) | 100.82.44.42 | **SSH 미개방** | 렌더 워커 예정. 별도 에이전트가 담당한다 |
| `blah-03` | 이 VPS (Lightsail, 43.203.82.239) | 100.101.93.38 | `ssh ubuntu@blah-03` | postpilot API + 큐 + SQLite, cosimosi, edge-caddy |

접속에서 걸리기 쉬운 곳이 셋 있다.

- **`blah-01`에 `ubuntu` 계정은 없다.** `blah`로 붙는다. `ubuntu`로 시도하면
  `tailnet policy does not permit you to SSH as user "ubuntu"`가 돌아오는데, ACL 결함이 아니라
  계정 부재다.
- **자기 자신에게는 Tailscale SSH가 통하지 않는다.** blah-03에서 `ssh ubuntu@blah-03`을 하면
  tailnet을 타지 않고 로컬 sshd로 직행하므로 pem 키를 요구한다. 접속 검증은 **반드시 다른
  노드에서** 한다.
- **WSL2는 윈도우 호스트의 Tailscale을 타고 닿지 않는다.** NAT 구간이 tailnet으로 이어지지
  않아 100.x로 가는 TCP가 타임아웃된다. 로컬에서 직접 붙으려면 WSL 안에 tailscale을 설치해야
  하고, `systemd=true`가 이미 켜져 있어 조건은 갖췄다(2026-09-18 기준 미설치).

노드를 새로 붙일 때는 auth key를 쓰면 브라우저 로그인이 필요 없다. 관리 콘솔의
Settings → Keys → Generate auth key에서 Reusable과 Ephemeral을 모두 끈 키를 발급해
`sudo tailscale up --authkey=tskey-auth-... --hostname=<이름> --ssh`로 붙인다.
`--ssh`는 관리 경로이지 운영 경로가 아니다. 잡 전달은 위의 HTTP 계약으로 한다.

### 큐를 tailnet에만 노출하는 방법 — `tailscale serve`로 확정

blah-02의 워커가 blah-03의 큐를 폴링하려면 큐가 tailnet에서만 보여야 한다. 후보가 둘이었고
**`serve`로 확정했다.** 컨테이너는 `127.0.0.1:9000`에만 바인딩하고 tailnet 노출은 tailscaled가
맡는다.

```sh
# blah-03에서 한 번만 실행한다. 재부팅과 재배포를 넘어 유지된다
sudo tailscale serve --bg --http=9000 127.0.0.1:9000
```

| | docker가 tailnet IP에 직접 바인딩 | **`tailscale serve`** |
|---|---|---|
| compose | `"${TS_IP}:9000:9000"` | `"127.0.0.1:9000:9000"` |
| IP 관리 | 배포 스크립트가 `tailscale ip -4`를 `.env`에 주입 | **불필요** |
| tailscaled 장애 시 | `TS_IP`가 비면 `":9000:9000"` = **0.0.0.0 전체 노출 (fail-open)** | 그냥 닿지 않는다 **(fail-closed)** |
| 기동 경합 | docker가 tailscaled보다 먼저 뜨면 바인드 실패 | 없다 |

**fail-open이 기각 사유다.** `${TS_IP}`가 빈 문자열이면 compose는 오류를 내지 않고 모든
인터페이스에 바인딩한다. edge-caddy가 이미 `0.0.0.0:80/443`을 잡고 있는 박스여서 그 사고는
곧바로 공인 IP 노출이 된다. 기동 순서 문제도 같이 사라지므로 `docker.service`에
`After=tailscaled.service` drop-in을 넣거나 `ip_nonlocal_bind=1`을 켤 필요가 없다
(후자는 오타 난 IP에도 조용히 성공하는 나쁜 실패 양식이라 애초에 기각했다).

**검증 (2026-09-18, 더미 업스트림으로)**

| 경로 | 결과 |
|---|---|
| blah-01 → `http://blah-03:9000`, 업스트림 없음 | 502 — 경로는 살아 있고 업스트림만 비었다 |
| blah-01 → `http://blah-03:9000`, 업스트림 있음 | **200** |
| `http://43.203.82.239:9000` (공인 IP) | **거부** |
| 배포로 api 컨테이너가 교체된 직후 | serve 설정 **유지됨** |

마지막 행이 이 선택의 값어치를 보여 준다. tailscaled가 리스너를 소유하므로 컨테이너 교체나
재배포와 무관하게 살아남는다. 상태는 `tailscale serve status`로 보고, 되돌릴 때는
`sudo tailscale serve --http=9000 off`다.

**아직 없는 것**: 큐 API(`/internal/media/next`, `/internal/media/result`) 자체는 **미구현**이다.
`backend/`와 `proto/` 어디에도 없고 spec에도 태스크가 없다. 지금 9000을 듣는 것은 tailscaled
뿐이고 업스트림은 비어 있다. 즉 **경로만 먼저 뚫어 둔 상태**이며, 컨테이너가 `127.0.0.1:9000`에
붙는 순간 별도 조치 없이 동작한다.

### 멈출 지점

친구분 서버에서 렌더를 돌린다는 것은 **사용자가 올린 영상이 제3자 하드웨어에서 처리된다**는
뜻이다. 지금은 사용자가 본인 한 명이라 문제가 없다. **첫 외부 사용자가 들어오기 전에 7번을
끝내야 한다.** 가용성도 같은 이유로 알파까지다.

---

## 9. 비용과 과금

### 9-1. 규모별 월 비용 (컴퓨트만)

전제: 활성 사용자 1명이 하루 클립 1.5개, 클립 1개의 CPU 작업 0.57 vCPU-시(2026-09-15 잡 기준,
**과대추정 가능**), 고정 박스는 실효 사용률 60%까지만 계획.

| 일 사용자 | 월 클립 | 웹 박스 | 미디어 실행기 | 합계 |
|---|---|---|---|---|
| 10명 | 450 | $20 | 친구분 서버 $0 | **$20** |
| 50명 | 2,250 | $20 | $0 → 클라우드면 +$20 | **$20~40** |
| 100명 | 4,500 | $20 | 클라우드 +$39 | **$59** |
| 500명 | 22,500 | $40 | 클라우드 +$196 | **$236** |

클라우드 이전 후의 미디어 비용은 Fargate Spot 기준 클립당 약 $0.0087로 계산했다. Cloud Run은
인스턴스 기반 과금 무료 등급이 월 240,000 vCPU-초라 **월 100개까지 무료**다.

### 9-2. 코드가 막는 지점 — 돈으로 안 풀린다

| 천장 | 걸리는 시점 |
|---|---|
| `WorkerConcurrency = 1` | 잡당 8~10분이면 하루 144~180개, 월 4,320~5,400개. **일 사용자 100명에서 정확히 한계** |
| SQLite 단일 라이터 | API 인스턴스를 2대 이상으로 못 늘림. 100명보다 훨씬 뒤의 문제 |

**미디어를 떼어내도 `WorkerConcurrency = 1`이면 처리량이 안 는다.** 원격 실행이 끝날 때까지
워커가 붙잡혀 있기 때문이다. 5-1의 3번을 할 때 이 값도 같이 올려야 하고, SQLite 단일 라이터는
쓰기가 짧아 워커 4~8개까지는 감당한다.

### 9-3. 과금 구조와 구멍

```
클립 1회 청구 = ceil(LLM 원가 × ChargeMultiplier(3) / 크레딧) + ChargeBase(2 크레딧 = $0.02)
```

`plan.go:67-69`. 주석대로 배수는 "provider top-up fee, card fees, VAT and margin"을,
base는 "storage, database, worker"를 덮는다.

| 비용 동인 | 범위 | 회수 상태 |
|---|---|---|
| 소스 총 길이 (분석 디코드) | 최대 **49분** (`generation.go:791`의 `count > 49` 거절) | **비례 회수됨** — 청크 수가 곧 LLM 호출 수 |
| 결과 길이 (렌더 패스) | 15~90초 | 평평하지만 **충분** |
| **재렌더 횟수** | **무제한** | **회수 0** |

**`StartRender`(`rerender.go:106`)에 크레딧 차감도 쿼터도 레이트 리밋도 없다.** 애플리케이션
레벨 레이트 리미터 자체가 코드베이스에 없다. 재렌더는 LLM을 부르지 않으므로 청구가 0인데
**CPU 작업의 75%를 다시 쓴다.**

이것은 우연이 아니라 명시된 제품 결정이다. `sequence_cost.go:14-17`이
**CLIP-19, CLIP-20: rendering itself costs no credits**를 적어 두었고, 상한을 어디서 볼지는
**CLIP-145로 열려 있다.** 같은 주석이 `SequenceCaptionCost`를 "그런 상한이 필요한 숫자들이
쌓이는 표면"이라고 부른다.

**권고: 프로젝트당 무료 재렌더 N회(2~3 적당), 초과분만 과금.** CLIP-19/20의 취지를 지키면서
노출에 뚜껑을 덮고, CLIP-145가 열어 둔 질문에 답하는 형태다. 기획 변경이므로 `update-ssot` →
`create-task` 순서를 따른다. **오픈 베타 전에 닫는다.**

---

## 10. 아직 확인하지 못한 것

| 항목 | 어떻게 확정하나 |
|---|---|
| 버스트 크레딧 고갈이 실제로 일어났는가 | Lightsail 콘솔의 `BurstCapacityPercentage` 그래프 최근 2주 |
| 과거 잡의 단계별 소요 시간 분산 | 운영 DB 조회가 권한 정책(Production Reads)에 막혀 있음. 컨테이너 로그는 재생성으로 소실 — **2026-09-18에 실증됨**: `deploy-backend`가 돌 때마다 컨테이너가 교체되어 그 이전 로그가 통째로 사라진다. 그날 02:26과 07:52 두 번의 배포로 로그가 두 번 비었고, `docker logs --since 168h`를 줘도 남는 것은 마지막 배포 이후분뿐이다 |
| **blah-03이 렌더 없이 Lightsail Micro로 충분한가** | 렌더 재개 후 메모리·swap을 관측해 판단한다. 2026-09-18 관측값은 컨테이너 4개 합계 99.8 MiB, available 469 MB이지만 **렌더가 한 건도 없던 기간의 값이라 API 부하 데이터가 아니다.** `uptime`의 load average는 직전 1·5·15분 평균일 뿐이므로 "장기간 0.00"의 근거로 쓸 수 없다 |
| T190~T196 이후의 진짜 단계별 시간 | **5-1의 2번.** 모든 추정의 선행 조건 |
| 후보 박스의 실제 코어 성능 | 벤치마크 스크립트를 후보에서 한 시간씩 실행. **$0.2 미만** |
| `xfade`를 GPU에 유지할 수 있는가 | 친구분 서버, 5-2의 2번 |
| Cloud Run 서울(Tier 2) 무료 등급 초과 단가 | 가격 계산기 |
| Fargate 서울 단가 | us-east-1의 $0.04048/vCPU-시에 리전 계수 1.1을 곱한 **추정**. ARM 20%·Spot 70% 할인은 확인된 수치 |

---

## 11. 곁가지

- **`TestRenderSmoke`의 `vertical`·`horizontal`은 불안정하다.** 6회 교차 실행에서 수정본
  1승 2패, HEAD 0승 3패로 구별되지 않았다. 메모리 1 GB에 2 CPU로 묶인 테스트라 호스트가
  바쁘면 깨진다. CI에서는 통과하므로 로컬 부하 문제다.
- **`internal/clip/store`는 로컬에서 618~759초 걸린다.** `go test` 기본 타임아웃 600초를
  넘기므로 `go test ./internal/clip/store/ -timeout 1200s`로 따로 돌린다.
- **배포는 CI와 연동되어 있지 않다.** `deploy-backend.yml`이 `push`에 직접 걸려 있고 이미지도
  `deployable` 타깃이라 미디어 스모크를 거치지 않는다. 스모크는 `smoke` 잡에서 배포와 나란히
  돈다(ARCH-38). **CI가 빨간 상태에서도 배포가 나갈 수 있다.**

---

## 12. 다시 확인하는 방법

```sh
# 운영 접속 — pem 경로 (tailnet 밖에서)
ssh -i ~/.ssh/lightsail-cosimosi.pem ubuntu@43.203.82.239

# tailnet 안이라면 (8절)
ssh ubuntu@blah-03      # blah-01에서는 ssh blah@blah-01

# tailnet 상태와 큐 노출
tailscale status                 # 노드 목록과 온라인 여부
tailscale serve status           # 9000 프록시 설정
sudo tailscale debug netmap | python3 -c 'import json,sys; print(json.dumps(json.load(sys.stdin)["SSHPolicy"], indent=1))'

# 큐 경로가 tailnet에서만 열려 있는지 — 반드시 다른 노드에서 (자기 자신은 tailnet을 안 탄다)
ssh blah@blah-01 'curl -s -o /dev/null -w "%{http_code}\n" http://blah-03:9000/'   # 업스트림 없으면 502
curl -s -o /dev/null -w "%{http_code}\n" http://43.203.82.239:9000/                 # 반드시 거부

# 디스크와 이미지
df -h / ; docker system df

# 인스턴스 정체
TOK=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" \
  -H "X-aws-ec2-metadata-token-ttl-seconds: 60")
curl -s -H "X-aws-ec2-metadata-token: $TOK" \
  http://169.254.169.254/latest/meta-data/instance-type

# 실패한 잡의 전체 궤적 (컨테이너 재생성 전까지만 남는다)
docker logs --since 3h postpilot-prod-api-1 2>&1 | grep "<job id>"

# 실패 요약만
docker logs --since 3h postpilot-prod-api-1 2>&1 \
  | grep -E "job failed|workspace_limit|clip job stopped"
```

`job failed` 라인에 `media_limit_check`가 있으면 예산·디스크 계열이고, 없으면 2-3절의 파일
상한 계열이다.

단계별·명령별 소요 시간은 `clip media operation` 로그에 스테이지 라벨과 함께 남는다. 회귀
확인은 `docker build -f backend/Dockerfile --target identity-smoke` 후 `docker run`으로
전달 클립 지문을 비교한다.
