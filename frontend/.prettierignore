dist/

# buf output. CI gates it with `git diff --exit-code` right after `pnpm gen:proto`, so a
# formatting pass here would put the tree permanently at odds with regeneration.
src/shared/api/gen/

# Written by the Go design tests (UPDATE_REGION_LAYOUTS=1) and compared byte for byte with
# backend/internal/clip/design/testdata, so it keeps the encoder's own formatting.
src/entities/clip-design/model/region-layouts.fixture.json
