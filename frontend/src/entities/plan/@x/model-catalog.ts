// What the model-catalog entity may import from the plan entity: an estimator combo decides the
// rates a plan's post counts are derived from, so assigning one stales the plan read.
export { myPlanQueryKey } from '../api/useMyPlan'
