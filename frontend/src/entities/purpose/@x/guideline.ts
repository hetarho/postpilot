// What the guideline entity may import from the purpose entity: a guideline's scope names
// purposes, so the scope control needs the account's directory to offer and label them. It
// never reads a brief — genre and required content stay purpose-owned.
export type { Purpose } from '../model/types'
export { usePurposes } from '../api/usePurposes'
