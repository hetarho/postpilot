/** The clip design system and the composition grammar's bounds: the vocabulary every clip
 *  noun reads and none of them owns (CDS, ARCH-21). It is the leaf of the clip `@x` graph. */
export { CLIP_BROWSER_RENDER } from './config/clip-browser-render'
export {
  CLIP_CAPTION_PLACEMENT,
  CLIP_COMPOSITION_LIMITS,
  CLIP_DRAFT_PREVIEW,
  CLIP_REVISION,
  CLIP_TIMELINE,
} from './config/clip-composition'
export {
  CLIP_CAPTION_STYLES,
  CLIP_DEFAULT_CAPTION_STYLE,
  CLIP_DEFAULT_REGION_PRESETS,
  CLIP_DESIGN,
  CLIP_RAPID,
  CLIP_TRANSITION,
  clipCaptionSizes,
} from './config/clip-design'
export {
  CLIP_SOURCE_CONTAINERS,
  CLIP_SOURCE_FINGERPRINT_CHUNK_BYTES,
  CLIP_SOURCE_MAX_BATCH_BYTES,
  CLIP_SOURCE_MAX_COUNT,
  CLIP_SOURCE_MAX_DURATION_MS,
  CLIP_SOURCE_MAX_FILENAME_CHARS,
  CLIP_SOURCE_MAX_FILE_BYTES,
} from './config/index'
