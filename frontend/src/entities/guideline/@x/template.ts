// What the template entity may import from the guideline entity: a guideline's scope names
// templates, so a template rename or delete changes the chips every scope renders.
export { invalidateGuidelines } from '../api/guideline-cache'
