// What the guideline entity may import from the template entity: a guideline's scope names
// templates, so the scope control needs the account's directory to offer and label them. It
// never reads a brief — genre and required content stay template-owned.
export type { Template } from '../model/types'
export { useTemplates } from '../api/useTemplates'
