// What the post entity may import from the template entity: a post names at most one template.
export type { TemplateRef } from '../model/types'
export { emptyTemplateRef } from '../model/types'
export { toTemplateRef } from '../api/template-queries'
