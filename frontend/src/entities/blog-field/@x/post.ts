// What the post entity may import from the blog-field entity: a post names at most one 분야, read
// through the refusing mapper and written with the options save.
export type { BlogFieldChoice } from '../model/blog-field'
export { blogFieldToProto, requireBlogField } from '../api/blog-field-mappers'
