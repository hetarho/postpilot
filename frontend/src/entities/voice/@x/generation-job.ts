// What the generation-job entity may import from the voice entity: a revision may save its
// instruction as a voice rule, which republishes that one voice's profile.
export { voiceProfileQueryKey } from '../api/voice-queries'
