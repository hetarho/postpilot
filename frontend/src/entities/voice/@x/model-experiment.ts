// What the model-experiment entity may import from the voice entity: an applied analyze winner
// publishes a new profile head for the experiment's frozen voice, and no other.
export { voiceProfileQueryKey, voiceVersionsQueryKey } from '../api/voice-queries'
