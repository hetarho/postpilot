// What the post entity may import from the voice entity: a post names the voice it is written in.
export type { VoiceRef } from '../model/types'
export { voiceRefLabel } from '../model/types'
export { toVoiceRef } from '../api/voice-queries'
