/** What `clip-observation` exposes to `clip-project` (ARCH-13 @x). */
export { toClipAttemptInspection } from '../api/attempt-inspection'
export { toClipObservations } from '../api/observations'
export type { ClipAttemptInspection, ClipObservations } from '../model/observations'
