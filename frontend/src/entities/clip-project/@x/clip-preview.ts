/** What `clip-project` exposes to `clip-preview` (ARCH-13 @x). */
export type { ClipRatio } from '../model/types'
export { toClipProject } from '../api/clip-project'
export type { ClipProject } from '../model/types'
