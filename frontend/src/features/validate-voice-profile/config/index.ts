/** How many posts a validation round asks for. Mirrored from the voice context's
 *  personalization thresholds; no interval is present because personalization work is never
 *  scheduled or started by a mount. */
export const VOICE_VALIDATION_POST_COUNT = 3
