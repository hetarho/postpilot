{
  "type": "object",
  "additionalProperties": false,
  "required": ["candidates"],
  "properties": {
    "candidates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["text", "kind", "tags"],
        "properties": {
          "text": {"type": "string"},
          "kind": {
            "type": "string",
            "enum": ["preference", "persona", "place", "person", "history"]
          },
          "tags": {
            "type": "array",
            "items": {"type": "string"}
          }
        }
      }
    }
  }
}
