# 클립 렌더가 "임시 공간 부족"으로 멈추는 문제 — 2026-09-17 조사 기록

운영에서 클립 생성이 렌더 단계에서 반복해 중단된 건을 조사한 기록이다. 무엇이 확정됐고
무엇이 아직 추정인지, 무엇을 고쳤고 무엇이 남았는지를 구분해 적었다.

---

## 1. 증상

사용자 화면에 이렇게 뜬다.

> 영상 처리에 필요한 임시 공간이 부족해 중단했어요. 이전 결과는 보존돼요. 잠시 후 다시 시도해 주세요.

프론트의 `CLIP_WORKSPACE_LIMIT` 문구다. 분석과 편집안은 모두 완료된 뒤 **렌더 단계에서만**
멈춘다. 같은 잡을 세 번 시도했고 세 번 다 같은 자리에서 멈췄다.

| 잡 | 시각(UTC) | 멈춘 지점 | 코드 상태 |
|---|---|---|---|
| `4926f4b5` | 05:49:08 | 루트 병합 93.7초 | 수정 전 |
| `9cef8854` | 07:02:26 | 루트 병합 95.1초 | 수정 전 |
| `d34c342a` | 08:38:16 | 루트 병합 93.4초 | **수정 후** |

대상 잡은 컷 16개, 목표 길이 50초, 세로 1080×1920이다.

---

## 2. "임시 공간"의 정체

사용자의 저장 공간이나 크레딧이 아니라 **서버가 합성 중에만 쓰는 작업 디렉터리**다.
`CLIP_WORK_ROOT`(`/tmp/postpilot-clip-work`)이고 API 컨테이너 안에 있으므로 실제로는
Lightsail 박스의 38 GB 디스크를 나눠 쓴다. 잡마다 `postpilot-clip-<16진수>` 디렉터리가
만들어지고 끝나면 지워진다.

여기에 걸린 상한은 세 겹이다.

| 상한 | 값 | 정의 위치 |
|---|---|---|
| 잡 하나가 쓸 수 있는 디렉터리 총량 | 8 GiB (`WorkspaceMaxBytes`) | `config/clip.go:14` |
| 렌더가 쓰는 **파일 한 개**의 크기 | 2 GiB (`Sources.MaxFileBytes`) | `config/clip.go:34` |
| 실행 중 남겨 둘 디스크 여유 | 8 MiB (`diskHeadroom`) | `media/capacity.go` |

세 상한이 모두 같은 에러(`CLIP_WORKSPACE_LIMIT`)를 내기 때문에, 조사 초기에는 어느 것이
걸렸는지 로그로 구분할 수 없었다. 이 점은 아래 5절에서 고쳤다.

---

## 3. 왜 이렇게 큰가 — 무손실 중간 파일

합성 렌더는 영상을 세 세대로 만든다.

```
리프  bare-0000.mp4 …      (컷 수만큼)   무손실 H.264 4:4:4
머지  compose-00-0000.mp4 … (6개씩 묶어)  무손실 H.264 4:4:4
루트  composition-footage.mp4            무손실 H.264 4:4:4
```

중간 노드를 무손실(`-crf 0 -preset ultrafast`)로 두는 것은 의도된 선택이다. 1코어 박스에서
CPU를 아끼려고 디스크를 쓰는 교환이고, `media/render.go`의 주석에 그렇게 적혀 있다. 대신
용량이 크다. 1080×1920 30fps yuv444p의 원본 화소만 초당 186 MB이고, 무손실 압축 후에도
수십 MB/초가 남는다.

---

## 4. 고친 것 (배포 완료)

세 커밋 모두 main에 있고 운영에서 가동 중이다.

### `06f5c6a6` 무손실 3세대 동시 점유 해소

리프가 **루트 인코드가 끝난 뒤에야** 삭제되고 있었다. 그래서 루트를 쓰는 동안 리프 16개와
머지 3개와 커지는 루트가 한 디렉터리에 공존했다. 한 클립의 무손실 사본 세 벌이 8 GiB 안에
들어가야 했던 셈이다.

머지 트리가 다 읽은 리프를 **루트 인코드 전에** 해제하도록 바꿨다. 피크가 3세대에서 2세대로
내려갔다.

삭제를 공용 머지 함수 안에 넣으려다 기존 테스트에 막혔는데, 그 판단이 옳았다. 그 함수는
레거시 컷 렌더 경로와 공유되고 그쪽 컷 파일은 **오디오를 품고 있어** 머지가 지우면 안 된다.
그래서 삭제를 합성 경로의 호출 지점으로 옮겼다. 합성 경로의 리프는 `-an`으로 인코딩된 무음
영상이고 소리는 별도 `audio-*.wav`에 있다.

### `238499b2` 배포가 이미지를 쌓던 문제

롤아웃 스크립트가 `docker image prune -f`를 돌고 있었다. `-a`가 없으면 태그 없는 레이어만
지우는데, 배포는 매번 `postpilot-api:<sha>`라는 **태그 붙은** 이미지를 받으므로 아무것도
회수하지 못했다. 104개가 쌓여 38 GB 중 15 GB를 점유하고 있었다.

`docker image prune -af --filter "until=24h"`로 바꿨다. 롤백용으로 하루치는 남긴다.
정리 후 디스크는 23 GB 사용에서 8 GB로, 이미지는 104개에서 10개로 줄었다.

**다만 이것은 이번 실패의 원인이 아니었다.** 디스크가 30 GB 비어 있는 상태에서도 같은 자리에서
멈추는 것을 확인했다. 필요한 정리였지만 별개 문제다.

### `6fc19415` CI를 깨뜨리던 리전 테스트

조사 중 main이 빨간 상태인 것을 발견했다. `regions_test.go`의 테스트 두 개가 CLIP-139 이전
계약(문서가 프리셋을 정한다)을 주장하고 있었는데, 프리셋이 프로젝트로 옮겨간 뒤로 계속 실패하고
있었다. T219 세션이 이미 같은 수정을 작업 트리에 해 둔 상태여서 그것을 검증하고 커밋했다.

---

## 5. 관측 보강 (배포 완료)

세 상한이 한 에러를 내는 문제를 고쳤다. 이제 실패 시 다음이 로그에 남는다.

- `media_limit_check` — `request` / `listing` / `budget` / `prepared` / `disk` / `output`
- `media_workspace_bytes` / `media_requested_bytes` / `media_free_bytes`

작업 라벨도 고쳤다. 합성 렌더의 리프와 루트가 둘 다 `decode`로 기록되고 있었다. `decode`는
가장 일을 적게 하는 명령에 붙는 이름인데, 정작 렌더 시간을 지배하는 두 패스가 그 이름을
달고 있었다. 이제 각각 `render_cut`과 `compose_video`다.

이 보강이 곧바로 값을 했다. 세 번째 실패(`d34c342a`)에서 **`media_limit_check`가 로그에
없다는 사실 자체가** 원인을 좁혀 줬다. 예산 계열 거절에는 전부 수치를 붙여 두었으므로,
필드가 비어 있다는 것은 예산이 아닌 다른 경로에서 났다는 뜻이기 때문이다.

---

## 6. 지금 막고 있는 것 — 파일 한 개당 2 GiB

`media/render.go:806`이 렌더가 쓰는 **모든 출력 파일**의 상한으로
`Sources.MaxFileBytes`를 쓴다.

```go
ceiling := r.media.cfg.Sources.MaxFileBytes   // 2 GiB
limit := ceiling - diskHeadroom
args = append(args, "-fs", strconv.FormatInt(limit, 10), output)
```

`ClipSourceFileBytes = 2 GiB`는 원래 **사용자가 업로드하는 원본 영상 한 개**의 최대 용량이다.
성격이 전혀 다른 무손실 중간 파일이 같은 자에 묶여 있다. `-fs`는 ffmpeg에게 그 크기에 닿으면
멈추라는 지시이므로, 루트 병합이 2 GiB에 닿는 순간 중단된다.

```
머지 3개 : 각 약 17~19초 분량 → 각 2 GiB 미만 → 통과
루트     : 50초 전부        → 2 GiB 초과     → -fs로 정지
```

즉 **50초 클립의 루트 병합은 구조적으로 성공할 수 없다.** 4절의 수정으로 8 GiB 예산에는 더
이상 걸리지 않게 됐고, 그 아래 숨어 있던 이 상한이 드러난 것이다. 두 상한이 공교롭게 거의 같은
지점에 있어서 이전 로그만으로는 구분되지 않았다.

---

## 7. 측정값 — 확정된 것과 아닌 것

이 구분이 중요하다. 조사 중 내가 한 번 섞어서 말한 적이 있다.

| 값 | 성격 | 출처 |
|---|---|---|
| **46.5 MB/초** (372 Mbps) | **실측** | 2026-09-15 운영 박스에서 실사 영상으로 직접 측정 |
| 67~76 MB/초 | **추정** | 이번 로그의 시간만으로 역산. "인코딩 1초에 출력 몇 초"라는 또 다른 미지수에 의존 |

무손실 크기는 화면 내용에 따라 달라지므로 두 값 모두 틀릴 수 있다. 다만 **결론은 갈리지 않는다.**

50초 클립의 최대 점유량(머지 세대 + 루트 세대):

| 기준 | 한 세대 | 피크 | 8 GiB 예산 |
|---|---|---|---|
| 실측 46.5 MB/초 | 2.33 GiB | 4.66 GiB | 여유 3.3 GiB |
| 추정 상단 76 MB/초 | 3.8 GiB | 7.6 GiB | 여유 0.4 GiB |

**추정이 가장 나쁜 쪽으로 틀려도 8 GiB 안에 들어간다.** 따라서 작업 공간 예산을 올릴 필요가
없다. 조사 중간에 "16 GiB로 올리자"고 제안했던 것은 추정치를 기준으로 한 것이었고 철회한다.
38 GB 박스를 cosimosi와 나눠 쓰는 상황에서 한 잡의 몫을 두 배로 키우는 것은 좋지 않다.

---

## 8. 다음에 할 일 (아직 미구현, 승인 대기)

### (1) 파일 상한 분리

| 파일 | 지금 | 바꿀 값 |
|---|---|---|
| 전달물 `clip-result.mp4` (crf 20, 실제 수십 MB) | 2 GiB | 2 GiB 유지 |
| 무손실 중간 파일 | 2 GiB | 작업 공간 예산에서 남은 만큼 |

안전장치를 없애는 것이 아니라 **맞는 자 하나만 남기는 것**이다. 디스크 보호는 8 GiB 예산이
계속 담당한다. 이것만으로 50초가 통과할 것으로 본다.

### (2) 출력 파일 크기 로깅

렌더 명령이 만든 파일의 크기를 `clip media operation` 라인에 남긴다. 그러면 다음 실행에서
무손실 초당 바이트가 **실측으로 확정된다.** 7절의 46.5와 76 사이에서 헤매는 상황이 반복되지
않고, 아래 90초 문제를 근거 있게 정할 수 있다.

### (3) 90초는 별도 결정

제품 상한은 90초(`ClipMaxDurationMS`)인데, 실측 46.5 MB/초로도 2세대가 8.4 GiB로 8 GiB를
넘는다. 즉 (1)을 해도 90초는 막힌다. 두 갈래가 있다.

- **중간 노드를 시각적 무손실로** (crf 0 → 12~14). 바이트가 5~10배 줄어 90초도 여유롭다.
  대신 리프·머지·루트·전달로 재인코드가 4세대 쌓이므로 화질 확인이 필요하다.
  `media/composition_quality_test.go`의 오프라인 리뷰 게이트가 그 수단이다.
- **예산 상향**. 값싸지만 동시 잡이 늘면 되돌아온다. 박스 사정상 권하지 않는다.

(2)의 실측이 나온 뒤에 정하는 것이 맞다.

---

## 9. 조사 중 확인된 곁가지

- **`TestRenderSmoke`의 `vertical`·`horizontal`은 불안정하다.** 같은 이미지가 통과했다
  실패했다 한다. 6회 교차 실행에서 수정본 1승 2패, HEAD 0승 3패로 구별되지 않았다. 메모리
  1 GB에 2 CPU로 묶인 테스트라 호스트가 바쁘면 깨진다. CI에서는 통과했으므로 로컬 부하 문제다.
- **`internal/clip/store` 패키지는 로컬에서 618초 걸린다.** `go test` 기본 타임아웃 600초를
  간신히 넘긴다. CI 러너에서는 통과했지만 경계선이다. 언젠가 다시 물릴 수 있다.
- **배포는 CI와 연동되어 있지 않다.** `deploy-backend.yml`이 `push`에 직접 걸려 있고 이미지도
  `deployable` 타깃으로 빌드하므로 미디어 스모크를 거치지 않는다. 스모크는 `smoke` 잡에서
  배포와 나란히 돈다(ARCH-38). CI가 빨간 상태에서도 배포가 나갈 수 있다는 뜻이다.

---

## 10. 다시 확인하는 방법

```sh
# 운영 접속
ssh -i ~/.ssh/lightsail-cosimosi.pem ubuntu@43.203.82.239

# 디스크와 이미지
df -h / ; docker system df

# 실패한 잡의 전체 궤적
docker logs --since 3h postpilot-prod-api-1 2>&1 | grep "<job id>"

# 실패 요약만
docker logs --since 3h postpilot-prod-api-1 2>&1 \
  | grep -E "job failed|workspace_limit|clip job stopped"
```

`job failed` 라인에 `media_limit_check`가 있으면 예산·디스크 계열이고, 없으면 6절의 파일
상한 계열이다.
