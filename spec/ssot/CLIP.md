# CLIP generated video projects and templates
> r12 | Account-owned footage becomes a downloadable clip through bounded analysis, template-authored content, scene-grounded copy and preview-led timeline correction, independently of blog posts.

## decisions
- CLIP-1 [o] a clip project is independent of a post and owns its title, chosen video template, template answers, target duration, aspect ratio, analysis, edit plan and latest successful result
- CLIP-2 [o] only the authenticated owner may list, view, change, generate, cancel, finalize, download or delete a clip project or video template; an unknown or foreign id is presented as not found
- CLIP-3 [o] navigation groups 글 · 말투 · 글 템플릿 · 지침 under 글 and 클립 · 영상 템플릿 under 영상; account, plan, billing and administration remain common destinations
- CLIP-4 [o] a video template is a reusable account-owned composition with authored footage sections, generated and fixed text, information fields, repeated item groups, copy guidance, approved visual styles and independent caption pace; category presets do not govern its content
- CLIP-5 [o] every generation selects exactly one valid video template and collects only the information that template explicitly marks required before it can start
- CLIP-6 [o] a project accepts up to 20 source videos whose combined duration is at most 30 minutes
- CLIP-7 [o] the owner chooses a target result duration from 15 to 90 seconds before generation; fresh composition first reconciles the timeline to that target within observed footage, but may deliver a shorter result of at least 15 seconds when the selected footage cannot reach it; source identity, observed-range boundaries and the minimum result duration remain hard limits
- CLIP-8 [o] the owner chooses one project ratio at creation: 세로 9:16, 가로 16:9 or 정방형 1:1
- CLIP-9 [o] a project's ratio is fixed; producing another ratio starts another project ← reframing after cut and copy placement would make the approved composition unreliable
- CLIP-10 [o] generation analyzes each source for usable time ranges, events, visual subjects, quality and audible speech
- CLIP-11 [o] generation follows the template narrative, selects observed source ranges, associates relevant facts with each selected subject and writes copy grounded in those ranges before applying visual styles (→CLIP-61 →CLIP-63 →CLIP-64)
- CLIP-12 [o] generated copy is rendered as exact typeset text through approved visual templates ← AI-drawn Korean lettering can be misspelled or visually inconsistent
- CLIP-13 [o] the copy renderer uses only product-bundled, fixed-version fonts — Pretendard Variable as the primary face, Paperlogy for hook and emphasis text, Noto Sans KR as the fallback (→CDS-17) — and never a device or host system font ← the same project must render identically across environments
- CLIP-14 [o] the copy styles are the five CDS defines — 깔끔하게 · 메모 · 크게 강조 · 형광펜 · 가벼운 텍스트 (→CDS-22) — with one project accent from the approved seven-colour palette (→CDS-15)
- CLIP-15 [o] template-authored text uses the CDS safe areas and supported anchors, with header-role elements using the symmetric header bounds; automatic placement applies only where the template leaves placement automatic (→CDS-9 →CDS-12 →CDS-13 →CDS-57)
- CLIP-16 [o] cuts join with a hard cut by default and a short fade only where the scene type changes (→CDS-36)
- CLIP-17 [o] step ② 클립 다듬기 is a preview-led timeline editor for cut order and range, generated or fixed text, approved placement and style, phrase timing and original-audio volume (→CLIP-53 →CLIP-54 →CLIP-55)
- CLIP-18 [o] original audio is retained by default, volume is controlled per cut, and the whole track is loudness-normalised at render (→CDS-35)
- CLIP-19 [o] AI analysis and AI composition regeneration consume the account's existing credits within the maximum shown and approved before generation under QUOTA-45; preparation verifies sources and prepares bounded analysis copies before reserving the complete AI run under QUOTA-43, and explicit cancellation settles under QUOTA-49 through QUOTA-52; no AI call may precede a successful reservation
- CLIP-20 [o] manual corrections, rerendering the corrected plan, preview, download and finalization consume no additional credits; cancelling a manual rerender is also credit-free
- CLIP-21 [o] unfinalized projects retain private source originals for 24 hours under CLIP-73; analysis copies and local worker files remain attempt-scoped, while metadata, analysis, the edit plan and latest successful result remain until project deletion
- CLIP-22 [o] before source selection, the upload surface discloses external analysis of compressed footage and audible speech through OpenRouter, the 24-hour original-retention rule and original deletion on finalization; service cleanup makes no promise about external-provider retention
- CLIP-23 [o] while retained originals remain available, refresh, reopening, AI retry and manual rerender reuse matching originals without reselection; after expiry or missing media, only operations needing those pixels request matching originals again and never start automatically
- CLIP-24 [o] deleting a project removes its originals, result, analysis, edit plan and metadata, with object cleanup recoverable after interruption
- CLIP-25 [o] deleting a video template detaches it from projects while preserving their answers, analysis, resolved composition, edit plan and result; manual correction uses that retained composition, while another AI generation requires a selected valid template
- CLIP-26 [o] a failed or cancelled attempt preserves the project and previous successful result, reports its stage and settlement, and offers explicit retry using retained originals; AI retry requires a newly approved ceiling, manual rerender stays credit-free, and unavailable originals alone require reselection
- CLIP-27 [o] the result is previewable and downloadable in a form accepted by Naver Clip for the selected ratio; the product does not submit it to Naver
- CLIP-28 [x] direct Naver publishing, generated source footage, AI-drawn copy images, per-project fonts, arbitrary decorations, automatic background music, custom transitions and advanced motion templates — out of scope; the disclosure badge, information chips, hook and ending cards and loudness normalisation are in scope under CDS and are not decorations
- CLIP-29 [o] analysis sends only bounded compressed copies directly through the existing OpenRouter connection as inline video data, not a signed object URL or the original file; each copy covers at most 60 seconds, has a long edge at most 720 pixels and occupies at most 8 MiB before encoding ← an input modality flag alone does not establish signed-video-URL compatibility
- CLIP-30 [o] clip-analysis model admission is vendor-neutral: every registered observe model whose current catalog entry advertises video input is evaluated, and generation enables it only when a current OpenRouter endpoint can accept the bounded inline MP4 with every required request parameter and a complete price ceiling; no provider slug or model family allowlist may decide eligibility, and incompatibility is refused before credit approval or a model call; the quote, admission and each completion recheck read the live endpoint contract rather than relying on a previously cached eligibility result
- CLIP-31 [o] the composition model receives only bounded structured source analysis, template guidance and answers, never video bytes or video URLs; final rendering uses the original footage and audio, not the compressed analysis copies
- CLIP-32 [o] preparation verifies every source and every analysis copy before the first AI call; bounded recompression may occur only during preparation, and a copy still exceeding a limit fails without AI rather than silently increasing the chunk count, omitting footage or changing its timing
- CLIP-33 [o] clip media work runs one job at a time on the shared service and processes originals sequentially through bounded temporary disk storage; source verification and analysis-copy preparation share the same source read, analysis copies are not uploaded back to object storage, and transmission never accumulates whole original files or all encoded copies in memory
- CLIP-34 [o] local source previews survive attempt completion while the working page still uses them; leaving or refreshing releases local references without stopping a server job, and reopening obtains authorized playback access to unexpired retained originals without persisting media in browser storage
- CLIP-35 [x] reusing source analysis for a separate composition-only AI regeneration action — deferred; manual correction and credit-free rerendering under CLIP-20 remain available
- CLIP-36 [o] `/clips/$clipId` has three steps 클립 생성 · 클립 다듬기 · 클립 완성 under THEME-39: no editing state opens ①, an unfinalized editing state opens ② even when its render matches, and explicit finalization opens ③; ① contains settings/sources/models, ② correction with preview and download, and ③ only the retained result preview/download; step selection never finalizes, and finalized projects cannot reopen editing; panels share one mounted page and `/clips/new` has no step bar
- CLIP-37 [o] the top row is the editor's (→POST-45): a 목록 link, the one status line and 삭제하기 in that order, wrapping when a refusal needs the width; the `display` project-title heading and the ghost-styled back button are gone — the project's name is ①'s 제목 field, and a screen carries exactly one title — and 삭제하기 leaves the settings form
- CLIP-38 [o] outside the focused job view, one status region prioritizes failing autosave, upload phase, unsaved/unrendered correction, save state and project state, with one `top-chrome` progress bar for upload; while a job is active CLIP-78 supplies the sole progress/status region instead (→THEME-40)
- CLIP-39 [o] the settings autosave on a pause as a draft's title and memo do (→POST-4): no 저장 button, no dirty gate on 생성 and no leave-confirmation dialog, and the approval action flushes the queue before it starts; `/clips/new` keeps exactly one committing action, 클립 만들기, in its dock ← the ratio it carries can never be changed again (CLIP-9), so the project may not be minted by an incidental first keystroke
- CLIP-40 [o] each editable step has one ActionBar: ① 생성 with model refusals and the approved ceiling; ② primary 확정하기 with its refusal, alongside 다시 렌더 and download of the identified latest successful result; ③ offers result download only; the template and model selectors remain in ①, and the focused job view replaces these controls with cancellation (→CLIP-75 →CLIP-78)
- CLIP-41 [o] `/clips` uses whole-row links, title, one status badge, template/ratio metadata, relative updated time, URL-backed title search/status filtering and one docked 새 클립 under POST-43 and POST-65 through POST-69; badges prioritize active work 생성 중/취소 중, failure 실패, cancellation 취소됨, then 초안/다듬는 중/완성, with 완성 reserved for explicit finalization and automatic polling until the latest job is terminal
- CLIP-42 [o] the video-template list and editor follow the post-template surfaces, with one saved draft shared by a composition editor and 원문 and one docked save; composition-specific controls follow CLIP-58 through CLIP-67
- CLIP-43 [o] analysis sends the prepared bounded copy as one pass, selects `static` processing only for an endpoint that supports it and otherwise omits provider-specific video-processing controls; adaptive video exploration is excluded
- CLIP-44 [o] the clip analysis-model picker shows every registered observe model, enables those currently eligible under CLIP-30 and gives each disabled model one stable reason: video input absent · compatible inline endpoint absent · required request parameters absent · complete price ceiling unavailable

- CLIP-45 [o] caption pace is independent of typography: templates choose 문장형 (default, including older templates) or 빠른 구절형; generated rapid copy is split into sequential short phrases without an extra AI call, and step ② lets the owner change pace and edit individual phrases and times; this is editorial timing, not forced alignment to recorded speech

- CLIP-46 [o] campaign disclosure is a template-authored fixed or answer-bound text element with editable content, visibility, position and timing; project creation and generation require no separate product-owned campaign-type selection (→CLIP-65 →CLIP-68)

- CLIP-47 [o] source selection uses a horizontal snapping strip with thumbnail, filename, duration and per-source upload state; selecting a source opens one larger preview from a matching local file or an authorized retained original, and progress follows CLIP-38 or CLIP-78 (→THEME-25)
- CLIP-48 [o] editable, non-running steps ① and ② offer read-only observations from the latest successful generation: horizontal source choice, a recorded summary and expandable ranges, events, subjects, audible speech and quality notes; inspection makes no AI call and consumes no credits
- CLIP-49 [o] each observed range shows the intersecting source ranges and cut numbers used by the saved edit plan; usage follows saved cut order, deletion and trimming, and is labelled as the saved edit plan whenever its revision differs from the rendered result or there is no result ← unsaved corrections and a previous render must not be presented as the delivered video.
- CLIP-50 [o] observations remain readable without source footage in editable steps; source playback uses matching local media or an unexpired authorized retained original, with distinct empty states for expired/missing media, absent observations and a source with no observed ranges
- CLIP-51 [o] regeneration, failure and cancellation preserve previous successful observations labelled as previous results when the editable view is visible; new observations replace them only after successful generation, and no selection reasons or confidence scores are invented
- CLIP-52 [x] observation editing, source-range exclusion before generation, partial observation streaming, per-source AI retries and mandatory observation approval — deferred beyond read-only inspection.

- CLIP-53 [o] step ② places one composed video preview above a horizontally navigable timeline and the selected item's controls; the preview remains visible while editing, shrinks when the phone keyboard opens and preserves one document scroller and the committing action (→THEME-9 →THEME-25)
- CLIP-54 [o] timeline cut thumbnails and text-duration bars share one playhead; selecting a cut or text seeks its visible interval and opens only that item's fields, and playback highlights the current cut without switching the owner's focused editing field
- CLIP-55 [o] cut trimming uses draggable range handles, scrubbing and current-frame start/end actions with precise time entry as an alternative; reordering, deleting, text edits and timing edits support undo/redo, and every drag operation has touch and keyboard alternatives
- CLIP-56 [o] the editing preview reflects the current draft's selected footage, crop, text, position and exposure without requiring a full export; the latest completed render is separately identified and cannot stand in for an updated draft preview
- CLIP-57 [o] an unfinalized project without matching source media still permits text/timing editing and playback/download of its revision-labelled last render; current draft footage preview requests missing originals and never represents old burned-in text as updated composition (→CLIP-23 →CLIP-50)
- CLIP-58 [o] video templates offer 원문, 원문 복사 and 형식 안내 복사 like post templates (→TMPL-41 →TMPL-42), so an outside AI can author a supported composition for paste import; authoring, parsing and previewing a template make no model call
- CLIP-59 [o] the video composition vocabulary covers footage sections, repetition over selected scenes or authored item groups, scene-bound AI copy, exact fixed text, answer-bound text and instructions invisible in the output; only explicitly declared visible elements appear
- CLIP-60 [o] information fields have template-authored labels, prompts and required or optional presence; blank optional fields omit their dependent optional element without a placeholder, and neither price nor any business category field is universally required
- CLIP-61 [o] repeated item groups retain each item's own name, price and other template-defined facts together; any number, unit, currency and price basis belongs to that item rather than to a shared project-wide price, and item repetition never requires footage that was not selected
- CLIP-62 [o] an item may be associated with one or more observed source ranges; automatic association requires supporting subject evidence, uncertain associations remain unassigned, and the owner can assign or correct them beside the preview without changing the recorded observations
- CLIP-63 [o] each generated scene-bound statement retains its source range and any referenced item facts; a caption about a visible item may use only that item's facts, while project-wide context is allowed only in a template-declared context section and cannot be presented as that item's price or identity
- CLIP-64 [o] copy follows the template's narrative and the selected scenes as a coherent sequence, avoids repeated generic promotion, and never invents taste, experience, numbers or identities from appearance alone; an unsupported item-specific statement falls back to an observed description or no copy, with the reason inspectable in step ②
- CLIP-65 [o] each visible template element declares generated or exact text, its fact binding if any, an approved style, an automatic or explicit supported position, and its visibility interval; fixed text is preserved verbatim after explicit answer substitution and is never rewritten by AI
- CLIP-66 [o] text intervals may cover the whole output, a span measured from the output start or end, or a span relative to a selected cut; output-relative elements may span cuts, while cut-relative elements follow their cut through reordering and are removed with it
- CLIP-67 [o] the editor distinguishes output time from source time in seconds with precise entry; trimming, reordering or changing duration recomputes resolved intervals, identifies intervals outside the output or their cut and requires correction before rendering rather than silently moving or dropping authored text
- CLIP-68 [o] a template without a disclosure, information chip, opening card or closing CTA produces none of them; no hidden category preset, fixed label list, minimum fact count, compulsory CTA or campaign type may insert or require on-screen content
- CLIP-69 [o] a generation freezes the selected template composition, answers and item associations; subsequent template edits do not change that attempt or an existing result, and manual correction edits the project's retained composition rather than its reusable template
- CLIP-70 [o] existing templates and projects remain readable with their prior content and visibility represented as explicit editable composition; conversion preserves current results and project-specific disclosure choices without applying one project's choices to other users of the template
- CLIP-71 [o] the composition editor and 원문 edit one losslessly round-trippable draft; invalid source, unknown constructs, unresolved required bindings or invalid timing identify the offending element or source line, preserve the pasted draft and prevent saving without silently substituting defaults
- CLIP-72 [o] the copy and timeline quality gate covers several differently priced items, ambiguous subject matches, missing optional information, silent footage and reordered cuts; the owner must be able to connect each item-specific sentence to the visible subject and its own supplied facts

- CLIP-73 [o] the 24-hour original-retention deadline renews after successful source confirmation, a changed project/settings/plan save, an accepted generation/rerender start or that active attempt ending; listing, viewing, polling, playback and download never extend it, and the editable workspace shows the expiry time
- CLIP-74 [o] expiry stops new access and work against expired originals and schedules deletion; an already-active attempt protects its bound originals until it stops, after which CLIP-73 applies unless the project was deleted or finalized
- CLIP-75 [o] 확정하기 follows the post editor interaction (→POST-57): flush pending saves, require a valid saved plan with a matching successful render and no active attempt, then explicitly finalize and open ③ without another AI call or rerender; stale or missing renders explain that 다시 렌더 is required first
- CLIP-76 [o] finalization is irreversible for that project: preserve the confirmed result for preview/download, delete its originals and disable further source upload, editing, generation and rerender; downloading in ② neither finalizes nor deletes originals
- CLIP-77 [o] existing projects with successful results remain unfinalized and open in ② after migration; migration preserves their results and never infers consent to finalization or restores originals already deleted
- CLIP-78 [o] queued/running clip generation or rerender shows a focused central progress view with the real current stage and progress, plus 목록 and 취소; settings, steps, source inspection and correction controls are hidden while the mounted save/upload/poll lifecycle survives, and leaving the page never implicitly cancels the job (→THEME-40)
- CLIP-79 [o] the owner can cancel queued/running clip generation or rerender; accepted cancellation shows 취소 중 until processing stops, prevents subsequent AI calls and result replacement, and reaches a distinct 취소됨 terminal state with explicit retry
- CLIP-80 [o] cancellation and successful completion have one authoritative winner: an accepted cancellation preserves the previous result, while a completion already committed returns its completed result without a cancellation charge; repeated cancellation is idempotent and polling/reopening reflects the durable outcome
- CLIP-81 [o] before AI start and beside cancellation, show confirmed-use credits plus half the unused job reservation as the cancellation rule, with the final charge, cancellation addition and returned credits shown separately after settlement (→QUOTA-49 →QUOTA-50)
- CLIP-82 [o] finalization and project deletion immediately deny new original access and durably request physical cleanup; interrupted deletion retries without restoring editing or losing the retained confirmed result, and temporary playback links remain short-lived without a promise to revoke an already issued link before object deletion

## flow
- create: author or paste a video template → define required/optional information and repeated items → choose template, ratio and target duration → fill required facts → select source footage → inspect or correct item associations when available → approve credit ceiling and generate
- template: 형식 안내 복사 → outside AI authors supported composition → paste 원문 → validate with element/line errors → inspect composition and representative timing preview → save → select for a project
- compose: observed ranges + template narrative + grouped answers → associate subjects with items or leave uncertain → choose cuts → write scene-bound copy and explicit context → resolve authored elements and times → verify → render originals
- steps: ① settings → generate → ② preview, edit, rerender and download → flush saves and explicitly confirm a matching result → ③ result preview/download only
- generate: approve ceiling and cancellation rule → focused durable job → verify sources and prepare bounded analysis copies → reserve within approval (refusal: no AI/debit, preserve prior result and reusable originals) → sequential inline analysis → text-only composition → render originals → ② preview and settlement → clean worker copies, renew original retention
- correct: ② composed draft preview → select cut/text and inspect facts → edit/trim/place/time → preview draft → validate → 다시 렌더 → ② matching result; download is available before confirmation
- failure: identify stage → keep prior result and reusable originals → settle under QUOTA-46 → clean worker copies → explicit AI retry(new ceiling) | manual rerender retry(no credits)
- cancel: request cancellation → durable 취소 중 → stop further work and release active media → settle under QUOTA-49 → 취소됨 with previous result/originals preserved → explicit retry
- finalize: flush saves → verify matching render and no active work → record confirmation and deny original access → ③ retained result → delete originals with recoverable cleanup
- expire: last meaningful work + 24 hours → deny new access to expired originals → cleanup when no active attempt protects them → keep result/plan/analysis, request reselection only when pixels are needed
- inspect: editable non-running step → latest successful observations → select source → summary/ranges/saved-plan usage → local or retained-original playback, with reselection after expiry
- delete: deny project access → recoverably remove originals · result · analysis · edit plan · metadata

## constraints
- source limits: 20 videos per project · 30 minutes combined
- result limits: 15–90 seconds · exactly one of 9:16, 16:9 or 1:1 per project
- source originals are private, time-limited editing inputs; confirmation or project deletion ends retention before the 24-hour deadline
- analysis limits apply before provider transmission: at most 60 seconds · long edge at most 720 pixels · at most 8 MiB per compressed copy; the encoded request and temporary workspace have separate finite bounds, and compression never authorizes extra paid calls
- analysis copies preserve source timing, visible events and audible speech; codec, frame-rate and bitrate choices require media-quality and resource verification under ARCH-24 and provider-contract verification under ARCH-33, not a change to original-footage rendering
- original uploads remain browser-to-private-storage transfers; inline analysis is a worker-to-provider exception and does not enlarge public RPC bodies or make source objects public
- worker-local original copies, analysis copies and encoded request bodies are cleaned on success, failure, cancellation and crash recovery; private object originals follow CLIP-73 through CLIP-76 and CLIP-82, and media bytes/signed links never enter logs, durable job payloads or persistent browser storage
- credit authority is QUOTA-43 through QUOTA-52; a provider unit-price limit and a per-attempt user-credit ceiling are distinct protections
- delivery must satisfy the current Naver Clip upload contract for all three ratios
- surface: directories and editor chrome follow THEME-39 and POST-43 through POST-49 and POST-65 through POST-69; step ② uses CLIP-53 through CLIP-57 while preserving the mounted page and one scroller, with THEME-40 governing the focused running view

- template semantics stay bounded by the existing output durations, supported ratios, bundled fonts and approved visual styles; source grammar, preview implementation and migration mechanics are implementation-task decisions
- draft preview supports source selection and text verification; frame-identical real-time rendering of all transitions, contrast adaptation and final audio on every device is not an established capability
- displayed item identity and scene-to-fact meaning require representative video review; schema validity and a project-wide text match alone do not establish semantic correctness

## chg
- r12 260912 CLIP-2✎ ownership operations→include cancel/finalize; CLIP-19✎ AI-only settlement→explicit cancellation settlement; CLIP-20✎ correction/export waiver→also finalize/manual cancel; CLIP-21✎ attempt-only originals→24-hour editing retention; CLIP-22✎ attempt-end deletion disclosure→24-hour/finalization disclosure; CLIP-23✎ every retry reselects→reuse unexpired originals; CLIP-24✎ result-only deletion→recoverable original/result deletion; CLIP-26✎ failure/reselection→failure or cancellation with source reuse; CLIP-34✎ terminal local-preview release→page-lifetime preview and retained playback; CLIP-36✎ matching render auto-completes→explicit confirmation; CLIP-38✎ top-only progress→focused-job exception; CLIP-40✎ rerender-only step action→confirm plus rerender/download; CLIP-41✎ job-derived completion badge→explicit completion and cancellation badges; CLIP-47✎ local-only preview→local/retained preview; CLIP-48✎ every-step observations→editable non-running observations; CLIP-50✎ local playback only→retained access and expiry states; CLIP-51✎ running-visible observations→preserved observations on editing return; CLIP-57✎ missing-media editing→unfinalized editing and download; CLIP-73+ CLIP-74+ retention clock/active protection; CLIP-75+ CLIP-76+ CLIP-77+ confirmation and migration; CLIP-78+ CLIP-79+ CLIP-80+ focused progress/cancel races; CLIP-81+ cancellation disclosure; CLIP-82+ durable original cleanup
- r11 260912 CLIP-4✎ category preset recipe→authored composition; CLIP-5✎ all requested answers→template-required fields only; CLIP-11✎ project-wide composition→scene/item-grounded narrative; CLIP-15✎ automatic placement→template placement with automatic opt-in; CLIP-17✎ expanded cut forms→preview-led timeline; CLIP-25✎ detached template metadata→retained editable composition; CLIP-42✎ field editor→composition and lossless source modes; CLIP-46✎ product-owned campaign visibility→template-authored timed disclosure; CLIP-53+ CLIP-54+ CLIP-55+ CLIP-56+ CLIP-57+ preview correction; CLIP-58+ CLIP-59+ CLIP-60+ CLIP-61+ CLIP-62+ CLIP-63+ CLIP-64+ portable templates and scene facts; CLIP-65+ CLIP-66+ CLIP-67+ CLIP-68+ authored timed elements; CLIP-69+ CLIP-70+ CLIP-71+ frozen composition, conversion and validation; CLIP-72+ semantic quality gate
- r10 260912 CLIP-47+ horizontal source browsing; CLIP-48+ 49+ retained observation inspection and revision-aware cut usage; CLIP-50+ 51+ missing-media and previous-result states; CLIP-52+ read-only scope exclusions
- r9 260912 CLIP-15✎ one safe area for all elements→caption safe area plus symmetric header badge bounds; CLIP-46+ independent project disclosure visibility, shown by default, applied to generation and rerendering
- r8 260912 CLIP-4✎ recipe style choices→style choices plus independent caption pace; CLIP-14✎ four→five styles with lightweight text; CLIP-17✎ sentence editing→phrase split/merge and exposure editing; CLIP-45+ backward-compatible sentence and rapid-phrase modes
- r7 260912 CLIP-7✎ target-only description→observed-footage reconciliation with a shorter result allowed above the 15 s floor; CLIP-30✎ current endpoint requirement→explicit live checks at quote, admission and completion; CLIP-41✎ latest-job badge→automatic refresh until terminal state
- r6 260911 CLIP-30✎ fixed sampling with `static` where supported→vendor-neutral endpoint-qualified clip analysis with no provider/model-family allowlist; CLIP-43+ one-pass bounded input with `static` only where supported and no adaptive exploration; CLIP-44+ every registered observe model shown with eligibility and one refusal reason
- r5 260911 CLIP-4✎ recipe→recipe with a category preset; CLIP-13✎ Pretendard only→Pretendard + Paperlogy + Noto Sans KR fallback, all bundled; CLIP-14✎ three styles 깔끔하게·기록처럼·강조형→four CDS styles 깔끔하게·메모·크게 강조·형광펜; CLIP-15✎ platform-safe regions and approved positions→CDS safe areas, anchors and selection rules; CLIP-16✎ fade on every scene change→hard cut default with conditional fade; CLIP-18✎ per-cut volume→per-cut volume plus loudness normalisation; CLIP-28✎ exclusions clarified against the CDS components
- r4 260911 CLIP-36+ 37+ 38+ 39+ 40+ 41+ 42+ the clip and video-template surfaces take the post editor's shape (three steps from durable state, the editor top row, one status region, autosaved settings with 클립 만들기 only on `/clips/new`, one dock per step, the post list's rows and narrowing, the post-template screens for 영상 템플릿); CLIP-17✎ the correction screen→step ② 클립 다듬기
- r3 260910 CLIP-19✎ duration-only preparation and hold→bounded-copy preparation and approved ceiling; CLIP-21✎ transient originals→transient originals and analysis copies; CLIP-22✎ original-video disclosure→compressed-copy disclosure; CLIP-26✎ stage-only retry→stage and settlement with explicitly reapproved retry; CLIP-29+ CLIP-30+ CLIP-31+ CLIP-32+ CLIP-33+ CLIP-34+ CLIP-35+ bounded inline/static analysis, original-quality rendering, serialized preparation, attempt-long local previews and deferred analysis reuse
- r2 260910 CLIP-19✎ generic QUOTA admission→durable preparation then full-run credit reservation before any AI call
- r1 260909 initial
