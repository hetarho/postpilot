---
job: '38'
type: 'change'
source: 'changes/11.header-icon-menus-profile-popover-tab-ic'
plan: 'plan/14.theme-preferences'
requires: []
status: done
title: 'Header icon menus, profile popover, tab icons, and a typography primitive'
---

# Job 38: Header icon menus, profile popover, tab icons, and a typography primitive (change)

> Implementation work doc. Source spec: [changes/11.header-icon-menus-profile-popover-tab-ic](../../changes/archive/11.header-icon-menus-profile-popover-tab-ic.md).
> /implement-job 38 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.

## Acceptance Criteria (from changes/11.header-icon-menus-profile-popover-tab-ic)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 The authenticated header renders, right of the brand/nav: the theme icon menu, the locale icon menu, and the avatar profile control — and no standalone logout button, plan badge/gauge, or account-id text in the header row.
- [x] A2 The theme menu trigger's icon reflects the stored preference (monitor/sun/moon) at first paint and after every change; its panel marks the active option; selecting Light/Dark/System applies immediately with plan 14 persistence semantics (System removes the override) and closes the menu, focus returning to the trigger.
- [x] A3 The locale menu lists 한국어/English as autonyms with the active one marked; selection switches the interface language immediately, announces politely (existing live region), and never changes the URL or theme.
- [x] A4 Both menus are app-drawn (no native select option list), fully keyboard operable (open, arrow between options, Enter selects, Escape closes and restores focus), have translated trigger `aria-label`s, ≥44px targets, and no horizontal overflow at 320px, in both themes.
- [x] A5 The login page and `/about` mount the same two icon menus through the shared composition, with no profile control there.
- [x] A6 The avatar popover shows the account id, the plan badge and three usage meters (fetched only while open, exactly as before), the `/admin` link only for `master`, and a logout action; successful logout lands on `/login` with the session cache dropped; a failed logout keeps the popover open and shows the localized failure inline.
- [x] A7 The five voice tabs each carry an icon; below the container width where the full labels fit, the row shows all five tabs icon-first with compact labels and no horizontal scrolling at 320–390px viewports; at wide widths the row renders text labels as today; the current tab stays `aria-current="page"` in both modes.
- [x] A8 A `Typography` primitive exists in `shared/ui` covering the documented roles with semantic element control; design-language §3 states when each role/size/weight is used, mandates the primitive for slice text, and documents the lint gate.
- [x] A9 Outside `shared/ui`, no non-test `.tsx` composes raw text-size/weight/tracking utilities for text; every migrated surface renders through `Typography` (or a primitive), `pnpm lint:style` enforces the rule and fails on a violation, and the full sweep leaves no off-role size in slice code.
- [x] A10 Per-surface hierarchy is normalized: each page has one `display` title, section headings share one `title` role, metadata uses `meta` — no screen renders two different ad-hoc recipes for the same hierarchical level.
- [x] A11 All existing behavior contracts hold: plan 13/14 acceptance criteria (locale/theme persistence, bootstrap order, cross-tab, System following), plan 01 logout, plan 17 gating — verified by the updated test suites; `pnpm test`, `pnpm lint`, `pnpm lint:fsd`, `pnpm lint:style`, `pnpm lint:style:probe`, and `pnpm build:web` all pass.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

- [x] T001 `shared/ui/typography/` — `typographyStyles.ts` (the §3 role recipes as one class
      builder: `display` · `title` · `body` · `label` · `meta` · `eyebrow`, plus an optional `mono`
      face for verbatim technical values) and `Typography.tsx` (role-driven component; default
      elements h1/h2/p/span/span/span, overridable via `as` so the semantic level and the visual
      role stay independent; caller `className` merged last for layout-only additions). The
      `input` role stays inside the field primitives. Export both from `shared/ui/index.ts`;
      focused test.
- [x] T002 Extend `scripts/lint-style-escapes.mjs` with a fourth finding kind: an ad-hoc TYPE
      utility (`text-<size>`, arbitrary `text-[NNpx]`, `font-<weight>`, `font-mono`,
      `tracking-*`, `leading-*`) in a non-test `.tsx` outside `frontend/src/shared/ui/` — with the
      same `// style-escape: <why>` pragma, alignment/wrap utilities (`text-center`,
      `text-nowrap`, …) NOT matched, and `--probe` cases proving both directions. Run last, after
      the sweep (T011–T014) makes the tree green.
- [x] T003 `shared/ui/menu/Menu.tsx` — the app-drawn dropdown for one bounded choice (§7 owner
      decision: the OS never draws an option list): an icon-capable 44px trigger built on
      `Button`, a `surface-highest` right-aligned panel opening below, options as
      `role="menu"`/`menuitemradio` rows with a check on the active one, ArrowUp/Down + Home/End
      roving focus, Enter/Space selects, Escape/outside-pointer closes with focus returned to the
      trigger. Export + focused test.
- [x] T004 `features/change-theme/ui/ThemeMenu.tsx` replaces `ThemeSelector.tsx`: `Menu` with the
      trigger icon mirroring the stored preference (Monitor=System · Sun=Light · Moon=Dark),
      `useThemeController` untouched. Rewrite `ThemeSelector.test.tsx` → `ThemeMenu.test.tsx`
      (same persistence/System semantics through the new surface).
- [x] T005 `features/change-locale/ui/LocaleMenu.tsx` replaces `LocaleSelect.tsx`: `Menu` with a
      Languages trigger icon, autonym options, the existing polite live-region announcement and
      `changeLocale` model untouched. Rewrite the test.
- [x] T006 `widgets/interface-preferences/ui/InterfacePreferences.tsx` — drop the outer `Popover`;
      render the two menus side by side (theme, then locale). All three shells (login,
      authenticated, `/about`) inherit through the existing mounts. Update its test.
- [x] T007 New account widget (placement per `/fe-architecture`; expected `widgets/account-menu`)
      — avatar trigger (`rounded-full`, the account id's first character, translated
      `aria-label`), popover holding: the account id, the plan badge + three usage meters +
      master-only `/admin` link (panel code moves here from `widgets/plan-usage`, which
      dissolves — same open-only fetching), and the logout action. Logout runs `useLogout`
      inside the widget; the shell supplies the on-success callback (cache drop + navigation stay
      app-owned); a failure renders inline in the open popover. Focused test.
- [x] T008 `app/routes/AuthenticatedLayout.tsx` — right side becomes `InterfacePreferences` + the
      account widget; delete the standalone id text, `PlanUsage` mount, logout `Button`, and the
      under-header failure notice (now in-popover). Update `app/routes/router.test.tsx` and any
      test reaching the old header controls.
- [x] T009 `shared/ui/tab-links/TabLinks.tsx` — optional `icon` per `TabLink`; when every item
      carries one, a `@container` query switches the row below the width where the full labels
      fit to evenly divided icon-above-compact-label tabs (no horizontal scroll); wide containers
      keep today's text row; `aria-current` in both modes. `app/routes/VoiceLayout.tsx` supplies
      the five icons. Tests for both modes.
- [x] T010 i18n `app/providers/i18n/resources/{ko,en}/` — new keys for the account control
      (trigger `aria-label`, popover heading) and any menu-specific labels; retire
      `interfacePreferences.trigger` if nothing renders it; both locales in the same task.
- [x] T011 [P] Typography sweep — `app/` + `pages/` (21 files listed under Affected files):
      every text element renders through `Typography` / `typographyStyles`; off-role sizes
      (`text-xl`, prose `text-base`) normalize to the nearest §3 role; one `display` per page,
      one `title` role per section level (A10).
- [x] T012 [P] Typography sweep — `widgets/` (9 files, including the surfaces this job rebuilds).
- [x] T013 [P] Typography sweep — `features/` (36 files).
- [x] T014 [P] Typography sweep — `entities/` (2 files); confirm `shared/ui` primitives keep
      owning their internal recipes (the gate exempts them) and no slice-facing raw class leaks
      remain.
- [x] T015 [tech/design-language.md](../../tech/design-language.md) — rewrite §3 as role → when-used
      (size/weight per hierarchy level), mandate the primitive for slice text, document the lint
      gate; §7: replace the "build that shared primitive when the first surface needs it" clause
      with the shipped `Menu`, note `TabLinks` icon-compact mode, the recomposed preferences pair,
      and the account popover.
- [x] T016 Verification battery: `pnpm test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style` ·
      `pnpm lint:style:probe` · `pnpm build:web`; 320px no-overflow pass over header, menus,
      popover, voice tabs; both themes eyeballed via `data-theme`.

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3 (layers/slices/segments) ·
  BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/be-architecture` for the
  surfaces this job touches.
- PRD / policy / tech touched: [tech/design-language](../../tech/design-language.md) §3 · §7;
  [policy/themes](../../policy/themes.md) / [policy/languages](../../policy/languages.md) only where they
  describe the selector surface (contracts unchanged); reflection into plans 01 · 03 · 13 · 14 · 17
  per the change doc.

## Affected files (blast radius)

**Rebuilt surfaces** (FE layer/slice/segment)

- `frontend/src/shared/ui/typography/*` (new) · `shared/ui/menu/*` (new) · `shared/ui/index.ts` ·
  `shared/ui/tab-links/TabLinks.tsx` — shared/ui primitives
- `frontend/src/features/change-theme/ui/*` · `features/change-locale/ui/*` — feature ui segments
  (models untouched)
- `frontend/src/widgets/interface-preferences/ui/*` · `widgets/plan-usage/*` (dissolves into) ·
  `widgets/account-menu/*` (new) — widget layer
- `frontend/src/app/routes/AuthenticatedLayout.tsx` · `app/routes/VoiceLayout.tsx` ·
  `app/routes/router.test.tsx` — app shell
- `frontend/src/app/providers/i18n/resources/{ko,en}/common.ts` (+ `plans.ts`/`auth.ts` if keys
  move) — i18n resources
- `scripts/lint-style-escapes.mjs` — the style gate
- `spec/tech/design-language.md` §3 · §7 — the binding style guide

**Typography sweep** (call-site migration only; found by
`grep -rl 'text-(2xl|xl|lg|base|sm|xs)|font-(medium|semibold|bold)|tracking-' --include='*.tsx'`,
tests excluded)

- `app/` (3): `AuthenticatedLayout.tsx` · `RoutePending.tsx` · `VoiceLayout.tsx`
- `pages/` (18): `about/ui/AboutPage.tsx` · `admin/ui/AdminPage.tsx` ·
  `ai-models/ui/AIModelsPage.tsx` · `editor/ui/DraftEditor.tsx` · `editor/ui/EditorPage.tsx` ·
  `guidelines/ui/GuidelinesPage.tsx` · `login/ui/LoginPage.tsx` ·
  `model-experiment/ui/ModelExperimentPage.tsx` · `posts/ui/PostsPage.tsx` ·
  `publishing-agents/ui/PublishingAgentsPage.tsx` · `purposes/ui/PurposesPage.tsx` ·
  `voice-rule-comparison/ui/VoiceRuleComparisonPage.tsx` ·
  `voice-validation/ui/VoiceValidationPage.tsx` · `voice/ui/VoiceImportPage.tsx` ·
  `voice/ui/VoiceScreen.tsx` · `voice/ui/VoiceValidationsPage.tsx` ·
  `voice/ui/VoiceVersionsPage.tsx` · `voices/ui/VoicesPage.tsx`
- `widgets/` (9): `candidate-comparison/ui/CandidateComparison.tsx` ·
  `candidate-comparison/ui/TextCandidateComparison.tsx` · `contact-sheet/ui/ContactSheet.tsx` ·
  `export-panel/ui/ExportPanel.tsx` · `interface-preferences/ui/InterfacePreferences.tsx` ·
  `model-leaderboard/ui/ModelLeaderboard.tsx` · `plan-usage/ui/PlanUsage.tsx` ·
  `publish-panel/ui/PublishPanel.tsx` · `voice-warning/ui/DeletedVoiceWarning.tsx`
- `features/` (36): `apply-model-recommendation/ui/ApplyRecommendation.tsx` ·
  `configure-model-pair/ui/ModelPairForm.tsx` · `create-guideline/ui/CreateGuidelineForm.tsx` ·
  `create-purpose/ui/CreatePurposeForm.tsx` · `create-voice/ui/CreateVoiceForm.tsx` ·
  `edit-guideline/ui/EditableGuidelineScope.tsx` · `edit-guideline/ui/EditableGuidelineText.tsx` ·
  `edit-post-content/ui/BlockEditor.tsx` · `edit-purpose/ui/EditablePurposeField.tsx` ·
  `edit-voice-profile/ui/ProfileField.tsx` · `edit-voice-profile/ui/RulesEditor.tsx` ·
  `edit-voice-profile/ui/StructuredProfileEditor.tsx` ·
  `edit-voice-profile/ui/StyleguideEditor.tsx` · `edit-voice-profile/ui/VoiceVersionHistory.tsx` ·
  `edit-with-ai/ui/ReviseForm.tsx` · `edit-with-ai/ui/SaveAsGuidelineButton.tsx` ·
  `finalize-post/ui/FinalizeActions.tsx` · `finalize-post/ui/VoiceLearningPanel.tsx` ·
  `generate-post/ui/GenerationActions.tsx` · `generate-post/ui/GenerationOptions.tsx` ·
  `learn-voice/ui/LearnVoiceForm.tsx` · `manage-users/ui/UserPlanSelect.tsx` ·
  `manage-voice-rules/ui/VoiceRulesManager.tsx` · `manage-voice-samples/ui/SampleList.tsx` ·
  `pair-publishing-agent/ui/PairPublishingAgent.tsx` · `rename-voice/ui/RenameVoiceField.tsx` ·
  `save-draft/ui/SaveStatus.tsx` · `select-model/ui/StageModelSelect.tsx` ·
  `select-post-language/ui/PostLanguageSelect.tsx` ·
  `select-post-purpose/ui/PostPurposeSelect.tsx` · `select-post-voice/ui/PostVoiceSelect.tsx` ·
  `upload-photos/ui/PhotoDropZone.tsx` · `upload-photos/ui/PhotoStrip.tsx` ·
  `upload-photos/ui/SkippedList.tsx` · `upload-photos/ui/UploadProgress.tsx` ·
  `validate-voice-profile/ui/ValidateVoiceProfile.tsx`
- `entities/` (2): `guideline/ui/GuidelineScopeField.tsx` · `post/ui/BlockList.tsx`
- plus the test files beside every touched surface.

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration: N/A — this is a frontend-only change with no proto, SQL, dependency, or backend edit
- [x] FE `pnpm build:web` · `pnpm lint` · `pnpm --filter ./frontend lint:fsd` pass; BE gates are N/A
- [x] Constitution sanity ([I1]–[I7]): nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (`/fe-architecture`; `/be-architecture` is N/A)

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

Applied **nine review groups; rejected zero**: menu state/focus and Tab traversal; account/about
popover bounds; responsive voice-tab geometry; semantic heading/section structure; typography role
and metadata normalization; server-string wrapping; field help/count/error associations; exhaustive
Tailwind type-escape probes; and SSOT/sequencing accuracy. Three independent focused reviews ended
with no P0–P2 finding, and the read-only Codex review's three findings (closed theme state, tab
breakpoint, stale SSOT wording) were all incorporated.

Verification evidence (2026-09-01): `pnpm test` (110 files / 684 tests), `pnpm lint`,
`pnpm lint:fsd`, `pnpm lint:style`, `pnpm lint:style:probe` (53 cases), `pnpm build:web`,
`pnpm lint:spec`, and `git diff --check`. Runtime review also checked the 320–390px header/menu/
popover/tab geometry and both theme contracts; the named `@tabs` transition is pinned at 38rem.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `spec/policy/**`·`spec/tech/**` · config values in their owner
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
