---
job: '08'
type: 'new'
source: 'plan/03.voice-profile-learning'
plan: 'plan/03.voice-profile-learning'
status: done
title: 'Voice Profile Backend and Analysis Job'
---

# Job 08: Voice Profile Backend and Analysis Job (new)

> Implementation work doc. Source spec: [plan/03.voice-profile-learning](../../plan/03.voice-profile-learning.md).
> /implement-job 08 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.
>
> **Split:** plan 03 is built as two jobs. This is the backend: `voice_profiles` / `voice_samples`, `voice.proto`, the
> `voice` context (samples, editable profile, `ProfileForPrompt`, `AppendRule`), and the `analyze_voice` job handler —
> the first real job kind on the queue. The `말투` screen and the editor warning are
> [job 09](09.voice-screen-and-empty-profile-warning.md). Requires jobs 01, 06 (a model to analyze with) and 07 (the
> queue).

## Acceptance Criteria (from plan/03.voice-profile-learning)

<!-- Plan 03 criteria owned by this job (plan AC numbers in parentheses). AC 1 and the UI halves of 4, 6, 7, 8 belong to job 09. -->

- [x] A1 (plan AC2) Two accounts hold different styleguides and rules; no RPC can return one account's profile to the other (no request carries a user id; queries are interceptor-scoped).
- [x] A2 (plan AC3) Pasting a single sample of ≥ 200 characters yields a styleguide whose first section is the 종결어미 distribution and which contains a "never uses" section — the handler fails the job if the 종결어미 section is absent.
- [x] A3 (plan AC4) A sample under 200 characters is refused with `InvalidArgument` and the character count.
- [x] A4 (plan AC5) Adding a second sample runs the analysis over both samples (the job's input contains both bodies — asserted by a unit test on corpus assembly).
- [x] A5 (plan AC6, server half) `UpdateVoiceProfile` persists edited `styleguide` and `rules`; `GetVoiceProfile` returns them.
- [x] A6 (plan AC7, server half) `DeleteVoiceSample` removes the sample and re-enqueues the analysis when at least one sample remains; with none left the styleguide is untouched.
- [x] A7 (plan AC8, server half) `AddVoiceSample` returns immediately with a `job_id` (or the active job's id when one is already running); `GetVoiceProfile.active_job_id` exposes the in-flight analysis.
- [x] A8 (plan AC9) `ProfileForPrompt` returns styleguide, ≤ 3 excerpts (most recent, each ≤ 1500 chars), rules, in that order, plus `empty` when there is neither a styleguide nor a sample.
- [x] A9 (derived) `AppendRule` appends a de-duplicated line to `rules` and never touches `styleguide`; the analysis overwrites `styleguide` wholesale and never writes `rules`.
- [x] A10 (derived) `AddVoiceSample` without an enabled analyze model is refused with `FailedPrecondition` before anything is stored or enqueued.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

- [x] T001 `backend/internal/voice/config.go`: typed constants `SampleMinChars = 200`, `ExcerptCount = 3`, `ExcerptChars = 1500`, `LabelFallbackChars = 20` (owned by the context per plan 03).
- [x] T002 (migrate) `backend/internal/platform/db/migrations/0005_voice_profiles_samples.sql` — plan 03 schema. (Plan 03's illustrative `0003` is superseded by build order.)
- [x] T003 sqlc `backend/internal/voice/store/queries/voice.sql`: `UpsertProfile`, `GetProfile`, conditional `SetStyleguideIfCorpusVersion`, `SetRules`, sample CRUD, and corpus-version reads/writes → `pnpm gen:sql`.
- [x] T004 (gen) `proto/postpilot/v1/voice.proto` (imports `provider.proto` for `ModelRef`): `VoiceService { GetVoiceProfile, UpdateVoiceProfile, AddVoiceSample, DeleteVoiceSample }`, `VoiceProfile{styleguide, rules, updated_at, samples, active_job_id}`, `VoiceSample{id, label, chars, created_at}`, requests/responses per plan 03. `pnpm gen:proto`.
- [x] T005 `backend/internal/voice`: context-owned types, ports, corpus/prompt assembly, profile projection, serialized edits/rule appends, compensated sample mutations, and the version-aware `analyze_voice` handler. The handler retries when the corpus changes during a provider call and only publishes a styleguide for the exact analyzed corpus version.
- [x] T006 `backend/internal/voice/store/store.go` and `rpc/handler.go` (no user id in any request; all from ctx; error mapping); registrar.
- [x] T007 `backend/cmd/api/main.go`: wire the voice service (registry from job 06, job context from job 07) and `job.Register("analyze_voice", voice.AnalyzeHandler)`.
- [x] T008 Tests: validation/model precondition, full corpus and prompt rules, profile projection/truncation, rule de-duplication/concurrency, account isolation, add/delete enqueue and compensation, active-job reuse, corpus-version retry, last-sample race, RPC ownership/error mapping, and durable job success/failure state.

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_) — this job lands **[I4]** (one profile per account, 종결어미 first) and is the first consumer of **[I5]**.
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — BE §2.1 (`internal/voice` owns its tables; reads the LLM port; registers a handler; publishes `ProfileForPrompt` / `AppendRule` for other contexts), §2.2, §2.4 (call the model, then persist). Invoke `/be-architecture`.
- PRD / policy / tech touched: [PRD.md](../../../PRD.md) F-3 (200자, 누적 재분석, 분석 항목, 완료 조건), §3.4, §5 (`voice_profiles`, `voice_samples`, 프로필 주입 순서, prompt-caching note), S2/S3. Policy to land on completion: `spec/policy/voice.md`.

## Affected files (blast radius)

| Path | Placement (ARCHITECTURE §2) | Change |
|---|---|---|
| `backend/internal/voice/config.go` | context `voice` | typed constants |
| `backend/internal/platform/db/migrations/0005_voice_profiles_samples.sql` | platform/db | (migrate) |
| `backend/sqlc.yaml`, `backend/internal/voice/store/queries/voice.sql`, `…/store/sqlc/*` (gen) | voice/store | queries |
| `proto/postpilot/v1/voice.proto` + generated Go/TS | contract (gen) | VoiceService |
| `backend/internal/voice/{types,ports,corpus,prompt,service,profile_for_prompt,rules,analyze_handler}.go` | context `voice` | domain, use-cases, handler |
| `backend/internal/voice/store/store.go`, `rpc/handler.go` | voice/store, voice/rpc | mappers |
| `backend/cmd/api/main.go` | composition root | wiring, handler registration |

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration applied (if any): `pnpm gen:proto` · `pnpm gen:sql` · migrations embedded and run at boot
- [x] FE `pnpm build:web` · `pnpm lint` · `pnpm --filter ./frontend lint:fsd` / BE `go vet ./... && go build ./... && go test ./...` (Docker) pass (if any)
- [x] Constitution sanity ([I1]–[I7]): nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (the relevant `/fe-architecture` · `/be-architecture` checklist)

## Review

- [x] `/code-review` applied · two independent Codex review passes applied; the final pass reported no high/medium correctness or security findings.

The first pass's stale-corpus, queue-failure, and concurrent-rule findings produced corpus-version CAS/retry,
mutation compensation, and serialized profile writes. Its input-size observation was not applied: plan 03 explicitly
requires the full corpus, the product is restricted to two operator-created accounts, and no product cap is specified.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · create `spec/policy/voice.md` · config values in their owner
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
