---
job: '25'
type: 'new'
source: 'plan/12.automated-naver-publishing-through-a-pai'
plan: 'plan/12.automated-naver-publishing-through-a-pai'
requires: []
status: doing
title: 'Automated Naver Publishing Through a Paired Mac Agent'
---

# Job 25: Automated Naver Publishing Through a Paired Mac Agent (new)

> Implementation work doc. Source spec: [plan/12.automated-naver-publishing-through-a-pai](../plan/12.automated-naver-publishing-through-a-pai.md).
> /implement-job 25 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.

## Acceptance Criteria (from plan/12.automated-naver-publishing-through-a-pai)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [ ] A1 A user can pair two different postpilot accounts on one Mac; each stores a distinct token in Keychain, owns a distinct Hermes/browser profile, sees only its account's jobs, and publishes through the Naver identity verified for that connection. Revoking one leaves the other usable and the revoked token immediately fails.
- [ ] A2 The Mac exposes no public listener: its setup UI binds loopback only and work arrives through outbound authenticated polling. Turning the Mac off leaves a confirmed job durably queued; starting its LaunchAgent later claims it and proceeds without another phone action.
- [x] A3 In 글 완성, `내보내기` is unchanged and a separate `발행하기` section follows it. No other lifecycle step renders the panel. Opening/rendering it performs no publication; only the explicitly confirmed `네이버에 발행` action does.
- [x] A4 `StartPublish` rejects an unfinalized post, stale `expected_content_revision`, foreign/revoked agent, stale/foreign category, second nonterminal job, and a post already successfully published to Naver. Finalization and voice learning behavior are unchanged.
- [x] A5 A successful request freezes the exact finalized protojson, category, visibility, tags and ordered image set. A later post edit/delete cannot alter the job, and every staged object is the existing converted JPEG copied inside R2 without entering or being decoded by the API process.
- [ ] A6 Given a finalized fixture containing every block type and eight photos, the paired Mac publishes one Naver post whose title, plain-text block order, eight JPEG positions, captions, tags, category and visibility match the frozen manifest, then stores and renders the verified Naver URL.
- [x] A7 Agent/job progress survives phone reloads and Mac/VPS reconnects. A lease lost before `committing` safely requeues; stale lease/progress tokens cannot update a newer attempt or regress its stage.
- [ ] A8 Immediately before Hermes activates the final Naver publish control, the server has durably recorded `committing`. Any crash/timeout/disconnect from that point until verified URL capture becomes `outcome_unknown` and is never automatically retried. Reproducing this fault never creates a second post through the system.
- [ ] A9 Expired Naver login, CAPTCHA, 2FA or account mismatch becomes `needs_attention`; the agent neither bypasses the challenge nor sends Naver credentials/cookies/CDP/profile data to the VPS. After local repair, an explicit safe retry before commit can finish the same job.
- [ ] A10 The Hermes run receives only a local job handle in its prompt, treats manifest strings as data, can resolve only current staged assets, and is blocked from navigating outside the allowed Naver editor/login hosts. Missing controls/assets or a changed editor fail closed before commit.
- [x] A11 On terminal completion/cancel, staged R2 objects and sensitive manifest content are purged while job metadata, normalized error, selected settings and published URL remain. The orphan sweep removes partial copies left by a killed staging operation and never deletes a live job asset.
- [x] A12 The first version has no schedule control and sends no Web Push/email/chat notification. The in-app panel alone shows live status and actionable failure text.
- [x] A13 Existing Naver/Tistory/site/Markdown conversion snapshots and clipboard behavior remain unchanged; disabling or never pairing the Mac agent leaves postpilot fully usable as the copy-only product it is today.
- [ ] A14 Setup installs or verifies the implementation-time latest compatible official Hermes release, runs `hermes doctor` and the plugin/CDP compatibility probe, and refuses to arm the connection when the required capabilities are absent. No stale hardcoded installer/version is treated as authoritative.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

- [x] T001 Dependency, architecture, and regression preflight: this job has no product dependency on jobs 18 or 22
      and may be implemented alongside them. Before integration, re-read their as-built post contract, editor
      composition, generated files, and migration assignments, then reconcile rather than overwriting either change.
      Invoke `/be-architecture` and `/fe-architecture`; record green backend/frontend/export baselines and reserve a
      unique migration number across in-flight jobs. Keep `publishing` as its own context, `storage` as an outer
      adapter, and `agent/` as a separate runtime that imports neither backend internals nor frontend source.
- [ ] T002 Current dependency/setup gate: invoke `/library-setup` before adding the agent module, Hermes, browser/CDP,
      Keychain, Connect, launchd, or packaging integrations. Read the implementation-time official Hermes install,
      one-shot CLI, named-profile, plugin/hook, skill, and Chromium/CDP documentation; record the latest compatible
      versions and commands in an agent-owned compatibility manifest/test instead of copying the version mentioned in
      plan 12. Run `hermes doctor` and a no-publish capability probe before any connection can become ready.
- [x] T003 [P] Typed configuration: add positive-duration/count parsing for `PUBLISH_PAIRING_TTL` (10m),
      `PUBLISH_MAX_PENDING_PAIRINGS` (8), `PUBLISH_LEASE_TTL` (45s), `PUBLISH_ASSET_URL_TTL` (10m),
      `PUBLISH_ORPHAN_SWEEP_INTERVAL` (24h), and `PUBLISH_ORPHAN_MIN_AGE` (1h) to
      `backend/internal/platform/config`, with boot-failure tests and both env examples. Add
      `PUBLISH_JOB_POLL_MS` (2000) and `PUBLISH_AGENT_STALE_MS` (30000) to `frontend/src/shared/config`; no handler,
      component, or polling hook owns a duplicate literal.
- [x] T004 (gen) Contract seam: create `proto/postpilot/v1/publishing.proto` with the user-session
      `PublishingService`, agent-token `PublishingAgentService`, agent/category projections, immutable manifest,
      ordered staged-asset descriptors, explicit status/stage/error enums, pairing/start/get/cancel calls, and
      claim/lease/progress/commit/complete/fail calls. Requests carry no user id, Naver credential, cookie, profile
      path, CDP endpoint, or arbitrary filesystem path; `StartPublish` carries the expected content revision and
      selection ids only. Add an agent-only Go generation template whose managed package mapping targets
      `agent/internal/gen`, update `scripts/gen.mjs` to run both Go consumers plus TS, and run `pnpm gen:proto` without
      hand-editing any generated file.
- [x] T005 (migrate) Add the uniquely reserved next embedded migration, renumbering before integration if another
      in-flight job lands first, for
      `publishing_pairings`, `publishing_agents`, `publish_jobs`, and `publish_assets`, with account-scoped composite
      foreign keys, token/code hash uniqueness, one live-or-successful Naver job guard per post, monotonic progress,
      lease/commit timestamps, and safe cascade behavior. Add publishing queries as a new sqlc context, run
      `pnpm gen:sql`, and cover upgrade/down, boot, FK/index, expired pairing, revoked token, and duplicate-publication
      constraints with migration/store tests.
- [x] T006 Backend publishing domain and state machine: add pure agent/pairing/job/asset/settings types, normalized
      errors, transition tables, and consumer-owned `PostSnapshots`, `ObjectStaging`, `Clock`, and token/id ports in
      `backend/internal/publishing`. Enforce `queued → claimed → preparing → opening_editor → filling_content →
      uploading_photos → committing → verifying → published`, terminal failure/attention/unknown states, monotonic
      sequence numbers, cancellation only before committing, and no automatic transition out of
      `outcome_unknown`. Add table-driven state/race tests independent of SQL, proto, R2, and Hermes.
- [x] T007 Backend pairing and agent identity: implement single-use hashed device codes, a global pending-code cap,
      one-time raw agent-token return, hashed-token authentication, account-scoped list/update/revoke, safe profile
      metadata/category sync, last-seen tracking, and immediate revocation. Add separate session and bearer-token
      Connect boundaries so the existing HttpOnly cookie cannot authenticate agent procedures and an agent token
      cannot authenticate human procedures. Log neither raw token nor pairing code; cover replay, expiry, forged
      ownership, two-account isolation, revocation, and procedure-routing tests.
- [x] T008 Exact finalized post hand-off: add an ownership-checked published behavior in the post
      context that returns the exact current finalized `PostContent`, content/finalized revision, ordered image
      metadata/object keys, and the post's assigned account identity without exposing post persistence. The publishing
      adapter rejects unfinalized/stale content, missing canonical blocks/assets, and a confirmation whose
      `expected_content_revision` no longer matches. Prove that this read neither finalizes/learns nor mutates post,
      voice, generation, experiment, or export state.
- [x] T009 R2 immutable staging adapter: extend `backend/internal/storage` only with the S3-compatible operations the
      publishing-owned port requires—server-side copy, short-lived GET signing, metadata/size verification, prefix
      listing, and idempotent deletion. Copy confirmed JPEGs to `publishing/{job_id}/{ordinal}.jpg` without reading or
      decoding bytes in the API, fail closed and clean partial copies before queue visibility, mint URLs only for the
      active lease, purge terminal assets/manifest content, and sweep only old unreferenced publishing keys. Test
      pagination, partial-copy compensation, URL TTL, live-prefix protection, and R2/MinIO error mapping.
- [x] T010 User publication use-cases and RPC: implement `CreateAgentPairing`, agent list/update/revoke,
      `StartPublish`, `GetPublishJob`, and pre-commit `CancelPublish` in `publishing/service.go` plus thin mappings in
      `publishing/rpc`. `StartPublish` validates owned active connection, tested Naver identity, synced category,
      visibility, exact finalized revision, no nonterminal/successful Naver job, then freezes canonical protojson,
      tags/settings and staged JPEG identities before the job becomes claimable. Map stale/foreign/conflict/error
      cases to stable Connect codes and Korean-safe messages; add service/RPC/privacy/idempotency tests.
- [x] T011 Agent lease and result use-cases/RPC: authenticate the connection first, atomically claim only that
      account's queued job, return a random lease token plus freshly signed immutable manifest, renew below TTL, and
      accept only allowed monotonic reports for the current attempt. Persist `committing` before acknowledging the
      call that permits the final click; after that fence, expiry, timeout, disconnect, browser loss, or malformed
      completion yields `outcome_unknown`, never requeue. Require a verified expected-blog Naver URL for `published`;
      normalize login/CAPTCHA/2FA/account mismatch to `needs_attention`. Cover stale leases, delayed reports,
      restart/reconnect, pre-commit requeue, commit-boundary faults, and duplicate-completion races.
- [x] T012 Backend composition and lifecycle: wire publishing store/service, post snapshot adapter, storage staging
      adapter, session handler, agent-token handler/interceptor, RPC registration, expired-lease recovery, terminal
      cleanup, and orphan sweep only in `backend/cmd/api`. Extend account-deletion orchestration to revoke agent
      credentials and purge remaining staged objects without crossing table ownership. Add composition tests that
      prohibit sibling-context store imports and integration tests proving the API remains usable when no Mac is
      paired or all agents are offline.
- [x] T013 [P] FE publication entities and action: add `entities/publishing-agent` and `entities/publish-job` with
      account-scoped query keys, proto mappers, category/default/last-seen projections, job polling, and explicit
      terminal/error models. Add `features/publish-post` for category/visibility selection, stale-revision-aware
      confirmation, start/cancel, and query invalidation. Keep all generated imports behind `shared/api`; mounting a
      query or rendering a control must never call `StartPublish`.
- [x] T014 FE publish panel and editor composition: create a separate `widgets/publish-panel` and compose it directly
      after the unchanged `ExportPanel` only in 글 완성 in `pages/editor`; never import one widget from the other.
      Implement no-agent, unavailable-but-queueable, ready, running/history, published-link, failed,
      `needs_attention`, and `outcome_unknown` states with the explicit `네이버에 발행` confirmation that says the
      final Naver button will be pressed without another prompt. Cover mobile width, keyboard/focus, reload polling,
      offline queueing, stale revision, duplicate prevention, no-schedule/no-push UI, and byte-identical export
      snapshots/clipboard behavior.
- [x] T015 [P] FE agent management: add the `/publishing-agents` page, route, and navigation entry using lower-layer
      pairing/update/revoke features. Create and display a short-lived single-use device code, show only safe server
      metadata (connection/blog/browser labels, defaults, categories, health/last seen), and explain that Naver login
      remains on the Mac. Never render/store an agent bearer token, Naver credential, cookie, CDP endpoint, or local
      path in browser state, query cache, analytics, or logs. Add account-switch/cache-isolation and accessibility
      tests.
- [x] T016 [P] Mac companion foundation: create the separate `agent/` Go module with a thin composition root,
      typed local config, postpilot Connect adapter, pure poll/lease supervisor, loopback setup service, Keychain
      credential port/adapter, browser discovery/profile registry, launchd installer, structured logs, and owner-only
      app/work directories. Defaults are poll 5s with jitter/backoff, heartbeat 10s, job timeout 15m, max Hermes turns
      60, HTTPS except loopback development, and an OS-selected loopback setup port. Validate heartbeat against the
      server-advertised lease TTL before claiming; expose no public listener and never persist raw tokens outside
      Keychain.
- [x] T017 Mac pairing/profile setup: implement loopback-only setup screens/actions for API URL, device-code enroll,
      connection naming, supported Chromium-family binary selection, per-connection agent-owned user-data directory,
      visible Naver login/test, exact blog-identity verification, category sync/defaults, Hermes health, status, and
      redacted logs. Two Postpilot accounts on one Mac must use separate Keychain entries, browser directories,
      Hermes profiles, queues, and work directories; profile locks or a failing setup test leave the connection
      unarmed and actionable.
- [x] T018 Hermes publisher integration: under `agent/hermes/postpilot-publisher`, add the implementation-time valid
      named profile, versioned Naver skill, narrow structured plugin/tools, compatibility probe, and pre-tool guard.
      The one-shot prompt is constant and carries only a local job handle; manifest title/body/caption/tag strings
      are inert plugin data, never prompt or shell fragments. Permit only current-job staged paths and enumerated
      Naver login/editor hosts; disable general filesystem, shell, search, messaging, delegation, and unrelated
      tools. Map TEXT/HEADING/QUOTE/LIST and ordered IMAGE+caption blocks exactly as plan 08 specifies, failing closed
      before commit on missing assets, unexpected blog identity, or changed controls.
- [x] T019 Agent execution and cleanup: download every signed JPEG to a random owner-only per-job directory without
      forwarding auth, reject redirects/hosts outside the signed storage target, verify filenames/byte counts/count,
      supervise the structured Hermes one-shot, forward only legal progress transitions, and delete local job data
      on every terminal/abort path. A pre-commit crash may safely return to the queue; immediately before activating
      Naver's final control the plugin must synchronously enter the durable commit fence; navigation/readback must
      verify the created URL belongs to the paired blog before completion.
- [x] T020 Agent automated tests and packaging: use fake Postpilot, storage, Keychain, browser, Hermes, and clock
      adapters to cover two accounts, token revocation, Mac sleep/offline startup, launchd restart, backoff, lease
      expiry, stale progress, local-only binding, path/redirect escape, content-as-instruction, profile mismatch,
      login/CAPTCHA/2FA, all block types with eight JPEGs, pre/post-commit process death, and cleanup. Add a minimal
      reproducible build/install/uninstall/diagnostics flow for the user LaunchAgent; uninstall never deletes a
      browser profile or Keychain credential without a separate explicit confirmation.
- [ ] T021 End-to-end verification: against MinIO/fake Naver prove durable queueing, immutable R2 copies, reconnect,
      revocation, cleanup, and every commit-fault invariant; then perform one explicitly authorized live smoke publish
      to a designated Naver test blog containing every block type and eight JPEGs, recording title/order/captions/
      tags/category/visibility and the verified URL without recording credentials or cookies. If a real Naver account
      is unavailable, do not mark A6/A8/A9/A10/A14 complete or claim the job done.
- [ ] T022 Full gates and review: run deterministic proto/sql generation; embedded migration upgrade/down/boot tests;
      backend `go test ./... && go vet ./... && go build ./...`; agent tests/vet/build on macOS; frontend tests,
      `pnpm build:web`, `pnpm lint`, `pnpm lint:style`, `pnpm --filter ./frontend lint:fsd`, `pnpm lint:spec`, and
      `git diff --check`. Run `/code-review` plus `/codex:review --background`, resolve or explicitly reject findings,
      walk A1–A14, and perform both architecture self-audits without weakening existing export/finalization tests.
- [ ] T023 Reflect only verified reality into the SSOT: create `spec/policy/publishing.md`; update export/posts/
      uploads/auth policies, plan 12 status/as-built notes, PRD/ARCHITECTURE/DEPLOY and the paired-agent technical
      decision where implementation differs, both env examples/config ownership, and 00.overview. Mark job 25 done
      and archive it per workflow. Keep scheduling, push notifications, Safari, native menu-bar UI, Reels, Naver Clip,
      video generation, and republishing outside the shipped claim; do not commit.

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments) ·
  BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/be-architecture` for the
  surfaces this job touches.
- PRD / policy / tech touched: [PRD](../../PRD.md) goals, S1, §3.1, §3.2, §3.5, F-2, F-7, F-10,
  §6.2 and §10 · [plan 12](../plan/12.automated-naver-publishing-through-a-pai.md) ·
  [export](../policy/export.md) · [posts](../policy/posts.md) · [uploads](../policy/uploads.md) ·
  [auth](../policy/auth.md) · [paired local publishing agent](../tech/paired-local-publishing-agent.md) ·
  [DEPLOY](../../DEPLOY.md). At implementation time add `spec/policy/publishing.md`.

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

| Path | Target placement | Change |
|---|---|---|
| `proto/postpilot/v1/publishing.proto` (new), `backend/buf.gen.yaml`, `agent/buf.gen.yaml` (new), `scripts/gen.mjs` | contract/codegen seam | user + agent services and separate generated Go consumers; one TS consumer |
| `backend/internal/gen/postpilot/v1/**`, `frontend/src/shared/api/gen/postpilot/v1/**`, `agent/internal/gen/postpilot/v1/**` | generated output | `pnpm gen:proto` only; never hand-edit |
| `backend/internal/platform/db/migrations/00xx_publishing.sql`, `backend/internal/platform/db/{db,migrate}_test.go`, `backend/sqlc.yaml` | BE platform/db (migrate) | publishing tables, constraints, sqlc context, migration/boot tests |
| `backend/internal/platform/config/{config,config_test}.go`, `.env.example`, `.env.production.example` | BE platform/config + deployment config | six validated publishing values and documented env ownership |
| `backend/internal/publishing/{types,ports,service,state,cleanup}.go` and colocated tests | BE `publishing` context/domain behavior | pairing, immutable start, lease/state machine, commit fence, retention/orphan behavior |
| `backend/internal/publishing/store/queries/publishing.sql`, `store/{store,store_test}.go`, `store/sqlc/**` | BE publishing/store (gen) | publishing-owned persistence and atomic claims/transitions |
| `backend/internal/publishing/rpc/{user_handler,agent_handler,agent_interceptor}.go` and tests | BE publishing/rpc | cookie-session human surface, bearer-token agent surface, proto/domain mapping |
| `backend/internal/post/{types,ports,service}.go` and tests | BE post context published behavior | ownership-checked exact finalized publishing snapshot; reconcile concurrent job-18 types without a product dependency |
| `backend/internal/storage/{r2,r2_test}.go` | BE outer storage adapter | copy/presign/delete/list capability satisfying publishing-owned port |
| `backend/cmd/api/main.go` and composition/boundary tests | BE composition root | wire/register both auth surfaces, snapshot/storage adapters, lease recovery and sweeps |
| `frontend/src/shared/{api,config}/**` | FE shared segments | generated seam export and typed poll/stale constants |
| `frontend/src/entities/publishing-agent/{index.ts,model/**,api/**}` (new) | FE entity slice | safe connection/category/default/health projections and account-scoped queries |
| `frontend/src/entities/publish-job/{index.ts,model/**,api/**}` (new) | FE entity slice | job states, mappers, polling and history queries |
| `frontend/src/features/publish-post/{index.ts,model/**,api/**,ui/**}` (new) | FE action slice | settings, explicit confirmation, start/cancel and cache effects |
| `frontend/src/features/{pair-publishing-agent,configure-publishing-agent,revoke-publishing-agent,cancel-publish,retry-publish}/**` (new) | FE action slices | human-session device management and retained-job recovery actions |
| `frontend/src/widgets/publish-panel/{index.ts,ui/**}` (new) | FE widget | visually separate publish/status section below export |
| `frontend/src/pages/editor/ui/DraftEditor.tsx`, `EditorPage.test.tsx` | FE page composition | mount PublishPanel only in 글 완성 after the unchanged ExportPanel |
| `frontend/src/pages/publishing-agents/{index.ts,ui/**}` (new), `frontend/src/app/routes/{router,AuthenticatedLayout}.tsx` and tests | FE page/app routes | connection management route and navigation |
| `frontend/src/test/{posts,session}.ts` and publishing fixtures | FE test support | finalized revision, two-account connections and publish jobs |
| `agent/go.mod`, `agent/go.sum`, `agent/cmd/postpilot-agent/**` (new) | Mac composition root | executable, setup/pair/run/install commands and adapter wiring |
| `agent/internal/{config,publishing,postpilot,setup,credentials,browser,hermes,launchd,workdir}/**` (new) | Mac domain/app + outer adapters | polling/lease supervisor, loopback UI, Keychain, Chromium profiles, Hermes and cleanup |
| `agent/hermes/postpilot-publisher/**` (new) | constrained Hermes integration | compatible profile/skill/plugin/guard/probe, exact Naver mapping and structured progress |
| `agent/packaging/**`, `agent/README.md` (new) | Mac install/ops | reproducible user LaunchAgent install, diagnostics and explicit uninstall behavior |
| `spec/policy/publishing.md` (new), `spec/policy/{export,posts,uploads,auth}.md`, `spec/plan/12.automated-naver-publishing-through-a-pai.md`, `spec/tech/paired-local-publishing-agent.md`, `PRD.md`, `spec/ARCHITECTURE.md`, `DEPLOY.md`, `spec/plan/00.overview.md` | product/architecture/ops SSOT | reflect only verified as-built publishing behavior |

## Verification / DoD

- [ ] Every **Acceptance Criteria** item above is true in the current code
- [ ] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration applied (if any): `pnpm gen:proto` · migrations embedded and run at boot
- [x] FE `pnpm build:web` · `pnpm lint` · `pnpm --filter ./frontend lint:fsd` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity ([I1]–[I7]): nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (the relevant `/fe-architecture` · `/be-architecture` checklist)

## Review

- [ ] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

### Offline verification log — through 2026-08-31

- Deterministic proto and sqlc generation passed; embedded migration upgrade/down/boot coverage passed.
- Backend `go test ./...`, `go vet ./...`, and `go build ./...`; agent tests/vet/build plus twenty-five Python
  plugin guard tests; and all 394 frontend tests/build/lints passed in the latest full pass. Publishing/post/storage and all touched agent
  packages also passed `go test -race`.
- Manual structured review fixed: the valid-lease failure timestamp omitted from SQL parameters, delayed lease
  recovery tied to the 24-hour orphan cadence, incomplete current-copy compensation, a stale safe-retry form,
  stale Mac availability observation, Hermes stderr corrupting structured output, callback proxy/token exposure,
  invalid staged asset ordering/types, and a Keychain token passed in process argv.
- Eight independent read-only Codex review passes were run. Their findings were resolved with regression tests:
  publication history now survives post deletion; signed storage URLs never reach Hermes; progress/failure SQL closes
  commit races; every browser action rechecks active hosts; heartbeat starts before download and renewal failure cancels
  Hermes; attention/cancel UI works without a ready agent; setup observes the real identity/categories and persists an
  unarmed recoverable record before sync; abandoned directories are cleaned only under a global Mac process lock;
  pending edits are flushed before confirmation; safe retry uses only frozen job values; raw failure detail stays off
  the VPS; transient auth failures back off; and every claim revalidates lease TTL. The third pass additionally bound
  bearer injection to the configured API origin and rejected redirects; bound clicks to current accessibility-snapshot
  refs and blocked final-control activation even before model progress; keyed history by the immutable post incarnation; gave every repeated IMAGE occurrence a
  unique staged/local filename; made the global pairing cap one conditional SQL insert; hot-loaded newly armed Mac
  connections; and separated mandatory lease recovery from retryable R2 cleanup. The fourth pass added one atomic
  Start gate: after R2 staging, the publishing store reserves the serialized writer, re-reads post and agent state
  through their owning behaviors, and inserts in that transaction. It also made the authenticated loopback finish
  callback—not Hermes stdout—the sole terminal-result authority, and rejects an unsafe server lease before setup can
  advertise or activate a ready Mac. The fifth pass made unknown final buttons and native-dialog acceptance fail
  closed; requires successful one-file CDP upload plus an increased editor-image snapshot for every JPEG; binds
  success to the active browser URL and frozen-title readback; preserves frozen retry after source deletion; and
  rejects a stale `DevToolsActivePort` whose browser id no longer matches the live endpoint. The sixth pass made image
  uploads follow exact manifest ordinals with an exact-one-image full-snapshot delta, replaced title-only checks with full trusted
  editor/readback manifest verification, froze the category name through the atomic start gate, and prevents a
  disappearing selected Mac from leaking its category/visibility defaults into the replacement connection.
  The seventh pass binds every full snapshot to its exact current page URL and paired blog identity, rejects inserted
  semantic content, grants one consumable activation to the exact verified final control, adds same-profile
  `needs_attention` repair, leaves transient renewal failures to server lease recovery, prevents retry UI from
  substituting a different Mac, checks two-account isolation/revocation, exercises process-death and offline recovery,
  and makes CI verify the committed agent proto client after regeneration.
- The eighth pass binds every browser fact and irreversible control to one exact CDP target, requires selected/checked
  category and visibility plus a manifest-derived category-ID selector, and performs the navigation allowlist check
  before typing or upload. Setup identity/category discovery now uses deterministic local CDP instead of Hermes stdout,
  and its loopback mutation surface has nonce, Host/Origin, action, and body-size checks. Revocation is rechecked in
  each atomic leased mutation. Job IDs are durably reserved before R2 staging and random-source errors propagate.
  Retained attention jobs now have an account-scoped retry RPC/UI outside the source-post route, and frontend tests are
  a CI gate.
- The ninth independent pass found five remaining release blockers. They were resolved by accepting exactly one
  versioned final-control accessible name instead of broad keywords; deriving setup identity from the generic writer
  URL selected by the signed-in Naver session with editor-specific evidence and a post-navigation target recheck;
  comparing the complete ordered tag collection; separating revoked-agent business state from human-session
  authentication; and adding confirmed retained-job cancellation outside the deleted post route. The same pass also
  prompted non-ambiguous terminal cleanup responses, React Query empty-response normalization, and segment-preserving
  S3 `CopySource` encoding, all with regressions.
- Subsequent adversarial passes rejected canonically duplicate tags at both the post and Hermes boundaries; identify
  category/tag/visibility settings by accessibility role so identical legitimate body text is not truncated; continue
  terminal cleanup across failed assets and jobs while retaining incomplete rows for retry; add planner-backed indexes
  for queue, history, recovery, expiry, and cleanup reads; and revalidate legacy finalized content before any R2
  staging. The final focused read-only pass reported no remaining P1/P2/P3 findings, and the writable environment's
  focused Go/Python regressions passed.
- The 2026-08-31 continuation followed the current official Hermes documentation, updated the supported macOS git
  install to official `main` (reported release `0.20.6`), and passed the global `hermes doctor` plus the checked-in
  publisher plugin doctor (`5` tools and `2` hooks). All four existing Postpilot Hermes profiles retain Nous auth and
  the constrained plugin, but the real browser/CDP probe still waits for a completed connection.
- Fresh deterministic generation was clean. Backend Docker vet/build/tests, publishing/post/storage/API race tests,
  macOS agent tests/vet/build/race, all 26 Python plugin tests, and the Node `24.18.0` frontend gate (`100` files,
  `599` tests, production build, lint/FSD/style/spec) passed. A host Node 26 run was intentionally disregarded after
  its experimental global `localStorage` conflicted with jsdom; the repository-pinned Node version is green.
- A focused read-only Codex review found two pre-live gaps and both were applied with regressions: backend completion
  now accepts only the same exact numeric Naver post URL shape as the plugin, and reopening an already-running
  dedicated browser now navigates its single bound CDP target to Naver login instead of reporting a no-op as success.
- The first 2026-09-01 production pairing attempt exposed a live Chrome process with zero page targets. Login repair
  now creates one `about:blank` target through the verified loopback browser CDP endpoint, rechecks that it is the sole
  bound page and the returned target id, then attaches and navigates that exact session to Naver login. Browser race
  tests and a focused Codex review cover the zero-page recovery; multiple, foreign, or unbound targets still fail closed.
- The current dev API and MinIO are healthy, the setup server is already waiting on a loopback-only listener, and the
  API is usable with zero enrolled agents. The dev database has one account, no publishing agent/job, no unexpired
  pairing code, and one finalized eight-image post whose blocks are only TEXT/IMAGE. The user must therefore create a
  fresh code, complete pairing, provide a second Postpilot account/Naver identity for A1, and add HEADING/QUOTE/LIST to
  the eight-image smoke fixture before the authorized live verification.
- The repository's named `/code-review` workflow remains unavailable, so the combined review checkbox stays open.
  The authorized real-Naver run, live Hermes CDP probe, identity/category verification, challenge paths, and
  commit-fault reproduction also remain open and prevent job completion.

## After completion — reflect into the SSOT

- [ ] Update `plan/` to the new reality · update affected `spec/policy/**`·`spec/tech/**` · config values in their owner
- [ ] (if type=change) move the `changes/` source doc to `changes/archive/`
- [ ] 00.overview progress board ✅ · this doc's frontmatter `status: done`
