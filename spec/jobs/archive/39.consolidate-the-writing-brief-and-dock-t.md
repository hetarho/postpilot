---
job: '39'
type: 'change'
source: 'changes/12.consolidate-the-writing-brief-and-dock-t'
plan: 'plan/02.post-drafting-and-list'
requires: ['38']
status: done
title: 'Consolidate the writing brief and dock the revision step'
---

# Job 39: Consolidate the writing brief and dock the revision step (change)

> Implementation work doc. Source spec: [changes/12.consolidate-the-writing-brief-and-dock-t](../../changes/archive/12.consolidate-the-writing-brief-and-dock-t.md).
> /implement-job 39 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.

## Acceptance Criteria (from changes/12.consolidate-the-writing-brief-and-dock-t)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 On 글 생성, one options surface holds 관찰 모델, 작성 모델, 말투, 용도, 목표 언어, 목표 분량 and the A/B 후보 설정 link; none of them is rendered anywhere else in the editor, and the 글 생성 panel itself shows only 제목, 메모, 사진, the voice warning and the contact sheet.
- [x] A2 The options trigger names the post's current voice without the panel being opened.
- [x] A3 Every relocated control keeps its behavior: a voice reassignment still confirms and is still blocked by `reassignmentBlocker`; the purpose select stays enabled during a job and says the running job keeps its brief; the language select still shows the frozen note; an out-of-range target length still shows its range message and does not save.
- [x] A4 `grep -r '<select'` over `frontend/src` (excluding generated code) returns nothing, and `shared/ui/select/` no longer exists; every previous mount renders an app-drawn control that is operable by keyboard (open, arrow, Home/End, Escape, select) and exposes the same accessible name and validation state as before.
- [x] A5 At 360px width, opening the options surface produces a bottom sheet that is full-bleed to the bottom edge, scrolls only its own body, locks the page scroll, and closes by scrim, Escape and a visible control; from `sm:` up it is a popover that opens upward, aligns to the dock's right edge, and does not extend past the viewport.
- [x] A6 At 360px width the contact sheet is one horizontal snap carousel showing a sliver of the next card, with a `현재 / 전체` position indicator, `overscroll-x-contain`, and no inner vertical scroller; the page still scrolls vertically from anywhere on the strip. From `sm:` up the current `w-60` strip is unchanged.
- [x] A7 The 가제 field appears only on 글 생성; 글 다듬기 and 글 완성 render exactly one title, which is `content.title`.
- [x] A8 Confirming a post writes the finalized `content.title` into `posts.title`: the post list shows the confirmed title afterwards, the slug is unchanged, `content_revision` is unchanged, no job is created, and no provider is called. An empty `content.title` leaves `posts.title` as it was.
- [x] A9 A post still `draft` or `review` is listed under its 가제; the existing fallback to `content.title` for an empty 가제 still applies.
- [x] A10 On 글 다듬기 the dock is always present and shows row 1 (instruction field + icon send button) and row 2 (확정, 확정하고 말투 학습); neither the revision section nor the finalize section is rendered in the panel any more, and the page holds exactly one docked bar.
- [x] A11 The counter, `규칙으로 저장` and `지침으로 저장` are hidden while the instruction field is empty and unfocused, and are shown while it is focused, holds text, or a revision is running or failed.
- [x] A12 `규칙으로 저장` behaves exactly as before: checked, it appends the instruction to the voice's rules before enqueue and the rule survives a failed revision; a duplicate line is not appended twice; the checkbox is disabled with its reason shown on a content/voice language mismatch.
- [x] A13 Every blocker, validation message and failure on 글 다듬기 renders above the dock's controls, and starting either confirm action still flushes a pending block edit before naming the revision (a finalize can never omit a pending edit).
- [x] A14 `ActionBar` uses the reduced padding at the base breakpoint and its `sm:` geometry, float, and safe-area clearance are unchanged.
- [x] A15 The opaque and translucent dock backgrounds are shown side by side in light and dark for the owner's decision, every value they use is a registered token, and the unchosen variant is removed before the job closes.
- [x] A16 No proto file, generated client, or migration changed; `pnpm build`, lint, `go vet` and the full test suite pass; every new or moved string exists in both `ko` and `en`.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

**Sequencing note.** Archived job 38 (change 11) edited several of these files for the `Typography`
migration. This job builds on that landed result — never revert it. Every new text element uses
`Typography`/`typographyStyles` and must pass `pnpm lint:style`.

### A. Shared primitives (everything else depends on these)

- [x] T001 `shared/ui/listbox/Listbox.tsx` + `Listbox.test.tsx` — the app-drawn replacement for the
      native `<select>` (design-language §7, owner decision 2026-08-31). Contract: a 44px field
      trigger wearing the `field-bg` well with the current option's label and the `ChevronDown`
      affordance; an app-drawn `role="listbox"` panel on `surface-highest`; `role="option"` rows with
      `aria-selected`, a check on the active one, and roving programmatic focus (the trigger stays the
      only tab stop, as `Menu` does); ArrowUp/Down/Home/End move, Enter/Space select and close,
      Escape and an outside pointerdown close with focus returned to the trigger; `id`,
      `aria-labelledby` (so `FieldLabel htmlFor` keeps working), `aria-invalid` and the disabled state
      behave exactly as the `Select` they replace. Generic over the option value like `Menu`, so a
      numeric enum value (BlockType, heading level) round-trips without stringly-typed casts.
      Export from `shared/ui/index.ts`. Do NOT reuse `Menu` — it is `aria-haspopup="menu"` with an
      icon-only trigger, which is the wrong role and shape for a labelled form field.
- [x] T002 `shared/ui/sheet/Sheet.tsx` + test — the overlay for ARBITRARY content, extracted from
      `Dialog`'s existing mechanics (portal, scrim, focus trap, focus return, Escape, body-scroll
      lock, phone bottom sheet full-bleed + `rounded-t-xl` + safe-area padded with its own body the
      one scroller, centred from the `md:` breakpoint `Dialog` already uses). Rebuild `Dialog` on top
      of it with its **public props and behavior unchanged** — its title/confirm/cancel shape stays,
      so no existing `Dialog` caller changes.
- [x] T003 `shared/ui/popover/Popover.tsx` grows two props with defaults that keep both current
      callers byte-identical in behavior: `align?: 'start' | 'end'` (default `'end'` = today's
      `right-0`) and `phone?: 'popover' | 'sheet'` (default `'popover'`). With `phone: 'sheet'` it
      renders the T002 `Sheet` below `sm:` and the ordinary popover from `sm:` up. The popover panel
      also gains viewport clamping so it can never extend past either edge. Existing callers
      (`AccountMenu`, `GenerationOptions`) keep their behavior until they opt in.
- [x] T004 `shared/ui/action-bar/ActionBar.tsx` — padding drops one step at the base breakpoint
      (`p-3 sm:p-4`); the float (`bottom-dock-nav`, `sm:bottom-4`), the `sm:pb-dock-b` safe-area
      clearance, the radius and the shadow are unchanged. Add a `surface?: 'solid' | 'translucent'`
      prop for T023's comparison, defaulting to `'solid'` (today's opaque `surface-highest`). Any
      blur radius / alpha used by `'translucent'` is a token registered in
      `frontend/src/app/styles/index.css` (§1.2 — no arbitrary values at the call site).

### B. Remove every native `<select>` (T005–T008 are [P] with each other)

Per surface, apply §7's existing rule: a bounded 2–5 fixed options may become a `SegmentedControl`;
anything longer, anything unbounded, and anything inside a dense card becomes the T001 `Listbox`.
Each migration keeps the surface's current accessible name, disabled/invalid state, empty/placeholder
option semantics and its `FieldMessage` copy — this is a control swap, not a redesign.

- [x] T005 [P] Editor selects → `Listbox`: `features/select-model/ui/StageModelSelect.tsx`,
      `features/select-post-voice/ui/PostVoiceSelect.tsx`,
      `features/select-post-purpose/ui/PostPurposeSelect.tsx`,
      `features/select-post-language/ui/PostLanguageSelect.tsx`. `PostVoiceSelect`'s reassignment
      `Dialog` and `reassignmentBlocker` gate are untouched; `PostPurposeSelect` keeps its
      enabled-during-a-job behavior and its running-job note; `PostLanguageSelect` keeps its frozen
      note. Update `StageModelSelect.test.tsx` and `PostLanguageSelect.test.tsx` to drive the new
      control.
- [x] T006 [P] `features/edit-post-content/ui/BlockEditor.tsx` — the three selects (block type,
      attached photo, heading level). The heading level list is 1–6 and the photo list is unbounded,
      so both are `Listbox`; the block-type switch is 4–5 options inside a card, so it is a `Listbox`
      too unless a `SegmentedControl` reads better at 320px — the implementer decides and records
      which in a comment. The `Editable` open/cancel snapshot semantics must not change.
- [x] T007 [P] Voice and model surfaces → `Listbox`:
      `features/create-voice/ui/CreateVoiceForm.tsx`,
      `features/configure-model-pair/ui/ModelPairForm.tsx` (+ `ModelPairForm.test.tsx`),
      `features/give-voice-feedback/ui/SentenceFeedback.tsx`,
      `pages/ai-models/ui/AIModelsPage.tsx` (two mounts).
- [x] T008 [P] Publishing and admin surfaces → `Listbox`:
      `features/configure-publishing-agent/ui/ConfigurePublishingAgent.tsx` (two mounts),
      `features/publish-post/ui/PublishPostForm.tsx` (two mounts),
      `features/manage-users/ui/UserPlanSelect.tsx` (3 plan tiers — `SegmentedControl` is the better
      fit here per §7; use it unless the admin row's width forbids it).
- [x] T009 Delete `frontend/src/shared/ui/select/` (`Select.tsx` + `Select.test.tsx`) and its
      `shared/ui/index.ts` export. Extend `scripts/lint-style-escapes.mjs` with a finding for a
      literal `<select` in any non-test `.tsx` outside `shared/api/gen/**`, with `--probe` cases
      proving both directions, so the ban is mechanical from here on.

### C. The generation brief becomes one options surface

- [x] T010 `widgets/generation-brief/` (NEW widget — required by FSD: this composes FIVE different
      `features/*` slices, and a feature may not import a sibling feature; ARCHITECTURE §3). It
      renders a `Popover` with `align="end" phone="sheet"` (T003) whose panel holds, in order:
      관찰 모델 · 작성 모델 (`StageModelSelect`, with the existing `optional`/no-photo note),
      말투 (`PostVoiceSelect`), 용도 (`PostPurposeSelect`), 목표 언어 (`PostLanguageSelect`),
      목표 분량 (the target-length checkbox/field and its save, moved out of `GenerationOptions`),
      and the `/ai-models` A/B 후보 설정 link. **The trigger names the post's current voice**
      (`옵션 · <voice name>`, truncating with `min-w-0`) so a wrong voice is visible without opening
      the panel (A2); it stays icon+label safe at 320px. All the callbacks it needs
      (`reassign`, `assignPurpose`, `assignTargetLanguage`, `onTargetLengthSaved`) are props supplied
      by `pages/editor`, so every assignment keeps riding the draft queue that lives above the panels.
- [x] T011 `features/generate-post` — `GenerationOptions.tsx` is reduced to the target-length form
      body (no `Popover` of its own) and re-exported for the widget to place inside its panel;
      `GenerationActions.tsx` stops rendering it and receives `targetLength` as a prop instead of
      owning the state. Lift `targetLength` into `pages/editor/ui/DraftEditor.tsx` (it must be shared
      by the brief widget and the generate action, which are now different layers). `GenerationActions`
      keeps its `sharedDisabled`/precondition logic, its `ref` handle and its A/B button unchanged.
- [x] T012 `pages/editor/ui/DraftEditor.tsx` — the four always-visible selects
      (`PostVoiceSelect`, `PostPurposeSelect`, `PostLanguageSelect`) are removed from above the step
      panels; the 제목 `Textarea` MOVES INTO the 글 생성 panel (and into the `/posts/new` composition,
      which is step ① content with no lifecycle). The 글 생성 panel becomes: 제목 · 메모 · 사진 ·
      voice warning · contact sheet, plus the brief widget and the generate action in the dock.
      Nothing on 글 다듬기 or 글 완성 renders the 가제. **The autosave queue, the mint plumbing and the
      caret handoff stay above the panels** (tech/draft-autosave) so a step change still cannot strand
      a queued save or a reassignment.
- [x] T013 `widgets/refine-dock/` (NEW widget — composes `features/edit-with-ai` and
      `features/finalize-post`, two sibling features). It lays out the 글 다듬기 dock body:
      **row 1** = the revision instruction field + an icon-only send `Button`
      (`aria-label`led, 44px), **row 2** = 확정 and 확정하고 말투 학습 side by side. Blockers,
      validation and failures render ABOVE both rows (§8.3 — the keyboard covers the bottom ~40%,
      so it may hide a control but never the reason it is disabled).
- [x] T014 `features/edit-with-ai/ui/ReviseForm.tsx` — reshaped for the dock: the `section`/heading
      wrapper and the `w-full sm:w-auto` submit go away; the instruction `Textarea` becomes the dock's
      row-1 field (still `autoGrow`, still capped at `REVISION_INSTRUCTION_MAX_CHARS`, still
      `enterKeyHint="enter"` so Return inserts a line). The character counter, the `규칙으로 저장`
      checkbox with its language-mismatch note, and the post-revision `SaveAsGuidelineButton` move
      into a **collapsible row** that is shown while the field is focused, holds text, or a revision is
      running or failed, and hidden while it is empty and unfocused (A11). Toggle it with `hidden`/
      conditional render — never with a `hover:`-only affordance (§8.4). **`규칙으로 저장`'s behavior is
      unchanged in every respect** (pre-flight checkbox, `save_as_rule` still sent, language gate still
      disables it): only its position moves. The `ReviseFormHandle.start()` ref (used by the dock's
      failure retry) stays.
- [x] T015 `features/finalize-post/ui/FinalizeActions.tsx` — reshaped for row 2: the `section`,
      heading and description move out; the two buttons, their `canFinalize`/`canLearn` gating, their
      blocked-reason lines, the confirm `Dialog` and the carry to 글 완성 are unchanged. The
      `finalized` state's success `Notice` and `글 완성으로` button render in the dock too, so the step
      never loses its way out.
- [x] T016 `pages/editor/ui/DraftEditor.tsx` — `LifecycleSteps` renders `refinePanel` WITHOUT the
      revision and finalize sections (they exist only in the dock now), and the dock is mounted on ②
      unconditionally: the current `step === 'generate' || jobNotice || saveState === …` condition and
      `EditorDock`'s `saveState === 'idle' && !children` early return both gain the ② case. There is
      still exactly ONE `ActionBar` in the page's scroller (§4.3). **The flush chain must keep
      working from the dock**: `contentEditorRef.current?.flush() ?? flushContentQueue(post.slug)` is
      passed down to both confirm actions, so a finalize can never name a revision that omits a
      pending block edit (A13). ③ is untouched.
- [x] T017 `pages/editor/ui/DraftEditor.tsx` — **re-seed the local title after a finalize.** The
      editor holds `title` in state and `useAutosave` sends `{title, memo}` on every save, so once the
      server copies `content.title` into `posts.title` (T019/T020) the stale 가제 in state would be
      written back by the next autosave. On a successful finalize, set the local title from the
      refetched post before any further save can queue. Cover it with a test: finalize, then edit the
      memo, and assert the save carries the confirmed title.

### D. The contact sheet swipes on a phone

- [x] T018 `widgets/contact-sheet/ui/ContactSheet.tsx` — the base breakpoint becomes one horizontal
      snap carousel: `snap-x snap-mandatory overflow-x-auto overscroll-x-contain`, cards at ~85% of
      the content width so a sliver of the next card is visible, `snap-start`, and a `현재 / 전체`
      position indicator under the strip (derived from scroll position or `IntersectionObserver`; it
      is presentation, so it announces politely at most once). The card width fraction is a registered
      token, not `w-[85%]` (§1.2). No inner VERTICAL scroller — a verbose observation makes its card
      taller, exactly as today (§4.4). The `sm:` and wider shape keeps its current `w-60` strip, its
      filename heading and its four observation fields. Replace the stale "phone shape is a plain
      vertical list" comment with the reason for the new shape. Update `ContactSheet.test.tsx`.

### E. Backend — 확정 writes the AI title back

- [x] T019 (gen) `backend/internal/post/store/queries/posts.sql` — `FinalizePost` also sets
      `title = ?`. Keep it ONE statement guarded by `content_revision = ?` and `content IS NOT NULL`,
      so the copy is atomic with the finalization and a concurrent content save simply matches zero
      rows. Run `pnpm gen:sql` and commit the regenerated `store/sqlc` output. **No migration** — the
      column already exists.
- [x] T020 `backend/internal/post` — `ContentStore.Finalize` (ports.go) and `Store.Finalize`
      (store/store.go) take the title to write; `Service.Finalize` (service.go:604) passes
      `strings.TrimSpace(found.Content.Title)`, falling back to `found.Title` when it is empty, so an
      untitled generation leaves the 가제 in place. The already-finalized-at-this-revision early
      return is unchanged (the copy already happened). Update `fake_test.go`'s fake store and add
      `lifecycle_test.go` cases: the copy happens, an empty content title is a no-op for
      `posts.title`, the slug is untouched, `content_revision` does not move, and no job or provider
      call results. Confirm the list read (`store.go:284–300`) still falls back to `content.title`
      for an empty 가제 (A9) with no change of its own.

### F. Strings, tests, the glassmorphism review, verification

- [x] T021 [P] i18n — every new or moved string in BOTH `app/providers/i18n/resources/ko/posts.ts`
      and `.../en/posts.ts` (and `common.ts` where shared): the brief trigger with its voice name, the
      sheet's close control, the send button's `aria-label`, the carousel position label, the listbox
      trigger/announcement copy. Keys removed with their old surface are deleted from both files.
      `pnpm --filter ./frontend test` covers the i18n parity check
      (`app/providers/i18n/metadata.test.ts`).
- [x] T022 Test sweep for the moved surfaces: `pages/editor/ui/EditorPage.test.tsx` (the 가제 is
      only on ①; ② renders one title; the ② dock exists with both rows),
      `features/edit-with-ai/ui/ReviseForm.test.tsx` (collapse/expand, `규칙으로 저장` unchanged),
      `widgets/contact-sheet/ui/ContactSheet.test.tsx`, plus a focused test per new primitive
      (T001/T002) and per new widget (T010/T013).
- [x] T023 **Glassmorphism comparison (owner decision gate).** Render the solid and translucent
      `ActionBar` surfaces side by side in light AND dark — a temporary route, a story-style page, or
      screenshots from `/run`, whichever gets the owner a picture fastest — and STOP for the owner's
      choice. Check the translucent variant's contrast for every control resting on it (§9) before
      recommending it. Then either adopt it as the default or delete the variant, its tokens and the
      comparison surface entirely. Do not close the job with dead variant code in the tree.
- [x] T024 Verification: `pnpm lint`, `pnpm lint:fsd`, `pnpm lint:style`, `pnpm lint:style:probe`,
      `pnpm test`, `pnpm build:web`; `go vet ./... && go build ./... && go test ./...` for the backend.
      Confirm `git diff --stat` touches NO `proto/**`, no `shared/api/gen/**`, and no
      `backend/internal/platform/db/migrations/**`, and that `grep -rn '<select' frontend/src
      --include='*.tsx' | grep -v gen/` is empty.

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../../plan/00.overview.md) §3 (_The constitution_) — **[I2]**
  (the block array stays canonical; nothing here touches the content shape), **[I3]** (observe/write
  stay separate and every model choice stays explicit — relocating a select changes the screen, never
  what is sent), **[I4]** (voice isolation and rules are untouched), **[I5]** (generation and revision
  stay durable jobs). No invariant is approached, let alone broken.
- Architecture (placement): [ARCHITECTURE.md](../../ARCHITECTURE.md) — FE §3. **The two new
  compositions must be widgets, not features**: `widgets/generation-brief` composes five different
  `features/*` slices and `widgets/refine-dock` composes two, and same-layer cross-import is
  forbidden. New shared primitives (`listbox`, `sheet`) go in `shared/ui` as one directory each with
  its test. BE §2: the only backend edit stays inside `internal/post` (its own table, its own
  queries) — no other context is read or written. Invoke `/fe-architecture` before creating each new
  directory and `/be-architecture` for the `internal/post` change.
- Source change: [changes/12](../../changes/archive/12.consolidate-the-writing-brief-and-dock-t.md) — its
  *Scope / Non-goals (regression boundary)* section is the contract this job may not exceed.
- PRD / policy / tech touched:
  - [policy/posts.md](../../policy/posts.md) — *Editor presentation* (the 가제 belongs to ①, the brief is
    one options surface, ② now docks and carries two committing actions, superseding "at most one
    committing action — 생성 on ①, and none on ② or ③" and "absent on a quiet 글 다듬기");
    *Canonical content editing and learning baseline* (the finalize-time title copy).
  - [policy/revision.md](../../policy/revision.md) — the revision form's docked shape; `규칙으로 저장`
    explicitly unchanged.
  - [policy/generation.md](../../policy/generation.md) · [policy/templates.md](../../policy/templates.md) ·
    [policy/languages.md](../../policy/languages.md) — selector placement only.
  - [tech/design-language.md](../../tech/design-language.md) — §7 (the Menu/Select paragraph becomes a
    description of the SHIPPED listbox; the `Sheet` primitive for arbitrary content; `ActionBar`'s
    padding and its surface), §4.4 (the phone carousel as the documented horizontal exception), §4.3
    (② now docks).
  - [tech/draft-autosave.md](../../tech/draft-autosave.md) — unchanged contract, but T012/T017 must be
    checked against it.
- Landed prerequisite on the same files:
  [change 11](../../changes/archive/11.header-icon-menus-profile-popover-tab-ic.md) →
  [job 38](38.header-icon-menus-profile-popover-tab-ic.md). The `requires: ['38']`
  frontmatter keeps this job behind that archived baseline.

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

**FE — `shared` (primitives)**

| Path | Change |
|---|---|
| `frontend/src/shared/ui/listbox/Listbox.tsx` · `Listbox.test.tsx` | NEW — the app-drawn select replacement (T001) |
| `frontend/src/shared/ui/sheet/Sheet.tsx` · `Sheet.test.tsx` | NEW — arbitrary-content overlay extracted from `Dialog` (T002) |
| `frontend/src/shared/ui/dialog/Dialog.tsx` | rebuilt on `Sheet`; public props unchanged (T002) |
| `frontend/src/shared/ui/popover/Popover.tsx` · `Popover.test.tsx` | `align` + `phone` props, viewport clamping (T003) |
| `frontend/src/shared/ui/action-bar/ActionBar.tsx` | phone padding, `surface` variant (T004, T023) |
| `frontend/src/shared/ui/select/Select.tsx` · `Select.test.tsx` | **DELETED** (T009) |
| `frontend/src/shared/ui/index.ts` | `Listbox`/`Sheet` exported, `Select` export removed |
| `frontend/src/app/styles/index.css` | carousel width + translucent-surface tokens (T004, T018) |

**FE — `features` (Select migration + reshaping)**

| Path | Change |
|---|---|
| `features/select-model/ui/StageModelSelect.tsx` (+ test) | → `Listbox` (T005) |
| `features/select-post-voice/ui/PostVoiceSelect.tsx` | → `Listbox`; reassignment dialog untouched (T005) |
| `features/select-post-purpose/ui/PostPurposeSelect.tsx` | → `Listbox` (T005) |
| `features/select-post-language/ui/PostLanguageSelect.tsx` (+ test) | → `Listbox` (T005) |
| `features/edit-post-content/ui/BlockEditor.tsx` | three selects migrated (T006) |
| `features/create-voice/ui/CreateVoiceForm.tsx` | → `Listbox` (T007) |
| `features/configure-model-pair/ui/ModelPairForm.tsx` (+ test) | → `Listbox` (T007) |
| `features/give-voice-feedback/ui/SentenceFeedback.tsx` | → `Listbox` (T007) |
| `features/configure-publishing-agent/ui/ConfigurePublishingAgent.tsx` | two mounts (T008) |
| `features/publish-post/ui/PublishPostForm.tsx` | two mounts (T008) |
| `features/manage-users/ui/UserPlanSelect.tsx` | → `SegmentedControl` (T008) |
| `features/generate-post/ui/GenerationOptions.tsx` | reduced to the target-length body (T011) |
| `features/generate-post/ui/GenerationActions.tsx` | stops owning options/length (T011) |
| `features/edit-with-ai/ui/ReviseForm.tsx` (+ test) | dock row 1 + collapsible row (T014) |
| `features/finalize-post/ui/FinalizeActions.tsx` | dock row 2 (T015) |

**FE — `widgets` · `pages`**

| Path | Change |
|---|---|
| `widgets/generation-brief/` (`ui/GenerationBrief.tsx`, `index.ts`, test) | NEW composition (T010) |
| `widgets/refine-dock/` (`ui/RefineDock.tsx`, `index.ts`, test) | NEW composition (T013) |
| `widgets/contact-sheet/ui/ContactSheet.tsx` (+ test) | phone carousel (T018) |
| `pages/editor/ui/DraftEditor.tsx` | panel/dock/title restructure, length lifted, title re-seed (T011, T012, T016, T017) |
| `pages/editor/ui/EditorPage.test.tsx` | updated for the new composition (T022) |
| `pages/ai-models/ui/AIModelsPage.tsx` | two mounts (T007) |

**BE — `internal/post` (the only backend context touched)**

| Path | Change |
|---|---|
| `backend/internal/post/store/queries/posts.sql` | `FinalizePost` also sets `title` (T019) |
| `backend/internal/post/store/sqlc/**` | (gen) `pnpm gen:sql` output (T019) |
| `backend/internal/post/ports.go` | `ContentStore.Finalize` signature (T020) |
| `backend/internal/post/store/store.go` | `Store.Finalize` passes the title (T020) |
| `backend/internal/post/service.go` (`Finalize`, ~line 604) | resolves the title to write (T020) |
| `backend/internal/post/fake_test.go` · `lifecycle_test.go` | fake + new cases (T020) |

**Tooling / strings**

| Path | Change |
|---|---|
| `scripts/lint-style-escapes.mjs` | a `<select` finding + probe cases (T009) |
| `app/providers/i18n/resources/{ko,en}/posts.ts` · `common.ts` | both locales (T021) |

**Explicitly NOT touched:** `proto/**`, `frontend/src/shared/api/gen/**`,
`backend/internal/platform/db/migrations/**`, `backend/internal/generation/**`,
`backend/internal/voice/**`, and every step-③ surface (`widgets/export-panel`,
`widgets/publish-panel`, `features/finalize-post/ui/VoiceLearningPanel.tsx`).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen: `pnpm gen:sql` re-run and its `internal/post/store/sqlc` output committed (T019).
      **No proto change and no migration** — `proto/**`, `shared/api/gen/**` and
      `internal/platform/db/migrations/**` must be absent from `git diff --stat`
- [x] FE `pnpm build:web` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style` · `pnpm lint:style:probe` ·
      `pnpm test` / BE `go vet ./... && go build ./... && go test ./...` (Docker) pass
- [x] `grep -rn '<select' frontend/src --include='*.tsx' | grep -v gen/` is empty and
      `frontend/src/shared/ui/select/` no longer exists
- [x] T023's glassmorphism comparison was shown to the owner, their choice applied, and the
      unchosen variant (code + tokens + comparison surface) deleted
- [x] Constitution sanity ([I1]–[I7]): nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (the relevant `/fe-architecture` · `/be-architecture` checklist)

## Review

- [x] `/code-review` applied (rejections noted with reason) · for non-trivial logic, `/codex:review --background`

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `spec/policy/**`·`spec/tech/**` · config values in their owner
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
