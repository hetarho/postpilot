---
job: '21'
type: 'change'
source: 'changes/06.start-write-comparisons-from-the-model-l'
plan: 'plan/09.stage-model-experiments-and-leaderboards'
status: done
title: 'Start Write Comparisons from the Model Lab'
---

# Job 21: Start Write Comparisons from the Model Lab (change)

> Implementation work doc. Source spec: [changes/06.start-write-comparisons-from-the-model-l](../changes/06.start-write-comparisons-from-the-model-l.md).
> /implement-job 21 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.

## Acceptance Criteria (from changes/06.start-write-comparisons-from-the-model-l)

<!-- The source spec's acceptance criteria. After building, verify each is true in the running code. -->

- [x] A1 Selecting the `글 작성` tab shows an existing-post selector and a `비교 시작` action instead of the editor-only notice; observe and analyze tabs retain their current controls.
- [x] A2 A write comparison cannot start until a post and two configured write candidates are available. A selected post is loaded before enablement, and an active job, unresolved experiment, unavailable required observe model, or request failure is represented with an accurate visible state.
- [x] A3 Starting from a no-photo post sends its slug, the exact configured write A/B refs, no observe ref, and the post's optional target length to `StartWriteExperiment`, then navigates to the returned blind experiment.
- [x] A4 Starting from a post with photos additionally requires and sends the account's explicit active vision-capable observe ref; the page never infers or silently saves any model selection.
- [x] A5 Mounting or switching tabs starts no experiment or provider work. The existing editor write-comparison path, ordinary generation, observe/analyze starts, blind review, winner application/adoption, ranking, and post canonical-content behavior do not regress.
- [x] A6 Focused frontend tests plus lint, FSD/style gates, and the production frontend build pass without a backend or generated-contract change.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

- [x] T001 Frontend architecture and regression preflight: apply `/fe-architecture`, read the plan-09 experiment
  methodology and design-language rules, and preserve the existing `pages -> features -> entities -> shared`
  dependency direction. Reuse the current post-backed `StartWriteExperiment` contract, mutation, post entity,
  model selections, pair projection, and shared controls. Add no dependency, backend change, generated contract,
  migration, or runtime configuration value.
- [x] T002 Model-lab write start: in the AI model page, show the existing-post selector for both post-backed stages
  with stage-accurate labeling. Replace the write-only notice with a start surface that loads the selected post,
  resolves the explicit active observe selection and configured write candidates, applies the shipped generation
  preconditions plus the unresolved-experiment guard, forwards optional target length, and navigates to the returned
  blind experiment. Preserve the observe/analyze starts and start no work on mount or tab switch.
- [x] T003 Frontend test gate: expose/reuse the existing write-experiment start mutation without duplicating its
  transport mapping. Extend the fake experiment service only as needed for the real routed model page, then cover a
  no-photo write start payload/navigation and a photo-backed start that remains blocked without an active vision
  model and sends that explicit ref once configured. Cover visible pending/precondition/error behavior without paid
  calls.
- [x] T004 Verification and regression: run focused tests, the full frontend test suite, `pnpm build:web`, `pnpm
  lint`, `pnpm --filter ./frontend lint:fsd`, `pnpm lint:style`, `pnpm lint:style:probe`, and `pnpm lint:spec`.
  Review the diff against the FE architecture/design checklists and plan 09's existing acceptance boundary.

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../plan/00.overview.md) §3 (_The constitution_)
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3 (layers/slices/segments) ·
  BE §2 (context layout, dependency rule). Invoke `/fe-architecture` · `/be-architecture` for the
  surfaces this job touches.
- PRD / policy / tech touched: [plan/09.stage-model-experiments-and-leaderboards](../plan/09.stage-model-experiments-and-leaderboards.md)
  · [tech/model-experiment-methodology](../tech/model-experiment-methodology.md). No policy, contract, schema, or
  config change.

## Affected files (blast radius)

<!-- Exact paths found from the source spec + a code grep — nothing outside this scope is touched.
     For each, note its target placement: FE layer/slice/segment (§3.1) or BE context/package (§2). -->

- `frontend/src/pages/ai-models/ui/AIModelsPage.tsx` — page/ui composition and stage-specific start feedback.
- `frontend/src/pages/ai-models/ui/AIModelsPage.test.tsx` — page/ui routed interaction coverage.
- `frontend/src/entities/post/api/usePost.ts` · `usePost.test.tsx` — entity/api detail-refresh state used by the
  selected-post gate.
- `frontend/src/entities/model-catalog/api/useModelSetup.ts` · `frontend/src/entities/model-catalog/index.ts` —
  entity/api comparison-pair save state and public export.
- `frontend/src/features/generate-post/index.ts` — feature public API for the existing write-start mutation.
- `frontend/src/test/experiments.ts` — shared frontend test adapter for experiment list/leaderboard/start behavior.
- `spec/plan/09.stage-model-experiments-and-leaderboards.md` — as-built product SSOT.
- `spec/tech/model-experiment-methodology.md` — write-comparison entry-point clarification.
- `spec/plan/00.overview.md` · `spec/changes/06.start-write-comparisons-from-the-model-l.md` · this job — workflow
  registration and completion records.

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration applied (if any): n/a — the existing RPC and schema are unchanged
- [x] FE `pnpm build:web` · `pnpm --filter ./frontend test` (61 files / 281 tests) · `pnpm lint` · `pnpm --filter
  ./frontend lint:fsd` · `pnpm lint:style` · `pnpm lint:style:probe` pass in the shared worktree. The production build
  and full suite also passed in an isolated clean worktree containing only this job's diff.
- [x] Constitution sanity ([I1]–[I7]): nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (`/fe-architecture`; no backend surface)

## Review

- [x] Diff review applied: post-switch mutation lifetime and pair-save race fixed. `/codex:review --background`
  found one high-severity cached-post refresh race; fixed by exposing and gating on `usePost.isFetching`. No findings
  were rejected.

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality · update affected `spec/tech/model-experiment-methodology.md`; policy/config
  unchanged
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
