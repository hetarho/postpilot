---
job: '01'
type: 'new'
source: 'plan/01.accounts-and-login'
plan: 'plan/01.accounts-and-login'
status: done
title: 'Persistence Foundation and Auth Backend'
---

# Job 01: Persistence Foundation and Auth Backend (new)

> Implementation work doc. Source spec: [plan/01.accounts-and-login](../plan/01.accounts-and-login.md).
> /implement-job 01 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.
>
> **Split:** plan 01 is built as two jobs. This job is the backend half — SQLite + embedded goose migrations + sqlc
> (the persistence foundation every later job stands on), the `auth` context, the Connect interceptor, credentialed
> CORS, `auth.proto`, and the `adduser` operator command. The login screen, credentialed transport and route guard
> are [job 02](02.login-screen-and-session-guard.md). Build order: this is the first job of the whole backlog.

## Acceptance Criteria (from plan/01.accounts-and-login)

<!-- Plan 01 criteria owned by this job (plan AC numbers in parentheses). AC 9 and the devtools half of AC 4 belong to job 02. -->

- [x] A1 (plan AC1) Any RPC other than `Login`/health called without a session returns `Unauthenticated` (HTTP 401); the same call with a valid cookie succeeds.
- [x] A2 (plan AC2) Altering a single character of the `pp_session` value yields 401 (the hashed lookup misses).
- [x] A3 (plan AC3) Login failure for an unknown id and for a known id with the wrong password return byte-identical error messages and codes, and the unknown-id path performs a real argon2id verification (asserted by a unit test on the dummy path).
- [x] A4 (plan AC4, server half) No response body, log line, or URL ever contains the session token; it travels only in the `Set-Cookie` / `Cookie` headers.
- [x] A5 (plan AC5) There is no signup RPC or screen; an account created with `adduser` can log in; a duplicate id is refused with a clear message.
- [x] A6 (plan AC6) After `Logout`, replaying the previous cookie yields 401 (server-side revocation, not just cookie clearing).
- [x] A7 (plan AC7) A session expires 30 days after login on both ends: cookie `Max-Age=2592000` and `sessions.expires_at`.
- [x] A8 (plan AC8) A deliberately failing migration makes the api process exit non-zero before it serves; `/health` never turns green ([I7]).
- [x] A9 (plan AC10) The Set-Cookie header carries exactly `HttpOnly; Secure; SameSite=Lax; Path=/` and `Max-Age=2592000`, and no `Domain`.
- [x] A10 (derived) `docker compose run --rm api adduser <id>` works against the production image unchanged (`ENTRYPOINT ["/api"]` dispatches the `adduser` subcommand); `go run ./cmd/adduser <id>` does the same on a host.
- [x] A11 (derived) The api opens SQLite in WAL mode with exactly one write connection; `go test ./...` runs in CI.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

- [x] T001 `/library-setup` before adding Go deps: `modernc.org/sqlite` (pure Go driver — the image stays `CGO_ENABLED=0` distroless), `github.com/pressly/goose/v3`, `golang.org/x/crypto` (argon2), `golang.org/x/term` (no-echo password prompt). sqlc runs as a Docker tool (`sqlc/sqlc`), not a Go dependency. Verify the modernc DSN pragma syntax from current docs.
- [x] T002 Config (`backend/internal/platform/config/config.go`): add `DBPath` (env `DB_PATH`, default `data/postpilot.db` — resolves to `/data/postpilot.db` in the prod image (WORKDIR `/`, volume `./data:/data`) and `/app/data/postpilot.db` in the dev container), `SessionTTL = 720 * time.Hour`, `SessionCookieName = "pp_session"`; validate `CORS_ORIGIN` at load (non-empty, not `*`, absolute URL with scheme+host) so a wildcard can never meet `AllowCredentials`.
- [x] T003 [P] `.env.example`: document `DB_PATH` (default is fine locally); `.env.production.example`: `DB_PATH=/data/postpilot.db`; `.gitignore`: `backend/data/`; `docker-compose.yml`: add a `backend_data` volume mounted at `/app/data` and drop the "No database yet / postgres" comment.
- [x] T004 `backend/internal/platform/db/db.go`: `Open(path) (*DB, error)` — create the parent dir, DSN pragmas `journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000`; `Writer *sql.DB` with `SetMaxOpenConns(1)` (the single serialized writer, ARCHITECTURE §2.4) and `Reader *sql.DB`; `Close()`.
- [x] T005 (migrate) `backend/internal/platform/db/migrations/0001_users_sessions.sql` (goose `Up`/`Down`; schema from plan 01: `users`, `sessions`, indexes on `sessions(user_id)`, `sessions(expires_at)`) + `backend/internal/platform/db/migrate.go`: `//go:embed migrations/*.sql`, `Migrate(ctx, writer)` via goose (`SetBaseFS`, dialect `sqlite3`, `UpContext`). Called from `cmd/api` **before** the listener; on error `slog.Error` + `os.Exit(1)` so `/health` never answers ([I7]).
- [x] T006 sqlc: `backend/sqlc.yaml` (v2, engine `sqlite`, schema = the migrations dir, one `queries` entry per context emitting `internal/<ctx>/store/sqlc`); `backend/internal/auth/store/queries/auth.sql` (`CreateUser`, `GetUser`, `CreateSession`, `GetSessionByToken`, `DeleteSession`, `DeleteExpiredSessions`); extend `scripts/gen.mjs` + `scripts/lib.mjs` with a `sqlc generate` Docker step (`hasSqlcConfig()`), add `gen:sql` to the root `package.json`; commit the generated package.
- [x] T007 (gen) `proto/postpilot/v1/auth.proto`: `AuthService { Login, Logout, GetMe }` + `LoginRequest{login_id,password}`, `LoginResponse{User}`, `LogoutRequest/Response`, `GetMeRequest/Response{User}`, `User{id}` (token never in a message). `pnpm gen:proto`; commit `backend/internal/gen/postpilot/v1/auth.pb.go`, `…/postpilotv1connect/auth.connect.go`, `frontend/src/shared/api/gen/postpilot/v1/auth_pb.ts`.
- [x] T008 `backend/internal/auth` (flat context): `types.go` (User, Session), `password.go` (argon2id PHC string encode/verify; constants `argonTime=3`, `argonMemoryKiB=64*1024`, `argonThreads=2`, `saltLen=16`, `keyLen=32`), `dummy.go` (`dummyHash` — a valid PHC hash of a random string, used when the id is unknown), `token.go` (32 random bytes → base64url; `sha256` hex for storage), `ports.go` (consumer-owned `Store` interface), `service.go` (`Login` with timing equalization + one generic failure, `Logout`, `Authenticate(token)` → user id with expiry check and lazy delete, `SweepExpired`).
- [x] T009 `backend/internal/auth/store/store.go`: row ↔ domain mapping over the sqlc package; timestamps as RFC3339 TEXT.
- [x] T010 `backend/internal/auth/rpc/handler.go`: Connect handler. `Login` → `res.Header().Add("Set-Cookie", (&http.Cookie{Name: pp_session, Value: token, Path: "/", MaxAge: 2592000, HttpOnly: true, Secure: true, SameSite: http.SameSiteLaxMode}).String())` — no `Domain`; `Logout` → delete session + same cookie with `MaxAge: -1`; `GetMe` → user from ctx. Bad credentials → `connect.CodeUnauthenticated` with the fixed message.
- [x] T011 `backend/internal/auth/interceptor.go`: `NewInterceptor(svc, exempt ...string) connect.Interceptor` — parse `Cookie` from `req.Header()`, `Authenticate`, inject via `auth.WithUser(ctx, id)` / `auth.UserFromContext(ctx)`; exempt `/postpilot.v1.AuthService/Login`; any failure → `CodeUnauthenticated`.
- [x] T012 `backend/internal/platform/rpcserver/server.go`: change `New` to take `Options{Interceptors []connect.Interceptor; Handlers []Registrar}` (a `Registrar` mounts one Connect service on the mux with the shared handler options) so each context registers itself; CORS `AllowCredentials: true`, remove the `Authorization` header allowance and rewrite the stale "Bearer token" comment (cookie auth). `/health` and `maxRequestBytes` unchanged.
- [x] T013 `backend/cmd/api/main.go`: `os.Args[1] == "adduser"` → `provision.Run(os.Args[2:])` and exit (keeps `ENTRYPOINT ["/api"]` and makes `docker compose run --rm api adduser <id>` work); otherwise config → `db.Open` → `db.Migrate` (exit 1 on error) → `auth.SweepExpired` → services → `rpcserver.New` with the auth interceptor → serve.
- [x] T014 `backend/internal/auth/provision/adduser.go`: `Run(args)` — id from args, password read twice from stdin (`x/term` when a TTY, plain read otherwise), hash, insert; duplicate id → non-zero exit with message; opens `DB_PATH` and runs migrations first (works on a fresh volume). `backend/cmd/adduser/main.go` is a thin wrapper for host use. `backend/Dockerfile` needs no change.
- [x] T015 [P] Docs: `DEPLOY.md` §4 (`data/` now holds the SQLite file; `adduser` command), §7 (DB and auth entries are now built: `internal/platform/db`, cookie interceptor — replace the "Bearer" wording); `README.md` if it lists env vars.
- [x] T016 Tests (`go test ./...`): argon2 PHC round trip + wrong password; `Login` runs the dummy verification when the id is unknown (spy verifier) and returns identical errors; interceptor (no cookie / tampered / expired / valid → ctx user; Login exempt); exact `Set-Cookie` string; session expiry math; `Migrate` returns an error for a broken migration (test FS); `provision.Run` duplicate id.
- [x] T017 `.github/workflows/ci.yml`: add `go test ./...` to the backend job (first Go tests land here).
- [x] T018 Manual verification on the dev stack: `curl` an RPC without a cookie → 401; login via curl, tamper one character → 401; logout then replay → 401; `docker compose run --rm api adduser test` on the dev image.

## Deviations from the checklist (as built)

The tasks below were built differently from their wording. Each is reflected in
[plan/01](../plan/01.accounts-and-login.md) and [policy/auth.md](../policy/auth.md).

| Task | Wording | As built | Why |
|---|---|---|---|
| T002 | `SessionCookieName` in `platform/config` | `auth.SessionCookieName` in `internal/auth` | matches plan 01's config table; the cookie's wire name is an auth detail, while `SessionTTL` (an ops-tunable duration) stays on `Config` |
| T005 | goose global `SetBaseFS`/`SetDialect`/`UpContext` | `goose.NewProvider(DialectSQLite3, db, fsys)` | the current documented API; no package-level global state, and it takes the FS directly so the failure path is testable |
| T011 | interceptor at `internal/auth/interceptor.go` | `internal/auth/rpc/interceptor.go`, as a full `connect.Interceptor` | keeps `connect` and `net/http` inside the context's transport segment (ARCHITECTURE §2.1/§2.2); `UnaryInterceptorFunc` would leave streaming handlers unauthenticated |
| T011 | exempt `Login` only | exempt `Login`, `Logout`, `HealthService/Ping` | the cookie is HttpOnly: a 401 on Logout strands it in the browser for 30 days. Ping is the pre-account wire test |
| — | (not in the checklist) | `scripts/gen.mjs` runs both Docker tools as `--user $(uid):$(gid)` | generated files were landing root-owned and could not be edited or deleted without sudo |

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../plan/00.overview.md) §3 (_The constitution_) — this job lands **[I7]** and keeps the image CGO-free (**[I6]** depends on it).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — BE §2.1 (`internal/auth`, `internal/platform/{db,config,rpcserver}`), §2.2 (store maps row↔domain; handler maps proto↔domain), §2.4 (one writer, WAL, embedded migrations), §4 (config). Invoke `/be-architecture` for every new Go file.
- PRD / policy / tech touched: [PRD.md](../../PRD.md) F-1, §5 (`users`, `sessions`), §6.3, §10.1–.2; `DEPLOY.md` §2 (health gate), §4, §7. Policy to land on completion: `spec/policy/auth.md` (rules enumerated in plan 01 → Policy / Config Impact). No `spec/tech` doc — the PRD owns the decisions.

## Affected files (blast radius)

| Path | Placement (ARCHITECTURE) | Change |
|---|---|---|
| `backend/go.mod`, `backend/go.sum` | — | add modernc sqlite, goose, x/crypto, x/term |
| `backend/internal/platform/config/config.go` | platform/config | `DBPath`, `SessionTTL`, `SessionCookieName`, CORS origin validation |
| `backend/internal/platform/db/db.go`, `migrate.go`, `migrations/0001_users_sessions.sql` | platform/db (new) | SQLite open, writer/reader, embedded goose migrations |
| `backend/sqlc.yaml`, `scripts/gen.mjs`, `scripts/lib.mjs`, `package.json` (`gen:sql`) | tooling | sqlc generation via Docker |
| `proto/postpilot/v1/auth.proto` + generated `backend/internal/gen/postpilot/v1/auth*.go`, `frontend/src/shared/api/gen/postpilot/v1/auth_pb.ts` | contract (gen, committed) | AuthService |
| `backend/internal/auth/{types,password,dummy,token,ports,service,interceptor}.go` | context `auth` (flat) | domain + use-cases + interceptor |
| `backend/internal/auth/store/store.go`, `store/queries/auth.sql`, `store/sqlc/*` (gen) | `auth/store` | persistence |
| `backend/internal/auth/rpc/handler.go` | `auth/rpc` | Connect handler, cookie header |
| `backend/internal/auth/provision/adduser.go`, `backend/cmd/adduser/main.go` | `auth/provision`, `cmd/adduser` | operator provisioning |
| `backend/internal/platform/rpcserver/server.go` | platform/rpcserver | `Options` (interceptors, registrars), credentialed CORS |
| `backend/cmd/api/main.go` | composition root | subcommand dispatch, migrations, wiring |
| `docker-compose.yml`, `.env.example`, `.env.production.example`, `.gitignore` | infra/config | dev data volume, `DB_PATH` |
| `.github/workflows/ci.yml` | CI | `go test ./...` |
| `DEPLOY.md`, `README.md` | ops docs | DB/auth entries, `adduser` |

Not touched: `backend/Dockerfile` (single binary already), `frontend/src/**` except the generated `auth_pb.ts` (consumed by job 02).

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria
- [x] Codegen / migration applied (if any): `pnpm gen:proto` · `pnpm gen:sql` · migrations embedded and run at boot
- [x] FE `pnpm build:web` · `pnpm lint` · `pnpm --filter ./frontend lint:fsd` / BE `go vet ./... && go build ./... && go test ./...` (Docker) pass (if any)
- [x] Constitution sanity ([I1]–[I7]): nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (the relevant `/fe-architecture` · `/be-architecture` checklist)

## Review

- [x] `/code-review` (high) — **6 findings, all applied, 0 rejected**:
  1. interceptor was `UnaryInterceptorFunc` → streaming handlers unauthenticated (now a full `connect.Interceptor`)
  2. `decodeHash` did not validate cost params → `argon2.IDKey` panics on `t=0`/`p=0` instead of taking Login's
     malformed-hash path
  3. `Logout` was gated → an expired session got 401 and its HttpOnly cookie was never cleared
  4. `fmt.Fscanln` tokenized the piped password on whitespace → a passphrase with spaces was truncated
  5. `sync.OnceValue` made the first unknown-id login pay two argon2id derivations → timing leak on the first probe
     after every restart (now derived in `NewService`)
  6. `time.RFC3339Nano` trims trailing zeros → variable-width strings do not sort, so `expires_at < ?` could sweep a
     live session (now a fixed-width write layout)
- [x] Codex (`gpt-5.5`, xhigh) — 5 findings: 4 duplicated the above; **1 new, applied**:
  `CORS_ORIGIN=https://*.example.com` passed validation, and `rs/cors` reads an embedded `*` as a pattern and
  reflects any matching subdomain **with credentials**. Verified by probe, then rejected at config load.
  (`/codex:review` is not installed here, so Codex ran directly as a background task per the skill's fallback.
  A first run failed on an unsupported `reasoning.effort`; rerun with `xhigh`.)
- [x] Comment pass — no history/narration/tautology comments in the diff; one comment in `handler.go` that the
  Logout exemption made stale was rewritten.

## After completion — reflect into the SSOT

- [x] `plan/01` updated to as-built (adduser as an `api` subcommand, interceptor in `auth/rpc`, Logout public,
      fixed-width timestamps, wildcard-pattern CORS rule, sqlc wiring, new AC 11) · `spec/policy/auth.md` created ·
      config values in their owner (`platform/config`, `auth.SessionCookieName`, `.env*.example`)
- [x] (if type=change) move the `changes/` source doc to `changes/archive/`
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
