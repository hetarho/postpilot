---
job: '09'
type: 'new'
source: 'plan/03.voice-profile-learning'
plan: 'plan/03.voice-profile-learning'
status: done
title: 'Voice Screen and Empty Profile Warning'
---

# Job 09: Voice Screen and Empty Profile Warning (new)

> Implementation work doc. Source spec: [plan/03.voice-profile-learning](../plan/03.voice-profile-learning.md).
> /implement-job 09 builds it via the two checklists below. When done, reflect the result into the
> SSOT (plan/policy/tech) and set status: done. Write it in English.
>
> **Split:** the frontend half of plan 03 — the `말투` screen (`/voice`): paste + learn, sample list, styleguide and
> rules editors, analysis progress, the analyze-stage model picker — plus the empty-profile banner on the editor.
> Requires [job 08](08.voice-profile-backend-and-analysis-job.md), job 06 (`StageModelSelect`) and job 07 (`useJob`).

## Acceptance Criteria (from plan/03.voice-profile-learning)

<!-- Plan 03 criteria owned by this job (plan AC numbers in parentheses). -->

- [x] A1 (plan AC1) With an empty profile (no styleguide, no samples) the writing screen shows the warning; after one successful analysis it disappears.
- [x] A2 (plan AC4, UI half) The 학습 button stays disabled below 200 characters with a live count; the server's `InvalidArgument` (if reached) is shown verbatim.
- [x] A3 (plan AC6, UI half) Editing the styleguide or rules and saving persists; reloading shows the edited text.
- [x] A4 (plan AC7, UI half) Deleting a sample removes it from the list and, when samples remain, the re-analysis progress appears.
- [x] A5 (plan AC8, UI half) Progress is visible after 학습; reloading the page mid-analysis resumes polling from `active_job_id` to completion, and the new styleguide appears without a manual refresh.
- [x] A6 (derived) 학습 requires an analyze-stage model; with none selected the button is disabled with "모델을 선택하세요".
- [x] A7 (derived) When a styleguide already exists, 학습 asks for confirmation ("재분석하면 현재 문체 규칙을 덮어씁니다") before submitting.

## Implementation Checklist

<!-- How to build it, top to bottom. [P] = parallel (different files, no dependency). Flag (gen)/(migrate).
     Config values are never magic literals — FE via shared/config, BE via internal/platform/config. -->

- [x] T001 `frontend/src/shared/config/index.ts`: `VOICE_SAMPLE_MIN_CHARS = 200` (mirror; the backend is authoritative).
- [x] T002 `frontend/src/shared/api`: `voiceClient`; re-export `VoiceService`, `VoiceProfile`, `VoiceSample` types.
- [x] T003 `frontend/src/entities/voice-profile`: `model/types.ts` (`isEmptyProfile(p)` = no styleguide and no samples), profile query/mappers, update/add/delete mutations, and public API.
- [x] T004 `frontend/src/features/learn-voice`: label/body inputs, trimmed Unicode live count, analyze-selection gate, overwrite confirmation, raw server errors, and job-id handoff. The page composes `StageModelSelect` without feature cross-imports.
- [x] T005 `frontend/src/features/edit-voice-profile`: independent dirty drafts and field-scoped saves through `useUpdateVoiceProfile`; pending responses preserve newer input and patch only the field they settled.
- [x] T006 `frontend/src/features/manage-voice-samples`: metadata list, confirmed delete, cache refresh, and re-analysis job handoff.
- [x] T007 `frontend/src/pages/voice`: composition, `/voice` authenticated route, and the signed-in header's `말투` link in `AuthenticatedLayout` (the current as-built owner of authenticated chrome).
- [x] T008 `frontend/src/widgets/voice-warning`: empty-only banner and `/voice` link, mounted above both new and existing post editors.
- [x] T009 Tests: 199/200 boundary, missing model, confirmation, raw server error, delete/re-analysis, empty warning, active-job resume, terminal refresh, edit persistence, account-partitioned caches, and delayed-response input/cache races.
- [x] T010 Dev-stack-equivalent Connect integration covers learn/job resume/completion/profile refresh and warning state. A live provider call was not run because `OPENROUTER_API_KEY` is unset in this environment.

## Grounding

- Constitution ([I1]–[I7]): [00.overview](../plan/00.overview.md) §3 (_The constitution_) — **[I4]** (the screen never shows or edits another account's profile).
- Architecture (placement): [ARCHITECTURE.md](../ARCHITECTURE.md) — FE §3.1 (`entities/voice-profile`, `features/learn-voice`, `features/edit-voice-profile`, `features/manage-voice-samples`, `widgets/voice-warning`, `pages/voice`; features composed by the page, never cross-imported). Invoke `/fe-architecture`.
- PRD / policy / tech touched: [PRD.md](../../PRD.md) F-3 (말투 탭, 200자, 규칙 수정, 샘플 삭제, 완료 조건 1), S2. Policy: `spec/policy/voice.md` (created by job 08; add the confirm-before-overwrite rule on completion).

## Affected files (blast radius)

| Path | Placement (ARCHITECTURE §3) | Change |
|---|---|---|
| `frontend/src/shared/config/index.ts` | shared/config | `VOICE_SAMPLE_MIN_CHARS` |
| `frontend/src/shared/api/transport.ts`, `shared/api/index.ts` | shared/api | `voiceClient` |
| `frontend/src/entities/voice-profile/**` | entities/voice-profile | profile model + hooks |
| `frontend/src/features/learn-voice/**`, `features/edit-voice-profile/**`, `features/manage-voice-samples/**` | features | the three verbs |
| `frontend/src/widgets/voice-warning/**` | widgets/voice-warning | editor banner |
| `frontend/src/pages/voice/**` | pages/voice | `/voice` screen |
| `frontend/src/pages/editor/ui/EditorPage.tsx` | pages/editor | banner mounted |
| `frontend/src/app/routes/router.tsx`, `app/routes/AuthenticatedLayout.tsx` | app/routes | route + nav |
| `proto/postpilot/v1/voice.proto` | transport contract | presence-aware profile field patches |
| `backend/internal/voice`, `backend/internal/voice/store` | voice behavior + persistence adapter | atomic field-scoped profile saves |

## Verification / DoD

- [x] Every **Acceptance Criteria** item above is true in the current code
- [x] (if type=change) no regression of the existing plan's acceptance criteria — N/A (`type: new`)
- [x] Codegen / migration applied (if any): `pnpm gen` regenerated optional proto fields and the `SetStyleguide` sqlc query; no migration required
- [x] FE `pnpm build:web` · `pnpm lint` · `pnpm --filter ./frontend lint:fsd` · `pnpm test` / BE `go vet ./... && go build ./...` (Docker) pass (if any)
- [x] Constitution sanity ([I1]–[I7]): account-partitioned caches preserve [I4]; nothing in the diff breaks an invariant
- [x] Architecture self-audit passed (the relevant `/fe-architecture` · `/be-architecture` checklist)

## Review

- [x] Two Codex reviews applied: fixed account cache isolation, accessible labels, field-scoped persistence, stale snapshot cache writes, and delayed-response input loss; no findings rejected

## After completion — reflect into the SSOT

- [x] Update `plan/` to the new reality (plan 03: the re-analysis confirmation fires whenever a styleguide exists, not only after a hand edit) · update `spec/policy/voice.md` · config values in their owner
- [x] (if type=change) move the `changes/` source doc to `changes/archive/` — N/A (`type: new`)
- [x] 00.overview progress board ✅ · this doc's frontmatter `status: done`
