# T410 The 분야 phrase batch, its Naver search client and its table are removed
> st:todo | ssot:QUAL-1 QUAL-23 QUAL-25 QUAL-26 QUAL-47 | base:QUAL@5 | dep:T409

## goal
The API no longer starts a phrase batch, reads Naver search credentials or holds `field_phrase_lists`. The `naversearch` package is gone, and the 분야 catalogue keeps its ids and names, with no query strings.

## acceptance
- [ ] boot starts no phrase goroutine and logs nothing about phrase refresh; `NAVER_SEARCH_CLIENT_ID`, `NAVER_SEARCH_CLIENT_SECRET` and `QUALITY_PHRASE_REFRESH_INTERVAL` are read nowhere, and setting them changes nothing
- [ ] `backend/internal/naversearch/` no longer exists and nothing imports it; `quality.Service` has no phrase collaborator; `quality.Field` has no `Query`; M1–M4 measurement and `quality.Known`/`FieldByID` behave exactly as before
- [ ] new goose migration (next free number, after T409's) drops `field_phrase_lists`; Down recreates it empty with 0077's DDL; migration test in the `migration_0083_test.go` pattern; `TestMigration0077IsIdempotent` no longer expects the table
- [ ] QUAL-47 is pinned: a test fails if any backend package other than a migration names `openapi.naver.com`, `naverapihub` or `naversearch`. Extend `quality/boundary_test.go`, or add a cmd/api boundary test.
- [ ] DEPLOY.md, `.env.example` and `.env.production.example` carry no phrase-batch or Naver-search settings
- [ ] verify BE (ARCH-26) · `pnpm gen:sql && git diff --exit-code -- backend` · `pnpm lint:retirement`

## impl notes
- Delete from `backend/internal/quality/`:
  - `batch.go`, `phrases.go` and `stopwords.go`. `stopwords.go` is used only by `phrases.go`; `Tokens` in `text.go` stays.
  - `batch_test.go` and `phrases_test.go`
  - `store/queries/phrases.sql` and its sqlc output. `models.go` loses `FieldPhraseList`.
- Delete the whole `backend/internal/naversearch/` package, and `backend/cmd/api/phrase_batch_test.go`.
- Edit `backend/internal/quality`:
  - `limits.go`: drop the phrase `const` block and the `time` import.
  - `ports.go`: drop `PhraseLists` and `BlogSearch`.
  - `types.go`: drop `PhraseList`.
  - `service.go`: drop `Deps.Phrases`, the field, its panic and `PhrasesFor`.
  - `fields.go`: drop `Field.Query`, its derivation and `strings`; rewrite the package doc. `Name` stays as catalogue data.
  - `store/store.go`: drop the `PhraseLists` assertion, `PhraseList`, `ReplacePhraseList` and `encoding/json`.
- Wiring in `cmd/api`:
  - `adapters_quality.go`: delete `naverBlog`, `qualityBlogSearch`, `newPhraseBatch` and `phraseSearch`, plus their imports.
  - `contexts.go`: drop `phraseBatch`, `Phrases: qualityStore` and the `newPhraseBatch` block.
  - `serve.go`: drop `go c.phraseBatch.Run(ctx)`.
- Config, `backend/internal/platform/config/config.go`: drop `NaverSearchClientID`, `NaverSearchClientSecret`, `NaverSearchEnabled` and `QualityPhraseRefreshInterval`, with their reads and resolution. Keep `optionalPositiveDuration`: `clip_media.go` uses it.
- Tests:
  - `config_test.go`: delete the two phrase config tests.
  - `quality/limits_test.go`: delete the two phrase tests.
  - `fields_test.go`: drop `Query`.
  - `service_test.go`: drop `fakePhrases` and its test. `newQualityService` drops the phrase return, which changes 13 call sites; also drop the `"phrases"` panic case.
  - `store/store_test.go`: delete the three phrase-list tests.
  - `rpc/handler_test.go`: drop `noPhrases`.
  - `cmd/api/adapters_quality_test.go`: drop `Phrases:`, `TestAPhraseRefreshBelowTheFloorFailsTheBoot` and `qualityPhraseListsForTest`.
  - `quality/boundary_test.go`: drop the `naversearch` forbidden entry and the "phrase batch adds two" note. Decide the `guideline` entry by its remaining reason.
- devseed and seed:
  - `devseed/devseed.go`: drop the `PhraseLists` port and dep, `Report.PhraseListWritten`, the `EnsureList` block and its `valid()` case.
  - `devseed/fixtures.go`: drop `PhraseListFixture` and `PhraseList`.
  - `devseed_test.go`: delete the two phrase tests and `fakePhraseLists`.
  - `cmd/seed/main.go`: drop `PhraseLists:`, the `phraseLists` adapter, the report line and the `qualitystore` import; `quality.Known` stays.
  - `cmd/seed/main_test.go`: drop the phrase-list block and `TestSeedLeavesAnExistingPhraseListStanding`; edit the report test.
- Docs and env:
  - DEPLOY.md: delete the `NAVER_SEARCH_*` row and the `QUALITY_PHRASE_REFRESH_INTERVAL` row.
  - `.env.example`: delete the "분야 phrase batch" section.
  - `.env.production.example`: delete its matching lines.
  - The prod `.env` never held the keys.

## result
