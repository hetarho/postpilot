# T283 SmartEditor scripts are files with a DOM test; naver is plan vs driver
> st:todo | ssot:ARCH-12 ARCH-24 | base:ARCH@5 | dep:T282

## goal
Split `agent/internal/naver` into `naver` (publish plan + Port/CommitPort seams) and `naver/smarteditor` (CDP driver + observation/mutation scripts), move the ~30 KB of embedded JavaScript into `.js` files under `//go:embed`, and execute them in tests against a saved SmartEditor DOM fixture.

## acceptance
- [ ] `naver/smarteditor/scripts/*.js` hold `editorObservationScript`, `readbackObservationScript`, the nine `driver*Fn` blobs, the three identity scripts and the compatibility script; Go composes them via `embed.FS`, and no Go file contains a backtick JS blob over 5 lines
- [ ] a test runs each script against a saved SmartEditor DOM fixture (`testdata/smarteditor-*.html`, captured from the live editor with ids/text anonymised) in headless Chromium when `CDP_TEST_BROWSER` is set and in `goja` (or `chromedp` with a testdata page) otherwise, asserting the observation JSON shape and the mutation outcome; `compatibilityObservationScript` and `identityObservationScript` gain their first tests
- [ ] `naver/publisher.go` (885) shrinks to planning + `Port` use; `port.go` + `mutate.go` + `probe.go` move to `smarteditor`; the `Port`/`CommitPort` seam is unchanged
- [ ] behaviour unchanged: ARCH-27 passes; one live publish re-run after landing (owner-authorized)

## impl notes
- from review/arch-260919 F30 F26
- the review measured every SmartEditor gotcha so far was found live; a DOM fixture test is the cheapest way to catch a script regression before a live run
- capture the fixture once from the live editor (the T008 session has the account); keep it small (the editor shell + one paragraph + one photo module)

## result
