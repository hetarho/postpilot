# T372 The test harness pins what it claims, and template limits have one constructor
> st:todo | ssot:ARCH-24 | base:ARCH@9 | dep:T354 T355 T359 T369 T371

## goal
The title-area corpus reaches every title refusal, including the cross-area data-field ceiling, and a test fails when one goes missing. The revise-prompt test drives the worker's real job mapping. `template.Limits` is built one way, by the API, the seed and every test, with its target-length floor taken from post's own rule.

## acceptance
- [ ] Every title refusal is in the corpus, `too_many_asks` included:
  - `backend/internal/template/testdata/grammar/corpus.json` `titleCases` holds at least one cross-area `too_many_asks`: its body alone parses, and the pair is refused.
  - It is regenerated with `TEMPLATE_CORPUS_REGEN=1 go test ./internal/template/ -run TestCorpus`.
  - The 600 body `cases` are byte-identical to HEAD's.
- [ ] New `TestTitleCorpusReachesEveryTitleReason` in `internal/template/corpus_test.go`:
  - It fails if any of the nine title reasons (impl notes) is absent from `titleCorpus`, or if no `too_many_asks` entry is cross-area.
  - At HEAD's corpus it fails, naming `too_many_asks`.
- [ ] `frontend/src/entities/template/lib/grammar.test.ts` still passes against the regenerated file. Its `titleCorpus.length` stays 200.
- [ ] `cmd/api` `TestFieldPhrasesReachTheWritePromptAndNeverTheRevisePrompt` runs the generate and revise jobs through the real `registerJobs` handlers, and `grep -n "Register(job.Kind" backend/cmd/api/generation_freeze_test.go` returns nothing. Its assertions are unchanged, and it passes.
- [ ] `template.NewLimits(template.Ceilings, template.NumberBounds) template.Limits` is the only way a `Limits` is built. It is used by:
  - `cmd/api/contexts.go`;
  - `cmd/seed/main.go`;
  - the seven `cmd/api` test literals, through one `testTemplateLimits()`;
  - the template package's own tests.

  `grep -rn "template.Limits{" backend` returns nothing. Inside `backend/internal/template`, the only `Limits{…}` composite literal is the one in `NewLimits`: `grep -rnE "(^|[^.[:alnum:]])Limits\{" backend/internal/template` finds that one line. The `post.Limits` / `guideline.Limits` literals are other contexts' types and are untouched.
- [ ] `post.TargetLengthMin` (`= 1`) is the floor post's option save enforces, and both commands hand it to template. `grep -rn "TargetLengthMin: *1" backend/cmd` returns nothing.
- [ ] Tests:
  - `internal/template/limits_test.go` `TestNewLimitsMapsEveryField`;
  - `internal/post` "refuses a target length below TargetLengthMin and accepts it", beside the other option-save tests in `tag_count_test.go`;
  - `cmd/api/wiring_test.go` `TestBuildContextsWiresEveryRequiredCollaborator` also asserts that the built template service's `Limits()` carries `post.TargetLengthMin` and `post.TagCountRange`'s min and max.
- [ ] Behavior unchanged: the same env vars, defaults and validation (`internal/platform/config` tests updated only for the field move), and the same limits values at runtime. The existing tests still pass.
- [ ] Gates pass:
  - BE: `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test -timeout 30m ./...`
  - FE, since the corpus file changed: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- from review/published-quality-260924 F17 F29
- Dep T359: it rewrites `template.Service.Create`'s callers in `cmd/api/bootstrap_test.go` and `cmd/seed`, the same functions whose `Limits` literals this task replaces. T359 in turn follows T354, which moves the setup of `TestFieldPhrasesReachTheWritePromptAndNeverTheRevisePrompt` into `newDrainHarness` in `generation_freeze_test.go` and leaves its KindRevise closure alone "for F17". Line numbers below are HEAD's (58e1b5ca).
- **Title corpus** (`internal/template/corpus_test.go:103-139`, `titleCorpus`):
  - Why it misses today: with `AskMaxPerBody: 3` (`grammar_test.go:27`), a cross-area overflow needs four distinct labels across a title that parses and its body. The title alphabet has two ask labels (가게, 총평), and the largest body has two (a, b). So 0 of 200 pairs overflow; measured, the committed `titleCases` has none.
  - Decided: grow the generation, not hand seeds ← a seed would pin one shape the corpus already has by hand in `cases.json` ("the data-field ceiling counts both areas, title first"). The point of the corpus is shapes nobody wrote down.
  - Append two bodies to `bodies` (:117), keeping the existing four in order:
    - `"<ask label=\"a\"/>\n<ask label=\"b\"/>\n<ask label=\"c\"/>"`
    - `"<write>본문</write>\n<ask label=\"a\"/>\n<ask label=\"b\"/>\n<ask label=\"c\"/>"`

    Each asks three, so a body alone is within the ceiling and every overflow is cross-area. The second puts the overflowing ask on a later line.
  - Measured on a scratch copy of `grammar.go` with the same seed (20260923):
    - the 200 pairs then hold 4 `too_many_asks@body` and still every other title reason;
    - `not_in_title` 64, `unexpected_close` 63, `missing_attribute` 18, `unknown_tag` 11, `malformed_tag` 5, `unclosed_tag` 4, `duplicate_ask_label` 2 in title_area and 1 in body, `empty_write` 2, accepted 26.
  - The title draws barely move: only the body index's `Intn` range changes.
  - Update the function's doc comment (:100-102) to say which bodies make which cross-area reason reachable.
- **Coverage test:**
  - Declare `var titleReasons = []string{ReasonUnknownTag, ReasonUnclosedTag, ReasonUnexpectedClose, ReasonMalformedTag, ReasonMissingAttribute, ReasonEmptyWrite, ReasonNotInTitle, ReasonDuplicateAskLabel, ReasonTooManyAsks}` with a comment. The other six reasons cannot end a title pair: a title refuses `slot` / `repeat` / `note` as `not_in_title` before reading their attributes (`grammar.go:367-370`), and none of the fixed bodies fails alone.
  - `TestTitleCorpusReachesEveryTitleReason` collects `titleCorpus(t)`'s reasons and reports every missing one in one `t.Errorf`.
  - It also requires one `too_many_asks` entry whose `ParseTemplate("", entry.Body, fixtureParseOptions)` succeeds, i.e. cross-area.
  - It is a separate test so it also runs in REGEN mode.
- **Stale regenerate instructions:**
  - The committed `_comment` says "Generated by TestRegenerateCorpus" (:83). Make it "Generated by TestCorpusMatchesTheCommittedFile with TEMPLATE_CORPUS_REGEN=1".
  - `grammar.test.ts:142-146`'s doc says to run `-run TestRegenerateCorpus`. Make it `TEMPLATE_CORPUS_REGEN=1 go test ./internal/template/ -run TestCorpus`.

  Neither test exists under the old name. The `_comment` edit lands in the same regeneration.
- **Real revise mapping** (F17 second half):
  - `registerJobs(c *contexts)` (`jobs.go:24-86`) only captures context pointers inside closures, and `registerClipJobs(q, nil, nil)` registers closures and terminal observers that run only for clip jobs. So a partial `contexts` is enough.
  - In `newDrainHarness`, T354's extraction of `generation_freeze_test.go:199-296` (or in the test itself if T354 kept it inline), replace both `queue.Register(job.KindGenerate, …)` and `queue.Register(job.KindRevise, …)` (HEAD :261-273) with `registerJobs(&contexts{jobs: queue, generation: generationSvc})`, plus a comment: the worker's own handlers, not a copy (review F17).
  - Keep `queue.Admit(&stubAdmitter{})` and the worker start.
  - If a later change makes `registerJobs` dereference another context at registration time, this panics loudly, which is the point.
  - Do not extract a `reviseJob` mapping function ← that would still leave the closure body copied in the test.
- **One `template.Limits` constructor** (F29). New `internal/template/limits.go`:
  - `type Ceilings struct { NameMaxChars, DescriptionMaxChars, BodyMaxChars, TitleAreaMaxChars, MaxPerAccount, MaxRepeatExpansion, PhotoRowMax, AskLabelMaxChars, AskMaxPerBody int }`, in exactly this field order. The doc says: the operator-set half of `Limits` (`TEMPLATE_*`), and field-for-field identical to `config.TemplateCeilings`, so the composition root converts one into the other.
  - `type NumberBounds struct { TargetLengthMin, TagCountMin, TagCountMax int }`. The doc says: the POST option range a template's numbers seed (TEMPLATE-47), passed in and not owned here. Move the explanation from `types.go:93-97` here and leave a pointer there.
  - `func NewLimits(c Ceilings, n NumberBounds) Limits`. It is a plain field copy. `NewService` still panics on an invalid `Limits` (`valid()`), unchanged.
- **Config grouping** (`internal/platform/config/config.go`):
  - Move the nine template ceilings (:319-337, minus `TemplateAskValueMaxChars`) into `Template TemplateCeilings`, with `type TemplateCeilings struct {…}` field-identical to `template.Ceilings` and the same order.
  - `TemplateAskValueMaxChars` stays a top-level field. Only post reads it (`contexts.go:188` `AnswerValueMax`).
  - `Load` (:507-556) assigns `cfg.Template.NameMaxChars = …` etc. Env names, fallbacks and `positiveInt` checks stay byte-identical.
  - Keep the field comments on the new struct.
  - Why a conversion and not a mapping function: `cmd/seed` and `cmd/api` are two `main` packages that cannot share a function. `config` may not import `template` (ARCH-21), and `template` imports no config. `template.Ceilings(cfg.Template)` is then the only shared spelling, and a field added to either struct stops both builds.
  - `config_test.go` `:218-219`, `:295-296` and `:608-609` read `cfg.Template.PhotoRowMax` / `.NameMaxChars` / … instead.
- **Where the defaults live.** The nine `TEMPLATE_*` defaults stay env fallbacks in `platform/config`. `config.go:311-313` records them as env-owned editorial ceilings, and five are mirrored as `VITE_TEMPLATE_*` for the browser (`.env.example:106-139`). Moving them into template constants per ARCH-21 would move the FE mirrors too, so it is out of this task.
- **`TargetLengthMin` comes from post** (`internal/post/limits.go`):
  - Add `const TargetLengthMin = 1`, doc: the smallest 목표 글자 수 an option save accepts; the length has no ceiling (POST-63).
  - The option save's refusal (HEAD `service.go:887`, `*targetLength <= 0`) becomes `*targetLength < TargetLengthMin`. If T355 has moved the check into its `GenerationOptionsChange` validation, change it there. It is identical for every int.
  - The post test: `TargetLengthMin - 1` gives `errors.Is(err, ErrInvalidContent)` with the row unchanged; `TargetLengthMin` is stored. Write the call in whichever signature `SaveGenerationOptions` has at the base.
- **The two commands:**
  - `cmd/api/contexts.go`:
    - `func templateLimits(cfg *config.Config) template.Limits { return template.NewLimits(template.Ceilings(cfg.Template), postNumberBounds()) }`
    - `func postNumberBounds() template.NumberBounds { return template.NumberBounds{TargetLengthMin: post.TargetLengthMin, TagCountMin: post.TagCountRange.Min, TagCountMax: post.TagCountRange.Max} }`
    - `:225-242` becomes `c.template = template.NewService(templatestore.New(handle.Writer, handle.Reader), templateLimits(cfg))`. Keep the TEMPLATE-6 comment on `postNumberBounds`.
    - `:188` reads `cfg.Template.AskLabelMaxChars`.
  - `cmd/seed/main.go:95-107` becomes `template.NewLimits(template.Ceilings(cfg.Template), template.NumberBounds{TargetLengthMin: post.TargetLengthMin, TagCountMin: post.TagCountRange.Min, TagCountMax: post.TagCountRange.Max})`. The comment says it mirrors `cmd/api`'s `templateLimits`, and that every value is named, not a literal. A zero bound added later fails `NewService`'s `valid()` at seed start, and `cmd/seed/main_test.go` catches it.
- **Tests:**
  - `cmd/api/post_deps_test.go`, beside `testPostLimits()`: `func testTemplateLimits() template.Limits { return template.NewLimits(template.Ceilings{NameMaxChars: 40, DescriptionMaxChars: 200, BodyMaxChars: 4000, TitleAreaMaxChars: 200, MaxPerAccount: 50, MaxRepeatExpansion: 40, PhotoRowMax: 4, AskLabelMaxChars: 40, AskMaxPerBody: 10}, postNumberBounds()) }`. These are the values all seven literals use today.
  - Replace the seven literals with `testTemplateLimits()`: `bootstrap_test.go:269`, `:344`, `:478`, `:684`, `:800`, `:994`, and `generation_freeze_test.go:216`, which will be in `newDrainHarness` after T354.
  - In the template package, `testLimits()` (`service_test.go:19-27`) and `rpc/handler_test.go:58-63` become `NewLimits(Ceilings{…their own values…}, NumberBounds{TargetLengthMin: 1, TagCountMin: 1, TagCountMax: 10})`. These tests hold template to explicit bounds and must not import post.
  - `TestNewLimitsMapsEveryField` fills `Ceilings` and `NumberBounds` by reflection, giving each field a distinct value (i+1). It asserts every `Limits` field equals its same-named input, and that the two inputs together name every `Limits` field. So a dropped or crossed field fails.
- Do not change:
  - the corpus's 600 body cases or the body-piece alphabet;
  - `cases.json`;
  - the `TEMPLATE_*` env contract;
  - `memory_extraction_test.go:172`'s inline `KindExtractMemory` closure. It asserts inside the closure and is not in F17.

## result
