# T214 The renderer schedules captions across the whole timeline
> st:todo | ssot:CLIP-134 CLIP-66 CLIP-125 CDS-27 CDS-38 CDS-41 CDS-43 CDS-45 CDS-52 CDS-59 CDS-60 CDS-62 | base:CLIP@31 CDS@20 ARCH@3 | dep:T210

## goal
Lay out, verify and render narration captions as absolute-interval elements: disjoint on the timeline, floor-checked, rapid-split inside their own interval, anchored across the cuts they cover, with V11 and V18 checked timeline-wide.

## acceptance
- [ ] `scheduleDeclaredCaptions` no longer groups by cut: narration captions sort by start; an overlap omits the later one with `caption_overlap`; the CDS-41 floor is met by the text, the short alternative, then extension into free room, else `caption_floor`; the per-cut `sentence_count` rule applies only to legacy cut-bound captions
- [ ] `layoutDeclaredRapid` splits a narration caption inside `Resolved.StartMS..EndMS` and treats its stored phrases as absolute (no cut offset)
- [ ] anchor selection for a caption spanning cuts evaluates the subject box and readable-text flag of every cut it covers and picks one anchor; the one-step rule applies between consecutive captions
- [ ] `composition_verify.go`: V18 disjoint windows across every caption on the output timeline; V11 a collected fact behind every number, any item; V16 every interval inside the output
- [ ] the information-pair layer renders only for frozen legacy plans; new plans never carry one
- [ ] `PrepareClipPreview` shows narration captions at their absolute times across cut boundaries
- [ ] tests: scheduling ladder; absolute rapid phrases; spanning-cut anchor; verifier V18/V11; the identity smoke (`docker buildx build --target identity-smoke -f backend/Dockerfile .`) and the media smoke stage built and run locally (ARCH-37), with the new baseline digest pinned because the delivered clip changes on purpose (CLIP-125)
- [ ] verify BE (ARCH-26)

## impl notes
- files: media/composition_cues.go · composition_layout.go · composition_batches.go (windows are already absolute frames) · media/preview.go · clip/composition_verify.go
- `SplitRapid(seed, start, end)` already takes a window; `compositionWindows` already batches on absolute frames — the change is in scheduling and anchoring, not in the overlay pipeline
- the spanning anchor helper collects the cuts whose output intervals (`cutOffsets`) intersect the caption and folds their `Subject`/`ReadableText` observations the way a single cut's are read today
- T191's identity baseline was pinned against the per-cut model; re-pin it here and note the reason in the smoke

## result
