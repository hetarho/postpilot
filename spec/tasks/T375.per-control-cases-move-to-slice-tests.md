# T375 The editor's per-control cases live in the tests of the slices that own them
> st:todo | ssot:- | base:ARCH@9 | dep:T368

## goal
The 38 per-control cases T368 left in the editor page suites move to the tests of the slices whose control they exercise. The four slices with no UI test get one. Each page case is deleted only once its slice test exists and passes, so the editor page suites end at about 80 mounts with no loss of coverage.

## acceptance
- [ ] Every case in the impl-notes tables exists in its named slice test file under its named title, and asserts at least what its page version asserted about that control. Its page case is then gone from T368's suites. The two partial moves (`:2375`, `:2434`) and `:3507` are handled as their rows say.
- [ ] New test files:
  - `features/edit-post-content/ui/BlockEditor.test.tsx`
  - `features/select-post-voice/ui/PostVoiceSelect.test.tsx`
  - `features/select-post-template/ui/PostTemplateSelect.test.tsx`
  - `features/finalize-post/ui/VoiceLearningPanel.test.tsx`
  - `features/upload-photos/model/useDeletePhoto.test.tsx`
- [ ] Order: a page case is deleted in the same change that adds its slice case, and only after that slice case runs green. At no commit is a behavior pinned by neither.
- [ ] With the per-control cases gone, T368's row rule covers every editor page case. These both return nothing: `grep -rn "finalizedAt: '2026-08-20T12:00:00Z'" frontend/src/pages/editor` and `grep -rn "'https://blog.naver.com/alice/1'" frontend/src/pages/editor`. The slice tests may build their rows with `finalizedPostRow` / `publishedPostRow` / `FAKE_PUBLISHED_URL`.
- [ ] No moved case uses a real sleep. `:2102`'s 1.2s and `:3524`'s 1.5s become assertions made right after the sheet or panel closes.
- [ ] Behavior unchanged: existing tests still pass, including every other case in the files that receive the moves. T368's editor page suites still pass, minus the moved cases.
- [ ] FE gate passes: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- from review/published-quality-260924 F26
- Base: after T368.
  - Find each case by title in T368's suites. The `:NNN` numbers are the pre-split `EditorPage.test.tsx` at 58e1b5ca, as listed there.
  - T368 grouped the cases so each receiving file draws from one suite: generate → GenerationActions / GenerationOptions / PhotoStrip; assignments → PostVoiceSelect / PostTemplateSelect; refine → BlockEditor; finish → VoiceLearningPanel / PublishedUrlField.
- **What "moved" means:**
  - The slice case renders the slice's own component or hook, against the smallest fake it needs (`createFakeAuthTransport` / `createFakePostsTransport` with `withProviders` and `createTestQueryClient` from `src/test/session`).
  - It asserts that control's behavior: its role, name, state and description; the callback or RPC it causes; and the text it shows.
  - Page-only parts of a page case (the step, the dock around it) are dropped. T368's kept page cases already pin those.
- **`features/generate-post/ui/GenerationActions.test.tsx`.** Today it has one case (`:66`) and one `it.each` (`:84`), and T365 rewrote its callers. Extend `setup` with options for the `post` fields (`images`, `observations`, `pendingExperimentId`), providers and comparison pairs.
  - "keeps ordinary generation usable when only the A/B pair is missing" (`:511`): 생성 enabled, A/B 비교 `aria-disabled` with the pair reason ('작성 A/B 모델 두 개를 선택하세요.') as its accessible description, and no visible reason line.
  - "sends the active writer only for ordinary generation" (`:546`): StartGeneration carries the selected `writeModel` and no `targetLength`, and no StartWriteExperiment goes out.
  - "sends an EMPTY frozen set when the picker is confirmed untouched" (`:648`), "routes the A/B comparison through the same picker" (`:686`), "starts directly when the post has photos but no stored observation" (`:773`): `reobserveFiles` `[]` / `[]` / undefined, and the picker shown or not.
  - "keeps 생성 disabled while an A/B result is pending" (was `:732` "enqueues nothing when a confirmed picker has gone stale", renamed to what it asserts): with `pendingExperimentId`, 생성 is disabled, a click opens no picker, and neither `beforeStart` nor a start runs.
- **`features/generate-post/ui/GenerationOptions.test.tsx`** (T355 rewrote it for the whole-set form; add these beside its cases):
  - "fills 목표 글자 수 with a usable default the moment the box is ticked" (`:853`): ticking fills 1000, not `aria-invalid`, and 저장 enabled; a typed 2400 survives untick and retick.
  - "restores and explicitly clears a stored target length…" (`:877`): 1200 is checked and prefilled. Untick + 저장 saves natural length, with T355's expectation: one `optionSaves` entry whose `targetLength` is undefined and whose other four members are the post's. T355 made `GenerationOptions` the brief's form owner, so render it with T355's harness (`saved`, `locked`, `jobRunning` and a render slot).
- **`features/upload-photos/ui/PhotoStrip.test.tsx`:**
  - "restores its photos in the strip from their view URLs" (`:1320`): `img` `src` is `viewUrl`, and an image with no `viewUrl` still renders.
  - "deletes a photo through the same confirmation" (from `:1348`): photo × → sheet 삭제 → `onDelete` with the image, beside the clip case at `:96`.
  - "keeps a photo whose delete failed and says so" (`:1379`): `deleteFailedId` shows the alert inside the open sheet; 취소 keeps the photo.
- **New `features/upload-photos/model/useDeletePhoto.test.tsx`:** "deletes a photo through DeleteImage", the RPC half of `:1348`, via `renderHook` + `createFakePostsTransport({ calls, posts })`.
- **`features/record-published-url/ui/PublishedUrlField.test.tsx`.** Today it has only the server-refusal `it.each` (`:32`), rendering against a hand-built `createRouterTransport`. Add a helper that renders against `createFakePostsTransport({ calls, publishedUrlSaves, posts: [row] })`, with rows from `finalizedPostRow` / `publishedPostRow`.
  - "is a url field at the foot of ③, typed for the keyboard that pastes an address" (`:3892`): the six input attributes, an empty value, 저장 enabled and no 지우기. Drop "at the foot of ③" from the title: position is the page's.
  - "accepts the mobile share address and shows it normalized" (`:3923`)
  - "refuses another address in place and sends nothing" (`:3937`): the alert, `aria-invalid`, `aria-describedby` and no RPC.
  - "sends nothing for an empty 저장 on a post with no address" (`:3958`)
  - "prefills a published address and sends its replacement" (was `:3966` "replaces a published address and stays 발행됨"). The prefill is `FAKE_PUBLISHED_URL`, and the replacement `/alice/2` is sent.
  - "stays closed with its reason until the post is 확정" (`:4008`): in review, input and 저장 are disabled and the reason is in `aria-describedby`.
- **New `features/finalize-post/ui/VoiceLearningPanel.test.tsx`:**
  - A harness component calls `useVoiceLearning('alice', post)` and renders `<VoiceLearningPanel learning={…} onBackToRefine={vi.fn()} />` inside `withProviders(createFakeAuthTransport({ user, posts, jobs, providers }), createTestQueryClient())`, with T368's `stubLearningHandoff`. `post` is a `PostDraft` literal cast as in `widgets/refine-dock/ui/RefineDock.test.tsx:12-27`.
  - It gets the four cases, same titles:
    - `:1114` "keeps a failed learning handoff across reloads so only learning can be retried"
    - `:1167` "removes a failed-learning retry when the current voice language is ineligible"
    - `:1229` "keeps 말투 학습 disabled for a revision it has already learned from"
    - `:1275` "does not let a completed handoff from an older revision hide later learning"
  - `:1114`'s "no 확정하기" line is page-only; drop it.
- **New `features/select-post-voice/ui/PostVoiceSelect.test.tsx`**, rendering the select with `createFakeAuthTransport({ user, voice: { voices } })`:
  - "cancels a reassignment from the sheet without saving anything" (`:2102`): `onSelect` is never called and the trigger keeps the voice. Assert right after the sheet closes, with no sleep.
  - "blocks reassignment while a job is active and says why" (`:2120`): `blocked={reassignmentBlocker(postWithJob)}` shows 'AI 작업이 끝나면 말투를 바꿀 수 있어요.' and disables the trigger.
  - "reports a refused reassignment under the field and keeps the old voice" (`:2136`): `onSelect` rejects with `connectAppError('VOICE_NOT_FOUND', Code.NotFound)` from `src/test/app-error`, which `reassignmentFailureMessage` (`model/reassignment.ts:20-30`) turns into the '고른 말투를 찾을 수 없어요…' alert. The trigger keeps the old voice, enabled.
- **New `features/select-post-template/ui/PostTemplateSelect.test.tsx`**, rendering against `createFakeAuthTransport({ user, templates })`:
  - "says so and offers a retry when the directory cannot be read" (`:2512`)
  - "stays usable during a job and says the running one keeps its own brief" (`:2526`)
  - "reads 없음 with 없음 selected when nothing is assigned", the per-control half of T368's kept "defaults a new draft to 없음 and sends no template with the create" (`:2375`). Trim those lines from the page case, which keeps its create request.
  - "shows no description and no 템플릿 관리 link", the per-control half of T368's kept "carries a chosen template into the create" (`:2434`). Trim them the same way.
- **New `features/edit-post-content/ui/BlockEditor.test.tsx`:**
  - Harness: `<BlockEditor post={…} />` inside `withProviders(createFakePostsTransport({ calls, contentSaves, posts: [row] }), createTestQueryClient())`. The `PostDraft` is a literal cast as in `RefineDock.test.tsx:12-27`, with `content` / `images` / `videos` / `replacementCandidates` per case. `ReplacementMark` is reached through it: it has no mount of its own, only inside `BlockEditor`.
  - Cases, same titles:
    - the draft read-first: `:1784` "renders the draft as prose with no form control until a block is opened", `:1799` "restores a cancelled block and keeps a saved one as prose", `:1823` "keeps add, delete and move available from a block editor", `:1842` "edits a VIDEO block with the attached-video list, alt and caption";
    - the step split: `:1905` "closes a block editor when the block moves", `:1918` "keeps a block edit made while the header editor was open";
    - the marks: `:3380` "marks the title, a tag and a TEXT block", `:3409` "offers at most three phrases and no duplicate tag", T363's "spends a taken candidate, even where its phrase holds the source" (was `:3461`, with T363's expectations), `:3478` "puts focus on the pencil of the block the take changed", `:3493` "keeps a neighbouring mark after a take", `:3524` "sends nothing for an ignored mark" (no SavePostContent right after the panel is dismissed, no 1.5s sleep);
    - "shows no mark while a block editor is open", the half of `:3507` "drops a mark whose source was edited away" that no slice test pins. Its other half is `entities/post/model/replacements.test.ts:74`. Once this case is green, delete `:3507` from T368's refine suite.
- When a T368 suite's describe block empties, remove the block and any local helper it alone used. `the draft read-first` empties entirely; `the 발행 URL field` keeps `renderPost` for T368's two kept cases.
- Do not change:
  - T368's kept page cases, except the `:2375` / `:2434` trims;
  - production code. A moved case that needs a prop the component lacks has found a gap: say so in `## result` instead of adding it.

## result
