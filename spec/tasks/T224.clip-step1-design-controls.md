# T224 ① chooses the design, the caption styles and an optional template
> st:todo | ssot:CLIP-111 CLIP-139 CLIP-142 CLIP-130 CLIP-5 | base:CLIP@32 | dep:T217 T218

## goal
Give step ① the intro and outro presets, the allowed caption styles and a template picker that defaults to none.

## acceptance
- [ ] `/clips/new` offers the template as optional with 없음 selected by default and mints without one
- [ ] ① shows the intro preset, the outro preset and a multi-select of caption styles beside the existing pace and accent controls, each previewing its own look
- [ ] selecting a template fills all five and every one stays editable afterwards; clearing the template leaves the values in place
- [ ] the style multi-select states which styles cost a per-frame render, and an empty selection reads as the default style
- [ ] the controls autosave on a pause like the rest of ① and a change marks the result stale
- [ ] vitest covers the default-none mint, the template seed, editing after seeding and the empty-selection reading
- [ ] ARCH-25 passes

## impl notes
- the style multi-select is a `shared/ui` control over the style list the server already sends with the project, not a hand-rolled one (→ARCH-20)
- preset and style previews use the same served SVG fragments as the editor (→T222) rather than screenshots ← a picture would go stale the moment a style changes
- the template picker keeps the post-template pattern: presence-aware patch, 없음 sends a clear (→TMPL-32)
