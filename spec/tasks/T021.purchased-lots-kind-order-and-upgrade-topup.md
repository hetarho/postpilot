# T021 The purchased lot kind, consumption by kind, and the upgrade top-up
> st:todo | ssot:QUOTA-12 QUOTA-34 QUOTA-35 QUOTA-3 QUOTA-11 | base:QUOTA@3 ARCH@1 | dep:T020

## goal
Add the `purchased` lot kind, make consumption walk lots by kind (monthly → bonus → purchased) before expiry, and make a tier upgrade raise the current cycle's monthly lot by the credit difference on both paths that change a plan.

## acceptance
- [ ] migration `0027_purchased_credit_lots.sql` rebuilds `credit_lots` with `CHECK (kind IN ('monthly','bonus','purchased'))`, copies every existing row, keeps the remaining/granted CHECKs, and replaces `idx_credit_lots_user_expiry` with an index that serves the new order (`user_id`, kind rank, `expires_at`, `created_at`); the Down half restores the two-kind CHECK and the old index; a store test opens a fresh database, inserts one lot of each kind and asserts a fourth kind is refused by SQLite
- [ ] `usage.LotPurchased` joins `LotMonthly` and `LotBonus`, and any exhaustive mapping over kinds (store row ↔ domain, the plan handler, the FE mapper's `toLot`) carries it
- [ ] `LotsInConsumptionOrder` orders by `CASE kind WHEN 'monthly' THEN 0 WHEN 'bonus' THEN 1 ELSE 2 END`, then `expires_at IS NULL`, `expires_at`, `created_at`, `id`; its comment states that kind now orders consumption and is no longer a display distinction, and `pnpm gen:sql` is run with the regenerated code committed (ARCH-28 passes with no diff)
- [ ] a service test spends across four lots — a monthly lot expiring soonest, a never-expiring bonus, a never-expiring purchased lot created BEFORE that bonus, and a second monthly lot — and asserts both monthly lots drain first, then the bonus, then the purchased lot ← the purchased lot being the older non-expiring row is exactly the case expiry order alone got wrong
- [ ] `usage.Service.TopUpMonthlyLot(ctx, userID, credits)` raises the active monthly lot's `granted` and `remaining` by `credits` inside one write, is a no-op when no unexpired monthly lot exists, refuses a non-positive amount, and is covered by tests for all three cases plus one asserting the `remaining <= granted` CHECK still holds on a lot already partly spent
- [ ] `auth.Service.SetUserPlan` computes `plan.MonthlyCredits(target) - plan.MonthlyCredits(current)` before writing, changes the plan, and then tops up by the difference when it is positive and neither tier is unlimited; a downgrade tops up nothing, a same-tier call tops up nothing, and moving to or from `master` tops up nothing; tests cover each
- [ ] both paths that change a tier get the top-up: the `AdminService.SetUserPlan` RPC and `api setplan`; a test per path asserts the credit side ran
- [ ] `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...` passes, and `pnpm --filter ./frontend test` · `pnpm lint` pass
- [ ] no purchase RPC, no par-rate constant and no payment path in this task — buying credits is BILLING's; the kind exists so the order QUOTA-12 decides is real and testable

## impl notes
- SQLite cannot alter a CHECK, so the migration is the standard rebuild: `CREATE TABLE credit_lots_new (…)` with the three-kind CHECK → `INSERT INTO credit_lots_new SELECT … FROM credit_lots` → `DROP TABLE credit_lots` → `ALTER TABLE credit_lots_new RENAME TO credit_lots` → recreate the index; `credit_hold_lots.lot_id REFERENCES credit_lots(id)` means the rebuild must run with `PRAGMA foreign_keys` off inside the migration or after the goose statement that disables it — check how 0019 and any later rebuild handled it and follow the same shape
- the kind rank is spelled in SQL rather than passed in from Go ← the order is a product rule the query is the only reader of, and a rank passed as a parameter would let two callers disagree
- the top-up seam follows the `provision.CreditGrant` precedent exactly: declare `type MonthlyTopUp func(ctx context.Context, userID string, credits int) error` in `backend/internal/auth/ports.go`, take it as the third argument of `auth.NewService`, and let `cmd/api/main.go` supply `usageSvc.TopUpMonthlyLot`; `provision.SetPlan` gains the same injected func from `cmd/api` (like `GrantCredits`' `grantCreditsTo`) and adapts it before building its own auth service ← credits belong to the usage context and the auth context must not learn about `credit_lots`
- a nil `MonthlyTopUp` makes `SetUserPlan` return an error when an upgrade actually needs a top-up, rather than silently skipping the grant ← losing a paid-for grant quietly is worse than a loud boot-wiring failure; tests that only exercise sessions may pass nil safely because they never upgrade
- the plan write and the credit top-up are two statements, not one transaction: a failure between them leaves the tier moved and the credits ungranted, and the error names that so the operator can finish with `api grantcredits`. BILLING's charge path must revisit this seam when money is involved — record it in the task result rather than papering over it here
- `frontend/src/entities/plan`: `CreditLot['kind']` gains `'purchased'`, `toLot` maps the literal and keeps its unknown → bonus fallback; the account popover's lot list needs a `plans.balance.lotPurchased` label in ko and en (LANG-7 parity) — the popover renders `lot.kind === 'monthly' ? lotMonthly : lotBonus` today and must become a three-way lookup

## result
