# T107 Hook and ending cards, the Paperlogy face and brightness-aware scrims
> st:todo | ssot:CDS-16 CDS-17 CDS-19 CDS-25 CDS-28 CDS-29 CDS-32 CDS-44 CDS-45 CDS-46 CDS-47 CDS-48 CDS-52 CLIP-13 CLIP-17 CLIP-28 ARCH-21 ARCH-24 ARCH-32 ARCH-33 | base:CDS@1 CLIP@5 ARCH@2 | dep:T105 T106

## goal
Open every clip on a hook card and close it on an ending CTA card, set hook and 크게 강조 text in Paperlogy, and add a scrim under unplated copy only where the sampled footage is bright or busy.

## acceptance
- [ ] Paperlogy 8 ExtraBold is bundled at `backend/assets/fonts/paperlogy/` with its OFL notice from the official distribution, pinned by size and SHA-256 in the renderer like Pretendard, installed by the Dockerfile beside it, configured by `CLIP_FONT_PAPERLOGY_PATH` (in `.env.example` and the production example) and passed to resvg as a second `--use-font-file`; `t.hook` and `bold` use `font-family="Paperlogy"` weight 800 and the glyph-coverage check runs against the face each text actually uses; a Korean glyph Paperlogy lacks is `ErrInvalid`, never a silent substitution (CLIP-13)
- [ ] hook card: when the plan carries a non-empty `hook`, `Layout` places over the first cut from 0 to 1500 ms an `ink.900s` α0.88 card (`radius.card` 24, padding 40, `shadow.card`) at the ratio's geometry (9:16 x 144–936 centred y 840 · 16:9 width 1120 centred y 540 with chip and name on one row · 1:1 width 880 centred y 540) stacking the preset's category chip (`t.label` on accent α1.0, #111 text), the hook (`t.hook` 84/72/76 by ratio, ≤ 2 × 9) and the 상호 answer (`t.body` `text.muted`); present from frame 0, 200 ms fade-out, no copy or chip under it; the original audio dips −6 dB for its window (the gain filter, ahead of T108's normalisation)
- [ ] ending card: `Layout` places over the last cut from end − 2500 ms an `ink.900s` card (9:16 x 144–936 centred y 1040 · 16:9 width 1120 centred y 540 in two columns · 1:1 width 880 centred y 560) with the 상호 answer, the 위치 answer in `t.label` `text.muted`, one 가격 or 메뉴 answer in `t.caption` when present, and the resolved CTA phrase in accent `t.label`; 200 ms fade-in, no fade-out, no copy under it, the badge stays above it; a project without 상호 renders no card and the manifest says so
- [ ] scrims: for `bold` and `mark` only, the renderer samples the first, middle and last frame of the copy window (`ffmpeg -ss … -frames:v 1` to PNG), crops the plate region, computes Rec.709 relative luminance mean L and deviation σ in Go, and adds `scrim.top` (0, 250, 1080, 310) or `scrim.bottom` (0, 1040, 1080, 380) — 260/200 on the other ratios — when L ≥ 0.6 or σ ≥ 0.25, sharing the copy's in/out; an accent word turns white when L ≥ 0.6; a scrim is never placed under a plated element
- [ ] V3: the verifier computes WCAG 2.1 contrast of every text against its effective background — plate composited at its α over the sampled mean for plated elements, the sampled mean under stroke for unplated — and a pairing under 4.5:1 for `bold`/`mark` falls back to `clean` at the same anchor (compiler path) or is refused `plan_layout_contrast` (manual path); `CLIP_LAYOUT_CONTRAST` enters the catalog
- [ ] proto and FE: `ClipEditPlan.hook` (string, 3) is saved and rendered; step ② shows a 훅 문장 field with the ≤ 2 × 9 rule and grounding refusal; ko/en copy for the field
- [ ] `RENDER.md` documents the second face, the card geometry per ratio, the sampler and V3; the manifest gains kinds `card` `scrim` `chip-category`
- [ ] tests: Paperlogy pin and coverage · card goldens on three ratios with and without 가격 · no-상호 omission · sampler on synthetic bright, dark and busy frames · scrim decision at the thresholds · accent-to-white · V3 fixtures · the −6 dB dip in the filter-graph golden · FE hook field validation · Docker smoke extended to a plan with hook and ending cards, asserting card pixels at 0 ms and at end − 1000 ms
- [ ] ARCH-26, ARCH-28 and ARCH-25 pass; the production image completes the smoke as nonroot

## impl notes
- Paperlogy's official channel is the designer's release page (freesentation.blog/paperlogyfont) with the fonts-archive GitHub mirror; read both at implementation (ARCH-33), take the TTF from the official archive, record its SHA-256 and the OFL text in the assets directory, and note the source in `RENDER.md`; only 8 ExtraBold is bundled because CDS uses no other Paperlogy weight
- the Noto Sans KR fallback of CDS-17 never fires in the renderer: resvg runs with system fonts off and the coverage check refuses an unsupported glyph (T074), so the fallback is a FE preview concern; say so in `RENDER.md`
- cards are separate PNG plates on the overlay chain like copies, so their timing reuses the fade filters from T103; their contents come from the answers by reserved label (T104), never from model text except `hook`
- the sampler decodes PNG frames in Go (`image/png`) rather than parsing `signalstats`, so the luminance formula is one tested function and no FFmpeg text output is parsed
- three frames per unplated copy is the CDS-44 rule; a clip with ten `bold`/`mark` copies costs thirty single-frame extractions, well inside the media timeout, and the sampler runs during the same source callback that renders the cut so no second download happens (CLIP-33)

## result
