# T070 Video-template management UI
> st:todo | ssot:CLIP-4 CLIP-5 CLIP-14 CLIP-25 THEME-22 THEME-23 THEME-24 THEME-29 ARCH-13 ARCH-14 ARCH-15 ARCH-17 ARCH-18 ARCH-20 ARCH-24 | base:CLIP@1 THEME@8 ARCH@2 | dep:T069

## goal
Let an account create, inspect, edit and delete the reusable video templates that clip generation requires.

## acceptance
- [ ] `entities/clip-template` owns the generated-contract mappers, cache keys, query/mutation hooks and types for information fields, copy styles and accents; no slice imports generated code directly
- [ ] authenticated lazy routes `/video-templates`, `/video-templates/new` and `/video-templates/$templateId` render text loading/empty/failure states and preserve unknown/foreign not-found behavior
- [ ] the directory lists name, information-field count, allowed styles and updated time, links the whole row, and docks one `새 영상 템플릿` action at every width
- [ ] the editor has fields for name and cut guidance, an ordered information-field builder with add/reorder/delete plus label and prompt, three approved copy-style choices with at least one selected, and one optional accent from the code-owned palette
- [ ] live validation mirrors every T069 bound and the server remains authoritative; save sends the whole recipe once and keeps the unsaved draft after a refusal
- [ ] the three style choices show deterministic static previews using the same ids, palette and visual measurements T074 consumes; previews contain exact sample text and never call an image model
- [ ] delete uses `Dialog`, names the detached-project count returned by the server, invalidates template and clip-project caches, and routes away only after success
- [ ] all controls come from `shared/ui`, fields and sortable rows meet pointer floors at 320 px, and the page follows the existing template directory/editor rather than introducing a second design language
- [ ] ko/en `clips` resources have identical topology and no concatenated sentences; tests cover create/edit/delete, field reordering, the non-empty style gate, server failure retention and both not-found states
- [ ] ARCH-25 passes

## impl notes
- use one `entities/clip-template` slice rather than extending `entities/template`: the grammar-backed post template and the structured video recipe share a label only, not a contract
- represent `copyStyles` and `accent` by stable ids from `clip.proto`; CSS preview recipes may live in `entities/clip-template/ui` because T074's rendered pixels are backend output, not a shared runtime dependency
- keep the routes out of primary navigation in this task; T080 groups them under 영상 once both video destinations exist
- copy the page composition and mutation/cache patterns from `pages/templates` and `pages/template`, but do not copy the post-template grammar/source-mode controls

## result
