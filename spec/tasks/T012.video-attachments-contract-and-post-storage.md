# T012 Video attachments: proto contract, migration and the post context
> st:todo | ssot:VIDEO-1 VIDEO-2 VIDEO-3 VIDEO-4 VIDEO-5 VIDEO-6 VIDEO-12 POST-14 POST-18 POST-28 POST-61 GEN-1 MODEL-18 | base:VIDEO@1 POST@2 GEN@2 MODEL@3 ARCH@1 | dep:-

## goal
Add videos as the post's second attachment kind end to end on the server side of the contract: every proto field the later tasks need, one migration, the upload handshake for a video with its ceilings, listing, deletion, cascade and sweep, and the `VIDEO` block accepted by the content validator.

## acceptance
- [ ] `proto/postpilot/v1/post.proto`: `BlockType.VIDEO = 6`; `message Video {id, filename, width, height, bytes, view_url, duration_ms, content_type}`; `Post.videos = 23`; `enum AttachmentKind {ATTACHMENT_KIND_UNSPECIFIED, PHOTO, VIDEO}` and `CreateUploadRequest.kind = 3` (unspecified reads as photo); `ConfirmUploadRequest.duration_ms = 4`; `ConfirmUploadResponse.video = 2`; `DeleteVideo(DeleteVideoRequest{video_id})`; `Observation.events = 8` (repeated string), `speech = 9`; `model_catalog.proto` `CatalogModel.video_input` and `provider.proto` model info `video_input`; `pnpm gen:proto` output committed and `git diff --exit-code` clean afterwards (ARCH-28)
- [ ] migration `NNNN_video_attachments.sql` (next free number): `videos(id TEXT PK, post_slug TEXT NOT NULL REFERENCES posts(slug) ON DELETE CASCADE, filename, r2_key, content_type, bytes, duration_ms, width, height, created_at, UNIQUE(post_slug, filename))`; `uploads` gains `kind TEXT NOT NULL DEFAULT 'photo'` and `content_type TEXT NOT NULL DEFAULT 'image/jpeg'`; `catalog_models` gains `video_input INTEGER NOT NULL DEFAULT 0` (the mapping itself is T013)
- [ ] config: `UPLOAD_MAX_VIDEOS_PER_POST` (3) and `VIDEO_MAX_SECONDS` (60) read as positive ints, boot-fatal when invalid, documented in `.env.example` and `.env.production.example`; `MaxVideoBytes` 200 MiB typed constant beside `MaxImageBytes`; `VideoContentTypes` map `mp4→video/mp4 mov→video/quicktime m4v→video/x-m4v webm→video/webm` as code
- [ ] `CreateUpload(kind=VIDEO)`: extension outside the map is `InvalidArgument`; the post at `UPLOAD_MAX_VIDEOS_PER_POST` confirmed videos is `FailedPrecondition` (`ErrTooManyVideos`); a filename held by a confirmed photo OR video is `AlreadyExists`; a pending upload of either kind under that filename is replaced (row and object dropped, fresh id); key `posts/{slug}/{id}.{ext}`; the presigned PUT carries the container content type and the response returns it
- [ ] `ConfirmUpload` on a video upload: `duration_ms` 1 … `VIDEO_MAX_SECONDS`×1000 and positive dimensions else `InvalidArgument` (`ErrInvalidVideo`); HEAD size 1 … `MaxVideoBytes` and HEAD content type equal to the upload's, else the object is deleted and `InvalidArgument`; one transaction writes the `videos` row and drops the upload row; idempotent on a repeated confirm; a missing object is `FailedPrecondition` with the upload row kept; the response carries `video`
- [ ] GetPost lists `videos` ordered `created_at, id` with fresh presigned view URLs; `DeleteVideo` removes the object then the row and deletes the filename's observation entry (→POST-18); DeletePost cascades `videos` rows and deletes their objects; the orphan sweep treats `videos.r2_key` as referenced and sweeps expired video uploads
- [ ] the post-side content validator accepts `VIDEO` with the IMAGE field rules and rejects a `VIDEO.file` not attached as a video (and an `IMAGE.file` that names a video) on SavePostContent; the generation-facing `AttachedImages` port returns videos beside photos as attachments carrying kind, key, content type and duration (photos first, then videos, each `created_at, id`)
- [ ] the PUBLISH snapshot (→POST-21) is unchanged: videos are not exposed to publishing in this task
- [ ] tests: store round-trip for videos and uploads.kind; CreateUpload refusals (extension, count, cross-kind filename, pending replacement); ConfirmUpload refusals (duration, size, content type) and idempotency with a fake object store; DeleteVideo order and observation cleanup; sweep keeps referenced video keys; validator VIDEO cases; sqlc output regenerated (`pnpm gen:sql` diff clean)
- [ ] `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...` and both ARCH-28 gates pass

## impl notes
- proto: keep `Image` untouched; `Video` is its own message so the browser can tell the kinds apart without a discriminator; `AttachmentKind` unspecified = photo so every shipped client keeps working; number the new `Post` field 23 (22 is `content_language`); add `DeleteVideo` next to `DeleteImage` in `PostService`
- post types: `Video{ID, PostSlug, Filename, Key, ContentType, Bytes, DurationMs, Width, Height, CreatedAt}`; `Upload` gains `Kind` and `ContentType`; `Observation` gains `Events []string` and `Speech string`, JSON-encoded with the existing entries (`events` `speech` omitted when empty so old rows decode unchanged)
- service: split `CreateUpload` into a shared reservation path with a kind switch — photo path byte-identical to today (`uploadContentType`, `.jpg` key, `ErrTooManyPhotos`); video path chooses content type by lowercase extension; the filename check queries `ImageFilenameTaken OR VideoFilenameTaken`; `replacePendingUpload` already handles the pending case and only needs the kind-agnostic key delete
- confirm: read the upload row first; branch on `Kind`; for video verify `head.ContentType == upload.ContentType` — MinIO and R2 both echo the PUT's type; `ErrInvalidVideo` maps to `InvalidArgument` in the rpc with the existing image error mapping pattern
- store: `store/queries/videos.sql` (`CreateVideo` `ListVideosByPost` `GetVideo` `DeleteVideo` `VideoFilenameTaken` `CountVideosByPost` `ListAllVideoKeys` `VideoKeyInUse`); `uploads.sql` `CreateUpload` gains kind and content type; sqlc via `pnpm gen:sql`
- sweep (`sweep.go`): the referenced-key set unions `ListAllImageKeys` and `ListAllVideoKeys`; the `posts/` prefix listing already covers video objects because they share the prefix
- content validation (`content_edit.go`): add `BlockVideo` to the type set; the attachment filter takes two filename sets; keep `IMAGE`↔photo and `VIDEO`↔video strict — a filename is unique across kinds so a mismatch is a wrong type, not an unknown file
- generation port (`generation/ports.go` `Posts.AttachedImages`): extend `generation.Image` with `Kind` (`photo` | `video`), `ContentType`, `DurationMs` rather than adding a second slice — the reobserve selection, freezing and merge functions all iterate one list by filename; this task only fills the fields, T014 uses them
- FE is untouched except the regenerated client under `shared/api/gen`; nothing in the browser sends `kind` yet, so the photo path stays green

## result
