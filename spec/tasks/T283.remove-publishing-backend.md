# T283 Remove publishing backend, deletion guards and database tables
> st:todo | ssot:PUB-42 PUB-46 PUB-47 PUB-48 POST-21 POST-29 POST-42 AUTH-18 AUTH-46 QUOTA-25 LANG-23 ARCH-5 ARCH-7 ARCH-10 ARCH-43 | base:PUB@6 POST@9 AUTH@7 QUOTA@19 LANG@4 ARCH@7 | dep:T312

## goal
Remove the retired publishing runtime and schema after the cleanup checkpoint, preserving post lifecycle and all unrelated services.

## acceptance
- [ ] Delete backend/internal/publishing and the temporary retirement CLI; remove service construction, startup recovery, sweepers, handler registration, device interceptors/bypasses, publishing master-procedure entries, publishing config validation/env examples and deployment configuration.
- [ ] Remove the post LivePublishFinder port/adapter, ErrPostPublishing guard and publication-only snapshot/image projections; post deletion still respects active generation and existing detach/purge behavior. Voice finalization snapshots and export are preserved.
- [ ] Remove publishing-specific dependencies from storage/r2.go and its tests, including ObjectStaging/ListStaged/SignGet wrappers and publishing.ErrInvalid references; keep generic Copy/Head/List/Delete/signing behavior actually used by clip/media or other surviving contexts.
- [ ] A new forward-only migration first asserts all five retired tables contain zero rows, then drops their tables/indexes/triggers in dependency order; a populated installation fails before destructive drops with a clear cleanup-required error. Historical migration files are unchanged.
- [ ] Fresh database boot and a seeded pre-retirement database taken through cutoff/cleanup/final removal pass; skipping cleanup is rejected without losing records. Latest-schema tests no longer seed removed tables, while historical migration tests retain version-specific fixtures.
- [ ] Post deletion/finalization, manual export data, human authentication, master administration, generation, credits and clip object operations remain functional in regression tests; old publishing routes accept no work or credentials and have no handler.
- [ ] ARCH-26 and SQL generation checks pass; generated sqlc sources match the final schema and no publishing sqlc entry or publishing-only generated-directory cleanup remains in backend/sqlc.yaml or scripts/gen.mjs.

## impl notes
- Replaces the obsolete todo SmartEditor split/DOM-test task; removing that driver is T284, so no new DOM harness or script refactor is built. The retirement behavior comes from PUB r6, not the old behavior-preserving review proposal.
- The code dependency T312 means its implementation is available; deployment additionally requires its actual cleanup receipt for the target environment. Release order is a hard gate even when source tasks are all complete. Never migrate a dirty production DB directly to the final build.
- Remove both the composition roots and consumer seams: cmd/api/{contexts,serve,adapters_post} and any publishing adapters; internal/post/{ports,types,service,rpc}; internal/auth/rpc; platform/config; storage/r2.go; SQL generator configuration, fixtures and docs.
- Drop order: publish_assets → publish_jobs → publish_job_ids → publishing_agents → publishing_pairings; remove any retirement triggers still attached to surviving intermediate tables as part of the same migration. Reserve the next migration number at implementation time.
- The final migration reads only DB state and performs no object-store I/O. Zero rows are a necessary guard, while the T312 receipt proves bucket cleanup and device shutdown. Down must not restore an executable publishing system.
- Keep proto definitions temporarily so the still-present but disabled agent module builds; T284 removes every wire/generated artifact. Removed routes need no permanent tombstone handler or special bearer-auth bypass.
- Remove only unused storage methods: generic copying, listing and signing also serve other features. Use actual consumer searches and clip/storage tests, not a blanket removal of every method first introduced for publishing.
- Update DEPLOY.md to enforce the cutoff rollback floor and require the bridge for any older populated install. A dirty final migration failure is diagnosed as missing cleanup, not fixed by deleting rows manually or weakening its guard.

## result
