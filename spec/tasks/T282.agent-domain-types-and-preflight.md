# T282 the agent maps proto at one adapter and keeps preflight out of main
> st:todo | ssot:ARCH-3 ARCH-12 ARCH-24 | base:ARCH@5 | dep:T008

## goal
Make `agent/internal/postpilot` a real anti-corruption adapter returning agent-owned types, remove `postpilotv1` from `naver`, `publishing` and `setup`, and move the connection preflight out of `cmd/postpilot-agent/main.go`.

## acceptance
- [ ] only `agent/internal/postpilot` and `agent/internal/gen` import `postpilotv1`: `publishing/executor.go` (23 symbols today), `setup/server.go`, `naver/publisher.go` consume `postpilot.Claim`, `postpilot.Manifest`, `postpilot.Block`, `postpilot.Stage`, `postpilot.FailureKind` (agent types) and the manifest→`Input` mapping (`naver/publisher.go:369 loadInput`, `:675 validateInput`) moves into the adapter
- [ ] `connectionPublisher.Run` (`main.go:141-178`) with its `naverPort` and `publisherBoundaries` lives in `internal/publishing/preflight.go`; `main.go` constructs and calls; `main_test.go`'s 520 lines move with it
- [ ] `naver` tests run with no protobuf import; a `go list -deps` test pins the import surface
- [ ] behaviour unchanged: ARCH-27 passes; one live publish through the paired agent is re-run after landing since the executor path changed (owner-authorized, as in T008)

## impl notes
- from review/arch-260919 F24 F28
- ARCH-3: agent gen is consumed only through the postpilot adapter; ARCH-12 keeps the agent's own shape small
- depends on T008 so the live Naver e2e is not disturbed mid-run; lands right after

## result
