# T403 Six intro and five outro presets with their neutral decoration and the region scrim
> st:todo | ssot:CDS-70 CDS-88 CDS-89 CDS-90 CDS-91 CDS-92 CDS-93 CDS-94 CDS-95 CDS-96 CDS-97 CDS-98 CDS-99 CDS-22 CDS-17 CDS-14 CDS-32 CDS-44 CDS-84 CDS-52 CDS-79 | base:CDS@26 | dep:T401

## goal
The renderer draws, verifies and admits intro 매거진 커버 · 명조 매거진 · 프레임 박스 · 큰 외곽선 글자 · 로어서드 · 주아 스티커 and outro 엔딩 크레딧 · 세로 막대 로어서드 · 칩 줄 · 세 줄 목록 · 원형 도장 exactly as CDS-89..99 state, and a region block on a bright ground takes the CDS-32 scrim.

## acceptance
- [ ] design.json adds intro `cover` `serif` `frame` `outline` `lower` `sticker` and outro `credits` `sidebar` `chips` `list` `stamp` with the slots, sizes, floors, faces, gaps, anchors and decoration of CDS-89..99, and `color`/`scrim` gain `scrim.radial` (CDS-14); `design.Region` validates each id, so the project accepts them (CLIP-111)
- [ ] `design.LayoutRegion` handles every new item: left-set and edge-anchored blocks (top edge at y 200 on 9:16, bottom edge on the ratio's BOTTOM anchor, LEFT anchor + inset), per-preset measures, slot-bound decoration (flank hairlines, a leading dot, an outline frame following the text with a minimum width, a plate, a leading square, a pill), block decoration (a side bar over the block's height, two rings with dots), one-line slots, the chips flow (rows wrap inside the measure), the list group (left-aligned, one shared fitted size), arc slots (upper and lower ring arcs with a 560 px length) and a block rotation about its centre; slot-bound decoration disappears with its empty slot (CDS-73)
- [ ] the region overlay view gains the drawables these need — stroked or filled rectangles with a corner radius, circles, outline-only text (no fill, white stroke), a slot stroke width other than 4/6 (Jua 14), `<textPath>` on a `<defs>` arc, a rotation group — through a new view version registered in the catalog, bindings and the startup probe; the old view keeps rendering for stored assets until nothing binds it
- [ ] V20 recomputes the layout for every preset and matches each decoration part (new manifest part kinds added to `design.ElementKinds` with bounds, so safe-area and overlap checks keep working); a rotated block is checked on its unrotated layout plus the rotation
- [ ] a centred region block on a bright ground (CDS-44 on the block's text bounds) takes `scrim.radial` 1360 px wide and the block's height + 560 px tall; an edge-anchored block takes its edge's linear scrim from the frame edge to 150 px past the block (CDS-32); the scrim part keeps the block's interval
- [ ] a region slot whose preset face lacks a character is `Over` (CDS-77, CDS-84): authored or answer-bound text is refused before generation with `unsupported_glyph` naming the entry, generated text goes through T402's repair
- [ ] goldens for all 15 presets × 3 ratios; the no-accent assertion stays, the no-`rx` and centred-part assertions are narrowed to what the presets still promise (white-only, rounded only where a preset draws a pill, plate or frame; centred only for centred slots); overlap test covers every preset's fitted worst case
- [ ] `node scripts/media-assets.mjs --update`, `MediaAssetVersion` bumped, `CLIP_MEDIA_SMOKE=1` `TestRegionPresetsRealFontSmoke` over all presets and `pnpm smoke:media` pass locally (ARCH-37); BE gate (ARCH-26), FE gate (ARCH-25), `pnpm test:dev` green

## impl notes
- geometry source of truth is CDS-89..99; `tmp/region-presets/src/presets.py` is the drawing those lines were written from — copy its drawing shapes (flanked label, dot row, frame, outline numeral, pill, square list, rings + textPath arcs) but take numbers from CDS where they differ (label 34 on the stamp arcs, chip labels floor 34, list body floor 48, bottom edge on the BOTTOM anchor 1380)
- item kinds to add to T401's schema: slot fields `face`/`weight` (nanummyeongjo 800, jua 400), `stroke_width`, `outline` (px, draws fill none + white stroke), `lines: 1`, `width`, `decor` {`flank`:{w,gap,alpha} | `dot`:{r,gap} | `frame`:{pad_v,pad_h,stroke,alpha,min_w} | `plate`:{pad_v,pad_h,radius} | `square`:{size,gap} | `pill`:{pad_v,pad_h,stroke,alpha,fill_alpha}}; group items `{"chips":{slots,gap,row_gap}}`, `{"list":{slots,gap}}`; block fields `rotate`, `side_bar` {w,inset}, `stamp` {r_outer, r_inner, stroke_outer, stroke_inner, fill_alpha, dot_r, arc_len, below_gap}
- stamp layout: centre y 900 on 9:16 moved by CDS-79's fraction; slot 1 on the upper arc (textPath on an arc of radius r_inner+14 drawn left→right), slot 3 on the lower arc (radius r_outer−14, sweep reversed so the text reads upright), slot 2 fitted to 320 px inside the inner ring and centred on the stamp, slot 4 a centred caption 64 px below the outer ring; the whole stamp rotates −8°, the slot-4 caption does not
- `prefixIDs` (caption fragments) already rewrites `href="#…"`; give arc paths ids unique per region instance so two regions on one frame never collide
- scrim: extend `applyDeclaredGround` for hook/ending — sample on the union of the block's copy parts (unchanged), pick radial vs edge from the preset's `scrim` field; the overlay needs a radialGradient ellipse; the scrim part's Region is the ellipse's bounding box clipped to the canvas
- the FE mirror only has to keep compiling against the grown JSON; the browser preview still draws the defaults only

## result
