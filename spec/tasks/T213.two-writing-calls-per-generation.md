# T213 A generation runs the flow call and then the narration call
> st:todo | ssot:CLIP-135 CLIP-19 CLIP-93 CLIP-87 CLIP-90 CLIP-96 | base:CLIP@31 ARCH@3 | dep:T212

## goal
Quote, approve, run, checkpoint and settle two writing calls per generation, resuming at the narration when the flow already exists.

## acceptance
- [ ] `GenerationPricing` gains `Narration llm.CallPolicy` and replaces `SkipPlan` with `SkipFlow`/`SkipNarration`; `PricingPolicyVersion` 2→3; `Valid()` pins `Plan` (the flow) and `Narration` at 32768 completion tokens; `PlanCalls()` counts each unskipped writing call with its retries
- [ ] `ClipPricedCall` gains `string label = 9` (`flow` | `narration`); the quote lists both writing calls and the FE approval (`ClipApprovalAction`) shows two writing rows
- [ ] `Run`: the `plan` stage becomes `flow` then `narrate`; after `Flow` the recovery state stores the flow with `FlowReady`; a failure or cancellation in `narrate` resumes with `SkipFlow`, pricing one writing call; `planRecoveryDigest` keeps its meaning; `LayoutComposition` runs after `narrate`
- [ ] `ValidatePreparation`, `freezeWork` and the pricing adapter check and price both budgets; `Budgets()` returns three values; a recovery state with `PlanReady` and a version-6 plan that carries scene elements still resumes at render
- [ ] CLIP-88 diagnostics and CLIP-78 progress carry the stage names `flow` and `narrate` with ko/en labels; no prose enters logs
- [ ] `ValidatePreparation` checks the frozen input allowance for the larger of the two requests (the narration request adds the flow) under CLIP-90
- [ ] tests: pricing valid/invalid and call counts; run order flow → narrate → layout → render with a stub planner; resume after a narration failure reuses the flow; FE quote renders two writing rows; verify BE (ARCH-26), FE (ARCH-25), generated code (ARCH-28)

## impl notes
- files: clip/generation.go (plan stage, ~495–540) · quotes.go (`GenerationPricing`, `freezeWork`, `quoteInputs` skipPlan branch, `QuoteInputDigest`) · recovery.go (`RecoveryState.FlowReady`) · ai/service.go (`Budgets`) · the `remainingQuotePricing.FreezeWork` implementer in usage/quota · rpc quotes mapper · proto
- keep the field name `Plan` for the flow policy to limit churn; the label, not the field, tells the owner which call is which
- the reservation is one, for both calls (CLIP-19: no AI call before a successful reservation); a narration-only resume reserves one call
- `T202` (revision job) prices with the same helper: one call for a narration target, two for a flow target

## result
