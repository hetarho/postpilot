# T171 Remove the style controls and draw the presets in the previews
> st:todo | ssot:CLIP-14 CLIP-17 CDS-70 CDS-65 CDS-31 CDS-15 | base:CLIP@23 CDS@16 ARCH@2 | dep:T166 T167 T168 T169

## goal
No screen offers a style choice any more, the template preview draws the four presets and the retuned pill exactly as the export renders them, and the accent picker survives only for the caption's word.

## acceptance
- [ ] `frontend/src/shared/config/clip-design.ts` drops `ClipStyleId` and `CLIP_STYLES`; no FE file imports a retired style id, and `ClipRecipe.copyStyles`, `ClipTemplate.copyStyles` and the api mappers that sent them are gone (the request field is simply left unset)
- [ ] the video-template editor no longer presents the styles checkboxes (`CompositionBuilder`) or the per-text style select (`CompositionTextControls`); `CopyStylePreview` is deleted and `VideoTemplatesPage` stops listing styles
- [ ] step ② 클립 다듬기 offers no per-text style control; `ClipCorrectionWorkspace`'s `styles={state.copyStyles}`, `ClipEditingState.copyStyles` in the FE mapper and everything feeding it are gone, while text, placement and timing edits stay
- [ ] `CompositionPreview` (the template's representative preview) draws the intro and outro presets from `CLIP_REGIONS` — slots, hairlines and bar, white only, scaled per ratio — the unplated information pair and the disclosure pill at `t.badge` 36/800 with `pad_chip` 16/28, using the same tokens the backend reads
- [ ] the accent picker remains and its help text says the accent colours one caption word only
- [ ] i18n ko and en drop the style strings (`style.*`, `editor.styles`, `composition.styleLabel`, `errors.invalid_style`, `styleLimit`, `styles`) and rename 오프닝 카드 / 엔딩 카드 to 인트로 / 아웃트로 wherever the role names surface
- [ ] test: the template editor renders and saves without a style control and sends no `copyStyles`
- [ ] test: step ② shows a text element with no style affordance and still allows text, placement and timing edits
- [ ] test: a `CompositionPreview` snapshot of a document with `intro="b"` and `outro="e"` places the slots and rules at the `CLIP_REGIONS` y values scaled to the chosen ratio, and the pill height equals 36 + 2 × 16
- [ ] test: the step ② draft preview lists the hook and ending element ids among its assets for a plan that carries them (parity with the backend manifest, CDS-65)
- [ ] `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- `clip-design.json` is byte-identical across FE and BE and a Go test asserts it, so this task consumes T166's file rather than editing it
- the step ② draft preview composites backend-rendered PNG assets (`PreparePreview`), so its parity with T167 comes from the backend; the FE-drawn preview is the template editor's `CompositionPreview`, which is where the presets have to be drawn by hand from the tokens
- the preview is the owner's only proof before paying for a render (CDS-65), so a preset drawn only on the backend is a parity failure, not a cosmetic gap
- keep the accent picker: one project accent survives (CLIP-14) and colours the caption's accent word alone (CDS-15); nothing in a region block reads it
- the correction workspace still edits text, position and timing — only the style affordance goes; the design step and the skeleton forms are T172's, so this task leaves the builder's hook/ending controls as they are apart from the style select

## result
