# T035 /about states the self-signup path: Get started as the one CTA, Log in as the quiet link
> st:todo | ssot:MARKETING-6 MARKETING-11 MARKETING-16 MARKETING-4 MARKETING-15 MARKETING-2 AUTH-30 THEME-18 | base:MARKETING@3 AUTH@2 THEME@4 ARCH@2 | dep:T030

## goal
Move the public page's one filled CTA to `시작하기 / Get started` leading to `/signup`, keep `로그인 / Log in` beside it as a quiet link, and rewrite every sentence about how a person gets an account to the path that now exists.

## acceptance
- [ ] `AboutHeader` renders exactly one filled CTA — a router `Link` to `/signup` with the `cta` button contract and the accessible name `시작하기 / Get started` — and `로그인 / Log in` as a quiet `label`-styled link beside it; the login link alone keeps handing `redirect` through (`isInAppPath`, MARKETING-2) because `/signup` takes no redirect; header order stays CTA → quiet link → the two preference menus viewport-side last; every control keeps the 44 px floor
- [ ] `hero.access` (ko/en) states the self-signup path: an email address and a password open an account, the address is verified by mail before the first login, and Google sign-in is available — no sentence claims the operator makes accounts; `plans.assignment` keeps stating the operator assigns a plan and nothing here sells or upgrades (MARKETING-15 — BILLING has not shipped; T038 flips it)
- [ ] the `marketing` catalog carries `header.getStarted` in ko and en (parity test), and no remaining key or sentence on the page says `가입 절차는 없습니다` / "there is no sign-up"; `AboutPage.test.tsx`'s copy fixtures and assertions are updated: one filled CTA → `/signup`, the login quiet link → `/login` with `redirect` passthrough, the access sentence, and the unchanged claims (publishing still operator-tier and in live verification, no `<form>`, no input, no third-party request)
- [ ] the page still mounts with no Connect call and no session branch (MARKETING-1); the router test that asserts `/about` issues no request stays green
- [ ] `pages/login`'s About link, the login form's failure behaviour and the guard are unchanged (MARKETING-3); the login test stays green
- [ ] `spec/tasks` reference: MARKETING-4 discipline — the sentence about Naver publishing is not softened in this change
- [ ] gates: ARCH-25 (`test` · `lint` · `lint:fsd` · `lint:style:probe && lint:style` · `build:web`)

## impl notes
- the CTA and the access sentence move together with the change that shipped signup (MARKETING-16); T030 is that change, so this task lands right after it and never before
- one filled CTA per view (THEME-18): the login link becomes the same quiet `typographyStyles({ variant: 'label' })` link the About link on `/login` uses, not a secondary button
- `/signup` is a link to a page that OWNS the form (MARKETING-6): the About page itself still contains no form or input
- keep the plans assignment sentence exactly as T024 left it; changing it is T038's job when a rung can actually be bought

## result
