# T279 Pin enum mirrors in the surviving backend and frontend
> st:todo | ssot:ARCH-3 ARCH-24 ARCH-25 ARCH-26 | base:ARCH@7 | dep:T313

## goal
Finish the non-publishing enum-contract work on surviving BlockType, ModelPurpose and language mappings.

## acceptance
- [ ] Tests walk each surviving generated BlockType and ModelPurpose enum and check every hand-maintained mapping, including invalid/unspecified handling; no fallback silently assigns a valid business value to an unmapped enum.
- [ ] Repeated transport language conversion is consolidated at the existing platform/rpcserver adapter where appropriate, with every consuming context using the same tested mapping and pure domain language values preserved.
- [ ] No publishing Stage/FailureKind/STAGE_RANK mapping or agent adapter is recreated solely to satisfy the old task wording; removed shared failure values remain reserved.
- [ ] Existing behavior is unchanged; ARCH-25 and ARCH-26 pass for surviving code and corresponding generated-enum contract tests cover the changes.

## impl notes
- from review/arch-260919 F25; revised after PUB r6 to exclude every retired publishing/agent enum mirror and to depend on T313 rather than T008.
- This is existing independent technical-debt work, not a prerequisite for removing automatic publishing. Re-scan the surviving copies at implementation time because removal changes their count; do not preserve the old count of five/three as an acceptance target.
- This todo task is rebased to ARCH r7; unlike T008 it can be revised during create-task. Avoid changing domain behavior or introducing reflection/registries beyond the focused mapping tests.

## result
