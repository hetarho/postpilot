# T275 job/store, template/rpc and modelcatalog/rpc have tests
> st:todo | ssot:ARCH-24 | base:ARCH@5 | dep:T272

## goal
Add the missing test packages the review found: `job/store` (448 lines, 0 tests, including dispatch authorization), `template/rpc` and `modelcatalog/rpc`.

## acceptance
- [ ] `backend/internal/job/store` has tests over a temp SQLite (migrations applied) covering `Reserve`/`Activate`/`SweepUnactivated`/`AuthorizeDispatch` and the serialization of authorization with cancellation
- [ ] `template/rpc` and `modelcatalog/rpc` have handler tests covering the proto↔domain mapping for each rpc and the `toConnectError` reason for each domain error
- [ ] ARCH-26 passes

## impl notes
- from review/arch-260919 F21 (test gaps)
- runs after T272 so the `job/store` surface under test is the generic one
- follow the pattern of the existing `*/rpc/*_test.go` handler tests and the temp-DB helper the other store tests use

## result
