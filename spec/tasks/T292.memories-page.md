# T292 기억 is the fifth destination of the 글 group
> st:todo | ssot:MEM-24 MEM-25 ARCH-13 ARCH-14 ARCH-20 ARCH-24 | base:MEM@1 | dep:T289

## goal
Give memories the directory screen the other authored layers have, so a wrong memory can be fixed instead of reaching every future prompt.

## acceptance
- [ ] `/memories` is the fifth destination of the 글 group after 지침, registered in the route tree and the navigation config, and the navigation test names 기억 in the group's destinations
- [ ] the page IS the list: each row shows the text, a kind badge and its tags, in the server's stated order; there is no standing form
- [ ] a read-first text edit, an edit of kind and tags, and a delete whose confirmation states there is no undo
- [ ] one docked `새 기억` opens the shared `Sheet` with a textarea carrying a live remaining count from `shared/config`, the kind control and the tag input
- [ ] server refusals (over the text bound, the cap, a duplicate) render through the shared failure catalogue; the client mirrors no bound and predicts no message
- [ ] the list query is keyed `(accountId, 'memories')` with `staleTime: 0` and `refetchOnMount: 'always'`, invalidated by every create, edit and delete
- [ ] vitest beside the code covers the list order, a read-first edit, the cap refusal rendering and the sheet's create
- [ ] ARCH-25 passes

## impl notes
- the shape is the 지침 page's, deliberately: a docked add action opening a Sheet over a page that is only the list (GUIDE-20)
- the kind control belongs in `entities/memory/ui` because the create sheet and the row edit both need the identical control and a feature may not import a sibling feature
- i18n lives as slice fragments under the memory slices, assembled by `app/providers/i18n`; no new cross-cutting namespace
- every generic control is a `shared/ui` primitive; the badge, sheet, textarea and confirmation are existing ones

## result
