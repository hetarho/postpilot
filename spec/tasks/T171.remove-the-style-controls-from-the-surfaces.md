# T171 Remove the style controls and draw the regions in the preview
> st:todo | ssot:CLIP-14 CLIP-17 CDS-70 CDS-65 | base:CLIP@22 CDS@15 ARCH@2 | dep:T166 T167 T168

## goal
No screen offers a style choice any more, and the draft preview shows the same three regions the export renders.

## acceptance
- [ ] `frontend/src/shared/config/clip-design.ts` drops `ClipStyleId`, `CLIP_STYLES`, `clipStyle` and `clipType`, or narrows them to the single caption treatment, with no union of five ids left
- [ ] the video-template editor no longer presents a copy-style selection, and saving a template sends none
- [ ] step ② 클립 다듬기 no longer offers a per-text style control; `ClipCorrectionWorkspace`'s `styles={state.copyStyles}` and everything feeding it are gone
- [ ] the draft preview draws the intro and outro blocks and the unplated information pair exactly as T167 and T168 render them, keeping CDS-65 preview/export parity
- [ ] the disclosure pill in the preview matches the new padding and type size
- [ ] no FE file imports a retired style id; `pnpm lint:fsd` and `pnpm lint` stay clean
- [ ] test: the template editor renders and saves without a style control
- [ ] test: step ② shows a text element with no style affordance and still allows text, placement and timing edits
- [ ] test: a preview snapshot of a clip with both blocks matches the manifest geometry the backend returns
- [ ] `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- `clip-design.json` is byte-identical across FE and BE and a Go test asserts it, so this task consumes T166's file rather than editing it
- the preview is the owner's only proof before paying for a render (CDS-65), so a region drawn only on the backend is a parity failure, not a cosmetic gap
- keep the accent picker: one project accent survives (CLIP-14) and is what the outro rule paints
- the correction workspace still edits text, position and timing — only the style affordance goes
