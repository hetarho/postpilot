# T409 The 상위 노출 단어 사용 guideline preset leaves the backend and the guideline proto
> st:todo | ssot:GUIDE-14 GUIDE-15 GUIDE-37 | base:GUIDE@7 GEN@13 | dep:T408

## goal
No preset remains:
- no preset text, no per-account on/off or 분야 set, and no UpdateGuidelinePreset RPC
- `ListGuidelines` answers the owner's guidelines alone, and `ForPrompt` returns only them
- the two preset tables are dropped

## acceptance
- [ ] `ListGuidelines` returns guidelines only; `ForPrompt` returns the owner's ordered texts (global, then templates, then 분야, each by `created_at, id`) and nothing appended; the inside-section precedence "earlier line wins" (GUIDE-37) is unchanged in the prompt
- [ ] the dead `forRevision` argument, which existed only to omit the preset line from revise, is removed from `guideline.ForPrompt`, the `generation.GuidelinesForPrompt` port, `freezeGuidelines` and the cmd/api adapter; `GuidelineTexts`/`PromptTexts` lose `Preset`
- [ ] new goose migration (next free number, after T408's) drops `guideline_preset_fields` then `guideline_presets`; Down recreates both empty with 0078's DDL; migration test in the `migration_0083_test.go` pattern; `TestMigration0078IsIdempotent` no longer expects the tables on a fresh install
- [ ] proto `guideline.proto`: `rpc UpdateGuidelinePreset` and messages `GuidelinePreset`, `GuidelinePresetFields`, `UpdateGuidelinePresetRequest/Response` deleted; `ListGuidelinesResponse.preset = 2` removed with `reserved 2; reserved "preset";`; gen regenerated
- [ ] `rg -n "Preset|preset" backend/internal/guideline backend/cmd/api/*guideline* backend/internal/generation proto/postpilot/v1/guideline.proto` shows no preset code (clip presets are elsewhere and untouched)
- [ ] verify BE (ARCH-26) · verify gen (ARCH-28) · FE test and build still green after regen

## impl notes
- The dropped state loses nothing a user could see: with no phrase list the preset line never reached a prompt (GUIDE-40), and T408 already stopped freezing it.
- Delete `backend/internal/guideline/preset.go`, which holds `PresetText` and `presetPhraseHeading`.
- Edit `backend/internal/guideline`:
  - `types.go`: drop `Preset`, `PresetPatch` and `PromptTexts.Preset`; collapse to `[]string` if that reads cleaner. Fix the `ErrFieldNotFound` comment; the error stays for the fields scope.
  - `ports.go`: drop `Store.Preset` and `Store.UpdatePreset`.
  - `service.go`: drop `Preset`, `UpdatePreset`, the preset read in `ForPrompt`, the `forRevision` parameter and the `slices` import.
  - `store/store.go`: drop `Preset`, `UpdatePreset` and `preset()`.
  - `store/queries/guidelines.sql`: delete the six preset queries (`GetGuidelinePreset`, `ListGuidelinePresetFields`, `UpsertGuidelinePresetEnabled`, `TouchGuidelinePreset`, `DeleteGuidelinePresetFields`, `InsertGuidelinePresetField`), then `pnpm gen:sql`.
  - `rpc/handler.go`: drop the preset read in `ListGuidelines`, `UpdateGuidelinePreset` and `toProtoPreset`.
- Edit the other packages:
  - `generation/ports.go` and `generation/service.go`: `GuidelinesForPrompt` without `forRevision` and `Preset`.
  - `cmd/api/adapters_generation.go` `generationGuidelines.ForPrompt`.
- Tests:
  - guideline `service_test.go`: drop the fake-store preset members and the three preset tests; edit `TestForPromptDistinguishesNoTemplateFromATemplate`.
  - `store/store_test.go`: drop `presetIs` and the three preset tests.
  - `rpc/handler_test.go`: delete `TestUpdateGuidelinePresetMapsPresence`; drop the preset from the session test's call and message lists; keep the fields half of `TestFieldsAndThePresetProject`.
  - generation `guideline_test.go`: drop `fakeGuidelines.preset` and `TestThePresetLineRidesOnlyWithFrozenPhrases`; update the call sites for the dropped `forRevision`.
  - `cmd/api/bootstrap_test.go`: delete `TestPresetTextNamesThePhraseSectionHeading`. Reduce `TestGuidelineAdapterFreezesTheFieldGroupThenThePreset` to the field-group, other-분야 and delete checks, and rename it.
  - `cmd/api/boundary_test.go:55,64`: drop `"UpdatePreset": true` and "or the preset". Cite GUIDE-18/GUIDE-2 instead of GUIDE-33.
  - `quality/boundary_test.go:11`: drop the GUIDE-33 clause.
  - `cmd/seed/main_test.go`: drop the `guideline_presets`/`guideline_preset_fields` counts; they fail with "no such table".
- Comments in surviving code that cite deleted GUIDE IDs are fixed here: `bootstrap_test.go:949`, `generation/guideline_test.go:360` (GEN-57).

## result
