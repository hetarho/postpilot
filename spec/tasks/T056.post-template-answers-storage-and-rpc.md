# T056 The post's template answers: table, autosave field, and the way to generation
> st:todo | ssot:POST-62 POST-13 POST-4 TEMPLATE-43 | base:POST@3 TEMPLATE@4 ARCH@2 | dep:-

## goal
Give a post a durable, per-label set of template answers that autosave through SavePostDraft and reach the enqueue as part of the post's input.

## acceptance
- [ ] migration `0033_post_template_answers.sql` (embedded, boot-run): `post_template_answers(post_slug TEXT NOT NULL REFERENCES posts(slug) ON DELETE CASCADE, label TEXT NOT NULL, text TEXT NOT NULL DEFAULT '', enabled INTEGER NOT NULL DEFAULT 1, updated_at TEXT NOT NULL, PRIMARY KEY(post_slug, label))`
- [ ] `post.TemplateAnswer{Label, Text, Enabled}` in the post context's domain; the store upserts one row per present answer inside the draft save's own transaction and lists a post's answers ordered by label
- [ ] proto: `TemplateAnswer{label, text, enabled}`; `SavePostDraftRequest.repeated TemplateAnswer template_answers = 8` documented as upsert-per-entry (an entry sets that label, an absent label is preserved, an empty list changes nothing, and no entry ever deletes); `Post.repeated TemplateAnswer template_answers` so a reload seeds the fields
- [ ] the handler refuses `InvalidArgument` for a blank label, a label over `TEMPLATE_ASK_LABEL_MAX_CHARS`, a text over `TEMPLATE_ASK_VALUE_MAX_CHARS` (`POST_TEMPLATE_ANSWER_TOO_LONG` with `field` `max` `actual`) and two entries naming one label, validated before anything else in the request applies
- [ ] saving answers is an option save: status, `content_revision`, `machine_baseline`, `finalized_revision` and learn eligibility are untouched, and a `finalized` post can still take one
- [ ] answers survive assigning, changing and clearing `template_id`, and deleting the template that named them; deleting the POST removes them
- [ ] `generation.PostInput` carries `TemplateAnswers []TemplateAnswer` and the post→generation adapter in `cmd/api` fills it from the stored rows
- [ ] tests: store round-trip incl. upsert-over-existing and the cascade · handler presence semantics (absent list preserves, one entry leaves the other labels alone) · each refusal · an option save leaving revision/status/baseline untouched · the adapter carrying the answers into `PostInput`
- [ ] verify: `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...` · `pnpm gen:proto && git diff --exit-code -- backend/internal/gen frontend/src/shared/api/gen agent/internal/gen` · `pnpm gen:sql && git diff --exit-code -- backend`

## impl notes
- a child table rather than a JSON column on `posts` (owner's call): one row per label makes a single-field autosave one upsert instead of a read-modify-write of the whole set, which is what keeps POST-62's per-field presence honest when two tabs answer two fields; it also inherits the `images`/`videos` cascade pattern this schema already uses
- the table belongs to the POST context — the answers are part of the post aggregate, keyed by a label the template happens to have authored, and no foreign key points at a template. That is deliberate: TEMPLATE-43's labels are text, not ids, and POST-62 keeps an answer whose label the current template no longer carries
- upsert only, never delete: clearing an answer is `text: ""`, which the freeze already treats exactly like the switch being off (→TEMPLATE-45). No RPC deletes a row, so a label typed under an earlier template survives a swap back
- `enabled` defaults to 1 and an answer with NO row is treated as on with empty text: the field exists because the template says that data is required, so it must be presented switched on, and a blank answer is dropped at the freeze anyway — an off-by-default field would silently mean "never ask"
- validation order matters: the label/text checks run with the `template_id` validation POST-26 already does before the request applies anything, so a bad answer mints no post and changes no title
- the answers are NOT in the generation payload: the freeze bakes them into the rendered body (T057), so nothing here touches `generation_payload.go`. `PostInput` carries them only so `freezeTemplate` can pass them to the template context
- proto field 8 on `SavePostDraftRequest` — 7 is unused but check the file before claiming a number

## result
