# T407 The frontend drops ②'s replacement marks and the 상위 노출 단어 사용 preset row
> st:todo | ssot:POST-83 GUIDE-14 GUIDE-20 | base:POST@16 GUIDE@7 | dep:-

## goal
② renders the prose without replacement marks and nothing takes a replacement, and /guidelines lists only the owner's guidelines with no preset row. The frontend stops reading and sending every wire field the phrase feature added, so the later proto removal (T408, T409) cannot break it.

## acceptance
- [ ] ② (review) renders title, tags and body prose as plain text: no `aria-haspopup` mark, no phrase popover, no take, and a manual block edit still saves exactly as before
- [ ] /guidelines renders no preset row, switch, 적용할 분야 picker or needs-a-분야 notice; the list, the scope chips and 새 지침 are unchanged
- [ ] the frontend neither reads `Post.replacement_candidates` / `ListGuidelinesResponse.preset` nor sends `SavePostContentRequest.taken_candidates` or UpdateGuidelinePreset: `rg -n "replacementCandidates|takenCandidates|GuidelinePreset|updateGuidelinePreset|ReplacementCandidate|ReplacementSurface" frontend/src --glob '!**/shared/api/gen/**'` is empty
- [ ] i18n keys `posts.replacements.*` and `guidelines.preset.*` are gone in ko and en; `guidelines.page.description` and `guidelines.page.order` no longer mention the preset
- [ ] tests: the feature-only tests are deleted, and the edited tests (content queue, BlockEditor, BlockList, EditorPage refine/published, GuidelinesPage order string, post and guideline wire mappers, useSavePostContent) pass
- [ ] verify FE: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- Order: this lands before any proto change (T408, T409). An unread field is harmless and an empty `taken_candidates` is valid, so this task is wire-safe against today's backend. Don't touch `shared/api/gen` or any `.proto`.
- Delete (paths under `frontend/src/`):
  - `entities/post/model/replacements.ts` and its test
  - `entities/post/api/replacement-mappers.ts` and its test
  - `features/edit-post-content/ui/ReplacementMark.tsx`
  - `features/edit-post-content/config/i18n.ts`: the whole fragment is `replacements.*`. `config/index.ts` stays for `EDITOR_HANDOFF_TTL_MS`.
  - the whole `features/adopt-guideline-preset/` slice
  - `entities/guideline/api/guideline-mutations.test.tsx`: its only test is the preset one
- Edit, ② marks:
  - `entities/post/index.ts`: drop the replacement type and value exports (`ReplacementCandidate`, `ReplacementSpan`, `TextAt`, `applyReplacement`, `sameCandidate`, `spansAt`, `visibleSpans`).
  - `entities/post/config/index.ts`: drop `REPLACEMENT_PHRASES_MAX`.
  - `entities/post/model/types.ts`: drop `PostDraft.replacementCandidates`.
  - `entities/post/api/post-queries.ts`: drop the `toReplacementCandidates` mapping.
  - `entities/post/api/useSavePostContent.ts`: `save()` loses `takenCandidates` and returns `Promise<bigint>` again (`response.post.contentRevision`). Drop the `replacementCandidates` patch in `onSuccess`.
  - `entities/post/ui/BlockList.tsx`: drop the `renderText` prop and `plainText`. Render `content.title`, `#{tag}`, `block.content` and `item` directly, and `tags.map((tag)`.
  - `features/edit-post-content/ui/BlockEditor.tsx`: drop the `spent` state, the `spans` memo, the `pencils`/`pencilRef` refs, `take()`, the `renderText` prop, and the `editButtonRef` props on the header `Editable` and `BlockEditRow` (and `BlockEditRow`'s prop). Keep `key={slug:machineBaselineRevision}` in `pages/editor/ui/EditorRefinePanel.tsx`.
  - `features/edit-post-content/model/useContentAutosave.ts`: drop `candidates`, `take` and `taken`; the snapshot is `{content}`.
  - `features/edit-post-content/model/content-queue.ts`: `ContentSnapshot` loses `taken`, and `SendContent` is `(snapshot, expectedRevision) => Promise<bigint>`. Drop `candidates` from `Attachment`/options, `union()`, `resolveTakes()`, the `merge:` option, the `snapshot.taken.length === 0 &&` clause in `settled`, and the `same(snapshot, post.saved)` short-circuit plus the `answer.candidates` bookkeeping in `send`.
- Edit, preset row:
  - `entities/guideline/model/types.ts`: drop `GuidelinePreset`.
  - `entities/guideline/api/guideline-queries.ts`: drop `toGuidelinePreset`; keep `toFieldIds`.
  - `entities/guideline/api/guideline-mutations.ts`: drop `useUpdateGuidelinePresetCall` and the imports only it used.
  - `entities/guideline/api/useGuidelines.ts`: returns `{guidelines}`.
  - `entities/guideline/index.ts`: drop the three preset exports.
  - `pages/guidelines/ui/GuidelinesPage.tsx`: drop the row and the `preset` destructure.
  - Doc-comment fixes: `entities/guideline/ui/GuidelineFieldPicker.tsx:13` and `entities/blog-field/api/blog-field-mappers.ts:39`.
- Shared:
  - `shared/api/index.ts`: drop the re-exports `ReplacementCandidateSchema`, `ProtoReplacementSurface`, `ProtoReplacementCandidate`, `GuidelinePresetSchema`, `UpdateGuidelinePresetResponseSchema` and `ProtoGuidelinePreset`. Keep `SeedPresetFieldsResponseSchema`, `VoucherPreset*` and `GetClipRegionPreset*`; they belong to other features.
  - `app/providers/i18n/resources/index.ts`: remove `editPostContentI18n` and `adoptGuidelinePresetI18n` from the imports, from FRAGMENTS and from the ko/en spreads. A leftover fails `resources.test.ts`.
- Remove `shared/ui/editable/Editable.tsx` `editButtonRef`: it existed only to refocus after a take. Keep `InlinePopover`, `shared/lib/autosave` `merge` and `GuidelineFieldPicker`'s `legendVisible`: they are generic, or predate the feature.
- Tests, fakes (`test/`):
  - `test/posts.ts`: drop `FakePostRow.replacementCandidates`, `contentSaves[].takenCandidates`, `Row.replacementCandidates`, their proto mapping, and the taken-index validation and spend filter in the `savePostContent` handler.
  - `test/guidelines.ts`: drop the `preset*` options, `FAKE_GUIDELINE_PRESET_TEXT`, the preset state and the `updateGuidelinePreset` rpc.
  - `test/wire-enum.test.ts`: drop the `ReplacementSurface` row.
- Tests, specs:
  - `post-queries.test.ts`: delete the candidate-mapping test.
  - `useSavePostContent.test.tsx`: delete the taken-indices test; the expectation becomes `resolves.toEqual(2n)`.
  - `BlockList.test.tsx`: delete the three `renderText` tests.
  - `content-queue.test.ts`: delete `describe('taken candidates')`. Drop `taken` and `candidates: []`; the send mock resolves a bigint.
  - `BlockEditor.test.tsx`: delete `describe('the replacement marks')`.
  - `EditorPage.refine.test.tsx`: delete `describe('the replacement marks')` and fix the header comment.
  - `EditorPage.published.test.tsx`: drop the row's `replacementCandidates` and reword the "No mark" comments.
  - `guideline-queries.test.ts`: delete `describe('the guideline preset on the wire')`.
  - `GuidelinesPage.test.tsx`: delete `describe('the 상위 노출 단어 사용 preset')` and update the asserted `page.order` string.
- Keep: the brief's 분야 picker (`post.field`), guideline 분야 scope, the quality metric rows, nouns, and every clip or voucher "preset" (a different feature).

## result
