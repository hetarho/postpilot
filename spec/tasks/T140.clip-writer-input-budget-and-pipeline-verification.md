# T140 Fix clip writer input admission and verify the complete pipeline
> st:doing@260913.e2e | ssot:CLIP-86 CLIP-88 CLIP-90 CLIP-91 QUOTA-14 QUOTA-32 QUOTA-44 QUOTA-45 QUOTA-47 QUOTA-53 ARCH-24 ARCH-31 LANG-21 LANG-23 | base:CLIP@16 QUOTA@11 ARCH@2 LANG@3 | dep:T139

## goal
Allow detailed templates and complete multi-source observations to reach the writer within a correctly priced frozen allowance, and verify planning through a playable rendered output.

## acceptance
- [x] New clip policies freeze observe input 30,000 / writer input 64,000; quote math, endpoint/context checks and request admission use the same immutable value, while absent legacy fields retain 30,000 and existing approvals are never enlarged silently.
- [x] Known invalid settings or known prompt overflow stop before media preparation and paid analysis; complete observed input is rechecked before the writer with explicit bounded size/limit diagnostics and no content truncation, automatic retry or extra provider call.
- [x] Input failures show actionable ko/en explanations and numeric request size/limit instead of unknown diagnostics; privacy allowlists exclude prompts, owner facts, filenames, credentials and response bodies.
- [x] The triggering private input (20 sources, 37,198 encoded bytes against the prior 27,952 limit) reproduces the old refusal and reaches the writer with the new approved allowance; a public synthetic detailed-template/multi-source fixture covers the same failure shape through the real strict HTTP adapter and parser.
- [ ] Planning and real FFmpeg rendering produce a playable output from representative complete input, with explicit provider/media provenance; authorized live verification reuses the retained analysis for isolated diagnosis, keeps provider calls/cost bounded and does not modify the customer's project or credit ledger.
- [ ] Tests cover immutable legacy quotes, under-capacity endpoints, oversized input and privacy, early refusal before media work, preserved observations and confirmed usage; full local ARCH gates, pushed CI and exact-image production health pass before completion.

## impl notes
- Add an optional input-token allowance to the immutable llm.CallPolicy; zero reads as the historical 30,000. FreezeExecution sets the new stage-specific allowance before endpoint qualification. All readers use its effective bound; non-clip FreezeCall/hold assumptions stay unchanged. No DB/protobuf migration or new dependency.
- Keep the existing conservative byte accounting and wrapper/media reserves; the approved writer allowance grows rather than deleting observations, changing authored XML, estimating tokens optimistically or weakening upstream limits. Explain the measured encoded size and its actual limit on refusal.
- Run known-input/model checks before WithWorkspace/prepareBatch using validated declared metadata solely for early refusal; verified media and exact-count qualification still gate reservation afterward. Preserve approval and current pricing comparisons.
- Reproduce server-owned CLIP_INVALID_INPUT separately from model-output failures. Preserve detailed existing observation diagnostics and source/chunk locations; add closed input-specific checks/phase and safe byte/count fields.
- Keep the actual private replay in /tmp with mode 0600; do not commit customer facts, analysis content, filenames or provider secrets. Public regression data must be synthetic. Never start the customer's paid generation automatically; an isolated live writer call needs its concrete cap/approval and records verification provenance.
- Evidence: job 5176c61d008509dfd8206645c1a9f08b failed at plan 2026-09-13T13:57:59Z after 20 observe calls and zero write calls. The same saved settings/observations pass preparation and fail Plan before the stub writer: 37,198 encoded bytes > 27,952; strict adapter metadata also exceeds its old 28,976 allowance (34,047).
- Commit/push/deploy follows the continuing failure-fix authorization. Check for active production clip jobs before push/rollout and wait for normal completion rather than interrupting them. Do not treat a diagnostic-only change or build success as proof of successful generation.

## result
- Root cause reproduced from private saved input: preparation passed, but Plan refused 37,198 encoded bytes against 27,952 before any writer call. With the newly approved 64,000 allowance the identical input reaches the writer; zero-valued legacy policies still refuse it with exact size/limit diagnostics.
- New frozen input budgets feed pricing, endpoint/context qualification and both request guards. Known settings and prompt checks now run before source downloads/transcoding. Input-overflow checkpoints retain completed observations and carry safe numeric evidence; ko/en views display the size and limit.
- Offline complete-pipeline regression passed in the production nonroot media runtime (`--network none --memory 1g --cpus 2`): detailed synthetic XML, 20 synthetic originals/observations, strict HTTP adapter, 20 observe + 1 writer fixture responses, saved 15-second MP4, original/output speech correlation >=0.9999, settlement and cleanup. Elapsed 51.55 s, peak memory 283,516,928 bytes. This proves server integration/rendering using synthetic provider responses and footage, not a live model answer.
- `TestClipWriterInputRelease` now gates every production image through `clip-input-smoke`; this triggering multi-source request shape cannot be skipped by an ordinary successful build.
- Local gates: FE 227 files / 1,818 tests; lint/format/FSD/style/probe/web build; BE gofmt/vet/build/full tests plus immutable-input-quote regression; agent gofmt/vet/build/tests/shell syntax; skills check and spec lint passed (existing hook/chunk/spec warnings only). Proto/SQL regeneration produced no diff. Production-image and deployment verification remain pending.
- Isolated actual-input live harness is compiled privately. Read-only endpoint preflight admitted 64,000 input / 32,768 completion with external maximum $0.08544. It allows exactly one explicitly authorized writer call under $0.10, caches the response for unpaid parser replay, reads selected retained originals and renders privately; it has no production DB/ledger writes or object uploads/deletes. Owner approval is pending; no live provider call has been made.
