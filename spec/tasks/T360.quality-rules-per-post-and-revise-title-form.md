# T360 Ticked quality rules open the per-post half; in revise the title form binds only a title request
> st:todo | ssot:GEN-14 GEN-51 TMPL-51 | base:GEN@11 TMPL@11 | dep:T359

## goal
The ticked quality-rule section leaves the write prompt's stable prefix and becomes the first thing in the per-post half (GEN-14, GEN-51). The revise prompt's template title instruction applies only when the request asks to change the title; otherwise the title stays as it is (TMPL-51).

## acceptance
- [ ] Write prompt with ticks:
  - the system half is byte-identical to the same input with no ticks, for a Korean target, an English full profile and an English portable profile, each with a template and guidelines;
  - the per-post half is exactly `"[발행 글 측정 규칙]\n- " + r0 + "\n- " + r1 + "\n지침이 위 규칙과 충돌하면 지침을 우선하세요.\n\n"` followed by the no-tick per-post half, byte for byte.
- [ ] Write prompt with no ticks (nil and empty): both halves equal `write_prompt_no_template.golden`, unchanged.
- [ ] Heading and closing line: they stay Korean for an English target, as today, while the rule texts stay in the target language as frozen. The closing line is the only precedence statement in the section.
- [ ] Revise prompt with a title-area brief:
  - it carries `reviseTemplateTitleInstruction` once, in the fenced position `templateTitleInstruction` holds in the write prompt, and never `templateTitleInstruction`;
  - its template section equals the write prompt's section with that one line swapped;
  - Korean and English content languages both get the Korean line.
- [ ] Revise prompt with a brief without a title area, or with no template: byte-identical to today. `revise_prompt_no_template.golden` and `TestRevisePromptInjectsTheSameSectionAtTheSamePosition` stay green; neither golden carries a template, so no golden file changes.
- [ ] The write prompt with a title-area brief is unchanged: it still carries `templateTitleInstruction`.
- [ ] Behavior otherwise unchanged: existing tests still pass, with the updates listed in impl notes.
- [ ] Tests:
  - `internal/generation/quality_section_test.go` `TestTickedQualityRulesOpenThePerPostHalf`, replacing `TestQualityRulesSectionSitsBetweenTheStaticRulesAndTheBaseline`; `TestTheRevisePromptCarriesNoQualityRules` is kept.
  - `internal/generation/template_test.go` `TestTheReviseTitleFormBindsOnlyATitleRequest`, new.
  - `TestTemplateTitleAreaPrecedesTheBodyFence`, updated.
  - `TestTheWord지침NeverAppearsInTheTemplateSection`, which now also scans `reviseTemplateTitleInstruction`.
- [ ] BE gate passes: `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test -timeout 30m ./...`

## impl notes
- Current placement at HEAD 58e1b5ca:
  - `prompts.go:401` `writeQualityRulesSection(&stable, qualityRules)` writes `\n\n[발행 글 측정 규칙]\n- …\n지침이 위 규칙과 충돌하면 지침을 우선하세요.` into the stable half, after the static/tag-count/language/video lines and before the baseline and profile.
  - The per-post half is `prompts.go:410`: `"[이번 글]\n가제: %s\n메모: %s\n%s%s%s"`, holding title, memo, `fieldPhrasesSection`, `memorySection` and `attachmentMaterial`.
- The head of the per-post half is before `[이번 글]`. The section renders first in the user message, then one blank line, then today's `[이번 글]…` unchanged. Reasons:
  - GEN-14 lists the rules before the title hint.
  - `[이번 글]` opens the post's own material.
  - Keeping it whole makes the ticked per-post half a pure prefix of the unticked one, which is what the byte test pins.
- Closing-line copy: keep `qualityRulesPrecedence = "지침이 위 규칙과 충돌하면 지침을 우선하세요."` verbatim. It already says exactly GEN-51's line. It matches `memoryPrecedence`'s phrasing in the same half ("지침이 기억과 충돌하면 지침을 우선하세요."). 지침 still names one thing, the `[작문 지침]` section now in the system half above.
- Heading: keep `qualityRulesHeading = "[발행 글 측정 규칙]"`, Korean for every target. Every section heading and precedence line in this prompt frames rather than being output, and the existing comment already says so; keep that rule.
- Code, in `internal/generation/prompts.go`:
  - Replace `writeQualityRulesSection(out *strings.Builder, rules)` with `qualityRulesSection(rules []string) string`, shaped like `memorySection`: `""` for none, otherwise `qualityRulesHeading + "\n- " + each rule … + "\n" + qualityRulesPrecedence + "\n\n"`.
  - Delete the `writeQualityRulesSection(&stable, …)` call.
  - Build `perPost := qualityRulesSection(input.QualityRules) + fmt.Sprintf("[이번 글]\n가제: %s\n메모: %s\n%s%s%s", …)`.
  - Rewrite the comments on `qualityRulesHeading` ("the [작문 지침] section further down" becomes "above, in the stable half"), on the new function (GEN-14, GEN-51, MEM-20's reason: per-post material stays out of the cached prefix) and on `WritePromptInput.QualityRules` (from T359).
- Revise title instruction:
  - New constant beside `templateTitleInstruction`: `reviseTemplateTitleInstruction = "수정 요청이 제목을 바꾸라고 할 때만 JSON의 title을 바로 다음 --- 사이의 제목 형식에 맞춰 쓰고, 그 밖의 수정에서는 현재 제목을 그대로 두세요. 그 뒤 --- 사이의 내용은 본문의 형식입니다."`
  - It never says 지침 (TMPL-13). "수정 요청" is the prompt's own name for the request (`[수정 요청]`), and "현재 제목" points at `[현재 PostContent]`'s title.
  - Its tail sentence is shared with the write line, so the body fence keeps one meaning.
  - `writeTemplateSection(out *strings.Builder, brief *TemplateBrief, titleInstruction string)`: the write builder passes `templateTitleInstruction` and `BuildRevisePromptForLanguage` (`revise.go:124`) passes `reviseTemplateTitleInstruction`. Nothing else in the section differs. A brief with an empty `TitleArea` writes no instruction at all, so its revise bytes are today's.
  - Doc the constant with TMPL-51: a title the owner edited away from the form stays theirs until they ask, which is also what `RevisePrompt`'s "제목…고쳐 달라고 한 경우에만 바꾸세요" line says, so the two no longer conflict.
- `TestTickedQualityRulesOpenThePerPostHalf`:
  - Table: Korean target with a video, English full profile, English portable profile. Every row uses `testBrief()`, `testGuidelines()` and two `testQualityRules()`.
  - Build once with the rules and once without, and assert:
    - `system == systemWithout`;
    - `user == "[발행 글 측정 규칙]\n- "+r0+"\n- "+r1+"\n지침이 위 규칙과 충돌하면 지침을 우선하세요.\n\n"+userWithout`, written as a literal so a copy edit is deliberate;
    - `strings.Count(system, "[발행 글 측정 규칙]") == 0`.
  - Keep today's nil/empty golden check and the "closing line names 지침" check.
- `TestTheReviseTitleFormBindsOnlyATitleRequest`:
  - Pin the literal copy (`reviseTemplateTitleInstruction == "수정 요청이 제목을 바꾸라고 할 때만 …"`).
  - For `BuildRevisePrompt(…titleAreaBrief()…)` and `BuildRevisePromptForLanguage(LanguageEnglish, …titleAreaBrief()…)`, the system half contains `"\n"+reviseTemplateTitleInstruction+"\n---\n"+brief.TitleArea+"\n---\n---\n"+brief.Body` once and no `templateTitleInstruction`.
  - `BuildWritePrompt(…titleAreaBrief()…)` contains `templateTitleInstruction` and not the revise line.
- `TestTemplateTitleAreaPrecedesTheBodyFence`: replace `templateSection(revise) != templateSection(write)` with equality against `strings.Replace(templateSection(write), templateTitleInstruction, reviseTemplateTitleInstruction, 1)`, and reword its TMPL-51 comment.
- Unaffected, checked:
  - `write_rules_test.go` (revise carries no title prohibitions);
  - `cmd/api/bootstrap_test.go:520-529` (checks positions only);
  - `quality_freeze_test.go` drain checks, which read `System + user` together.
- `WriteExperimentPromptVersion` stays `write-v3-language-ab`. It has not moved for any prompt change since plan13, is only stored, and no rule compares it.
- Out of scope: the payload, the snapshot and the rule texts (quality context).

## result
