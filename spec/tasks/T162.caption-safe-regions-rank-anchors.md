# T162 Rank automatic anchors by the observed caption-safe regions
> st:doing@260915.cap | ssot:CDS-38 | base:CDS@14 CLIP@21 ARCH@2 | dep:T161

## goal
Automatic anchor selection prefers a candidate that lands inside an observed caption-safe region.

## acceptance
- [ ] `design.SelectAnchor` in `backend/internal/clip/design/compose.go` takes the cut's observed caption-safe regions and ranks a candidate lying inside one above a candidate lying outside every one
- [ ] the existing order is otherwise unchanged: safe-area drop, placed chip/badge/card drop with copy yielding to the badge, the ≤ 15 % subject-coverage preference, the readable-text TOP/BOTTOM restriction and the one-step limit all keep their current precedence
- [ ] with no regions observed the selection is byte-identical to today's, so stored plans and existing fixtures are unaffected
- [ ] `backend/internal/clip/media/composition_layout.go` passes the regions of the cut the element belongs to, matching source id and cut range the way `CutSubject` already does
- [ ] an explicitly authored template or owner position is still pinned and never re-ranked (CDS-64)
- [ ] test: a bottom-heavy subject with a top caption-safe region moves an automatic 가벼운 텍스트 caption to TOP
- [ ] test: identical inputs with no regions produce the anchor today's code produces (golden, over the existing layout fixtures)
- [ ] test: a pinned authored position ignores the regions entirely
- [ ] `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...`

## impl notes
- the four anchors and the per-style anchor lists are unchanged — the owner dropped free vertical placement, so this task only adds a ranking input
- V13 (anchor step ≤ 1) is unchanged and must keep passing; the region preference sits below it in precedence, not above
- T159 gives `simple` its TOP alternative; without that this task has nowhere to move a 가벼운 텍스트 caption, so land T159 first in practice even though the dependency is only on T161
