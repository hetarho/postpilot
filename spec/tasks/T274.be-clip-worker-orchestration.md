# T274 clip generation orchestration is a sequence of testable stages
> st:todo | ssot:ARCH-5 ARCH-6 ARCH-11 ARCH-24 | base:ARCH@5 | dep:T273

## goal
Move `GenerationService` and the worker orchestration (`generation.go`, `rerender.go`, `attempt*.go`, `recovery.go`) into `clip/app`, decompose `Run` into named stages, and finish the clock injection.

## acceptance
- [ ] `GenerationService` lives in `clip/app` and `Run` is ≤80 lines calling named stage functions (`plan`, `compose`, `render`, `verify`, `finish`) each ≤120 lines with its own unit test using fake ports
- [ ] the remaining bare `time.Now()` in `attempt.go` ×3, `generation.go:124`, `rerender.go:151` use the service clock
- [ ] `GenerationService`'s 55 methods are grouped: stage functions, attempt bookkeeping, recovery — into separate files; the exported surface used by `cmd/api` shrinks to construction + `Run` + recovery entry
- [ ] behaviour unchanged: ARCH-26 passes; the release smoke stage runs once locally (ARCH-37) — this is the generation path

## impl notes
- from review/arch-260919 F16 (worker half) F21 (remaining clocks)
- worker orchestration is application code (ARCH-11 runs it inside the API process), so `clip/app` is its ARCH-5 home; no new package name is introduced
- cut `Run` at the existing comment paragraphs; each stage takes and returns a small struct so the tests can start mid-sequence

## result
