# T277 experiment, usage and auth ports are per use-case
> st:todo | ssot:ARCH-6 ARCH-7 ARCH-24 | base:ARCH@5 | dep:T272

## goal
Split the table-shaped `Store` ports of `experiment` (24), `usage` (23) and `auth` (23) into behaviour-shaped interfaces per use-case group.

## acceptance
- [ ] no port interface in these three contexts exceeds 10 methods; usage's ledger/lot/anchor behaviours are separate interfaces the T270/T272 tx ports can reuse
- [ ] services and fakes consume only the narrow interfaces; behaviour unchanged: ARCH-26 passes

## impl notes
- from review/arch-260919 F17 (part 2)
- depends on T272 so usage's surface is the product-agnostic one before it is split

## result
