# T300 Blog and clip plan estimates
> st:todo | ssot:QUOTA-28 QUOTA-36 QUOTA-40 QUOTA-41 QUOTA-56 QUOTA-57 | base:QUOTA@17 | dep:-

## goal
- Separate per-item conditions from simultaneous model-level capacity comparisons for blogs and clips.

## acceptance
- [ ] The plans page starts with blog/clip basis selection and a condition summary; one bottom-right floating control opens the conditions sheet/dialog at every width.
- [ ] Blog conditions set characters/photos/videos; clip conditions set original count and finished length; each persists independently, safely bounded, without counts or model controls in the editor.
- [ ] Every plan displays four ordered model-level capacity rows; absent or incompatible rates are explicitly unavailable and condition changes recompute without RPCs.
- [ ] Server-published clip rates account for original analysis, flow and narration plus 1.5x token allowance, using assigned compatible model prices and a disclosed 60-second source assumption; actual reservations and settlement are untouched.
- [ ] Price/grant/bonus/subscription actions remain correct, and tests cover pricing, eligibility, transport, persistence and responsive comparison behavior.
- [ ] Applicable ARCH tests, lint, format, build, code generation and spec checks pass; browser reflow and floating-sheet behavior are verified.

## impl notes
- Add optional ClipEstimatorRates to EstimatorCombo in the proto; preserve existing wire fields and publish the source-seconds assumption on GetMyPlan even when no pair can be priced.
- Keep token assumptions in plan; derive clip rates for catalog pairs only when observer supports video and structured output and writer supports structured output, with no automatic model fallback.
- Clip assumptions: one 60-second original per observation call, 2000 prompt and 2000 observed output tokens; two writer calls with 10000/6000 base prompts, observed-source context carried to both, and 80 combined output tokens per finished second. All estimated tokens carry the existing 3/2 allowance; fixed per-call overhead does not.
- Persist the old blog storage shape unchanged for compatibility; use an independent clip storage key (default three originals and 30 seconds, bounds 1..20 and 15..90).
- The shared ladder accepts four result rows per offer; pages compose estimate functions from the plan entity. Reuse ActionBar and Sheet for a fixed right-aligned condition control above phone navigation.
- Only QUOTA is consumed; pending Wanted Sans deltas in CDS/CLIP/THEME belong to the existing separate work.

## result
- WIP checkpoint requested by the user on 2026-09-21. NOT FULLY VERIFIED; acceptance remains open and the task is returned to todo.
- Implemented the immersive plans comparison, blog/clip basis, independent persisted conditions, floating sheet trigger, four model-level rows, server clip rates and generated transport mappings. The checkpoint also includes the earlier $3/$10/$20 ladder, subscription bonuses and 1.5x estimate token allowance.
- Some focused checks were run, but complete verification was interrupted at the user's request. Do not treat this checkpoint as release-ready or as a completed billing rollout.
- Follow-up: investigate the 320px/200% text-size horizontal overflow observed during one browser run; the isolated diagnostic did not reproduce it. Complete integration, lint, generated-code and responsive checks when resumed.
- No further investigation or implementation of live billing price application is required for this checkpoint, per the user's instruction.
