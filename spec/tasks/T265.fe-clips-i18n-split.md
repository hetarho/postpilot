# T265 the clips namespace lives in the clip slices
> st:todo | ssot:ARCH-16 | base:ARCH@5 | dep:T261 T264

## goal
Move the 1,121/1,163-line `clips` resources out of `app/` into fragments owned by the clip entities, features, widget and pages settled by T260/T261.

## acceptance
- [ ] `app/providers/i18n/resources/{ko,en}/clips.ts` are deleted; every `clips` key lives in a `config/i18n.ts` fragment of the slice that renders it, and no fragment exceeds 250 lines per language
- [ ] `useTranslation('clips')` call sites (52) are unchanged; the namespace still exists and the parity/duplicate tests pass
- [ ] behaviour unchanged: all clip tests pass; ARCH-25 passes

## impl notes
- from review/arch-260919 F3 (clips half)
- reuse the ownership script from T264's result; the 34 top-level groups in `clips.ts` mostly map one-to-one onto the clip slices after T260/T261

## result
