# T150 Add per-source original-sound toggles
> st:todo | ssot:CLIP-17 CLIP-18 CLIP-20 CLIP-47 CLIP-55 CLIP-56 CLIP-100 CLIP-101 CDS-6 CDS-35 CDS-65 ARCH-13 ARCH-14 ARCH-15 ARCH-17 ARCH-20 ARCH-24 ARCH-25 | base:CLIP@18 CDS@12 ARCH@2 | dep:T143 T148

## goal
Give the owner one default-off `원본 소리 유지` toggle per video in steps ① and ②, with revision-safe persistence and preview feedback.

## acceptance
- [ ] Each current source card in editable steps ① and ② has a sibling semantic switch labelled `원본 소리 유지`, defaults off for new sources and independently selects neither the card nor playback when toggled.
- [ ] Reopened and canonically reselected sources display the persisted setting; latest-attempt/read-only observations, running work, finalized projects and foreign/unavailable sources cannot mutate it.
- [ ] A pre-plan switch calls the owner mutation with revision zero, and a post-plan switch joins the correction save queue with the current revision; optimistic state rolls back on failure while keeping a clear retry status.
- [ ] A real post-plan toggle makes the previous result visibly stale and offers credit-free rerender without changing observations, cuts, captions or model-reuse messaging; an idempotent toggle changes no revision.
- [ ] Step ② toggle changes participate in undo/redo by sending the inverse semantic mutation at a new optimistic revision, and concurrent plan autosave or revision conflict preserves both the local plan and intended sound state for explicit retry.
- [ ] Preview sound is the intersection of source permission, per-cut volume, transition gain and the preview master-audio control; volume zero, master mute and absent audio never rewrite the source setting.
- [ ] Source-strip, upload-session, retained-source, correction, page and i18n tests cover default/reopen/inherit, owner/read-only states, failure rollback, conflict, undo/redo, result staleness and independent accessible card/switch interaction.
- [ ] Browser checks cover keyboard and touch operation, 44 px targets, ko/en labels, light/dark themes and 320/360/390/430 px source strips without nested interactive controls or horizontal-page overflow.
- [ ] Run applicable ARCH-25, ARCH-28 and ARCH-29 checks with deterministic protobuf output and dirty-workspace snapshot protection.

## impl notes
- Restructure each source `<li>` into a selection button and sibling shared switch; never nest a switch inside the existing whole-tile button. Use `role=switch`, `aria-checked` and a filename-associated accessible name.
- Keep the setting in source/project entity state and call T143's dedicated mutation from both steps. Serialize it with debounced plan saves through one revision queue so two successful writes cannot claim the same expected revision.
- Treat the setting as server-owned. Template controls, model payloads and generated plan fields never expose an authoring path for it.
- Preserve the preview master-mute control as ephemeral browser state and keep per-cut volume in the edit plan; neither substitutes for the durable source toggle.

## result
