# T284 the agent generates only the protos it uses and drops the survey harness
> st:todo | ssot:ARCH-3 ARCH-27 | base:ARCH@5 | dep:T008 T281

## goal
Trim `agent/buf.gen.yaml` to `publishing.proto` and its imports and delete the retired `survey_test.go`.

## acceptance
- [ ] `agent/buf.gen.yaml` generates only the proto files the agent imports (publishing + its transitive imports); `agent/internal/gen` shrinks accordingly and `go vet ./...` passes without an `M` line per backend-only proto; a comment explains how to add a file
- [ ] `agent/internal/naver/survey_test.go` (2,146 lines, `//go:build survey`) is deleted; nothing references it
- [ ] ARCH-27 and ARCH-28 pass

## impl notes
- from review/arch-260919 F27 F29
- buf v2 supports per-template `inputs` with `paths`, so the agent template can list only the files it needs; ARCH-28's diff check covers the shrink
- depends on T281 so the file set is the final one

## result
