# T279 every hand-kept enum mirror is pinned to the generated enum
> st:todo | ssot:ARCH-3 ARCH-24 | base:ARCH@5 | dep:T008

## goal
Give each hand-kept copy of a proto enum (publish Stage / FailureKind in BE, agent and FE; `BlockType` ×5; `ModelPurpose` ×3; per-context `toProtoLanguage`) a test that walks the generated enum, and make the agent executor map from a typed `naver.FailureKind`.

## acceptance
- [ ] BE `publishing/types.go` + `publishing/rpc/mapping.go`: a test iterates `postpilotv1.PublishStage_name` / `PublishFailureKind_name` and asserts a round-trip through the from/to maps; same for `BlockType` (extend `generation/proto_contract_test.go` to every copy), `ModelPurpose` (3 copies), and one shared `toProtoLanguage` in `platform/rpcserver` replacing the per-context copies
- [ ] agent `publishing/executor.go`: `stageProto` and `failureKind` take `naver.FailureKind`/`Stage` typed values; the `default:` branch that degraded to `PUBLISH_FAILURE_SAFE` is replaced by an exhaustive switch checked by a test that walks the generated enum, so an unmapped kind fails the test rather than publishing as safe
- [ ] FE `entities/publish-job/model/types.ts` `STAGE_RANK`: a vitest walks the generated `PublishStage` enum and asserts every value has a rank
- [ ] behaviour unchanged: ARCH-25, ARCH-26, ARCH-27 pass

## impl notes
- from review/arch-260919 F25
- ARCH-3 r4: a fallback branch in an enum mirror is a test failure, not a value
- depends on T008 because the agent executor and the live Naver publish are T008's live-test surface; land after it closes

## result
