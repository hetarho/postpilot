# T370 Quality reads the post it needs and nothing more, and TopNoun is gone
> st:todo | ssot:ARCH-6 | base:ARCH@9 | dep:T357 T364

## goal
The quality read path reads one post row through a lean `post.Service.CurrentContent`, not the screen read `Get`, and computes M2's named run only for an over-band account. The dead `Repetition.TopNoun` and its `post_measurements.top_noun` column are removed.

## acceptance
- [ ] `post.Service.CurrentContent(ctx, userID, slug) (ContentSnapshot, error)` reads the post row alone. `TestCurrentContentReadsThePostRowAlone` (internal/post) sets up a post with a photo and a video attached, then swaps in collaborators that fail on any call: blobs whose `PresignGet` fails, a failing job finder, a failing experiment finder, and failing voice and template directories. It asserts:
  - `Get` fails, which proves the collaborators are live;
  - `CurrentContent` returns slug, revision, content, both languages and nouns equal to the stored row;
  - an unknown slug answers `ErrNotFound` and a foreign one `ErrForbidden`.
- [ ] `cmd/api/adapters_quality.go` `qualityPosts.Post` calls `CurrentContent`, never `Get`. `TestTheAggregateCountsPublishedPostsOnlyAndForgetsADeletedOne` (adapters_quality_test.go) still passes.
- [ ] namedRun: `Aggregate` (verdict.go:151-163) computes `CrossPost.Run` only when the M2 verdict is `VerdictOverBand`. `TestAWithinBandAccountNamesNoRun` (verdict_test.go) builds two accounts:
  - A: three or more published posts, where one pair shares a RunLength-어절 run but the median share is within 10%. Asserts `Verdict == VerdictWithinBand`, `Run == ""` and `Named(MetricCrossPostPhrases) == ""`. Today `Run` is non-empty here, so the case fails first.
  - B: the same run shared widely enough to go over band. Asserts `Run` names it.
- [ ] F16: `Repetition.TopNoun` no longer exists in `quality/metrics.go`. It is gone from `quality/store` (store.go:52, :72), from both statements in `store/queries/measurements.sql` and from the generated sqlc code. A new migration drops `post_measurements.top_noun`.
- [ ] `TestMigration<NNNN>DropsTheUnusedTopNoun` (new `internal/platform/db/migration_<NNNN>_test.go`, in the style of migration_0077_test.go) passes:
  - Up to the previous version, then seed a user, a voice, a post and a `post_measurements` row with every column set, `top_noun = '감자탕'` included.
  - Up: `columns0077(t, handle, "post_measurements")` equals 0077's list without `top_noun`; the row's other values are unchanged; `pragma_foreign_key_list` is unchanged; `PRAGMA foreign_key_check` is empty.
  - Down: `top_noun` is back as a nullable `TEXT` holding NULL and the row is otherwise intact.
- [ ] No `MeasureVersion` bump: T364's digest projection excludes TopNoun, and `TestMeasureSelfDigestIsPinnedToItsMeasureVersion` passes unchanged.
- [ ] Behavior unchanged: existing tests still pass, with only the TopNoun assertions removed. Those are metrics_test.go:205-206 and :213's `|| absentBody.TopNoun != ""`, verdict_test.go:196's `|| …TopNoun != "감자탕"`, and store_test.go, where :61 and :93 lose their `TopNoun:` fields and :108 loses the `SUM(top_noun IS NULL)` term, so its NULL count drops from 4 to 3.
- [ ] BE gate passes: `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test -timeout 30m ./...`. ARCH-28 passes too: `pnpm gen:sql && git diff --exit-code -- backend`.

## impl notes
- from review/published-quality-260924 F8 F16
- The lean read follows `PostStatus` (service.go:1084-1090): `ownedPost` and nothing else. One `GetPost` query, with no listing, presign, answers, job, experiment, voice or template read.
- New type in post/types.go, beside `PublishedPost` (:331):
  ```go
  // ContentSnapshot is one owned post's current content with what a reader that measures it needs
  // beside it, read from the post row alone.
  type ContentSnapshot struct {
      Slug            string
      ContentRevision int64
      Content         *PostContent // nil before the first write
      ContentLanguage *Language
      TargetLanguage  Language
      Nouns           []string
  }
  ```
- `CurrentContent` maps `found.Slug`, `found.ContentRevision`, `found.Content`, `found.ContentLanguage`, `found.TargetLanguage` and `found.ContentNouns`.
- Deliberately excluded: status ← quality reads none; published-ness reaches it through `Published` alone (quality/ports.go:22-24).
- Named for what it returns, not for its consumer ← the adapter's own rule is "Post never learns quality exists" (adapters_quality.go:15-17), and `PostStatus` / `PublishedPosts` are named the same way.
- Adapter (adapters_quality.go:20-46): `found, err := a.service.CurrentContent(ctx, userID, slug)`. The error switch stays (ErrNotFound/ErrForbidden → `quality.ErrPostNotFound`), and so do the mapping calls, reading the new field names: `found.ContentRevision`, `found.Nouns`, `found.Content`, `found.ContentLanguage`, `found.TargetLanguage`.
- T357 classification: `CurrentContent` is a new exported `*post.Service` method. If T357 has landed, add `"CurrentContent": "read"` to `publishedLockExempt` in lifecycle_test.go, or its reflection test fails. If T357 lands later, it picks the method up.
- Test fakes for `TestCurrentContentReadsThePostRowAlone` (package post, so fields are settable after `newTestService`):
  - `svc.jobs = failingJobs{}`, a new type whose `ActiveForPost` returns an error.
  - `svc.experiments = failingExperiments{}` (`PendingForPost` errors).
  - `svc.voices = failingVoices{}` (`Voices` errors).
  - `svc.templates = failingTemplates{}` (`Templates` errors).
  - `fakeBlobs` gains `failPresign bool`, checked in `PresignGet`.
  - Attach through the ordinary helpers (`attachPhoto`, `mustAttachVideo`) before swapping.
- namedRun decision: gate on the verdict, and do not reuse the median's overlaps:
  - verdict.go:161 `if median != nil {` becomes `if account.CrossPost.Verdict == VerdictOverBand {`.
  - `AccountCrossPost.Run`'s doc says "set only when OVER_BAND: the rule text is its one reader (service.go:98-103, :131-135)".
  - Why not reuse: the review measured 70 of 168 ms on a within-band account, and that is the case removed. Reusing the overlaps would change `namedRun`'s signature, which metrics_test.go:158-195 calls directly, for a second-order gain on the rarer over-band path.
  - `Run` is on no wire (quality/rpc/handler.go never reads it), so nothing outside `Named` observes the change.
- F16 in metrics.go:
  - Delete the `TopNoun` field (:251) and its doc mention (:245-247).
  - `MeasureRepetition` keeps computing `topCount` for `Share`. The max count is independent of `preferred`'s tie-break, so a plain max is fine, or keep the loop and discard the noun.
  - `preferred` stays; `MeasureTitleSaturation` uses it.
- Store and SQL:
  - Remove `top_noun` from `GetPostMeasurement`'s SELECT and from `UpsertPostMeasurement`'s column list, VALUES (12 → 11 placeholders) and DO UPDATE SET. Keep the file ASCII.
  - Run `pnpm gen:sql`. `GetPostMeasurementRow` and `UpsertPostMeasurementParams` lose `TopNoun`.
  - Drop the two `TopNoun` lines in quality/store/store.go.
- Migration:
  - File: `internal/platform/db/migrations/<NNNN>_drop_measurement_top_noun.sql`. NNNN is max existing + 1 at implementation time: 0079 at HEAD 58e1b5ca, but a peer session (ARCH r10 media-worker work) may add migrations first. Never edit 0077 ← an edited goose migration never re-runs on a DB that already applied it.
  - `-- +goose Up`: a comment (dead since T335: nothing reads it and QUAL-43 decided M3 names no noun), then `ALTER TABLE post_measurements DROP COLUMN top_noun;`. SQLite allows it because no index, key, CHECK or view names the column (0077:49). Up blocks already use DROP COLUMN: 0019, 0020, 0021.
  - `-- +goose Down`: `ALTER TABLE post_measurements ADD COLUMN top_noun TEXT;`. Write bare `TEXT`, never `TEXT NULL`, which sqlc reads as untyped. The Down re-adds the column because a rolled-back binary's upsert names it; there is no value to restore, and NULL reads as "" in the old code.
  - "Forward-only" here means a new migration, not an edit of 0077.
- migration_0077_test.go:297 keeps `top_noun`: that test stops at `UpTo(77)`.
- FE: `topNounShare` (entities/quality i18n, PostMeasurementRow.tsx:102) is the share's label, not this field. Nothing on the FE or in the proto changes.
- Out of scope, do not touch:
  - `quality.PostSource` / `PostSnapshot` (their shape already fits)
  - `PublishedPosts`
  - M1's `Noun`, which is live: the M1 rule text names it (QUAL-14)

## result
