# T276 voice, post and publishing ports are per use-case
> st:todo | ssot:ARCH-6 ARCH-7 ARCH-24 | base:ARCH@5 | dep:-

## goal
Split the table-shaped `Store` ports of `voice` (`PersonalizationStore` 37, `Store` 22), `post` (31) and `publishing` (29) into behaviour-shaped interfaces per use-case group, consumed narrowly.

## acceptance
- [ ] no port interface in these three contexts exceeds 10 methods; each is named for a behaviour (`post.DraftWriter`, `post.ImageCatalog`, `post.UploadLedger`, `voice.ProfileReader`, `voice.LearningRunStore`, `publishing.JobLease`, …) following the comment groupings already in `post/ports.go`
- [ ] the `*store.Store` types still implement every method; services hold only the narrow interfaces they call, and each existing fake in tests implements only the interfaces the tested use-case needs (fake line count drops)
- [ ] behaviour unchanged: ARCH-26 passes

## impl notes
- from review/arch-260919 F17 (part 1)
- ARCH-6 r4: a port is named for the behaviour one use-case group needs; ARCH-7 already banned the generic repository shape
- mechanical recipe: group by the callers in `service.go`; define the interface where it is consumed; pass `store` into the constructor as before — Go's structural typing makes the split compile without adapter code

## result
