# T293 ①'s 기억 사용 checkbox and ③'s candidate sheet
> st:todo | ssot:POST-71 POST-72 MEM-14 MEM-15 ARCH-13 ARCH-24 | base:POST@8 MEM@1 | dep:T290 T291

## goal
Let a draft opt into its memories in ① and turn a finished post into new ones in ③.

## acceptance
- [ ] `기억 사용` is a labelled checkbox at the foot of ①'s panel, under the memo and the template data fields, default off, autosaving through the draft's option-save path
- [ ] it appears nowhere in the writing brief, and the brief's other options are unchanged
- [ ] `기억으로 저장` sits in ③ beside 말투 학습, enabled whenever canonical content exists, disabled with its reason stated in place otherwise
- [ ] pressing it starts the extraction job, polls it, and opens the candidates in the shared `Sheet` — a bottom sheet below `sm:` — as a checkbox list
- [ ] 저장 creates only the checked candidates; a per-row refusal (duplicate, cap, bound) stays in the sheet with its catalogue reason while the rest are kept
- [ ] a start refusal, a job failure or a closed sheet leaves the post exactly as it was and stores nothing
- [ ] vitest covers the checkbox's autosave, every disabled reason, the partial-save path and the discard-on-close
- [ ] ARCH-25 passes

## impl notes
- the checkbox is on ①'s panel rather than in the brief because it silently changes what a run may write, which is POST-51's own reason for keeping 말투 and 템플릿 readable without opening anything
- the sheet's save loop is the bulk-approval shape 지침's 전부 수락 already uses: one create per checked row, each refusal left in place with its reason rather than failing the batch
- unchecked candidates are discarded when the sheet closes — do not add a "later" affordance; MEM-15 is explicit that a declined candidate does not queue
- the extraction is repeatable, so the empty-result case says so and offers the button again rather than hiding it

## result
