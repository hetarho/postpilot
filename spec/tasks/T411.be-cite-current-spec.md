# T411 Backend, proto and build docs cite current spec decisions instead of deleted docs and IDs
> st:todo | ssot:ARCH-35 ARCH-43 | base:ARCH@11 | dep:T410

## goal
No backend source, test, test fixture, sqlc query, proto comment or build doc points at `spec/legacy/`, a short-form legacy path (`policy/*.md`, `tech/*.md`), a legacy job, plan or change number, or a decision ID that no longer exists. Each citation names the current SSOT decision instead, or is dropped when none covers it. Behaviour is unchanged.

## acceptance
- [ ] `rg -n "spec/legacy|(policy|tech)/[a-z-]+\.md" backend proto --glob '!**/migrations/**' --glob '!**/internal/gen/**'` is empty
- [ ] `rg -nw "PUB-[0-9]+|CDS-(10|11|16|23|24|26|28|29|30|39|40|50|54|58|69)|CLIP-(62|77|103)|VIDEO-1[67]|MODEL-29|TMPL-5|ARCH-12|AUTH-(21|46)|POST-42" backend proto --glob '!**/migrations/**' --glob '!**/internal/gen/**'` is empty. Also cover the ranges `CDS-22..26` / `CDS-23..26` and the test name `TestPresetsMatchCDS50`, renamed to cite no retired ID.
- [ ] legacy job, plan or change references ("Job 05 A6", "plan 01 AC9", "changes 20/24", "legacy change 10") are gone from backend comments. The rule text they annotate stays, citing the current decision where one is obvious.
- [ ] applied migrations are untouched (ARCH-43): `git diff --stat -- backend/internal/platform/db/migrations` is empty
- [ ] behaviour unchanged: verify BE (ARCH-26) passes with no test logic edited beyond the renamed test and comment text; `pnpm gen:proto && git diff --exit-code -- backend/internal/gen frontend/src/shared/api/gen` and `pnpm gen:sql && git diff --exit-code -- backend` are clean after regenerating
- [ ] `backend/build/RENDER.md` describes the current regions: the hook/ending-card passages become the intro/outro regions (CDS-70), and it cites no retired ID

## impl notes
- These citations come from the docs-current cleanup of 260926. `spec/legacy/`, `spec/ssot/PUB.md` and the retired decisions were deleted, so the code now points at nothing. Git log still holds those docs if a rule's origin is needed.
- Legacy path → current decision:
  - `post/service.go:945`, `post/store/queries/posts.sql:65` (then `pnpm gen:sql`) → POST-17.
  - template grammar citations → TMPL-17..TMPL-20; §2 constructs are TMPL-18, §4 one-fixture is TMPL-20, §5 copy rule is TMPL-21, and `{{slot:n}}` is TMPL-22. They sit in `template/grammar.go:10`, `grammar_test.go:11`, `expand.go:11`, `types.go:10`, `testdata/grammar/cases.json:15`, `generation/prompts.go:169`, and the string in `template/corpus_test.go:74`. After that string, regenerate `testdata/grammar/corpus.json` with `TEMPLATE_CORPUS_REGEN=1`.
  - "analyze sends no reasoning effort" (`voice/service_test.go:718`, `generation/service.go:45`) → MODEL-9 (+GEN-22).
  - Proto: `post.proto:117` → TMPL-18; `template.proto:30` → TMPL-2 (+TMPL-17..20). Then `pnpm gen:proto`.
- Retired CDS/CLIP IDs → what now states the rule (drop the citation where the entry says drop):
  - CDS-10 / CDS-11 → CDS-78
  - CDS-16 → CDS-44, or CDS-3 for the 4.5:1 floor
  - CDS-23, CDS-24, CDS-26, CDS-58 → CDS-25, the one surviving style (CDS-80 for the approved set)
  - CDS-28 / CDS-29 → the intro/outro regions (CDS-22, CDS-70), or drop
  - CDS-30 → drop, or CDS-31 where names and prices are meant (CLIP-137 for narration-stated facts)
  - CDS-39 → drop
  - CDS-40 → drop (CDS-66 / CLIP-142: narration names each caption's style)
  - CDS-50 → drop (CLIP-14, CLIP-68: a template carries no hidden preset; CDS-37: no preset imposes a range)
  - CLIP-62 → CLIP-123
  - CLIP-103 → CLIP-136, or CLIP-140 for the legacy section grammar
  - PUB-13 in `migration_0037_test.go:49` → drop the ID and keep the test, which pins historical migration 0037.
- Files, mostly `backend/internal/clip/**`:
  - `clip/render.go`, `render_test.go`, `compose.go`, `compose_test.go`, `analysis.go`, `correction.go`, `cut_creation.go`, `evidence_test.go`, `app/service.go:167`, `store/store_test.go:269`
  - `composition/resolve.go`, `composition/corpus_test.go`
  - `ai/composition_prompt.go`, `ai/parse.go`, `ai/prompts.go`, `ai/multi_source_test.go`, `ai/service_test.go`
  - `design/compose.go`, `design/contrast.go`, `design/contrast_test.go`, `design/design.go`, `design/design_test.go`, `design/presets_test.go`, `design/verify.go`, `design/verify_test.go`
  - `media/copy.go`, `media/furniture_test.go`, `media/luminance_test.go`, `media/render.go`, `media/render_smoke_test.go`, `media/render_test.go`
  - `cmd/api/clip_release_fixtures_test.go:482`, `cmd/api/clip_release_test.go:514`
  - proto `clip.proto:404`: drop the CDS-28 sentence, then regen.
- Find everything: re-run the two acceptance `rg` commands, plus `rg -n "design-language|[Jj]ob [0-9]{2}|plan [0-9]{2}|changes? [0-9]{2}" backend proto`, and judge each hit. The loose job/plan pattern has false positives.
- Some clip code under these comments still implements what the retired decisions described: chips, category presets, `app/service.go:167` requiring one of five presets, `design/compose.go:192`'s preset cut range. This task changes comments only. Record each such spot in `## result` as a candidate for review-code or update-ssot; don't change behaviour here.
- Parallel clip work (region presets, T406 by another session) edits the same clip files. Rebase onto the latest tree, and keep each edit to the comment line.
- Leave `docs/design/*` and `docs/audits/*` alone: they are dated design and audit records.

## result
