# T263 the draft editor page composes hooks it does not own
> st:todo | ssot:ARCH-14 ARCH-15 ARCH-24 | base:ARCH@5 | dep:-

## goal
Move the step state machine, the server-mirrored brief state and the caret handoff out of `pages/editor/ui/DraftEditor.tsx` into model hooks and features, and break the five inline helper components into files.

## acceptance
- [ ] `DraftEditor.tsx` is ≤300 lines with ≤4 `useState`; the step machine lives in `pages/editor/model/useDraftSteps.ts` as a pure reducer + hook with a `.test.ts` covering every transition
- [ ] target-length / tag-count mirroring (`:215-229`) lives in `features/generate-post/model/useBriefMirror.ts` (or `entities/post` if it is pure) with a unit test for the sync-on-server-change rule
- [ ] the caret handoff (`:201`) lives in `features/edit-post-content/model`, tested
- [ ] the five inline components (`:445-917`) are separate files under the owning feature `ui/` or `pages/editor/ui/`, each ≤200 lines
- [ ] the `react-hooks/exhaustive-deps` warning at `DraftEditor.tsx:140` is resolved (the `storedAnswers` memo), so `pnpm lint` reports 0 warnings
- [ ] behaviour unchanged: `DraftEditor.test.tsx` and the editor feature tests pass
- [ ] ARCH-25 passes

## impl notes
- from review/arch-260919 F2 F10
- `pages/editor/model/` already exists (156 lines) and `useEditorJob.ts` sits there; T267 later moves its transport use into `entities/generation-job`, so leave `useEditorJob` alone here
- a state machine is `model` (ARCH-15); a component that renders a brief step is a feature `ui` if it owns a verb (fill-template-answers, select-post-voice) or page `ui` if it only lays out
- resolve the memo warning by moving `storedAnswers` inside the `useMemo` callback or its own memo — do not disable the rule

## result
