# T063 The per-post tag count option, frozen beside the target length
> st:todo | ssot:POST-63 GEN-46 GEN-5 GEN-14 GEN-27 GEN-30 GEN-40 LANG-18 MODEL-30 | base:POST@4 GEN@5 LANG@2 MODEL@6 | dep:-

## goal
A post carries `tag_count` (1–10, default 4) edited in the 글쓰기 옵션 brief beside 목표 글자 수; generation, revision and the write A/B snapshot freeze it and the prompts ask for exactly that many tags, trimming a surplus instead of failing.

## acceptance
- [ ] migration `0034_post_tag_count.sql` adds nullable `posts.tag_count`; `GetPost` / `GetLearningSnapshot` / list rows read NULL as 4 without a backfill, and a store test proves a legacy row with NULL comes back as `TagCount == 4`
- [ ] `SavePostGenerationOptions` accepts `optional int32 tag_count`: absent preserves the stored value, 1–10 writes it, anything else is `InvalidArgument` `POST_TAG_COUNT_INVALID` and writes nothing (service + rpc tests); a save that changes only the tag count advances no revision and touches no status
- [ ] `Post.tag_count` is always present on the wire and the FE `Post` model holds `tagCount: number` (mapper test: absent → `POST_TAG_COUNT_DEFAULT`)
- [ ] `StartGeneration` reads the owned post's `tag_count` server-side and freezes it in the generate payload; `DecodeGenerationPayload` on a payload without the member yields 4 (payload round-trip test both ways); the generate handler writes the frozen value, not the live post's, into the prompt
- [ ] `StartRevision` freezes it the same way in the revision payload (legacy payload → 4), and the revise handler builds its prompt from the payload value
- [ ] the write experiment snapshot carries `post.tag_count`; a snapshot taken with 4 and one with 7 hash differently, and a stored snapshot without the member decodes as 4 (`experiment_surface` test)
- [ ] the Korean write prompt says `정확히 N개의 tags` and the English one `exactly N tags`, `TagsMin` / `TagsMax` and the schema's `minItems: 3, maxItems: 6` are gone, the four goldens are updated, and a prompt test asserts N = 7 appears verbatim
- [ ] the revise prompt's unchanged-tags rule ends with the bound: `태그를 바꾸라는 요청이면 정확히 N개로 유지하세요.` / `A requested tag change keeps exactly N tags.` (golden + test)
- [ ] `ParseContent(raw, tagCount)` returns the first `tagCount` tags of an over-long list in model order, returns a shorter list as written, and still refuses a missing `tags` field (parse tests: 8→4, 2→2, 0→0, absent → `MODEL_OUTPUT_INVALID`)
- [ ] the brief's 분량 section shows the 목표 글자 수 control unchanged and, under it, an always-visible 태그 개수 number field prefilled from the post (4 on a post never touched); the range message reads `1–10개로 입력해 주세요.`; 저장 sends both values in one call; the field greys with the length while a job runs (`GenerationOptions.test.tsx`, `GenerationBrief.test.tsx` updated)
- [ ] `POST_TAG_COUNT_INVALID` has ko/en strings in `errors.ts` and an entry in `app-failure.ts`
- [ ] ARCH-25 · ARCH-26 · ARCH-28 pass; `pnpm gen:proto` and `pnpm gen:sql` outputs are committed

## impl notes
- constants live once, in `internal/platform/config` as typed constants `PostTagCountDefault = 4` · `PostTagCountMin = 1` · `PostTagCountMax = 10`, imported by `internal/post` (validation, NULL mapping) and `internal/generation` (legacy payload and snapshot decode); FE mirrors them in `shared/config` as `POST_TAG_COUNT_DEFAULT` · `POST_TAG_COUNT_MIN` · `POST_TAG_COUNT_MAX` (POST-63 names the FE keys) ← `internal/generation` may not import `internal/post`, and one literal in two contexts drifts
- schema: `ALTER TABLE posts ADD COLUMN tag_count INTEGER;` — plain add, nullable, no default in SQL; the row→domain mapper in `post/store/store.go` (`optionalInt` sites at ~306 and ~762) resolves NULL to the default so the domain type is a concrete `int` (`Post.TagCount`, `LearningSnapshot` needs nothing) ← POST-63: NULL reads as 4, existing posts need no backfill; a `NOT NULL DEFAULT 4` would say the same thing but would rebuild nothing and gain nothing, and a nullable column keeps "never set" visible to a later query
- proto: `Post` gains `optional int32 tag_count = 23` (current max is 22; `optional` only because proto3 has no other way to say "always present, distinguishable from 0" — the server always fills it); `SavePostGenerationOptionsRequest` gains `optional int32 tag_count = 3` with the presence semantics above; `StartGenerationRequest`, `StartRevisionRequest`, `StartWriteExperimentRequest` and `StartVoiceRuleComparisonRequest` gain nothing — the server reads the post (GEN-46: "from the post at start") ← unlike `target_length`, which the client still passes explicitly on start, there is no per-run override to carry
- `post.Service.SaveGenerationOptions(ctx, userID, slug, targetLength *int, tagCount *int)`: `tagCount == nil` keeps `found.TagCount`; the sqlc `SavePostGenerationOptions` statement sets both columns in one UPDATE (`tag_count = ?`), so "preserve" writes the current value back; range failure is `&InvalidContentError{Reason: "tag count must be between 1 and 10"}` mapped in `post/rpc` to `POST_TAG_COUNT_INVALID` the way `GENERATION_TARGET_LENGTH_INVALID` is mapped in `generation/rpc`
- generation: `PostInput.TagCount int` (`json:"tag_count,omitempty"` so a legacy write snapshot decodes 0) filled by the `cmd/api` adapter at `main.go:~1033` from `found.TagCount`; `StartRequest.TagCount` and `GenerateJob.TagCount` set in `Service.StartGeneration` from the loaded post beside `request.TargetLanguage`; `generationPayload.TagCount int json:"tag_count,omitempty"` and `GenerationOptions.TagCount`; the generate handler does `post.TagCount = job.TagCount` next to the `TargetLength` line and every decode path normalises 0 → `config.PostTagCountDefault` in ONE helper `resolveTagCount(n int) int` used by payload decode, snapshot decode and the handlers ← three decode sites, one rule
- revision: `StartRevisionRequest.TagCount` set beside `request.TargetLength = …` at `service.go:157`; `revisionPayloadJSON.TagCount int json:"tag_count,omitempty"`; `encodeRevisionPayloadForLanguage` gains the parameter; the handler passes `resolveTagCount(payload.TagCount)` — NOT `post.TagCount` from the live load — to the prompt builder ← GEN-46 freezes it; the live post is only consulted for content and images as today
- write experiment: nothing beyond `PostInput.TagCount` — `SnapshotWriteInput` serialises the whole `PostInput`, so the count enters the snapshot and its SHA-256 for free (MODEL-30, LANG-18); `WriteExperimentPromptVersion` stays `write-v3-language-ab` ← the prompt text changed for every post, but the version string identifies the contract, not the bytes, and the hash already separates the inputs
- prompts: `BuildWritePromptForLanguage(...)` and `BuildRevisePromptForLanguage(...)` gain `tagCount int` right after `targetLength`; the legacy `BuildWritePrompt` / `BuildRevisePrompt` wrappers pass `config.PostTagCountDefault`; the Korean write line becomes `title, 한 줄 summary, 정확히 %d개의 tags, blocks를 반환하세요.` and the English `Return title, a one-line summary, exactly %d tags, and blocks.`; the revise prompts' tag sentence gets the bound appended in both languages; the sentence stays in the stable (system) part where it is today ← moving it into the per-post user part would reorder the golden for no reason; goldens are hand-maintained files under `testdata/` compared byte-for-byte (`template_test.go:21`), edit them by hand
- `schemas/post_content.schema.json`: `"tags": {"type": "array", "items": {"type": "string"}}` with no item bounds ← a per-post bound would need a schema per request and the parser trims anyway; delete `TagsMin` / `TagsMax` from `config.go` (GEN r5 removed them from the constraints), leaving `BadOutputErrorHeadChars` and `RevisionInstructionMaxChars`
- `ParseContent(raw string, tagCount int)` — both callers (`write.go:39`, `revise_handler.go:62`) pass the frozen count; the count gate `len(wire.Tags) < TagsMin || > TagsMax → badOutput` is replaced by `if len(wire.Tags) > tagCount { wire.Tags = wire.Tags[:tagCount] }` before the `PostContent` is built; a missing `tags` key stays `MODEL_OUTPUT_INVALID` through `hasFields` ← GEN-46: surplus trimmed, shortfall accepted, and trimming inside the parser is the one place both handlers share
- FE: `entities/post/model/types.ts` `tagCount: number`; `post-queries.ts` maps `post.tagCount ?? POST_TAG_COUNT_DEFAULT`; `useGenerationOptions.save(slug, { targetLength?: number; tagCount: number })`; `GenerationOptions` keeps the tick + length block and adds beneath it a `FieldLabel` + `TextField type="number" min max` for the count with its own `FieldMessage`, both validated before the single 저장; the brief prop `targetLength` is renamed `options: { slug; targetLength?: number; tagCount: number; disabled; onSaved(next: { targetLength?: number; tagCount: number }) }` and `DraftEditor` mirrors `tagCount` from the post exactly as it mirrors `targetLength` (~lines 214–219, 248–256); the remount key becomes `${slug}-${targetLength ?? 'natural'}-${tagCount}`; `AIModelsPage` is untouched ← the write experiment reads the post server-side
- i18n (ko/en `posts.ts` under `generation.options`): `tagCount: '태그 개수'` / `'Tag count'` · `tagCountRange: '{{min}}–{{max}}개로 입력해 주세요.'` / `'Enter {{min}}–{{max}}.'`; the section header `generation.brief.length` (목표 분량) stays — a section named for length now also holding the tag field is acceptable; rename only if the owner asks; `errors.ts`: `POST_TAG_COUNT_INVALID: '태그 개수를 확인해 주세요.'` / `'Check the tag count.'`
- VOICE-42✎ (rule comparison freezes the tag count) has no code effect and is not part of this task: `BuildRuleComparisonPrompts` asks for prose and lists the source's tags as topic, it never asks the model to emit tags, and its candidates are plain strings — see STATE next for the update-ssot owed
- verify: ARCH-25, ARCH-26, ARCH-28 (`pnpm gen:proto`, `pnpm gen:sql`, both diffs clean)

## result
