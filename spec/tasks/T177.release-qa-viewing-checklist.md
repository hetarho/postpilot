# T177 Release QA viewing checklist
> st:blocked@260916 | ssot:CDS-53 CDS-70 CLIP-27 ARCH-24 | base:CDS@18 CLIP@24 ARCH@2 | dep:T176

## goal
Run CDS-53's owner review over rendered clips and record what it finds.

## acceptance
- [v] three clips are rendered, one per intro/outro preset pair currently selectable, each carrying a disclosure badge, an information pair and at least one caption
- [ ] each clip is reviewed muted and again with its configured source audio, and the CDS-53 questions are answered per clip: two facts recalled from the muted pass, subject known within 1.5 s, copy reads as editor-placed, tone constant, disclosure legible
- [v] the same clips are checked for the assembly cases CDS-53 names — a same-source split, every rate preset, a transition overlap, a transformed caption boundary, an absent optional field — with no unintended audio, temporal gap, stale caption or invented text
- [v] findings are written to `docs/design/clip-release-qa-YYMMDD.md` with the date, the clip sources and one line per question, and anything failing becomes a review finding or an update-ssot request rather than a silent fix here
- [v] no automated gate beyond ARCH-26 on any constant this task changes; the review is manual by nature and the document is its evidence

## impl notes
- this replaces the viewing half of the retired T110; the platform overlay measurement it carried is gone with CDS-10 and CDS-11
- render through a clip project's step ③, or the Docker media-smoke target with `CLIP_SMOKE_EXPORT` set to keep its own renders; the smoke needs Docker, which does not currently start on the owner's machine, so the project path is the reliable one
- the owner watches; an agent session can prepare the renders, list the questions and write the document from the owner's answers
- CDS-53 is a standing quality bar, so this task is repeatable — number a later run separately rather than reopening this one

## result
- blocked: acceptance 2 and 3 need the owner's own viewing answers, and this sitting produced only part of them. Nothing was answered on the owner's behalf.
- rendered: three clips, one per selectable intro/outro pairing (A/B, B/E, A/E), each 1080x1920 30 fps 15.8 s with a disclosure badge, an information pair and two captions. `TestReleaseQAViewingClips` in the Docker `media-smoke` target renders them from synthetic 60 fps footage under `CLIP_RELEASE_QA=1`; it is skipped everywhere else, so ARCH-26 gains no gate. Files land where `CLIP_SMOKE_EXPORT` points (this run: `tmp/release-qa/clips/`, git-ignored). Docker does start on this machine, so the smoke path the notes call unreliable is the one that worked.
- the three carry every assembly case CDS-53 names: one source split across cuts 1 and 2, all six CLIP-98 rates (1/1/0.5, 0.75/1.25/1, 1.5/2/1), a 200 ms fade into cut 2, captions crossing the 5.0 s and 9.8 s boundaries where both neighbouring cuts are rate-transformed, and an information element with no value row. A 60 fps original is what makes the slow rates offerable at all (CLIP-99) — 30 fps footage refuses 0.5x and 0.75x.
- answered (docs/design/clip-release-qa-260916.md): all three clips carry yes on Q3 copy reads as editor-placed, Q4 tone constant and Q5 disclosure legible, and the owner reported no unintended sound, temporal gap, stale caption or invented text at the cut boundaries, the rate changes or the outro spacing. Q1 is answered only by the authored information pair ("위치 성수동") and Q2 not at all.
- finding 1: synthetic flat-colour footage cannot answer CDS-53's recall and subject questions at all. The copy, placement, timing and disclosure half of the review it serves fine. A later run over real recorded material should answer questions 1 and 2, numbered separately as the notes allow.
- base freshness: while this ran, parallel sessions took CDS to r19 (chip corner radius) and CLIP to r25 (declared character maxima). Neither changes what the viewing questions ask, but r25 governs how long a slot's text may be, so whoever resumes should re-read both deltas and refresh the base line — the rendered clips' copy lengths predate that rule.
- what keeps this blocked: acceptance 2 asks for all five questions per clip, and Q1 and Q2 cannot be answered by any render this task can produce. That is a task-definition problem, not a review that went badly — the decision to make is whether the recall half becomes its own numbered run over real recorded footage (create-task), which would let this run close on the half it did establish.
