# T158 Answer the release smoke with a v2 observation
> st:done@260914 | ssot:- | base:ARCH@2 | dep:-

## goal
Bring the production image's synthetic observation answer up to the v2 contract so the mandatory gate stops refusing every build.

## acceptance
- [v] the smoke's synthetic observation carries `action`, `motion`, `certainty` and `usability` alongside the fields it already sends, and satisfies the v2 coverage rule
- [v] `TestClipWriterInputRelease` reaches its 21 provider calls and its expected terminal status instead of failing at the first chunk
- [v] behaviour otherwise unchanged — the existing smoke assertions and the whole clip suite still pass
- [v] the production image target builds, which is the only place this gate runs
- [v] verify BE `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...`

## impl notes
- from review/clip-release-smoke-260914 F1
- The answer is built at [clip_release_fixtures_test.go:477](backend/cmd/api/clip_release_fixtures_test.go#L477). Add the four fields the closed schema now lists as required ([chunk.schema.json](backend/internal/clip/ai/schemas/chunk.schema.json)) and that [parse.go:245](backend/internal/clip/ai/parse.go#L245) refuses a nil for: `action` and `motion` are free strings up to 2000 chars, `certainty` is one of `certain | uncertain | unknown`, `usability` is one of `usable | unusable`.
- Use `certain` and `usable`: the synthetic footage is a legible moving test pattern, and an `unknown`/`unusable` answer would authorize no item fact, which would change what the rest of the smoke exercises.
- Coverage needs nothing: the fixture already emits one segment spanning `0..chunk_duration_ms`, which is exactly what "first starts at 0, last ends at chunk_duration_ms, no gap" asks for.
- Do not touch the schema, the prompt or the parser. The contract is correct; only the test double lagged behind it.
- Verifying this locally is not possible today — the gate is `RUN` inside the image (`Dockerfile:66`) and `CLIP_RELEASE_SMOKE=1` is set nowhere else, while the harness wants the bundled faces at their absolute runtime paths and `prepare` wants ffmpeg. Build the `clip-input-smoke` image target if Docker is available; otherwise say plainly in `## result` that the gate was verified by reading the contract against the fixture, and let CI be the proof. That gap is review/clip-release-smoke-260914 F2 and is deliberately not part of this task.

## result
- observation: the synthetic segment now states `action`, `motion`, `certainty` and `usability` beside the fields it already sent, with `certain`/`usable` because the footage is a legible moving test pattern. Coverage needed no change — the one segment already spans `0..chunk_duration_ms`. The `detailed-input` long-`event` override still applies to the same segment and stays inside the 2000-character ceiling.
- beyond the stated scope, same root cause: the writing fixtures had also lagged behind CLIP-98. `plan.schema.json` lists `rate_permille` as required for every cut and `parse.go` refuses a nil one, so the three-cut and eight-cut plan answers would have failed the same way the observation did. Both now state `1000`, which is the rate this synthetic footage is recorded at, and the composition path inherits it instead of setting it separately.
- the schema, the prompt and both parsers are untouched: the contract was right and only the test double was behind it.
- verify: ARCH-26 passes (`gofmt`, `go vet ./...`, `go build ./...`, `go test ./...`). ARCH-25/27/28 need no rerun — nothing outside `backend/cmd/api` changed, and no proto or SQL was touched.
- NOT executed here: the image gate itself. It is a `RUN` inside `Dockerfile:66` and `CLIP_RELEASE_SMOKE=1` is set nowhere else, and the Docker Desktop daemon on this machine will not come up. The fixture was instead checked field by field against `chunk.schema.json`, `plan.schema.json`, `composition-plan.schema.json` and the refusals in `parse.go` and `ValidateSegments`. CI's production-image build is the proof. That gap is review/clip-release-smoke-260914 F2 and remains open.
