# T141 Recover clip generation with shared validation and bounded response correction
> st:done@260914 | ssot:CLIP-26 CLIP-65 CLIP-71 CLIP-86 CLIP-87 CLIP-88 CLIP-91 CLIP-92 CLIP-93 CLIP-94 CLIP-95 CLIP-96 CDS-40 CDS-66 CDS-67 QUOTA-43 QUOTA-44 QUOTA-45 QUOTA-53 QUOTA-54 ARCH-24 ARCH-25 ARCH-26 ARCH-28 ARCH-31 LANG-21 LANG-23 | base:CLIP@17 CDS@11 QUOTA@12 ARCH@2 LANG@3 | dep:T139

## goal
Reject deterministic template mistakes before expensive work, resume compatible completed stages, correct malformed model responses within reserved limits and verify the actual failing input through a playable original-footage result.

## acceptance
- [v] One shared role/style contract rejects the production badge/simple and six related conflicts before template save, quote or preparation; editor controls, copied ko/en authoring instructions, grammar corpus, preview and final renderer agree across all role/style combinations and supported ratios.
- [v] Known authored text, fact bindings, positions and timing receive deterministic early checks without a model or original download; generated text and output-dependent geometry are checked immediately after planning, preserving exact authored content and actionable element/line reasons.
- [v] Authoritative structured composition failures remain visible even for historical unknown checkpoints; typed render substages, retry counts and reuse counts survive errors without logging customer text, source keys, response bodies or credentials.
- [v] New quotes freeze three additional format-correction attempts per missing chunk and composition, retain legacy one-call behavior, meter each request once, preserve cancellation/ceiling protections and refund unused allowance; only generated-response parse/schema/field errors retry the same model within unchanged limits.
- [v] Observe and write prompts specify exact JSON, units, IDs, bounds and finite empty cases; correction feedback contains allowlisted validation facts, is bounded, and never includes untrusted prior output as instructions; tests cover recovery on attempts 2 and 4, exhausted attempts, cancellation, refusals, transport failures, deterministic inputs and no supplier fallback.
- [v] Durable recovery retains completed validated chunks and the candidate plan before rendering separately from canonical results; same-input retry skips completed source preparation/AI, template or fact changes reuse matching observations but invalidate the plan, and source/model/analysis-contract changes invalidate affected observations.
- [v] Recovery is owner/project/version/manifest-bound, quote/start races and ambiguous starts are idempotent, process interruption preserves completed evidence, finalization/deletion remove it, and missing/expired originals request matching media only when needed; legacy completed observations are reused only after explicit conservative validation.
- [v] The UI describes reused and remaining work before approval, identifies render-only continuation as AI-free, and exposes bounded correction progress with stable ko/en failure explanations; browser tests cover partial analysis retry, render retry, changed template, expired source and historical diagnostics.
- [v] Public synthetic fixtures exercise full strict-provider-adapter planning/rendering and restart after injected late failure with zero repeated successful AI calls; production-image mandatory release checks include the triggering template role combinations and changed-template recovery.
- [v] Authorized actual-provider verification stays below $0.50 cumulative, reuses the saved 20 analyses, caches responses privately for unpaid replay and renders actual originals; inspect the complete output/representative frames and audio, then exercise continuation without another paid call. A representative inline observation request validates the live observation contract within the same cap.
- [v] Record specification gaps versus implementation violations and map each changed decision to tests; all ARCH local checks, spec/skill validation, pushed CI, image release gates and exact production image/health pass before done. Finish T140's pending actual-input acceptance only after this evidence exists.

## impl notes
- Root evidence: job 4e406381644372bc32a9026152af23d9 stopped in render after 20 observations and planning; failure params identify disclosure_badge, source line 39, invalid_style. The saved template has seven named styles on non-caption roles. Parser accepts them while media.layoutDeclaredRole requires auto. Local isolated reproduction covers all seven without any provider/media operation.
- Audit: CLIP-65 and CDS-40 did not delimit caption-only named styles; CLIP-71/86 required validation/diagnostics but parser/render and job/checkpoint projections diverged; CLIP-35/52 and QUOTA-44 explicitly deferred reuse/forbade retries, now superseded. Keep the managed skills unchanged under ARCH-30; add executable contract/guide checks and task acceptance evidence rather than new unenforced prose.
- Extend the existing pure composition parser/contract and shared frontend vocabulary; preserve authored drafts, offer explicit correction for existing invalid templates, and do not silently rewrite styles or hide failures with automatic omission. For the authorized concrete repair, privately back up the affected current template/project inputs and apply only confirmed deterministic invalid-role style corrections through owned service boundaries with concurrency checks; preserve original text, bindings, placement and timing.
- Freeze a correction count in clip pricing (absent = zero additional attempts) and per-call policy; new counts price four requests per remaining unit. Explicitly support zero remaining observations and zero remaining writer work, without changing ordinary post/voice/experiment policies. Metered admission remains authoritative for every call.
- Add owner-scoped bounded recovery persistence separate from latest inspection/canonical results if required, with an embedded migration and SQL generated via Docker. Store validated domain chunks and a versioned candidate plan, source/analysis contract/model identity and plan input digest; never raw provider responses or media. Quote binds the exact reusable revision/digest; start atomically freezes its selection before a worker can replace current inspection. Revalidate saved data before using it and fail without more AI if durable recording fails.
- Legacy checkpoint reuse is conservative: source identity/fingerprint, successful complete-source chunk evidence and the original attempt's observe model/contract must be provable from its durable payload; incomplete or evidence-limited legacy sources restart only that source. Existing rendered canonical content is never replaced until a successful final commit.
- Keep response correction in the clip planner boundary with consumer-owned progress/diagnostic callbacks and bounded typed feedback; no generic registry retry or network/status retry, no weakening validation, no token cap increase. Aggregate returned usage for tests while the production metered boundary records each request.
- Original files and prepared proxies remain temporary; reuse validated analysis avoids preparing its proxies, while final rendering still probes/uses required originals. Persist candidate plans before the first render-side operation so a render failure or crash cannot erase the only resume input.
- The authorized live budget is total $0.50, superseding the prior unanswered $0.10 question. Reserve the worst-case cost of each call in a private durable budget journal before execution; charge uncertain calls at that bound, cache accepted/error responses at mode 0600 for zero-cost replay and stop before a call could exceed the remainder. No full 20-source paid rerun is necessary.
- Commit/push is explicitly authorized. Check active production jobs before rollout, wait for them normally, run local/remote release gates and verify exact image health before completion; publishing to Naver and unrelated T008/T110 owner checks remain outside this task.

## result
- Specification audit: CLIP-65/CDS-40 did not distinguish caption styles from other roles, while CLIP-71/86 already required early validation and diagnostic preservation that the implementation violated. Reuse and response correction were explicitly deferred by CLIP-35/52 and QUOTA-44. CLIP r17, CDS r11 and QUOTA r12 now define those decisions and their executable boundaries; managed skills remain unchanged.
- Shared strict role/style parsing, authoring guidance and controls reject all non-caption named styles before expensive work. Persisted old drafts remain readable for explicit correction, but every save/quote/preview/render still uses the strict contract. The confirmed original failure contained seven invalid role/style pairs, beginning at disclosure_badge line 39.
- Known fixed text/facts now receive actual-font layout admission before original download or AI. The same input exposed an existing CDS-20 implementation violation: info chips did not support their documented two-line word wrapping. They now preserve exact words within role, line and physical width limits. No customer fact or text was shortened.
- Versioned owner-scoped recovery persists validated observations and candidate plans independently of canonical output. Only a successfully validated candidate is eligible for render-only continuation; changed template/facts invalidate the plan while retaining compatible source analysis. Legacy complete observations require provable source/model/manifest evidence. Quote/start binds the recovery digest transactionally.
- New approvals reserve the initial request plus at most three same-model format corrections per missing unit. Prompts specify the closed JSON contract and bounded feedback carries only allowlisted validation facts. Every returned call is metered; cancellation, unknown supplier cost, refusal, transport failure, output truncation and deterministic input/feasibility errors never trigger another paid call. Existing approvals retain their original one-call limit.
- Authoritative historical element/line errors, render substages and bounded correction/reuse progress are visible without logging customer content. An injected worker interruption also exposed checkpoint error translation hiding context cancellation; cancellation identity is now preserved for the existing boot recovery sweep.
- Executable coverage: composition corpus/parse and store compatibility tests pin CDS-66/67; composition layout/admission tests pin CLIP-71 and CDS-20; response_correction_test pins CLIP-94/95 and QUOTA-44/54; store recovery and job allowance tests pin CLIP-92/93/96 and QUOTA-43/45; inspection/progress/quote browser and unit tests pin CLIP-86/87. TestClipWriterInputRelease now includes late-failure continuation with zero repeated AI/proxies and changed-template reuse.
- Live verification used the actual retained 20-source analysis and template, correcting only the seven confirmed invalid styles privately. One actual writer response (5,671 microUSD) and one representative inline video observation (1,150 microUSD) cost **$0.006821 total**, below the authorized $0.50. A private durable journal reserved a conservative $0.112073 upper bound before those two calls; responses and evidence are mode 0600 outside Git. No production job, ledger or object was written by verification.
- Actual selected originals rendered into four cuts / 16.333 seconds, 1080x1920, 30 fps, H.264/yuv420p with AAC 48 kHz stereo, 11,998,151 bytes. The full file decoded, representative frames showed complete captions and the two-line fact chip, and the renderer's loudnorm check measured -16.14 LUFS / -3.14 dBTP. A second render from the saved plan made zero AI calls and produced the identical SHA-256 6cf4a9fe089e3a25a9743128637516e00019ce7630d61b1ec5ca79c60d5487e4.
- Local verification: frontend 227 files / 1,843 tests; lint/format/FSD/style with 61 probe cases and web build; backend/agent formatting, vet, build and full tests; agent shell syntax. Browser verification covered 40 combinations of five failure/reuse states, ko/en, light/dark and 320/390 px at 200% font size, with no material horizontal overflow. Existing unrelated hook/build warnings remain.
- Production media runtime gates and the 20-source strict-adapter/render/recovery gate passed locally; restart prepare/hold/partial recovery, complete ARCH checks, proto/SQL regeneration and spec/skill validation also passed. The complete 28-scenario fault matrix passed in 549.69 seconds at 1 GiB / 2 CPU, with peak memory 389,144,576 bytes; pushed CI and exact-image rollout also passed.
- Final migration boundary audit reproduced a valid 2 MiB legacy checkpoint exceeding the recovery limit after wrapping. Migration 51 now copies only bounded recovery states and preserves every original inspection; TestMigration0051PreservesInspectionAtRecoverySizeBoundary covers ordinary reuse, the exact size boundary and idempotent startup.
- Shipped code: 375b30f followed by b72298ed78c24bb3ce5416bfbad2c35294c2a1fb. Final GitHub CI 34767269739, Workers Builds and backend deployment 34767269719 all passed, including mandatory production-image media and 20-source continuation gates. Read-only production verification at 260914 01:15 KST confirmed that exact b72298e image, HTTP 200 health, migration 51, zero active clip jobs and zero startup error lines.
- The authorized production repair first compared the current template/project against its private backup under one write transaction and refused active jobs or concurrent changes. It changed exactly seven invalid non-caption styles to auto through owned services; facts/associations and prior output remained identical. Readback after the final rollout found zero invalid style pairs and all 20 completed source analyses in durable recovery. No automatic customer generation was started.
- Done freshness review: CLIP r17, CDS r11, QUOTA r12, ARCH r2 and LANG r3 match the task base. T140's remaining actual-input acceptance is fulfilled by the same live evidence and archived with this task; unrelated T008/T110 work remains open.
