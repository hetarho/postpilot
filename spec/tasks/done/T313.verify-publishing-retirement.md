# T313 Verify complete retirement and preserved manual publishing workflow
> st:done@260922 | ssot:PUB-38 PUB-42 PUB-43 PUB-44 PUB-45 PUB-46 PUB-47 PUB-48 PUB-49 EXPORT-21 POST-21 POST-29 POST-54 MKT-4 AUTH-46 ARCH-10 ARCH-25 ARCH-26 ARCH-28 ARCH-43 | base:PUB@6 EXPORT@5 POST@9 MKT@6 AUTH@7 ARCH@7 | dep:T284

## goal
Finish the removal acceptance matrix and rollout record without issuing a Naver publication.

## acceptance
- [v] A fresh installation has no publishing tables, handlers, device capabilities, worker, generated contract, companion files or agent build job; a seeded upgrade via the bridge reaches the same state with original post/media/voice/credit/clip fixtures unchanged.
- [v] A dirty upgrade is refused with all remaining records intact; an ambiguous historical commit stays unknown in the private report, and retries, old URLs, stale clients or old tokens cannot enqueue or publish at any checkpoint.
- [v] Browser/route regression covers ko/en, master/non-master, the legacy redirect and the writing finish workflow: finalization, learning and the four export formats work, including ordered Naver photo/caption copies and video markers, with zero publishing requests.
- [v] A focused absence check scans active runtime/proto/config/generator/CI paths for the exact retired service names, table/runtime ownership and agent target; its reviewed exceptions are historical migrations/evidence, reserved enum names, regression assertions and the legacy redirect, not a blanket ignore of all publishing text.
- [v] Active README/DEPLOY/package/CI guidance contains no installation or feature-availability promise for the former publisher; deployment instructions retain only the minimal bridge/cleanup procedure necessary to upgrade older installs. Derived onboarding/narrative material is synchronized without rewriting legacy history.
- [v] The result records build/migration identifiers and redacted cutoff/shutdown/report/cleanup/final-verification receipts per deployed environment, or explicitly states that operational rollout is pending; code/fixture completion alone is never described as production retirement.
- [v] Run ARCH-25, ARCH-26, ARCH-28 and relevant deployment-script tests after final removal; document failures by scope and do not waive a publishing-retirement failure. No acceptance requires a real Naver post, CAPTCHA bypass or remote profile deletion.

## impl notes
- This task implements retirement, not the superseded live-smoke goal of T008; never launch a real Naver publish or mark T008 successful.
- This task is the integration/rollout verification of T310 → T311 → T282 → T312 → T283 → T284, not another product feature or recurring maintenance subsystem. Tests should exercise upgrade/export/no-side-effect boundaries rather than assert every deleted filename independently.
- Keep the private report and local profile paths out of Git and shared CI logs. Record only safe counts/digests, version identifiers and explicit completion/pending states in the task result.
- Code implementation may be complete before deployment authorization exists. Distinguish that status in the result and runbook; do not run production cleanup or a Mac command merely to close a checkbox. Operational checkpoints are applied in the same phase order when deployment is authorized.
- T008 remains an immutable historical doing artifact whose original live-publish goal was superseded by PUB r6; no task depends on it and it must never be resumed or marked successful as part of retirement. Preserve this dispatch warning in STATE until the original claiming session can reconcile its history.
- TMPL-39 remains an open manual-layout question with no agent dependency; VIDEO-17 is withdrawn. T279 retains only the independent enum-maintenance work on surviving code and follows this retirement chain.

## result
- Added `scripts/check-publishing-retirement.mjs` and made it a CI gate through `pnpm lint:retirement`. It rejects the removed module, generated contracts, service/RPC symbols, runtime table ownership, cleanup binary and companion build targets. Its exceptions are exact: historical migrations, database regression assertions, reserved failure identities, the deployment bridge and the single authenticated legacy redirect.
- Expanded migration 0076 coverage. A fresh database reaches schema 76 with no publishing tables or triggers; a seeded upgrade preserves the exact human session, finalized post, voice, photo, video, credit lot and clip project before a dirty refusal, after that refusal and after final cleanup/removal. Existing migration 0072 coverage pins conservative `outcome_unknown` handling and refuses every legacy insert/rearm/lease/commit path.
- Re-ran the ko/en and free/master route/editor/export matrix: the old URL redirects after ordinary authentication, finalization and explicit learning stay separate, all four export formats remain synchronous, ordered Naver photo/caption copy and video markers remain intact, and no publishing request is observed. Updated `spec/NARRATIVE.md` to the current manual-export boundary without changing legacy history.
- Build and migration identifiers: migration 0072/report cutoff `c87b0a9c`; local shutdown bridge `1c1c05e4`; cleanup bridge `87ca8aba`; migration 0076/backend removal `9b76b991`; companion/contract removal `108a025f`; validation base `63f41404`. No deployed environment exists in this repository, so every production cutoff receipt, private report digest, shutdown inventory digest, cleanup receipt digest and final image SHA remains explicitly pending. No production cleanup, browser-profile deletion or Naver request was performed.
- Validation passed for deterministic Proto/SQL generation, the retirement absence gate, all 8 deployment-script tests, all backend format/vet/build/tests, 180 focused route/editor/export tests, frontend lint/format/FSD/style/build and spec lint. The isolated full frontend run passed 289/290 files and 2,304/2,306 tests; its two pre-existing AI-model navigation assertions pass 12/12 in the separate navigation work already present in the shared worktree. No publishing-retirement test failed.
- No SSOT deviation.
