# T263 the draft editor page composes hooks it does not own
> st:done@260920 | ssot:ARCH-14 ARCH-15 ARCH-24 | base:ARCH@5 | dep:-

## goal
Move the step state machine, the server-mirrored brief state and the caret handoff out of `pages/editor/ui/DraftEditor.tsx` into model hooks and features, and break the five inline helper components into files.

## acceptance
- [v] `DraftEditor.tsx` is ≤300 lines with ≤4 `useState`; the step machine lives in `pages/editor/model/useDraftSteps.ts` as a pure reducer + hook with a `.test.ts` covering every transition
- [v] target-length / tag-count mirroring (`:215-229`) lives in `features/generate-post/model/useBriefMirror.ts` (or `entities/post` if it is pure) with a unit test for the sync-on-server-change rule
- [v] the caret handoff (`:201`) lives in `features/edit-post-content/model`, tested
- [v] the five inline components (`:445-917`) are separate files under the owning feature `ui/` or `pages/editor/ui/`, each ≤200 lines
- [v] the `react-hooks/exhaustive-deps` warning at `DraftEditor.tsx:140` is resolved (the `storedAnswers` memo), so `pnpm lint` reports 0 warnings
- [v] behaviour unchanged: `DraftEditor.test.tsx` and the editor feature tests pass
- [v] ARCH-25 passes

## impl notes
- from review/arch-260919 F2 F10
- `pages/editor/model/` already exists (156 lines) and `useEditorJob.ts` sits there; T267 later moves its transport use into `entities/generation-job`, so leave `useEditorJob` alone here
- a state machine is `model` (ARCH-15); a component that renders a brief step is a feature `ui` if it owns a verb (fill-template-answers, select-post-voice) or page `ui` if it only lays out
- resolve the memo warning by moving `storedAnswers` inside the `useMemo` callback or its own memo — do not disable the rule

## result
- `DraftEditor.tsx` is 253 lines with 3 `useState` (title, memo, answer edits — the values the autosave queue carries)
- `pages/editor/model/useDraftSteps.ts`: `draftStep` is a pure reducer over `{step, followed}` with 3 transitions tested (start per status, a picked step that survives re-renders, following every later status change). The status transition stays an EFFECT on purpose — applying it during render unmounts the panel that has something to say about the change, which is how the publish refusal after a landed edit disappeared (caught by `EditorPage.test.tsx`)
- `features/generate-post/model/useBriefMirror.ts`: one `useMirrored` pair for target length and tag count, adjusted during render as before, with the fallback projection for the tag count's product default; tested for edit-survives-rerender and follow-the-server
- `features/edit-post-content/model/caret-handoff.ts` (moved from `pages/editor/model/editor-handoff.ts`) now also owns `useCaretHandoff`, both halves — the stash at mint reads the live DOM, the claim focuses once. Its TTL constant moved to the feature's `config`; `pages/editor/config` is gone
- five inline components became eleven files, each ≤200 lines: `TitleField` `MemoField` `EditorVoiceWarning` `EditorDock` `EmptyStep` `EditorDockHeader` `EditorGeneratePanel` `EditorRefinePanel` `EditorFinishPanel` `EditorJobNotice` `EditorStepDock`, with `LifecycleSteps` down to 188 lines
- `pages/editor/model/useDraftAssignments.ts` holds the three choices a post-less draft owns (voice, template, target language)
- the `storedAnswers` warning is gone — the array is built inside the memo from `post?.templateAnswers` — and `pnpm lint` now reports 0 problems for the whole frontend
- verify: ARCH-25 green (2200 tests / 280 files, eslint 0, steiger clean, style + build ok)
