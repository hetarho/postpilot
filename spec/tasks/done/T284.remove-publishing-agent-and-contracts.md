# T284 Delete companion source, publishing contracts and build integration
> st:done@260922 | ssot:PUB-47 AUTH-46 LANG-7 LANG-23 VIDEO-16 ARCH-2 ARCH-3 ARCH-27 ARCH-28 ARCH-31 ARCH-41 ARCH-43 | base:PUB@6 AUTH@7 LANG@5 VIDEO@3 ARCH@7 | dep:T283

## goal
Remove the entire companion and obsolete publishing contract/tooling surface from the active repository.

## acceptance
- [v] Delete agent/ including its Go module, generated files, browser driver, surveys, setup/credentials/runtime and packaging; root agent:* scripts, agent generation templates/invocations, build outputs tracked in Git, installer instructions and the Mac-only agent CI job are absent.
- [v] Delete proto/postpilot/v1/publishing.proto and regenerate committed backend/frontend contracts through the configured generators; remove stale generated publishing files explicitly if generation does not prune them. No remaining application import/export references the removed service/messages/enums.
- [v] Remove publishing-only FailureReason symbols and reserve their exact numbers and names in error.proto: POST_PUBLISHING=135, PUBLISH_* currently 143..160, VIDEO_NOT_PUBLISHABLE=192; confirm the current schema before editing and never renumber/reuse surviving values.
- [v] Remove obsolete backend/frontend failure registries, mappings, catalogs, shared API exports, test fixtures and publishing namespace fragments while keeping locale parity, unknown-failure handling and general enum contracts correct.
- [v] Code generation, CI and root scripts work with no agent directory and do not recreate it; all surviving APIs generate and compile, and no publishing SQL generation path remains.
- [v] ARCH-25, ARCH-26 and ARCH-28 pass on the final two-runtime tree; historical migration tests and exact enum-reservation assertions pass. The former agent gate is removed rather than skipped by a hidden publishing feature flag.

## impl notes
- Replaces this todo task's agent proto-pruning/survey-deletion scope with complete agent/contract removal. It absorbs the removal outcome of arch-260919 F24/F26/F27/F28/F29/F30 rather than implementing their earlier refactoring approaches.
- Delete agent generation from scripts/gen.mjs, the agent CI job and diff path in .github/workflows/ci.yml, agent:* scripts from package.json and active docs/references. Remove only now-unused imports/dependencies; do not run blanket dependency upgrades.
- The publishing service deletion is an intentional API removal after T310 refusal and T311 UI retirement; document this specific compatibility break. Do not loosen global buf lint/breaking rules for unrelated services. The existing PACKAGE policy and reservation checks remain.
- The shared FailureReason enum outlives this feature, so its removed names/numbers are explicitly reserved. Types unique to publishing.proto disappear with the file; do not retain dead schema types just to keep an obsolete client compiling.
- The supported Mac cleanup command and temporary server cleanup command are already executed per deployed-environment checkpoint before this final artifact is rolled out. The runbook can point an old installation to a pinned retirement-bridge revision without retaining a runnable companion in current main.
- Historical SQL/spec/task/review text and the small legacy UI redirect may still name publishing. Current runtime imports, endpoints, build targets, generated types and active marketing promises may not. Generic voice-profile publication, rate publication and manual export names are unrelated and must not be renamed.

## result
- Removed the companion module, its root scripts, code generator, macOS CI job and installer surface; current documentation now describes manual export, while the deployment runbook points old installations at a pinned retirement-bridge revision.
- Deleted the publishing proto and all committed backend/frontend generated clients. Code generation no longer references or recreates the companion or publishing contract.
- Removed the retired publishing failure registry entries and reserved every former name and number in `error.proto`; a descriptor test pins the exact reservation and prevents accidental reuse.
- Verified deterministic proto and SQL generation, runtime-absence searches, backend formatting/vet/build and the affected package tests, plus frontend locale/delete tests, lint, formatting, FSD, style and production build. The isolated full-suite run passed every backend package when the load-sensitive clip store ran alone; its only two frontend failures were pre-existing duplicate-navigation assertions that pass with the separate navigation work already present in the shared worktree.
- No SSOT deviation.
