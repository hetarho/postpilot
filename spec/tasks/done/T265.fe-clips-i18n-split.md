# T265 the clips namespace lives in the clip slices
> st:done@260920 | ssot:ARCH-16 | base:ARCH@5 | dep:T261 T264

## goal
Move the 1,121/1,163-line `clips` resources out of `app/` into fragments owned by the clip entities, features, widget and pages settled by T260/T261.

## acceptance
- [v] `app/providers/i18n/resources/{ko,en}/clips.ts` are deleted; every `clips` key lives in a `config/i18n.ts` fragment of the slice that renders it, and no fragment exceeds 250 lines per language
- [v] `useTranslation('clips')` call sites (52) are unchanged; the namespace still exists and the parity/duplicate tests pass
- [v] behaviour unchanged: all clip tests pass; ARCH-25 passes

## impl notes
- from review/arch-260919 F3 (clips half)
- reuse the ownership script from T264's result; the 34 top-level groups in `clips.ts` mostly map one-to-one onto the clip slices after T260/T261

## result
- `app/providers/i18n/resources/{ko,en}/clips.ts` (1,121/1,163 lines) are deleted; the 36 top-level groups live in 17 clip-slice fragments, largest per language: `inspect-clip-observations` 205, `clip-template` 209, `correct-clip` 167 — every one under the 250-line bar
- ownership followed T264's script with four hand-corrections where the count rule reads worse than the domain does: `notices` → `entities/clip-project` (the project's own notice codes, and what keeps `inspect-clip-observations` under 250), `render` → `features/render-clip-browser` (a 4/4 tie with correct-clip), `generation` → `features/generate-clip` (the widget only reports it), and the design vocabulary (`ratio preset cta accent`) → `entities/clip-design`
- `useTranslation('clips')` is unchanged at all 52 call sites, the namespace still assembles to the same keys, and the fragment duplicate/parity checks cover it
- `app/providers/i18n/resources/{ko,en}` now holds only the five cross-cutting namespaces (common, auth, nav, errors, marketing)
- verify: ARCH-25 green (2201 tests / 280 files, eslint 0, steiger clean, style + build ok)
