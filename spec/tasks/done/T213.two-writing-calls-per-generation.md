# T213 A generation runs the flow call and then the narration call
> st:done@260916 | ssot:CLIP-135 CLIP-19 CLIP-93 CLIP-87 CLIP-90 CLIP-96 | base:CLIP@31 ARCH@3 | dep:T212

## goal
Quote, approve, run, checkpoint and settle two writing calls per generation, resuming at the narration when the flow already exists.

## acceptance
- [v] `GenerationPricing` gains `Narration llm.CallPolicy` and replaces `SkipPlan` with `SkipFlow`/`SkipNarration`; `PricingPolicyVersion` 2→3; `Valid()` pins `Plan` (the flow) and `Narration` at 32768 completion tokens; `PlanCalls()` counts each unskipped writing call with its retries
- [v] `ClipPricedCall` gains `string label = 9` (`flow` | `narration`); the quote lists both writing calls and the FE approval (`ClipApprovalAction`) shows two writing rows
- [v] `Run`: the `plan` stage becomes `flow` then `narrate`; after `Flow` the recovery state stores the flow with `FlowReady`; a failure or cancellation in `narrate` resumes with `SkipFlow`, pricing one writing call; `planRecoveryDigest` keeps its meaning; `LayoutComposition` runs after `narrate`
- [v] `ValidatePreparation`, `freezeWork` and the pricing adapter check and price both budgets; `Budgets()` returns three values; a recovery state with `PlanReady` and a version-6 plan that carries scene elements still resumes at render
- [v] CLIP-88 diagnostics and CLIP-78 progress carry the stage names `flow` and `narrate` with ko/en labels; no prose enters logs
- [v] `ValidatePreparation` checks the frozen input allowance for the larger of the two requests (the narration request adds the flow) under CLIP-90
- [v] tests: pricing valid/invalid and call counts; run order flow → narrate → layout → render with a stub planner; resume after a narration failure reuses the flow; FE quote renders two writing rows; verify BE (ARCH-26), FE (ARCH-25), generated code (ARCH-28)

## impl notes
- files: clip/generation.go (plan stage, ~495–540) · quotes.go (`GenerationPricing`, `freezeWork`, `quoteInputs` skipPlan branch, `QuoteInputDigest`) · recovery.go (`RecoveryState.FlowReady`) · ai/service.go (`Budgets`) · the `remainingQuotePricing.FreezeWork` implementer in usage/quota · rpc quotes mapper · proto
- keep the field name `Plan` for the flow policy to limit churn; the label, not the field, tells the owner which call is which
- the reservation is one, for both calls (CLIP-19: no AI call before a successful reservation); a narration-only resume reserves one call
- `T202` (revision job) prices with the same helper: one call for a narration target, two for a flow target

## result
- files: clip/{quotes,generation,recovery}.go · clip/ai/{service,preparation,flow_prompt,narration_prompt}.go · clip/rpc/quotes.go · cmd/api/clip_credits.go · internal/job/{clip,worker}.go · proto clip.proto + the three generated clients · FE entities/clip-project (types, credits), features/generate-clip (ClipApprovalAction), entities/generation-job stages, ko/en clips.ts · tests: clip/store/two_writing_calls_test.go (new), the drifted store/cmd fixtures, FE credits + ClipGeneration
- `internal/job/clip.go` had to change too: the reservation capped the writing line at one call plus retries, and a generation now makes two on the same model and budget
- `Valid()` also pins both writing policies to the SAME model ref — they are one reservation line, so a narration on another model could not be reserved
- the resume rule is `FlowReady || PlanReady` for the flow and `PlanReady` for the narration, which is what lets a recovery written before the split still resume at render
- `WidestFlow` (exported) is the synthetic cut-ceiling flow the preparation allowance is measured against, and `BuildFlowPrompt`/`BuildNarrationPrompt` are exported for the same measurement
- the retry stages are `flow_retry`/`narrate_retry`; `plan`/`plan_retry` stay in the worker and FE stage lists so a job queued before the split keeps a readable label
- PricingPolicyVersion 2→3 invalidates every queued job priced under the old policy, as the bump intends
