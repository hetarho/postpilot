# T249 ②'s reference opens on demand
> st:done@260918 | ssot:CLIP-150 CLIP-47 CLIP-48 CLIP-133 | base:CLIP@38 | dep:T248

## goal
②'s stored observations, source strip and verbatim request record move out of the page into one 참고 자료 sheet with a tab per section.

## acceptance
- [v] ②'s page carries one control opening a 참고 자료 sheet and none of the three sections stands open beneath the editor
- [v] the sheet holds the observations, the source strip with its sound settings and the request record as tabs, opening on the observations
- [v] every control those sections own — adding a cut from an observed scene, source selection, original-sound retention — still works from inside the sheet, and adding a cut closes the sheet onto the new cut's own sheet
- [v] ① keeps all three in flow, unchanged
- [v] vitest: ②'s page renders none of the three until the sheet is opened; each tab shows its section; adding a cut from an observation inside the sheet reaches the plan
- [v] verify: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- the three are `ClipObservationViewer`, `ClipSourcePicker` (with its required-sources list) and `ClipRequestRecord`, passed today as the workspace's `sourcePicker` prop plus the page's `observationPanel` and `requestRecord`
- the sheet is `Sheet` with a `SegmentedControl` in its `header` choosing the tab; `TabLinks` is for tabs that are addresses and these are not
- only the chosen tab's section is mounted, so the observation viewer's media work does not run while the source strip is showing
- `onAddCut` already dispatches a selection; after it fires, close the reference sheet so T245's cut sheet is what the owner lands on
- the workspace's `sourcePicker` prop goes and the page composes the reference sheet itself; ①'s composition of the same three is untouched
- new ko/en keys: `reference.label` and `reference.tabs.observations|sources|requests`

## result
- `ClipPage` composes one reference `Sheet` with observations, sources and requests behind its preview-row control. It resets to observations on opening and mounts only the selected section while open. `ClipCorrectionWorkspace` now accepts that control instead of an always-open source/reference panel.
- Source selection, retained-source sound changes and the verbatim request record keep their existing paths. Adding an observed scene awaits the existing correction action, closes the reference sheet and reveals the newly selected cut's sheet. Step ① keeps the same sections in flow, including the existing inspection of failed/cancelled attempts.
- Updated route tests cover hidden/unmounted sections, tab switching, source choice, request display, re-opening, generation-step preservation, cut addition and sound changes. Frontend: 254 files / 2,101 tests passed, plus lint/format/FSD/style/build. Backend and agent gofmt/vet/build/tests, packaging syntax, deployment tests, generated-code drift and skill/spec checks passed. Chromium verified all three tabs at 360/900 px in Korean/English (12 cases) without overflow.
- No SSOT deviation. Existing editor hook and bundle-size warnings remain outside scope.
