# T178 The disclosure badge is a rounded rectangle
> st:done@260916 | ssot:CDS-31 CDS-21 | base:CDS@19 ARCH@2 | dep:-

## goal
Paint the disclosure badge as a rectangle with 12 px corners instead of a full-round pill.

## acceptance
- [v] `radius_chip` is 12 in the design tokens, and the frontend mirror stays byte-identical to the embedded file
- [v] the badge SVG the renderer emits carries `rx="12.000"`, asserted by the existing furniture golden rather than a new one
- [v] the design token test expects 12, so a future edit of the token fails a test instead of only moving pixels
- [v] the template preview draws the same corner as the renderer (it already reads the token — confirm no literal 999 survives anywhere)
- [v] verify BE (ARCH-26) and FE (ARCH-25) pass

## impl notes
- the whole surface is four files: `backend/internal/clip/design/design.json:249` · its byte-identical mirror `frontend/src/shared/config/clip-design.json:249` (drift is caught by `design_test.go:211`) · the expected token in `backend/internal/clip/design/design_test.go:174` · the golden string in `backend/internal/clip/media/information_frame_test.go:82`
- no drawing code changes: `overlay.go:94` already passes `design.Spacing.RadiusChip` into the box, `presets/furniture/overlay.svg` already renders `rx="{{.Radius}}"`, and `CompositionDesignFrame.tsx:400` already reads `CLIP_DESIGN.spacing.radius_chip`
- the token keeps the name `radius_chip` — renaming it is a code-wide rename with no design consequence, and CDS-21 already scopes it to the disclosure
- the badge box is unchanged at 119x68 with `pad.chip` 16/28; only the corner changes, so no layout, safe-area or V-check result moves
- the token moved 999→12 in `backend/internal/clip/design/design.json` and its byte-identical mirror `frontend/src/shared/config/clip-design.json` (confirmed by `diff`, and by the drift test in `design_test.go`)
- no drawing code changed, as predicted: the badge box is built at `overlay.go:94` from `design.Spacing.RadiusChip`, the furniture template renders `rx="{{.Radius}}"`, and the preview reads `CLIP_DESIGN.spacing.radius_chip`
- one file beyond the four the task listed: `frontend/src/entities/clip-template/ui/CompositionDesignFrame.test.tsx:68` pinned `rx` to the literal `'999'`. It now asserts against the token (the same way the neighbouring rule assertions read `CLIP_RULES`), so the value itself stays pinned once, in `design_test.go`. Its local `pill` binding is renamed `badge`
- rendered a real 9:16 clip through the production path over owner footage to look at the result: the badge is a rectangle with soft corners at 119x68, unchanged in position, padding and text
- changed: backend/internal/clip/design/design.json · backend/internal/clip/design/design_test.go · backend/internal/clip/media/information_frame_test.go · frontend/src/shared/config/clip-design.json · frontend/src/entities/clip-template/ui/CompositionDesignFrame.test.tsx
- verify: ARCH-26 BE green (gofmt clean, vet, build, `go test ./...` exit 0) and ARCH-25 FE green (237 files / 1983 tests, lint, lint:fsd, style probe + style, build:web). The one lint warning is the pre-existing `DraftEditor.tsx` hook dependency, untouched here
- T177 is open on CDS@18 while this lands CDS@19; its checklist reviews the badge, so that session re-checks freshness before it records a badge answer
