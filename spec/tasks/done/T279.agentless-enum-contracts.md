# T279 Pin enum mirrors in the surviving backend and frontend
> st:done@260922 | ssot:ARCH-3 ARCH-24 ARCH-25 ARCH-26 | base:ARCH@7 | dep:T313

## goal
Finish the non-publishing enum-contract work on surviving BlockType, ModelPurpose and language mappings.

## acceptance
- [v] Tests walk each surviving generated BlockType and ModelPurpose enum and check every hand-maintained mapping, including invalid/unspecified handling; no fallback silently assigns a valid business value to an unmapped enum.
- [v] Repeated transport language conversion is consolidated at the existing platform/rpcserver adapter where appropriate, with every consuming context using the same tested mapping and pure domain language values preserved.
- [v] No publishing Stage/FailureKind/STAGE_RANK mapping or agent adapter is recreated solely to satisfy the old task wording; removed shared failure values remain reserved.
- [v] Existing behavior is unchanged; ARCH-25 and ARCH-26 pass for surviving code and corresponding generated-enum contract tests cover the changes.

## impl notes
- from review/arch-260919 F25; revised after PUB r6 to exclude every retired publishing/agent enum mirror and to depend on T313 rather than T008.
- This is existing independent technical-debt work, not a prerequisite for removing automatic publishing. Re-scan the surviving copies at implementation time because removal changes their count; do not preserve the old count of five/three as an acceptance target.
- This todo task is rebased to ARCH r7; unlike T008 it can be revised during create-task. Avoid changing domain behavior or introducing reflection/registries beyond the focused mapping tests.

## result
- Added an additive `ModelPurpose` schema enum while retaining every existing string request, response and persistence field. Generated Go and TypeScript contracts now give the backend `Purposes` list and frontend `MODEL_PURPOSES` list one closed vocabulary; tests walk every generated value and reject unspecified, unknown and unmapped fallbacks.
- Added generated-enum coverage for both surviving backend `BlockType` mirrors. The post RPC test exhausts both mapping directions, and the generation contract test checks every domain constant and ProtoJSON spelling, including `VIDEO`; invalid and unspecified values remain invalid.
- Centralized `ContentLanguage` enum/tag conversion in `internal/platform/rpcserver`. Post, voice, generation, experiment, job and clip RPC adapters now share the tested transport mapping while preserving each context's pure language type and its existing required/unsupported error behavior.
- No publishing enum, rank, agent or failure mapping was restored. `pnpm lint:retirement` still passes and the retired failure identities remain reserved.
- Validation passed for focused Go packages, 7 focused frontend enum/language tests, frontend lint/format/FSD/style/build, deterministic Proto and SQL generation, and the exact ARCH-26 backend command. The first contended backend run timed out in unrelated `clip/store`; a later unrelated recovery assertion failed once, then passed alone, and the full `clip/store` suite passed in 363.802s before the exact full command passed. The isolated frontend run passed 290/291 files and 2,307/2,309 tests; only the same two pre-existing `AIModelsPage` duplicate-navigation assertions recorded by T313 failed.
- No SSOT deviation.
