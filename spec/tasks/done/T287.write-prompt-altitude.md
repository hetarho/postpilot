# T287 the write prompt stops describing the photographs
> st:done@260920 | ssot:GEN-47 GEN-16 GEN-44 GEN-14 GUIDE-16 ARCH-24 | base:GEN@7 GUIDE@3 | dep:-

## goal
Add the altitude rule to the write prompt and stop the two constants that made describing the photographs the only safe thing to write.

## acceptance
- [v] a new altitude constant pair (Korean + English) sits beside the grounding constants in `generation/prompts.go`, states that the observations are grounding and placement material rather than a list to describe, that no paragraph must point at a photo, and that inert visual detail (lighting, walls, ceilings, fixtures, room arrangement) is not written
- [v] the altitude rule reaches the WRITE prompt only: a test asserts it is absent from the revise prompt and from both observe prompts
- [v] `koreanGroundingWriteScope` / `englishGroundingWriteScope` state that the rule binds factual claims alone and leave the author's impressions, opinions and changes of subject writable
- [v] `koreanNaming` / `englishNaming` no longer forbid a subject the memo alone carries; they forbid only writing something into the frame that the observations do not show
- [v] every `write_prompt_*.golden` is regenerated and its diff is exactly these lines
- [v] the `revise_prompt_*.golden` files do NOT change, and a test asserts the shared grounding core is still byte-identical in the write and revise prompts
- [v] `NaturalnessBaseline` is untouched: `TestNaturalnessBaselineContract` passes unchanged
- [v] ARCH-26 passes

## impl notes
- GUIDE-16 now counts four grounding constants; the altitude rule is the fourth and it is code, not config and not a row, exactly like its three siblings
- word it as subtraction, the way `NaturalnessBaseline` is worded: it forbids describing, it never licenses inventing. The grounding constraint keeps its full force — this task widens nothing about what may be stated
- place the altitude text in the static task rules after the grounding line and before the naming line, in both languages, so the golden stays readable top to bottom
- do not touch `NaturalnessBaseline`: GEN-17 pins its markers, its rejected folk rules and its 700-rune cap, and `naturalness_baseline_test.go` asserts all three
- the write prompt's byte identity is broken here on purpose. It invalidates the provider's prefix cache once, at the deploy, and nothing else — no payload, no job and no stored post changes
- `koreanNaming`'s replacement sentence must stay about the FRAME, not about sources: the memo is one of GEN-16's three legitimate sources and this line must never read as a ban on it again

## result
- `backend/internal/generation/prompts.go`: `koreanAltitude` / `englishAltitude` added beside the grounding constants and injected into `WritePrompt` / `englishWritePrompt` between the grounding line and the naming line; `koreanGroundingWriteScope` / `englishGroundingWriteScope` now open by binding the rule to factual claims and close by leaving impressions, opinions and changes of subject writable; `koreanNaming` / `englishNaming` traded the memo-shaped prohibition for a frame-shaped one ("사진 관찰에 없는 것을 사진 안에 있는 것처럼 쓰지는 마세요").
- `backend/internal/generation/testdata/write_prompt_no_template.golden`: the diff is exactly three lines — the grounding line's new scope clause, the inserted altitude line, the naming line's new second sentence. `revise_prompt_no_template.golden` is byte-unchanged (regenerated and diffed to prove it).
- `backend/internal/generation/naturalness_baseline_test.go`: `TestMemoNamingAuthorityIsInWritePromptsOnly` now pins the three lines in order; `TestFixedTextAdditionsAreTheOnlyGoldenDelta` strips the altitude line as a fourth known addition; new `TestAltitudeRuleIsInWritePromptsOnly` (absent from both revise prompts and from `ObservePrompt` and `ObserveVideoPrompt`) and `TestGroundingCoreIsByteIdenticalInWriteAndRevisePrompts` (the core line is the same bytes in both passes, differing only in its scope clause).
- `write_prompt_pre_naturalness.golden` was deliberately NOT regenerated: it is the frozen pre-addition baseline `TestFixedTextAdditionsAreTheOnlyGoldenDelta` measures the current golden against, so regenerating it would erase the delta it exists to prove. The acceptance line's "every `write_prompt_*.golden`" is satisfied by the one golden the code generates.
- `NaturalnessBaseline` untouched; `TestNaturalnessBaselineContract` passes unchanged. ARCH-26 green (gofmt, vet, build, full `go test ./...`). No FE, proto or sql file changed, so ARCH-25/27/28 are unaffected.
- The write prompt's byte identity is broken on purpose: the provider prefix cache misses once at the deploy. No payload, job or stored post changes.
- No SSOT deviation.
