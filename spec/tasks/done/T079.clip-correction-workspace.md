# T079 Clip correction workspace
> st:done@260910 | ssot:CLIP-12 CLIP-14 CLIP-15 CLIP-17 CLIP-18 CLIP-20 CLIP-23 CLIP-26 CLIP-27 CLIP-28 THEME-22 THEME-23 THEME-24 THEME-29 LANG-21 LANG-22 ARCH-13 ARCH-14 ARCH-15 ARCH-17 ARCH-18 ARCH-20 ARCH-24 | base:CLIP@2 THEME@8 LANG@2 ARCH@2 | dep:T077 T078

## goal
Give a completed clip a focused correction workspace for cuts, exact copy, approved placement/style and per-cut source volume, followed by source reselection and free rerender.

## acceptance
- [v] `/clips/$clipId` enters 수정 mode without replacing the result preview and builds a local draft from the current edit plan and its revision
- [v] an ordered cut list uses `SortableList`; every row names its source, shows start/end/duration controls, delete, exact copy, copy exposure times, `top|center|bottom`, allowed style/accent and original-audio volume 0–100%
- [v] extending/shortening is bounded live by the retained source duration and final 15–90 second result limit; errors sit beside the responsible field and no invalid plan is sent
- [v] copy fields show deterministic previews for `clean|diary|emphasis`, use the template's approved choices only and cannot select arbitrary font, decoration, motion, generated image or background music
- [v] save is optimistic, reports a revision conflict without dropping local edits, and after success clearly distinguishes `수정됨 · 다시 출력 필요` from a result already matching the plan
- [v] rerender first asks for only the source files still referenced by cuts, fingerprints them through T072's bounded helper, names missing/mismatched files and reserves a transient batch only after every required source matches
- [v] the rerender action states that no credits are used, starts once under double-click, polls the durable render job and replaces the preview only on success; failure keeps both the prior preview and saved edits
- [v] local source previews use revocable object URLs and may seek to a cut start for inspection; source File objects, URLs and bytes never enter query persistence, localStorage or IndexedDB
- [v] leaving with unsaved edits uses the app's existing navigation guard pattern; leaving after save needs no warning even when rerender is still owed
- [v] phone layout is one scroller with result preview before controls and one docked save/rerender bar; desk layout may widen the ordered list but does not introduce a multitrack NLE
- [v] tests cover every edit kind, bounds, reorder/delete, revision conflict, fingerprint reselection, source subset, no-credit copy, render progress, failure preservation, object-URL cleanup and 320 px accessibility
- [v] ARCH-25 passes

## impl notes
- this is a structured correction form, not a canvas timeline: integer time fields plus source seeking make every operation accessible and testable while satisfying CLIP-17's lightweight correction pass
- keep the result video mounted while editing so a form rerender never resets playback; cut-source previews are separate local elements created only after reselection
- expose pure edit-plan reducers and validation from `entities/clip-project/model`; UI components dispatch operations and never mutate nested JSON in place
- T078 saves before it renders, so a failed render does not discard authored corrections and retry needs no second save

## result
- Added immutable edit-plan reducers, source/timeline/caption/volume validation and explicit wire mappers in entities/clip-project, plus features/correct-clip for optimistic saves, conflict-preserving drafts and confirmed navigation guards.
- ClipPage keeps the successful video mounted across correction mode, uses an accessible SortableList form and approved style previews, and distinguishes saved corrections from the rendered revision. Detached templates do not prevent credit-free correction.
- Rerender reuses the bounded source manifest helper, verifies exactly the retained source subset before reservation and calls StartClipRender once without model choices or any AI-generation call. Source selection and edits lock during processing; failure retains both the saved plan and prior preview.
- Local File/object URLs remain outside query caches and persistence; cancellation discards and revokes, worker ownership revokes, and unload follows T072's existing local-revoke/server-lease cleanup policy.
- Verification: Node 24.18.0, 181 FE files / 1411 tests; ARCH-25, backend and agent gofmt/vet/build/tests, agent shell syntax, remote proto and SQL regeneration with zero diff, skill sync and spec lint pass. Added ten correction integration scenarios and pure mutation/reselection cases; existing upload and generation regressions pass.
- Isolated Chrome with synthetic RPCs passed ko/en, day/night and 320/360/390/430/1024 px: stable result element, no page overflow, 44 px coarse controls, 16 px phone fields and no browser exceptions. Existing 320 px shell wordmark/credit overlap and crowded primary destinations remain for T080.
- CLIP r2 was reread before completion; no policy divergence. No live paid provider calls, publishing or push. Existing unrelated hook, bundle-size and historical spec warnings remain non-failing; T008's concurrent edits are excluded.
