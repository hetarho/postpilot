# T384 Clip progress distinguishes waiting, execution and media recovery
> st:done@260925 | ssot:ARCH-14 ARCH-16 ARCH-17 ARCH-20 CLIP-78 CLIP-79 CLIP-81 CLIP-86 CLIP-88 CLIP-96 CLIP-161 CLIP-162 CLIP-164 | base:ARCH@11 CLIP@44 | dep:T383

## goal
Show the actual media stage and actionable failures while keeping the current focused clip view and cancellation controls.

## acceptance
- [v] Preparation/render waiting and retry states have ko/en owner-facing labels; they do not show fabricated percentage advancement or claim that a disconnected worker is still rendering.
- [v] Reopening/polling the same active job restores its durable state, completed-work counts and cancellation availability; an internal media retry keeps the same job id.
- [v] Cancellation remains 취소 중 until the durable terminal acknowledgement, and completion already committed retains the completed result.
- [v] Worker-unavailable, total-timeout and retry-exhausted failures explain the next owner action through the existing FailureNotice/explicit continuation flow, preserving previous results and completed observations.
- [v] No hostnames, GPU model names, lease tokens or deployment controls enter the product UI, and the existing browser/server render choice and credit wording retain their meaning.
- [v] Backend stage/failure mapping tests plus FE polling, progress and ClipStatus tests cover waiting→running→retry→done, exhausted/offline, cancellation and reopen; ARCH-25 and relevant ARCH-26/generated-code checks pass.

## impl notes
- Add allowlisted stage strings prepare_wait, render_wait, prepare_retry and render_retry at the clip stage vocabulary and job reporting boundary. Keep ordinary prepare/render substages and existing public job status values.
- Progress waiting/retry states are indeterminate unless a real completed/total count belongs to the current operation; never infer a duration or queue position from lease age.
- Update `entities/generation-job` domain progress helpers and transport adapter, `entities/clip-project` owned i18n/failure catalog, and `widgets/clip-workspace/ui/ClipStatus`; pages remain composition only.
- Use existing CancelClipGeneration and polling contracts; do not add a user-facing worker status API or hardware selector. Backend worker liveness belongs to internal operations.
- Preserve source inspection, original playback and focused-view restrictions under CLIP-78. Test with the current mounted workspace lifecycle rather than only snapshots of strings.
- Reason translations follow the existing shared API reason mapper and owning slice resources, with exact ko/en key parity and unknown-reason fallback retained.

## result
- Media dispatch parks the parent with prepare_wait/render_wait in the same transaction. A guarded worker claim records execution; transient failure and lease reclamation record prepare_retry/render_retry on that same job. Boot reconciliation restores these durable stages without resetting an unchanged operation's progress.
- Added the four stages to the clip reporting allowlist and ko/en entity-owned mediaStage resources. Shared job progress helpers keep waiting/recovery indeterminate, preserve ordinary analysis counts and label an accepted cancellation as Cancelling until the durable terminal result. Existing transport mapping carries these fields without a new browser API or hardware controls.
- The existing failure catalog from T383 supplies actionable unavailable, timeout and exhausted messages. Mounted workspace tests prove that retries are explicit, prior results and completed observations remain accessible, cancellation controls follow durable state and a committed completion remains authoritative.
- Validation: SQLite/private-worker stage lifecycle and owned reporting tests pass; seven focused FE files/95 tests and the full 314-file/2,711-test suite pass. ARCH-25/26, lint/format/FSD/style/retirement/build, eight deployment tests, generated-code consistency and spec lint pass in an isolated task worktree. Existing unrelated spec freshness warnings remain outside this wave. This task changes progress reporting, not media output execution.
- No SSOT decisions, deployment topology, credit policy or browser/server render choice changed. No deployment was performed.
