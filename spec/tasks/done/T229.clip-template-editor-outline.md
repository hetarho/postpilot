# T229 The template editor drops the design step and edits the outline
> st:done@260918 | ssot:CLIP-42 CLIP-112 CLIP-113 | base:CLIP@33 ARCH@3 | dep:T227

## goal
Make the video-template editor name, description and one body draft shared by a block builder and 원문, with the outline's entries added, reordered and deleted in order and no design selection anywhere on the screen.

## acceptance
- [v] `CompositionDesignStep` and the `designReady` save gate are gone from `ClipTemplateEditor`; a template saves with no design chosen
- [v] the builder lists the outline in document order and adds, reorders and deletes entries across kinds — stage, intro/caption/outro text, badge, field, group, guide
- [v] `composition-skeleton.ts` retires with the preset rebuild: no `CompositionDesign`, `compositionSkeleton`, `compositionDesign` or `rebuildCompositionSkeleton`, and no caller left behind
- [v] ①'s own preset and caption-style previews still render (`ClipDesignSelection` imports `CompositionDesignThumbnail` today)
- [v] the editor preview draws the regions in the shared default presets and says in one line that the clip's own design decides the real shape
- [v] switching builder ↔ 원문 stays lossless over the one draft (TMPL-26's rule, unchanged)
- [v] ①'s answer control folds the project's own preset back into a field's maximum where that answer reaches a region slot (CLIP-117; T227 left the parser unable to know the preset)
- [v] tests: saving a body with no design attributes, adding and reordering entries, an outline round-tripping through both modes, and ①'s previews still rendering
- [v] ARCH-25 verify clean, FE self-audit per ARCH constraints

## impl notes
- `CompositionDesignThumbnail` must survive T227's retirement of `compositionSkeleton`: give it its own small preset-sample builder inside `entities/clip-template/lib` that takes the preset id directly, since ① is now its only caller and ① knows the preset.
- the preview's default presets are the shared defaults (intro b, outro e) — the same fallback `ProjectDesign.RegionPresets()` uses — so a template author and a project owner never see two different "defaults".
- the builder's row order IS the outline order: reordering writes the body through the existing `patchCompositionSource`/`ReplaceNode` path, never a second representation (TMPL-2's rule, which CLIP-42 inherits).
- parallel sessions share this tree: `frontend/src/app/providers/i18n/resources/{ko,en}/clips.ts` is also edited by T230 and T232 — stage explicit paths and expect to rebase that one file.

## result
- `ClipTemplateEditor`: the design step, the `designReady` gate and the preset-rebuild button are gone; a new template starts at the empty outline (`<clip version="1"/>`) so the builder and 원문 are there from the first keystroke.
- `CompositionBuilder`: every entry — regions included — is one row of the one sortable outline, reorderable, expandable and deletable; the palette adds 인트로 문구 · 자막 · 아웃트로 문구 · 표시 문구 beside the field, the group, the stage and the guide (`newCompositionNode` gained the four roles).
- `CompositionTextControls`: an intro or outro entry is a list of LINES with 줄 추가 / 줄 지우기 — no slot count, no excess error, no `design` prop.
- `composition-skeleton.ts` and `CompositionDesignStep` are deleted. `CompositionDesignThumbnail` moved to its own file with a preset-sample builder that takes the preset id directly, which is what ① wanted from it all along.
- `EmptyCompositionBody` (Go) drops its design attributes and its two empty region entries: a project with no template freezes an outline with no entry at all (CLIP-5, CLIP-112).
- `VideoTemplatesPage` stops showing "인트로 B · 아웃트로 E" per row — a template names no design — and the fake server stops seeding one.
- `clipFieldMaximum` (new, in `entities/clip-template/lib`) folds the PROJECT's presets back into a field's bound where its answer lands in a region line, and ① passes its own selection in. That closes the CLIP-117 gap T227 opened, and the Go parser stays free of a preset it cannot know.
- the preview draws in the shared defaults and `composition.previewHelp` now says the real shape follows the design the clip chooses in ①.
- tests: reordering and deleting a region entry through the builder, the palette's four text kinds landing in outline order, a template created and saved with no design on screen, the ①-side answer bound narrowing to intro A (8) versus intro B (9), plus the suites that already pinned the lossless builder ↔ 원문 round trip and ①'s previews.
- ARCH-25 green (249 files / 2067 tests, lint, fsd, style, build:web) and ARCH-26 still green after the `EmptyCompositionBody` change.
