# T074 Deterministic caption and video renderer
> st:done@260910 | ssot:CLIP-8 CLIP-9 CLIP-12 CLIP-13 CLIP-14 CLIP-15 CLIP-16 CLIP-17 CLIP-18 CLIP-27 CLIP-28 ARCH-21 ARCH-24 ARCH-32 ARCH-33 | base:CLIP@1 ARCH@2 | dep:T073

## goal
Render an approved edit plan into one stable MP4 with subject-aware crops, exact Pretendard copy, three visual styles, per-cut audio and restrained fades.

## acceptance
- [v] the repository carries a pinned official Pretendard Variable font file and its OFL license; rendering loads that file explicitly and disables system-font discovery
- [v] a pinned static `resvg` CLI is built from its official source into the production image and converts generated SVG copy plates to transparent PNG with `--skip-system-fonts` and the bundled font only
- [v] the renderer maps ratios to exact canvases: vertical 1080×1920, horizontal 1920×1080, square 1080×1080; video is cover-cropped around the plan's normalized focal point and never stretched
- [v] copy positions are `top|center|bottom` inside ratio-specific safe regions; the placement validator rejects a caption outside them and the planner's avoid region can move a caption only among those approved positions
- [v] styles are exact code-owned recipes: `clean` = lower translucent dark rounded card with white semibold copy, `diary` = cream rounded label with dark semibold copy, `emphasis` = large bold white copy with a dark outline; the optional accent is restricted to the T069 palette
- [v] text is XML-escaped, fit by measured glyph bounds, wrapped at grapheme boundaries to at most two lines and never replaced by generated pixels; text that cannot fit at the style's minimum size returns `CLIP_COPY_TOO_LONG`
- [v] each cut trims only inside its source, keeps original audio by default, applies volume from 0.0–1.0, synthesizes silence for a source without audio, and overlaps adjacent video/audio by a 200 ms fade without changing the requested final timeline length
- [v] output is MP4 with H.264, AAC when any source has audio, yuv420p, constant 30 fps, square pixels and fast-start metadata; duration is 15–90 seconds and differs from the validated plan by at most one frame
- [v] the render contract contains no background-music, arbitrary font, decoration, animation or generated-media field, and no direct-publish behavior
- [v] generated SVG and FFmpeg filter graphs have golden tests; a Docker integration test renders all three ratios and styles from rotated/audio/silent fixtures, extracts frames for PNG assertions and probes the final codec, dimensions, duration and audio volume
- [v] the current official Naver Clip help is rechecked at implementation; the fixture output is manually accepted by Naver's upload picker or any changed acceptance constraint is returned to `update-ssot CLIP` before code is marked done
- [v] ARCH-26 passes and the production Docker image completes the renderer smoke test as nonroot

## impl notes
- generate SVG in Go and rasterize it with `resvg`; it is static, dependency-free at runtime and accepts explicit font files, while FFmpeg `drawtext` makes the rounded-card and Korean layout recipes harder to hold pixel-stable
- use stable accent ids with a fixed initial palette: `coral #ff6b5f`, `amber #ffb23f`, `lime #b8d94a`, `teal #43c5b5`, `blue #5b8def`, `violet #8b6fe8`, `pink #e96aae`; `none` uses each style's neutral recipe
- represent time in integer milliseconds in the domain and convert to FFmpeg seconds only at the adapter boundary; floats must never become persisted edit-plan authority
- use filter-complex with explicit stream labels and one concat/xfade plan assembled from validated domain values; user strings never enter command arguments as filter syntax because captions are already PNG paths
- keep Naver validation read-only: T074 may test the file picker with owner help but never clicks a publish/submit control

## result
- Manual acceptance verified on 2026-09-10: the owner confirmed all three files accepted by Naver web and supplied a screenshot listing vertical.mp4, horizontal.mp4 and square.mp4, each at 0:15; no format/ratio rejection was reported and this agent performed no publish/submit action.
- Changed: bundled official Pretendard Variable v1.3.9 and OFL; pinned static resvg v0.48.1 builder with corresponding sources/notices; shared production/development font and rasterizer installation; FFmpeg PNG/transition/audio filters; render configuration and documented provenance in `backend/build/RENDER.md`.
- Changed: domain edit-plan validation, normalized focal/avoid placement and default original-audio gain; measured grapheme-safe SVG copy plates; sequential source-loader renderer with bounded trims, cover crops, audio mixing, exact-duration fades and final output probing; stable `CLIP_COPY_TOO_LONG` RPC/frontend translations.
- Verified: Go domain/adapter/config/RPC unit and golden tests; actual Pretendard glyph coverage and weight measurement; resvg transparent PNGs; malformed-plan, source-loader, cancellation and panic cleanup paths.
- Verified: production Docker nonroot `TestRenderSmoke` passes vertical/horizontal/square, all three styles, rotation, source audio gains 1/0.5/0, extracted caption pixels and fast-start output; additional 15017 ms silent/audio plans pass one-frame duration tolerance; `TestMediaSmoke` also passes.
- Verified: frontend 175 test files / 1371 tests, lint, FSD lint, style probe/style lint and web build pass under Node 24.18.0; backend gofmt/vet/build/test and race tests pass; agent gofmt/vet/build/test and shell syntax gates pass in a committed-HEAD snapshot, preserving the separate T043 session's dirty files.
- Verified: production and development Docker builds pass; protocol/SQL regeneration has no backend drift; skill checks, whitespace checks and spec lint pass; remote CI is not applicable because nothing was pushed.
- Existing nonblocking warnings: two React-hook warnings, web entry-chunk size warning and ten historical spec warnings; no failing check was waived.
- Official Clip and Naver TV help were rechecked on 2026-09-10; time limits cover these 15-second fixtures, but the documentation does not prove every ratio is accepted by the picker; links and distinctions are in `backend/build/RENDER.md`.
- Manual gate fixtures: `/tmp/postpilot-t074-picker.r1BMaN/{vertical,horizontal,square}.mp4`, generated from synthetic sources in the nonroot production image with networking disabled; extracted style PNGs and a README are alongside them.
- Manual gate: select each MP4 in the owner's Naver Clip upload picker and report any format/ratio rejection; never click publish/register/submit; return an actual changed acceptance constraint to `update-ssot CLIP` before completion.
- Base rechecked: CLIP@1 and ARCH@2 remain current; no SSOT decision was changed.
- Final post-confirmation verification: all frontend/backend/agent ARCH gates, protocol/SQL drift checks, skill/spec checks and the actual production Docker nonroot render/media smoke were rerun successfully after T043's commit; Naver acceptance is verified, not deferred or waived.
