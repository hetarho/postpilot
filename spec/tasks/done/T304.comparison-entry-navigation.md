# T304 Comparison entry navigation
> st:done@260921 | ssot:MODEL-44 MODEL-60 ARCH-14 ARCH-16 | base:MODEL@13 ARCH@6 | dep:-

## goal
- Return from comparison review to the actual entry point while keeping model-lab and writing navigation distinct.

## acceptance
- [v] Model comparison starts return to the matching stage's comparison form; history entries return to matching history even when a source post exists.
- [v] Editor result/retry links use a writing-group review route and return to the source post; post-list entries return to the filtered list.
- [v] Direct/reloaded URLs preserve context; legacy model-review URLs default to history, invalid origin values fall back safely, and a missing source post returns to the post list.
- [v] Loading/failure screens retain a usable return action; candidate selection, verdict, retry and adoption keep the shared existing behavior.
- [v] Route/integration tests, frontend gates and browser navigation checks pass in ko/en.

## impl notes
- Keep /ai-models/experiments/$id for model review, with validated from=compare for comparison-form entry and history as the default; preserve stage on the return link and select the matching model submenu.
- Add /posts/experiments/$id under the writing group. Its validated from=posts context returns to /posts with q/status; otherwise use the experiment's source post, falling back to /posts when unavailable.
- Export thin ModelExperimentPage/PostExperimentPage wrappers from the existing page slice and share its review composition; no duplicated candidate, decision or mutation implementation.
- Update every editor/result/retry entry and post-list pending-result link. Keep source ownership and all server behavior unchanged; route context is presentation metadata only.
- Explicit destinations must work without history.back(), referrer or mutable in-memory origin, including a new tab, reload or authentication redirect.
- Cover write/observe model-start returns, post-backed history, editor/list entries, filtered list restoration, invalid/default context, guarded access, browser back/reload and shared candidate B selection. Preserve unrelated uncommitted work.

## result
- Added distinct model/writing route wrappers in pages/model-experiment around the shared ExperimentReview composition. Model results carry validated from=compare when started in the lab; history is the default for existing model URLs. Writing results live at /posts/experiments/$id, with from=posts preserving the originating list's q/status filters.
- Updated app/routes/models/posts/ContentGroupLayout, lab start links, editor result/retry links, the generation dock's pending-result link and post-list result rows. The correct primary and group menus remain selected; all read states offer a return action. No comparison mutations, server contracts or account data changed.
- Tests cover 20 entry-context cases plus the writing navigation route, all three lab starts, existing editor retry, filtered list restoration, invalid/default context, loading/failure exits, authentication redirect and the shared candidate-B selection.
- Node 24.18.0: full frontend suite 2,292 tests/289 files passed; pnpm lint (including format:check), lint:fsd, lint:style:probe (61 cases), lint:style and build:web passed. Browser checks passed 24 entry/width/locale combinations with reload, browser back, new tabs and no navigation-triggered mutations. Spec lint has no errors (17 existing warnings); build retains the repository's mixed static/lazy page-import warnings.
- MODEL@13 and ARCH@6 rechecked before completion. Earlier uncommitted T302/T303 changes and unrelated pending tasks remain intact.
