# T080 Grouped writing and video navigation
> st:done@260910 | ssot:CLIP-3 THEME-23 THEME-26 THEME-29 ARCH-13 ARCH-14 ARCH-15 ARCH-16 ARCH-20 ARCH-24 | base:CLIP@2 THEME@8 ARCH@2 | dep:T070 T072

## goal
Reshape primary navigation into visible 글 and 영상 groups without hiding existing destinations or changing their URLs.

## acceptance
- [v] the ordinary primary destinations become 글 (`/posts`), 영상 (`/clips`) and AI 모델 (`/ai-models`); master additionally keeps 발행 도구, while account, credit/plan, billing, locale and theme controls remain in their common header/account locations
- [v] one pathless writing-group layout shows address-backed `TabLinks` for 글 · 말투 · 글 템플릿 · 지침 across `/posts`, `/voices`, `/templates` and `/guidelines`, including their new/detail descendants
- [v] one pathless video-group layout shows address-backed `TabLinks` for 클립 · 영상 템플릿 across `/clips` and `/video-templates`, including their new/detail descendants
- [v] a primary group remains active on every child URL, browser back/forward restores both active levels, direct links keep working and no existing route path redirects merely to fit the grouping
- [v] phone bottom navigation has three ordinary or four master targets at the coarse-pointer floor; laptop header and desk rail render the same top-level set and every child destination remains visible in its group tabs rather than a menu
- [v] group tabs scroll horizontally rather than crush at 320 px, stay in normal page flow, and do not create a second scroller or second dock
- [v] the logo/home link still targets `/posts`; non-group screens such as `/plans`, `/billing` and admin keep the primary destinations visible with none falsely marked current
- [v] the single destination registry remains the source for phone/header/rail and one group registry remains the source for each subnav; labels and icons have ko/en parity
- [v] router and layout tests cover every child/detail URL, active states, master filtering, direct navigation and all three responsive render shapes
- [v] ARCH-25 passes

## impl notes
- add `WritingLayout` and `VideoLayout` under `app/routes`; they compose `Outlet` and shared `TabLinks`, while page slices remain unaware of navigation hierarchy
- determine primary active state by route matches for the group's child ids, not by string-prefix guesses (`/voices` is in 글 without sharing `/posts`)
- keep `/ai-models` as the one model-choice destination outside content groups; `/admin/models` remains inside the existing master admin layout and is not a duplicate primary entry
- T080 may land as soon as T070/T072 provide real destinations and can run in parallel with the generation/rendering chain

## result
- Added pathless WritingLayout and VideoLayout with one text-tab registry and a shared content frame; public addresses, redirects and page behavior are unchanged. Existing page parameter/search reads no longer depend on shell layout ids.
- Extracted one primary destination registry and matched-route activity for all three shell shapes: posts, clips and AI models, plus publishing tools for master. Admin, account, plan and billing destinations remain common and never falsely select a content group.
- TabLinks supports descendant activity without changing its default exact sibling-tab behavior. Writing/video tabs stay in document flow and scroll horizontally at narrow widths; the page remains the only vertical scroller and retains its single action dock.
- Fixed the previously recorded small-header brand/credit overlap with nonshrinking branding and a wrapping account cluster. Laptop navigation has its own header row and the shared header-offset token matches its height; the desk keeps the original rail. Short English phone publishing copy preserves the full accessible name and the fixed bottom-bar clearance.
- Verification: 32 new matched-navigation scenarios cover every content child/detail address, all three primary render shapes, master filtering, non-group screens and browser history; existing localized route and correction tests pass. Node 24.18.0, 182 FE files / 1443 tests; ARCH-25, backend and agent gofmt/vet/build/tests, agent shell syntax, remote proto/SQL regeneration with zero diff, skill sync and spec lint all pass.
- Isolated Chrome synthetic-RPC checks pass 144 combinations: writing/video, ordinary/master, ko/en, day/night and 320/360/390/430/640/768/900/1024/1280 px. Asserted one visible primary nav, three/four targets, matched selection, no overflow or brand/credit overlap, 44 px coarse controls, 16 px phone fields and exact header/bottom-nav offsets. Settled screenshots of all three shapes were visually reviewed; no browser exceptions.
- Freshness rechecked at CLIP@2 THEME@8 ARCH@2; no SSOT divergence. No paid provider calls, publishing, push or deployment. Existing unrelated hook, bundle-size and historical spec warnings remain non-failing; concurrent T008 edits are preserved and excluded.
