# T248 확정하기 opens the finalization dialog
> st:done@260918 | ssot:CLIP-75 CLIP-109 CLIP-152 | base:CLIP@38 | dep:T247

## goal
확정하기 opens one confirmation dialog that states what confirming ends, lists the unresolved notices and carries the confirming action, and it stands only while a render matches the current plan.

## acceptance
- [v] pressing 확정하기 flushes pending saves, then opens a dialog rather than finalizing
- [v] the dialog states that confirming deletes the originals and ends editing, lists every unresolved notice with the cut or caption it names, and carries the confirming action beside a way out
- [v] a stale or absent render explains that 렌더하기 is required first, said where the owner presses, with no dialog opening
- [v] every other refusal (busy, already finalized, invalid plan, unavailable) is said in the same place and opens no dialog
- [v] a render of either kind satisfies the match
- [v] confirming from the dialog finalizes and opens ③, exactly as before
- [v] no finalization copy and no notice list stands in ②'s page
- [v] vitest: 확정하기 opens the dialog with the notices and does not finalize; the dialog's action finalizes once; an absent render keeps the dialog shut and names 렌더하기
- [v] verify: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- `Dialog` is `Sheet` with the confirm shape and stacks over an open item sheet, which is the right order if the owner confirms with one open
- `FinalizeClipNotices` becomes the dialog's body: `finalization.notice`, the `ClipNoticeList` with `withTargets` and the refusal line; `FinalizeClipAction` stays the dock's button and gains the opening behaviour
- the flush happens BEFORE the dialog opens, not on confirm, so the listed notices are the saved plan's; the confirm path then calls `action.confirm()` unchanged
- `action.uncertain` and `action.failure` keep their place beside the button — they are reasons a dock may carry (THEME-39) and must be seen without opening anything
- `finalization.refusal.stale_render` and `missing_render` are re-worded to name 렌더하기; add `finalization.dialogTitle` in ko/en

## result
- `useFinalizeClip.prepare` flushes settings and plan edits, then refreshes the owned project before the confirmation opens. The existing guarded, revision/result-bound `confirm` and ambiguous-write reconciliation remain the finalizing path.
- `FinalizeClipAction` opens `Dialog` with the saved plan's consequences and `ClipNoticeList withTargets`; only its confirming action finalizes. Missing/stale renders and all other refusals stay beside the dock button, as do failures and reconciliation controls. Both render kinds qualify. Removed the standing finalization copy from the correction workspace and page; localized the dialog title and render-first refusals.
- Tests cover flush ordering and duplicate opening, stale/absent renders, all refusal labels, targeted notices, cancel above an open item sheet, both render kinds, single finalization and lost-reply recovery. Frontend: 254 files / 2,100 tests passed, plus lint/format/FSD/style/build. Backend and agent gofmt/vet/build/tests, agent packaging syntax, deployment tests, generated-code drift and skill/spec checks passed. Chromium exercised Korean/English confirmation and completion at 360/900 px (four cases).
- No SSOT deviation. Existing editor hook and bundle-size warnings remain outside scope.
