# T216 ① arranges the sources and hints the writer
> st:done@260917 | ssot:CLIP-136 CLIP-123 CLIP-121 CLIP-47 CLIP-55 | base:CLIP@31 ARCH@3 | dep:-

## goal
Let the owner order the sources in ①, carry that order to the writer, turn the source→item binding into an optional hint, and tell the owner what the instruction now directs.

## acceptance
- [v] migration 0058 adds `clip_source_leases.position INTEGER NOT NULL DEFAULT 0`; `GetClipSources` and `SourceBatch.Sources` load in position order, then confirmation order; a new batch starts in confirmation order
- [v] new RPC `ReorderClipSources(project_id, batch_id, repeated string source_ids)` persists positions for the whole batch and refuses a list that is incomplete, duplicated or names a foreign source
- [v] the flow request's `source_order` (T211) follows that order; a batch never reordered plans exactly as today
- [v] `ClipSourceStrip` supports drag reorder with a touch path and a keyboard alternative (move earlier/later on the focused tile with a live announcement), autosaving through `ReorderClipSources`
- [v] the per-tile item Listbox stays, labelled as a hint (ko "이 원본은 어느 메뉴인가요? (선택)", en equivalent) with help text that nothing depends on it; the instruction textarea's help text says it directs footage order, rhythm and what the captions say
- [v] tests: BE — reorder persistence, refusals, ordered load; FE — keyboard reorder and the autosave call; verify FE (ARCH-25), BE (ARCH-26), generated code (ARCH-28)

## impl notes
- leases are per batch (migration 0036 `clip_source_leases`); positions are per batch too, so a discarded batch takes its order with it
- CLIP-55 requires touch and keyboard alternatives for every drag; a small pointer-event implementation on the existing snapping strip is preferred over a new dependency, and if a library is chosen ARCH-33 applies (read the current docs first, install through pnpm, confirm the lockfile)
- T189's `useClipSourceBinding` keeps its shape; only the wording and help text change here — its semantics changed in T210/T211 (a hint the writer receives)
- files (agent map): entities/clip-project/ui/ClipSourceStrip.tsx · features/upload-clip-sources/ui/ClipSourcePicker.tsx · features/bind-clip-source-item/model/useClipSourceBinding.ts · features/edit-clip-project/ui/ClipProjectForm.tsx · entities/clip-project/api/sources.ts

## result
- files: migration 0058 · clip/store/queries/sources.sql + sqlc · clip/store/source_order.go, clip/source_order.go (new) · clip/rpc/sources.go · proto clip.proto + the three generated clients · FE entities/clip-project (model/source-order.ts new, ui/ClipSourceStrip, api/sources, index), features/upload-clip-sources (session, hook, picker), pages/clip/ui/ClipPage, shared/api/index, test/clips.ts, resources/{ko,en}/clips.ts · new tests: clip/store/source_order_test.go, entities/clip-project/model/source-order.test.ts, pages/clip/ui/ClipSourceOrder.test.tsx
- position starts at 1 and 0 means "never arranged", so `ORDER BY position, ordinal` gives a batch nobody touched exactly the confirmation order it had
- the drag and the two buttons share one meaning through `moveInOrder`/`reorderTargetIndex` — pure functions with their own tests, since jsdom has no layout to drag in; the handle is a grip, not the tile, so the strip still scrolls under a finger
- the reorder is refused while the batch is being consumed, and a refused list leaves the order untouched (tested)
- ①'s instruction help now says what it directs (order, rhythm, what the captions say) and the per-source item control says it is an optional hint nothing depends on
