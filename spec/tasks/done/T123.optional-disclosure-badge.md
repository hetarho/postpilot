# T123 Optional disclosure badge with symmetric header margins
> st:done@260912 | ssot:CLIP-15 CLIP-46 CDS-2 CDS-5 CDS-31 CDS-51 CDS-52 CDS-57 | base:CLIP@9 CDS@9 ARCH@2 | dep:T122

## goal
Allow independent disclosure visibility while aligning both ends of the header to equal video-edge margins.

## acceptance
- [v] Project checkbox persists independently of campaign type; legacy projects and clients keep the badge shown, and toggling invalidates an existing render and quote.
- [v] Generation and manual rerender freeze and apply visibility; hidden badges leave no empty plate, retain chips and pass the explicit visibility-aware verifier.
- [v] Header margins are symmetric in all ratios; vertical left/right are 96/984 while captions retain their anchors.
- [v] Backend, frontend, code generation and local CI checks pass; an actual rendered frame verifies shown/hidden header behavior without paid model calls.
- [v] Approved T122 plus T123 changes are ready for commit, excluding unrelated ideation changes.

## impl notes
- Add backward-compatible hide_disclosure boolean to project storage/RPC, default false; optional update preserves older clients. Keep the campaign type separate.
- Freeze visibility in approved generation and rerender inputs, include it in quote digests, and increment an existing edit-plan revision only on a changed visibility value.
- Expose explicit verifier visibility policy with legacy shown defaults; use header bounds for the badge and preserve caption-safe geometry. Omit badge SVG and manifest entries when hidden, without deleting chips.
- Commit and push after local verification; then confirm remote CI and deployment.
- Keep approved caption styles, timing, fonts and top inset unchanged. Use bundled fonts and local rendering only; paid model spend is $0.

## result
- Added migration 0045, optional update/create/read RPC fields, project checkbox and byte-free job snapshots; missing flags remain shown. Visibility participates in quote binding and render revision checks, with an active-job database guard.
- Vertical badge right edge is 984 while the chip starts at 96; other ratios retain symmetric margins. Nullable header badge/label views suppress both text and plate without removing chips; caption anchors and the 80 px top remain unchanged.
- Backend generation/rerender/persistence/quote/ownership tests and frontend save/reopen tests pass. Full backend and agent vet/build/test, 1,627 frontend tests, lint/format/FSD/style/build and reproducible proto/SQL code generation pass; spec has 11 pre-existing warnings.
- All production media cases were exercised: the initial vertical pixel assertion still assumed the caption-only safe region, so it was corrected to exempt only the verified badge box and that real vertical case reran successfully (34.54 s); all other media/profile/ratio cases passed unchanged.
- Rendered and inspected both visibility choices using all eight owner originals: 20 s 1080×1920 H.264 clips, 512 MiB / 2 CPUs / no swap / no network, no OOM. Shown total 192.47 s, hidden 147.45 s; concurrent local checks mean these are not comparative benchmarks. No model calls ($0). Videos, inspected frames, hashes and logs: `/Users/ihaelam/Downloads/postpilot-disclosure-review-lf30d6hx`.
- Owner-approved T122 typography/pace changes are included; unrelated source-observation ideation remains outside this commit. No additional design changes or plan deviations.
