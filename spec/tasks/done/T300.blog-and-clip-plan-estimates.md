# T300 Blog and clip plan estimates
> st:done@260921 | ssot:QUOTA-28 QUOTA-36 QUOTA-40 QUOTA-41 QUOTA-56 QUOTA-57 | base:QUOTA@17 | dep:-

## goal
- Separate per-item conditions from simultaneous model-level capacity comparisons for blogs and clips.

## acceptance
- [v] The plans page starts with blog/clip basis selection and a condition summary; one bottom-right floating control opens the conditions sheet/dialog at every width.
- [v] Blog conditions set characters/photos/videos; clip conditions set original count and finished length; each persists independently, safely bounded, without counts or model controls in the editor.
- [v] Every plan displays four ordered model-level capacity rows; absent or incompatible rates are explicitly unavailable and condition changes recompute without RPCs.
- [v] Server-published clip rates account for original analysis, flow and narration plus 1.5x token allowance, using assigned compatible model prices and a disclosed 60-second source assumption; actual reservations and settlement are untouched.
- [v] Price/grant/bonus/subscription actions remain correct, and tests cover pricing, eligibility, transport, persistence and responsive comparison behavior.
- [v] Applicable ARCH tests, lint, format, build, code generation and spec checks pass; browser reflow and floating-sheet behavior are verified.

## impl notes
- Add optional ClipEstimatorRates to EstimatorCombo in the proto; preserve existing wire fields and publish the source-seconds assumption on GetMyPlan even when no pair can be priced.
- Keep token assumptions in plan; derive clip rates for catalog pairs only when observer supports video and structured output and writer supports structured output, with no automatic model fallback.
- Clip assumptions: one 60-second original per observation call, 2000 prompt and 2000 observed output tokens; two writer calls with 10000/6000 base prompts, observed-source context carried to both, and 80 combined output tokens per finished second. All estimated tokens carry the existing 3/2 allowance; fixed per-call overhead does not.
- Persist the old blog storage shape unchanged for compatibility; use an independent clip storage key (default three originals and 30 seconds, bounds 1..20 and 15..90).
- The shared ladder accepts four result rows per offer; pages compose estimate functions from the plan entity. Reuse ActionBar and Sheet for a fixed right-aligned condition control above phone navigation.
- Only QUOTA is consumed; pending Wanted Sans deltas in CDS/CLIP/THEME belong to the existing separate work.

## result
- Resumed on 260921 from WIP checkpoint cdc9745c: the implementation stood, so this pass completed verification and closed the one open follow-up.
- Responsive follow-up RESOLVED. The 320px/200%-text overflow reproduces reliably and is `widgets/plan-ladder/ui/PlanLadder.tsx`: the card header holds a `shrink-0 size-12` glyph plate beside a `Badge`, which is `shrink-0 whitespace-nowrap` by design, so on one line the pair fixed the card's min-content at 341.7px; a grid item's automatic minimum size then floored the single phone column there inside a 256px container. The row now carries `flex-wrap`. Measured in Chrome at 320px with browser text size 200% (root 32px): document scrollWidth 382 -> 320.
- Changed: `widgets/plan-ladder/ui/PlanLadder.tsx` (one `flex-wrap` + why), `pages/plans/ui/PlansPage.test.tsx` (pins the wrapping row and the chip's by-design nowrap). Nothing else moved from the checkpoint.
- Browser verification (Playwright + system Chrome, CDP `Page.setFontSizes` for the reader's text size), signed in, `/plans` at 320/390/1280px x 100%/200%, both bases, sheet open: no horizontal overflow in any combination. The floating trigger stays bottom-right at every width (1120,844 at 1280x900); the overlay is a bottom sheet below `md:` and a centred 448px dialog above it; the sheet's body is the one thing that scrolls (888 over 335) and both clip sliders are reachable.
- Gates: ARCH-25 (2252 FE tests, lint, prettier, steiger, lint:style, build:web), ARCH-26, ARCH-27, ARCH-28 (buf and sqlc regenerate with no diff), ARCH-30, and `haeram-spec-creator lint` with no errors.
- Out of scope, untouched: `/about` at 320px/200% still overflows (scrollWidth 565) from the marketing header's own row, and the ladder nested in its padded stage stays ~38px over a 192px column because a Badge never wraps its label; the same fix already took that column from 341.7px to 229.7px. Worth a task against MKT/THEME, not QUOTA.
- Live billing price application stays out, per the checkpoint's instruction; no SSOT deviation, QUOTA r17 consumed as written.
