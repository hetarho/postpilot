# T254 ② runs the browser render with its own progress and cancellation
> st:done@260918 | ssot:CLIP-156 CLIP-79 CLIP-34 CLIP-152 | base:CLIP@38 | dep:T253 T247

## goal
Choosing the browser kind runs the render inside ②, showing real progress across the encode and the store, cancellable there, and never the focused job view.

## acceptance
- [v] a browser render shows its real progress in ②, covering the encode and the store that completes it
- [v] it is cancellable in ②, and cancelling stops the work and leaves the plan and the previous result standing
- [v] the focused job view never opens for it, and the settings, steps and correction controls stay where they are
- [v] leaving the page ends the render while the project keeps its plan and its previous result
- [v] a server render keeps its focused view and its own cancellation, unchanged
- [v] the page stays interactive while the render runs
- [v] vitest: the browser render reports progress through both phases in ②; cancelling stops it and preserves the previous result; unmounting ends it; the focused view never opens
- [v] verify: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- CLIP-126 closed at r37 with no wall-clock committed: the progress names the stage it is on — encode, then store — and never an estimate, a remaining time or a slow-run state of its own; a long render is a slow success
- the render runs in the Worker T251 set up; ② holds a handle carrying progress, a cancel and a promise, and the Worker is terminated on cancel and on unmount
- progress is one scale across both phases — frames encoded, then bytes stored — so the owner sees one bar rather than two that each restart; `ProgressBar` is determinate here because both totals are known
- this render is NOT a `generation_jobs` row, so `useClipGeneration`'s `busy` and the focused-view branch in `ClipPage` must not see it; it is local state beside them, and CLIP-78's view stays for server jobs alone
- the dock's 렌더하기 (T247) is where the kind is chosen; this task gives that choice its browser path and leaves the server path untouched
- leaving the page ending the render is CLIP-156's own decision, so no leave-confirmation dialog is added for it


## result
- `render-clip-browser/api/run-render.ts` admits the saved revision before preparing assets or touching originals, shares one original cache between the two workers, and consumes their tracks through T253's direct store. A failure stops the sibling worker and releases assets, packets, original blobs and playback decoders; a late admission is cancelled even after navigation. If durable completion won before a cancel, the stored project is retained.
- `useBrowserRender` owns the page-local controller and flushes settings and correction edits before reading the exact saved plan. It cancels on unmount and pagehide, refreshes the full server projection after every terminal outcome, and never enters the job queue or upload-attempt ownership path. `ClipBrowserRenderStatus` places a determinate scale and cancellation beside the existing correction dock, with English/Korean stage and terminal messages. Frame progress covers the encoding portion; transferred bytes cover storage, which remains short of complete until promotion succeeds. The scale is not a time estimate.
- `ClipPage` now executes the chosen browser kind and retains the server path and focused-job branch. The settings, step tabs, editor and reference access stay in place; writes that could invalidate an in-progress plan are disabled while navigation and preview remain interactive. Browser verdict notices and failures stay by its controls. Changing steps keeps the run; leaving the page ends it without a new leave-confirmation dialog.
- Migration 0067 and `CancelClipBrowserRender` serialize cancellation with result promotion. An owner-scoped accepted cancellation fences subsequent reservation, verdict and completion calls while keeping the prior plan/result. Retrying cancellation is idempotent; a completion that already won is never undone.
- Tests cover both progress phases in the real page, navigation while rendering, cancellation during encode and upload, unmount, failed storage, late admission, sibling-worker failure, cleanup and both cancellation/completion orders. Existing server lifecycle tests continue to pass. The full frontend suite passes 270 files / 2,156 tests; backend/agent checks, lint, FSD, style probe/style lint, formatter and web build pass with the existing DraftEditor dependency and bundle-size warnings only.
- A real Chromium run used the application page with a local Connect fixture and an HTTP object-store fixture. It encoded 594 frames, remained interactive across step changes, PUT the MP4 directly, and exposed only its stored HTTP download after completion. ffprobe found 1080×1920 H.264 High at 30/1 and AAC-LC at 48 kHz, both exactly 19.8 seconds. Two further runs were stopped by the cancel button and navigation; neither completed a stored result. The temporary harness's initial transport-signature error was fixed outside the repository.
- Remote protobuf generation succeeds, and exact-version local regeneration plus SQL regeneration show no drift. Deploy tests and skill synchronization pass; the real-binary image media-smoke gate also passes; spec lint passes. No SSOT behavior changed; CLIP r38's caption selection and quote delta is unrelated to this task.
- Removed admission-age limits from upload and completion: a two-hour encoding regression test still stores successfully. The existing orphan sweep alone applies its storage-object-age grace period, and it now claims a browser orphan through the same serialized cancellation gate as promotion before deleting it. Completion winning protects the file; deletion winning fences a late completion or PUT. This closes the deletion race without a render wall-clock under CLIP-126.
- Orphan cleanup additionally covers a delayed PUT recreating an already superseded completed file: the current result remains referenced, while the old completed marker cannot retain the recreated orphan forever.
- Final verification: the orphan/cancellation regression set and the complete backend format/vet/build/test gate pass after the cleanup fencing change. ARCH r3, CLIP r38 and CDS r23 remain current; all required local checks are complete.
