# T120 Align overlay geometry and create a shorts SVG example
> st:done@260912 | ssot:ARCH-24 CLIP-12 CDS-12 CDS-28 CDS-29 CDS-31 CDS-47 CDS-48 CDS-57 | base:ARCH@2 CLIP@7 CDS@6 | dep:T119

## goal
Align the existing overlay grid and deliver an opt-in, file-based shorts design rendered from all eight owner originals.

## acceptance
- [v] Badge and information share the header row; actual role-specific glyph bounds centre text and reserve space for long disclosures without collision in all ratios.
- [v] Vertical caption and card geometry use the same LEFT/RIGHT midpoint; frontend design constants, golden fixtures and documentation agree.
- [v] A separate example catalog draws concise dark labels, bold white type and lime details through SVG assets; default bindings remain unchanged and no provider is called.
- [v] A curated 20 s example uses all eight originals, grounded captions and no duplicate caption beneath the intro/ending title; real PNG/video inspection confirms aligned text, legible disclosure and clean composition.
- [v] Targeted geometry/glyph tests, ARCH checks, production media/release gates and the original-footage example pass; retain the local MP4 and file-only preset sources for review at no more than $0.10 external model spend.

## impl notes
- T119 was committed first as d1c3770 at the owner's request. This follow-up is an explicit design example plus the requested alignment correction, not automatic rollout of an unreviewed sample theme.
- Keep the SVG catalog API and file/view contracts. Change shared geometry and measurement where pixels/manifest must agree; do not fake placement by moving text outside its measured slot in SVG.
- Use a non-default example directory loaded through CLIP_OVERLAY_DIR. Preserve actual font metrics and disclosure text; static artwork is vector markup, without raster-generation calls or extra runtime dependencies.
- Curate the demonstration locally from observed footage; example titles are editorial sample copy, not claims about a merchant name, price or geographic location. Keep the supplied source files read-only.

## result

- CDS r6 and the mirrored design JSON align the vertical centre at x 492, card edges at 96–888 and header tops at 290/112/112. `badgeAndChips` measures each text role with its own weight/tracking, remeasures truncated values, reserves the disclosure width, and shares the row height. The SVG view accounts for glyph bearings and centres visible glyph bounds. Geometry tests cover long disclosures across all ratios; the full-width vertical caption now correctly fits its safe area.
- `backend/examples/overlays/shorts-editorial/` provides four optional SVG presets (caption, header, opening, ending) through the existing catalog. The example uses white outlined captions, compact aligned information/disclosure, unboxed titles and a lime accent. Default embedded bindings remain unchanged. `shorts_example_test.go` curates all eight originals into 20 s, leads with the active cooking shot, reframes the brief sideways cut toward the food, and omits duplicate text in the two title windows. This is an explicitly requested visual sample, not a new default theme or a verified merchant identity.
- Two real renders were inspected; the final sample removes the remaining ending panel and caption backgrounds. The final pre/post-render manifests have zero layout collisions. Output: 1080×1920, 30 fps, H.264 High / AAC 48 kHz stereo, 20.000 s, 23,331,902 bytes, SHA-256 `91691ca670a331e302b0f7cd69c278b096e2e6020c2f0e690e1ce99cf49e07b3`. Rendering took 122.571 s (149.67 s including source probing/harness) with 512 MiB / 2 CPU / no swap / no network; OOM and OOM kills were zero. Both visual iterations used $0 in external model calls.
- Private review files are in `~/Downloads/postpilot-shorts-svg-review-jczalhd6/`: final MP4, editable `svg-preset/`, actual filled SVGs and rasterized PNG plates in `rendered-overlays/`, plan JSON, frame previews, verification JSON/log and the first draft. Original footage was mounted read-only and was not uploaded. Visual approval remains with the owner.
- Verification passes: targeted geometry tests and deliberately updated goldens; backend gofmt/vet/build/full tests plus final clip tests; agent gofmt/vet/build/tests and packaging syntax; FE 200 files / 1,613 tests on Node 24.18.0, lint/format/FSD/style probe/style/build; isolated proto/SQL generation is unchanged; skills sync/spec lint. Production media/profile/render gates pass (`postpilot-api:t120`), and all 27 isolated release scenarios pass (`postpilot-clip-release:t120`, 1 GiB / 2 CPU). An old assertion that a full-width centred caption must fail was updated to the new shared midpoint, retaining the one-pixel overflow check. Existing hook/chunk and historical spec warnings remain non-blocking.
- Completion base is current at ARCH@2 / CLIP@7 / CDS@6. No dependency, RPC, schema, paid provider call or deployment was introduced. T119 was committed as d1c3770 before this follow-up; this task's alignment and review assets are committed separately under the same request.
