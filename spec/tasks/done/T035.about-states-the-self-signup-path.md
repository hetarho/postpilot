# T035 /about states the self-signup path: Get started as the one CTA, Log in as the quiet link
> st:done@260908 | ssot:MARKETING-6 MARKETING-11 MARKETING-16 MARKETING-4 MARKETING-15 MARKETING-2 AUTH-30 THEME-18 | base:MARKETING@3 AUTH@2 THEME@4 ARCH@2 | dep:T030

## goal
Move the public page's one filled CTA to `시작하기 / Get started` leading to `/signup`, keep `로그인 / Log in` beside it as a quiet link, and rewrite every sentence about how a person gets an account to the path that now exists.

## acceptance
- [v] `AboutHeader` renders exactly one filled CTA — a router `Link` to `/signup` with the `cta` button contract and the accessible name `시작하기 / Get started` — and `로그인 / Log in` as a quiet `label`-styled link beside it; the login link alone keeps handing `redirect` through (`isInAppPath`, MARKETING-2) because `/signup` takes no redirect; header order stays CTA → quiet link → the two preference menus viewport-side last; every control keeps the 44 px floor
- [v] `hero.access` (ko/en) states the self-signup path: an email address and a password open an account, the address is verified by mail before the first login, and Google sign-in is available — no sentence claims the operator makes accounts; `plans.assignment` keeps stating the operator assigns a plan and nothing here sells or upgrades (MARKETING-15 — BILLING has not shipped; T038 flips it)
- [v] the `marketing` catalog carries `header.getStarted` in ko and en (parity test), and no remaining key or sentence on the page says `가입 절차는 없습니다` / "there is no sign-up"; `AboutPage.test.tsx`'s copy fixtures and assertions are updated: one filled CTA → `/signup`, the login quiet link → `/login` with `redirect` passthrough, the access sentence, and the unchanged claims (publishing still operator-tier and in live verification, no `<form>`, no input, no third-party request)
- [v] the page still mounts with no Connect call and no session branch (MARKETING-1); the router test that asserts `/about` issues no request stays green
- [v] `pages/login`'s About link, the login form's failure behaviour and the guard are unchanged (MARKETING-3); the login test stays green
- [v] `spec/tasks` reference: MARKETING-4 discipline — the sentence about Naver publishing is not softened in this change
- [v] gates: ARCH-25 (`test` · `lint` · `lint:fsd` · `lint:style:probe && lint:style` · `build:web`)

## impl notes
- the CTA and the access sentence move together with the change that shipped signup (MARKETING-16); T030 is that change, so this task lands right after it and never before
- one filled CTA per view (THEME-18): the login link becomes the same quiet `typographyStyles({ variant: 'label' })` link the About link on `/login` uses, not a secondary button
- `/signup` is a link to a page that OWNS the form (MARKETING-6): the About page itself still contains no form or input
- keep the plans assignment sentence exactly as T024 left it; changing it is T038's job when a rung can actually be bought

## result
- Replaced the About header's filled login CTA with a single `/signup` Get started CTA, kept Log in as a quiet redirect-preserving link, and retained the preferences as the final controls. The compact header stacks on narrow screens so all controls preserve their 44 px target without horizontal overflow.
- Updated the Korean and English access copy to describe email/password signup, email verification before first login, and Google sign-in. Plan assignment and the live-verification Naver publishing statement remain unchanged.
- Expanded About and router coverage for CTA count, destinations, redirect handling, control order and sizing, localized copy, and the absence of forms, inputs, Connect calls, and third-party requests.
- Verification: frontend test suite (146 files, 1,138 tests), lint, format check, FSD boundaries, style probe/style lint, and production web build passed. Lint retains the pre-existing `GenerationActions.tsx` hook-dependency warning and the build retains its existing chunk-size warning. Spec lint still reports the repository's pre-existing long SSOT-ID/base-reference errors and historical change-log warnings.
