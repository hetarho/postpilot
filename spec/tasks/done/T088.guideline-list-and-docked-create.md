# T088 Guideline list with a docked create sheet
> st:done@260911 | ssot:GUIDE-20 GUIDE-25 GUIDE-5 GUIDE-19 THEME-24 THEME-29 ARCH-14 ARCH-20 ARCH-24 ARCH-25 | base:GUIDE@2 THEME@9 ARCH@2 | dep:-

## goal
Make `/guidelines` the list of saved rules with one docked `새 지침` opening the create surface, the way its sibling directories work.

## acceptance
- [v] `/guidelines` renders the title, its description, the saved list in injection order and nothing else standing — no create section and no form on the page
- [v] one `새 지침` sits in an `ActionBar dock="list"` at every width, full-bleed on a phone and right-aligned at its natural width from `sm:`, and opens the shared `Sheet` (a bottom sheet below `md:`)
- [v] the sheet holds the textarea with its live remaining count, the help line and the scope control, with the committing action in flow after the last field; 전역 is preselected
- [v] a successful create closes the sheet and the new rule appears in the list in server order; a refusal (over 300 characters, duplicate, account cap) keeps the sheet open with the catalogue message under the field and nothing is cleared
- [v] the empty account still shows the worked example as copy, with the dock present
- [v] one create surface exists at any width — no phone form plus a desktop copy — and the scope control is still the shared `entities/guideline/ui` one
- [v] existing behaviour is unchanged: text edit, whole-scope edit, delete with its frozen-text confirmation, the `(accountId, 'guidelines')` query keying and the load-failure notice
- [v] `GuidelinesPage` tests cover opening the sheet, creating, each refusal keeping the sheet open, the empty state and the dock at both phone and desktop widths
- [v] ARCH-25 passes

## impl notes
- replace `features/create-guideline/ui/CreateGuidelineForm.tsx` with `CreateGuidelineSheet.tsx` and model it on `features/create-voice/ui/CreateVoiceSheet.tsx` — the trigger owns the open state so there is exactly one instance and one overlay (VoicesPage is the reference for the dock plus sheet pair)
- keep every rule already in the form: 전역 default, the countdown `CharCount`, clearing only what was submitted, resetting the scope after a success, and rendering the mutation's message under the field
- the textarea must not be docked (a field inside a bottom bar sits behind the keyboard); the dock holds the trigger only
- new `guidelines` i18n keys in ko and en: the trigger label, the dock's `aria-label` and the sheet title; drop the retired `page.new` section heading
- the page keeps composition only — no domain logic moves into `pages/guidelines`
- do not touch the candidate section in this task beyond leaving it where it renders; T089 moves and rebuilds it

## result
- `features/create-guideline` now exports `CreateGuidelineSheet` (the retired `CreateGuidelineForm` is gone): a `cta` trigger that owns the sheet's open state, modelled on `CreateVoiceSheet`, with the textarea, its countdown, the help line and the shared scope control inside and the committing pair in flow after the last field. Every rule the standing form carried is kept — 전역 default, Unicode-scalar countdown, the mutation's message under the field — and a success now closes the sheet while a refusal keeps it open with the text intact.
- `GuidelinesPage` is composition only: title, description, the saved list (or the worked example), and one `ActionBar dock="list"` holding the trigger, with `flex flex-1 flex-col` on the page so `mt-auto` drops the bar below a short list. The `page.new` heading and its section are gone; `create.open`, `create.dockAria` and `create.title` are new in ko and en.
- Tests: one new case pins the shape (no region and no textbox until the trigger is used, `w-full sm:w-auto` on the trigger, `sticky sm:ml-auto sm:w-fit mt-auto` on the dock, one dialog, 전역 preselected, `300자 남음`, the submit disabled while empty); the three creation cases now open the sheet first, and the duplicate case asserts the sheet stays open.
- Verification: Node 26.8.1, 185 FE files / 1470 tests; `pnpm lint`, `lint:fsd`, `lint:style:probe && lint:style`, `build:web` pass (ARCH-25). Chrome at 390 px (day) and 1024 px (night): no horizontal scroll, the dock full-bleed on the phone and at its natural width on the desk, and the sheet as a bottom sheet below `md:` and a centred dialog above it — both reviewed as screenshots.
- The 후보 queue is untouched and still renders above the list; T089 moves it. No SSOT divergence, no backend or proto change.
