# T259 proto symbols and connect-query reach no further than entity api
> st:done@260920 | ssot:ARCH-17 ARCH-14 ARCH-13 | base:ARCH@5 | dep:-

## goal
Make `entities/*/api` the only place outside `shared/api` that names a proto service, message schema or `@connectrpc/connect-query`, for every domain except clip, and enforce it with ESLint.

## acceptance
- [v] an ESLint block over `src/pages/**`, `src/widgets/**`, `src/features/**` forbids `@connectrpc/connect-query` imports and any `ImportSpecifier` from `@/shared/api` whose name matches `/(Service|Schema)$|^Proto[A-Z]/` (`no-restricted-imports` + `no-restricted-syntax`); `pnpm lint` fails on a violation
- [v] the rule carries a temporary allowlist for the clip slices only (`pages/clip`, `pages/clips`, `features/*clip*`, `widgets/clip-*`), with a comment naming T262 as the task that removes it
- [v] every other page/feature/widget consumes hooks and domain types from an entity: `entities/session/api` (auth), `entities/voice/api`, `entities/post/api`, `entities/template/api`, `entities/guideline/api`, `entities/model-catalog/api`, `entities/model-experiment/api`, `entities/plan`/`subscription/api`, `entities/publish-job`/`publishing-agent/api` gain the query/mutation hooks the features used to build inline
- [v] `shared/api/index.ts` still re-exports the `*_pb` modules (entities need them) but its non-proto surface is transport, `AppFailure`, language and auth events only; a test pins that no page/feature/widget file names `Service`/`Schema` symbols (a `grep`-style vitest over the source tree)
- [v] behaviour unchanged: existing tests pass, including `test/*` fakes, and the ESLint warning count does not grow
- [v] ARCH-25 passes

## impl notes
- from review/arch-260919 F5 (non-clip half; clip is T262 after the entity split)
- hook shape: an entity exports `use<Noun>Query(args)`, `use<Verb><Noun>()` (mutation with `onSuccess` invalidation of its own keys) and `<noun>QueryKey(args)`; features pass callbacks/options, never descriptors
- the verb line (ARCH-14): a mutation hook no feature renders around is entity `api`; a feature keeps only ui + the model that is about the interaction (confirm dialogs, optimistic state)
- mappers proto↔domain stay in `entities/*/api` (ARCH-15), so `ProtoPlan`-style names disappear from features: the entity returns domain types
- `eslint-plugin-boundaries` already types elements in `frontend/eslint.config.*`; add element types `shared-api` and `entity-api` there so the connect-query ban is a `boundaries/external` rule, and use `no-restricted-syntax` for the symbol-name half
- do not touch clip slices beyond the allowlist: T260/T261 reshuffle them and T262 finishes the migration

## result
changed (frontend only)
- `eslint.config.js`: a new ARCH-17 block over `src/pages|widgets|features` — `no-restricted-imports` bans `@connectrpc/connect-query` and `@connectrpc/connect`, `no-restricted-syntax` bans an `@/shared/api` ImportSpecifier matching `/(Service|Schema)$|^Proto[A-Z]/`; the clip allowlist names T262
- `src/test/arch-proto-symbols.test.ts`: the same rule as a source-tree pin (3 tests), with the clip allowlist written out slice by slice so T262 deletes a visible list
- 45 slice files stopped naming a descriptor; the calls moved into the entity that owns the noun:
  `entities/session/api/session-mutations.ts` (signup, verify, resend, request/reset password, register email, change password) + `useSignInWithGoogle.ts` · `entities/voice/api/voice-mutations.ts` (create, delete, rename, restore, set-default, field override, profile restore), `voice-learning.ts` (learning actions, sentence feedback, rule actions), `voice-validation.ts` (start/poll/decide/retry for validations and rule comparisons) · `entities/post/api/useFinalizePost.ts`, `useGenerationOptions.ts` · `entities/generation-job/api/useStartGeneration.ts`, `useStartRevision.ts` · `entities/model-experiment/api/useStartWriteExperiment.ts`, `useStartModelExperiment.ts`, `useExperimentOwners.ts` · `entities/template/api/useCreateTemplate.ts`, `useDeleteTemplate.ts`, `useUpdateTemplate.ts` · `entities/model-catalog/api/useAssignEstimatorCombo.ts` · `entities/publish-job/api/publish-job-mutations.ts` (start/cancel publish, retry, cancel retained) · `entities/publishing-agent/api/agent-mutations.ts` (pair, configure, revoke) · `entities/image`: `useUploadHandshake` · `entities/guideline`: `useInvalidateGuidelineCandidates`
- transport-free cache targets so a page/feature can name a stale entry without a transport: `usePostQueryKey`, `useListPostsQueryKey`, `useVoiceProfileQueryKey`
- `entities/post/model/content.ts` gained `copyPostContent`/`postContentWith`/`blockWith`/`newBlock`, so the block editor and the content queue construct post content without naming `PostContentSchema`/`BlockSchema`
- new `@x` edges (cross-noun cache dependencies, ARCH-14): `post/@x/{voice,template,model-experiment}`, `guideline/@x/template`, `voice/@x/{generation-job,model-experiment}`, `plan/@x/model-catalog`, `model-catalog/@x/model-experiment` (+`getSelectionsQueryKey`)
- three page tests (voice-validation, voice-rule-comparison ×2) now fake the entity hook instead of connect-query + react-query; assertions unchanged except the two that named the removed transport plumbing (`decide` is asserted as `decide('B')`, the invalidate-not-called check moved with the invalidation into the entity)

deviations
- the ESLint rule skips `**/*.test.{ts,tsx}`, for the reason steiger skips tests: a fake backend is built from `createRouterTransport` + the service descriptor and `src/test/*` owns those harnesses. The source-tree pin states the rule for slice source, which is what a proto rename actually fans out to
- seven slices became segment-less barrels once their only hook moved, which `fsd/no-segmentless-slices` refuses, so they were deleted and their pages now import the entity directly: `features/{sign-up,reset-password,request-password-reset,verify-email}` (T267 expected the first three), `features/{create-template,edit-template,start-model-experiment}`. T267's F9 note ("homes are `features/edit-template/model/useTemplateDraft`") now means CREATING that slice with a model segment rather than adding to one
- the cross-noun invalidations the moved hooks carried (voice/template writes stale post lists; an experiment stales post + selections + voice) went with them into the entity through `@x` rather than staying at the feature. T267 still owns collapsing them into one `invalidatePostsDependingOn` entry — and note its stated signature takes no transport, which the keys need: an entry exposed as a hook (or taking the transport) is the shape that works

verify — ARCH-25 all green on Node 24.18.0: 272 files / 2175 tests pass · `pnpm lint` (eslint 0 errors, 1 warning — the pre-existing `DraftEditor` memo warning T263 owns, same count as HEAD) · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`. The build's 500 kB chunk warning is pre-existing: the entry chunk is 774.70 kB here against 776.71 kB on a same-host build of 22d39f2a (the comment in `vite.config.ts` claiming 352 kB is stale)

note for the owner — another session's commit 50f725f6 swept nine of this task's staged file renames into itself (`git mv` stages, and the index is shared), which left HEAD with moved files whose importers still pointed at the old paths; this commit is what makes HEAD build again
