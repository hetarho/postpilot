# T149 Add cut, split and fixed-rate editing controls
> st:done@260914 | ssot:CLIP-17 CLIP-49 CLIP-53 CLIP-54 CLIP-55 CLIP-56 CLIP-62 CLIP-67 CLIP-72 CLIP-97 CLIP-98 CLIP-99 CDS-59 CDS-60 CDS-62 CDS-64 CDS-65 CDS-68 ARCH-13 ARCH-14 ARCH-15 ARCH-17 ARCH-20 ARCH-24 ARCH-25 | base:CLIP@19 CDS@13 ARCH@2 | dep:T144 T148

## goal
Let the owner create footage-only cuts, split existing cuts and choose supported rates inside the preview-led correction workflow.

## acceptance
- [v] An observed scene offers an accessible add-cut action with source-range handles and precise source-time fields; the new draft cut uses injected canonical UUID identity and the T144 request provenance without editing the observation.
- [v] The selected cut offers playhead and precise-time split actions plus a fixed-rate control limited to the server-authorized choices; unsupported slow choices explain cadence refusal and cannot be submitted.
- [v] ADD creates a 1x text-free hard cut and SPLIT keeps parent content on the left and a text-free right cut, preserving server-owned binding/audio data and surfacing any now-invalid authored interval for correction.
- [v] Add, split, rate, trim, reorder and delete recompute transformed duration, selection, playhead and observation usage; each observed scene lists intersecting saved cut numbers and rates in plan order with source-time ranges.
- [v] Every new operation participates in the existing 100-transaction undo/redo model, including an accepted save; undo submits a new optimistic revision and never restores an old revision number or forged retired identity.
- [v] Save conflicts, invalid overlap, evidence bounds and timing failures preserve the complete local draft and identify the affected cut; no operation silently changes rate, range, text, association or source order.
- [v] Pointer operations have touch and keyboard alternatives, controls meet the existing touch target, and the preview remains visible with one document scroller at 320, 360, 390 and 430 px widths and an open mobile keyboard.
- [v] Reducer, timeline, observation viewer, correction workspace, page integration and browser tests cover add/split/remove/reorder/rate/undo, UUID stability, same-source adjacency, invalid text and ko/en accessibility.
- [v] Run applicable ARCH-25, ARCH-28 and ARCH-29 checks with deterministic protobuf output and dirty-workspace snapshot protection.

## impl notes
- Extend the existing reducer with `addCut`, `splitCut` and `rate` transactions. Generate `owner-<canonical UUIDv4>` through an injected identity function so tests are deterministic; creation provenance is sent only until the first accepted save.
- Compose observation UI and correction through a page-owned `onAddCut` callback; do not import one feature slice from another. Reuse or extend shared range controls only when the existing accessible primitives cannot express the source selection.
- ADD uses the chosen observed half-open range and server-projected focal/defaults. SPLIT keeps the original id/range start and all cut-bound text on the left, while the new right side has no copies/elements; do not duplicate claims.
- Keep explicit captions and phrases at their stored output milliseconds after any operation. Validation, not automatic repair, tells the owner when the transformed cut became too short.

## result
- Files: `proto/postpilot/v1/clip.proto` and generated Go/TS; `backend/internal/clip/cut_creation.go`, `rpc/{handler.go,observations.go}` and tests; `frontend/src/entities/clip-project/{model,api,index.ts}`, `features/correct-clip/{model,ui}`, `features/inspect-clip-observations/ui`, `pages/clip/ui`, ko/en clip resources and `shared/api/app-failure.ts`.
- ADD selects a precise observed source range with keyboard/touch handles and starts a text-free 1x hard cut; the scene projection supplies the same canonical focal as server admission. The page carries a newly used source's persisted sound permission from its retained lease when available; neither rate nor volume authors source permission.
- SPLIT keeps the left identity/content and gives the right an injected UUID, inherited transform/crop/gain and no text. The rate control lists all six rates and disables choices absent from the server cadence set. Cut and observation usage labels show rates; source-frame-preserving playhead mapping follows edits and undo.
- Creation flushes prior edits so its origin is an accepted cut. New-cut geometry/rate/gain controls wait for its first save because admission owns those initial values; invalid left text stays editable. Acknowledgment strips creation metadata throughout current/past/future snapshots, including concurrent typing, so delete/save/undo restores a known identity at a new revision.
- Range, cadence, overlap, scene-evidence and explicit-window failures preserve the draft. The RPC boundary now preserves a creation refusal's cut id and check under the existing invalid-input reason; the workspace identifies and selects the affected cut. Native text-free cuts no longer acquire a legacy empty-caption placeholder in the API mapper.
- Verification: FE 230 files / 1877 tests; lint, formatter, FSD, style probe/style and web build passed. Backend and agent format/vet/build/tests passed; agent installer shell checks passed. Docker proto/SQL regeneration matched a before/after SHA-256 snapshot; skill check and spec lint passed. Existing DraftEditor hook, large-entry-chunk and historical spec/stale-task warnings remain outside scope.
- Chromium: 16 ko/en × light/dark × 320/360/390/430 px touch cases passed add/save/rate/split/undo/redo and UUID/revision checks without page overflow or a second vertical scroller. A 420 px viewport check kept the preview and focused split field visible; this emulates keyboard space, not a physical-device keyboard. Synthetic evidence and hash index: `/tmp/postpilot-assembly-prv/`.
- No SSOT deviation. Unrelated concurrent edits in `backend/build/media-tools.sh` and `backend/internal/clip/media/rate_smoke_test.go` are preserved and excluded from this commit. T150/T151 remain for the next batch; no push or deployment.
