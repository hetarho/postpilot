# T117 Deliver clips despite text overlap
> st:done@260912 | ssot:CDS-52 CDS-55 CDS-56 CLIP-17 CLIP-27 ARCH-24 | base:CDS@4 CLIP@6 ARCH@2 | dep:-

## goal
Keep caption and furniture overlaps from blocking a reviewable video, including the eight supplied source videos, without hiding unrelated validation failures.

## acceptance
- [v] automatic layout, generation rendering, manual rerendering and final manifest validation accept overlap while retaining the text, placement and timing
- [v] strict diagnostic verification still identifies caption and furniture collisions; delivery verification continues through an overlap and refuses unrelated invalid size, disclosure, contrast or sequence
- [v] regressions cover overlapping chips across a fade, caption/card collisions and manual captions; the caller's plan stays unchanged when overlap is the only issue
- [v] the eight MP4s in the supplied local folder render successfully using production media binaries, with a saved reviewable MP4 and no private footage committed
- [v] backend checks, production media and release smokes, applicable CI checks and spec lint pass
- [v] render documentation describes overlap as advisory at both validation points

## impl notes
- Preserve strict `design.Verify` / `VerifyApproved` diagnostics; add a delivery verifier sharing the same checks but skipping V7. `clip.VerifyLayout` uses the delivery verifier so both pre-download and final checks share the policy.
- Do not catch an overlap error and return success: that would hide later sequence failures. Other checks still run through the shared verifier.
- Keep the existing repair ladder for other compiler-generated caption violations and the existing manual-plan rules for other checks. Overlaps keep their words for review through the existing correction workspace.
- Any live model verification must use an inexpensive model and remain within the owner-approved $0.10 aggregate ceiling; offline verification costs $0.
- Verify the local originals offline with a deterministic multi-cut plan that deliberately repeats chips across fades and places captions against cards. No paid provider call, remote account mutation, or media committed to the repository is needed.

## result
- behavior: `design.Verify` / `VerifyApproved` keep strict diagnostics; `VerifyRenderable` shares every check except V7 and still runs sequence checks after it. `clip.VerifyLayout` uses delivery validation at both `Layout` and `Render`, so caption/card collisions and crossfade chip collisions preserve the selected plan in automatic generation and manual rerendering. No RPC or frontend contract change.
- cause: chips on adjacent cuts occupy the same fixed region during their fade, producing a furniture-slot `plan_layout_overlap` that T116 cannot repair; manual plans and final verification also used the same blocking check. The new policy is CDS r4, not an exception swallowed at the first error.
- files: `backend/internal/clip/design/verify.go`, `design/overlap_test.go`, `render.go`, `media/render.go`, `media/overlap_test.go`, `media/layout_test.go`, `media/originals_smoke_test.go`, `backend/build/RENDER.md`, `spec/ssot/CDS.md`.
- regressions: 18 layout cases cover three ratios × compiled/manual × chip fades / caption-card / caption fades; strict diagnostics still find the collision and the delivered plan is unchanged. Additional cases prove overlap cannot hide anchor-step, size, disclosure or contrast failures. The ladder's drop case now uses an unrepairable anchor step instead of advisory overlap.
- verification: ARCH-26 and ARCH-27 pass; ARCH-25 passes on Node 24.18.0 (200 frontend files, 1612 tests); proto and SQL generation leave generated files unchanged; skill synchronization and spec lint pass (existing history warnings and T110's stale base remain). Production media-smoke passes; release-smoke passes all 27 scenarios at 1 GiB / 2 CPU with no external network.
- original-footage verification: all eight MP4s in `Photos 001 (6)` rendered offline through the production media binaries as both generated and manual plans, with intentional overlaps, 30.000 s, 1080×1920, 30 fps, H.264 High/yuv420p, AAC 48 kHz, 32,368,501 bytes each; both delivered manifests still diagnose overlap. The opening and ending output frames were inspected. No paid model call, account mutation or private media committed; external model cost $0, within the approved $0.10 ceiling.
- resources: the originals smoke passes at 2 GiB / 2 CPU in 683.77 s for both renders, peak 1,658,642,432 bytes, zero OOM events. An initial one-minute helper timeout failed the final encode; the fixture now uses production's existing 15-minute operation timeout. A separate 1 GiB original-footage attempt was killed by the OS; the original-footage success is not a 1 GiB guarantee. The existing synthetic release gate remains green at 1 GiB; production settings were not increased.
- artifacts: `/Users/ihaelam/Downloads/postpilot-overlap-test-xv3ackyy/generated.mp4`, `manual.mp4`, `README.md` and `verification.log`; deterministic test copy is used instead of a paid AI composition. Logs for backend, frontend, codegen, release and media checks are `/tmp/postpilot-t117-*`.
