# T024 The public plans copy tells the truth about what a plan decides
> st:done@260907 | ssot:MKT-5 MKT-4 MKT-16 QUOTA-2 QUOTA-19 QUOTA-7 | base:MKT@3 QUOTA@4 LANG@1 ARCH@2 | dep:T020

## goal
Fix the `/about` plans section: state the monthly credit grant as the only thing a plan decides, and move its figures to the raised ladder.

## acceptance
- [v] `marketing.plans.body` states that a plan decides the monthly credit grant and nothing else, and no longer claims a number of AI jobs per day, a daily or monthly spend allowance, or a range of selectable models; ko and en are rewritten together at full parity (LANG-7)
- [v] the table's figures become basic `220 크레딧` / `$2`, pro `575 크레딧` / `$5`, max `1,200 크레딧` / `$10`, with free `50 크레딧` / 무료 unchanged, and every tier keeps 등록된 모든 모델 (en: every registered model)
- [v] `AboutPage.test.tsx`'s duplicated `PLANS` table moves to the same figures ← MKT-5 makes that duplication the drift alarm, so it is updated deliberately in this task and not quietly
- [v] a new assertion in `AboutPage.test.tsx` fails the claim rather than the layout: for both locales the plans body contains none of 하루 · 일일 · 사용 금액 · 범위 (en: per day · daily · spend · range of)
- [v] the section keeps every MKT-6 property: no form, no input, no button, no link inside the plans region, `master` present only as prose, and the assignment sentence still naming the operator as the path that assigns a plan (MKT-15 — BILL has not shipped, so that sentence is still true and must not be pre-dated)
- [v] `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm build:web` pass

## impl notes
- files: `frontend/src/app/providers/i18n/resources/{ko,en}/marketing.ts` (`plans.body`, `plans.basic/pro/max.monthlyCredits`) and `frontend/src/pages/about/ui/AboutPage.test.tsx` (the `PLANS` fixture near the top and the A17 case that reads it)
- the recommended mark and the post estimate are deliberately left OFF the public table: MKT-15 permits them but the page is static copy with no GetMyPlan read, so carrying them would add a second place to keep in step for no claim the page needs ← the in-app comparison (T023) is where a visitor decides between rungs after logging in
- dep on T020 is about honesty, not code: shipping the copy before the ladder moves would put 220 on a public page while the server still grants 200, which is exactly the drift MKT-4 forbids
- no proto, backend or component change — copy and its test only

## result
- `i18n {ko,en}/marketing.ts`: `plans.body` now says a plan decides the monthly credit grant and nothing else, with model choice stated as identical for every plan; the table moves to 220 / 575 / 1,200 credits
- `pages/about/ui/AboutPage.test.tsx`: the duplicated `PLANS` fixture moves with the ladder (that duplication IS the drift alarm, MKT-5), and a new case fails on the CLAIM rather than the layout — the plans region may not contain 하루 · 일일 · 사용 금액 · 범위 (en: per day · daily · spend · range of), so the retired sentence cannot come back anywhere in that section
- the assignment sentence and the Login CTA are untouched: MKT-16 binds them to self-signup actually shipping, and it has not, so the operator path is still the true one
- the recommended mark and the post estimate are deliberately absent from the public table, as the impl notes decided
- verified: FE 1093 tests · lint · lint:fsd · build:web
