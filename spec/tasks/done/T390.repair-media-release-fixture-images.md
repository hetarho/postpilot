# T390 Build media release fixture storage from pinned upstream source
> st:done@260925 | ssot:ARCH-24 ARCH-31 ARCH-36 ARCH-38 | base:ARCH@11 | dep:T389

## goal
Make the disposable media release smoke runnable on a clean CI host by building its storage tools from pinned public upstream source.

## acceptance
- [v] MinIO server and client are built from fixed official source revisions using digest-pinned multi-architecture bases; no cached MinIO container image, discontinued binary endpoint or registry credential is required.
- [v] Fixture storage is prepared before expensive application release builds, including --skip-build reuse, and source/build failure stops the smoke clearly without creating test resources or affecting deployment.
- [v] Regression tests, the source-built storage image and both separate-process release layouts pass locally; required repository gates pass, with pushed CI/deployment results checked during delivery.

## impl notes
- Follow up the media-worker wave only: the authorized production rollout for T389 succeeded, but inspection of the previous run exposed an independent fixture image pull failure in scripts/media-release.py. Do not consume pending SSOT deltas or change production images, storage settings, render behavior or other task waves.
- Direct verification found Docker Hub references inaccessible, official Quay references returning 401 and dl.min.io binaries returning 410. Upstream now documents source-only distribution. Build one disposable MinIO/mc image using official Go module pseudo-versions tied to full source commits and digest-pinned multi-architecture bases. Do not substitute an unverified mirror or publish the fixture image.
- Preserve the existing private-storage initialization and API/worker isolation; retain source links/licenses in the fixture image. Prepare storage before application fixture builds, even when --skip-build reuses the API/worker. Test budget/failure ordering and exercise both private layouts with the actual source-built storage.
- User authorization includes fixes required to finish deployment, a separate commit per task, push and the resulting production rollout. No additional infrastructure migration is in scope.

## result
- Files: deploy/media/fixture.Dockerfile builds one disposable storage image from official MinIO commit 07c3a429bfed433e49018cb0f78a52145d4bedeb and mc commit 7394ce0dd2a80935aded936b09fa12cbb3cb8096 using fixed Go module versions and digest-pinned Go/Alpine bases. Source labels and upstream licenses remain in the image; it is neither published nor used by production.
- scripts/media-release.py always prepares storage before application image builds or disposable resources, including when --skip-build reuses application images. Budget validation also precedes builds. The storage credentials, private networks, resource limits, lease/recovery behavior and cleanup are unchanged; backend/build/RENDER.md documents the dependency build.
- Verification: the Linux arm64 source image builds and both binaries start; the full script rebuilds the current API/worker fixtures and passes colocated (185.19s) and remote (194.32s) release cases with real private storage, killed-worker reclaim, duplicate completion, cancellation and cleanup retry. Disposable containers, volumes and networks are cleaned up.
- Regression/repository gates: all 51 deployment tests pass, including dependency failure and budget/build ordering. gofmt/vet/build/full backend tests pass; Node 24.18.0 passes 328 frontend files/2740 tests, dev/lint/format/FSD/retirement/style/skill/build gates. Proto/SQL generation leaves no generated diff; Python compilation, git diff --check and spec lint pass (existing unrelated changelog warnings only).
- Delivery context: T389 commit 86066a52 was pushed and its production CPU rollout succeeded, including worker initialization, SQLite backup, API/worker health and browser/storage checks; its CI is green and external /health returns ok. T390 repairs the separate smoke's unavailable registry dependency; the subsequent push/deployment is monitored during delivery.
- Freshness/scope: ARCH@11 is unchanged and no pending SSOT delta is consumed. No production render behavior, storage configuration, GPU activation or second-host migration is changed. Official source-build guidance checked on 2026-09-25: https://github.com/minio/minio#install-from-source and https://github.com/minio/mc#install-from-source .
