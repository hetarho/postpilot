# T116 The caption repair ladder
> st:done@260912 | ssot:CDS-55 CDS-52 CLIP-17 ARCH-24 | base:CDS@3 CLIP@6 ARCH@2 | dep:-

## goal
Repair a compiled clip whose manifest fails a check by walking style, then anchor, then dropping the copy, so a design-system violation costs a caption rather than the clip.

## acceptance
- [v] `design.Verify` reports WHICH caption failed: it returns a typed error carrying the violation and the slot (cut index, copy index), and the furniture slot `-1` for a badge, card or chip; `Legible` keeps its boolean shape
- [v] a repair pass runs on a COMPILED plan (`EditPlan.Compiled()`) when `VerifyLayout` fails before any download: rung 1 the failing caption's style → 깔끔하게, rung 2 its anchor → that style's own default, rung 3 the copy is dropped; after each rung the plan is laid out and verified again, and the pass gives up only when the ladder is exhausted for every failing caption
- [v] a violation whose slot is furniture fails the render at once with the check named — it is a renderer defect, not a composition the ladder can repair
- [v] a plan a PERSON corrected is still refused with the failing check named and never silently moved (the CDS-52 constraint, today's `Compiled()` branch); only the compiler's own plans walk the ladder
- [v] the existing contrast fallback becomes rung 1 of this one ladder rather than a second mechanism beside it, and the post-sample verify keeps using it
- [v] every rung taken is recorded on that cut's `Composition.Fallback` (`contrast` stays, `style`, `anchor` and `dropped` join it) so step ② can say what happened
- [v] the verify AFTER the cuts are rendered stays a hard failure: the footage is already encoded there, and a repair would mean re-rendering — document that in `RENDER.md` beside the two verify points
- [v] tests: each rung repairs and the next one takes over when it does not · a furniture violation fails at once · a person's plan is refused rather than repaired · a clip whose every caption is dropped still renders its footage · the ladder is recorded per cut · `RENDER.md` documents the ladder
- [v] ARCH-26, ARCH-28 and ARCH-25 pass

## impl notes
- CDS-52 has always said "reapplies the fallback rules"; the renderer implemented that for V3 alone and failed on the other eight. CDS-55 r3 names the ladder so the rest is implementable — this task is the code catching up to the SSOT, not a new policy
- 깔끔하게 is rung 1 because its plate answers V2, V3 and V5 by itself (CDS-16's α ≥ 0.72 ink under white text clears the contrast floor over any frame), so the rung that fixes the most checks comes first
- V13 and V14 are sequence rules with no single failing element: report the LATER of the pair as the slot, which is the one whose style or anchor the ladder can change without invalidating what came before
- bound the pass: at most one full ladder per caption and one re-layout per rung, so a plan of N captions costs at most 3N layout passes and no downloads — the repair runs before any source byte is fetched, which is the whole reason the pre-download verify exists
- do not let the ladder touch the hook or ending card, the badge or a chip: they are the design system's own and CDS-45 fixes their placement

## result
- files: `internal/clip/design/verify.go` (`Failure{Check, Cut, Copy}` with `FurnitureSlot = -1`; every check names the element's slot, an overlap names the caption — the later one when two meet — and the V13/V14 sequence rules name the LATER of the pair; `Legible` keeps its boolean shape; the plain `Violation` still answers `errors.Is`) · `internal/clip/render.go` (`LayoutError` = the invalid-plan reason plus the slot, `Furniture()`; `VerifyLayout` returns it; `LayoutViolation(v, cut, copy)`) · `internal/clip/compose.go` (`Composition.Fallback` is comma-joined in order; `Recorded(step)`) · `internal/clip/media/render.go` (`repair` walks CDS-55's ladder — `rungStyle → rungAnchor → rungDrop` — re-laying out after each rung, at most one ladder per caption; it runs at the pre-download verify in `Render` and inside `Layout`, which now returns the repaired plan; a person's plan and a furniture failure return the named check untouched; the post-sample contrast fallback is `rung(..., rungStyle, "contrast")` — the same rung, the same clone-and-relayout, recorded as `contrast`) · `internal/clip/ai/service.go` (`CaptionSizer.Layout` returns the repaired plan and the composer stores it, so what is persisted and rendered is what verified) · `build/RENDER.md` (the two verify points and the ladder between them) · tests in `design/verify_test.go`, `media/layout_test.go` (style · anchor · drop rungs with the recorded step, a person's plan refused with the check, a furniture failure refused at once, every caption dropped still lays out, RENDER.md says so)
- rung 2 falls back to the CURRENT style's own default anchor and alignment (after rung 1 that is 깔끔하게's); rung 3 clears the copy to the empty caption `ValidCopy` already knows as "dropped", so a cut whose copy fell keeps its footage, its chips and the badge
- the ladder's bound: `repair` tries the next untried rung for the failing caption and stops when the ladder is exhausted for it, so a plan of N captions costs at most 3N layout passes and never a download; a rung that would change nothing (a caption already on 깔끔하게, already at the default anchor, already empty) costs no layout pass
- the verify after the cuts are rendered stays a hard failure (documented): the footage is encoded, a repair there would mean re-rendering
- the Docker `release-smoke` stage passes on this tree (28 scenarios), the ladder included in the image
- not in this task: nothing reaches the wire — `Composition` is in-process (the correction step reads it through the projection it already has); no proto or frontend change
