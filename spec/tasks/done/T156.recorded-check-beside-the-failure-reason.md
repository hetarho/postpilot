# T156 Show the recorded check beside the failure reason
> st:done@260915 | ssot:- | base:ARCH@2 | dep:-

## goal
Stop a failure reason from hiding the validation check the attempt already recorded, so a refused generation says which rule it broke.

## acceptance
- [v] every check the inspection can receive resolves to owner-facing text, including the `composition_*` and `plan_*` checks that today reach only the fallback chain
- [v] when a job carries a structured failure AND a known check, both are shown; when the check is unknown, today's wording is unchanged
- [v] behaviour otherwise unchanged — the existing inspection tests still pass
- [v] vitest covers a `CLIP_PROCESSING_FAILED` / `MODEL_OUTPUT_INVALID` job whose check is `composition_section_order`, asserting the reason and the order message both appear, in ko and en
- [v] verify FE `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- from review/clip-failure-visibility-260914 F1
- [ClipAttemptInspection.tsx:159](frontend/src/features/inspect-clip-observations/ui/ClipAttemptInspection.tsx#L159): `namedExplanation` reads only `observationChecks` and `renderChecks`, so every check resolved by the ternary chain below it lands in `explanation` alone, which `job.failure` then replaces. Fold the chain's check-keyed arms into one lookup so a known check is always a named explanation; leave the `phase`-keyed arms (`timeline_grow` / `timeline_shrink` / `timeline_total`) where they are, since they answer to phase rather than check.
- The messages already exist (`inspection.orderInvalid`, `inspection.sourceInvalid`, `inspection.evidenceGap`, `inspection.rangeInvalid`, `inspection.captionTimeInvalid`); this is wiring, not copy, except for checks that have no message yet. `SafeAttemptCheck`'s allowlist ([attempt_diagnostics.go:178](backend/internal/clip/attempt_diagnostics.go#L178)) is the full set the screen can receive — walk it and give every entry a message.
- The production case to keep in mind while choosing wording: job `546a0878` recorded `composition_section_order` at cut 2, and 순서 message must tell the owner it is the template's section order that the plan broke.
- No backend change: the check is already recorded, persisted and projected.

## result
- Changed ClipAttemptInspection.tsx and both clips catalogs: one named-check lookup covers all 97 SafeAttemptCheck entries; structured failure reasons remain visible beside the recorded check, with explicit template section/item order wording.
- Preserved unknown-check wording, phase-specific timeline guidance, historical observations and playback behavior; added ko/en allowlist coverage and regressions for CLIP_PROCESSING_FAILED and MODEL_OUTPUT_INVALID with composition_section_order at cut 2.
- Verified focused acceptance 27 tests and full frontend 232 files/1948 tests; lint, FSD, style probe/style, formatter, web build, Docker proto/SQL generation without drift, backend and agent format/vet/build/tests, installer shell syntax, managed-skill check and spec lint passed.
- Existing warnings: DraftEditor.tsx hook dependencies, web bundle size and 11 spec history/stale unrelated task warnings. No SSOT deviation; ARCH@2 rechecked. No push or deployment.
