# T114 The template screen authors the two numbers and the post shows what a template seeded
> st:done@260911 | ssot:TMPL-2 TMPL-6 TMPL-25 TMPL-47 TMPL-48 TMPL-49 POST-20 POST-63 POST-70 ARCH-13 ARCH-14 ARCH-15 ARCH-17 ARCH-18 ARCH-20 ARCH-21 ARCH-24 ARCH-25 | base:TMPL@6 POST@6 ARCH@2 | dep:T113

## goal
Let a template's 목표 글자 수 and 태그 수 be authored on the template screen as part of its one draft, and make the values a template seeds visible on the post the moment the picker applies it.

## acceptance
- [v] `entities/template` carries the two optional numbers on its `Template` type and its proto mapper, and `TEMPLATE_LIMITS`/`canSaveTemplate` stay about the three text fields — the numeric bounds come from the existing `POST_TARGET_LENGTH_MIN`/`MAX`/`DEFAULT` and `POST_TAG_COUNT_MIN`/`MAX`/`DEFAULT` in `shared/config`, with no new key and no literal in a component (ARCH-21)
- [v] the template screen edits both numbers above the composition, beside 이름 and 설명, each behind its own 사용 tick: unticking clears the value, ticking with nothing typed this session fills the default, and a value typed earlier in the session outranks the default when the tick returns (the 목표 글자 수 rule of POST-20, applied to both)
- [v] both numbers belong to the one draft behind the one 저장: they make it dirty, they are part of the leave guard, they are compared against the same saved baseline, and an out-of-range number blocks 저장 with its message under the field rather than being sent
- [v] `useCreateTemplate` and `useUpdateTemplate` send both numbers on every save, sending nothing for an unticked one, and the server's `TEMPLATE_NUMBER_OUT_OF_RANGE` renders through the existing template error message path
- [v] the builder, `원문`, the format guide (형식 안내 복사) and the grammar parser neither show nor carry the numbers — the `원문` round trip and the copied guide stay byte-identical to today (TMPL-49)
- [v] picking a template in the post editor makes the post's 목표 글자 수 and 태그 수 show the seeded values without a reload: `applyingSavedDraft` copies `targetLength`/`tagCount` from the response ONLY when the response's template id differs from the cached one, so an ordinary autosave of title/memo can never roll back an option save that landed while it was in flight
- [v] a seeded length turns 목표 글자 수 사용 on in the generation options form, a template with no length leaves the tick as it stands, and nothing on the post says a template wrote the value (POST-70)
- [v] Korean and English `templates` copy covers both fields, their ticks, their help and their range message, with locale parity green
- [v] tests: the template screen's two fields (tick/untick, default seeding, dirty, range block, save payload), the mapper's optional numbers, `applyingSavedDraft`'s copy-on-assignment-only rule, and a post-editor test proving the picker updates both displayed options
- [v] `pnpm --filter ./frontend test` (1562 pass) · `pnpm --filter ./frontend lint` (0 errors) · `pnpm lint:fsd` · `pnpm build:web`; `format:check` and `lint:style` fail ONLY on the parallel CDS session's in-flight files (see result)

## impl notes
- the two fields live in `pages/template`'s `Editor` beside `NameField`/`DescriptionField`, not in `entities/template/ui` — that component is the composition editor over `body` alone, and the numbers are not part of the body (TMPL-2)
- extend `Draft` with `targetLength?: number` and `tagCount?: number` and keep the input's own text in local state per field; the draft holds the parsed value, `undefined` meaning unticked, so the dirty comparison stays a value comparison against `draftOf(stored)`
- reuse the checkbox + number-field shape already written in `features/generate-post/ui/GenerationOptions.tsx` (`Checkbox`, `FieldLabel`, `TextField type="number"`, `FieldMessage`) rather than a second control; it is the same question asked in the other place, and the user should meet the same field twice
- do not add a 태그 수 tick to the post's own options form — the post's count is always concrete (POST-63); only the TEMPLATE's is optional
- `applyingSavedDraft` in `entities/post/api/useSavePostDraft.ts` must compare the template ids BEFORE it overwrites `post.template`, or the guard will always read equal
- no new query, no new invalidation: the seeded values arrive on the SavePostDraft response the picker already awaits, and `DraftEditor` already mirrors `post.targetLength`/`post.tagCount` into its local state when the server value changes
- the template list rows stay as they are — name, description, post count. The numbers are an authoring detail, not a thing to scan a directory by

## result
- `entities/template`: `Template.targetLength/tagCount` + the mapper carries them (absent stays absent)
- `features/create-template` · `edit-template`: both numbers on every save, `undefined` = 의견 없음
- `pages/template`: `Draft` carries both, `NumberDraft` holds the editing state per number (tick + text), one `NumberField` component with its own 사용 tick, the default filled on ticking, typed text surviving an untick, the range message under the field and the save gate extended
- `entities/post/api/useSavePostDraft.ts`: `applyingSavedDraft` copies `targetLength`/`tagCount` ONLY when the response's template id differs from the cached one — compared before `post.template` is overwritten
- copy: `templates.numbers.*` in ko/en (a separate tick label and field label per number, so the checkbox and the field never share an accessible name) · `TEMPLATE_NUMBER_OUT_OF_RANGE` in `shared/api/app-failure.ts` and both `errors` catalogs, with `max` optional because the length has no ceiling
- test doubles: `test/templates.ts` records and stores both numbers (update writes the pair every time, like the server) and `test/posts.ts` seeds on an assignment that CHANGES the template
- tests: `pages/template/ui/TemplateNumbers.test.tsx` (7), the seeding rule in `useSavePostDraft.test.ts`, a picker-seeds-the-brief test in `EditorPage.test.tsx`, and the two existing exact-payload assertions extended with the new fields
- NOT green in this tree, and not from this task: `pnpm lint`'s `format:check` flags three `pages/clip*` test files and `lint:style` flags `entities/clip-template/ui/CopyStylePreview.tsx` + the untracked `shared/config/clip-design.ts` — all the parallel CDS session's in-flight work (T105). Everything this task touched is Prettier-clean and adds no style escape. `DraftEditor.tsx`'s one eslint warning is pre-existing
