# T232 A project with no template stops reading as a missing one
> st:done@260918 | ssot:CLIP-5 CLIP-146 | base:CLIP@33 ARCH@3 | dep:-

## goal
Show 템플릿 없음 as plain metadata wherever a project carries no video template, and keep the re-selection copy for a project whose named template is gone.

## acceptance
- [v] `/clips` shows a project with no `videoTemplateId` as neutral 템플릿 없음 metadata with no re-selection ask
- [v] ① shows the same neutral state instead of `project.detachedTemplate`
- [v] a project whose `videoTemplateId` is set but whose template is missing still says the template is gone and offers another choice
- [v] the loading and error states for a named template are unchanged
- [v] ko and en copy both land
- [v] tests: the list row and ①'s field for each of the three cases — none chosen, named and loaded, named and gone
- [v] ARCH-25 verify clean

## impl notes
- the bug is one collapsed branch: `ClipsPage.tsx:197` falls through to `project.detachedTemplate` whenever the id is empty, which CLIP-5 (r32) made the normal case; `ClipProjectForm.tsx:274` has the same fall-through.
- add `project.noTemplate` and keep `project.detachedTemplate` for the genuinely detached case rather than rewording the existing key, so the deleted-template path keeps its own words (CLIP-25).
- this task is free of T227's chain: it touches no grammar and can go first.
- parallel sessions: `resources/{ko,en}/clips.ts` collides with T229 and T230.

## result
- `frontend/src/pages/clips/ui/ClipsPage.tsx`: the metadata branch now reads the empty id first and falls to `project.noTemplate`; the loading, unavailable and detached branches keep their own conditions.
- `frontend/src/app/providers/i18n/resources/{ko,en}/clips.ts`: `project.noTemplate` added beside `project.detachedTemplate`, which keeps its wording for the deleted-template case.
- ① needed no change: `ClipProjectForm.tsx:274` already guards the detached message with `!!draft.videoTemplateId`, and the selector already offers 없음 as a normal choice. A test now pins that so the two cases cannot collapse again — which is exactly how they collapsed in the list.
- tests: `ClipsPage.test.tsx` (list, three cases) and `ClipPage.test.tsx` (① , chosen-none vs named-and-gone). Both fail against the previous branch.
- ARCH-25 green (250 files / 2074 tests, lint, fsd, style probe + style, build:web). No Go, proto or SQL touched, so ARCH-26/28 are unaffected.
