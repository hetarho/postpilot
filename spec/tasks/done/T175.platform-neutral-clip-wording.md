# T175 Clip wording that names no single platform
> st:done@260915 | ssot:CLIP-115 CLIP-27 CLIP-28 CDS-49 CDS-78 | base:CDS@17 CLIP@24 ARCH@2 | dep:T174

## goal
State what a clip is and which ratio suits what without naming one publishing service.

## acceptance
- [v] the ratio guidance in `ko` and `en` matches CDS-49 — short-form feed, horizontal upload or embedding, square feed — and names no platform
- [v] no clip-domain source comment, model prompt or bundled document gives a platform's UI or upload contract as the reason for a design value or a delivery rule
- [v] `docs/design/clip-output-design-spec-v1.md` records the symmetric 9:16 geometry and the retired SA-N, so the reference CDS constraints point at stays true
- [v] verify FE (ARCH-25) passes with the existing i18n and composition regression tests covering the changed copy

## impl notes
- identifiers, routes, tables and i18n keys named clip stay exactly as they are (CLIP-115); only prose changes
- the only user-facing platform mention found is `ratioGuidance` in both locale files; grep the clip domain for the platform name before assuming that is all of it
- publishing domains (PUB, POST, VIDEO, EXPORT, MKT) keep their platform names — that dependency is real there and out of this task's scope
- the design spec document is a reference, not a SSOT; update its safe-area and anchor numbers and its SA-N section rather than rewriting the document

## result
Changed: `ko/clips.ts` and `en/clips.ts` `ratioGuidance` (short-form feed · horizontal upload and blog or site embed · square feeds and blog bodies), and `docs/design/clip-output-design-spec-v1.md` — §1.1 marked retired with the removed runtime constants named, §1.2 replaced by the symmetric CDS-9 safe area with its 856 px reading measure, §1.3 anchors corrected to TOP 80, CENTER 540 and RIGHT 984, §1.4 and the JSON example restated on the new safe area, the §6 ratio table row rewritten as CDS-49 guidance, the release checklist item and the closing open-question note reworded around CDS-78.

Notes:
- identifiers, routes, tables and i18n keys named clip are untouched, as CLIP-115 states.
- `backend/internal/clip/ai/testdata/README.md` still mentions Naver once: it records what a past run did not do, not a reason for a design value, so it stays.
- publishing domains keep their platform names by design.
- verify FE (ARCH-25) passes. The first `vitest run` of the session failed without naming a test in captured output; two consecutive full reruns passed 1980/1980, so it is treated as the known flake rather than a regression from this change.
