# T162 Rank automatic anchors by the observed caption-safe regions
> st:done@260915 | ssot:CDS-38 | base:CDS@14 CLIP@21 ARCH@2 | dep:T161

## goal
Automatic anchor selection prefers a candidate that lands inside an observed caption-safe region.

## acceptance
- [v] `design.SelectAnchor` in `backend/internal/clip/design/compose.go` takes the cut's observed caption-safe regions and ranks a candidate lying inside one above a candidate lying outside every one
- [v] the existing order is otherwise unchanged: safe-area drop, placed chip/badge/card drop with copy yielding to the badge, the ≤ 15 % subject-coverage preference, the readable-text TOP/BOTTOM restriction and the one-step limit all keep their current precedence
- [v] with no regions observed the selection is byte-identical to today's, so stored plans and existing fixtures are unaffected
- [v] `backend/internal/clip/media/composition_layout.go` passes the regions of the cut the element belongs to, matching source id and cut range the way `CutSubject` already does
- [v] an explicitly authored template or owner position is still pinned and never re-ranked (CDS-64)
- [v] test: a bottom-heavy subject with a top caption-safe region moves an automatic 가벼운 텍스트 caption to TOP
- [v] test: identical inputs with no regions produce the anchor today's code produces (golden, over the existing layout fixtures)
- [v] test: a pinned authored position ignores the regions entirely
- [v] `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...`

## impl notes
- the four anchors and the per-style anchor lists are unchanged — the owner dropped free vertical placement, so this task only adds a ranking input
- V13 (anchor step ≤ 1) is unchanged and must keep passing; the region preference sits below it in precedence, not above
- T159 gives `simple` its TOP alternative; without that this task has nowhere to move a 가벼운 텍스트 caption, so land T159 first in practice even though the dependency is only on T161

## result
- Files: clip/design/compose.go and compose_test.go; clip/caption_safe.go and caption_safe_test.go; clip/media/composition_layout.go, composition_anchor_test.go and testdata/automatic-anchor-defaults.json; legacy compose.go/pace.go call sites retain nil evidence.
- Automatic candidates wholly contained in observed empty space rank first after safe-area, furniture, readable-text and one-step filtering; the existing subject-coverage preference remains stronger than region ranking.
- Source-space regions follow the renderer's cover crop; unrelated sources/times and cropped-out boxes are ignored, and cuts spanning scenes use only their common empty space. Authored and stored owner positions remain pinned.
- Verified: TOP selection with/without a bottom-heavy subject at both paces; subject, badge, safe-area, readable-text and one-step precedence; pinned positions and repeat renders; 30 pre-change manifest hashes remain byte-identical without regions across all ratios/paces/styles.
- Validation: full backend test/vet/build/gofmt after the ranking change; release-wide frontend 233 files/1955 tests, lint/FSD/style/format/build, full agent checks and generated-code reproducibility already passed and were unaffected; final spec lint passes with pre-existing warnings. No SSOT deviation.
