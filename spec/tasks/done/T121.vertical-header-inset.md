# T121 Reduce the vertical header inset
> st:done@260912 | ssot:ARCH-24 CDS-9 CDS-11 CDS-12 CDS-14 CDS-30 CDS-31 CDS-32 | base:ARCH@2 CDS@7 | dep:T120

## goal
Place vertical top overlays at 80 px using a 40 px design inset plus a 40 px gap, and deliver a revised SVG example video.

## acceptance
- [v] Backend and frontend agree on the vertical safe region (64, 40, 856, 1380), TOP/chips/badge y 80 and top scrim y 40; the bottom edge and other ratios stay unchanged.
- [v] Existing geometry, safe-boundary and SVG golden tests pass with the intended coordinates, alongside the ARCH checks.
- [v] All eight owner originals render into the existing 20 s SVG example with zero manifest collisions; retain the MP4 and inspect an actual header frame, using $0 external model spend.

## impl notes
- Change the shared layout configuration and its frontend mirror, not SVG-only translations that disagree with manifest bounds.
- Retain the separate unverified Naver UI estimates for T110; the design safe area is not a cross-platform visibility guarantee.
- Reuse the optional shorts-editorial catalog and curated plan unchanged. The header, captions, intro and ending are templated SVGs rasterized by resvg and composed by FFmpeg.
- Update the existing assertions and expected SVG output. No new dependency, API, model call or source-footage mutation is needed.

## result
- CDS r7 and mirrored backend/frontend configuration set the vertical safe region to (64, 40, 856, 1380), TOP/chips/badge y 80 and top scrim y 40. The safe bottom, horizontal and square layouts remain unchanged; separate platform estimates remain pending real-device measurement.
- Updated existing geometry, one-pixel safe-boundary and three SVG golden expectations; comments and overlay documentation describe the new bounds and the four example SVG roles. Actual filled header plates move up exactly 210 px with identical text and all other attributes.
- Backend and agent tests/vet/gofmt/build pass; FE 200 files / 1,613 tests, lint/format/FSD/style probe/style/build pass on Node 24.18.0. Isolated proto/SQL generation is unchanged. Skills sync and spec lint pass; 11 pre-existing non-blocking historical/base warnings remain.
- Re-rendered all eight owner originals through the unchanged shorts-editorial SVG catalog and the updated media test binary inside the production media runtime, non-root at 512 MiB / 2 CPU / no swap / no network. The 20.000 s 1080×1920 H.264 High / AAC 48 kHz stereo output is 23,393,172 bytes, SHA-256 b14d7b108baa38f00ae73e666d2c9be2df819bf4022bf532e5ea2b73a28adfd2. Rendering took 142.355 s (175.43 s with probing); zero manifest collisions, OOM or OOM kills; $0 external model spend.
- Retained the MP4, source and filled SVGs, transparent PNG plates, frame previews, edit plan and verification evidence at ~/Downloads/postpilot-top80-svg-review-ueibis83/. Inspected actual cooking/interior frames: menu and disclosure share y 80 with aligned text. Original files are unchanged; no deployment or default-theme adoption occurred.
- Completion base remains ARCH@2 / CDS@7. Existing T008, T110 and the separate ideation work are preserved.
