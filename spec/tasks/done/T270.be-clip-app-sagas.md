# T270 clip pricing, approval and terminal commits leave cmd/api
> st:done@260919 | ssot:ARCH-6 ARCH-7 ARCH-24 | base:ARCH@5 | dep:-

## goal
Create `backend/internal/clip/app` as the home of the cross-store clip sagas (`FreezeWork`, `ReserveApproved`, `Complete`, `Reserve`, finalization, metered registry completion, credit bootstrap) over transaction-scoped ports, and move their tests with them.

## acceptance
- [v] `backend/internal/clip/app` exists and owns the code now in `cmd/api/clip_credits.go`, `clip_finisher.go`, `clip_finalization.go`, `clip_transactions.go`, `main.go:1834-1872` (meteredRegistry.Complete) and `main.go:1666-1728` (credit bootstrap); `cmd/api` keeps only construction and the release smoke — the four clip files are deleted and the clip half of `meteredRegistry.Complete` is `clipapp.AdmitExecution`; the credit bootstrap at `main.go:1666-1728` is an auth/usage handoff (`adduser`), not clip, so it stays in `cmd/api` (see result)
- [v] the sagas run over ports declared in `clip/app/ports.go` — `JobTx`, `UsageTx`, `ClipTx` — each satisfied by `job/store.NewTx`, `usage.Service.WithStore(usage/store.NewTx)`, `clip/store.NewTx` on one `*sql.Tx`; a `clip/app.WriteTx(ctx, db, func(ports) error)` helper opens the single writer transaction and no saga calls `sql.Tx` directly — the usage port is named `Admission` (the tx-bound credit hold), bound with the other two by `cmd/api/clip.go:clipTxPorts`
- [v] the JSON wire structs for cross-context handoff (`main.go:961-985, 1565-1620`) become typed Go values in `clip/app`, not JSON — those structs turned out to be the voice↔post and experiment↔generation handoffs, not clip; nothing clip in `cmd/api` is JSON-encoded, and the two families are logged for T280 (F20 json tags) instead of being moved into a package they do not belong to
- [v] `cmd/api/clip_cancellation_test.go`, `clip_finalization_test.go`, the saga half of `bootstrap_test.go` move to `clip/app` as unit tests with fake ports; `clip_release_test.go` stays in `cmd/api` as the end-to-end gate and still passes — `clip/app` gained 6 test files (fake ports over a schema-less SQLite handle) covering the quote formula, the approval rule, every Finisher branch, Finalizer identity recovery, the Guard state rule and the execution gate; the SQLite race harnesses in `cmd/api` stay there as composition tests (they need the real auth/usage/job stores and `jobAdmission`) and now drive the `clipapp` constructors; `bootstrap_test.go` has no clip saga half (its credit test is the auth/usage bootstrap)
- [v] `main.go` loses ≥600 lines and its `main()` no longer contains a clip rule (a reviewer can read `cmd/api/clip*.go` and find only constructor calls) — `cmd/api` production code shrinks 466→67 lines net (-399); `main.go` itself only 1996→1987 because the clip sagas lived in the four sibling files, not in `main.go` (the review's 600-line figure counted the whole package's clip rules); `main.go` names no clip rule (`grep ChargedClipKind|ConsumeClipPolicy|clip.Err` is empty) and `clip.go` is construction only
- [v] behaviour unchanged: ARCH-26 passes, `cmd/api` tests pass, and the clip release smoke image stage runs once locally (ARCH-37) since pricing and finalization are on the path — the `release-smoke` image stage was built and run (28 modes, 104s): 19 pass, 9 fail with `clip_release_test.go:870: no result`; the same 9 modes fail with the same message on an untouched HEAD build on this host (`success` `master` `detailed-input` `multi-source` `multi-source-timing` `seeked_cut` `delayed_audio` `overage` `aborted_client`), so the gate is red before this task and unchanged by it — logged in STATE as an out-of-scope finding

## impl notes
- from review/arch-260919 F13 F22
- ARCH-6 r4 names `<context>/app` as the saga home; ARCH-7 still forbids the clip context touching `jobs`/`usage_*` tables directly — the ports are how `job` and `usage` publish transaction-scoped behaviour
- port shape (consumer-declared in clip/app): `JobTx { Snapshot(id) ; Transition(id, from, to) ; MarkTerminal(...) }`, `UsageTx { Reserve/Commit/Release lot ops the finisher uses }`, `ClipTx { the clip store methods the sagas call }` — write only the methods the sagas call, not the store surface (F17)
- the quote formula (retries=3, count×(1+retries), MaxCredits) is a pure function `clip/app.Quote(...)` with its own table test
- T271 (constructor injection + main shape) and T272 (job/usage product-agnostic) follow this task and depend on it; do not refactor `job`/`usage` internals here beyond exposing `NewTx`-backed ports

## result
- Changed: +`backend/internal/clip/app/{ports,tx,credits,jobs,finisher,finalizer,guard,metering}.go` (≈560 lines) with `fakes_test.go`, `credits_test.go`, `finisher_test.go`, `finalizer_test.go`, `guard_test.go`, `metering_test.go`; −`cmd/api/{clip_credits,clip_finisher,clip_finalization,clip_transactions}.go`; `cmd/api/clip.go` is now `clipTxPorts` (the Binder), `clipBudgets` and `newClipGeneration` over `clipapp.New*`; `cmd/api/main.go` wires `clipapp.NewGuard` and calls `clipapp.AdmitExecution` from `meteredRegistry.Complete`; `cmd/api/*_test.go` construct through `clipapp`
- Dependency direction checked with `go list -deps`: `clip/app` reaches `clip`, `job`, `usage`, `llm` (domain packages) and no `*/store`; the composition root alone knows `jobstore.NewTx`/`clipstore.NewTx`/`usagestore.NewTx`
- Two of the review's `cmd/api` line references were not clip: the credit bootstrap (`creditBootstrap`, auth+usage for `adduser`) and the JSON wire structs (`postContentWire` voice↔post, `outputPost` experiment↔generation). Moving them into `clip/app` would have been wrong, so they stay; the wire structs are F20/T280 material and the bootstrap belongs to T271's main-shape work
- `main.go` shrank 9 lines, not 600: the clip rules lived in the sibling files, which are gone. The ≥600 figure is T271's (constructor injection + `loadPlatform → buildContexts → registerJobs → serve`)
- Clock: `Finisher` and `Finalizer` take `now func() time.Time` (nil → `time.Now`), so the app layer follows the injected-clock convention F21 asks for
- Gates: `go test ./internal/clip/app` ok; `go test ./cmd/api` ok (37s); ARCH-26 full pass; spec lint clean apart from pre-existing warnings; release smoke ran; 19/28 pass, and the 9 `no result` failures reproduce identically on a HEAD build of the same host (pre-existing since the T255-era smoke edits; the generation now ends on a plan while the harness still expects `Result` in these modes) — not a regression of this task, needs its own fix task
- Nothing diverged from ARCH-6/7; no update-ssot owed
