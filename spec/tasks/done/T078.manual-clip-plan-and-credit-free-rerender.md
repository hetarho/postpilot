# T078 Manual clip-plan save and credit-free rerender
> st:done@260910 | ssot:CLIP-2 CLIP-17 CLIP-18 CLIP-20 CLIP-21 CLIP-23 CLIP-24 CLIP-26 CLIP-27 QUOTA-10 QUOTA-14 QUOTA-15 ARCH-3 ARCH-6 ARCH-7 ARCH-10 ARCH-11 ARCH-21 ARCH-24 | base:CLIP@2 QUOTA@7 ARCH@2 | dep:T071 T074 T076

## goal
Persist validated manual cut/copy/audio corrections and rerender them as a durable job that creates no LLM call, hold or credit charge.

## acceptance
- [v] `clip.proto` adds optimistic `SaveClipEditPlan` and `StartClipRender`; the save carries expected edit-plan revision, the render carries that revision plus one ready source-batch id, and both remain owner-scoped
- [v] save accepts reorder/delete, source start/end changes, exact copy/timing changes, approved position/style/accent changes and volume 0.0–1.0 while keeping ratio immutable
- [v] server validation requires at least one cut, every cut inside its retained source duration, captions inside the cut, only retained source fingerprints, a final 15–90 second timeline and every T074 renderer invariant; invalid input changes nothing
- [v] a successful save increments `edit_plan_revision` and leaves the current result and `rendered_plan_revision` untouched, so an edited-but-not-rendered state survives reload
- [v] `StartClipRender` requires a ready batch containing exactly the source fingerprints the corrected plan still references, freezes the plan/revision and links the batch to a new `render_clip` job; unrelated sources are refused and unused original sources need not be reselected
- [v] `job.NewJob` gains an explicit non-metered admission mode used only by `render_clip`: it rejects any planned LLM call, skips Hold/Settle entirely and leaves every existing job kind's admission behavior byte-for-byte unchanged
- [v] the handler probes selected files, renders the frozen plan through T074, uploads a new result, atomically swaps result metadata and `rendered_plan_revision`, and queues the prior key for outbox deletion
- [v] a newer plan revision, active job, missing/mismatched source or expired batch is refused before render; a failed render preserves the previous result and saved corrections and exposes a retryable durable failure
- [v] source cleanup runs on success, failure, cancellation, panic and boot recovery exactly as in T076; no source key is copied into project columns
- [v] deleting a project with no active job removes even an unrendered corrected plan and queues its current result; a busy project remains `CLIP_BUSY`
- [v] service/job/store/RPC tests cover optimistic conflict, every plan mutation, source-subset matching, no admission/usage rows, result preservation, revision swap, cleanup retry and owner isolation
- [v] ARCH-26 and ARCH-28 pass

## impl notes
- represent volume on the wire and in persisted JSON as integer thousandths (`0..1000`), mapping to FFmpeg's float only at T074's boundary; this avoids unstable JSON float equality in optimistic saves
- save the complete plan as one guarded JSON document: cut order, caption timing and total length are one invariant and cannot be correct under per-row partial writes
- non-metered is a queue input, not a new QUOTA rule; the queue has no hold to settle because this job makes zero provider calls, satisfying CLIP-20 without teaching the credit ledger about render cost
- disallow plan saves during an active clip job so the frozen revision and the screen never diverge; the client keeps local edits and retries after the job ends

## result
- Added owner-scoped `SaveClipEditPlan` / `StartClipRender` and detail-only editing metadata/limits in `clip.proto`; generated both Go clients and TypeScript. The wire exposes no editable ratio/focal point, source object key or owner claim.
- `clip/correction.go` validates whole-plan reorder/delete/trim/copy/timing/approved style/accent/volume; the same renderer measures exact caption fit before save. Frozen styles survive template detachment. Versioned plan JSON uses integer thousandths, with a strict reader for valid T076 legacy plans.
- Transactional optimistic saves increment only the edit revision. Render enqueue rechecks the revision while linking its ready batch, closing the read/enqueue race; the worker validates the frozen plan and exact remaining fingerprint subset against newly probed originals.
- Added explicit `NewJob.NonMetered` for `render_clip` only. Any model ref, pricing call or call-count declaration is rejected, no hold/release/settlement runs (including recovery), and the metered provider boundary rejects any accidental render-kind LLM call before the provider or ledger.
- Rerender shares bounded streaming media work, linked transient source cleanup and outbox/orphan recovery with generation; successful output swaps only result/rendered revision after workspace cleanup. Saved corrections and the old video survive every failed stage.
- Tests cover all mutation bounds, exact integer round-trip, legacy decoding, owner isolation, simultaneous saves, stale revision at linking/swap, incomplete/extra/mismatched/expired sources, zero admission/usage rows, busy deletion, unrendered deletion, cleanup retry, cancellation/malformed payload/panic/boot recovery, and the closed RPC failure registry.
- Gates pass: backend Go formatting/vet/build/all tests; agent formatting/vet/build/all tests and packaging shell syntax; frontend 178 files / 1,382 tests plus lint/format/FSD/style/build; race checks for correction/render store, queue and metering; skill sync/spec lint. Existing unrelated lint/bundle/spec-history warnings remain.
- BSR rejected the remote regeneration attempt with its rate limit; reran the identical pinned Go 1.36.11 / Connect 1.20.0 / ES 2.12.0 generators locally in Docker and confirmed repeated output is byte-identical (normal TS EOF normalization); SQL regeneration also has zero diff. No generator/dependency settings changed.
- Freshness confirmed at CLIP@2 QUOTA@7 ARCH@2. No source bytes persisted in project fields, paid provider calls, production writes, pushes or SSOT divergence.
