# T151 Verify review-clip assembly end to end
> st:done@260914 | ssot:CLIP-7 CLIP-10 CLIP-11 CLIP-18 CLIP-20 CLIP-26 CLIP-31 CLIP-49 CLIP-56 CLIP-67 CLIP-72 CLIP-87 CLIP-91 CLIP-92 CLIP-93 CLIP-97 CLIP-98 CLIP-99 CLIP-100 CLIP-101 CDS-35 CDS-52 CDS-53 CDS-59 CDS-60 CDS-62 CDS-65 CDS-68 ARCH-24 ARCH-25 ARCH-26 ARCH-27 ARCH-28 ARCH-29 ARCH-31 | base:CLIP@19 CDS@13 ARCH@2 | dep:T146 T147 T149 T150

## goal
Prove that complete observations, authoritative assembly, owner correction, preview and export produce the same stable review clip across every supported rate and audio state.

## acceptance
- [v] One deterministic source-owned corpus runs observation → writer → saved assembly → correction preview → export with same-source adjacent splits, reordered cuts, all six rates, certain/uncertain/unusable scenes and item-bound copy without unsupported claims or silent plan changes.
- [v] The matrix covers source audio off, on, mixed, enabled-but-silent and per-cut zero volume; enabled speech retains pitch, disabled originals do not enter the mix, and the all-off result has no audio stream or in-product BGM.
- [v] All three ratios and sentence/rapid paces compare source range, cut id/order/rate, transformed duration, transition, crop, caption/element interval, source-audio state and representative frames between the browser manifest and final export within the declared frame-timing limits.
- [v] Unsupported rate, insufficient cadence, overlap, observation gap, unusable span and invalid authored interval preserve the exact draft and previous result with typed diagnostics; no case silently substitutes 1x, drops a cut/text or starts an unapproved provider call.
- [v] A render failure after valid assembly followed by continuation makes zero repeated observation/writer calls and uses the latest owner sound setting; a changed sound toggle stales only the render, while changed observation/assembly inputs invalidate only the incompatible recovery stage.
- [v] Migration fixtures cover v0-v4 correction, portable v5, v1 observations and legacy audio inference; existing result bytes and behavior remain unchanged until an explicit v6 save/rerender, and v1 observations are never promoted to v2 reuse.
- [v] Production media verification covers 30/40/60 fps originals, CFR slow-rate admission, VFR/unknown refusal, fixed-rate duration and pitch, optional audio output and same-source splits under the existing nonroot resource limits.
- [v] Full local ARCH gates, deterministic proto/SQL regeneration and spec lint pass; fixture/model responses are recorded and synthetic, no paid AI call, private footage, deployment or Naver publication is part of this task.
- [v] Archive a concise hashed evidence index outside the repository and record remaining browser-decoder, final loudness and physical-device limits without treating T110 as completed.

## impl notes
- Extend the existing clip quality-release, composition-quality, media smoke/stress and frontend preview corpora rather than creating a second pipeline. Assertions compare semantic manifests and frame/sample boundaries, not only JSON validity or a playable MP4.
- Use generated tone and speech-like fixtures with known cadence to measure output duration and dominant frequency at every rate. Keep personal/customer footage and provider payloads outside the repository.
- Exercise response correction with recorded malformed rate/overlap outputs and explicit exhaustion/cancellation; no live model quality claim is made from fixtures.
- Run T110 only under its separate owner/device conditions after refreshing its CLIP/CDS base; this task does not edit, unblock or claim that immutable task.

## result
- Extended the existing AI/composition-quality corpus: two complete v2 observations and one recorded writer response per ratio/pace, six adjacent source selections at all supported rates, reordered stable cut IDs, item-bound copy, an explicit transition and a v6 correction. The same encoded plans drive the real renderer and its preview assets; no second execution pipeline was added.
- Added continuation through an injected render failure after changing owner sound, plus invalid-rate/cadence/overlap/authored-interval saves that preserve the exact submitted draft, saved plan, revision, previous result and provider counts. Existing gap/usability, response-retry/cancellation, recovery invalidation and v0-v5/v1-observation migration suites pass.
- The real cadence matrix exposed a VFR original averaging 48 fps incorrectly admitting 0.75x. Admission now records positive constant-interval evidence from `vfrdet` during the existing full decode, using bounded stderr alongside progress. Missing/variable evidence refuses slow playback; 30/40/60 fps CFR admits the correct set. Older metadata remains readable and offers 1x/faster; existing result bytes are untouched. An unsupported correction rate now identifies the cut before duration resolution can obscure its cause. This implements the existing CLIP-99/CDS-68 decisions; no SSOT policy changed.
- Six nonroot exports under 512 MiB/no extra swap and 2 CPUs pass: 17.8 s, 534 decoded frames at 30 fps, 13 text elements and one final lossy video encode each. Off/on/mixed/enabled-silent/per-cut-zero cases verify stream presence, silent samples and pitch at each rate with 440/880 Hz tones. Peak memory: 407900160 bytes; peak workspace: 20537589 bytes. Production speech fixtures separately verify timing/correlation.
- Chromium with an explicit sRGB profile passes 156 paused frame/semantic comparisons, including source identity/range/order/rate, crop, transition, element intervals, original-sound permission and native pitch/gain. Full-canvas maximum mean channel difference is 4.875/255 and 111 tight element regions peak at 4.922/255; the declared limits are 8/255 and 15/255. Output seek error is 0 ms in this corpus, against a one-frame tolerance.
- Local ARCH gates pass: frontend 231 files/1890 tests, lint/FSD/style/formatter/build; backend and agent gofmt/vet/build/test plus installer syntax; proto and SQL regeneration unchanged (950 files compared); package-managed skill check and spec lint. Existing DraftEditor hook/build-size and historical spec warnings remain.
- Evidence is outside the repository at `/tmp/postpilot-assembly-snd/evidence-index.json` (corpus, manifests, frames, probes, logs and hashes). Paused Chromium checks do not prove every browser codec or physical playback. Source contrast and final loudness remain export-only; no listening/device/Naver/T110 completion is claimed. No paid AI, private footage, deployment or publication occurred. The production image `postpilot-api:t151` passes its mandatory media smoke and 20-source writer-input/continuation gates (zero repeated AI calls).
