# T159 Sequence colliding captions at every pace and give 가벼운 텍스트 a second anchor
> st:done@260915 | ssot:CDS-43 CDS-52 CDS-58 | base:CDS@14 ARCH@2 | dep:-

## goal
Two captions declared on one cut never share a window at any caption pace, and an automatic 가벼운 텍스트 caption can move off BOTTOM when the subject sits there.

## acceptance
- [v] `scheduleDeclaredCaptions` in `backend/internal/clip/media/composition_cues.go` schedules rapid-pace captions too: the `text.Pace != "rapid"` filter is gone and the two captions sharing a cut receive disjoint windows before phrase splitting
- [v] the per-caption readability floor used to split the window is pace-aware — `clip.MinExposureMS` for steady, `len(clip.RapidPhrases(text)) * design.Rapid.MinMS` for rapid — so a rapid sentence is not dropped for `readability` against the sentence-mode floor
- [v] a third and later caption on one cut still drops with `sentence_count`, and a second whose floor does not fit still drops with `readability`, at both paces
- [v] `design.json` gives `simple` (가벼운 텍스트) `anchor_alt: "top"`; `clean`, `memo`, `bold` and `mark` anchors are unchanged
- [v] CDS-52 V18 is enforced: `VerifyCompositionManifest` fails when two caption-role elements on one cut hold overlapping [StartMS, EndMS)
- [v] regression test in `backend/internal/clip/media/composition_cues_test.go` reproducing the shipped defect — a `pace="rapid"` document with `dish_intro` and `dish_review` on one 3.0 s cut — and asserting disjoint windows and disjoint phrase times
- [v] test in `backend/internal/clip/design/compose_test.go` (or `design_test.go` beside the existing anchor pins) that `simple` offers TOP when BOTTOM covers the subject box past `Guards.SubjectCoverMax`
- [v] test that V18 rejects a hand-built overlapping manifest
- [v] `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...`

## impl notes
- the shipped defect this fixes: prod clip `a3d32f4f`, cut `cut_stew_intro` — `dish_intro` and `dish_review` both resolved to lower_mid 16346–19102 ms with identical phrase times 120/420/920, because the scheduler skipped the whole cut for being rapid
- keep the existing proportional split (`MinExposureMS`-weighted boundary); only the eligibility filter and the floor function change
- the scheduler runs BEFORE `layoutDeclaredRapid`, so a narrowed window flows into `SplitRapid` unchanged; do not re-split inside the scheduler
- `simple` is CDS-58 가벼운 텍스트, not CDS-23 깔끔하게 — `clean` keeping a single anchor is deliberate here and left to a later decision
- V18 belongs beside the existing V-checks in `backend/internal/clip/composition_verify.go`; caption-role only, because info/badge/card overlap stays advisory under CDS-56

## result
- Files: clip/media/composition_cues.go and composition_cues_test.go; clip/composition_verify.go; clip/design/design.json, compose_test.go and design_test.go; mirrored frontend clip-design.json; attempt diagnostic and ko/en inspection mapping.
- Verified: rapid dish_intro/dish_review on a 3 s cut have disjoint parent/phrase windows and repeat identically; both pace floors/count limits; simple TOP alternative; V18 rejects overlap while output-level text remains independent.
- Validation: backend and agent tests/vet/build; frontend suite plus corrected locale/mutation tests, lint/FSD/style/build/format; generated proto/SQL reproduced without drift; spec lint passes with pre-existing warnings. No SSOT deviation.
