# T255 the release smoke's continuation ends on a plan, not on a result
> st:done@260918 | ssot:CLIP-151 CLIP-152 CLIP-93 | base:CLIP@37 | dep:T241

## goal
`TestClipWriterInputRelease` asserts the plan-only completion CLIP-151 made the generation's end, so the production image builds again.

## acceptance
- [v] `verifyCandidateContinuation` treats a finished continuation as successful on its saved plan — the advanced `edit_plan_revision` and the plan the job wrote — instead of on `Project.Result`
- [v] the continuation still proves what it exists to prove: no further writing call, no balance movement, no repeated proxy preparation, and the reused chunk count it expected
- [v] a project that held a previous result still holds exactly that result after the continuation, and `rendered_plan_revision` stays where it was — the plan is ahead of the render, which is CLIP-152's normal state
- [v] the changed-template quote still keeps its observations and charges nothing new
- [v] a failing continuation reports why it failed: the job's own failure where it failed, and the missing plan where the job succeeded without one
- [v] go test on the host: a plan-only continuation is accepted and a continuation that saves no plan is refused, run from `go test ./...` without the smoke's env opt-in
- [v] verify: `cd backend && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...` (`-timeout 30m` for `clip/store`)
- [v] docker: `docker buildx build --target clip-input-smoke -f backend/Dockerfile .` completes, which is the mandatory image gate this task restores (ARCH-36 ARCH-37)

## impl notes
- reproduced at 191e2f9d: the stage fails at `cmd/api/clip_release_test.go:214` with `retained candidate failed <nil>` — the job reaches `done` with no failure and `Project.Result` is nil, because T241 removed the render stage from the generation (CLIP-151). Nothing in the product is broken; the smoke's expectation is the stale thing
- the whole run is ~64 s inside the stage, so this is a build-and-read loop, not a fifteen-minute one. Build with `--target clip-input-smoke`: the `RUN` at `backend/Dockerfile:74` executes the test at build time, so a green build IS the passing test
- there is no host path for this smoke (the harness reads `/usr/share/postpilot-media/speech.m4a` at an absolute path and the test skips without `CLIP_RELEASE_SMOKE=1`), which is why a stale expectation survived `go test ./...`. That is what the host-side test in acceptance closes: put the plan-only-continuation assertion in an ordinary test beside `TestReleaseDetailedBodyUsesCurrentGrammar`, which was added for exactly this reason — keep the break inside ARCH-26 rather than inside the deploy's gate
- `quote.Msg.RenderOnly` keeps its name and its meaning — a continuation needing no paid call — even though a generation no longer ends in a render. Renaming it is a contract change and belongs to its own task, not here
- `backend/internal/clip/media/local_authored_test.go` is another session's uncommitted diagnostic work in the tree; leave it untouched and out of this task's commit

## result
- `cmd/api/clip_release_test.go` only. `continuationVerdict(before, after, job)` replaces the two inline conditions the loop used: it takes the project read BEFORE the continuation and states success as CLIP-151 defines it — the job done, a plan with cuts saved, `edit_plan_revision` advanced, `rendered_plan_revision` standing and the result id unchanged. Each refusal names the number or id that moved, so the next drift reads as itself instead of as `retained candidate failed <nil>`.
- `TestContinuationVerdictExpectsAPlanAndNoNewResult` runs on the host with no `CLIP_RELEASE_SMOKE` opt-in and covers all six cases (plan-only accepted; failed job, absent plan, standing revision, moved rendered revision, replaced result refused). That is what closes the hole this task came from: the smoke's expectation was only executable inside the image, so `go test ./...` could not see it go stale.
- verified: `gofmt -l` empty, `go vet ./...`, `go build ./...`, `go test ./... -timeout 30m` all green; `docker buildx build --target clip-input-smoke -f backend/Dockerfile .` completes with `--- PASS: TestClipWriterInputRelease (70.00s)`, against `FAIL` on the same stage before the change. No proto, sqlc or frontend file is touched, so ARCH-28's generated-code check and ARCH-25 are unaffected.
- no SSOT divergence. `quote.RenderOnly` keeps its name although a generation no longer ends in a render; renaming it is a contract change and was left alone as the task said.
- left untouched: `backend/internal/clip/media/local_authored_test.go` carries another session's uncommitted diagnostic work and is not in this commit.
