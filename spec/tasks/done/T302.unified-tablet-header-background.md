# T302 Unified tablet header background
> st:done@260921 | ssot:THEME-38 THEME-41 | base:THEME@17 | dep:-

## goal
- Remove the dark tablet navigation band by sharing the brand header background.

## acceptance
- [v] At 640..1023px the primary navigation and brand row share one raised background, including one translucent backdrop on /plans.
- [v] Phone bottom navigation, desktop rails, selected destinations and sticky offsets retain their behavior; existing navigation/router/plans tests pass.
- [v] Browser checks cover both themes, ordinary and immersive routes, and phone/tablet/desktop breakpoint boundaries.
- [v] Applicable frontend checks and spec lint pass.

## impl notes
- AuthenticatedLayout owns the background once on its header; the brand row and tablet primary nav are transparent children.
- Retain the desktop rail's lowest background and the group row's base background; preserve dimensions and navigation actions.
- Only THEME-38 is consumed here; THEME-19 remains pending at the older tasked watermark because its font verification belongs to separate work.
- This reversible styling change uses existing behavior tests and browser verification; no new implementation-mirroring tests.

## result
- AuthenticatedLayout paints its header background once; its brand row and tablet primary navigation are transparent children, removing the lowest-plane stripe and duplicate immersive backdrop.
- Browser verification: /posts and /plans at 390/639/640/768/1023/1024/1280px in day/night themes (28 combinations); no horizontal overflow, tablet nav visible only at 640..1023px, current destination preserved and tablet navigation to /clips works.
- At 768px the header remains 96px tall (52px brand + 44px navigation); regular pages share the raised background, /plans shares one 70% raised background and 24px backdrop blur.
- Verification passed: 100 focused navigation/router/plans tests, all 2252 frontend tests, lint, Prettier check/write, FSD, 61 style probes, style lint, production web build and spec lint (only existing stale-base warnings).
- No changes to phone navigation, desktop rail backgrounds, navigation actions or sticky dimensions; THEME-19 font verification remains pending.
