# T275 job/store, template/rpc and modelcatalog/rpc have tests
> st:done@260920 | ssot:ARCH-24 | base:ARCH@5 | dep:T272

## goal
Add the missing test packages the review found: `job/store` (448 lines, 0 tests, including dispatch authorization), `template/rpc` and `modelcatalog/rpc`.

## acceptance
- [v] `backend/internal/job/store` has tests over a temp SQLite (migrations applied) covering `Reserve`/`Activate`/`SweepUnactivated`/`AuthorizeDispatch` and the serialization of authorization with cancellation
- [v] `template/rpc` and `modelcatalog/rpc` have handler tests covering the proto↔domain mapping for each rpc and the `toConnectError` reason for each domain error
- [v] ARCH-26 passes

## impl notes
- from review/arch-260919 F21 (test gaps)
- runs after T272 so the `job/store` surface under test is the generic one
- follow the pattern of the existing `*/rpc/*_test.go` handler tests and the temp-DB helper the other store tests use

## result
- `internal/job/store/store_test.go` (7 cases over the migrated temp DB the T285 harness builds): the queued→running→progress→failure lifecycle with the three failure columns round-tripping, `Finish` refusing a status that contradicts its failure and refusing a second finish of an immediate kind, `FailQueued` owner-scoped and queued-only, `SweepRunning`, `LatestFor`/`ActiveFor` per subject and account — and the serialization the review named: `AuthorizeDispatch` allows repeated calls for one job, refuses another account's, and refuses everything once `RequestCancellation` has landed, with `RecoverCancellations` finishing what nobody did. `Activate`/`SweepUnactivated` were already covered by T285
- `internal/template/rpc/handler_test.go`: every rpc's mapping (including "no opinion" travelling as an absent number rather than a zero, and presence as the edit unit in the patch), the detached-post count, all nine `toConnectError` reasons with the parse failure's line/reason params and the open-ended number claiming no ceiling, and every rpc refusing an anonymous caller
- `internal/modelcatalog/rpc/handler_test.go`: the catalog projection field by field with its estimator combos, an unknown purpose refused, the purpose/curation writes answering with the row they wrote (per-purpose level and effort), the model-id guard before anything is read, all six `toConnectError` reasons, and the document plan/issue mapping line by line
- verify: ARCH-26 green (gofmt, vet, build, 61 test packages)
