# T368 The editor page suites pin behavior, not wiring
> st:done@260925 | ssot:- | base:ARCH@11 | dep:T355 T356 T363 T365 T367

## goal
`pages/editor/ui/EditorPage.test.tsx` becomes six step-scoped page suites on shared helpers and row builders. Duplicates of slice tests go, and the lock wiring they held folds into the published-lock cases. No case reads the query cache by key position, and the stale header and timeout override go. The per-control cases stay in the page suites, unchanged, until T375 moves them, so coverage never drops between the two tasks.

## acceptance
- [v] `src/test/posts.ts` exports `finalizedPostRow(row)` and `publishedPostRow(row)`.
  - Every finalized or published row of a case this task keeps (tables in impl notes) is built with them. So is every describe-level row constant.
  - The rows of the 38 per-control cases stay literal until T375 moves those cases.
- [v] No page suite reads a query key by position. Two new cases pin this in `src/test/arch-post-cache-keys.test.ts`:
  - "lets no page, widget or feature test read a query key by position": no `*.test.ts(x)` under `pages/`, `widgets/` or `features/` matches `/\bqueryKey(\[\d|\.at\()/`.
  - "lets nothing outside entities/quality name a quality query key": the source-file scan it already runs for post keys, over `qualityQueriesKey`, `accountQualityQueryKey`, `accountQualityQuery` and `postMeasurementQueryKey`, with `entities/quality/` as owner.
- [v] The stale header and the timeouts are gone: the "mounts … 71 times" comment, `vi.setConfig({ testTimeout: 10_000 })` (`:28-31`), and the two per-test `15_000` overrides (`:3804`, `:3852`). The configured `testTimeout: 30_000` (`vite.config.ts:81`) applies, which is above the 15s testing-library deadline, as that file requires.
- [v] The file is split by editor step into the six suites in impl notes, with shared helpers in `src/test/editor.tsx`. `EditorPage.test.tsx` no longer exists.
- [v] Every case of today's file is disposed of exactly as the tables in impl notes say:
  - kept, in the named suite;
  - kept for T375, unchanged, in the named suite;
  - folded into the named case;
  - or deleted, with the named test that already pins it still passing.
- [v] Cases other tasks add or rename first are placed by the same rule:
  - T355's `describe('the brief run options')` is kept in the generate suite with its three cases: "keeps 분야 and 기억 사용 out of ①, after the quality rows in the brief", "saves the brief's run options together and keeps them across a reload" and "offers no run options before the first save, and the create carries no 분야".
  - T363's "spends a taken candidate, even where its phrase holds the source" is kept for T375, in the refine suite.
- [v] Behavior unchanged. No kept or folded case loses an assertion except the lines named below, each with the test that already pins it. Every slice test named as a pin still passes.
- [v] FE gate passes: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- from review/published-quality-260924 F26
- Base: after T355, T356, T363, T365 and T367.
  - The peer session's 분야-chip and A/B-tooltip edits that were uncommitted at 58e1b5ca landed in 63330adb and 88a7e1a4.
  - T355 (POST-89) has then already changed this file:
    - it deleted `describe('the post 분야')` (six cases), "keeps 목표 글자 수 when 기억 사용 is ticked after it" and `the brief quality rows`' "omits the rows on /posts/new";
    - it added `describe('the brief run options')` (three cases);
    - it moved ①'s 기억 사용 line of "shows ① read-only…" into that case's brief section, beside the 분야 chips.
  - Use case titles as the handle. The `:NNN` numbers are the on-disk file at 58e1b5ca (4,035 lines) and will have drifted.
- Measured at that point:
  - 127 `it` / `it.each` declarations run 131 cases;
  - 111 `renderAppAt(` sites mount 133 times (`renderMarks`, `renderPost` and `renderPublished` mount once per case);
  - classified 72 page cases, 17 duplicates, 38 per-control cases.
  - This task leaves about 119 mounts. T375's moves take it to about 80.
  - T355's changes shift these counts. It removed three of the 17 duplicates ("clears with 없음", "takes a refused pick back and says why" and the 분야 "is disabled on a published post"), and it trims about five mounts. Recount at the base; the tables are the handle.
- **Row builders** (`src/test/posts.ts`, beside `FAKE_PUBLISHED_URL` :230):
  - `export function finalizedPostRow(row: FakePostRow & { slug: string }): FakePostRow`. It returns `{ status: 'finalized', content: POST_CONTENT_FIXTURE, contentRevision: 1n, machineBaselineRevision: 1n, canFinalize: true, finalizedRevision: 1n, finalizedAt: '2026-08-20T12:00:00Z', ...row }`, importing the fixture from `./fixtures/postContent`.
  - `export function publishedPostRow(row: FakePostRow & { slug: string }): FakePostRow` returns `finalizedPostRow({ status: 'published', publishedUrl: FAKE_PUBLISHED_URL, publishedAt: '2026-08-21T09:00:00Z', ...row })`.
  - `canFinalize` is always explicit ← the fake derives it on GetPost (`test/posts.ts:402-403`) but a SavePostDraft answer uses `existing?.canFinalize ?? false` (`:592`), so rows that go through a draft save (`:3757`, `:3808`) depend on it.
  - `finalizedAt` / `publishedAt` are always set, which is inert: no UI reads either, only `entities/post` maps them.
  - Apply them here:
    - `:1076`, `:2076` (`{ ...reviewPost, finalizedRevision: reviewPost.contentRevision }` becomes a builder call with those overrides), `:2563`, `:2646`;
    - the describe-level constants `:3350` (marks `finalized`), `:3577` `published`, `:3859` `finalizedPost`, `:3870` `publishedPost`;
    - `:3767`, `:3812` and its `getSequence` entries `:3831`;
    - the folded rows below.
  - The fixture address in each of these rows is `FAKE_PUBLISHED_URL`.
  - Leave these literal:
    - `:2309` / `:2337` (provenance rows with no revisions) and `:1578` (a spread of `reviewPost`);
    - the rows of the cases kept for T375 (`:1131`, `:1188`, `:1249`, `:1295`), and `:3970`'s expected address;
    - the deliberately different addresses: `/alice/223000000001` (`:3257`, `:3914-3920`), the mobile `/alice/7` pair (`:3928-3933`), `/alice/2` (`:3972-3976`) and the refused inputs (`:3942-3944`).
- **Positional reads:**
  - "refetches after a block save" (`:3076-3080`): delete the cache inspection. The case keeps its calls-ordering proof (`calls.indexOf('SavePostContent') < calls.lastIndexOf('GetPostMeasurement')`) and the no-loading-line check. Reading at the new revision is the entity's rule, pinned by `entities/quality/ui/PostMeasurementRow.test.tsx:252` "reads again when the revision moves, keeping the last reading meanwhile".
  - "re-reads the aggregate when the target language changes" (`:3202-3209`): replace it with `expect(calls.lastIndexOf('GetAccountQuality')).toBeGreaterThan(calls.indexOf('SavePostDraft'))`. The server renders rule texts in the post's stored target language (`backend/internal/quality/rpc/handler.go:57`), and `GetAccountQualityRequest` carries only the slug, so "the rows read in the new language" is exactly "a read after the language save".
  - "refetches the aggregate after a URL save" (`:3227`): delete (see the deletions).
  - `queryClient.invalidateQueries()` in "reseeds ① from the server when the post reopens" (`:3844`) names no key and stands in for another tab's URL clear. Keep it.
- **Arch pin** (`src/test/arch-post-cache-keys.test.ts`):
  - Add a `testFiles(dir)` beside `sourceFiles` that keeps only `*.test.ts(x)`.
  - Add the two cases in acceptance, and extend the header comment by one line each.
  - The file name stays.
  - Both cases pass after the positional reads above are gone. Today the positional rule's only offenders are those three reads, and nothing outside `entities/quality` names a quality key.
- **Split: yes, by editor step.**
  - Why:
    - vitest runs files in parallel, so six files cut the wall time;
    - a failure then names its step;
    - T375's moves then each touch one suite;
    - the only cost is shared helpers, which go to `src/test`.
  - The helpers go in `src/test/editor.tsx` (ARCH-16: `src/test` holds shared harnesses):
    - `USER`;
    - `openStep`, `openFinalize`, `finalize`, `BRIEF_TRIGGER`, `generateTab`, `openBrief`, `briefField`, `dockField`, `voiceField`, `templateField`, `stubBrowserImagePipeline` (today's `:26-126`);
    - `stubLearningHandoff(entries: Record<string, string>)`, which replaces the inline `localStorage` stubs at `:1117`, `:1170`, `:1232`, `:1278`, `:2661` and `:3654`, with `length` and `key` on every copy. T375's `VoiceLearningPanel.test.tsx` reuses it;
    - `M1_OVER_BAND`, today `OVER`, which T355 lifted from `the brief quality rows` to file scope for `the brief run options`. The generate and published suites both use it;
    - `resetEditorTest()`: `cleanup`, the clipboard restore (capture the original at module load), `discardUploadBatches`, `initializeI18n('ko')` and `vi.unstubAllGlobals`.

    Each suite's `afterEach` calls `resetEditorTest()` and `clearCaret()`. `clearCaret` is a deep import, so keep it in the test files, which steiger skips, not in the harness.
  - Shared fixtures used by more than one suite (the `PROVIDERS` shapes at `:299/342/451/483`, `:609/664/751/781`, `:1048/1155/1216/1263/1309/2087/2675`) become named exports of `src/test/editor.tsx` when two suites use the same one. Otherwise leave them local.
  - Keep each describe block's local helpers (`pickVoice`, `pickTemplate`, `pickField`, `renderMarks`, `renderPost`, `renderPublished`, …) with the block in its new suite.
- **The six suites** (`pages/editor/ui/`). "Kept" cases stay for good. "For T375" cases stay unchanged until T375 moves them.
  - `EditorPage.lifecycle.test.tsx`:
    - kept:
      - "restores the title and the memo", "routes a failed A/B job to its existing recovery screen";
      - "reports a slug that belongs to someone else as theirs, not as missing", and "reports an unknown slug as missing" with "offers a retry only when the failure was not an answer" merged into it (same mount);
      - all of `the delete control` and `a new draft`;
      - `the editor lifecycle steps`, with `it.each` "opens a %s post on %s" trimmed to the `['review', '글 다듬기']` row ← the mapping is `pages/editor/model/steps.test.ts:5` and `useDraftSteps.test.ts:4`. One row pins that the page follows it; `published` is pinned by "lands on 글 완성…";
      - `the editor status region`.
  - `EditorPage.generate.test.tsx`:
    - kept:
      - "resumes polling the active job exposed by the post", "refreshes observations while running and renders the review draft after completion", "keeps the empty-profile warning non-blocking for zero-photo generation", "flushes the newest memo before it starts generation", "routes generation through the re-observation picker and freezes the confirmed set", "starts an explicit A/B comparison with the configured pair and optional target";
      - `the brief quality rows`: "saves a tick and keeps it across a reload", "re-reads the aggregate when the target language changes", and "disables the ticks while a job runs" minus its tip line (`:3296`);
      - `the brief run options` (T355), all three cases. "offers no run options before the first save, and the create carries no 분야" is a page case because `EditorDockHeader` is what hands a post-less draft no `options`, and no slice test sees that.
    - for T375:
      - `:511` "keeps ordinary generation usable when only the A/B pair is missing", `:546` "sends the active writer only for ordinary generation", `:648` "sends an EMPTY frozen set…", `:686` "routes the A/B comparison through the same picker", `:732` "enqueues nothing when a confirmed picker has gone stale", `:773` "starts directly when the post has photos but no stored observation";
      - `:853` "fills 목표 글자 수 with a usable default…", `:877` "restores and explicitly clears a stored target length…";
      - `:1320` "restores its photos in the strip…", `:1348` "deletes a photo from the strip through DeleteImage", `:1379` "keeps a photo whose delete failed and says so".
  - `EditorPage.assignments.test.tsx`:
    - kept:
      - `the post voice`: "starts a new draft in the default voice…", "lets a new draft pick another voice…", "reassigns an existing post after confirmation…", "keeps learning disabled when a finalized post is reassigned…", "renders a deleted voice as a tombstone…", "recovers a deleted-voice post…";
      - `the post language`: "snapshots…", "sends an explicit pre-create language…", "restores an existing post language…";
      - `the post template`: "defaults a new draft to 없음 and sends no template with the create", "shows the numbers a picked template seeded", "carries a chosen template into the create". The two new-draft cases are the only pin of `useDraftAssignments`, which has no test of its own, like the voice's "starts a new draft in the default voice…". Drop the doubled `{Escape}` (`:2388/2390`). T375 later moves their per-control lines;
      - `the template data fields in ①`: "seeds the fields from what the post already answered", "autosaves an answer on the memo's own queue" ← after T367 these are the page's only pins of `answerPatch` / `setAnswers`;
    - for T375:
      - `:2102` "cancels a reassignment from the sheet…", `:2120` "blocks reassignment while a job is active…", `:2136` "reports a refused reassignment under the field…";
      - `:2512` "says so and offers a retry when the directory cannot be read", `:2526` "stays usable during a job…".
  - `EditorPage.refine.test.tsx`:
    - kept:
      - "resumes an active revision beside the rendered content", "does not offer a no-op retry when a resumed revision fails", "re-seeds the local 가제 from the confirmed title before another save can queue", "finalizes without an analyze model or learning call", "keeps the post finalized when explicit learning fails", "returns a finalized post to review after the first changed content save";
      - "saves an edited block before finalizing it";
      - `the post measurement row`: "sits above the article on ②", "refetches after a block save", "is absent while ② has no draft";
      - `the replacement marks`: "saves a take with the expected revision and returns 확정 to 검토".
    - for T375:
      - `the draft read-first` (`:1784`, `:1799`, `:1823`, `:1842`);
      - `:1905` "closes a block editor when the block moves", `:1918` "keeps a block edit made while the header editor was open";
      - `the replacement marks`: `:3380`, `:3409`, T363's "spends a taken candidate…" (was `:3461`), `:3478`, `:3493`, `:3524`.
      - Also `:3507` "drops a mark whose source was edited away", unchanged ← its source-edited-away half is pinned by `entities/post/model/replacements.test.ts:74`, but its "an open editor shows no marks" half has no slice test until T375 writes `BlockEditor.test.tsx`. So T375 deletes it, not this task.
  - `EditorPage.finish.test.tsx`:
    - kept:
      - "switches export formats without making another client request", the `it.each` "keeps manual export and makes no publishing call…";
      - "exports the frozen content provenance…", "fails closed instead of guessing…";
      - `sentence feedback` (both);
      - `the 발행 URL field`: "publishes on a pasted address and stays on ③", and the `it.each` "clears by %s back to 확정, and ② is editable again".
    - for T375:
      - `:1114`, `:1167`, `:1229`, `:1275` (the learning-handoff cases, now on `stubLearningHandoff`);
      - `the 발행 URL field`: `:3892`, `:3923`, `:3937`, `:3958`, `:3966`, `:4008`.
  - `EditorPage.published.test.tsx`: all six cases of `a published post`, with the folds below.
- **Folds** (the lock wiring a deleted case held, kept inside the lock cases):
  - The 분야's "is disabled on a published post" is already folded: T355 deleted it and asserts the 분야 chips and 기억 사용 disabled in "shows ① read-only, says why once and sends no write"'s brief section.
  - "disables the ticks on a published post" (`:3299`) goes into "shows ① read-only, says why once and sends no write". The `published` row gains `qualityRules: ['title_saturation']`, and `renderPublished` gains `quality: { accounts: { [published.slug]: M1_OVER_BAND } }`. In the open brief, M1's box is disabled and checked. The tip line is not carried.
  - "is hidden on a published post" (measurement, `:3085`) goes into "reads ② as prose under the one sentence…". `renderPublished` gains `quality.measurements` for the slug, and `calls`. Assert no `이 글의 측정값` region and 0 `GetPostMeasurement`.
  - "shows no marks on a published post" (`:3539`) goes into the same case. The `published` row gains `replacementCandidates: [{ surface: 'title', index: 0, source: '제주', phrases: ['제주도'] }]`, whose source stands in `POST_CONTENT_FIXTURE`'s title "비 온 뒤의 제주". Assert the article has no button.
- **Deletions, each with the test that already pins it** (six: five cases plus the tip lines). T355 already deleted the 분야's "clears with 없음" and "takes a refused pick back and says why" with the 분야 channel. The review's ninth, `:3507`, is T375's, as noted above.
  - "assigns and clears an existing post through the draft queue" (`:2458`): the assignment is "shows the numbers a picked template seeded"; the clear is `assignment-queue.test.ts` (T366) "treats 없음 on a post that holds one as a clear" [템플릿]; the cache follow is `entities/post/api/useSavePostDraft.test.ts:65`.
  - "survives a delayed title save" (`:2487`): `assignment-queue.test.ts` "does not let text typed during an assignment revert it" [템플릿].
  - "renders no field section for a post on 없음" (`:2782`): `features/fill-template-answers/ui/TemplateAnswerFields.test.tsx:51` and `model/answers.test.ts:35`.
  - "refetches the aggregate after a URL save" (`:3227`): `entities/post/api/useSavePublishedUrl.test.tsx:61` "marks every quality query stale after a paste, a replace and a clear". The page's remaining call count is vacuous, since the test client's `staleTime` is 0 and any remount reads.
  - The tip lines of "disables the ticks while a job runs" / "…on a published post" (`:3296`, `:3329`, the reviewer's other example): `features/choose-quality-rules/ui/QualityRuleChoices.test.tsx:270` "keeps the tip operable while the box is disabled". That file is also T356's; check the title still exists after it.
  - "says a busy refusal in place and leaves the post as it was" (`:4020`): `features/record-published-url/ui/PublishedUrlField.test.tsx:32`, the `POST_BUSY` row.
- **Out of scope:**
  - the 38 per-control moves and the new slice test files (T375);
  - trimming the B-/C-part assertions that sit inside kept page cases, which cost no mount;
  - the real sleeps in kept cases (`:3753`, `:3794`, `:2601`);
  - any production code change.

## result
- The split was done mechanically by `split368.py` (scratchpad): cases move verbatim by title, and each suite's imports are pruned to what it uses. `EditorPage.test.tsx` (122 case declarations) is replaced by six suites:
  - `lifecycle` (23), `generate` (23), `assignments` (19), `refine` (24), `finish` (18) and `published` (6): 113 declarations after the merge and the eight removals.
  - 'opening a post' and 'the post language' are split case by case, and the other describes move whole.
- `src/test/editor.tsx` holds the shared harness: `USER`, the step, brief and dock helpers, `stubBrowserImagePipeline`, `stubLearningHandoff`, `M1_OVER_BAND` and `resetEditorTest`. Each suite's `afterEach` calls `resetEditorTest()` and `clearCaret()`.
- `src/test/posts.ts` gains `finalizedPostRow` and `publishedPostRow`. They build every kept finalized or published row and the describe-level constants.
  - Rows left literal: the four learning-handoff cases (for T375), the two provenance rows, and the refused and expected addresses.
  - The voice case's builder call keeps an explicit `status: 'finalized'`, because it spreads `reviewPost`, whose status is review.
- Changed on the way:
  - `stubLearningHandoff` replaces the six inline `localStorage` stubs.
  - The step `it.each` is trimmed to the review row.
  - The unknown-slug case absorbs the retry case.
  - The doubled `{Escape}` is dropped, as are the tip line of the job-running tick case, the stale header, `vi.setConfig` and both `15_000` overrides.
  - Positional reads: the measurement case keeps its ordering proof, and the language case asserts a GetAccountQuality after the SavePostDraft.
- Folds into the published suite:
  - "shows ① read-only…" asserts M1 disabled and checked in the brief.
  - "reads ② as prose…" asserts no mark, no measurement region and 0 GetPostMeasurement. `renderPublished` now carries the quality accounts and measurements.
- Deleted, each pinned by the slice test named in impl notes: "assigns and clears…", "survives a delayed title save", "renders no field section…", "refetches the aggregate after a URL save" and "says a busy refusal…". The three published cases above were folded.
- The arch test gains "lets nothing outside entities/quality name a quality query key" and "lets no page, widget or feature test read a query key by position". The second failed on the old file and passes now.
- Base: ARCH@9 → @11. The r10–r11 deltas are media-worker and deploy only.
- Gates: FE tests pass (322 files, 2720 tests), as do lint, lint:fsd, lint:style:probe + lint:style and build:web.
- SSOT: no deviation.
