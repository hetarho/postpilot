# T080 Grouped writing and video navigation
> st:todo | ssot:CLIP-3 THEME-23 THEME-26 THEME-29 ARCH-13 ARCH-14 ARCH-15 ARCH-16 ARCH-20 ARCH-24 | base:CLIP@1 THEME@8 ARCH@2 | dep:T070 T072

## goal
Reshape primary navigation into visible 글 and 영상 groups without hiding existing destinations or changing their URLs.

## acceptance
- [ ] the ordinary primary destinations become 글 (`/posts`), 영상 (`/clips`) and AI 모델 (`/ai-models`); master additionally keeps 발행 도구, while account, credit/plan, billing, locale and theme controls remain in their common header/account locations
- [ ] one pathless writing-group layout shows address-backed `TabLinks` for 글 · 말투 · 글 템플릿 · 지침 across `/posts`, `/voices`, `/templates` and `/guidelines`, including their new/detail descendants
- [ ] one pathless video-group layout shows address-backed `TabLinks` for 클립 · 영상 템플릿 across `/clips` and `/video-templates`, including their new/detail descendants
- [ ] a primary group remains active on every child URL, browser back/forward restores both active levels, direct links keep working and no existing route path redirects merely to fit the grouping
- [ ] phone bottom navigation has three ordinary or four master targets at the coarse-pointer floor; laptop header and desk rail render the same top-level set and every child destination remains visible in its group tabs rather than a menu
- [ ] group tabs scroll horizontally rather than crush at 320 px, stay in normal page flow, and do not create a second scroller or second dock
- [ ] the logo/home link still targets `/posts`; non-group screens such as `/plans`, `/billing` and admin keep the primary destinations visible with none falsely marked current
- [ ] the single destination registry remains the source for phone/header/rail and one group registry remains the source for each subnav; labels and icons have ko/en parity
- [ ] router and layout tests cover every child/detail URL, active states, master filtering, direct navigation and all three responsive render shapes
- [ ] ARCH-25 passes

## impl notes
- add `WritingLayout` and `VideoLayout` under `app/routes`; they compose `Outlet` and shared `TabLinks`, while page slices remain unaware of navigation hierarchy
- determine primary active state by route matches for the group's child ids, not by string-prefix guesses (`/voices` is in 글 without sharing `/posts`)
- keep `/ai-models` as the one model-choice destination outside content groups; `/admin/models` remains inside the existing master admin layout and is not a duplicate primary entry
- T080 may land as soon as T070/T072 provide real destinations and can run in parallel with the generation/rendering chain

## result
