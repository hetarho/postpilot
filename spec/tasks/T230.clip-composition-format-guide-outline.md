# T230 형식 안내 and 원문 teach the outline grammar
> st:todo | ssot:CLIP-58 CLIP-71 CLIP-113 | base:CLIP@33 ARCH@3 | dep:T227

## goal
Rewrite the copyable format guide and the 원문 refusal copy so an outside AI is told the outline grammar alone and a paste is refused only for what the grammar cannot read.

## acceptance
- [ ] `GRAMMAR` and `CLIP_COMPOSITION_EXAMPLE` teach the outline: entry kinds, their order, bindings and the size ceilings — and no design attribute, no `basis`/`start`/`end`, no slot count, no per-preset character table, no `invalid_skeleton`
- [ ] the example declares a caption entry and more than one intro line, and parses under `ParseTemplate` in the existing guide test
- [ ] the guide states that where an entry is drawn follows the clip's own design, chosen in ①
- [ ] refusal copy (`builder.reasons`) covers the reasons that survive and drops the retired ones, in ko and en
- [ ] a pasted body is refused only with an entry id and a line from the parser — no surface refuses a paste for a preset, a slot count or an absent region
- [ ] tests: the guide's example parses, every reason key the parser can emit has copy, and a paste with an unknown tag shows its line
- [ ] ARCH-25 verify clean

## impl notes
- one file owns the grammar text (`entities/clip-template/model/composition-guide.ts`); the ko/en `composition.guide` strings interpolate it, so the grammar is written once.
- CLIP-113 binds the guide and the grammar to change together: if T227 changed a construct, this task is where the guide catches up — list the constructs against `composition/README.md` rather than against memory.
- keep `원문 복사` as it is: handing the saved body back to the AI is half the round trip the owner asked for.
- parallel sessions: `resources/{ko,en}/clips.ts` collides with T229 and T232.

## result
