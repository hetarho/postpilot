# T096 Complete multi-source clip planning and playable generation
> st:doing@260911.fix | ssot:CLIP-6 CLIP-7 CLIP-10 CLIP-11 CLIP-12 CLIP-16 CLIP-26 CLIP-27 CLIP-31 QUOTA-44 QUOTA-46 ARCH-24 ARCH-26 ARCH-31 | base:CLIP@3 QUOTA@9 ARCH@2 | dep:T091

## goal
Resolve multi-source composition failures and verify the actual analysis-to-playable-video workflow, not only provider acceptance or single-cut rendering.

## acceptance
- [v] Identify concrete planning failure modes and correct the affected parsing/composition path without inventing source footage, silently changing requested settings, paid automatic retries or relaxed approved credit ceilings.
- [v] Pin the eight-short-source, mixed-orientation, 15-second case with regressions covering valid plans, malformed plans, timing, supported Korean copy and the existing preservation/settlement guarantees.
- [v] Run real analysis, real composition and original-footage rendering across multiple synthetic sources, then inspect the resulting MP4 visually and by full decode; record exact verification scope and distinguish any replay from live calls.
- [v] Verify the authenticated queued application flow with multiple inputs, persisted result preview/download and transient-source cleanup; do not substitute a narrow adapter test for this gate.
- [ ] Pass ARCH local gates and scoped commit/push CI and deployment verification; leave the task unfinished if functional evidence is incomplete.

## impl notes
- User-approved functional follow-up to T091, not a new product-policy project; preserve the completed task and concurrent MODEL/T008 changes.
- Revalidate production job 6d555057a5cfc2e15f68e64f6f8cc114: eight observations preceded a plan-stage MODEL_OUTPUT_INVALID; old raw output was not retained, so do not claim an exact rejected field without new evidence.
- Use existing provider-neutral boundaries and production budgets. Add only code-owned diagnostic categories, never private prompt/output text, footage, signed URLs or credentials.
- Initial model-backed verification used synthetic media. The owner subsequently supplied the exact eight original videos and explicitly authorized their bounded analysis for reproduction; private inputs/responses remain outside git, the existing cumulative allowance still applies, and production account/credit settings must not change.
- Persisted edit plans must obey renderer invariants; any proposed relaxation that changes product policy goes through update-ssot first.

## result
- The original diagnostic delivery (`6db5115`) did not fix the failure: two synthetic live compositions succeeded, while the historical rejected response was unavailable. The owner then supplied the actual eight originals and explicitly authorized a reproduction. That input hold is resolved.
- Reproduced with all eight owner-supplied MP4s and the actual `일상` / `균등분할`, vertical, 15-second settings. All eight observations succeeded. Composition returned four cuts with a declared 15200ms duration but an actual fade-overlapped duration of 12700ms; three caption ends exceeded their cut lengths. The first rejection was `plan_caption_time`. This is new direct evidence, not a claim about the unavailable historical raw response.
- `internal/clip/ai/timeline.go` now compiles fresh AI timing hints before strict plan validation: computes overlap arithmetic, intersects a nonempty caption window with its cut, and distributes required end adjustments within the same observed scene. It preserves cut identity/order/start, copy/style/accent, ratio, target and audio; refuses out-of-source ranges, empty/negative exposure, unobserved extensions and newly overlapping/reused footage. An impossible timeline remains a typed failure. The persisted/manual renderer validator and approved provider budgets remain unchanged.
- Neutral regressions in `timeline_test.go` reproduce the exact failing timing shape without private IDs, observations or copy; cover both output modes, deterministic distribution, arithmetic-only corrections, shortening, growth bounds, scene gaps, overlaps and unchanged usage with exactly one provider call. Existing invalid-output tests now distinguish correctable timing hints from genuinely invalid plans. Prompt documentation describes the deterministic caller-owned timing step.
- Replayed the captured nine responses with networking disabled against the same eight originals using the production media/font runtime as nonroot with 2 CPUs and 1 GiB. This replay made no model requests. The real result has four cuts, Korean Pretendard captions, 1080x1920 H.264 at 30fps (450 frames), stereo 48kHz AAC, exactly 15.000 seconds and 18662707 bytes. Full decode passed; four visual frames show the intended footage and readable captions. Original/output audio correlations at one interior sample per cut are 0.9938, 0.9821, 0.9555 and 0.9476.
- A convenient local result copy is `Downloads/postpilot-clip-test-15s.mp4`. Originals remain unchanged. Private captures, analysis, test harness and media remain outside git in the dedicated T096 temporary directories; no raw provider output, source bytes, signed URL or credentials enter committed fixtures or logs.
- `cmd/api/clip_release*_test.go` adds `multi-source-timing`: authenticated RPC, durable preparation of all eight sources before AI, a local counted provider stub with the failing timing shape, SQLite/credit reservation, queued composition/rendering, persisted compiled plan, readable preview/download, settlement and cleanup. It passes network-disabled as nonroot with 2 CPUs/1GiB: peak memory 515010560 bytes, one original and subprocess at a time, one job workspace plus one bounded caption scratch workspace, eight proxies, nine stub calls, approved/held/charged/refund 151/151/5/146. This is an offline app gate, not another paid live run.
- The first new app assertion assumed the original HEVC duration of 4290ms; its synthetic 30fps source probes as 4300ms. The assertion now uses the authoritative synthetic duration; the exact owner-original timing remains independently pinned in the unit regression and actual MP4. App audio correlations are 1.0000 / 1.0000 / 0.9998.
- The strict cumulative live-verification allowance is 99999 micro-USD, including the previously reported 11057. Nineteen total paid calls now report 20052 micro-USD (USD 0.020052): actual-original observations added 6566 and composition 2429. Every dispatch first persisted its full maximum; no unknown usage, automatic retry, higher completion budget or further paid call was used. The existing spend ledger remains authoritative and was not reset.
- Local verification: 187 frontend files / 1497 tests, ESLint/Prettier/FSD/style probes/style checks/web build and skills synchronization pass; complete backend and agent format/vet/build/tests and agent shell syntax pass. Isolated proto/sql generation is unchanged. Pre-existing editor-hook and bundle-size warnings remain. Final race, scoped spec and remote delivery evidence are recorded below before completion.
- No SSOT policy change: the implementation fulfills the existing automatic composition target with deterministic source-grounded timing, while persisted plans retain all renderer invariants and QUOTA ceilings. Unrelated working-tree edits remain excluded.
