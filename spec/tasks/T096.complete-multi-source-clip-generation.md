# T096 Complete multi-source clip planning and playable generation
> st:blocked@260911 | ssot:CLIP-6 CLIP-7 CLIP-10 CLIP-11 CLIP-12 CLIP-16 CLIP-26 CLIP-27 CLIP-31 QUOTA-44 QUOTA-46 ARCH-24 ARCH-26 ARCH-31 | base:CLIP@3 QUOTA@9 ARCH@2 | dep:T091

## goal
Resolve multi-source composition failures and verify the actual analysis-to-playable-video workflow, not only provider acceptance or single-cut rendering.

## acceptance
- [ ] Identify concrete planning failure modes and correct the affected parsing/composition path without inventing source footage, silently changing requested settings, paid automatic retries or relaxed approved credit ceilings.
- [v] Pin the eight-short-source, mixed-orientation, 15-second case with regressions covering valid plans, malformed plans, timing, supported Korean copy and the existing preservation/settlement guarantees.
- [v] Run real analysis, real composition and original-footage rendering across multiple synthetic sources, then inspect the resulting MP4 visually and by full decode; record exact verification scope and distinguish any replay from live calls.
- [v] Verify the authenticated queued application flow with multiple inputs, persisted result preview/download and transient-source cleanup; do not substitute a narrow adapter test for this gate.
- [ ] Pass ARCH local gates and scoped commit/push CI and deployment verification; leave the task unfinished if functional evidence is incomplete.

## impl notes
- User-approved functional follow-up to T091, not a new product-policy project; preserve the completed task and concurrent MODEL/T008 changes.
- Revalidate production job 6d555057a5cfc2e15f68e64f6f8cc114: eight observations preceded a plan-stage MODEL_OUTPUT_INVALID; old raw output was not retained, so do not claim an exact rejected field without new evidence.
- Use existing provider-neutral boundaries and production budgets. Add only code-owned diagnostic categories, never private prompt/output text, footage, signed URLs or credentials.
- Use synthetic media for model-backed verification and require an explicit bounded additional allowance before paid calls; never replay historical user media or change production account/credit settings.
- Persisted edit plans must obey renderer invariants; any proposed relaxation that changes product policy goes through update-ssot first.

## result
- Blocked on actual reproduction input: the rejected production response was not retained, both synthetic multi-source compositions succeeded, and the owner has been asked for the failed originals' local folder with explicit bounded provider-replay consent. Do not claim a functional fix from diagnostic codes alone.
- Owner approved additional synthetic verification strictly below USD 0.10 aggregate: at most 99999 micro-USD, not a per-call allowance. Before dispatch count confirmed costs plus full maxima for unresolved calls plus the next call's maximum; retain production completion budgets and stop paid work when that sum would exceed the allowance.
- Ten paid calls completed with reported aggregate cost 11057 micro-USD (USD 0.011057); no unresolved call or automatic retry. Eight real synthetic observations and general-guidance composition cost USD 0.008697. Reusing those observations for one actual `일상` / `균등분할` composition cost USD 0.002360. Further paid calls stopped pending relevant reproduction input; preserve `/tmp/postpilot-t096.HRFfRS/spend.json` as cumulative spend authority.
- Both real model compositions selected four cuts and rendered 15-second 1080x1920 H.264/AAC MP4s with actual Pretendard Korean captions. Full decode and visual frame checks passed. The latest result is `/tmp/postpilot-t096.HRFfRS/multi-source-result.mp4` (13965425 bytes). These successful synthetic cases do not establish the owner's original rejection cause. Temporary paid harness was moved outside the repository; committed-candidate fixtures contain only synthetic analyses/responses and explicit provenance.
- `internal/clip/ai/multi_source_test.go` covers a deterministic eight-cut fade timeline, the two recorded real four-cut outputs, and eleven invalid-output categories without new calls, altered usage or private content. Existing clip/store failure preservation and settlement regressions pass. `parse.go`, `service.go`, `render.go`, `output_error.go` and `job/worker.go` now preserve static allowlisted validation codes; no contract relaxation or functional plan repair has been introduced without evidence.
- `cmd/api/clip_release*_test.go` adds authenticated eight-source/eight-cut queued generation using a local counted HTTP stub, actual SQLite/credits, production media binaries, original-quality rendering, readable stored preview/download and cleanup/recovery assertions. This is a network-disabled app gate, not an additional live-provider run. Passed as nonroot with 2 CPUs and 1 GiB: peak memory 528814080 bytes; worker disk peak 1724807 bytes; one original and one subprocess; eight prepared proxies, largest 65395 bytes; speech correlations 1.0000 / 1.0000 / 0.9598; synthetic ledger approved/held/charged/refund 151/151/5/146.
- First multi-source app run exposed an instrumentation assumption: nonempty captions briefly create one nested glyph-measurement scratch workspace; previous release fixtures had empty captions. Instrumentation now independently enforces one job workspace and at most that one extra caption workspace. No production resource allowance changed; the corrected gate passed.
- Local gates passed: full ARCH-26 backend and ARCH-27 agent; focused clip/AI/job race tests; isolated HEAD proto/sql generation snapshot unchanged; spec skills check and lint (eight historical warnings). Full frontend tests encountered five failures in concurrently edited T094 model-management fixtures (185 files passed, two failed); these unrelated files were left untouched. Remaining frontend gates and original-failure reproduction are still required before completion/delivery.
- Before the owner's delivery request, T096 was not committed, pushed or deployed. Main contained separate unpushed MODEL commits; none belong in the new clip commit.
- Additional offline runtime regressions passed: success, partial provider failure, result-save failure and interrupted partial analysis; all preserve cleanup/recovery/accounting behavior with no external traffic. FE FSD and style scanner probes passed. Full FE lint/format and style checks remain non-green in concurrent T094 changes (four unformatted files and `CatalogDocumentPanel.tsx`'s `font-mono`); build was not reached by the gated command chain. No unrelated files were changed to force these gates green.
- Continuation recheck: production still runs image `2d484e8` with zero restarts/OOM; the fresh 45-minute log window contains only the same plan-stage failure and no detailed rejected output. Remote main remains `353736b`; no T096 rollout occurred. The two previously failing T094 test files now pass (18 tests), but an independent pinned-Node web build reports five `levels` type errors in the concurrent `src/test/model-catalog.ts` changes. No new model request, user-source replay, speculative repair or unrelated edit was made; original input and delivery gates remain outstanding.
- Owner subsequently requested commit and push of the current work. Deliver the diagnostic categories and verified regressions as a partial implementation, not a claim that the production failure is fixed. The normal main push also carries four already-committed MODEL changes (T092/T093/T095/T094), disclosed before push; unrelated uncommitted configuration, SSOT wording and T008 edits remain excluded. No additional model call is authorized or needed for delivery.
- Delivery recheck passes all local ARCH gates: 187 frontend files / 1497 tests, ESLint, Prettier, FSD, style probe/style check, web build, skills synchronization; full backend and agent format/vet/build/tests plus agent shell syntax; staged proto/sql generation unchanged and staged spec lint valid. Remaining warnings are pre-existing editor hook dependencies, bundle size and historical spec/T008 metadata. Staged STATE revision metadata is aligned to the committed SSOT revisions while uncommitted wording edits remain intact. Remote CI/rollout evidence is still pending; T096 remains blocked on the actual failing input.
