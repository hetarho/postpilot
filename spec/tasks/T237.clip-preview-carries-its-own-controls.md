# T237 the preview carries its scrubber, its info control and its download
> st:todo | ssot:CLIP-53 CLIP-148 CLIP-149 CLIP-109 | base:CLIP@34 | dep:T236

## goal
②'s preview keeps one scrubber row stating its own second, one info control holding everything the step says about the whole clip, and one icon download for the latest successful render.

## acceptance
- [ ] the output-time scrubber is one row with no separate label line and states the second it sits at while it is dragged
- [ ] one info control beside the preview opens the preview-to-render difference (timing tolerance, approximate frame, contrast, final loudness) and every notice naming no cut and no caption
- [ ] none of that copy stands as permanent text in ②'s flow
- [ ] the latest successful render downloads from an icon control directly under the preview, named with the revision it delivers; ③'s docked download is untouched
- [ ] vitest: the scrubber exposes its seconds through its accessible value with no separate label element; the info control opens the parity copy and an untargeted notice; the download link under the preview carries the render's URL and names its revision
- [ ] verify: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`

## impl notes
- the `Slider` primitive already carries `valueText`; drop the `label={t('preview.outputTime')}` line and give the slider an `aria-label` instead, so the seconds live in `aria-valuetext` and no second row is drawn — do this in BOTH `ClipCorrectionWorkspace` and `ClipDraftPreview`
- info control: `Popover` with `triggerSize="icon"`, `triggerVariant="secondary"`, `phone="sheet"` and `placement="below"` (it is a header-side control, not a dock one); `label` is a new `preview.aboutLabel` key in ko/en
- it absorbs the `<details>` carrying `preview.parityLabel` / `preview.parity` / `preview.frameApproximate` in `ClipDraftPreview`, and the workspace's remaining `ClipNoticeList` of notices with no `cutId` and no `elementId`
- download: reuse `ClipDownloadAction` with a new `icon` shape — `buttonStyles({ variant: 'ghost', size: 'icon' })` with lucide `Download` and the existing `finalization.downloadRevision` string as `aria-label`; the `compact` variant and ③'s CTA variant both stay
- the previous change left `FinalizeClipNotices` and the download in a page section above the dock; this task removes that section, moving its notices into the info control and its download under the preview — T238 takes the finalization copy
