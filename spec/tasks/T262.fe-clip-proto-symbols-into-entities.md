# T262 clip pages and features consume entity hooks, allowlist removed
> st:todo | ssot:ARCH-17 ARCH-14 | base:ARCH@5 | dep:T259 T261

## goal
Finish the ARCH-17 migration for the clip domain: every `ClipService`/`*Schema` reference in clip pages, widgets and features moves into the four clip entities' `api` segments, and the T259 allowlist is deleted.

## acceptance
- [ ] the ESLint allowlist from T259 is removed and `pnpm lint` passes with no clip exception
- [ ] `ClipService` is named only in `entities/clip-*/api/**` and `shared/api`; the 35 former call sites consume `use*` hooks from the entity barrels
- [ ] `ClipProjectSchema`, `ClipSourceBatchSchema`, `ObservationSchema` and friends are constructed only inside entity mappers; features hand domain objects to the hooks
- [ ] `frontend/src/test/clips.ts` registers fakes through the entity hooks' service descriptors only (one `registerClipService` still exists but no test file names a proto message)
- [ ] behaviour unchanged: all clip tests pass; the T259 source-tree vitest now covers the clip slices too
- [ ] ARCH-25 passes

## impl notes
- from review/arch-260919 F5 (clip half)
- runs after T261 so the hooks land in the widget's handles rather than in a page that is about to be deleted
- each clip entity owns the descriptors of its rpc family — `clip-project` (projects, sources, quote, finalize), `clip-plan` (revision, captions), `clip-preview` (render, browser render, preview), `clip-observation` (attempt inspection); T281 later splits the proto service along the same line, so this mapping is the one to keep

## result
