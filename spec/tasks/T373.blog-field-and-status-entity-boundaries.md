# T373 Entity boundaries for 분야 and post status
> st:todo | ssot:ARCH-3 ARCH-17 | base:ARCH@9 | dep:T356 T367

## goal
- Wire mappers stop at their entity: no entity index exports one, and the draft save takes a typed 분야 that the entity maps itself.
- An unknown 분야 on the wire fails the read.
- Status splits come from one exhaustive predicate pair in `entities/post`.

## acceptance
- [ ] F20: none of the three entity indexes exports a wire mapper:
  - `entities/blog-field/index.ts` no longer exports `blogFieldFromProto` / `blogFieldToProto`.
  - `entities/quality/index.ts` no longer exports `qualityMetricFromProto` / `qualityMetricToProto` / `qualityVerdictToProto`.
  - `entities/post/index.ts` no longer exports `replacementSurfaceToProto`.
- [ ] F20, continued:
  - No file under `src/test` imports an entity mapper.
  - `features/save-draft` imports no `*ToProto`, and it no longer uses `contentLanguageToProto`.
  - `useSavePostDraft().save` takes a domain `PostDraftSave` with `field?: BlogFieldChoice` and resolves the saved slug.
  - The `fieldId as BlogFieldChoice` cast is gone because the 분야 is typed `BlogFieldChoice` end to end.
- [ ] F21: a 분야 number this build does not know throws from `toPostDraft`, `toGuideline` and `toGuidelinePreset`, instead of reading as 없음 or being dropped. A guideline or preset set that carries UNSPECIFIED also throws. Pinned by:
  - `entities/blog-field/api/blog-field-mappers.test.ts`: `requireBlogField` / `requireBlogFieldId` throw on 9_999; `requireBlogFieldId` also throws on UNSPECIFIED, while `requireBlogField(UNSPECIFIED)` is ''.
  - `entities/guideline/api/guideline-queries.test.ts`: the two "dropping anything that names no 분야" cases become "refuses a set with a 분야 this build does not know" (9_999, and separately UNSPECIFIED).
  - `entities/post/api/post-queries.test.ts`: "refuses a 분야 this build does not know".
- [ ] F27: `entities/post` exports `isUnfinalized` and `isFinalizedOrLater`, both built on one `Record<PostStatus, …>`, so a new status fails typecheck until it is placed. The four `||` chains use them. The dead lock checks at `EditorRefinePanel.tsx:67` and `BlockEditor.tsx:87` are deleted. Pinned by `entities/post/model/types.test.ts` "splits every status into exactly one lifecycle half, and an unknown one into neither".
- [ ] ARCH-17 guard: ESLint rule plus the `src/test/arch-proto-symbols.test.ts` case "imports no entity wire mapper into a page, widget or feature" (a name ending `ToProto`/`FromProto` imported from `@/entities/*`). The new case fails at HEAD on `features/save-draft/model/useAutosave.ts`.
- [ ] `useSavePostDraft.test.ts` "sends the 분야 as the wire enum: an id as its value, 없음 as a present UNSPECIFIED, absent as absent".
- [ ] New `src/test/wire-enum.test.ts` pins the fakes' name rule over the four enums.
- [ ] Behavior is otherwise unchanged and existing tests still pass. This covers `useAutosave.test.tsx`, `field-queue.test.ts`, the draft-queue suites, `QualityRuleChoices.test.tsx`, `PostMeasurementRow.test.tsx`, the guideline and editor page tests, and `ExperimentActions`, `FinalizeActions` and `PublishedUrlField` tests. The only exceptions are the drop cases that F21 turns into refusals.
- [ ] FE gate passes: `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web`.

## impl notes
- from review/published-quality-260924 F20 F21 F27
- dep T367: T366/T367 reshape `features/save-draft` (draft-queue channels, `SendDraft`, useAutosave's lock). Do this on top of their shape. The anchors below are HEAD's (58e1b5ca). Find the SavePostDraft send and the 분야 channel wherever they live after T367.
- **Who imports the index mappers today:**
  - `src/test/guidelines.ts:18,135,146,177,203`
  - `src/test/quality.ts:14-15,81-82`
  - `src/test/posts.ts:47-53,262,275,395,408,499,534,671`
  - `features/save-draft/model/useAutosave.ts:2,142`

  Nothing else outside the entities does.
- **Fakes get their own name-derived mapping.** Neither `@x` nor a test entry point:
  - `@x` is ARCH-13's entity↔entity notation.
  - A second public file per slice is what "exactly one index.ts" forbids.
  - A fake that reuses the production mapper cannot catch that mapper's bug.

  Add `frontend/src/test/wire-enum.ts` with two functions:
  - `toWire<E extends Record<string, string | number>>(wire: E, id: string): number` returns `wire[id.toUpperCase()]`, and throws `fake: <id> names no member` when that is not a number.
  - `fromWire<T extends string>(wire: Record<string | number, string | number>, value: number, ids: readonly T[]): T | undefined` looks up the reverse-mapped member name, lower-cases it, and returns it only if it is in `ids`. Otherwise it returns undefined, so UNSPECIFIED and unknown numbers are undefined.

  The rule holds for all four mirrored enums: `domestic_travel`→`DOMESTIC_TRAVEL`, `title_saturation`→`TITLE_SATURATION`, `over_band`→`OVER_BAND`, `tag`→`TAG`.

  Rewrite the fakes:
  - `test/guidelines.ts`: `fromWire(ProtoBlogField, f, BLOG_FIELD_IDS)` and `toWire(ProtoBlogField, id)`.
  - `test/quality.ts`: `toWire(ProtoQualityMetric, …)` and `toWire(ProtoQualityVerdict, …)`.
  - `test/posts.ts`:
    - `fixtureField` keeps its '' → UNSPECIFIED branch and uses `toWire(ProtoBlogField, id)`.
    - `:499` / `:534` read UNSPECIFIED as '' explicitly, then `fromWire(ProtoBlogField, req.field, BLOG_FIELD_IDS)`.
    - `:395` uses `toWire(ProtoReplacementSurface, …)`.
    - `:408` / `:671` use `toWire` / `fromWire(ProtoQualityMetric, v, QUALITY_METRICS)`.
    - The `:262` type becomes `ProtoQualityMetric[]`.

  `BLOG_FIELD_IDS`, `QUALITY_METRICS` and the proto enums already come from the public indexes and `@/shared/api`.

  `wire-enum.test.ts` asserts:
  - every id in `BLOG_FIELD_IDS`, `QUALITY_METRICS`, the four verdicts and the three surfaces maps to a member and back;
  - 9_999 and UNSPECIFIED read as undefined;
  - an unknown id throws.
- **The entity owns the draft-save request.** In `entities/post/api/useSavePostDraft.ts`:
  - Add `export interface PostDraftSave { slug: string; title: string; memo: string; templateAnswers: readonly { label: string; text: string; enabled: boolean }[]; voiceId?: string; templateId?: string; targetLanguage?: ContentLanguage; field?: BlogFieldChoice }`.
  - `useSavePostDraft()` returns `{ save: (draft: PostDraftSave) => Promise<string> }`. `save` maps `targetLanguage` via `contentLanguageToProto`. It maps `field` via `blogFieldToProto`: undefined stays absent, and '' becomes a present UNSPECIFIED, the clear (POST-82). Then it calls `mutateAsync`, throws `Error('SavePostDraft returned no post')` when the answer has no `post.slug` (moved from `useAutosave.ts:146`), and resolves the slug.
  - Keep `onSuccess` / `onError` as they are.
  - `mutateAsync` is no longer returned ← a feature holding the proto-typed send could bypass the domain input again.
  - Export `type PostDraftSave` from `entities/post/index.ts`.
  - `useSavePostDraft.test.ts:200` calls `save(...)`.
  - The new case uses `createRouterTransport` capturing `req.field` for three saves: `field: 'cafe'` → `ProtoBlogField.CAFE`, `field: ''` → `ProtoBlogField.UNSPECIFIED` (present), and no `field` → `undefined`.
  - `useAutosave` sends a `PostDraftSave` and returns the slug. It drops `blogFieldToProto`, the cast and `contentLanguageToProto`, and keeps `appFailureFromConnect`.
- **Typed 분야 end to end.** `UseAutosaveArgs.post.field?` / `fieldId?` and `assignField(fieldId)` become `BlogFieldChoice` (`useAutosave.ts:25,43,80`). So does the queue's 분야 channel: at HEAD `draft-queue.ts:72,98,129,159,638`, and after T366 whatever its channel/`Assignment<T>` is, instantiated with `BlogFieldChoice`. The queue imports `type BlogFieldChoice` from `@/entities/blog-field`. Callers already pass `BlogFieldChoice` (`useDraftAssignments.ts:19`, `PostFieldSelect` `onSelect`). The test `setup` helpers type `field?: BlogFieldChoice` (`useAutosave.test.tsx:26`).
- **Mappers stay reachable where they belong.**
  - `@x/post.ts` exports `blogFieldToProto`, `requireBlogField` and `type BlogFieldChoice`.
  - `@x/guideline.ts` exports `BLOG_FIELD_IDS`, `blogFieldLabelKey`, `type BlogFieldId`, `blogFieldToProto` and `requireBlogFieldId`.
  - `entities/quality/@x/post.ts` is unchanged.
  - `blogFieldFromProto` stays in `blog-field-mappers.ts`, the non-throwing primitive the two `require*` are built on. Its own test still pins it. It is no longer exported from any `@x` or index.
- **F21:**
  - `blog-field-mappers.ts`, following `requireQualityMetric` (`quality-mappers.ts:47-51`):
    - `export function requireBlogField(value: ProtoBlogField): BlogFieldChoice` throws `unsupported blog field enum: <n>` for an unknown number, and gives '' for UNSPECIFIED.
    - `export function requireBlogFieldId(value): BlogFieldId` also throws on UNSPECIFIED ← the server filters UNSPECIFIED out of every guideline set (`backend/internal/guideline/rpc/handler.go:224`), so one arriving is a read this build does not understand. Dropping it would be the same silent fallback ARCH-3 forbids.
  - `post-queries.ts:34` becomes `field: requireBlogField(post.field)`. That is the path `requireContentLanguage` already takes in the same mapper. Do not move `usePost`'s mapping out of its `useMemo` in this task.
  - `guideline-queries.ts:50-55` `toFieldIds` becomes `fields.map(requireBlogFieldId)`. Update its doc comment. Both callers (`toGuidelinePreset`, `toGuideline`) run inside `guidelineListQuery`'s `queryFn`, so the page shows its read failure with a retry, as for an unknown scope.
  - Leave `post-queries.ts:59-62`'s drop of an unknown quality tick and `toReplacementCandidate`'s drop of an unknown surface as they are: they are not in F21.
- **F27**, in `entities/post/model/types.ts` beside `isPublished`:
  - `const PHASE: Record<PostStatus, 'open' | 'closed'> = { draft: 'open', review: 'open', finalized: 'closed', published: 'closed' }`.
  - `export function isUnfinalized(post: { status: string }): boolean { return isPostStatus(post.status) && PHASE[post.status] === 'open' }`.
  - `isFinalizedOrLater` is the same with `'closed'`.
  - The names are the lifecycle halves, not the member list ← a status added to either half keeps the name true.
  - An unknown status is in neither half, the same as every current `||` chain.
  - Export both from the index.
  - Replace the chains:
    - `PublishedUrlField.tsx:43`: `closed = isUnfinalized(post)`
    - `ExperimentActions.tsx:66`: `Boolean(post && isUnfinalized(post))`
    - `FinalizeActions.tsx:55`: `finalized = isFinalizedOrLater(post)`
    - `useVoiceLearning.ts:98`: `isFinalizedOrLater(post) && …`
  - `isPublished` stays and is still used where the question is "published" alone.
  - Dead checks (verified: `BlockEditor` has one mount, `EditorRefinePanel.tsx:59`, in the `isPublished` false branch; T351's result records the `BlockEditor` guard as an equivalent mutant):
    - `EditorRefinePanel.tsx:67`: `beforeArticle={<PostMeasurementRow …/>}`, keeping the comment's QUAL-36 half.
    - `BlockEditor.tsx:87`: `useMemo(() => visibleSpans(content, post.replacementCandidates), [content, post.replacementCandidates])`. Drop the now-unused `isPublished` import.
  - Other `status ===` literals (`pages/clips/model/search.ts:9`, `VoiceRuleComparisonPage.tsx:66`, `model-experiment/model/types.ts:111`) are other nouns' statuses. Leave them alone.
- **ARCH-17 guard (decided: extend, entity sources only).** In `frontend/eslint.config.js`'s pages/widgets/features block (`no-restricted-syntax`), add a second entry:
  - selector: `"ImportDeclaration[source.value=/^@\\/entities\\//] > ImportSpecifier[imported.name=/(ToProto|FromProto)$/]"` (checked: the installed esquery parses the escaped slashes)
  - message: `'엔티티의 wire 매퍼는 엔티티 안에서만 써요. 엔티티가 도메인 값을 받는 훅을 내보내게 하세요 (ARCH-17).'`

  In `src/test/arch-proto-symbols.test.ts`, add the case using `importedNames(source, '@/entities/[a-z-]+')`. The helper interpolates the module into a RegExp. Filter with `/(ToProto|FromProto)$/`.

  This needs no ARCH change: ARCH-17 already says pages, widgets and features consume "hooks and domain types an entity exports". Do NOT extend it to `@/shared/api` names: ARCH-17 makes language mapping and failure normalization shared/api's own surface, and `appFailureFromProto` is used by two pages. Restricting those would be an ARCH decision, so it is left alone. Covering `entities/*/{ui,model,lib}` (review notes) is also left to create-architecture.

## result
