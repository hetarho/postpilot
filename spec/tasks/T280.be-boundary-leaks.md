# T280 six boundary leaks translated at the adapter
> st:todo | ssot:ARCH-6 ARCH-7 ARCH-24 | base:ARCH@5 | dep:-

## goal
Fix the six ARCH-7 leaks the review listed so the next context does not copy them.

## acceptance
- [ ] `billing/purchase.go:114` no longer matches `usage.ErrLotTouched`: the `billing.Credits` port returns a billing-owned error the adapter translates
- [ ] `plan/rpc/handler.go:62`: the plan port returns a plan-owned balance type, not `usage.Balance`
- [ ] `billing/ports.go:33`: `Provider.ParseNotification` takes the parsed body/headers as a billing type; the `*http.Request` is unwrapped in the rpc/http adapter
- [ ] `json:"` tags leave `generation/generation_payload.go`, `generation/parse.go`, `voice/learning.go`, `voice/diff.go`: the wire structs move to `*/store` or `*/rpc` with a mapper, domain types are tag-free
- [ ] `auth/provision/adduser.go` receives its config from the caller (`cmd/api`/`cmd/adduser`) instead of calling `config.Load()`
- [ ] `internal/health` moves under `internal/platform/health` (ARCH-6) and imports `connect`+`gen` there
- [ ] behaviour unchanged: ARCH-26 passes; the JSON shape stored/emitted is pinned by a golden test before and after the tag move

## impl notes
- from review/arch-260919 F20
- each item is a one-mapper change; the golden test on JSON is the only risk (a struct tag move must not change the stored payload)

## result
