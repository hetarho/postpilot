# T088 Guideline list with a docked create sheet
> st:todo | ssot:GUIDE-20 GUIDE-25 GUIDE-5 GUIDE-19 THEME-24 THEME-29 ARCH-14 ARCH-20 ARCH-24 ARCH-25 | base:GUIDE@2 THEME@9 ARCH@2 | dep:-

## goal
Make `/guidelines` the list of saved rules with one docked `새 지침` opening the create surface, the way its sibling directories work.

## acceptance
- [ ] `/guidelines` renders the title, its description, the saved list in injection order and nothing else standing — no create section and no form on the page
- [ ] one `새 지침` sits in an `ActionBar dock="list"` at every width, full-bleed on a phone and right-aligned at its natural width from `sm:`, and opens the shared `Sheet` (a bottom sheet below `md:`)
- [ ] the sheet holds the textarea with its live remaining count, the help line and the scope control, with the committing action in flow after the last field; 전역 is preselected
- [ ] a successful create closes the sheet and the new rule appears in the list in server order; a refusal (over 300 characters, duplicate, account cap) keeps the sheet open with the catalogue message under the field and nothing is cleared
- [ ] the empty account still shows the worked example as copy, with the dock present
- [ ] one create surface exists at any width — no phone form plus a desktop copy — and the scope control is still the shared `entities/guideline/ui` one
- [ ] existing behaviour is unchanged: text edit, whole-scope edit, delete with its frozen-text confirmation, the `(accountId, 'guidelines')` query keying and the load-failure notice
- [ ] `GuidelinesPage` tests cover opening the sheet, creating, each refusal keeping the sheet open, the empty state and the dock at both phone and desktop widths
- [ ] ARCH-25 passes

## impl notes
- replace `features/create-guideline/ui/CreateGuidelineForm.tsx` with `CreateGuidelineSheet.tsx` and model it on `features/create-voice/ui/CreateVoiceSheet.tsx` — the trigger owns the open state so there is exactly one instance and one overlay (VoicesPage is the reference for the dock plus sheet pair)
- keep every rule already in the form: 전역 default, the countdown `CharCount`, clearing only what was submitted, resetting the scope after a success, and rendering the mutation's message under the field
- the textarea must not be docked (a field inside a bottom bar sits behind the keyboard); the dock holds the trigger only
- new `guidelines` i18n keys in ko and en: the trigger label, the dock's `aria-label` and the sheet title; drop the retired `page.new` section heading
- the page keeps composition only — no domain logic moves into `pages/guidelines`
- do not touch the candidate section in this task beyond leaving it where it renders; T089 moves and rebuilds it

## result
