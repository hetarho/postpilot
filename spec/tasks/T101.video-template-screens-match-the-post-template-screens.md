# T101 The video-template screens match the post-template screens
> st:todo | ssot:CLIP-42 CLIP-25 | base:CLIP@5 TMPL@5 THEME@10 ARCH@2 | dep:-

## goal
`/video-templates` and `/video-templates/$templateId` carry the shapes `/templates` and `/templates/$templateId` already have — the same rows, badge, failure and empty treatments, and the same detail heading, status line and docked save.

## acceptance
- [ ] `/video-templates`: the `display` heading with `/templates`' description treatment (`body`, `text-content-secondary max-w-measure mt-2`), a `Notice tone="danger"` load failure with a `pending` retry, and `/templates`' two-part empty state (a `title` heading plus help copy that says what a video template is for)
- [ ] rows follow `TemplateRow`: one `relative` `li` at `min-h-16 py-2` with `divide-divider divide-y` and full-bleed gutters, the name as a `label` link stretched over the row through `after:absolute after:inset-0`, the information-field count and the copy styles as `meta`, one `Badge tone="neutral"` carrying `project_count`, and the delete above the link layer so it acts without navigating
- [ ] `features/delete-clip-template` (new slice, mirroring `features/delete-template`): the row's button, its `Dialog` warning how many projects the delete will detach from `project_count` before it happens, and the mutation; `ClipTemplateEditor` no longer renders 삭제, its dialog, or the `clipDetachedCount` navigation state, and that field leaves the router's state type
- [ ] one `ActionBar dock="list"` with an `ariaLabel` and the 새 영상 템플릿 CTA; the page's `gap-8` header wrapper goes
- [ ] `/video-templates/$templateId`: `/templates/$templateId`'s shape — the same back-link treatment, the STORED template's own name as the `display` heading (the create copy only when there is no id), the field layout beneath it, the one `role="status"` `meta` line above the dock for saved and the `FieldMessage` for failed, and one docked save
- [ ] a load failure on the detail renders through `AppFailureMessage` with a retry rather than plain copy, and an unknown id keeps its own not-found line
- [ ] every credit, analysis, render and template-recipe policy is untouched: the information fields, cut guidance, copy styles and accent controls keep whatever behaviour and validation they have when this task starts (CLIP-4)
- [ ] ko/en `clips` gains the empty-state help, the delete warning and the dock aria labels; the locale parity test passes
- [ ] vitest: the badge's count, a row whose link navigates and whose delete does not, the delete dialog naming the detach count, the empty state, the detail heading showing the stored name, and the status line sitting above the dock; `VideoTemplatePage.test.tsx` updated
- [ ] `pnpm --filter ./frontend test` · `pnpm lint` · `pnpm lint:fsd` · `pnpm lint:style:probe && pnpm lint:style` · `pnpm build:web` (ARCH-25)

## impl notes
- the detach count moves from an after-the-fact route-state message to the confirm dialog's own warning, which is where `/templates` puts it. CLIP-25's policy is unchanged either way — what changes is that the owner is told before the delete instead of after, and the `clipDetachedCount` hop disappears with it
- the delete belongs on the ROW because that is where the post-template directory has it and because the detail screen's dock then holds exactly one action, which is what `/templates/$templateId` has; it is its own slice for the same reason `delete-template` is (a feature rendered by the page, not from inside another feature)
- the detail heading shows the stored name rather than a fixed 영상 템플릿 편집: the directory-item form's heading IS the record's identity, and a fixed label makes two templates' screens indistinguishable
- `project_count` is already on the `VideoTemplate` message (field 7) and mapped by the FE entity, so the badge needs no BE or proto work
- this task touches no clip project surface; T097 and T100 own those, and the two sets of files do not overlap
- SHARED FILE: `features/edit-clip-template` is also the CLIP r5 / CDS r1 work's target — CLIP-4✎ adds a category preset to the recipe and CLIP-14✎ replaces the three copy styles with CDS's four. That content is NOT this task's: move the screen's chrome (heading, failure, status line, dock, and the delete off to the row) and leave every recipe field's contents and validation exactly as found. If the CDS task has already reshaped those fields, rebase onto it rather than restoring the older field set, and re-read CLIP and CDS before starting
- agreed with the dsgn session on 260911: no CDS code task exists yet and that session is touching no code, so T101 takes `ClipTemplateEditor.tsx` FIRST and the later CDS task declares `dep:T101` and rebases onto it. Both edit that one file and a split there cannot be staged apart, so they never run at the same time

## result
