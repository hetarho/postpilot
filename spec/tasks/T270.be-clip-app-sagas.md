# T270 clip pricing, approval and terminal commits leave cmd/api
> st:todo | ssot:ARCH-6 ARCH-7 ARCH-24 | base:ARCH@5 | dep:-

## goal
Create `backend/internal/clip/app` as the home of the cross-store clip sagas (`FreezeWork`, `ReserveApproved`, `Complete`, `Reserve`, finalization, metered registry completion, credit bootstrap) over transaction-scoped ports, and move their tests with them.

## acceptance
- [ ] `backend/internal/clip/app` exists and owns the code now in `cmd/api/clip_credits.go`, `clip_finisher.go`, `clip_finalization.go`, `clip_transactions.go`, `main.go:1834-1872` (meteredRegistry.Complete) and `main.go:1666-1728` (credit bootstrap); `cmd/api` keeps only construction and the release smoke
- [ ] the sagas run over ports declared in `clip/app/ports.go` — `JobTx`, `UsageTx`, `ClipTx` — each satisfied by `job/store.NewTx`, `usage.Service.WithStore(usage/store.NewTx)`, `clip/store.NewTx` on one `*sql.Tx`; a `clip/app.WriteTx(ctx, db, func(ports) error)` helper opens the single writer transaction and no saga calls `sql.Tx` directly
- [ ] the JSON wire structs for cross-context handoff (`main.go:961-985, 1565-1620`) become typed Go values in `clip/app`, not JSON
- [ ] `cmd/api/clip_cancellation_test.go`, `clip_finalization_test.go`, the saga half of `bootstrap_test.go` move to `clip/app` as unit tests with fake ports; `clip_release_test.go` stays in `cmd/api` as the end-to-end gate and still passes
- [ ] `main.go` loses ≥600 lines and its `main()` no longer contains a clip rule (a reviewer can read `cmd/api/clip*.go` and find only constructor calls)
- [ ] behaviour unchanged: ARCH-26 passes, `cmd/api` tests pass, and the clip release smoke image stage runs once locally (ARCH-37) since pricing and finalization are on the path

## impl notes
- from review/arch-260919 F13 F22
- ARCH-6 r4 names `<context>/app` as the saga home; ARCH-7 still forbids the clip context touching `jobs`/`usage_*` tables directly — the ports are how `job` and `usage` publish transaction-scoped behaviour
- port shape (consumer-declared in clip/app): `JobTx { Snapshot(id) ; Transition(id, from, to) ; MarkTerminal(...) }`, `UsageTx { Reserve/Commit/Release lot ops the finisher uses }`, `ClipTx { the clip store methods the sagas call }` — write only the methods the sagas call, not the store surface (F17)
- the quote formula (retries=3, count×(1+retries), MaxCredits) is a pure function `clip/app.Quote(...)` with its own table test
- T271 (constructor injection + main shape) and T272 (job/usage product-agnostic) follow this task and depend on it; do not refactor `job`/`usage` internals here beyond exposing `NewTx`-backed ports

## result
