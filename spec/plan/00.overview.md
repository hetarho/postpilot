# 00. Overview — plan index, progress board, constitution

The entry point for the spec workflow. Every plan registers here so a second agent can see what exists, what is
claimed, and what is done without reading every doc.

- Product SSOT: [PRD.md](../../PRD.md) — what postpilot is, and why.
- Structural SSOT: [ARCHITECTURE.md](../ARCHITECTURE.md) — where code goes, which way dependencies point.
- Deploy/ops SSOT: [DEPLOY.md](../../DEPLOY.md).

## 1. The workflow

```
new feature      /create-plan  NN → /create-plan-job  NN → /implement-job MM
change to built  /create-change NN → /create-change-job NN → /implement-job MM
audit → refactor /create-code-review → /create-refactor-job NN → /implement-job MM
```

- `spec/plan/NN.*.md` — the **as-built** WHAT. Updated when a job lands; no checkboxes.
- `spec/changes/NN.*.md` — a WHAT **delta** against a shipped plan. Archived when its job lands.
- `spec/jobs/MM.*.md` — the buildable work doc (Acceptance Criteria + Implementation Checklist). Archived when done.
- `spec/code-review/NN.*.md` — a read-only audit; findings only, never code changes.
- `spec/policy/**` — canonical product rules that are **currently true**.
- `spec/tech/**` — a doc per non-obvious technical decision the PRD doesn't already own
  ([draft-autosave](../tech/draft-autosave.md)).

Plan numbers and job numbers are independent sequences, monotonic across `archive/` — never reused, never renumbered.

## 2. Plan index

| Plan                                                 | Title                                                                                                                                                                          | Scope    | Status                                                                                                         |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------- |
| [01](01.accounts-and-login.md)                       | Accounts and Login — operator-created accounts, HttpOnly cookie sessions, `adduser`, persistence foundation ([I7])                                                             | FS       | ✅ done                                                                                                        |
| [02](02.post-drafting-and-list.md)                   | Post Drafting and List — autosaved title/memo, browser image pipeline → R2 ([I6]), photo delete, orphan sweep, post list & resume                                              | FS       | ✅ done · [change 12](../changes/archive/12.consolidate-the-writing-brief-and-dock-t.md) → [job 39](../jobs/archive/39.consolidate-the-writing-brief-and-dock-t.md) archived · [change 13](../changes/archive/13.expose-post-deletion-in-the-editor.md) → [job 40](../jobs/archive/40.expose-post-deletion-in-the-editor.md) archived · [change 15](../changes/archive/15.move-editor-status-to-the-page-top-and-c.md) → [job 42](../jobs/archive/42.move-editor-status-to-the-page-top-and-c.md) archived |
| [03](03.voice-profile-learning.md)                   | Voice Profile Learning — shipped account-singleton baseline, sample/finalized-post learning, editable/versioned profile, injection order contract                              | FS       | ✅ done · [change 16](../changes/archive/16.preview-a-voice-version-before-restoring.md) → [job 43](../jobs/archive/43.preview-a-voice-version-before-restoring.md) archived |
| [04](04.model-provider-registry.md)                  | Model Provider Registry — `internal/llm` port, `config/providers.yaml`, capability-aware per-stage dropdowns, per-account last selection                                       | FS       | ✅ done · [change 24](../changes/24.bound-the-write-completion-budget-and-ma.md) → [job 52](../jobs/52.bound-the-write-completion-budget-and-ma.md) todo |
| [05](05.generation-job-queue.md)                     | Generation Job Queue — `generation_jobs` record, in-process worker, boot sweep, `GetGeneration` + 2 s polling ([I5])                                                           | FS       | ✅ done                                                                                                        |
| [06](06.two-stage-generation-and-contact-sheet.md)   | Two-Stage Generation and Contact Sheet — `Block` canon ([I2]), observe → write ([I3]), validation + attachment filter, contact sheet                                           | FS       | ✅ done · [change 21](../changes/21.reuse-finished-observations-and-pick-whi.md) → [job 49](../jobs/49.reuse-finished-observations-and-pick-whi.md) todo                                                                                                        |
| [07](07.ai-revision.md)                              | AI Revision — natural-language edits as `revise` jobs, minimal change, profile re-injection, "save as rule"                                                                    | FS       | ✅ done                                                                                                        |
| [08](08.platform-export.md)                          | Platform Export — Naver / Tistory / site / Markdown derived in the browser, copy with clipboard fallback ([I1])                                                                | FE       | ✅ done · [change 17](../changes/archive/17.copy-each-naver-photo-from-the-export-pa.md) → [job 44](../jobs/archive/44.copy-each-naver-photo-from-the-export-pa.md) archived · [change 18](../changes/archive/18.render-the-naver-export-preview-as-the-p.md) → [job 45](../jobs/archive/45.render-the-naver-export-preview-as-the-p.md) archived · [change 22](../changes/22.expose-the-generated-tags-as-a-copy-fiel.md) → [job 50](../jobs/50.expose-the-generated-tags-as-a-copy-fiel.md) todo · [change 23](../changes/archive/23.tell-a-blocked-photo-copy-apart-from-an-.md) → [job 51](../jobs/archive/51.tell-a-blocked-photo-copy-apart-from-an-.md) ✅ |
| [09](09.stage-model-experiments-and-leaderboards.md) | Stage Model Experiments and Leaderboards — opt-in recommended pairs, blind observe/analyze/write A/B, winner application, private Elo                                          | FS       | ✅ done                                                                                                        |
| [10](10.independent-voice-profiles-and-post-assi.md) | Independent Voice Profiles and Post Assignment — user → voice → post, required selection, isolated learning, default and soft-delete lifecycle ([I4])                          | FS       | ✅ done · [change 14](../changes/archive/14.voice-directory-rows-and-described-initi.md) → [job 41](../jobs/archive/41.voice-directory-rows-and-described-initi.md) archived |
| [11](11.post-purpose-presets.md)                     | Post Purpose Presets — account-owned 용도 briefs (name · description · instructions), optional per-post selection, frozen into write/revise prompts after the voice profile    | FS       | ✅ done                                                                                                        |
| [12](12.automated-naver-publishing-through-a-pai.md) | Automated Naver Publishing Through a Paired Mac Agent — explicit finalized-post job, immutable JPEG manifest, account-paired deterministic Chromium driver, final publish + URL ([I1]) | FS + Mac | 🟡 deterministic publisher implementation and live verification pending · [change 09](../changes/09.persist-dedicated-browser-sessions-acros.md) planning |
| [13](13.multilingual-interface-and-target-langua.md) | Multilingual Interface and Target-Language Generation — Korean/English UI, browser locale preference, per-post generation language, and language-safe voice learning           | FS       | ✅ done                                                                                                        |
| [14](14.theme-preferences.md)                        | Theme Preferences — system/light/dark browser preference applied before paint across public, login, and authenticated surfaces                                                 | FE       | ✅ done · [change 11](../changes/archive/11.header-icon-menus-profile-popover-tab-ic.md) → [job 38](../jobs/archive/38.header-icon-menus-profile-popover-tab-ic.md) archived |
| [15](15.public-marketing-page.md)                    | Public Marketing Page — localized, theme-aware `/about` explanation page for external visitors with a login-only CTA, presenting the shipped plan ladder without purchase flows| FE       | ⬜ planning                                                                                                     |
| [16](16.writing-guidelines.md)                       | Writing Guidelines — account-owned 지침 (global or purpose-scoped writing rules), `지침으로 저장` revision capture, fixed grounding constraint in the write/revise prompts     | FS       | ⬜ planning                                                                                                    |
| [17](17.plan-based-authorization-and-usage-quota.md) | Plan-Based Authorization and Usage Quotas — `free/basic/pro/max/master` account plans granting a monthly credit balance, held at job start and settled against the ledger, plus master-only publishing/admin | FS       | ✅ done · [change 19](../changes/archive/19.credit-metering-and-open-model-access.md) → [job 47](../jobs/archive/47.credit-metering-and-open-model-access.md) archived |
| [18](18.openrouter-model-catalog-and-admin-model.md) | OpenRouter Model Catalog and Admin Model Management — usable models moved from `providers.yaml` into `catalog_models` DB rows curated on a master-only `/admin` 모델 관리 tab fed by OpenRouter's live catalog | FS       | ✅ done · [change 20](../changes/archive/20.purpose-scoped-model-enablement-with-adm.md) → [job 48](../jobs/archive/48.purpose-scoped-model-enablement-with-adm.md) archived                      |

Plans 01–09 cover PRD F-1…F-9 and the original design decisions in PRD §3, §5, §6. Plan 10 extends the profile/post
model after the explicit [I4] amendment so one account may keep several isolated voices. Plan 11 adds a reusable
_purpose_ brief beside the voice so a post can say what kind of text it is (genre/structure) without touching the
voice profile (register). Plan 12 records the explicitly approved [I1] amendment after real use showed that manually
uploading/placing every Naver photo remained the dominant last-mile work: the VPS owns a durable publish request, but
platform credentials and browser execution stay on an account-paired Mac. PRD §9 items 2 (mobile app),
3 (rules auto-cleanup) and 4 (backup) remain open and are deliberately **not** planned; §9.5 (orphan cadence) is
carried as config in plan 02.

Plan 13 adds the Korean/English product-language foundation without changing routes: browser locale controls app
copy, each post independently controls the language of its next full generation, and each voice keeps one immutable
source language for safe learning. Plan 14 adds browser-owned system/light/dark preference over the existing day and
night tokens. Plan 15 composes those foundations into a public `/about` page while preserving `/` as the authenticated
product home and `/login` as the sole entry CTA; since plan 17 exists, the page also presents the shipped
`free/basic/max` plan ladder (values mirroring plan 17's limits table) without any purchase or signup affordance.

Plan 16 adds the third orthogonal prompt axis beside the voice (how sentences sound, plans 03/10) and the purpose
(what kind of text, plan 11): account-owned **writing guidelines** — reusable rules about what every post must avoid
or watch out for (clichéd filler, invented facts). A guideline applies globally by default or is scoped to specific
purposes, can be captured from the AI-revision flow as an explicit save, and is frozen at enqueue like the purpose
brief; the fixed write/revise prompts also gain a universal grounding constraint (no facts absent from the memo and
photo observations), complementing change 10's stylistic naturalness baseline. Nothing about a guideline is learned —
[I4] stays entirely with voice.

Plan 17 introduces the first authorization tier above "has a session": every account carries a plan
(`free < basic < max < master`) that bounds daily LLM-job starts (10/30/100/∞), daily and monthly spend
(free $0.10/$2 · basic $0.50/$12 · max $1/$25 · master ∞, enforced at job admission against a persisted per-call
usage ledger, calendar windows in Asia/Seoul), and model access (a `min_plan` floor per model in
`config/providers.yaml`). The human publishing surface and the new user-administration RPCs become master-only.
The ladder is deliberately shaped like a future subscription (PRD §9), but no billing is built.

Plan 18 moves the usable-model list out of `config/providers.yaml` and into `catalog_models` DB rows curated on a
new master-only 모델 관리 tab of `/admin`: candidates come live from OpenRouter's public models endpoint (featured
providers pinned first, search + provider/capability filters), an admin enables a model together with its plan-17
`min_plan` floor, and only enabled rows are selectable by users — applied without a restart through a dynamic
`llm.ModelSource` injected into the registry. It records the OpenRouter-단일화 decision (one provider entry; the
yaml keeps connection config and recommendation sets only), keeps enabling strictly separate from selecting ([I3]),
and routes a source-delisted model through the existing vanished machinery, flagged for the admin rather than
auto-disabled.

### Progress board

Status: ⬜ planning · 🟡 in flight (claimed by a job) · ✅ done

| Plan | Job                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status                                                  | Notes                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01   | [01](../jobs/archive/01.persistence-foundation-and-auth-backend.md) · [02](../jobs/archive/02.login-screen-and-session-guard.md)                                                                                                                                                                                                                                                                                                                                                          | ✅ done                                                 | 01 persistence foundation + auth BE · 02 login screen + guard                                                                                                                                                                                                                                                                                                           |
| 02   | [03](../jobs/archive/03.posts-uploads-and-r2-backend.md) · [04](../jobs/archive/04.post-list-and-editor-autosave.md) · [05](../jobs/archive/05.browser-photo-upload-pipeline.md) · [14](../jobs/archive/14.migrate-current-ui-to-the-design-system.md) · [change 05](../changes/archive/05.present-the-post-editor-as-lifecycle-ste.md) → [20](../jobs/archive/20.present-the-post-editor-as-lifecycle-ste.md) · [change 12](../changes/archive/12.consolidate-the-writing-brief-and-dock-t.md) → [39](../jobs/archive/39.consolidate-the-writing-brief-and-dock-t.md) · [change 13](../changes/archive/13.expose-post-deletion-in-the-editor.md) → [40](../jobs/archive/40.expose-post-deletion-in-the-editor.md) · [change 15](../changes/archive/15.move-editor-status-to-the-page-top-and-c.md) → [42](../jobs/archive/42.move-editor-status-to-the-page-top-and-c.md) | ✅ done | 03–05 base plan · job 14 archived [change 01](../changes/archive/01.migrate-current-ui-to-the-design-system.md) after migrating the current UI; also reflected into plans 01 and 04. Job 20 presents the editor as 글 생성 / 글 다듬기 / 글 완성 steps derived from `post.status` and restores the block reading view, read-first on the shared `Editable` primitive. Change 12 consolidates the whole AI brief into one options surface, replaces every native `<select>` with an app-drawn listbox, makes the observation area a phone carousel, gives the 가제 to 글 생성 while 확정 writes the AI title back to `posts.title`, and docks 글 다듬기's revision input and both confirm actions. Job 40 exposed the already-shipped `DeletePost` RPC in the editor's destructive region: a confirmation dialog that names what is destroyed, a per-slug autosave-queue discard so no retry outlives the post, and a `POST_PUBLISHING` refusal for a non-terminal publish job read through a consumer-declared port, while terminal publication history keeps outliving the post ([I1]) Change 15 moves everything the editor has to SAY about state — job progress, save state, post status — to a page-top 2px progress bar plus one status line, leaves the docked bar holding only controls and the reason a control is refused, drops the duplicated pending-A/B refusal, the prose candidate counts and the standing 확정 success notice, adds a `ProgressBar` primitive and a `fieldTitle` type role, and fixes the shared primitives the layout depends on: focus rings clipped by an overlay's scroller, and `Listbox` panels — now portalled, elevated and on a sixth `surface-overlay` step — that were clipped and indistinguishable inside a sheet. |
| 04   | [06](../jobs/archive/06.model-provider-registry.md) · [change 07](../changes/archive/07.bound-reasoning-tokens-and-surface-candi.md) → [23](../jobs/archive/23.bound-reasoning-tokens-and-surface-candi.md) · [change 24](../changes/24.bound-the-write-completion-budget-and-ma.md) → [52](../jobs/52.bound-the-write-completion-budget-and-ma.md) | ✅ done · change → job 52 todo | whole plan, FS; `StageModelSelect` is mounted by jobs 09 · 11. Job 23 bounds reasoning tokens with stage defaults and strict per-model overrides, raises the shared completion cap to 8192, preserves failed-call usage, and makes truncation/provider failures diagnosable. BE only, no dependency Change 24 makes the reasoning/budget seam stage-aware end to end. The operator's `reasoning_effort` moves from `catalog_models` onto `catalog_model_purposes`, because it was model-wide while the stage policy was per stage: on 2026-09-03 lowering the effort on the 글 작성 tab silently changed photo observation, and the value chosen (`minimal`, an OpenAI-dialect value `z-ai/glm-5.3-flash` does not honor) behaved like sending nothing — 647–733 completion tokens became 8,192, the cap, and the observation failed (an A/B observes once, so both candidates showed the one error). The migration clears every override rather than copying it. Alongside that: the completion budget becomes per-stage and derived from the requested target length, the cap moves to config, and provider-reported reasoning tokens reach the ledger and the curation surface — the only reliable check, since `supported_parameters` says a model accepts `reasoning_effort` but never which values it honors. It also retires the stale free-tier explanation of `ErrRateLimited`. Per-user effort selection was considered and rejected: the right effort is a per-model, per-task measurement, not a user preference. |
| 05   | [07](../jobs/archive/07.generation-job-queue.md)                                                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ done                                                 | whole plan, FS; verified with a fake job kind                                                                                                                                                                                                                                                                                                                           |
| 03   | [08](../jobs/archive/08.voice-profile-backend-and-analysis-job.md) · [09](../jobs/archive/09.voice-screen-and-empty-profile-warning.md) · [change 02](../changes/archive/02.progressive-voice-personalization-from-f.md) → [16](../jobs/archive/16.progressive-voice-personalization-from-f.md) · [change 04](../changes/archive/04.split-the-voice-screen-into-tabs-and-fix.md) → [19](../jobs/archive/19.split-the-voice-screen-into-tabs-and-fix.md) · [change 16](../changes/archive/16.preview-a-voice-version-before-restoring.md) → [43](../jobs/archive/43.preview-a-voice-version-before-restoring.md) | ✅ done | Job 16 shipped progressive zero-history learning with no scheduled/background AI. Job 19 split `/voice` into five routed tabs (`말투 · 버전 기록 · 기존 글 가져오기 · 대조 규칙 · 프로필 검증`), made profile fields read-first, and gave the six axes presence so an unanswered axis publishes as unknown instead of 0 Change 16 gives every profile version a copied snapshot of the last post generated under it, so a version can be read before it is adopted and the restore dialog is replaced by the preview itself; renames 기존 글 가져오기's 라벨 to 제목 (the 200-character minimum is deliberately unchanged); removes the 이전 수동 안내 carry-over entirely — both free-text columns, their RPC surface, the `[Legacy manual guidance]` double injection, and the free-text rules position in the prompt — reworking analyze/seed's corpus_version guard and rerouting plan 09's analyze-winner application to publish a structured version; and moves 문장 의견 to 글 완성, the only state the server accepts it in, with a purpose statement and a wrapping sentence list instead of a horizontally scrolling Listbox. |
| 06   | [10](../jobs/archive/10.generation-pipeline-backend.md) · [11](../jobs/archive/11.contact-sheet-and-generate-action.md) · [change 08](../changes/archive/08.stop-dropping-blocks-over-a-schema-manda.md) → [24](../jobs/archive/24.stop-dropping-blocks-over-a-schema-manda.md) · [change 10](../changes/archive/10.korean-naturalness-baseline-beneath-the-.md) → [36](../jobs/archive/36.korean-naturalness-baseline-beneath-the-.md) · [change 21](../changes/21.reuse-finished-observations-and-pick-whi.md) → [49](../jobs/49.reuse-finished-observations-and-pick-whi.md) | ✅ done · job 49 todo | 10 = Block contract + observe→write BE · 11 = contact sheet + generate action. Job 24 keeps schema-compliant non-heading blocks by normalizing their meaningless `level` to zero while preserving real drops and warnings. Job 36 adds the fixed Korean naturalness floor beneath the voice profile, shared by write/revise and inherited by A/B candidates. Change 21 stops the generate/A-B runs from re-observing photos that are already understood: a re-observation picker (desktop dialog / phone sheet) lists every photo with its stored observation and a clear-by-default checkbox, the chosen filename set is frozen in the durable payload, and the handler observes only those while seeding a complete snapshot so the contact sheet never shrinks. The write stage's completion budget is deliberately out of scope. |
| 07   | [12](../jobs/archive/12.ai-revision.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ done                                                 | whole plan, FS; revision prompt, rule save, durable job, and editor action                                                                                                                                                                                                                                                                                              |
| 08   | [13](../jobs/archive/13.platform-export.md) · [change 17](../changes/archive/17.copy-each-naver-photo-from-the-export-pa.md) → [44](../jobs/archive/44.copy-each-naver-photo-from-the-export-pa.md) · [change 18](../changes/archive/18.render-the-naver-export-preview-as-the-p.md) → [45](../jobs/archive/45.render-the-naver-export-preview-as-the-p.md) · [change 22](../changes/22.expose-the-generated-tags-as-a-copy-fiel.md) → [50](../jobs/50.expose-the-generated-tags-as-a-copy-fiel.md) · [change 23](../changes/archive/23.tell-a-blocked-photo-copy-apart-from-an-.md) → [51](../jobs/archive/51.tell-a-blocked-photo-copy-apart-from-an-.md) | ✅ done · job 50 todo · job 51 ✅ | whole plan, FE only; four converters, export panel, and clipboard fallback Change 17 adds per-photo clipboard copy to the Naver export, ordered against the `[사진 …]` markers, writing one image/png-only ClipboardItem — the single payload measured to work in SmartEditor ONE. Change 18 makes the Naver tab's preview the rendered post itself (BlockList, body only): photos inline at their marker positions with the copy control overlaid top-right, markers only in the copied text, and the raw field revealed only for the manual-copy fallback. The derived text, the other three formats and the upload pipeline ([I6]) are untouched; bulk photo download stays the unfilled PRD gap. Change 22 gives the generated tags a copy field in the export panel — read-only, `#tag1 #tag2` on the clipboard, on all four format tabs, absent when there are no tags — because plan 08 omits tags from the Naver and Tistory bodies (those platforms have their own tag box) and then showed them nowhere. All four derived outputs stay byte-identical. Change 23 splits the per-photo copy's single read failure in two: a rejected fetch becomes a new `blocked` kind that points at the text output instead of telling the user to reload, while `unreadable` narrows to the expired view URL a reload actually remints — the bucket-CORS case that made a displayed photo uncopyable was sending users around a loop that could not end. It also makes the R2 GET CORS rule a repo-tracked `deploy/r2-cors.json` applied by a Wrangler command, with a deploy-time preflight gate (no new GitHub variable) so a PUT-only rule fails loudly instead of silently. The clipboard payload, the presign TTL and the refetch policy are untouched; auto-refetch-and-retry is an explicit non-goal. |
| 09   | [15](../jobs/archive/15.stage-model-experiments-and-leaderboards.md) · [change 03](../changes/archive/03.separate-generation-experiments-and-fina.md) → [17](../jobs/archive/17.separate-generation-experiments-and-fina.md) · [change 06](../changes/archive/06.start-write-comparisons-from-the-model-l.md) → [21](../jobs/archive/21.start-write-comparisons-from-the-model-l.md)                                                                                                      | ✅ done                                                 | Job 17 separates ordinary generation from opt-in write A/B, makes length optional, and separates exact post finalization from voice learning. Job 21 adds the model lab as another explicit entry point for the existing post-backed write comparison.                                                                                                                  |
| 10   | [18](../jobs/archive/18.independent-voice-profiles-and-post-assi.md) · [change 14](../changes/archive/14.voice-directory-rows-and-described-initi.md) → [41](../jobs/archive/41.voice-directory-rows-and-described-initi.md) | ✅ done | Job 18 shipped independent voice directories/profiles, required post assignment, tombstones, per-voice learning/jobs, exact prompt projection, and migration 0009. Change 14 turns the voice directory into a list of one-target rows with `기본으로 설정`/`삭제` on the row itself, moves rename to the voice detail screen, replaces the inline creation form with a bottom sheet, and lets that sheet describe a new voice in words so a `seed_voice` job writes its first profile with the account's analyze-stage model (migration 0016 widens the `voice_profile_versions.origin` check with `seed`) |
| 11   | [22](../jobs/archive/22.post-purpose-presets.md)                                                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ done                                                 | Job 22 shipped the account-owned `purpose` context, the optional presence-aware `posts.purpose_id` (migration 0011, detach by trigger), the brief frozen at enqueue into the write/revise payloads and the A/B snapshot, `/purposes`, and the editor selector beside the voice select                                                                                   |
| 12   | [25](../jobs/25.automated-naver-publishing-through-a-pai.md) · [change 09](../changes/09.persist-dedicated-browser-sessions-acros.md)                                                                                                                                                                                                                                                                                                                                                     | 🟡 replacement publisher implementation + verification pending (job 25) · change planning | The server, UI, durable queue and Mac companion foundation remain built. The retired executor is removed and the agent now fails closed without claiming work; T017–T021 cover the replacement deterministic publisher, its offline verification and the authorized real-Naver smoke. Change 09 separates the persistent local connection-draft identity from replaceable device codes so setup retries reopen the same isolated browser session. Migration 0010 is reserved; job 22 took 0011. |
| 13   | [32](../jobs/archive/32.multilingual-interface-and-target-langua.md)                                                                                                                                                                                                                                                                                                                                                                                                                      | ✅ done                                                 | Job 32 shipped Korean/English UI, URL-neutral browser locale, post target/content provenance, immutable voice source, frozen language-aware generation/revision/A-B, portable cross-language projection, safe learning gates, and structured localized failures                                                                                                         |
| 14   | [33](../jobs/archive/33.theme-preferences.md) · [change 11](../changes/archive/11.header-icon-menus-profile-popover-tab-ic.md) → [38](../jobs/archive/38.header-icon-menus-profile-popover-tab-ic.md)                                                                                                                                                                                                                                                                                     | ✅ done                                                 | Job 33 shipped pre-paint System/Light/Dark resolution, browser-local persistence, OS and cross-tab lifecycle, synchronized document chrome, and the translated phone-safe interface-preferences surface on login and authenticated shells. Job 38 dissolved that popover into two icon-triggered app-drawn menus, moved id/plan/logout into an avatar profile popover, made the voice tab row icon-compact under a container query, and componentized the §3 type roles as a mandated `Typography` primitive (also touches plans 01 · 03 · 13 · 17)                       |
| 15   | [34](../jobs/archive/34.public-marketing-page.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ built (job 34)                                        | Public `/about` shipped in job 34: localized ko/en marketing copy, the shared locale/theme controls, Login as its only CTA, and a plans section mirroring plan 17's code-owned limits table. It is outside the auth guard and issues no Connect call at all                                                                                                                                                                                    |
| 16   | [35](../jobs/archive/35.writing-guidelines.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                    | ✅ built (job 35)                                        | The guideline aggregate, `/guidelines`, the 지침으로 저장 revision capture, prompt injection and enqueue-time freezing, and the fixed grounding constraint all shipped in job 35 on migration 0014. Guidelines are prompt material only — nothing about one is learned, and no guideline surface enqueues work                                                                                            |
| 17   | [37](../jobs/archive/37.plan-based-authorization-and-usage-quota.md) · [change 19](../changes/archive/19.credit-metering-and-open-model-access.md) → [47](../jobs/archive/47.credit-metering-and-open-model-access.md) | ✅ done | Job 37 shipped the `free/basic/max/master` ladder (migration 0013 backfills existing accounts to `master`), the transactional admission gate at the single job-enqueue seam, the usage ledger written at the llm boundary, required `min_plan` floors with lock-not-delete selections, the master-only publishing/admin procedure set, `GetMyPlan` + admin RPCs + `/admin`, and the FE plan badge, usage meters and tier gating Change 19 re-denominates the whole ladder in credits: a `credit_lots` balance consumed in expiry order, a `BASE + cost × MULTIPLIER` charge per job, two-phase hold/settle so a balance can never go negative, the three USD axes and the `min_plan` model floors both retired (every account may select every model; affordability is the only gate), a `pro` rung, a 30-photo ceiling, and a plan comparison screen where a checkout will later attach — still no money moves. |
| 18   | [46](../jobs/archive/46.openrouter-model-catalog-and-admin-model.md) · [change 20](../changes/archive/20.purpose-scoped-model-enablement-with-adm.md) → [48](../jobs/archive/48.purpose-scoped-model-enablement-with-adm.md)                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅ done                                                 | Job 46 moved the usable-model list out of `providers.yaml` into `catalog_models` (migration 0018 seeds the twelve models the yaml declared, so no saved selection was cleared), gave `internal/llm` an injected `ModelSource` so curation applies without a restart, added the `internal/modelcatalog` context with its OpenRouter catalog client (public endpoint, TTL cache, availability written only on a successful read), the master-only `ModelCatalogService`, and `/admin` as routed 계정 관리 / 모델 관리 tabs with a virtualized, searchable, filterable catalog. `providers.yaml` keeps the one provider connection and the recommendation sets, whose refs are now validated at apply time. Its `min_plan` column was retired days later by [change 19](../changes/archive/19.credit-metering-and-open-model-access.md) → job 47; [plan 18](18.openrouter-model-catalog-and-admin-model.md) and [policy/providers](../policy/providers.md) carry the as-built truth. Change 20 → job 48 made curation per purpose: `catalog_model_purposes` (migration 0020, deliberately seeding nothing) registers each model to 사진 해석·문체 분석·글 작성·이미지 생성·비디오 생성; the 모델 관리 tab became five capability-force-filtered purpose tabs; `SetModelPurpose` replaced `EnableModel`; every stage picker and every execution boundary (provider·generation·experiment·voice) serves only its purpose's registrations via `ModelInfo.Stages`; capability drift after a refresh stops the stage without retiring the registration; the generation purposes are settings-only until their features ship |
| —    | [review 01](../code-review/01.full-repo-audit-after-twelve-plans.md) → [26](../jobs/archive/26.resolve-spec-links-and-widen-the-hygiene.md) · [27](../jobs/archive/27.throttle-the-publishing-agent-heartbeat-.md) · [28](../jobs/archive/28.fix-stale-sentence-selection-and-finish-.md) · [29](../jobs/archive/29.remove-backend-and-script-residue.md) · [30](../jobs/archive/30.pin-the-js-toolchain-and-correct-repo-ed.md) · [31](../jobs/archive/31.split-the-frontend-bundle-by-route.md) | ✅ 26–31 done                                            | The first full-repo audit, covering all twelve plans. Every gate passed when run; the 13 findings are edges of an otherwise well-kept discipline. Refactor jobs belong to no plan (`plan: none`) — they are listed here so the board stays the one place that shows outstanding work                                                                                    |

### Implementation queue

**The single ordered answer to "implement the next job."** Work top to bottom. A job is claimable only when every
job listed in its frontmatter `requires:` is archived.

This list exists because the progress board alone cannot express the real order. `/implement-job` Step 0 claims
**per plan**, so it cannot see a cross-plan dependency — job 19 waits on job 17, and those sit on plans 03 and 09.
The `requires:` key on each pending job carries the same fact to an agent that opens only the job.

| #   | Job                                                                  | Plan      | Requires          | State                |
| --- | -------------------------------------------------------------------- | --------- | ----------------- | -------------------- |
| 1   | [16](../jobs/archive/16.progressive-voice-personalization-from-f.md) | 03        | —                 | ✅ archived          |
| 2   | [17](../jobs/archive/17.separate-generation-experiments-and-fina.md) | 09        | 16                | ✅ archived          |
| 3   | [19](../jobs/archive/19.split-the-voice-screen-into-tabs-and-fix.md) | 03        | 16 · 17           | ✅ archived          |
| 4   | [20](../jobs/archive/20.present-the-post-editor-as-lifecycle-ste.md) | 02        | 17 · 19           | ✅ archived          |
| 5   | [23](../jobs/archive/23.bound-reasoning-tokens-and-surface-candi.md) | 04        | —                 | ✅ archived          |
| 6   | [24](../jobs/archive/24.stop-dropping-blocks-over-a-schema-manda.md) | 06        | —                 | ✅ archived          |
| 7   | [18](../jobs/archive/18.independent-voice-profiles-and-post-assi.md) | 10        | 16 · 17 · 19 · 20 | ✅ archived          |
| 8   | [25](../jobs/25.automated-naver-publishing-through-a-pai.md)         | 12        | —                 | 🟡 implementation + verification |
| 9   | [22](../jobs/archive/22.post-purpose-presets.md)                     | 11        | 18                | ✅ archived          |
| 10  | [27](../jobs/archive/27.throttle-the-publishing-agent-heartbeat-.md) | review 01 | —                 | ✅ archived          |
| 11  | [28](../jobs/archive/28.fix-stale-sentence-selection-and-finish-.md) | review 01 | —                 | ✅ archived          |
| 12  | [26](../jobs/archive/26.resolve-spec-links-and-widen-the-hygiene.md) | review 01 | —                 | ✅ archived          |
| 13  | [29](../jobs/archive/29.remove-backend-and-script-residue.md)        | review 01 | —                 | ✅ archived          |
| 14  | [30](../jobs/archive/30.pin-the-js-toolchain-and-correct-repo-ed.md) | review 01 | —                 | ✅ archived          |
| 15  | [31](../jobs/archive/31.split-the-frontend-bundle-by-route.md)       | review 01 | —                 | ✅ archived          |
| 16  | [36](../jobs/archive/36.korean-naturalness-baseline-beneath-the-.md) | 06        | —                 | ✅ archived          |
| 17  | [32](../jobs/archive/32.multilingual-interface-and-target-langua.md) | 13        | 31                | ✅ archived          |
| 18  | [33](../jobs/archive/33.theme-preferences.md)                        | 14        | 32                | ✅ archived          |
| 19  | [37](../jobs/archive/37.plan-based-authorization-and-usage-quota.md) | 17        | 32 · 36           | ✅ archived          |
| 20  | [35](../jobs/archive/35.writing-guidelines.md)                       | 16        | 32 · 36           | ✅ archived          |
| 21  | [34](../jobs/archive/34.public-marketing-page.md)                    | 15        | 32 · 33 · 37      | ✅ archived          |
| 22  | [38](../jobs/archive/38.header-icon-menus-profile-popover-tab-ic.md) | 14        | —                 | ✅ archived          |
| 23  | [39](../jobs/archive/39.consolidate-the-writing-brief-and-dock-t.md) | 02        | 38                | ✅ archived          |

Why this order:

- **17 before 19 and 20.** Job 17 rewrites the editor's actions and establishes the `draft`/`review`/`finalized`
  machine. Job 20's steps are derived from that machine, and job 19 reads the finalization/learning flow it changes.
- **19 before 20.** Job 19 creates the `Editable` primitive in `shared/ui`; job 20 consumes it. Reversing them makes
  job 20 hand-roll the shape and job 19 the duplicate — the design-system bug §1.1 names.
- **18 last.** It partitions the voice profile, the finalization aggregate, and these same screens by voice. Running
  it before 19/20 would force both to rebase onto a changed structure; running it after lets it partition one
  settled arrangement.
- **24 may run alongside 18.** It is a backend production fix with no product or contract dependency on multi-voice
  behavior. Job 24 shares the generation package with job 18, but that is an integration concern rather than a
  prerequisite; reconcile the combined tests and doc reflection before merging. Job 23 is already archived.
- **25 may also run alongside 18 and 22.** Publishing consumes the stable finalized-post behavior and does not depend
  on multi-voice or purpose semantics. Concurrent proto/post/editor/config edits are merged deliberately, and every
  in-flight migration reserves a unique number or is renumbered before integration.
- **22 alone waits for 18.** Its purpose selector is explicitly composed beside job 18's required voice selector,
  extends the post contract/migration that job 18 creates, and therefore retains `requires: 18`.
- **26–31 (review 01) have no dependencies on each other or on any plan job**, so every one carries
  `requires: []` and the numbering above is a _recommendation_, not a constraint. They are ordered by what the
  finding costs while it stands: **27** first because it is the only one with a running production cost (a paired
  Mac takes the single SQLite writer every five seconds for a display hint), then **28** because it is the only one
  a user can hit (feedback on an edited block is refused by the server with a message that explains nothing), then
  **26** because until the spec links resolve every later job's own doc links are unreliable, then **29 · 30 · 31**
  as cleanup. Two overlaps to reconcile if they land together, neither of them a dependency: 26 and 29 both edit
  `scripts/check-spec-hygiene.mjs` (different parts), and 26 and 30 both edit `.github/workflows/ci.yml` (26 adds
  the probe steps, 30 changes how the Node version is resolved).
- **36 before 32.** Job 36 (change 10's Korean naturalness baseline) is a small, dependency-free edit to the
  write/revise prompt constants and the two golden files. Job 32 rewrites the same builders and goldens for
  target-language generation and must omit the Korean baseline for non-Korean targets, so it rebases on job 36's
  settled prefix instead of the reverse; plan 16's future job sequences after both (its own integration note).
- **32 follows 31, then 33, then 34.** Job 31 settles the lazy route boundaries that job 32 must localize across the
  entire application. Job 33 consumes job 32's translated preference control, and job 34 composes both the locale
  and theme runtimes in the public `/about` header. These are implementation prerequisites, so the same order is
  encoded in each job's `requires:` frontmatter. Job 34 additionally waits for job 37: its `/about` plans section
  markets the shipped `free/basic/max` ladder, so it carries `requires: 32 · 33 · 37`.
- **35 follows 36 and 32.** Job 35 (plan 16) adds the `[작문 지침]` section, the fixed grounding line, and a payload
  member to the same write/revise prompt builders, payload struct, and golden files that jobs 36 and 32 rewrite, so
  it carries `requires: 32 · 36` and lands last of the three; its grounding line composes beside job 36's baseline
  section without restating it.
- **37 follows 36 and 32, independent of 33 and 35 — and 34 now waits for it.** Job 37 (plan 17) instruments the
  job-enqueue seam and the services those two jobs edit (generation, experiment, the error-detail contract job 32
  establishes), but it never touches the prompt builders or golden files, so it may run alongside 33 and 35 once
  32 lands; reconcile only the shared `providers.yaml`/proto regeneration if it overlaps another in-flight job.
  Job 34 markets the plan ladder job 37 ships, so 37 must land before 34.
- **38 before 39.** Job 38 establishes the shared `Typography` and `Menu` primitives and migrates
  editor-adjacent call sites. Job 39 consumes those landed contracts while reshaping the same editor
  surfaces, so its `requires: 38` prevents a conflicting implementation order.
- **42 before 43; 44 is independent of both.** The three jobs come from one owner UI review split across plans 02,
  03 and 08. Job 42 delivers the portalled, elevated `Listbox` and the un-clipped focus ring in `shared/ui`, and
  job 43's 문장 의견 sheet consumes that panel — so 43 carries `requires: 42` while nothing else in it depends on
  42. Job 44 touches only `widgets/export-panel`, `features/export-naver` and `shared/lib`, so it carries
  `requires: []` and may run alongside either. Two overlaps to reconcile if they land together, neither of them a
  dependency: 42 and 43 both edit `pages/editor/ui/DraftEditor.tsx` and `EditorPage.test.tsx` (42 the status region
  and the dock, 43 the 문장 의견 mount on 글 완성), and all three add strings to
  `app/providers/i18n/resources/{ko,en}/posts.ts`. Job 43 also carries the only migration of the three and must
  reserve a number against in-flight job 25.

New-build jobs 01–13 were numbered in their original **build order**. Later change/refactor jobs keep monotonic ids.
Job 14 was inserted after job 06 and completed the design-system migration before the remaining FE jobs 09/11–13;
backend-only jobs 07/08/10 can proceed independently. Migration numbers follow the
original plan order (`0001` users/sessions ✅ · `0002` posts/images/uploads ✅ · `0003` model_selections ✅ · `0004`
generation_jobs · `0005` voice · `0006` model experiments + selection slots); the illustrative numbers inside plans
03 and 05 are corrected when their jobs land.

### Dependency graph

```
01 accounts-and-login ──┬──▶ 02 post-drafting-and-list ──┬──▶ 05 generation-job-queue ──┐
                        │                                 │                              │
                        └──▶ 04 model-provider-registry ──┼──────────────────────────────┤
                                                          │                              ▼
                                                          │        03 voice-profile-learning  (needs 01 · 04 · 05)
                                                          │                              │
                                                          └──────────────────────────────┴──▶ 06 two-stage-generation-and-contact-sheet
                                                                                                    │
                                                                            ┌───────────────────────┼───────────────────────┐
                                                                            ▼                       ▼                       ▼
                                                               09 stage-model experiments   07 ai-revision          08 platform-export
                                                               (also 03 · 04 · 05)          (also 03 · 05)

01 · 02 · 03 · 04 · 05 · 06 · 07 · 09 · job 17 ─────────────────────────────────────────────▶ 10 independent voices
02 · 05 · 06 · 07 · 09 · 10 (job 18) ───────────────────────────────────────────────────▶ 11 post purpose presets
02 · 06 · 08 ────────────────────────────────────────────────────────────────────────▶ 12 paired Mac Naver publishing

01 · 02 · 03 · 04 · 05 · 06 · 07 · 08 · 09 · 10 · 11 ───────────────────────────────▶ 13 multilingual UI + content language
13 ─────────────────────────────────────────────────────────────────────────────────▶ 14 theme preferences
13 · 14 · 17 ─────────────────────────────────────────────────────────────────────────▶ 15 public marketing page
02 · 05 · 06 · 07 · 09 · 11 ─────────────────────────────────────────────────────────▶ 16 writing guidelines
01 · 03 · 04 · 05 · 06 · 07 · 09 · 10 · 12 ──────────────────────────────────────────▶ 17 plans + usage quotas
01 · 04 · 17 ─────────────────────────────────────────────────────────────────────────▶ 18 openrouter model catalog
```

| Plan | Depends on                                                                                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 02   | 01                                                                                                                                        |
| 03   | 01 · 04 · 05                                                                                                                              |
| 04   | 01                                                                                                                                        |
| 05   | 01 · 02                                                                                                                                   |
| 06   | 02 · 03 · 04 · 05                                                                                                                         |
| 07   | 03 · 05 · 06                                                                                                                              |
| 08   | 06                                                                                                                                        |
| 09   | 03 · 04 · 05 · 06                                                                                                                         |
| 10   | 01 · 02 · 03 · 04 · 05 · 06 · 07 · 09 · job 17                                                                                            |
| 11   | 02 · 05 · 06 · 07 · 09 · 10 (job 18)                                                                                                      |
| 12   | 02 · 06 · 08                                                                                                                              |
| 13   | 01 · 02 · 03 · 04 · 05 · 06 · 07 · 08 · 09 · 10 · 11; localizes the built plan 12 surface without waiting for its live Naver verification |
| 14   | 13                                                                                                                                        |
| 15   | 13 · 14 · 17 (the `/about` plans section mirrors plan 17's shipped limits table)                                                          |
| 16   | 02 · 05 · 06 · 07 · 09 · 11; its job 35 shares the write/revise prompt seam with jobs 36 and 32 — sequence after both                     |
| 17   | 01 · 03 · 04 · 05 · 06 · 07 · 09 · 10 · 12; gates every LLM-job admission path and the auth interceptor — sequence its job after 36 · 32  |
| 18   | 01 · 04 · 17 (amends 04's registry source and consumes 17's plan ladder, `/admin`, and `min_plan` floor); apply-time validation touches plan 09's `ApplyRecommendationSet`; its migration reserves a number against in-flight job 25 |

Suggested build order (one linearization of the graph, matching PRD §10):
01 → 02 → 04 → 05 → 03 → 06 → 09 → 07 → 08, then plan 10 after jobs 16 and 17. Plan 11 follows plan 10 (job 18).
Plan 12 consumes 02 + 06 + 08 and adds the separate local-agent runtime; sequence its implementation after job 18
while that job is pending because both touch the post contract/editor, not because publishing depends on voice. 02
and 04 are independent of each other and may run in parallel. For the new public experience, implement plan 13, then
14, then 15 so the marketing page composes the established locale and theme controls rather than defining parallel
preference systems. Plan 16 may be implemented any time after plan 11; schedule its job after job 32 and change 10's
job land, because all three rewrite the same write/revise prompt builders and golden files. Plan 17 touches the
auth interceptor, the registry schema, and every LLM-job admission path rather than the prompt seam; schedule its
job after jobs 36 and 32 so the generation/experiment services it instruments are settled, independent of plans
14–16.

## 3. The constitution

The invariants below are derived from the PRD's core design decisions (§3) and confirmed stack decisions (§6). They
are the rules a plan, change, or job may **not** break silently. If a request requires breaking one, stop and resolve
it with the user first — reversing one of these reverses a stack decision with it.

- **[I1] Publishing is explicit and local-capability-bound.** Copy-ready output remains the universal fallback.
  Automatic publication may start only from a separate user confirmation over an exact finalized snapshot, and it is
  a durable server-side job. The VPS/frontend never hold platform credentials or a browser profile: an account-paired
  local agent claims the job outbound and drives its own browser. No finalization, learning, schedule, reconnect or
  retry may imply a publish, and an ambiguous external commit is never auto-retried. (PRD §3.1, plan 12.)
- **[I2] The canonical generated post is a block array, not HTML.** Per-platform output (Naver plain text + photo
  markers, Tistory HTML fragment, …) is _derived_ from it. Making HTML canonical makes Naver unsupportable. (PRD §3.2)
- **[I3] Generation separates observe from write and keeps every model choice explicit.** The observation stage looks
  only at photos and produces structured output; writing consumes observations + notes + the style profile. Ordinary
  generation observes at most once and calls one active writer; only the explicit A/B action may fan out to two
  writing candidates over the same snapshot. The app may ship a versioned recommended pairing, but never applies it
  automatically; the user explicitly applies/changes every active model and A/B pair. (PRD §3.3)
- **[I4] One or more mutually isolated style profiles per account, exactly one per voice.** Every post selects one
  voice. Profiles, samples, authored sources, rules, evidence, comparisons, validations, and prompt excerpts are never
  shared or inherited across accounts **or voices**—a merged profile cannot be separated later. Korean style analysis
  treats the sentence-ending form (종결어미) as the primary signal. (PRD §3.4, plan 10)
- **[I5] Long work is a server-side job record, not a blocking RPC.** `StartGeneration` returns a `job_id`; the client
  polls `GetGeneration`; a worker goroutine in the API process consumes the queue; `running` jobs are swept to
  `failed` on restart. Edits ride the same queue. (PRD §3.5)
- **[I6] Image work happens in the browser, before upload.** HEIC decode + 1024px downscale + JPEG encode client-side;
  originals never reach the API. This is what keeps the Go image CGO-free and distroless. (PRD §6.2)
- **[I7] Schema migrations are embedded in the binary and run at boot.** A migration failure kills the process so the
  deploy's `/health` gate rolls back. No separate migration container, no manual step. (PRD §6.3, DEPLOY.md §2)

The provider abstraction is a boundary, not a preference: nothing above `internal/llm` learns which provider answered
(PRD §6.4, ARCHITECTURE.md §2.1).
