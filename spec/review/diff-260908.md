# REVIEW diff-260908
> st:converted@260908 | scope:21649b8~1..HEAD (billing, auth self-signup, usage anchors, agent/naver, FE billing+auth) | at:9d02837 | base:ARCH@2

## summary
- 42 commits over two days landed the whole `billing` context, self-signup, and the Naver mutation driver; ARCH-26 passes clean, so every finding below ships green
- biggest risk is the credit seam between `billing` and `usage`: three money paths (F1 F2 F3) can take a card and leave the account without what it paid for, and F13 explains why the tests never saw them
- F1 fires on the most common onboarding path there is — sign up and subscribe the same day

## findings
- F1 [o] P1 `billing/subscription.go:69` Subscribe: bug: the subscription lot's id is keyed on the subscribe INSTANT while the free lot's is keyed on its window START, so the two collide whenever the account subscribes on its own anchor day of month, and `OpenMonthlyLot`'s `InsertLotIfAbsent` then discards the tier grant and its `inserted` bool with it ← same-day signup→subscribe collides every time (proved: both ids are `monthly:u:2026-09-08`), so the account is charged, `AssignTier` succeeds, and the balance stays at the free 50 for the whole first window with no error anywhere →T047
- F2 [o] P1 `billing/change.go:100` ChangeSubscription: bug: an upgrade charges the card outside the tx (correct per ARCH-10) and then rolls the whole upgrade back when `credits.RaiseMonthlyLot` answers `ErrLotNotFound` inside it ← between a monthly lot's expiry and the next `RunDue` tick (`BillingTickInterval` = 10m) or the next metered request no lot is current, so the money leaves, `UpsertSubscription`, the `charge` event and `AssignTier` are all discarded, and nothing compensates; `usage.TopUpMonthlyLot` deliberately makes the identical condition a no-op →T047
- F3 [o] P1 `billing/purchase.go:113` RefundPurchase: bug: void lot → provider refund → mark refunded are three non-atomic steps with no marker between them ← a failure at the third leaves the money refunded, the credits gone, `refunded_at IS NULL`, and no `refund` row (BILL-15); the retry re-enters `VoidUntouchedLot`, now sees `remaining != granted`, and answers `PURCHASE_SPENT` for a purchase that was already refunded →T048
- F4 [o] P2 `billing/service.go:66` GetMyBilling: one `LotUntouched` per in-window purchase, and `usage/store.LotUntouched` deliberately reads on the writer ← the writer is one connection (ARCH-10), so a read-only billing screen serializes N round trips ahead of every concurrent generation hold and every billing tx; one `WHERE id IN (...)` on the read pool answers the same question once →T050
- F5 [o] P2 `cmd/api/main.go:234`: the boot renewal pass runs synchronously in the composition root, before `rpcserver.New` and `ListenAndServe` ← `RunDue` walks every due row doing Eximbank and Toss calls on `http.DefaultClient` timeouts, so a backlog after an outage holds `/health` shut and the health-gated rollout (ARCH-32) rolls the release back; the sweeper 290 lines below documents the opposite choice for exactly this reason and the 10m ticker already covers the work →T051
- F6 [o] P2 `cmd/api/main.go:521` /webhooks/toss: a public route mounted through `Options.Routes`, which bypasses every Connect interceptor by design, with no throttle of its own ← any anonymous POST forces one outbound `PaymentByOrder` against Toss, and a real order id appends to `provider_notifications`, which carries no dedupe key and no retention (`0030_billing.sql:63`); the handler is right not to trust the payload, but nothing bounds how often a stranger can spend the product's provider quota →T052
- F7 [o] P2 `auth/service.go:409` reserveResend: one 60s reservation map keyed by email alone, shared by email verification and password reset ← a signup followed inside `ResendFloor` by "forgot password" returns `nil` with no link stored and no mail sent while the handler still reports success, so the user waits for a mail that will never arrive; the collision runs both ways →T053
- F8 [o] P2 `frontend/src/shared/api/app-failure.ts`: `BILLING_SELECTION_INVALID` (`billing/rpc/handler.go:154`), `COMBO_INCOMPLETE` and `COMBO_UNKNOWN` (`auth/rpc/admin.go:113,119`) are in neither `appFailureSpecs` nor the two locale catalogs ← the handler's chosen message degrades to the catch-all unknown copy on a money path and on the operator's combo screen, and `resources.test.ts` cannot catch it because it only checks the catalog against itself, so every future reason can ship the same way →T054
- F9 [?] P3 `agent/internal/naver/mutate.go:173`: `(radios[0].closest('li,div,span') || {}).querySelector(...)` in the `category` and `visibility` branches ← `({}).querySelector` is `undefined`, so the fallback the author wrote for a missing ancestor can only raise a TypeError, which `CallFunction` reports as a generic driver failure instead of the intended fail-closed `{matches, activated:false}`; `?.` on the `closest` result is the whole fix
- F10 [?] P3 `agent/internal/browser/page.go:173` CallFunction: resolves `document` to a RemoteObject on every call and never releases it — the package contains no `Runtime.releaseObject` and no `objectGroup` ← every driver point, settings read and activation retains one more handle in the target's remote-object table, and T018's bound page lives across jobs, so a long session accumulates them until it navigates
- F11 [?] P3 `frontend/src/shared/lib/script/loadScript.ts:9`: when a matching `<script>` is already in the DOM the promise waits on a `load` event that has already fired and will not re-dispatch ← nothing reaches it today (the only caller is the Toss SDK and the error path removes the tag it created), so this is a fallback that cannot work rather than a live hang: whoever next adds a script tag outside `loadScript` gets a silent forever-pending await
- F12 [?] P3 `billing/change.go:117,153`: `CancelScheduledChange` and `ResumeSubscription` write the subscription row with no counter-event, while `CancelSubscription` and the scheduled-change path both record one ← the account's own history keeps `cancel_scheduled` and `change_scheduled` with nothing undoing them, so a user reading `/billing` concludes a subscription is still ending after they resumed it (whether the ledger owes a row here is the open half — see notes)
- F13 [o] P2 `billing/subscription_test.go:318` `billing/service_test.go:174`: every billing test fakes the `Credits` port with stubs that return `nil`, so no test drives billing against a real `usage.Service` ← the entire credit seam between the two contexts is unverified, which is exactly how F1 and F2 shipped with ARCH-26 green, and the next credit-side contract change has nothing to break →T047

## notes
- resolved into BILL@3 as BILL-18, so it is not a finding here: `remainingWholeMonths` returns 0 inside an annual term's last window (verified: day 360 of a 2026-09-08 term), so `ChangeSubscription` skips the whole charge block and still runs `AssignTier` + `RaiseMonthlyLot` — a free tier upgrade with a free credit rise. BILL-5 says the charge IS "the remaining whole months × the monthly-equivalent difference" and BILL-17 rules out day-level proration, so the code was literally conformant to BILL@2; BILL-18 now counts the running month as whole, which makes the last window cost one month
- resolved into BILL@3 as BILL-18, same pass: BILL-5 said "monthly-equivalent difference" while the code multiplies `plan.MonthlyPriceCents`; BILL-4 prices an annual term at ten months for twelve, so an annual monthly-equivalent is 10/12 of the monthly list price and the code charges the full one (11 months' difference on a fresh term against a 10-month annual difference); BILL-18 now pins the unit at a twelfth of the annual price
- verified and dismissed, so the next review does not re-raise it: `Login` runs the argon2id verification before the `LockedUntil` check (`auth/service.go:482,490`). The ordering is deliberate — the function's own contract is equal work and an identical answer for every failure, and returning early on a lock would leak that the address exists. The wasted derivation is bounded by `ThrottleLogin` (10 per IP per 5m)
- verified and dismissed: `Subscribe` re-anchoring to `now` can leave a free lot and a subscription lot both unexpired, but the free lot always expires no later than the subscription lot, so `ActiveMonthlyLot`'s `ORDER BY expires_at DESC` still picks the right one and the only effect is that an unspent free remainder stays spendable. F1 is the harmful half of the same re-anchoring
- out of scope, owned by T046: `agent/cmd/postpilot-agent/main.go`'s `runAgents(paths, nil)` refuses to start, so the LaunchAgent `install` now writes crash-loops. That is T046's entire goal and its `dep:T045` chain is the reason it has not landed
- out of scope, owned by T042 T043 T045: the body, image and commit-fence branches of `agent/internal/naver/mutate.go`. F9 sits in the settings branches T044 already closed and F10 in the shared page surface, so both can land without touching that work
- ARCH-26 passes on `9d02837` (gofmt clean, vet, build, test) and the FE test half of ARCH-25 passes (154 files, 1169 tests); the lint, FSD, style and build gates of ARCH-25 and all of ARCH-27 were not run in this pass
