# REVIEW publishing-260922
> st:converted@260922 | scope:agent backend/internal/publishing frontend/src/pages/publishing-agents spec/ssot/PUB.md | at:08698028 | base:ARCH@6

## summary
- Decision adopted 260922 through PUB r6 and ARCH r7: retire automatic Naver publishing from the product and preserve the existing manual export flow; implementation is planned in T310 → T311 → T282 → T312 → T283 → T284 → T313, all todo.
- A successful browser run would prove one technical execution, not permission or account safety; official Naver policies expressly restrict unapproved automation and allow account/service sanctions.
- No successful complete live publication is recorded in the reviewed task evidence; T008 is still doing with every acceptance unchecked. Existing safety mechanisms are useful, but the remaining platform dependency makes further refactoring a poor investment before the product decision.

## findings
- F1 [x] P3 `automatic publishing retirement`: no independent refactoring finding follows because this is a product-scope decision; T008's original verification goal is superseded, the todo T282/T283/T284 scopes now implement retirement, and T279 retains only the independent enum work on surviving code

## notes
- Evidence boundary: inspected the working tree at 08698028, tracked history, PUB@5, EXPORT@4, MKT@5 and task results; the owner's report of no successful publication is consistent with the missing acceptance evidence, but this review did not inspect production job records or local Mac logs and does not independently establish a lifetime success count or diagnose the latest failed attempt.
- Current implementation: `agent/internal/naver/publisher.go:318-367` loads the immutable manifest, prepares the editor, records the commit fence, activates Naver's final control once and requires matching readback before returning `published`; this is automated publication even though the owner initiates it.
- Existing safeguards: the local profile/Keychain design keeps Naver login state off the server, identity and page checks bind actions to the intended blog, CAPTCHA/2FA stop execution, and the commit fence avoids automatic repetition after an ambiguous result; these reduce credential/accidental-publication risks, not Naver's authority to restrict automation.
- Policy application (inference): owner-operated Mac, personal IP, a real Chromium window, explicit Postpilot confirmation, deterministic code rather than an LLM and low publication volume are not permission exceptions identified in the reviewed official policies; no Naver authorization was established by the reviewed project evidence.
- Technical uncertainty: recent failures need not have been bot blocking. T018 records three previously impossible selectors; T042/T043 record caret, conversion, image placement and trailing-word issues; T046 records a stale compatibility signature that deliberately refused jobs. These are implementation/compatibility failures and are not evidence of an account sanction.
- Live proof: `spec/tasks/T008.end-to-end-and-live-naver-verification.md:8-16` still requires fake-Naver/MinIO end-to-end coverage, two-account isolation and a complete real post with every block type and eight photos; its result section is empty, and `agent/README.md:65-74` explicitly keeps a release unverified until the real smoke passes.
- Partial progress is real: T018, T042 and T043 document signed-in editor observations and individual mutation experiments; they do not establish a complete publish-and-readback success. T046 completed daemon wiring, while its verification describes a fake job that safely rejects an empty manifest and explicitly leaves the live install to T008.
- Test boundary: `agent/internal/naver/port_test.go:359-391` returns canned values for CDP JavaScript calls and `apply_test.go:29-45` asserts action sequences; passing these tests cannot establish that the shipped JavaScript works against today's SmartEditor DOM. T283 already owns executable script/DOM-fixture coverage.
- Compatibility cost: `agent/internal/naver/compatibility.json:3-7` pins driver 1.0.0, signature `smarteditor-one-20260910-a6` and Chromium majors 136..152; the same file and `mutate.go` depend on Naver's editor structure and CSS prefixes. Changes require diagnosis, driver updates and reprobes; a browser version outside the declared range is intentionally refused.
- Scope measurement: tracked non-generated Go/TS/TSX in `agent/`, `backend/internal/publishing/` (excluding sqlc) and the nine dedicated frontend publishing slices totals 10,186 source lines plus 10,713 test lines across 93 files. Counts include comments/blanks and the live survey harness, and exclude protobuf, migrations, SQL, packaging and cross-context wiring; size measures maintenance surface, not intrinsic code quality.
- Concrete coupling: `backend/cmd/api/contexts.go:206-219` creates publishing and runs lease recovery during API boot; `serve.go:72` starts its sweeper; post deletion consumes the live-publication port; auth maintains separate agent bearer and master-only user paths. Removing only a navigation tab leaves this runtime and maintenance cost intact.
- Exposure: `frontend/src/app/routes/publishing.ts:7-13`, `frontend/src/pages/editor/ui/EditorFinishPanel.tsx:112-132` and `backend/internal/auth/rpc/interceptor.go:80-88` restrict the human publishing surface to master/operator accounts; this narrows current rollout impact but provides no exemption from Naver policy.
- Preserved product value: EXPORT already provides title/body/tags copy plus ordered individual photo and caption copy from the canonical post; `EXPORT-13` records manual SmartEditor photo-paste measurement. This review does not claim a fresh live manual-paste test or that manual publishing exempts content from spam/content policies.
- Option A, continue full automation: preserves remote one-action publication, but requires finishing T008, addressing script coverage and browser/editor support, Mac setup/recovery support and ongoing policy exposure; not recommended without explicit Naver permission and demonstrated product demand.
- Option B, automatically fill but let the user click Naver's final button: reduces final-submit accident exposure, but retains most editor automation and maintenance cost; the reviewed policies do not establish a safe harbor for automated insertion, so this is not a sufficient policy remedy.
- Option C, retire automation and retain manual export: gives up unattended phone-to-Mac publication while preserving writing, revision, per-platform output and assets; recommended because it removes the unsupported integration from the product's core support obligation.
- Planning completed 260922 through update-ssot/create-architecture/create-task: PUB, ARCH, AUTH, QUOTA, POST, EXPORT, MKT, VIDEO, TMPL and LANG now describe retirement; T282/T283/T284 replace their obsolete refactoring scopes, and T279 follows removal with only surviving enum mappings.
- Retirement implementation outline, not executed: stop accepting new pairing/start/retry/claims server-side as well as removing UI promises; settle active leases conservatively without reclassifying any post-fence uncertainty as a retryable failure; retire Mac execution/credentials with an explicit migration path; clean staged assets and deletion guards; remove publishing RPC/wiring/config/CI/package code; preserve historical migrations and any required audit metadata rather than deleting migration files or browser profiles blindly.
- Reconsideration gate: obtain Naver's explicit permission covering the actual automated actions or a supported blog-writing integration; finish a full authorized fixture plus failure/recovery acceptance; then evaluate reliability and support cost on an agreed bounded pilot. Passing any number of posts alone does not prove account safety.
- Official web sources checked 2026-09-22: [Naver terms](https://policy.naver.com/rules/service.html), effective 2025-07-10, prohibit unapproved automated posting and permit temporary/permanent partial or whole-service restrictions for violations; the restriction is not worded solely as a bulk-posting prohibition.
- [Naver post policy](https://policy.naver.com/rules/service_group.html), effective 2026-07-07, includes posts made with unapproved automated means among restricted posts and permits hiding, deleting or refusing posts and service restrictions.
- [Naver account policy](https://policy.naver.com/rules/service_group2.html), effective 2023-09-15, distinguishes protective login restrictions for abnormal activity from sanctions for policy violations; a CAPTCHA or recoverable protection challenge is not proof of a permanent ban.
- [Official blog operating-policy PDF](https://notice.naver.com/api/v1/file/download/122), supplementary source with no verified current effective date in its retrieved text, addresses repeated mechanical posting with unapproved automation and describes post/category/blog visibility restrictions and temporary/continuing writing restrictions; current general terms independently support the automation-risk conclusion.
- [Writing-restriction help](https://help.naver.com/service/19212/contents/8012?lang=ko&osType=COMMONOS) describes 30-day and permanent writing restrictions after problematic posting; this establishes available sanction types, not a prediction that a single Postpilot run receives one of them.
- [Service-access notice](https://policy.naver.com/rules/disclaimer.html) permits specified automated access only under explicit permission, a compliant official API or allowed exploration under published guidance; none establishes an exception for this editor-writing driver. This review makes no criminal-liability determination.
- [Blog writing API closure](https://developers.naver.com/notice/article/7527) states closure on 2020-05-06 following abusive bulk publication; the [current public API catalog](https://developers.naver.com/products/intro/plan/plan.md) lists blog search/sharing and cafe writing, but no public blog-writing API. Cafe writing or URL sharing is not the current rich-post publication requirement.
- Risk conclusion: policy exposure is evidenced and sanctions may be severe; detection probability, a safe daily count and a Postpilot-specific ban rate cannot be quantified from these sources. Neither guaranteed immunity nor inevitable/immediate permanent suspension is supported.
- Validation environment: Linux, Go 1.26.4, Node 24.18.0; ARCH-27 agent formatting/vet/build/tests and shell syntax passed, but macOS Keychain/LaunchAgent execution was not exercised. Backend formatting/vet/build and all three publishing package suites passed; the full ARCH-26 test run failed because `internal/clip/store` exceeded its aggregate 10-minute package timeout (the final recovery test had run for 2 seconds), outside this publishing review. No claim of a completely green backend gate or a diagnosed timeout cause.
- Frontend validation: initial full run had 2,299 passing tests and 3 failures across `PlansPage.test.tsx` and `ClipCompositionInputs.test.tsx`; a bounded rerun of those files plus publishing entities/form/management/editor tests passed all 131 tests in 8 files with `--maxWorkers=2`. All initial failure cases passed on rerun; no assertion or application code was changed to obtain that result. This does not claim a second green run of the whole suite.
- Frontend gates: lint/format, FSD, style probe/style checks and production build passed; build reported ineffective dynamic-import warnings on several route modules, outside this review scope.
- Scope of actions: no Naver publication, Mac installation, production-data mutation or message to Naver was performed.
- Existing uncommitted `frontend/src/app/routes/NavLinks.tsx` was present before the review; concurrent frontend navigation/theme/menu and related test edits appeared during validation. All remain untouched by this review and subsequent planning; validation describes the shared working tree, not an immutable commit. No retirement code or operational cleanup has been executed.
- Review lifecycle: converted because there are no new independent refactoring findings; subsequent product adoption is recorded in PUB r6 and the seven retirement tasks, with their dependency graph in STATE. The original review evidence and test results describe the pre-retirement implementation.
