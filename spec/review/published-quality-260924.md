# REVIEW published-quality-260924
> st:ready@260924 | scope:T322..T353 (fe065cdc..f42a3c95) | at:f42a3c95 | base:ARCH@9

## summary
- gates are green and the new quality context is well bounded (bands defined once and sent on the wire, every enum mirror pinned, the phrase batch restart- and failure-safe), but one shape recurs across BE and FE: a single fact copied by hand across N hops, sites or languages with nothing failing when one copy is missed — it already shipped three bugs (T352's memories, T353's length, F1)
- biggest risk: F1 (every durable generate on a native-effort model has run without reasoning headroom since 260905 — the known truncation cost class) and F4 (목표 글자 수 lost or split between screen and row by the options' mixed presence rules)

## findings
- F1 [o] P1 `backend/cmd/api/adapters_generation.go:350-355` generationJobs.EnqueueGeneration: bug: the `GenerationOptions` literal never sets `WriteNativeEffort`, so every durable generate on a native-effort write model decodes false (`jobs.go:138` → `generate_handler.go:39` → `write.go:33` `budget.Write(len, false)`) while `generationPricingCalls` (:415) priced the hold with headroom; absent from this literal since f993b4b9 (260905), T344 saw it and scoped it out, no task tracks it ← the provider gets the bare budget, which is exactly the "reasoning eats the completion cap and returns empty" truncation class that was 86% of wasted write spend; the revise path (`EnqueueRevision` gets pre-encoded bytes) keeps the flag, so only generate is hit
- F2 [o] P1 generate's frozen inputs are retyped field by field through six hand-written hops, two of them in `backend/cmd/api` (PostInput → StartRequest → GenerationOptions → `EncodeGenerationPayload` literal in `adapters_generation.go:350` → Decode → `GenerateJob` literal in `jobs.go:131-139` → PostInput in `generate_handler.go:36-52`); T344's two members touched 7 files, and a missed hop compiles and silently degrades the prompt ← two drops of this exact kind already exist (Memories, fixed in T352; WriteNativeEffort, F1); revise already does it right — generation encodes its own payload and cmd/api carries opaque `[]byte` (`EnqueueRevision(ctx, request, payload []byte)`), so the fix is to give generate the same shape and pin the round trip with one full-struct test; extends arch-260919 F13 (rules in the composition root)
- F3 [o] P2 `backend/internal/post` published lock: enforced by hand at ~40 sites across three layers — 28 `refusePublished(`/`lockedOrGone(` calls in `service.go`, 12 `status <> 'published'` predicates in `store/queries/{posts,images,videos}.sql`, 4 tx guards in `store/store.go`, 15 `publishedLocked` copies in `fake_test.go` — and pinned only by literal lists (`published_lock_store_test.go:92`, `lifecycle_test.go:270`) ← POST-74's own reason is "a named list of locked operations would leave every unnamed write open", yet a new `UPDATE posts` statement or exported `Service` write with no guard passes every test (T337's AssignPostField had to be added to service, SQL, fake and test table by hand); one `writablePost` helper plus a default-deny test over the `-- name:` write statements and exported Service methods (the AST-walk pattern of `cmd/api/boundary_test.go:60`, added in this range) closes it
- F4 [o] P1 `backend/internal/post/service.go:886` SaveGenerationOptions (+ `post.proto:406-407`) and `frontend/src/entities/post/api/useGenerationOptions.ts:20,37,49`: bug: one record, three unordered partial writers — the wire's `target_length` nil means CLEAR while `tag_count`/`use_memory`/`quality_rules` nil means KEEP (T337 added a fourth keep-on-nil pointer beside the odd one), so every option save must resend the length, and the three callers (`GenerationOptions.tsx:75`, `UseMemoriesField.tsx:40`, `QualityRuleChoices.tsx:85`) each build their own mutation (their isPending guards never cover each other) and resend it from two different client sources (the toggle sends `post.targetLength` via `EditorGeneratePanel.tsx:50`, a tick sends the brief mirror via `EditorDockHeader.tsx:101` ← `DraftEditor.tsx:190`); scenario: the post holds 1500, the brief saves 2000 (A), a tick in the same still-open popover sends {ticks, 1500} (B), the server applies A then B and stores 1500 — if A's refetch reads after B, useMirrored sees no change and onSaved sets the mirror to 2000, so the brief shows and StartGeneration sends 2000 while the row holds 1500, and from then on a tick writes 2000 and a toggle writes 1500 ← T353 was the caller that forgot and the STATE log's open race is this shape; every new generation option and caller walks into the same trap; fix on the wire (keep-when-absent `target_length`, e.g. a wrapper like `QualityRuleTicks`, needs POST-20 update-ssot) or route every option save through one per-post queue in `entities/post`; the BE options struct follows SaveDraft's `DraftSave`
- F5 [o] P2 `backend/internal/generation/prompts.go:378` BuildWritePromptForLanguage: 15 positional parameters, six of them `[]string` with `guidelines, memories, qualityRules, fieldPhrases` adjacent; the one production caller (`write.go:27`) passes `post.X` for every argument, and 23 test calls end in `nil, nil, nil, nil, nil` ← each new frozen input appends another slice and edits every call (T324 and T330 each touched all of them) and a swapped pair compiles into a wrong prompt; take the `PostInput` (or a prompt-input struct); same swap hazard at `template/service.go:47` Create(…, body, titleArea string, …), which T342 itself cited when it replaced guideline Create's positional pair
- F6 [o] P2 `backend/internal/generation/replacements.go` ValidateReplacements ↔ `frontend/src/entities/post/model/replacements.ts` visibleSpans: a Go/TS mirror of which spans stand (title, tag by index, TEXT without a slot, HEADING, QUOTE, first LIST item) whose only pin is the comment "a change here is a change on both sides" ← a rule change on one side makes ② mark spans the server never kept, or hide ones it kept, with every test green; the grammar and the published URL already share `testdata/*/cases.json` read by both suites, and the 22-row Go table in `replacements_test.go` is that fixture waiting to be moved; the copies already differ — TS `\s` includes U+FEFF and Go's `unicode.IsSpace` does not, so `canonicalTag('\uFEFF#카페')` is `카페` in TS and unchanged in Go — and FE `REPLACEMENT_PHRASES_MAX = 3` (`entities/post/config`) is an unpinned copy of Go's cut to 3 though its doc says it is not a mirrored limit
- F7 [o] P2 `backend/internal/quality/limits.go:12-14` MeasureVersion: whether a stored M3/M4 row is stale rests on a manual bump, and no test fails when MeasureSelf's answer changes without one (`limits_test.go:12` pins the constant `1`) ← published posts are locked (T334), so their revision never advances: a change to `text.go`/`containment.go`/`sentence.go`/`metrics.go` without a bump freezes the old numbers into the 20-post aggregate window forever; a golden digest of MeasureSelf over a fixture corpus, asserted beside the version, makes the bump impossible to forget (F12 is the first change that needs it)
- F8 [o] P3 `backend/cmd/api/adapters_quality.go:21` qualityPosts.Post + `backend/internal/quality/verdict.go:151-163` namedRun: the quality read path does avoidable work on every call — the port is backed by `post.Service.Get`, the screen read (~8 queries plus a presign per image and video, answers, active job, pending experiment, voice and template lists) though quality needs content, revision, languages and nouns; and Aggregate recomputes all 20 window overlaps in namedRun though its result is read only when M2 is OVER_BAND (measured 70 of 168 ms on a within-band account) ← this runs on every ② content revision, every GetAccountQuality and every enqueue with ticks; a failing presign, job or template read turns a quality badge into Internal; `post.Service.PostStatus` (`service.go:1084`) is the lean-read precedent
- F9 [o] P3 `backend/internal/post/service.go:1452` DeleteImage, `:1493` DeleteVideo: bug: the object is deleted before the now-guarded row DELETE — if SavePublishedURL commits during that round trip, the DELETE matches 0 rows and answers POST_PUBLISHED_LOCKED, leaving a published, locked post whose `images` row points at a gone object (Get presigns a 404) ← the doc comment's "the user can retry" no longer holds under the lock, and its other reason is stale because `sweep.go:122-136` already reclaims a stray object; delete the row first, then the object
- F10 [o] P3 `backend/internal/quality/text.go:26` edgeRune: bug: the edge trim stops at U+FE0F (Mn) and U+200D (Cf), so a trailing ❤️ ☕️ 1️⃣ stays on the token — `Contains(Tokens("I love latte☕️"), Tokens("latte"))` is false, and ExtractPhrases counts "성수 카페 추천" 2 instead of 3 beside a separate "성수 카페 추천❤️" phrase ← emoji-tailed phrases reach the frozen 분야 phrase list and emoji-tailed nouns miss containment; fixing it changes M3/M4 output, so it must ship with the MeasureVersion bump F7 is about
- F11 [o] P3 small Go/TS or cross-context mirrors pinned by nothing: `post/limits.go:20` PublishedURLMaxChars ↔ FE `entities/post/config` POST_PUBLISHED_URL_MAX_CHARS (each suite builds its limit cases from its own constant; cases.json has no maxChars) · `quality/text.go:30` slotToken regexp is a third copy of the slot grammar beside `generation/template_slots.go:166` and `template/expand.go` · `quality/limits.go:57-59` PhrasePageSize ↔ unexported `naversearch/client.go:27` pageSize, tied by a comment (batch paging stops when `len(got) < PhrasePageSize`) · `quality/rules.go:23-24` spells DistinctBlockTypesBand+1 as "세 가지 이상" · `generation/replacements.go` replacementsInstruction hard-codes "최대 20개"/"1~3개" and its test asserts the literal, not ReplacementSpansMax ← each is one edit on one side from a silent disagreement (e.g. naversearch pageSize 50 → every refresh stops after page 1); put the value in the shared fixture or derive the text from the constant
- F12 [o] P3 `backend/internal/platform/config/config.go:443` QUALITY_PHRASE_REFRESH_INTERVAL: only `> 0` is checked and quality sets no floor — `=1m` makes all 9 fields due every minute (27 Naver calls/min ≈ 38,880/day against a 25,000/day quota), then every 429 retries at min(interval, PhraseRetryDelay) indefinitely ← ARCH-42 asks for bounded owning-context validation of documented overrides, and one mistyped value burns the shared Naver application's quota for every stack using it; give quality a minimum and validate there
- F13 [o] P3 "the current revision is the finalized one" is written in Go three times — `post/service.go:982` LearningSnapshot, `:1032` SavePublishedURL, `store/store.go:338-340` (the store re-checking a domain rule) — plus posts.sql PublishPost and two fake copies ← T329 had to add `published` to the service and store gates separately; one `Post.FinalizedAtCurrentRevision()` with the store only mapping rows (ARCH-6/7)
- F14 [o] P3 `backend/internal/generation/types.go` PostInput/TemplateBrief: the domain type doubles as the experiment snapshot's hashed wire shape, and this range grew its load-bearing JSON tags 1→7 (`quality_rules`, `field_phrases`, three `json:"-"`, `TitleArea json:",omitempty"`) with mixed key casing (`Slug`/`UseMemory` vs `tag_count`/`quality_rules`) ← every new PostInput field is a silent "does this re-hash every snapshot?" decision, and TemplateBrief now has two encodings (snake_case `templatePayload` for jobs, Go names for snapshots); extends arch-260919 F20 — give the snapshot its own mapped struct
- F15 [o] P3 `backend/internal/post/store/queries/images.sql:19`, `videos.sql:18`: `post_slug IN (SELECT slug FROM posts WHERE status <> 'published')` plans as `LIST SUBQUERY → SCAN posts` over every account on each photo/video delete; the correlated `EXISTS (… WHERE posts.slug = images.post_slug AND status <> 'published')` plans as an index SEARCH ← cost grows with the whole installation, not the account
- F16 [o] P3 `backend/internal/quality/metrics.go:251,285` + migration 0077 `top_noun`: Repetition.TopNoun is computed, stored and read back, but nothing consumes it (not in proto, aggregate or any rule; QUAL-43 decided M3 names no noun) ← a dead column that later needs a migration to remove, and a value readers will assume is used
- F17 [o] P3 tests that pin less than they look: `template/corpus_test.go` titleCorpus (200 pairs) never produces `too_many_asks` (0 of 200; the cross-area ceiling rests on one hand case in cases.json, no reason-coverage assert) · `cmd/api` `TestFieldPhrasesReachTheWritePromptAndNeverTheRevisePrompt` re-implements registerJobs' KindRevise closure inline, so it tests a copy of the mapping ← a regression in the real revise mapping or in the cross-area ceiling passes every test
- F18 [o] P1 `frontend/src/shared/ui/inline-popover/InlinePopover.tsx:79-89` onPanelKeyDown: bug: any Tab anywhere in the panel returns focus to the trigger and closes it, so a keyboard user reaches only the panel's first control — ReplacementMark (`features/edit-post-content/ui/ReplacementMark.tsx:36-49`) puts up to 3 phrase buttons there, and phrases 2 and 3 can never be taken from the keyboard (Enter on a mark → phrase 1 focused → Tab → panel gone) ← breaks THEME-33 (everything keyboard reachable) and every future panel on this shared primitive inherits it; copied from Listbox, which is safe only because it moves by arrow keys; the one test fixture (`InlinePopover.test.tsx:10-31`) has a single button; hand focus back only on Tab from the last control and Shift+Tab from the first
- F19 [o] P3 `frontend/src/shared/ui/anchored-panel/useAnchoredDisclosure.ts:77-82` (InlinePopover and Toggletip): a hover-opened panel is dismissible only by moving the pointer — Escape is wired to the trigger's and panel's onKeyDown, and a hover open never moves focus into either; inside a Popover the Escape reaches the Popover's document listener and closes the whole container ← fails WCAG 1.4.13 (dismissible without moving pointer or focus) for every toggletip in the product
- F20 [o] P3 entity indexes export wire mappers — `entities/blog-field/index.ts:3` blogFieldToProto/FromProto, `entities/quality/index.ts:18-22`, `entities/post/index.ts:24` replacementSurfaceToProto — and `features/save-draft/model/useAutosave.ts:142` now builds the SavePostDraft wire field itself (`field: blogFieldToProto(fieldId as BlogFieldChoice)`, an unchecked cast from string) ← ARCH-17's point is that a proto change stops at the entity; this one now reaches a feature, and the ESLint rule (names `Proto*`/`*Service`/`*Schema`) lets `*ToProto` through; most of these exports exist only for the `src/test` fakes
- F21 [o] P3 `frontend/src/entities/guideline/api/guideline-queries.ts:50-55` toFieldIds, `entities/post/api/post-queries.ts:34`: an unknown 분야 is silently dropped from a guideline's or the preset's set and a post's unknown 분야 reads as 없음 (`?? ''`), while an unknown guideline scope throws in the same file ← if the BE ships a new 분야 before the Cloudflare build, a guideline scoped [cafe, new] reads as [cafe], and the next tick sends [cafe, pets] and erases `new` on the server; ARCH-3 says a fallback in such a mapping is a test failure, not a value (low likelihood while QUAL-23 fixes the list)
- F22 [o] P3 copies beside their own extraction: `valuesOf<M>(…) as Extract<QualityValues,{metric:M}>` and the absent-else-format `share` helper are copied word for word in `entities/quality/ui/PostMeasurementRow.tsx:38-44` and `features/choose-quality-rules/ui/QualityRuleChoices.tsx:41-47` because `QualityReading` (`entities/quality/model/types.ts:19-48`) carries `metric` twice and is not a discriminated union · `InlinePopover.tsx:13-21` and `Toggletip.tsx:14-19` build the same PLACEMENT and wire useAnchoredPanel identically, and `InlinePopover.tsx:20` repeats Popover's focusableSelector, though useAnchoredDisclosure was extracted "so the two cannot drift apart" ← a new metric or a selector fix (e.g. excluding `[inert]`) lands in two or three places
- F23 [o] P2 `frontend/src/features/generate-post/model/preconditions.ts:69,127,148`: bug: the published blocker was added as a defaulted trailing positional (`videos = [], published = false`), so a caller that is not updated fails open — `pages/ai-models/ui/ModelComparisonPage.tsx:252-259` calls comparisonGenerationPreconditions without `videos` or `published`, so on /ai-models/compare a published post leaves 비교 시작 enabled until the server refuses it (`experiment/rpc/handler.go:283`), and no comparison-page test covers a published post ← every future blocker repeats this and every caller still compiles; take one named input object with required members
- F24 [o] P2 `frontend/src/features/save-draft/model/draft-queue.ts` (884 lines): T345's 분야 channel copies the 템플릿 one line for line (22 hunks, +135) — per channel 3 Queue members, xDirty/xToSend/settleX/rejectX, four conjunction lists (:489-495, :498-502, :587-594, :707-713), a take-back block (:551-556), the locked-refusal lists (:572-579), settle (:526), discard (:880), attach init (:664-666) and a handle method; `assignField` (:783-797) is `assignTemplate` with names swapped; SendDraft grew a 6th positional arg; `attachDraftQueue`'s `fieldId?` was made optional "so the tests compile", and a caller omitting it gets a '' baseline under which a 없음 pick on a post with a 분야 resolves without sending (:788) ← ~12 hand-kept sites per channel and missing one is a silent bug (T345's mutation run started with 8 survivors exactly there); extends arch-260919 F8 inside one queue — one typed `Assignment<T>` record per channel iterated in run/queue, one parametrized suite
- F25 [o] P2 `frontend/src/pages/editor/ui/DraftEditor.tsx:96-120`: the page owns the autosave's lock policy (while locked it swaps in the server's title, memo and answers via `shownTitle`/`lockedAnswers` and runs the `wasLocked` reseed as a state change during render) because `UseAutosaveArgs.post` carries no status; the lock is then threaded as a `locked` prop through ~12 components and re-derived from `post` in 9 others (`GenerationActions:77`, `RefineDock:69`, `EditorRefinePanel:47,56,67`, `BlockEditor:87`, `FinalizeActions:55`, `useVoiceLearning:98`, `PublishedUrlField:41`) ← ARCH-14 (a page owns no state machine); a second editor on useAutosave or any change to the lock rule must copy the masking, and T345, T349 and T353 each had to add page wiring; let useAutosave take the post's status and stop queueing while locked
- F26 [o] P2 `frontend/src/pages/editor/ui/EditorPage.test.tsx`: 2,730→4,029 lines (+41 cases), 111 full-app `renderAppAt` mounts, a stale header ("mounts … 71 times", :28) and `testTimeout: 10_000` (:31); the finalized/published row literal is rebuilt per case (`finalizedAt: '2026-08-20T12:00:00Z'` ×13, `machineBaselineRevision: 1n` ×23, the Naver URL typed 10× though `test/posts.ts:230` exports FAKE_PUBLISHED_URL); :3198-3201 and :3260-3263 read quality query keys by position (`queryKey[3] === 'account'`), the coupling `test/arch-post-cache-keys` already forbids for post keys; several cases repeat feature tests ← slow, flake-prone, and a pure refactor of a query key or row shape breaks a page test; row builders, assertions through calls and the screen, per-control cases back in the feature tests
- F27 [o] P3 status splits written as `||` chains in features — `post.status === 'draft' || … 'review'` in `PublishedUrlField.tsx:43`, `ExperimentActions.tsx:66`; `'finalized' || isPublished(post)` in `FinalizeActions.tsx:55`, `useVoiceLearning.ts:98` — plus two dead lock checks (`EditorRefinePanel.tsx:67` `!isPublished` inside the non-published branch; `BlockEditor.tsx:87`, recorded by T351 as an equivalent mutant) ← a new status compiles and silently lands on one side; the entity has `isPublished` but no "finalized or later" predicate
- F28 [o] P3 `frontend/src/pages/template/ui/TemplatePage.tsx:244-245,263-264`: bug: the ask-conflict flags go stale in 원문 — they are set only by `TemplateComposition.tsx:245`'s effect, which has no unmount reset, and the mode switch (:382-386) resets nothing; a body row asking "가게 이름" while the title asks it sets bodyAskConflict, switching to 원문 unmounts the builder with the flag still true, and 저장 stays disabled with no reason shown though the source parses (renaming the label in 제목 원문 does not clear it; only going back to 블록 does) ← T340 doubled the flags and added the cross-area case; reset on unmount (`return () => onAskConflict?.(false)`) or derive the flags in the page from the draft
- F29 [o] P3 `template.Limits` is built as a hand literal in 8 places — `backend/cmd/seed/main.go:95-107` (T348) copies `cmd/api/contexts.go:227-241` field for field, including the bare `TargetLengthMin: 1` that mirrors `post/service.go:887`'s `<= 0`, and 7 cmd/api tests (T333 had to add TitleAreaMaxChars to each or NewService panics) beside the `testPostLimits()` helper precedent ← a missing field panics loudly, but a changed value drifts silently between the seed and the API; one constructor both commands and the tests call

## notes
- gates at f42a3c95: BE `gofmt -l`, `go vet`, `go build`, `go test -timeout 30m ./...` green (67 packages); FE vitest 313 files / 2,654 tests, eslint, prettier, steiger, `lint:style:probe` + `lint:style` green — run on Node 26.8.1 because the pinned 24.18.0 is not installed here; `build:web` not run
- STATE tasks is empty, so no area was excluded as owned by a doing/todo task
- triage 260924: all 29 adopted (P1, P2 and P3 each taken whole); ids are in discovery order; by priority: P1 F1 F2 F4 F18 · P2 F3 F5 F6 F7 F23 F24 F25 F26 · P3 the rest; F1+F2 are one change (the fix for F2 closes F1), F7 must land with or before F10, F4's wire half waits on POST-20
- update-ssot MEM MODEL: SnapshotWriteInput freezes 지침, rules and phrases but no 기억 (already in STATE next from T352); the snapshot records `UseMemory: true` with `Memories: null`, so an editor-origin winner lands as if the option were off
- update-ssot POST (POST-20): `target_length` has no keep-when-absent on the wire — F4's wire fix needs this decided first
- update-ssot QUAL GUIDE (QUAL-41): `guideline.ForPrompt` appends the preset text whenever the preset is on for the post's 분야, even when that 분야's phrase list is empty (a box with no Naver keys, QUAL-42), so `[작문 지침]` names a `[분야 상위 글 문구]` section the prompt lacks; T342/T344 deferred it to "R33", which exists in no SSOT
- update-ssot GEN (GEN-14): the quality-rules section sits in the stable/system half ahead of the baseline and profile, yet it varies with each post's ticks and the account's over-band state — MEM-20's reason for keeping per-post material out of the prefix applies
- update-ssot TMPL (TMPL-51): the revise prompt carries templateTitleInstruction ("JSON의 title은 … 제목 형식을 따르세요") beside "제목…고쳐 달라고 한 경우에만 바꾸세요"; for a title the owner edited away from the form the two conflict
- update-ssot GEN POST (GEN-53, POST-79): a phrase that contains its source (제주→제주도) keeps the candidate standing after a take, so the mark returns on the inserted text and offers the same phrase again (a second take gives 제주도도); the code follows GEN-53's "valid while the phrase is still present" and EditorPage.test.tsx:3452-3469 pins it, while T351's acceptance expected "the taken mark is gone" — decide which
- update-ssot THEME (THEME-30/33): T328's acceptance mandated the Tab hand-back that causes F18; a rule for panels with several controls is missing
- update-ssot POST (POST-77): says "a non-empty path", but code and the shared fixture also refuse `/` and `///` (the blog's home) — say "a path below the blog's home"
- update-ssot QUAL: an HTTP 200 with empty `items` counts as success and replaces a good 50-phrase list with `[]` for 24h (T338 acceptance says so) — consider treating an empty corpus as a failure while a list exists · M3's ≤8% band needs ~13+ evenly spread nouns and Korean prefix containment double-counts 감자/감자탕, so many posts may read OVER_BAND — check against real posts
- create-architecture ARCH-6/ARCH-3: no rule says a job's frozen input is encoded and decoded inside its owning context with the composition root carrying opaque bytes (revise and the clip jobs already do; F1, F2), nor that every member of a partial-update request keeps when absent (F4)
- create-architecture ARCH-5: the context list omits quality, naversearch, clip and memory (`quality/fields.go:5-7` says so itself)
- create-architecture ARCH-17: the ESLint rule and `src/test/arch-proto-symbols.test.ts` cover pages/widgets/features only, not `entities/*/{ui,model,lib}`, and the name pattern misses `*ToProto`/`*FromProto` (F20)
- pre-existing, outside the range: Revise reads post.TargetLength live for prompt and budget while the hold priced the start-time value · the durable generate reads the voice profile live at drain while the A/B snapshot freezes it (GEN-6; deliberate but written nowhere as one rule) · SaveDraft is separate statements (reassign, template, AssignField, answers, draft), so a publish landing mid-sequence answers LOCKED after earlier parts applied — the range makes it reachable
- hardening, below finding weight: `naversearch` http.Client forwards `X-Naver-Client-Secret` on a cross-host redirect (only Authorization/Cookie/WWW-Authenticate are stripped) — a refusing CheckRedirect closes it · quality.Service depends on the write-capable PhraseLists port (QUAL-22 read-only by convention) · `quality/store.Store.writer` unused · `generationQuality`'s language switch (`adapters_generation.go:33-41`) duplicates `qualityLanguage` · `post/store/content.go` marshalNouns/marshalQualityRules are one `[]string` codec twice · UseMemoriesField's optimistic value is never cleared though its comment says it falls back · `EditorDockHeader:57` jobRunning re-spelled at :71 and :132 · the content queue retries POST_CONTENT_INVALID indefinitely · `test/quality.ts:32` types `values` as `Record<string, number|undefined>`, so a misspelled key silently makes a band edge 0 · `quality-queries.ts:11-13` orphaned doc comment · dead nil-guards on generation collaborators NewService already requires
- checked and fine: M1–M4 bands and minimums live only in `quality/limits.go` and travel on the wire (the FE computes no verdict) · metric, verdict, 분야, guideline scope and replacement surface mirrors walk the generated enum with no default branch · the title-area grammar lockstep is pinned by shared cases.json + corpus.json both suites run, round trip included · the Naver URL rule is one shared fixture both sides read · the phrase batch fetches every page before one upsert (no tx across HTTP), keeps a durable next-refresh per field, runs one goroutine, isolates a field's failure, and its disabled mode makes zero calls · the Naver secret travels only in headers and never in an error · migration 0077 is additive with pair and json_valid CHECKs, 0078 rebuilds with FK links and `foreign_key_check` · new queries use their indexes · FSD, ARCH-13/16/17/18/20 clean in range · the Listbox migration kept its behaviour · replacements.ts index math is UTF-16 consistent · devseed holds ARCH-44 · the T352 fix is correct and tested through Generate
