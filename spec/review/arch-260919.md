# REVIEW arch-260919
> st:ready@260919 | scope:all (architecture: backend/ frontend/ agent/) | at:50574ae0 (FE re-verified at 19c19cc2) | base:ARCH@3

## summary
- Direction is sound: every gate is green at 50574ae0 (BE 43 packages, steiger, boundaries, style, 1 ESLint warning), the mechanical rules ARCH-7/-9/-13/-17/-20 hold in production code (0 sql/proto imports in domain roots, 0 raw controls outside shared/ui, 0 same-layer FE imports outside `@x`, ports consumer-declared and small at the cross-context seams); the FE import matrix confirms domain separation in practice: 0 same-layer imports outside `entities/@x`, the `@x` graph is a DAG with `post` (6 edges) and `clip-project` (2) as hubs, and every page imports only its own domain's verbs — `pages/clip` → 12 clip features, `pages/editor` → 11 post features — with `features/select-model` (3 domains + 1 widget) the only deliberately shared verb, and the two FE re-implementations of BE grammar/composition rules are pinned by shared JSON corpora
- The debt is concentration, not decay, and it is all in the clip domain: the backend has no application layer for cross-context sagas so `cmd/api` became one (F13, 6k test lines pinned to package main F22), `platform/config` owns clip rules and imports the domain (F14), `job`/`usage` know the clip product (F15), root `clip` is a 10.9k flat package (F16); on the FE the clip page (F1), clip entity (F6/F7), `clips` i18n file (F3) and the config/api barrels (F4/F5) are each one file every clip change must edit, and `clip.proto` is 34 rpcs behind a FILE-level breaking rule (F23)
- Biggest single risk: F13 — pricing, approval and terminal-commit rules live in package main behind setter-injected collaborators (F18), so a forgotten wire is a nil at runtime and the only tests are the slowest package in the tree

## findings
- F1 [o] P2 `frontend/src/pages/clip/ui/ClipPage.tsx:150-799` ExistingClip: a 650-line orchestrator that wires 11 feature/entity hooks, 6 useState, 5 useEffect, and encodes clip rules in the page (required-source rule :174, job→upload-attempt lifecycle :203, sound-batch handoff :187, conflict-retry routing :245), then passes 22 props into `features/correct-clip/ui/ClipCorrectionWorkspace.tsx:60-82` ← ARCH-14 says a page composes with no domain logic; 36 commits in 60 days (13% of FE commits) hit this one file and every clip change also edits the workspace (29), `entities/clip-project/index.ts` (37) and `clips.ts` (78) — four hot files per change, and none of the wiring is testable without the full page. Home: a `widgets/clip-workspace` (or `features/correct-clip/model/useClipWorkspace`) returning handle objects instead of 22 scalars
- F2 [o] P2 `frontend/src/pages/editor/ui/DraftEditor.tsx:92-422`: 917-line page with 13 useState, the step state machine (:190), mirrored server-sync state for target length / tag count (:215-229), caret handoff (:201) and five helper components inlined at :445-917 while `pages/editor/model/` (156 lines) already exists ← same ARCH-14 violation as F1; 33 churns in 60 days, and a brief/step rule change means editing a component nobody can unit-test
- F3 [o] P2 `frontend/src/app/providers/i18n/resources/{ko,en}/clips.ts`: 1121/1163 lines, 34 top-level groups, 78 churns each in 60 days — 29% of FE commits edit the `app` layer; namespaces are domains (`useTranslation('clips')` at 52 call sites in 21 slices) and registration is a hand-written 28-import list in `resources/index.ts:1-28` ← every feature PR edits `app/`, and two parallel sessions collide on one 1.1k-line file; ARCH-15 already allows a per-slice `config` segment (used by only 2 slices) so per-slice `{ko,en}` modules assembled by `app/providers/i18n` — or at least splitting `clips` into slice-shaped namespaces — is possible without new rules; keep `resources.test.ts` key-parity checks as the safety net
- F4 [o] P2 `frontend/src/shared/config/index.ts`: 439 lines, 87 exports of which only 14 read `import.meta.env`; the rest are domain limits (`CLIP_SOURCE_*` :82-97, `POST_TARGET_LENGTH_MIN` :304, `TEMPLATE_*` :347-379), UI tuning (`LISTBOX_*` :263-277, `POPOVER_*` :249-258, `PROMO_*` :163-182) and re-exports of an 882-line `clip-design.json`; fan-out is mostly 1 consumer; 51 churns ← ARCH-21 ("no setting is a literal in a component") has been read as "every constant goes to shared/config", so a listbox gap and a clip byte limit churn the same file; shared/config should hold env + cross-slice constants, slice-local tuning belongs in `<slice>/config` (ARCH-15) — see notes for the ARCH-21 wording
- F5 [o] P2 `frontend/src/shared/api/index.ts`: a 373-line barrel re-exporting 16 `*_pb` modules, 54 churns; proto symbols are imported directly by pages/features/widgets (`ClipService` in 13 files, `ProtoPlan` 12, `BlockType` 13, `*Schema` 4-5 each) and 50 feature/page files call `@connectrpc/connect-query` directly ← ARCH-17 holds for the import path only; a proto message rename still fans out to ~25 slices; entities should own service descriptors + mappers and `shared/api` export transport, failure and language only
- F6 [o] P2 `frontend/src/entities/clip-project`: one entity carrying four nouns — lifecycle/api (`api/clip-project.ts`, `model/types.ts`), plan editing (`model/edit-plan.ts` 656 + `model/timeline.ts` 682, reducer + validation + px layout math), preview/render (`ui/ClipDraftPreview.tsx` 601, `preview-assets.ts`, `draft-preview.ts`, `video.worker.ts`, `browser-*.ts`), observations (`model/observations.ts`, `api/attempt-inspection.ts`); 5,529 lines / 30 files, `index.ts` 192 lines exporting 116 symbols, 37 churns, 15 consumer slices ← every clip change edits this barrel and every consumer re-renders its public API; `@x` is already the house pattern (9 entities, 40 imports) so a split into `clip-project` / `clip-plan` / `clip-preview` / `clip-observation` is mechanical
- F7 [o] P2 `frontend/src/entities/clip-project/ui/ClipDraftPreview.tsx:346,390`: entity UI calls `useTransport()` and `clipPreviewRequest(...)`, 8 useState / 7 useEffect, an 8-dependency effect at :414, failure normalization at :428 — the only entity UI touching transport ← FSD entity UI is presentational; a preview cannot be rendered from a fixture without a fake service; fetching + preparation belong in a feature (or the F1 widget), the entity keeps `<PreviewVideo>` and canvas math
- F8 [o] P2 `frontend/src/features/save-draft/model/draft-queue.ts` (748) · `features/edit-clip-project/model/clip-draft-queue.ts` (232) · `features/edit-post-content/model/content-queue.ts` (238): three autosave queues re-implement one machine — module `Map` of queues, `stateOf`, `publish`, retry backoff, flush waiters, `discard*`, all importing the same `AUTOSAVE_*` constants and `SaveState` ← a backoff or conflict-policy change is three edits and three test suites; a generic `shared/lib/autosave` (keyed queue, debounce, retry, subscribe) with per-feature `merge`/`send` is covered by the existing 685-line `draft-queue.test.ts`
- F9 [o] P3 `frontend/src/pages/template/ui/TemplatePage.tsx:174-438` Editor (265 lines, 9 useState: trim policy :203, dirty diff :212, bounds :184, ask-conflict rule :231) · `pages/ai-models/ui/AIModelsPage.tsx:450-458,73-76` · six pages own transport + service calls that ARCH-14 places in entities/features: `pages/editor/model/useEditorJob.ts:2-5`, `pages/voice-validation/ui/VoiceValidationPage.tsx:27-41` (useQuery + `VoiceValidationService.method.retryVoiceProfileValidation`), `pages/voice-rule-comparison/ui/VoiceRuleComparisonPage.tsx:34-48` (decide + retry mutations), `pages/voice/ui/VoiceRunStatus.tsx:22`, `pages/google-sign-in-callback/ui/GoogleSignInCallbackPage.tsx:19-20` (`AuthService.method.signInWithGoogle` while `features/sign-in-with-google` exists), `pages/clip/ui/ClipPage.tsx` ← domain rules and RPC verbs in pages (ARCH-14); homes are `features/edit-template/model/useTemplateDraft`, `features/generate-post` / `entities/post`, `features/validate-voice-profile` (exists, 106 lines), `entities/voice/api`, `features/sign-in-with-google`
- F10 [o] P3 frontend model files without a sibling unit test: 25, led by `features/correct-clip/model/useClipCorrection.ts` (408, render test only), `features/save-draft/model/useAutosave.ts` (205), `entities/clip-template/model/composition.ts` (237); pages are 53 `.test.tsx` vs 2 `.test.ts`, and `frontend/src/test/clips.ts` is a 938-line `registerClipService` fake (35 churns, 10 importers) ← clip behaviour is reachable only through a fake transport, which is the tax F1/F7 impose; moving wiring out of pages makes the fake shrink on its own
- F11 [o] P3 `frontend/src/app/routes/router.tsx`: 621 lines, 52 `createRoute`, 22 inline `validateSearch`/`beforeLoad`, legacy voice-tab redirect logic :459-505, 31 churns ← every new screen edits one file; split by route group and let pages export their search schemas
- F12 [o] P3 verbs live in two places: entities hold 22 mutation hooks (`entities/session/api/useLogin.ts`, `entities/post/api/useDeletePost.ts`, `entities/subscription/api/useSubscribe.ts`, …) while 35 of 75 features are under 100 lines and several are a button around an entity mutation (`features/delete-post/ui/DeletePostButton.tsx` 81 lines over a 32-line hook) ← workable today, but the next author guesses; a one-line rule (entity = CRUD only, feature = user-facing action) belongs in ARCH-14 (notes); the thinnest cases are `features/{sign-up,reset-password,request-password-reset}` (11-line barrels over one `api/` hook each, no `ui`/`model`) and `entities/observation` (8 lines exporting one function, 2 consumers) — slices whose only content is the layer decision
- F31 [o] P3 cross-domain cache dependencies are hard-wired in verb features: `features/delete-template/api/useDeleteTemplate.ts:3-4` and `features/edit-template/api/useUpdateTemplate.ts:3-4` import `invalidateGuidelines` + `postDetailQueriesKey`/`listPostsQueryKey`; `delete-voice`, `rename-voice`, `restore-voice` each import `entities/post` keys; `review-model-experiment` imports `post` + `voice` + `model-catalog` keys; `edit-with-ai` invalidates `voice` (8 features, 12 cross-domain key imports) ← the fact "a post embeds its template/voice, so a template/voice write stales post lists" is a property of `entities/post` (which already reaches `template` and `voice` through `@x`), yet it is restated per verb; the next voice verb (e.g. merge-voices) must remember to invalidate posts or the list shows a stale name, and a post key-shape change edits 8 features. Fix: `entities/post` exports one `invalidatePostsDependingOn({voiceId?|templateId?})` (or `entities/voice`/`template` export `invalidateAfterWrite(queryClient)` that composes it via `@x`), and verb features call the single entry

- F13 [o] P1 `backend/cmd/api`: the composition root carries clip business rules, not wiring (ARCH-6) — `main.go` 1996 lines / 93 funcs / 38 adapter types with a single 539-line `main()` (:94-632) structured by comment paragraphs; rules in package main: `clip_credits.go:26-58` FreezeWork (the quote formula: retries=3, count×(1+retries), MaxCredits), `clip_credits.go:96-113` ReserveApproved (approval validation), `clip_finisher.go:37-118` Complete (terminal-commit saga: idempotency, staging row, one `*sql.Tx` spanning `jobstore.NewTx` + `clipstore.NewTx`), `clip_finalization.go:19-55`, `clip_transactions.go:36-52` Reserve (job state-machine guard), `main.go:1834-1872` meteredRegistry.Complete, `main.go:1666-1728` credit bootstrap, `main.go:961-985,1565-1620` JSON wire structs for cross-context handoff ← root cause: ARCH-7 forbids a context touching another's tables and there is no application/composition package, so a write that must join `clip_*`, `jobs` and `usage_*` in one transaction has nowhere to live except `main`; every clip pricing/finalization/cancellation change edits package main, is testable only from `cmd/api` (F22), and the `clipWriteTx` shape will be copied for the next cross-store commit. Home: `internal/clip/app` (or `internal/cliprun`) owning the sagas over consumer-declared tx-scoped ports from `job` and `usage` (the `usage.Service.WithStore` primitive already exists), `main()` split into loadPlatform / buildContexts / registerJobs / serve
- F14 [o] P2 `backend/internal/platform/config`: platform owns domain rules and imports the domain — `config.go:14` → `internal/llm`; `clip.go`, `clip_ai.go`, `clip_composition.go`, `clip_generation.go`, `clip_render.go` → `clip`, `clip/ai`, `clip/composition`, `clip/design`; content is business rules, not env (`clip.go:32-56` ClipMinDurationMS=15000, ClipSourceCount=20; `clip_ai.go:12-16` MaxSegments 60, PlanCompletionTokens 32768; `clip_render.go:11` FPS 30 / CRF 20; `config.go` 192 lines of voice personalization thresholds); contexts then reach up into platform for their own rules (`clip/store/store.go:157`, `clip/store/composition.go:105,113`, `clip/store/quotes.go:88`, `clip/store/source_retention.go:16,68,104` call `config.Clip*`; `post/service.go:789`, `generation/config.go:16`, `generation/revise.go:43,78` read `config.PostTag*`) ← ARCH-6 says platform has no business meaning and ARCH-21 says formulas are code; a clip limit change touches platform + store, and `clip/store` depends on a package that depends on `clip` (a subpackage-mediated near-cycle). Fix: constants move to the owning context (`clip.DefaultLimits()`, `post.TagCountRange`), platform keeps env parsing, the ctor merges env fields into the domain config
- F15 [o] P2 `backend/internal/job` and `backend/internal/usage` know the clip product: `job.Queue` has 10 clip-specific methods (`ActivateClip`, `ReserveClip`, `SweepUnactivatedClips`, `GuardClips`, `ClipJobSnapshot`, `AuthorizeClipDispatch`, …) in a 243-line `job/clip.go` plus `ClipReservation ClipCall ClipGuard ClipStore ClipCancellationStore`; `job.Store` enumerates every consumer's aggregate (`ActiveForPost/ActiveForVoice/ActiveModelExperiment`); `job` imports `llm` in 4 files; `usage/clip_pricing.go` carries `ClipCredits`, `ClipAccounting`, `ClipReservation` ← ARCH-11's generic queue and the ledger are edited instead of extended for each new long-running or charged product kind; `job` cannot be reasoned about without clip and llm
- F16 [o] P2 `backend/internal/clip` root: a flat 10,941-line package (54 non-test files, 155 exported types, 251 exported funcs) mixing pure domain (~3.5k: `render.go`, `types.go`, `composition_verify.go`, `pace.go`, `caption_*.go`), use-cases (~2.5k: `service.go`, `sources.go`, `quotes.go`, `finalization.go`, `correction*.go`) and worker orchestration (~2.5k: `generation.go`, `rerender.go`, `attempt*.go`, `recovery.go`); `GenerationService` 55 methods, `GenerationService.Run` 422 lines, `applyNativeCorrection` 208, `VerifyCompositionManifest` 203; "composition" lives in 5 homes (root 6 files/1,630 lines, `media` 10/3,594, `ai` 3/932, `store` 1/709, plus the `composition` and `design` subpackages); `clip/render.go` vs `clip/media/render.go` share no function but the name misleads ← ARCH-5's "split when the flat package is actually noisy" threshold is passed: every subpackage (`ai` 17 files, `media` 25, `rpc` 12, `store` 17) imports root, so a root edit recompiles ~50k lines and `Run` cannot be unit-tested piecewise; a domain/ · app/ split of the root (ARCH-5's own escape hatch) is the fix, F13's app package being the first tenant
- F17 [o] P2 one "Store" port per context mirrors the whole table surface: `voice.PersonalizationStore` 37 methods, `post.Store` 31, `publishing.Store` 29, `experiment.Store` 24, `usage.Store` 23, `auth.Store` 23, `voice.Store` 22, each with exactly one implementer ← the generic-repository shape ARCH-7 bans; a fake must implement 31 methods to test one use-case, and the port grows with every query because it describes tables, not the behaviour a use-case needs; split per use-case group (posts / images / uploads is already the comment grouping in `post/ports.go`)
- F18 [o] P2 setter / `With*` injection of required collaborators, 34 `Service` setters + 3 on `GenerationService`: `post.Service` 8 (`service.go:51-98`), `generation.Service` 6, `auth.Service` 5, `clip.Service.SetGeneration/SetSources/SetFinalizer`, `GenerationService.WithFinisher/WithCredits/WithAdmission`, `usage.SetAnchors` ← a dependency the boot forgot is a nil at runtime or a silently skipped branch, the constructor no longer states what a context needs, and `main()` grows one line per setter (F13); required collaborators go in the constructor, optional ones stay setters
- F19 [o] P2 `backend/internal/platform/rpcserver/failure_reasons_test.go`: a hard-coded allowlist of 212 reason strings, and its comment mandates three FE edits (`shared/api/app-failure.ts`, `errors.ts` ko/en) per new reason; reasons are untyped strings (`"UNKNOWN_FAILURE"` at 33 literal sites, `"AUTH_REQUIRED"` at 19) ← every new domain error edits a platform test plus three FE files (the 42/41/41 churn counts on those files), and a typo compiles; a typed reason (proto enum or a generated catalog consumed by both sides) collapses four edits into one — placement itself is right: all 280 `NewAppError` sites sit in `*/rpc/` with one `toConnectError` per context
- F20 [o] P3 boundary leaks: `billing/purchase.go:114` matches `usage.ErrLotTouched` across the boundary although `billing.Credits` is a consumer-owned port · `plan/rpc/handler.go:62` a plan port returns `usage.Balance` · `billing/ports.go:33` `Provider.ParseNotification(*http.Request)` puts a transport type in a domain port · `json:"` tags in context roots (`generation/generation_payload.go` 28, `generation/parse.go` 23, `voice/learning.go` 23, `voice/diff.go` 13) · `auth/provision/adduser.go:49,133,184` call `config.Load()` inside `internal` · `internal/health` imports `connect`+`gen` at root though ARCH-6 files health under platform ← each is one adapter-translation away from ARCH-7; cheap individually, and they are the pattern the next context copies
- F21 [o] P3 clock and test gaps: 19 services inject `now func() time.Time` yet `clip` root has 13 bare `time.Now()` (`service.go` ×7, `attempt.go` ×3, `generation.go:124`, `rerender.go:151`) though `GenerationService` owns a clock · `job/store` (448 lines, including `AuthorizeClipDispatch` which serializes authorization with cancellation) has 0 tests · `template/rpc` and `modelcatalog/rpc` have 0 tests ← ARCH-24 (tests beside the code) and the clock convention are applied inconsistently in the newest and most-changed context
- F22 [o] P2 `backend/cmd/api/*_test.go` ≈ 6,000 lines are unit tests of F13's logic pinned to package main: `clip_cancellation_test.go` 11 tests on `clipGuard`/`clipFinisher`, `clip_finalization_test.go` 10 on `clipFinalizer`, `bootstrap_test.go` 8 named for adapters, `clip_release_test.go` (1,131 + 737 fixture lines) a legitimate end-to-end gate pinned to main only because the adapters live there ← the slowest test package is also the one every clip change must run; moving F13 moves ~4,000 of these lines with it, so treat as the same task
- F23 [o] P2 `proto/postpilot/v1/clip.proto`: 865 lines, one service with 34 rpcs (next largest: publishing 16), 119 messages (50 of them domain shapes rather than Request/Response), 47 commits in 60 days (post.proto: 19); five rpc families share the service (templates 5, projects 5, sources 8, generation/quote/revision 6, render + browser render 6, captions/capabilities 4), mirrored by a 12-file `internal/clip/rpc` and a ~75-line clip block in the FE barrel; `buf.yaml:17-19` uses `breaking: use: [FILE]`, so moving a message into `clip_source.proto` is itself a breaking-check failure ← every clip task regenerates all three gen trees and two parallel sessions collide on one proto; a split into `clip_template` / `clip_source` / `clip_generation` / `clip_render` services requires switching the breaking rule to `PACKAGE` first
- F24 [o] P2 agent: proto types are the agent's domain types and `agent/internal/postpilot/client.go` (98 lines) is a pass-through — it returns `*postpilotv1.ClaimPublishJobResponse` and takes `postpilotv1.PublishStage`/`PublishFailureKind`, so gen is imported by 4 of 10 agent packages (`publishing/executor.go` uses 23 distinct `postpilotv1.*` symbols, `setup/server.go`, `naver/publisher.go` with `PublishManifest`, `Block`, `BlockType_*` and `proto.Clone`); the manifest→`Input` anti-corruption mapping (`naver/publisher.go:369 loadInput`, `:675 validateInput`) lives in the browser-driver package ← ARCH-3 says agent gen is consumed only through the postpilot adapter; a publishing.proto rename or a `Block` shape change edits the CDP driver, the setup UI and the job executor at once, and `naver` cannot be tested without protobuf
- F25 [o] P2 publish Stage / FailureKind is hand-kept in 6 places with no cross-side guard: proto enums · BE `publishing/types.go:37-62` string enums · BE `publishing/rpc/mapping.go` from/to maps · agent `publishing/executor.go:47-59` Stage strings + `:367` stageProto map + `:376 failureKind(string)` switch whose `default:` degrades to `PUBLISH_FAILURE_SAFE` · agent `naver/publisher.go:30-37` a second string `FailureKind` re-parsed by that switch · FE `entities/publish-job/model/types.ts:42 STAGE_RANK`; same pattern for `BlockType` (5 definitions, one pair pinned by `generation/proto_contract_test.go`), `ModelPurpose` (3 copies, none pinned) and `toProtoLanguage` re-implemented per BE context ← adding one failure kind is six edits in three languages and an agent typo silently becomes "safe"; the agent executor should map from a typed `naver.FailureKind`, and each hand-kept enum needs at least one cross-copy test
- F26 [o] P2 `agent/internal/naver` + `agent/internal/browser`: ~30 KB of SmartEditor JavaScript embedded as 15 Go backtick blobs (`naver/port.go:32` editorObservationScript 7,926 chars / 137 lines, `:182` readbackObservationScript 4,124, nine `driver*Fn` blobs in `mutate.go` composed by string concatenation at :93/:150, three in `browser/identity.go`, one in `browser/compatibility.go:39`) that no test executes — `port_test.go:360-371` is a fake CDP answering canned JSON, so tests pin which script is sent, not what it does; `compatibilityObservationScript` and `identityObservationScript` are referenced by 0 tests ← a Naver DOM change fails only live (every SmartEditor gotcha so far was measured live); the scripts belong in `.js` files under `//go:embed` with a headless-Chromium or goja test running them against a saved SmartEditor DOM fixture
- F27 [o] P3 dead code: `agent/internal/naver/survey_test.go` (2,146 lines, `//go:build survey`) describes itself as a throwaway harness "DELETED once T042 records the answers", and T042 is in `spec/tasks/done/` ← 2,146 lines of untagged-build-invisible code that still compiles against the driver API and must be kept in step or rots
- F28 [o] P3 `agent/cmd/postpilot-agent/main.go:141-178` connectionPublisher.Run: a five-step preflight classifying `EditorChanged`/`BrowserLost`/`LoginExpired`/`AccountMismatch` with its own `naverPort` interface (:80) and `publisherBoundaries` struct (:88), tested by a 520-line `main_test.go` ← behaviour in the composition root (ARCH-6's agent analogue); a second entry point must copy it; home is `internal/publishing` or `internal/naver`
- F29 [o] P3 `agent/buf.gen.yaml`: 34 `M…` mapping lines with the comment "EVERY proto NEEDS A LINE IN BOTH PLUGINS" or `go vet` fails; `agent/internal/gen` is 1.5 MB / 18 files for a module that instantiates one client ← a backend-only `foo.proto` breaks agent CI until two lines are added; generate only `publishing.proto` + its imports for the agent
- F30 [o] P3 `agent/internal/naver`: one package for four concerns — `publisher.go` 885 (flow orchestration + proto mapping + mutation planning), `port.go` 1,057 (CDP port + observation scripts), `mutate.go` 573 (JS fns + low-level driving), `probe.go` 130 (compatibility manifest); prod 2,645 lines, tests 5,534 ← the Port/CommitPort seam (`publisher.go:280/:299`) is right, so `naver` (plan) / `naver/smarteditor` (driver + scripts) is the natural cut and pairs with F24 and F26
## notes
- Triage 260919 (owner, level mid): rule = "anything that accrues with each postponed change is cleared now"; every finding carries a per-change cost clause by construction, so all 31 are [o]. Only F27 (dead `survey_test.go`) is static rather than accruing; adopted anyway as a one-commit deletion. Sequencing for create-task: ① ARCH amendments first (ARCH-6 cross-store saga home, ARCH-21 wording, ARCH-14 verb rule, buf breaking FILE→PACKAGE) since F13/F14/F4/F12/F23 depend on them; ② F13+F22 as one task; F1+F7+F10 as one clip-workspace task; F6 split before F5; F28 bundled with F30; ③ F24-F30 (agent) land only after T008 closes
- Criteria used beside ARCH: FSD official layer reference (pages may be large but "not common for a page to have a dedicated data model"; entity UI need not be a complete block; "not everything needs to be a feature"), threedots.tech clean architecture for Go (application layer = thin glue, interfaces kept where used, main = dependency injection), and general clean-code review criteria (SRP, DRY after the second real use, dependency direction, change isolation, tests as the safety net)
- ARCH gap (create-architecture): ARCH-5..7 have no home for a use-case that must commit across two contexts' tables in one transaction; F13 exists because the only legal place was `cmd/api`. Proposal: an ARCH-6 clause naming `<context>/app` (or `internal/<flow>`) as the composition tenant that owns cross-store sagas over consumer-declared tx-scoped ports
- ARCH gap (create-architecture): ARCH-21 "no setting is a literal in a component" is read as "every constant goes to shared/config / platform/config" (F4, F14). Proposal: shared/config and platform/config hold env-derived values only; product limits and formulas live in the owning entity / context, slice-local tuning in `<slice>/config` (ARCH-15 already allows it)
- ARCH gap (create-architecture): ARCH-14 does not say whether a mutation hook is an entity `api` (CRUD) or a feature (F12); pick one and write it down
- ARCH gap (create-architecture): `buf.yaml` breaking rule FILE vs PACKAGE is undocumented; F23 needs PACKAGE before any proto file split
- Not findings: `clip/render.go` vs `clip/media/render.go` share no code (naming collision only) · the FE grammar/composition duplicates are pinned by `backend/internal/template/testdata/grammar/cases.json`, the composition `corpus.json` and `design_test.go:215-220` · `post/service.go` (39 methods, longest 89 lines) and `auth/service.go` (36, longest 57) are large but cohesive
- Memory correction: the note "Naver Port never implemented" is stale — `naver.CDPPort` (`agent/internal/naver/port.go:312`, `var _ CommitPort` at :852) is a full driver wired at `cmd/postpilot-agent/main.go:100`
- Out of scope, owned by T008 (doing): live Naver publish end-to-end; F24-F30 touch `agent/` structure only and do not change publish behaviour, so they can land after T008 closes
- Out of scope, owned by T177 (blocked): release QA viewing checklist
- FE re-verification at 19c19cc2 (the nav commit touched 26 FE files, none of the F1-F12 anchors): steiger clean, ESLint 1 warning (`DraftEditor.tsx:140`); transport usage by layer — `@connectrpc/connect-query` imported in entities 55 / features 44 / pages 6 / widgets 0 files, `useTransport` in entities 38 / features 32 / pages 6; proto `*Service`/`*Schema` symbols named in 33 page/feature/widget files (`ClipService` 35 sites, `ModelRefSchema` 23, `BlockSchema` 23, `PostContentSchema` 21, `AuthService` 19); i18n `clips` namespace is read by 18 slices, `posts` by 12, `common` by 12; only 2 slices use a `config` segment
- Gates: ARCH-26 full pass; ARCH-25 lint (1 warning `pages/editor/ui/DraftEditor.tsx:140` react-hooks/exhaustive-deps), format, lint:fsd, lint:style pass; `pnpm test` and `build:web` were not re-run in this pass (green at the last task, T257); ARCH-27 and ARCH-28 not run
- Untracked working-tree edits (list-controls / navigation / i18n nav) belong to another session and were not reviewed
