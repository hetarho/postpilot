# IDEATION post-quality-and-related-links
> st:open@260918 | Measure what code can count about a blog's own recent posts and offer the owner a rule against it, author the title in the template that already owns the body, shape a post to be citable by Naver's generative answer, and let an already-published Naver URL come back as a 추천글 position at the foot of a later post

## vision
- [o] Problem A: the product owns how a post sounds (VOICE), what shape it has (TMPL) and what it must avoid (GUIDE), but nothing measures what the account's posts look like next to each other, which is where an outside scorer and Naver's own spam rules both look.
- [o] Problem B: a verified `blog.naver.com` URL is already frozen and retained (PUB-15, PUB-25) and then never read again, so the account's own published back catalogue cannot appear at the foot of a later post.
- [o] Problem C: the title and the tags are the two fields of a generated post that no rule reaches — `NaturalnessBaseline` excludes 제목 in its own first line (GEN-17) and never mentions tags, no voice section describes either, and a template decides body shape only; `tag_count` (POST-63) fixes how many tags there are and nothing about what they are — so every account inherits the model's defaults for both.
- [o] Problem D: readers increasingly arrive through a generated answer rather than a ranked link (네이버 AI 브리핑 live 2025-03-27), and nothing in the product is shaped for being cited rather than ranked.
- [o] Target: the owner publishing to Naver repeatedly, whose blog is judged on behaviour across posts rather than on one post's prose.
- [o] Core value: the product measures what it can count and hands the owner one switch per finding, so blog-level quality stops being luck without a rule ever being applied behind the owner's back.

## explored
- [o] measurement and rule are two separate things: code counts the metric deterministically and what reaches the model is prompt text ← a model cannot count the keyword ratio of prose it has not written, so a prompt-only rule would be a wish rather than a constraint; the measurement is also what tells the product when to speak at all
- [o] rule text is a code-owned constant that an owner's 지침 overrides ← owner 260923; the default is ours to write and improve for every account at once (the GEN-16 and GEN-17 precedent), and GUIDE's existing precedence already makes a 지침 beat a conflicting instruction, so no new concept is introduced
- [x] owner-editable rule rows ← the most flexible, but broken wording could not be fixed centrally and [작문 지침] already occupies that role
- [o] the rule is offered, never imposed: a metric over its band puts a checkbox in ①'s dock whose tooltip states what the metric is, why it appeared and what ticking it changes; only a ticked box adds text to the prompt ← the owner keeps the decision, and a clear box leaves the prompt byte-identical to today's, the property GUIDE-4 and GUIDE-18 have protected since r1
- [o] the measurement is taken over the account's recently published posts, not over the draft ← the checkbox stands in ①'s dock before any content exists, so what it can report is the blog's recent behaviour, which is also the number the owner already compares against the outside site
- [o] v1 measures four things: 제목 도배율, fixed-phrase repetition across posts, per-post morpheme repetition with title-body relevance, and the 분량·구성 set (character count, photo count, structural variety, sentence length)
- [o] every metric is a pass/warn badge carrying where its band came from, never a weighted score ← no published weighting exists for any of them, and a composite number would invent precision the sources do not support
- [x] measuring the finished draft in ② and re-running a correction pass when a metric is over ← it buys accuracy for one more model call and one more credit charge per generation, and cannot promise the second pass lands inside the band either
- [x] the owner typing the analysis site's number in by hand ← it never disagrees with the scorer, but it makes the owner the measuring instrument and goes stale silently
- [o] 제목 도배율 is not a Naver metric but 블라이's own screen label with an undisclosed formula; the denominator is confirmed as the account's last 100 titles and the numerator is not ← the product defines and names its own number on screen rather than claiming parity with a formula nobody publishes
- [o] the numerator is the share of those 100 titles containing the account's single most frequent noun ← owner 260923; deterministic, explainable in one screen line, and it is the same computation the per-account title phrase ban reads, so one measurement serves both
- [x] a dictionary of commercial keywords as the numerator ← closer to the intent, but the dictionary would be the product's judgement and would date, the reason the fixed title-phrase list was rejected
- [o] the product sets the band values itself and every screen carrying them states they are the product's own ← owner 260923; without a band nothing decides when a checkbox appears, and an unlabelled number would be the borrowed authority this doc rejects everywhere else
- [o] 블라이's observed bands (14% and 18% 양호, 26% 주의) do not carry over to this numerator ← they were produced by an undisclosed formula, so reusing their thresholds against a different numerator would borrow a boundary from a measurement that is not ours; the bands are the product's own and the screen says so
- [o] the single quantitative rule Naver itself publishes is that a keyword repeated twice or more in a title risks a penalty (서치어드바이저 콘텐츠 마크업); everything else Naver states about quality is qualitative ← any band beyond that one is the product's own and is labelled as such
- [o] 유사 문서 is deduplication, not a penalty: Naver's Q&A states that publishing the same text twice does not affect ranking and that the earlier one is treated as the original, with 원본 반영 요청 as the remedy ← no screen may say "유사 문서에 걸리면 저품질"; the real exposure is the separate 도배성 · 대량 생성 spam rule
- [x] keyword density as a percentage, a recommended title length, a target character count, alt-text and heading-tag checks, and any "blog index" prediction from views or dwell time ← no primary source for any of them: Naver states titles have no length limit, Google states length alone is not a ranking factor, Naver Blog's editor emits no h1-h6 and `alt=""` with no input UI, and Naver explicitly denies that reader-side numbers the author cannot control move ranking
- [x] jitter on publish times ← Naver states a perfectly regular publishing interval can read as abuse, but PUB-31 rules out scheduling entirely, so there is nothing to jitter
- [x] stating in every post that it was written with AI ← Naver recommends it and it costs nothing, but it would repeat one identical sentence across every post, feeding the very metric this work adds; an owner who wants it writes it as a 고정 문구
- [o] the recurring cliché title ("솔직 후기") is the absence of a rule, not a model fault: the write pass returns `title` as one field of one JSON object with no constraint on it anywhere in the prompt ← no amount of voice tuning reaches it, because every voice section measures body prose
- [o] the title is authored inside the post template, in the template grammar already parsed, rather than as a separate entity ← one template is then the complete design of one post, and `<write>`, 고정 문구 and `<ask>` (→TMPL-18 →TMPL-43) already express every title form asked for, so the builder, the enqueue freeze and the revise path carry it with no new picker and no new freeze
- [x] a separate 제목 템플릿 entity chosen beside the post template ← it buys title-only reuse at the cost of a second entity, a second picker and a second freeze, and the free pairing it enables is not something the owner asked for
- [x] title rules as a code-owned constant alone with no authoring ← it fixes the cliché for every account with zero setup but cannot express an account's own title form, which is the half the owner asked for; the two constants below ship anyway, underneath the authored form
- [o] exactly two title prohibitions have a primary source and both ship as code-owned text: Google Search Central names boilerplate titles — long text varying by a single piece of information — as bad practice, and Naver 서치어드바이저 warns that a keyword repeated twice or more in a title risks a penalty ← everything else said about titles (ideal length, keyword position, bracket forms, number-first openings) has no primary source and stays out, exactly as the rejected metrics above did
- [x] a named list of Korean stock title phrases banned unconditionally ← a phrase is not bad in itself: 솔직 후기 over an account's first honest review is the right title, and a blanket ban deletes a correct choice; what hurts is repetition, and repetition is measurable
- [o] the prohibition names the phrase: "최근 제목에 '솔직 후기'가 자주 나옵니다. 이번 제목에는 쓰지 마세요" rather than an abstract instruction to avoid overlap ← owner 260923; the model acts on a named string reliably and on "겹치는 표현" barely at all, and the name is a measurement rather than the product's judgement
- [o] a phrase is prohibited only where it is measured to be not helping, meaning it already stands in the account's own recent titles ← owner 260923 "실제로 도움이 안 될 때만": the first use earns its place, the fourth is boilerplate, and 제목 도배율's denominator (the last 100 titles) is already the set that decides which
- [o] this settles the cold start too: an account with nothing published bans nothing and its first titles are free ← a new blog has no repetition to avoid, so a rule there would be the product's taste rather than a measurement
- [o] the template's title area is optional, and a post with no template or a title-less template receives the sourced prohibitions alone with the form left to the model ← no new entity and no migration of stored templates; a title form is something an owner opts into, not a field every template must answer
- [x] a required title area on every template ← it guarantees a deliberate title everywhere at the cost of rewriting every template already saved
- [x] an account-level default title form for template-less posts ← the most consistent output, but it adds a second place a title is decided and a precedence rule between the two, for a gain the prohibitions already cover
- [x] a body word-choice rule written into the write prompt ← it collides head-on with the voice profile, which already owns 자주 쓰는 접속어와 부사, 말버릇 and 절대 사용하지 않는 표현, and GEN-17 makes the profile authoritative on conflict; a rule that loses every conflict is not a rule
- [x] the word recommendation living in the 말투 tab ← owner 260923; a voice is nuance and 종결어미 — "내가 그랬어요" against "제가 그랬습니다" — not vocabulary, and hanging word lists off it would blur what a voice means
- [o] word choice is delegated to 템플릿 and 지침, and the product ships it as preset 지침 the owner adopts ← owner 260923; 지침 is already the per-post opt-in channel with settled precedence over the template, so a preset introduces no new concept and reaches the prompt through `[작문 지침]`
- [o] the vocabulary is per 분야 ← a word that earns its place in a 맛집 post is not the word a 뷰티 post needs, and one global list would be the product's taste again
- [o] v1 covers only the 분야 a photo-driven review blog actually uses — 맛집 · 카페 · 국내여행 · 패션·미용 · 상품리뷰 · 육아·결혼 · 반려동물 · 인테리어·DIY · 일상·생각 ← owner 260923; the daily batch stays small, and a field is added when an account needs one
- [x] a preset for all 32 주제 ← every account would find its field, but the batch would collect vocabulary daily for fields this product's users never write in
- [o] a post with no 분야 receives the global 지침 only, exactly as GUIDE-14 already treats a post with no template ← owner 260923; no new rule, and an unchosen 분야 imposes nothing
- [o] `guidelinePrecedence` narrows from 문체·종결어미·어휘 to 문체·종결어미 ← owner 260923; as written, the sentence shipping beside every 지침 hands 어휘 to the voice profile and would overrule a vocabulary preset on arrival, and narrowing it also matches the voice's own definition as nuance and 종결어미 rather than word lists
- [o] the tag rule text rides in the same 분야 preset as the body vocabulary ← owner 260923; one channel stays one channel, and the right tag for a 맛집 post is not the right tag for a 뷰티 post, the same reason the vocabulary is per field
- [o] the 분야 list is defined fresh with Naver Blog's own 주제 categories as the reference ← owner 260923; it is the classification the account's readers and Naver itself already use
- [o] Naver's four are GROUPS, not categories: 엔터테인먼트·예술 · 생활·노하우·쇼핑 · 취미·여가·여행 · 지식·동향 hold 32 categories between them, and 육아·결혼 is one of them alongside 반려동물, 요리·레시피, 패션·미용, 인테리어·DIY, 상품리뷰, 원예·재배 and 일상·생각 ← recorded because the four was read as the whole list
- [o] 분야 is a first-class per-post attribute chosen at write time, not a property of the account or of the template ← owner 260923; a blog covering both 맛집 and 여행 needs the right vocabulary per post, and ①'s dock already owns everything else a run is given (→GEN-25)
- [o] a 지침 gains a 분야 scope beside the two it has: `scope` becomes `global` | `templates` | `fields`, and an owner's own 지침 may carry it too, not only a preset ← owner 260923; GUIDE-5 and GUIDE-14 already model a scope as a kind plus a link set, so a third kind reuses GUIDE-14's resolution instead of adding a parallel mechanism
- [o] a preset is refreshed by the product and never editable by the owner ← owner 260923; an edit would fork it away from the refresh, and a preset drifting silently from what the product ships is worse than one the owner cannot touch
- [o] the preset is ONE fixed product-owned 지침 — 상위 노출 단어 사용 — carrying a multi-select 적용할 분야, not one preset per 분야 ← owner 260923; its text never changes, and the words are material resolved at enqueue from the post's 분야 rather than part of the guideline
- [o] that shape dissolves GUIDE-18 rather than reversing it: no model and no batch writes guideline text, the batch maintains per-분야 word lists as data, and the data reaches a run exactly as a template's frozen facts and the memory bank's texts already do (→GUIDE-15 →MEM-19) ← a switch the owner turns on is not a learned rule, which is what GUIDE-18 exists to forbid
- [o] GUIDE-19's exclusion of seeded guidelines is unaffected: nothing is seeded into the account, the preset is one row the product owns and the owner switches on ← an account that never touches it keeps zero guidelines and a byte-identical prompt, the property GUIDE-4 protects
- [o] the preset's 적용할 분야 selector IS the `fields` scope kind, so the preset and an owner's own 분야-scoped 지침 resolve through one mechanism ← two selectors meaning the same thing would be the parallel mechanism GUIDE-5's scope model exists to avoid
- [x] reusing CDS-50's five categories (음식점 · 카페 · 숙소·여행 · 뷰티 · 생활용품·가전) ← they were a video template's design preset fixing hook tone, caption style mix, chip order, CTA, cut rhythm and accent colour, not a subject classification, and CDS retired them outright
- [o] CDS-50's retirement is the precedent this work must stay clear of: a category preset was retired in this codebase once already because it carried implicit content ← the difference has to be that a 분야 preset is 지침 text the owner reads and adopts, never a default that decides content behind them; a preset that applies itself is CDS-50 again
- [o] a preset's vocabulary is the vocabulary of the posts Naver itself ranks for that 분야 ← it is the only source that answers "which word is the better one" with evidence from outside the product rather than with its taste
- [o] that vocabulary is collected by a product-side batch running about once a day, never by a live call per owner ← owner 260923; no per-owner key, the 25,000/day quota stays entirely inside the product's control, and a field's vocabulary distribution does not move between one day and the next
- [o] that corpus is the official 네이버 검색 API (`/v1/search/blog.json`, `sort=sim`) and nothing else ← Naver forbids crawling blog.naver.com in robots.txt and its terms and names the Open API as the sanctioned route; a feature that needs a scraper is a feature that breaks the day Naver enforces
- [o] the corpus is therefore titles plus `description` excerpts, never full post bodies: the API returns title, link, description, bloggername, bloggerlink and postdate, `display` ≤ 100 per call, `start` ≤ 1000 ← the comparison runs over the vocabulary a searcher actually sees in the result list, a narrower but honest sample, and no screen may imply the product read the whole post
- [x] full-text collection of top-ranked posts ← the only way to compare body vocabulary properly, and exactly what Naver's robots.txt and terms forbid
- [o] a preset states what is observed about its words and never what they improve ← "같은 주제 상위 글 다수가 이 말을 씁니다" is a measurement; "품질이 올라갑니다" is a claim with no source, and this doc has already cut every other unsourced quality statement (유사 문서 저품질, blog index prediction, GEO's +41%)
- [o] the source-bound word-choice rule governs tags as well as the title ← owner 260923; a tag IS a keyword choice with no prose around it, so picking between labels that are all already true of the post is the purest form of "동일한 뜻 중에 고른다" and the one surface where a choice cannot read as stuffing; the body reaches the same end through an adopted 지침 preset rather than through a rule of ours
- [o] tags are as unruled as the title: `tag_count` (POST-63) fixes how many and GEN-14 requires the frozen count and the target language, and nothing anywhere says what a tag should be ← the same absence that produced the cliché title produces whatever tag the model reaches for first
- [o] choosing among true labels is categorically different from keyword stuffing and the measured -9% does not apply to it: stuffing repeats a keyword into prose that did not need it, this picks one of several words already true of the post ← it is also why such a choice may read search-demand data without the product ever asserting a fact
- [o] tags are governed by prompt rule text alone, with no search-demand data read anywhere ← owner 260923; it closes the real gap (nothing says what a tag should be) at zero integration cost, while an API route would make every owner connect a 검색광고 account for a gain the product cannot yet demonstrate
- [x] reading the Naver 검색광고 API for candidate tag volumes in v1 ← it is the only version that actually knows demand, and it stays the shape to revisit, but it buys a per-owner integration before the plain rule has been shown to fall short
- [?] whether the tag rule is a rule the owner ticks or is always on ← POST-63 already states a tag list has no "natural" value worth preserving the way prose length has, which argues for always-on, but every other rule in this doc is offered rather than applied
- [x] GEO rules in v1 ← the product cannot observe an AI 브리핑 citation, so every rule would ship unmeasured, which is the exact position that got "blog index prediction" rejected above; the evidence below is kept because it already decides what may never ship, and the work returns when a citation becomes observable
- [o] GEO is parked rather than dropped, for one measured reason: the gain goes to the documents that are losing — the founding study (Aggarwal et al., KDD '24) measured +115.1% visibility for a rank-5 source and -30.3% for a rank-1 source under the same edit ← this product's user is the small blog that effect favours, so the work is worth returning to
- [x] 네이버 AI 브리핑 as a v1 target, though it stays the right first engine when the work returns ← the product publishes to Naver Blog, a measured 49.3% of AI 브리핑 citations came from documents outside the integrated-search top 10, and blog sources dominated its citation mix; Google AI Overviews and ChatGPT would double what has to be measured for a surface this product does not publish to, and none of the three can be read back today
- [o] when GEO returns, a tactic is adopted only where the author's own material can satisfy it ← the two strongest measured tactics add facts (quotation +41%, statistics +33% position-adjusted word count) and GEN-16 forbids any concrete fact the memo, the photo observations and the template fields do not carry; the template's data fields (→TMPL-43) are already the channel through which the author's real numbers reach a post, so "add statistics" is implemented as "the template asks the author for the number"
- [x] an opt-in that lets the model add statistics, quotations or citations of its own ← it is the highest-measured tactic, and the survey reviewing it states factuality is not guaranteed; a product whose entire grounding design exists to stop invented prices and invented conversations cannot ship a switch that re-licenses them
- [o] no rule this work adds may introduce a word for something the source does not contain; a rule may only choose between words that mean the same thing about what is already there, and only where that word fits naturally ← owner constraint 260922, and it is the line between a vocabulary rule and a fabrication: writing 대출 where the memo says 돈을 빌렸다 is a synonym choice, writing 대출 where the memo has no loan is a lie
- [x] keyword stuffing, in title or body ← the one tactic measured as negative in the founding study (-9% position-adjusted word count), and the 2026 survey's verdict is that it does not transfer from conventional SEO at all
- [o] the founding study's headline numbers are conditional and no screen may repeat them as a promise: they are relative gains inside a simulator where five documents are already in the context window, they establish neither organic discoverability nor traffic, and across 54 method-domain combinations in C-SEO Bench only three were significantly positive ← the standard that kept 유사 문서 and 블로그 지수 claims off the screen applies to every GEO number too
- [o] the two factors the 2026 survey calls robust are query-document relevance and position within the retrieved context, neither of which a writing rule controls; a 252,000-trial factorial study found moving a source higher in context beats most rewrites ← whatever ships here is a second-order gain on top of the post being about what the reader asked, so no screen may present a GEO checkbox as the thing that decides exposure
- [o] no writing rule this work ships may cost the plain search path, GEO or not: body-only optimization was measured reducing top-20 retrieval presence by ~9% and post-rerank top-10 by ~16% ← a rule that wins a citation and loses the ranking is a net loss for an account whose traffic is overwhelmingly ranked search, and it is why a title rule is a prohibition rather than an instruction to pack a title
- [?] parked with the GEO work, the tactics that survive the grounding constraint and are therefore rule candidates when it returns: an answer-first opening sentence, a question-shaped 소제목, the author's own supplied figures stated as figures rather than prose, and naming the source of a fact the author supplied ← each still has to be written as rule text, and none may license a new fact
- [?] 고단가 키워드 stays under review and out of v1 ← what the blog-analysis sites label 키워드 단가 is the Naver 파워링크 bid for that keyword, i.e. advertiser demand, and their grades (S~F, 레벨1~3) are each tool's own undisclosed cut-offs, the same category as 블라이's 제목 도배율; it also has no path to this owner's revenue, because a Naver Blog earns through 애드포스트, which publishes no per-keyword rate
- [o] if high-value keywords ever enter the product they enter at topic and title selection, before any prose exists, and never as a word to insert into a written sentence ← inserting them is keyword stuffing by definition, the one measured-negative tactic, and it would manufacture exactly the title repetition Naver penalises
- [o] if keyword data ever enters the product, tags are its first and most defensible surface ← a tag is selected rather than written, so reading 검색량 to choose between 강남맛집 and 강남식당 changes no claim the post makes, while the same data applied to a sentence is the stuffing this doc rejects
- [o] the one official Korean per-keyword source is the Naver 검색광고 API `/keywordstool` (RelKwdStat), returning monthly search volume, click counts, click-through rate and a competition band ← every third-party 단가 screen is derived from this or from Google Ads, so the product would read the source rather than a scraper
- [x] 추천글 in v1 ← deferred by the owner 260923; it is the only block of this work needing a block type GEN-1 does not have and an unverified PUB-22 publish, and nothing else in v1 depends on it
- [o] when it returns, the pool is links the owner explicitly saved off a finished post, and those saved links are what gets recommended ← owner 260923; an explicit save states that this post is worth sending a reader to, which publish history alone never states
- [o] the 추천글 pool draws on the account's own verified `blog.naver.com` URLs already kept by PUB-15 and PUB-25, never postpilot URLs ← half of "keep the published link" is already built; what is missing is promoting it from terminal history to material a later run reads
- [o] the count belongs to the position, exactly as it does for a photo (TMPL-38): `count` links stand where the position stands, default 1 ← "1 by default, n when n is asked for" resolves to the attribute the grammar already has, so the template stays the single place a post's shape is decided
- [o] links are selected by shared tags first ← the position exists to send a reader to a related post, and recency alone would staple the same few posts to the foot of everything
- [o] a link already used in the account's last N posts is excluded from the candidate set ← Naver names 기-승-전-링크 as its representative link-spam case, and the feature would otherwise generate exactly that pattern automatically, without the owner ever seeing it happen
- [o] a link renders as the post's title followed by its URL ← Naver asks that a link make clear where it leads, and a titled link is also less likely than a bare address to be auto-expanded into the card that would break PUB-22's fence
- [o] a position matching no eligible post is removed entirely rather than filled with unrelated recent posts ← an unrelated link at the foot of a post is worse than no link
- [o] the model never sees a 추천글 URL, nor a copy token standing for one: code fills the position after the content is validated, at the place the template holds ← the database already owns the verified URL and routing it through a model buys nothing while risking a dead link from one altered character; it is also what keeps this position out of TMPL-37's retired category, since nothing is left for a human or a model to fill
- [x] the model picking which links go in, or writing a per-post introduction for them ← TMPL-16 and GUIDE-18 have held "no model writes, suggests or ranks anything about a template" since r1; an introductory line is authored once as a 고정 문구 above the position (TMPL-18), the construct that already exists for exactly this
- [x] the owner hand-picking links per post ← that is exactly the reserved position TMPL-37 retired, a placeholder to hunt for after pasting; automatic filling is what keeps this position out of that category
- [x] asking for links through the memo or a new option in ①'s dock ← the position already states both where and how many, and a memo phrase would need the model to act on a position it deliberately never sees
- [x] claiming anywhere that 추천글 links raise C-Rank ← the official C-Rank post ties Chain to how other sources consume the content, never to an account's own internal links

## shape
- flow (quality): publish → measure that post's metrics (code, no provider call) → the next run's dock reads the account's recent measurements → a metric outside its band renders its checkbox and tooltip → ticked boxes freeze into the payload at enqueue → the write prompt carries their rule text
- flow (word choice): one source-bound constraint reaching three surfaces by two routes — title and tags through prompt rule text, body through a per-분야 지침 preset the owner adopts — saying a word may be chosen between synonyms for what the source already contains and may never name something the source does not
- flow (presets): a daily product-side batch queries the 네이버 검색 API per 분야 → the returned titles and excerpts give that field's word list, held as data → the owner switches on the 상위 노출 단어 사용 preset and ticks 적용할 분야 → a post whose 분야 matches freezes that field's current words at enqueue → they ride into the write prompt inside the preset's fixed 작문 지침 sentence
- flow (title): the post template carries an optional title area authored in the template grammar → it freezes at enqueue with the body → the write prompt states the title form as its own instruction → the two sourced prohibitions ship as code-owned text beside the grounding constants, under the authored form
- flow (추천글): publish → verified Naver URL retained (already true) → a later post whose template holds a 추천글 position is generated normally, the model unaware of the position → after validation, code resolves `count` links, tag-matched and excluding those used in the last N posts, and inserts each as its title plus URL, or removes the position when nothing is eligible → export and publish carry them
- v1: the four metrics with their bands and their pass/warn badges, the offered checkbox and tooltip in ①'s dock, the ticked rule text frozen at enqueue; the optional title area in the post template, the two code-owned title prohibitions and the measured per-account phrase ban; the tag rule text; the per-분야 vocabulary presets in 지침 and the daily batch that fills them
- not: 추천글 and its saved-link pool, GEO rules of any kind, a body word rule in the write prompt, any search-demand data, automatic rewriting when a metric is over, a composite quality score, model-authored statistics or quotations, high-CPC vocabulary anywhere, Google AI Overviews or ChatGPT as targets, any screen stating an expected exposure gain, a separate 제목 템플릿 entity, any model involvement in link selection or link prose, links to anything other than this account's own published posts, a memo or dock route to 추천글, an AI-authorship line in every post

## domains
- [?] QUAL (new): the four metrics, how each is computed, its band and its source label, the per-post measurement record, and the rule text each offers when ticked
- [?] GEN: where a ticked rule sits in the write prompt and how it freezes at enqueue, beside the template brief and the guideline texts; the two code-owned title prohibitions and the source-bound word-choice rule for title, tags and body, and where they sit relative to `NaturalnessBaseline`, which excludes titles and never mentions tags
- [?] POST: the checkbox row in ①'s dock and where a post's own measurements are shown
- [?] GUIDE: the per-분야 vocabulary presets, how an owner adopts one, and how a preset is refreshed from the daily batch
- [?] QUAL (or GUIDE): the daily product-side batch over the 네이버 검색 API that produces each 분야's vocabulary
- [?] TMPL: an optional title area on the template, authored in the existing grammar, frozen and rendered as its own prompt instruction
- [x] TMPL (deferred with 추천글): a seventh construct, `<slot kind="related" count="n"/>`, authored on the builder like a photo position and removed at fill time when nothing is eligible
- [x] PUB (deferred with 추천글): the verified Naver URL promoted from terminal history to a readable pool, queried by tag and by recent use

## open
- [?] N, the number of recent posts a link is excluded for, and whether it is counted in posts or in days
- [?] what the dock shows before the account has published enough posts to measure: no checkbox at all, or a stated minimum
- [?] what a position does when the eligible pool is smaller than `count`: render fewer links, or remove the position as the empty case does
- [?] whether a 고정 문구 authored above a 추천글 position disappears with the position when it resolves to nothing: the grammar relates no two sibling blocks, so today the line would stay and announce links that are not there
- [?] the block a link becomes: GEN-1's flat Block types have no LINK, and the four exports (EXPORT) plus the reading view each have to render it
- [?] whether revision may rewrite a 추천글 block: GEN-41 filters only IMAGE and VIDEO against a fresh snapshot, so a revise pass would currently be free to reword or drop a link
- [?] a link whose Naver post the owner later deleted on Naver: the product cannot observe a deletion it did not perform, so the pool would keep offering a dead URL
- [?] same-image reuse across posts, which Naver's Q&A names directly ("동일하게 사용된 사진의 비중이 높으면 유사문서가 될 수 있습니다"): low exposure here because each post carries its own freshly attached photos, but it is the one thing a photo-driven product should know it is not measuring

- [?] the exact 주제 names, which have to come from Naver's own 주제 picker rather than be reconstructed from secondary sources
- [?] whether title-and-excerpt vocabulary is a large enough sample to recommend from, and what the screen says when it is not
- [?] whether the saved-link pool replaces PUB-15/25's automatic retention or sits on top of it
- [?] whether a tag rule interacts with POST-65, whose list search matches tags and names the matched ones on the row
- [?] whether the title rules apply to an English target too, or are Korean-only like `NaturalnessBaseline` (→LANG)
- [?] whether 확정 rewriting `posts.title` interacts with an authored title form, since the title a template produces is then edited by hand downstream
- [?] whether an AI 브리핑 citation can be observed at all, which is the single condition for the parked GEO work returning: today no screen, feed or API is known to report that a given post was cited
- [?] whether `/keywordstool` returns a cost field or only volume, clicks, CTR and a competition band — the 단가 screens may be deriving it from the bid-estimate endpoint instead
- [?] RISK, unverified: PUB-22's fence requires the exact semantic body sequence with no inserted text or image, and PUB-36 inserts text at a caret; if Naver's editor turns a pasted address into a link card, the fence observes a block it never froze and fails the publish closed — a titled link reduces but does not remove this, so it has to be settled by an actual publish before v1 ships rather than reasoned about
