{
  "rules": [
    {
      "allowed": {
        "origins": ["https://postpilot.<domain>"],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["content-type", "if-none-match"]
      },
      "exposeHeaders": ["etag"],
      "maxAgeSeconds": 3600
    }
  ]
}
